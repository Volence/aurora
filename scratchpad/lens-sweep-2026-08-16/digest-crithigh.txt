
########## ART-A1 [critical] Block tier lacks the three-state availability: matched reclaimed block ids are overwritten by the same plan
file: src/core/art/classic-commit-plan.ts:373  confidence: certain
CLAIM: internBlock's pool-reuse scan matches block ids the same plan reclaims and later hands out from the reclaim queue (and ids already given new defs this gesture), so a replaced chunk's unchanged cells end up pointing at a block id the plan overwrites with different art and colind.
EVIDENCE: classic-commit-plan.ts:373-382 scans doc.blocks with no exclusion of reclaim.blocks or blockWrites; :386 allocates from the same reclaimBlocks queue. Spec §4 step 8 explicitly requires 'reclaimed block ids pending overwrite are excluded from matching' — not implemented. EXECUTED REPRO (scratchpad/repro-block-availability.cjs, bundled from repo source at 2b38e0b): replace chunk 0 keeping the first 16x16 block identical to old block 1 and drawing new art in the second; output: cell0 (old art) -> block 1 AND blockWrite id 1 carries the NEW art's def — 255 of 256 chunk cells render the wrong 16x16, with colind 9 attached to art it never described. Store applies blockWrites verbatim (classicLevelStore.ts:1042-1045) and the corrupt chunk/blocks are saved into the user's s1disasm. This is the exact partial-edit-then-commit-replace main path; the tile tier has PoolAvailability for precisely this class (tile-pool-match.ts:33-48) and the existing regression (classic-surface-plan.test.ts:421) pins only the tile site, not the class.
FIX: Mirror PoolAvailability at the block tier: skip ids in a pendingReclaim set and ids already in blockWrites inside the doc.blocks match scan (classic-commit-plan.ts:373), and remove any pool-matched id from the reclaim allocation queue. Add a block-tier regression with a planted match+allocate collision that fails against today's code.
VERDICT: CONFIRMED — I tried to refute A1 and could not — the path holds end to end, and I reproduced it by executing the real planner.

CODE PATH (src/core/art/classic-commit-plan.ts @ 2b38e0b):
- Tile tier is protected by the three-state PoolAvailability: a match does `avail.matched.add(m.tileIndex)` (:312) and `freeSlot()` skips `unavailableForAllocation(avail)` (:291-302), so a reclaimed tile that gets matched can never also be allocated.
- Block tier has no equivalent. `internBlock`'s pool scan at :373-383 walks every `doc.blocks[b]` with only a colind/cell-equality test — no exclusion of `reclaim.blocks` and no exclusion of ids already pushed into `blockWrites`. Allocation at :386 then hands out `reclaimBlocks[reclaimBlockCursor++]` unconditionally. Nothing records a matched id anywhere the allocator reads (`blockIdOf` is keyed by def+colind and never consulted by the allocator). So the same id can be both "reused" for one cell and rewritten with different art/colind for another in one plan.

EXECUTED REPRO (bundled the actual repo sources with esbuild, ran under node; files in /tmp/claude-1000/-home-volence-sonic-hacks-aurora/375dbea4-.../scratchpad/entry.ts and entry.cjs — nothing in the repo was modified):
Doc: 64-tile pool, tile 1 = flat entry 5; block 1 = 4x{tile 1, pal 1} with colind 9; chunk 0 = 256 cells of block 1 (so block 1 and tile 1 are exclusive to chunk 0); chunk 1 uses block 2. Gesture: target chunk 0, canvas painted so the top-left 16x16 keeps the old art (canvasIndex(1,5)) and the rest is new art (canvasIndex(1,6)).
Output:
  report: blocksReused:1, blocksNew:1, blocksReclaimed:1, no warnings
  tileWrites [2]            <- tile tier correctly refused to reallocate matched tile 1
  blockWrites [{id:1, tiles:[2,2,2,2], colind:9}]
  chunk cell 0 -> block 1, and block 1 is overwritten by this same plan (verified: written.has(cells[0].block) === true)
So the 16x16 the artist deliberately kept is bound to block id 1 while block id 1 is simultaneously given the new art's def. The kept art is destroyed with no refusal and no warning.

REACHABILITY: live main path, not synthetic. planFromSnapshot (src/renderer/components/canvas/canvas-commit-model.ts:208-210) feeds planCanvasCommit; targets with a non-null chunkFileIndex come straight from the UI chunk picker (CommitPlanView.tsx:84,117), and several refusal messages actively push the artist toward "Replace existing chunks". classicCommitCanvas applies blockWrites verbatim (src/renderer/state/classicLevelStore.ts:1042-1045) and colind verbatim (:1050-1057); structuralError sees nothing wrong because the doc is structurally valid, just wrong. The state that triggers it is ordinary: replace a chunk you partially edited, where the chunk owns blocks used nowhere else (exactly what reclaim collects) — every unchanged 16x16 matches its own pool block, and the first reclaimed id handed to a newly drawn block is the lowest of those same exclusive ids.

SPEC: docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:358 states the requirement verbatim ("reclaimed block ids pending overwrite are excluded from matching for the same reason their tile counterparts are"); neither that exclusion nor the tile-tier alternative (matched => not allocatable) exists in the code. The existing regression at classic-surface-plan.test.ts:421 pins only the tile-tier instance of this identical class.

I found no guard that breaks the chain: the reclaim filter (computeReclaim :180-204) restricts reclaim to blocks exclusive to replaced chunks — which is precisely the set most likely to be matched by unchanged cells, so it aggravates rather than prevents the collision.
SEV-CORR: Severity critical stands, with one correction to the claim's damage description. The art half is loud-ish (the just-committed chunk renders wrong immediately, and undo exists), which on its own would read as "high". What makes it critical is the collision half, which is genuinely silent: the overwritten id's colind comes from whichever displaced cell allocated it, so when the replaced chunk's old cells carry different colinds (e.g. old cell 0 -> block with colind 9, old cell 1 -> block with colind 4), the reused cell's collision shape silently changes under unchanged-looking art, and it is written into doc.collision.colind and saved to the user's s1disasm with no warning in the report. Also note the blast radius is narrower than the finding implies: corruption is confined to the chunks being replaced in that gesture (reclaimed ids are exclusive to them), not the wider level, and the "255 of 256 cells wrong" figure is repro-specific — in my run the single kept cell was the one destroyed.

########## SAVE-A1 [high] No unsaved-work guard on window close / app quit
file: src/main/index.ts:56  confidence: certain
CLAIM: Closing the Aurora window (X button / Ctrl+W / SIGTERM-initiated quit) discards all dirty classic/canvas/sprite/aeon work with no prompt, while every other exit door in the app prompts.
EVIDENCE: src/main/index.ts:32-79 — BrowserWindow has no 'close' interception and app has no before-quit dirty check; grep for 'beforeunload' across src/ returns zero renderer handlers. Contrast: tab close prompts (src/renderer/shell/tab-activation/dispatch.ts:149-155), act switch prompts (src/renderer/shell/tab-activation/level.ts:179), project open prompts (src/renderer/shell/project-open-guard.ts), Project Setup Apply prompts (src/renderer/components/setup/ProjectSetupTab.tsx:161-183). Failure scenario: user paints tiles (dirty.tiles=true, confirmed set at classicLevelStore.ts:678-686), clicks the window X — window closes immediately, edits gone, no dialog. The app trains users to expect a prompt at every other destructive transition, so the one unguarded door is exactly where the loss will happen.
FIX: Intercept 'close' on the BrowserWindow in src/main/index.ts, ask the renderer (IPC round-trip to the same dirty-snapshot the tab-close path uses, src/renderer/shell/dirty-snapshot.ts) and show the save/discard/cancel confirm before allowing destroy.
VERDICT: CONFIRMED — I attempted to refute and could not; every candidate escape hatch is absent in the tree.

1. Main process: read /home/volence/sonic_hacks/aurora/src/main/index.ts in full (79 lines). Line 56 is `win.on('closed', ...)` — the past-tense, non-cancellable event fired AFTER destruction. There is no `win.on('close')`, no `app.on('before-quit')`, and no `event.preventDefault()` in the file. `app.on('will-quit')` (line 73) only calls `stopMcpServer()`; `window-all-closed` (75-79) calls `app.quit()` unconditionally off-darwin.

2. No renderer backstop: `grep -rn "beforeunload|before-quit|onbeforeunload|will-prevent-unload|window-close|closeWindow|confirm-close" src/` returns zero hits. A `beforeunload` handler is the only way the renderer could veto a close without main-process cooperation, and it does not exist. `grep -rni "quit|close|unload|destroy" src/main src/preload` surfaces only the three lines above plus unrelated agent-bridge/mcp-server cleanup.

3. No autosave to soften the loss: `grep` for `setInterval|autosave|autoSave|debounce.*save|periodic` over src/renderer/state/*.ts and src/renderer/shell/*.ts returns nothing. src/renderer/shell/session-lifecycle.ts:127-138 persists only `{tabs, activeId}` and the workspace record (facet + viewport) to localStorage — tab identity, never document content. So a relaunch restores which tabs were open and restores them from disk, i.e. it restores exactly the pre-edit bytes.

4. Dirty state is genuinely in-memory: classicLevelStore.ts:678-681 (`applyCommit`) sets `dirty: {...s.dirty, ...dirtyPatch}` next to the new `doc` in the zustand store with no disk write. canvasStore.ts:252/260/270/300 set `unsavedEdits: true` on in-memory doc entries.

5. The stated contrast is accurate: src/renderer/shell/tab-activation/dispatch.ts:148-155 (confirmCloseSpriteDoc / confirmCloseCanvasDoc), tab-activation/level.ts:174-186 (three-button "Unsaved changes in <act>" confirm), and project-open-guard.ts confirmProjectOpen (full ask→save→re-snapshot guard, whose header comment explicitly calls the prior unguarded version "silent data loss") all prompt.

Additional evidence the seat missed that strengthens rather than weakens the finding: `grep -rn "Menu|setApplicationMenu" src/main` returns zero, so Electron's DEFAULT application menu is active and supplies the `close` role bound to Ctrl+W; and Ctrl+W is not claimed by the renderer either (no 'w' key case in App.tsx's keydown handler at 100-121, none in shell/commands.ts). In a tab-centric editor, the reflexive "close tab" chord therefore destroys the whole window and every dirty document.

End-to-end triggering state: classic project open, one tile painted (commitArt sets dirty.tiles=true, doc held only in zustand), then titlebar X or Ctrl+W. Nothing intercepts 'close', the window is destroyed, window-all-closed quits the app, and the edits plus undo history are gone with no dialog.

Severity check: the seat said high. Under the given rubric ("critical = silent loss of user work") this arguably qualifies as critical rather than high; it is certainly not theatrical and does not warrant a downgrade. I am not issuing a formal correction because the X-button path is a deliberate, unambiguous user action, which is the one factor arguing for high over critical — but the Ctrl+W path is not intent-aligned at all.


########## SAVE-A2 [high] Dirty flags cleared unconditionally after async save — edits landing mid-save are silently marked clean (class: 3 of 4 save paths)
file: src/renderer/state/classic-save.ts:205  confidence: certain
CLAIM: Classic, canvas, and aeon saves all clear dirty state captured before the write without checking whether the document changed during the IPC/write await, so an edit committed mid-save is left un-flagged while its bytes are not on disk; the sprite path already fixes this exact race, proving the class.
EVIDENCE: Classic: saveClassicProject snapshots doc+dirty in collectDirtyLevels (classic-save.ts:106-112), then awaits api.writeGuarded (real IPC, classic-save.ts:73) — pointer/keyboard macrotasks interleave there, and any classic command commits via applyCommit which sets dirty (classicLevelStore.ts:678-686); on return, markDomainsClean deletes the domain unconditionally (classicLevelStore.ts:487-494 — no doc-identity or edit-generation check). Canvas: saveCanvasDocument reads doc at canvas-save.ts:49-50, awaits encodeCanvasFiles + writeGuarded, then markSaved sets unsavedEdits:false unconditionally (canvasStore.ts:308-312). Aeon: aeon-save.ts:32 calls markClean() after the write loop with no comparison. The codebase KNOWS this race: the sprite save re-reads the doc after the await and clears the flag only when framesEqual(after.frames, frames) (export-sprite.ts:230-235, 283-290) — the other three paths never got the fix. Consequence chain: dirty flag false while doc≠disk → next Ctrl+S returns 'nothing' (collectDirtyLevels, classic-save.ts:109-110; canvas saveableDirtyCanvasDocIds), and every close/switch prompt keys off these same flags (dirty-snapshot.ts, project-open-guard.ts, confirmCloseCanvasDoc) → the mid-save edit is silently lost on close. Failure scenario: user is painting; a stroke's pointer-up commits while Ctrl+S's guarded write is in flight; save resolves, clears dirty.tiles; user closes — stroke gone, no prompt, no toast.
FIX: Port the sprite pattern: after the write resolves, compare the live doc's domain against the snapshot that was encoded (or use a per-domain edit-generation counter captured before write) and clear only unchanged domains; canvas should compare pixels/doc revision before markSaved; aeon should gate markClean the same way. Toast 'edits made during the save are still unsaved' as export-sprite.ts:288 does.
VERDICT: CONFIRMED — Traced all three cited paths end-to-end. Classic: collectDirtyLevels (classic-save.ts:106-112) snapshots doc+dirty synchronously; saveClassicProject awaits levels.write (197) and api.writeGuarded (73, real ipcRenderer.invoke); a mid-flight edit goes through applyCommit (classicLevelStore.ts:678-686) which installs a NEW doc object and re-sets dirty; on resolve, line 205 calls markDomainsClean, which (classicLevelStore.ts:487-494) checks ONLY ref zone/act identity and then unconditionally `delete dirty[d]` — no doc-identity or generation check. Canvas: saveCanvasDocument reads canvasDocState before the await (canvas-save.ts:49-50) and calls markSaved after (canvasStore.ts:308-312, unsavedEdits:false unconditional); tellingly the same file DOES re-read live state for the source baseline (refreshBaselines, canvas-save.ts:137-145) so the mid-save mutation hazard was recognized for mtimes but not for the dirty flag. Aeon: aeon-save.ts:32 markClean() -> editorStore.ts:299 set({dirty:false}), unconditional; setLoading(true) is not an input block (no component consumes projectStore.loading). Consequence chain verified: currentDirtySnapshot (dirty-snapshot.ts:20-33) derives classicDirty from Object.values(classic.dirty).some(Boolean) and aeonDirty from useEditorStore.dirty — the exact flags just cleared, so no tab dot and no close prompt; the next Ctrl+S returns {kind:'nothing'} at classic-save.ts:109-110 with no toast. The sprite path really does implement the fix the others lack (export-sprite.ts:271-290: re-read via spriteDocState after the await, framesEqual gate, 'edits made during the save are still unsaved' toast), confirming the class. Refutation attempts that failed: no save-in-flight lock exists anywhere (grep isSaving/savePending/saveInFlight -> zero hits), no blocking overlay during save, no coordinator serialization of edits, and domainsToClear is conservative about file paths only, not about time. One narrowing the seat did not state (does not break the finding): loss requires the mid-save edit to touch a domain already present in the pre-write dirty snapshot — a new domain is not cleared — which is the common painting-while-saving case. Concrete trigger: classic act open with dirty.tiles; Ctrl+S; a pointer-up commits a tile stroke while writeGuarded is in flight; save resolves 'saved', domainsToClear returns ['tiles'], markDomainsClean deletes it, toast says 'Saved 1 level(s)'; the stroke is not on disk, has no dirty dot, and closing discards it with no prompt.
SEV-CORR: None — 'high' is correct and arguably conservative. By the rubric this is silent loss of user work (critical territory: bytes not on disk, flag false, no dot, no close prompt, no toast on the follow-up no-op save), tempered only by the narrow race window and the requirement that the mid-save edit hit a domain already in the pre-write dirty snapshot. Keep at high; do not downgrade.

########## FMT-A1 [high] kosinskiDecompress loops without bound on malformed input — exhausts the heap and kills the process
file: src/core/formats/kosinski.ts:20  confidence: certain
CLAIM: kosinskiDecompress has no EOF error, no output cap and no iteration guard, so a .kos file whose descriptor words are zero decodes forever, growing an unbounded JS array until V8 aborts the process — which no try/catch can intercept.
EVIDENCE: kosinski.ts:20-23 readByte() returns 0 once readPos passes the end instead of throwing (contrast nemesis.ts:26, which throws 'Unexpected end of Nemesis data'). kosinski.ts:47-80 is a `for(;;)` whose ONLY exit is the full-match terminator at line 66 (`count === 1` after `hi&7 === 0`). With an all-zero descriptor: popDescriptor() -> false (not literal), popDescriptor() -> false (short match), count=2, distance=(0^0xFF)+1=0x100, two bytes appended per iteration, terminator never reachable. Executed repro at the pinned commit: `kosinskiDecompress(new Uint8Array([0x00,0x00]))` under `node --max-old-space-size=800` printed 'FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory' and dumped core (exit 134) after ~3s. An all-zero file of ANY length behaves identically (every descriptor read is 0x0000). Reachable unguarded at src/core/level-classic/s1-io.ts:285 `kosinskiDecompress(await readBytes(paths.chunks))` — the S1 open path over the user's own disasm tree — and at src/core/formats/chunk-mappings.ts:116-117 (importChunks, both chunk and block files). Related silent-corruption sibling on the same path: kosinski.ts:77 indexes `window[(writePos - distance) % SIZE]`, which is negative before enough output exists, yielding `undefined & 0xFF` = 0 rather than an error. Note: I truncated the real GHZ.kos at 88 different offsets and none ran away, so the trigger is a zero/near-zero descriptor stream (empty-but-created file, zero-padded placeholder, wrong-file path), not arbitrary truncation.
FIX: Make kosinski.ts:20-23 throw a KosinskiError at EOF like nemesis.ts:26 does, and add an output ceiling to the decode loop (e.g. reject once output.length exceeds a caller-supplied max or a hard 4 MB) so a corrupt file surfaces as a routed error instead of an unrecoverable process abort. Also make the negative-window read at line 77 an explicit error rather than a silent zero.
VERDICT: CONFIRMED — Attempted refutation on four links; all held.

TRACE (kosinski.ts:47-80): with an all-zero descriptor, popDescriptor() -> false (not literal), popDescriptor() -> false (short-match branch, line 69), count=2, distance=(0^0xFF)+1=0x100, two bytes appended per iteration. The only break is line 66, which lives inside the FULL-match branch (requires line 54's popDescriptor() to be true) and is therefore unreachable. readByte() (line 21) returns 0 past EOF rather than throwing, so getDescriptor() at line 41 refills descriptor with 0x0000 forever. output: number[] grows without bound.

EXECUTED at pinned SHA 2b38e0b: esbuild-transpiled the unmodified src/core/formats/kosinski.ts and ran kosinskiDecompress under node --max-old-space-size. Inputs new Uint8Array(0), [0x00,0x00], and new Uint8Array(4096) each printed "FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory" and dumped core. A control input (60 bytes of shell-script text) returned normally in 0ms, confirming the harness is sound and the failure is input-dependent.

GUARDS CHECKED AND INSUFFICIENT:
- src/core/project/s1/index.ts:368 `if (!meta.available)` — the most likely refutation. Availability comes from resolveEntry (lines 178-196), which is pure `fa.exists()`. Presence only; never size or content. A zero-byte/placeholder chunks file resolves as available:true.
- src/core/level-classic/s1-io.ts:221-229 readBytes throws only ENOENT for a missing prefetch entry; an existing empty file passes through to line 285 unguarded.
- src/renderer/providers/chunk-library-import.ts:28 try/catch is inert against a V8 heap abort, exactly as the seat claimed.

CORRECTION THAT WIDENS THE FINDING: the seat characterized the trigger as "a zero/near-zero descriptor stream". Too narrow. A git-lfs pointer file (ordinary non-zero ASCII, "version https://git-lfs.github.com/spec/v1\noid sha256:...\nsize 1234\n") ran away identically. The true trigger class is any input whose descriptor bits never route to the full-match terminator before EOF, which includes plain text — making an LFS-pointer clone of a disasm tree a realistic path to the crash.

SIBLING CLAIM CONFIRMED: (0-256)%8192 === -256 in JS, window[-256] is undefined, and writeByte's `byte & 0xFF` converts NaN to 0. Silent zero, no error. Only reachable on malformed input (valid Kosinski never back-references before stream start), so it is a robustness wart rather than a corruption risk on real files.

Both cited call sites match the report verbatim: s1-io.ts:285 and chunk-mappings.ts:116-117.
SEV-CORR: No change — "high" is correct as stated. Recording the reasoning since I checked it both directions: it fails the "critical" bar because the failure is a loud process abort, not silent loss or corruption of data at rest; it clears the "high" bar because two main-path features (S1 project open, chunk import) kill the process instead of routing an error. Triage note: the chunk-import path is the one that costs real work, since it runs in the renderer mid-session and the abort takes unsaved editor state with it, whereas the open path fails before edits exist.

########## CLASSIC-A1 [critical] MCP get_classic_level silently destroys unsaved classic edits + undo history
file: src/renderer/agent/agent-handler.ts:625  confidence: certain
CLAIM: The agent/MCP `get_classic_level` handler calls `useClassicLevelStore.getState().openAct(ref)` unconditionally, and `openAct` disposes both undo stacks and resets `dirty` to `{}` before re-reading from disk — so any unsaved classic edits are destroyed with no confirmation, no error, and no way back, even when the requested act is the one already open.
EVIDENCE: src/renderer/agent/agent-handler.ts:620-625 — `classic-get-level` looks the ref up in `zoneTree` and immediately `await useClassicLevelStore.getState().openAct(ref)`; there is no dirty check and no early-return when `ref` equals the currently-loaded act. src/renderer/state/classicLevelStore.ts:356-357 `disposeStacksFor(get().ref); disposeStacksFor(ref);` (drops `level:<zone>:<act>` AND `zoneart:<zone>` history) and :358-387 `const fresh = { dirty: {} as DirtyDomains, ... }` which is spread into every `set` below, then :401 `const doc = await handle.levels.read(ref)` re-reads from disk. The UI path deliberately does NOT do this: src/renderer/shell/tab-activation/level.ts:3-6 and :56-58 route a dirty classic act to `{kind:'classic-confirm'}` (Save & switch / Discard / Cancel) precisely because "openAct resets doc + dirty + undo history". The same agent file already applies the fail-closed pattern to the sibling tool: agent-handler.ts:575-584 `if (planProjectOpen(currentOpenDirtySnapshot()).kind === 'confirm') throw new Error('Unsaved changes present ...')` for `classic-open-project` — so the omission at :625 is a gap, not a policy. Failure scenario: agent calls `edit_chunk`/`set_colind`/`place_object` on GHZ act 1, then calls `get_classic_level('ghz',1)` to verify its own work; the act is re-read from disk and every uncommitted edit plus the entire undo timeline is gone, and the tool returns a successful-looking payload describing the on-disk state.
FIX: In `agent-handler.ts` `case 'classic-get-level'`, (a) return the current doc without re-reading when `ls.ref` already matches `req.zone`/`req.act` and status is 'ready', and (b) for a genuine switch, apply the same fail-closed guard used at :579 — throw when `Object.values(useClassicLevelStore.getState().dirty).some(Boolean)` rather than calling `openAct`. Alternatively give `openAct` an explicit `{ allowDiscard: boolean }` and make the default refuse.
VERDICT: CONFIRMED — Traced end-to-end and reproduced empirically; could not refute. (1) agent-handler.ts:621-625 `classic-get-level` calls `useClassicLevelStore.getState().openAct(ref)` unconditionally — no dirty check, no same-ref early return. (2) classicLevelStore.ts:349-357 `openAct` immediately does `disposeStacksFor(get().ref); disposeStacksFor(ref);` (drops both the `level:<zone>:<act>` and `zoneart:<zone>` stacks) with no short-circuit above it; :362 `dirty: {}` is inside the `fresh` object spread into every later `set`; :401 `const doc = await handle.levels.read(ref)` replaces the doc. (3) The real S1 adapter's read (src/core/project/s1/index.ts:365-392) always calls `readS1Level` from disk — the `readStates` map at :356 is write-side bookkeeping ("so write() can re-pair it with the (edited) doc"), not a doc cache, so nothing returns the edited doc. (4) No upstream guard: `registerAgentHandler`/`handleAgentRequest` (agent-handler.ts:46-58) is a bare try/catch dispatcher, and the file's only fail-closed guard is at :575-584 for `classic-open-project`; src/main/editor-methods.ts:115 exposes the tool over MCP ungated. (5) The UI counter-path is even stronger than the seat claimed: `planLevelActivation` (shell/tab-activation/level.ts:56-59) has BOTH a same-act no-op (`return {kind:'none'}`) and a `classic-confirm` for dirty, neither of which the agent path has; the file header at :3-6 states outright that "openAct resets doc + dirty + undo history".

Empirical repro (scratchpad-only test cloned from the repo's own agent-handler.classic harness; repo unmodified): open ghz/1 ready → agent `classic-place-object` (objects 1→2, dirty set, layout stack canUndo=true) → agent `classic-get-level('ghz',1)` on the SAME open act. Output: reads from disk: 1 / objects after: 1 / dirty after: {} / canUndo after: false / tool reported objects: 1. The unsaved edit and the entire undo timeline are destroyed and the tool returns a successful-looking payload describing on-disk state.

Severity: critical is correct per the rubric (silent loss of user work, no confirmation, no recovery via undo), and it is reachable on the most natural agent sequence (edit, then read back to verify) as well as destroying a human's unsaved UI edits mid-session. The only nuance is that the tool description says "Open and read one act" — but the UI's equivalent open is confirm-guarded for exactly this reason and the sibling agent tool is fail-closed, so this is a gap, not a policy.


########## UNDO-A1 [critical] MCP `classic-get-level` (a read tool) silently discards the loaded act's unsaved edits and both undo stacks
file: src/renderer/agent/agent-handler.ts:625  confidence: certain
CLAIM: `classic-get-level` calls `useClassicLevelStore.openAct(ref)` with no dirty check and no confirm; `openAct` disposes both undo stacks and re-reads the LevelDoc from disk with `dirty: {}`, so every unsaved classic edit in the currently-loaded act is destroyed by what the agent surface presents as a read.
EVIDENCE: agent-handler.ts:625 `await useClassicLevelStore.getState().openAct(ref);` is reached from the `classic-get-level` case with no guard — the only dirty guard in the whole handler is agent-handler.ts:579, in `classic-open-project`. classicLevelStore.ts:356-357 `disposeStacksFor(get().ref); disposeStacksFor(ref);` drops `level:<zone>:<act>` and `zoneart:<zone>` (classicLevelStore.ts:331-335), classicLevelStore.ts:359 sets `dirty: {}`, and classicLevelStore.ts:401 `const doc = await handle.levels.read(ref)` replaces the doc. There is no same-ref early return, so this fires even when `req.zone/req.act` IS the loaded act. The UI door for the same call has a three-button confirm (shell/tab-activation/level.ts:175-197, planLevelActivation's `classic-confirm` at level.ts:59) — the agent door has none. Failure scenario: user paints 20 minutes of GHZ tile art (dirty.tiles=true), asks the agent "what's in GHZ act 2?", the agent calls classic-get-level → the art is gone from memory, never written to disk, and the undo stack that could recover it is disposed.
FIX: Route the agent through the same gate as the tab strip: either call `activateLevelTarget(classicLevelTab(ref).id)` / reuse `planLevelActivation`, or refuse with an error when `Object.values(useClassicLevelStore.getState().dirty).some(Boolean)` and the target ref differs; add a same-ref early return in `openAct` so re-opening the loaded act is a no-op.
VERDICT: CONFIRMED — Could not refute; traced independently and confirmed. (1) agent-handler.ts:621-625 `classic-get-level` calls `useClassicLevelStore.getState().openAct(ref)` with no dirty check — grepping `dirty`/`planProjectOpen` across the whole 739-line handler yields one hit, line 579 in `classic-open-project`, whose comment states the very policy this case breaks. (2) classicLevelStore.ts:350-362 `openAct` unconditionally runs `disposeStacksFor(get().ref); disposeStacksFor(ref);` (disposing `level:<zone>:<act>` and `zoneart:<zone>`, lines 331-335) and sets `dirty: {}`; there is no same-ref early return in the function — the only same-ref short-circuit lives in the UI planner, shell/tab-activation/level.ts:58, unreachable from the agent path. (3) The doc really is re-read from disk: classicLevelStore.ts:401 `handle.levels.read(ref)` → core/project/s1/index.ts:365-392, which calls `readS1Level(...)` on every invocation; the `readStates` map (line 356) caches only write-side bookkeeping, never the edited doc. No autosave exists anywhere in src (grep autosave|autoSave → nothing), so the lost edits were never persisted. (4) The equivalent UI door has a three-button confirm (level.ts:175-197) plus a save-failure guard (isSaveSuccess, line 37) that exists specifically to stop the fallthrough into openAct. Empirically, an existing repo test already encodes the destructive same-ref semantics and passes at this SHA: classicLevelStore.test.ts:681 edits via classicSetStart/classicSetPalette, calls `openAct(REF)` with the already-loaded REF, and asserts both undo stacks are cleared (`npx vitest run -t "loading a new act clears both"` → 1 passed). Concrete trigger: classic project open on ghz/1, user or agent makes any edit (dirty set, stacks populated), agent calls MCP get_classic_level for any zone/act including ghz/1 itself → edits and both undo stacks destroyed, no prompt, no recovery; the reply then reports the pristine on-disk data as if the edit never happened. Only correction to the finding's framing (not affecting the defect): the MCP description at main/editor-methods.ts:117 does say "Open and read one act", so the surface is not advertised as a pure read — but the name is `get_classic_level`, nothing warns about discarding, and no gate exists, so the loss is still silent.
SEV-CORR: Severity stands at critical. It meets the bar literally — silent, unrecoverable loss of unsaved user work (the undo stacks that could restore it are disposed in the same call) — and the codebase's own sibling tool fails closed on this exact hazard. The only argument for downgrading to high is that the trigger requires an agent-driven session, but that is the intended first-class door for this surface, and the self-inflicted variant (edit → get_classic_level to verify → edits erased and pristine data reported back) makes it a routine flow rather than an exotic one.

########## UNDO-A2 [critical] Leaving the aeon map viewport mid-drag drops an object/ring move: doc already mutated, no command, no dirty flag
file: src/renderer/components/MapViewport.tsx:1535  confidence: certain
CLAIM: An object/ring drag mutates `obj.x/obj.y` in place on every mousemove and only becomes a `move-object` command on mouseup; `onMouseLeave` nulls `dragTarget` without committing, so a drag that exits the viewport leaves the placement moved in the document with no undo entry and with `editorStore.dirty` still false — the move is invisible to save and to the unsaved-changes guard.
EVIDENCE: MapViewport.tsx:1240-1252 mutates `obj.x = Math.round(...)` / `ring.x = ...` directly and calls only `useEditorStore.getState().bumpLiveEdit()` (editorStore.ts:300 — bumps `liveEditVersion`, does NOT set `dirty`). The commit lives in `handleMouseUp` at MapViewport.tsx:1362-1400, which restores `target.startX/startY` and then `executeCommand({type:'move-object'...})` (executeCommand is the only caller of `markDirty`, editorStore.ts:396). MapViewport.tsx:1531-1537 `onMouseLeave={() => { ... if (dragTarget.current) dragTarget.current = null; isDragging.current = false; ...}}` runs none of that. There is no window-level mouseup listener in the file (grep for addEventListener finds only keydown at :702 and mousedown/keydown at :1443-1444), so a release outside the container never reaches handleMouseUp either. Consequence chain: `aeonDirty` = `useEditorStore.getState().dirty` (shell/dirty-snapshot.ts:31) feeds `tabHasDirtyDot` (shell/dirty-tabs.ts:41) and the aeon saver's `scope.isDirty` (state/project-runtime.ts:164) — so with no other edit in the session the tab shows no dot, Ctrl+S is a no-op, the close/project-switch guard says nothing is unsaved, and the move is lost. With another edit present it is saved silently and cannot be undone.
FIX: Bind the commit to the window (a pointer capture or a `window` mouseup listener, the shape `useWindowStrokeEnd` already uses for the classic composer at components/classic/composer-shared.tsx:145), and make `onMouseLeave` call the same commit path rather than discarding `dragTarget`.
VERDICT: CONFIRMED — Traced end to end and could not break it. (1) MapViewport.tsx:910-938 sets dragTarget/isDragging on select-mousedown over an object or ring; no setPointerCapture exists anywhere in the file. (2) MapViewport.tsx:1232-1252 mutates the LIVE document — getSectionByIndex (line 840-844) returns useProjectStore's act section by reference, so obj.x/obj.y are the real placement — and calls only bumpLiveEdit (editorStore.ts:300, increments liveEditVersion, never sets dirty). (3) The only commit is handleMouseUp MapViewport.tsx:1362-1400 via executeCommand, which is the only caller of markDirty (editorStore.ts:396; the sole other markDirty is executeAmbientCommand at :474). (4) MapViewport.tsx:1531-1540 onMouseLeave nulls dragTarget and isDragging with no commit, and there is no window mouseup listener — the file's only window.addEventListener calls are keydown at :702 and mousedown/keydown at :1443-1444 for the context menu — so a release outside the container never reaches handleMouseUp, and re-entry does not resume the drag (no onMouseEnter; mousemove with isDragging=false falls to the hover-bar branch). (5) Consequences verified in the stores, not assumed: dirty-snapshot.ts:31 aeonDirty = editorStore.dirty; dirty-tabs.ts:44 returns aeonDirty for a level tab, so no dot; core/editing/save-coordinator.ts:98-105 skips saveActive when scope.isDirty is false and project-runtime.ts:164 defines that scope as tabHasDirtyDot, so Ctrl+S is a real no-op; project-open-guard.ts:49-63 returns {kind:'proceed'} with aeonDirty false, so the project switch/close guard asks nothing and resetProjectRuntime discards the move; and core/project/aeon/save.ts:85-92 serializes section.objects/section.rings verbatim, so with any other edit present the move is written to disk with no undo entry. Concrete trigger: aeon level, select tool, press an object, drag past the viewport edge (toward the inspector/palette panel), release — the object renders at the new spot, dirty stays false, undo has nothing, Ctrl+S no-ops, close prompts nothing. Two nits in the seat's evidence that do not rescue the code: Save All (Ctrl+Shift+S) WOULD persist it, because the aeon saver's top-level isDirty is openEngine()==='aeon' (project-runtime.ts:159) rather than a dirty check, so 'invisible to save' is true only for Ctrl+S; and the fix shape it cites is corroborated — composer-shared.tsx:130-145 documents this identical onMouseLeave-discards-the-stroke bug being fixed for the classic composer with a window mouseup.
SEV-CORR: high — the defect is real and silent, but the magnitude is one placement per occurrence and the on-disk project is never corrupted: the lost case discards a single object/ring move (redoable by dragging again), and the saved case writes exactly the position the user intended, merely without an undo entry. Save All also still persists it (project-runtime.ts:159), so it is not fully invisible to the save path. Reserve critical for losses the user cannot reconstruct; this is a broken main-path interaction plus a lying dirty flag.

########## UNDO-A3 [high] BG-layer tile painting mutates the layout with no command — un-undoable, and Ctrl+Z reverts an unrelated FG edit instead
file: src/renderer/components/MapViewport.tsx:764  confidence: certain
CLAIM: `paintBgTile` writes straight into the resolved BG nametable and calls `markDirty()` + `bumpLiveEdit()` with no command dispatch, so every BG paint stroke is outside history; the next Ctrl+Z on the act tab pops whatever FG/object command preceded it and silently reverts that instead.
EVIDENCE: MapViewport.tsx:744-769: `resolved.layout[tile.tileIndex] = newNt; sectionRenderer.markBgDirty(...); useEditorStore.getState().markDirty(); useEditorStore.getState().bumpLiveEdit();` — no `executeCommand`. It is a first-class drag path (mousedown MapViewport.tsx:948-952 sets `isPaintDragging`, mousemove MapViewport.tsx:1199-1202 re-enters it). editorStore.ts:90-96 admits this by name ("level mutations that DON'T go through a command and so never reach an undo stack ... a direct BG tile write"). A `set-bg` command type exists (core/editing/commands.ts:131-140) but is a whole-plane swap, not a per-tile edit, and nothing on this path builds one. Failure scenario: stamp a chunk (one `set-tiles`/batch on `level:<zone>:<act>`), switch editingLayer to 'bg', paint 30 BG tiles, press Ctrl+Z — the 30 BG tiles stay and the chunk stamp disappears.
FIX: Give the BG plane a per-tile command (a `set-bg-tiles` sibling of `set-tiles`, or reuse `set-tiles` with a plane discriminator) and coalesce the drag into one entry on mouseup, the way the classic viewport already does for its stamp gesture (components/classic/ClassicLevelViewport.tsx:896-908).
VERDICT: CONFIRMED — Could not refute; traced end to end. (1) Reachability: facet-chrome.ts:85 makes `layout` plane-gated so the BG chip renders (LevelWorkspace.tsx:138-141), and facet-tools.ts:20 puts `paint-tile` in the layout facet — so "BG plane + Paint Tile" is a real UI combination. (2) MapViewport.tsx:948-954 returns from the mousedown handler via paintBgTile() before ever reaching the executeCommand({type:'set-tiles'}) at 965; mousemove 1200-1203 does the same for the drag. (3) paintBgTile (744-769) writes resolved.layout[...] = newNt and calls only markBgDirty/markDirty/bumpLiveEdit — no command. (4) resolveActiveBg (61-73) returns the LIVE arrays by reference (entry.layout or act.bgLayout), and core/project/aeon/save.ts:148-154 explicitly persists "BG-layer painting" to data/editor/<zone>_<act>_bg.bin, so the mutation is durable project state. (5) No mouseup coalescing: handleMouseUp (1338-1403) only clears isPaintDragging at 1339; by contrast object/ring drags ARE converted into move-object/move-ring commands at 1374/1389, proving bumpLiveEdit is a transient clock there and the BG case is a permanent gap (editorStore.ts:90-96 names it). (6) Undo scoping confirmed: set-tiles is act-scoped (ZONE_SCOPED_COMMAND_TYPES at editorStore.ts:410-414 lists only set-palette-line/set-tileset-tiles/set-chunk) and executeCommand pushes onto focusedHistory() (editorStore.ts:388-395), the same stack LevelWorkspace.tsx:98-101 pops on Ctrl+Z. (7) set-bg (commands.ts:131-140, history.ts:81-88) is a whole-plane swap of act.bgLayout/bgTiles, never built on this path, and would not even cover the bgLibrary-entry case paintBgTile can hit. Concrete trigger: aeon act tab, Layout facet, stamp a chunk (one act-scoped command), click BG chip, drag-paint N BG tiles, press Ctrl+Z — the BG tiles stay (and save to disk) and the chunk stamp is reverted instead.
SEV-CORR: Severity high is correct as stated; explicitly NOT critical — the painted BG data is neither lost nor corrupted (it persists and saves), and the wrongly-undone FG command is recoverable via redo. The defect is a broken main-path feature (undo silently acts on the wrong edit), which matches "high".

########## UNDO-A4 [high] Aeon composer's tile-space tools and doc-local pixel path mutate the open document with no history entry
file: src/renderer/components/art/ComposerCanvas.tsx:342  confidence: certain
CLAIM: `applyTileCell` (tile-stamp, collision paint, palette-apply) and the doc-local branch of `commitWrites` mutate the open ComposerDoc in place and only call `markOpenDirty()` + `bumpDoc()`; neither reaches an undo stack, so Ctrl+Z on the art facet reverts an unrelated `set-tileset-tiles`/`set-chunk` command while the composer edit stays.
EVIDENCE: ComposerCanvas.tsx:313-344 — `stampTile(doc, cx, cy, ...)` / `applyPaletteLineToDocCell(doc, ...)` / `paintDocCollision(doc, ...)` all mutate `o.doc` and end at `markOpenDirty(); bumpDoc();` with no `executeCommand`. It is guarded only against live-tile docs (ComposerCanvas.tsx:420-427), so it is fully live for CHUNK documents — the headline chunk-authoring workflow. ComposerCanvas.tsx:255-261 is the same shape for pixels: `adoptPaletteLineForEmptyCells(...); setPixels(doc, atlas, writes); markOpenDirty(); bumpDoc();`. artStore (state/artStore.ts:14-22, 40) holds `open` outside the DocumentHistoryHub entirely — there is no `doc:art:` factory in state/history-factories.ts:51-81. Meanwhile `focusedHistory()` on the art facet resolves to `zoneart:<zone>` (state/editorStore.ts:195, 205-224), a populated BoundEditHistory. Failure scenario: edit a tile's pixels (records `set-tileset-tiles`), switch to the collision tool, paint 10 chunk cells, press Ctrl+Z — the collision paint stays and the pixel edit is reverted. This is the exact class canvas-history.ts's R13 note and canvasStore.ts:263-271 were written to close, still open one surface over.
FIX: Either give the composer document its own hub stack (a `doc:art:` factory + a snapshot of `{cells, collisionA, collisionB}` plus the local tile pixels), or convert stamp/collision/palette-apply into commands. At minimum, disable Ctrl+Z while an unsaved composer doc has un-commanded edits rather than letting it hit the zone-art stack.
VERDICT: CONFIRMED — I traced every link and none breaks. (1) ComposerCanvas.tsx:313-344 `applyTileCell` mutates `o.doc` via stampTile/applyPaletteLineToDocCell/paintDocCollision and ends at markOpenDirty()+bumpDoc() with no executeCommand. (2) Reachable: ArtToolDock.tsx:16-18 exposes tile-stamp/collision/palette-apply; ComposerCanvas.tsx:410-443 builds hostPointer for them; PixelViewport.tsx:214-242 routes pointer down/move/up to it; the only bail is liveTileIndex!==null (line 420), so chunk/new docs are live. (3) No `doc:art:` factory exists — history-factories.ts registers only level:, zoneart:, doc:sprite:, doc:canvas:, and grep for "doc:art" has no hits anywhere. (4) focusedDocId() (editorStore.ts:205-224, ZONE_ART_FACETS at 195) maps the art facet to zoneart:<zone>; for aeon that factory returns aeonLevelHistory() (a BoundEditHistory), which LevelWorkspace.tsx:94-107 pops on Ctrl+Z. Concrete trigger: aeon project, act tab on the art facet, open an existing chunk; pencil a pixel on an atlas-backed cell (commitWrites chunk branch, line 242, records set-tileset-tiles on zoneart:<zone>); switch to the collision tool and paint cells (buffer-only); press Ctrl+Z — the pixel edit reverts, the collision paint stays. docs/ART_SUITE.md:244 explicitly promises tile stamp is one Ctrl+Z step, so this is a gap against stated intent rather than a documented design choice. What the finding overstates: the composer doc is a detached scratch buffer, not project state — docFromChunk (core/art/composer-buffer.ts:49-66) deep-copies cells and does `new Uint16Array(chunk.collisionA/B)`, and nothing reaches project.chunkLibrary until handleSave (art-facet.tsx:103-142) issues set-tileset-tiles + set-chunk as real commands; the buffer is flagged "unsaved" with a Save button (art-facet.tsx:261-281) and both openDocumentGuarded/closeDocumentGuarded (components/art/open-document.ts:13-41) confirm before discarding, so no user work is silently lost. Also the commitWrites doc-local branch is narrower than claimed: on a chunk doc only cells with atlasTile===null fall through to setPixels (lines 213-228) — doc-local writes never overwrite existing project pixels, and the allowCow path is copy-on-write into new local tiles by design. The R13/canvasStore.ts:263-271 analogy does not transfer: there the document already had a doc:canvas: stack that one mutation bypassed; here there is no per-doc stack at all, so this is missing undo coverage on an uncommitted buffer, not a mis-routed edit. The seat's fallback fix (disable Ctrl+Z while a composer doc has un-committed edits) would itself regress the properly-commanded atlas pixel edits made in the same session.
SEV-CORR: medium — no silent loss or corruption (the doc is a detached copy, edits are flagged unsaved, and discarding is confirm-guarded) and no wrong output; the harm is that Ctrl+Z on the art facet silently pops an older zone-art command instead of the composer gesture just made, an undo-coverage/consistency defect in a shipped surface, contradicted by ART_SUITE.md:244 but not a broken main-path feature.

########## UNDO-A5 [high] Aeon map paint-tile / paint-collision drags record one undo entry per cell
file: src/renderer/components/MapViewport.tsx:1213  confidence: certain
CLAIM: The paint-tile and paint-collision drag handlers call `executeCommand` per crossed cell instead of accumulating the gesture and committing once, so one pencil drag becomes tens of undo entries and, at `MAX_HISTORY = 200`, a couple of drags evict the rest of the session's history.
EVIDENCE: MapViewport.tsx:1195-1228: inside `if (isPaintDragging.current && (tool === 'paint-tile' || tool === 'paint-collision'))`, the paint-tile branch calls `executeCommand({type:'set-tiles', entries:[{index, oldNt, newNt}]}, level)` for the single hovered tile (MapViewport.tsx:1216-1224), and the collision branch calls `paintCollisionCell(info, ...)` which itself ends in `executeCommand({type:'set-collision-edit'...})` (MapViewport.tsx:829-836). `handleMouseUp` (MapViewport.tsx:1338-1340) only clears `isPaintDragging` — there is no gesture accumulator. core/editing/history.ts:20-26 does no coalescing and history.ts:3/23 caps at 200 with `shift()`. Failure scenario: drag the tile brush across 60 tiles → 60 undo entries; 4 such drags silently drop the oldest 40 commands of the session. Contrast: classic coalesces correctly — ClassicLevelViewport.tsx:896-908 flattens a whole stamp gesture into one `classicSetLayoutCells`, and TileTab.tsx:238-256 states and enforces "ONE GESTURE = ONE classicEditTiles = ONE UNDO ENTRY".
FIX: Accumulate the drag in a ref (the `strokeRef` Map shape ChunkTab.tsx:365-402 uses) and emit one `set-tiles` / `set-collision-edit` command with all entries on mouseup, ending on the window so a release outside the viewport still commits.
VERDICT: unverified


########## UNDO-A6 [high] Undoing a sprite frame delete restores the frame but not the animation steps it re-indexed
file: src/renderer/state/spriteStore.ts:541  confidence: certain
CLAIM: `deleteFrame` records a `SpriteSnapshot` and then writes both `frames` and `steps`, but `steps` is not a snapshot field — so undo restores the frame while every timeline step that referenced a later frame stays shifted down by one, permanently mis-pointing the animation.
EVIDENCE: spriteStore.ts:541-552: `recordEdit(s);` then `const steps = s.steps.filter((st) => st.frameIndex !== removed).map((st) => (st.frameIndex > removed ? { ...st, frameIndex: st.frameIndex - 1 } : st)); set({ frames, steps, currentIndex });`. `snap()` (spriteStore.ts:282-289) and `SpriteSnapshot` (core/editing/sprite-history.ts:7-14) contain frames/currentIndex/selection/paletteMode/zoneLine/standalonePalette — no `steps`. `writeSpriteSnapshot` (spriteStore.ts:405-416) installs only those fields, so `steps` is untouched by undo. The store even documents the split at spriteStore.ts:557-560 ("steps live OUTSIDE the snapshot history") but the delete path violates it by recording a step that half-restores. Failure scenario: a 10-frame sprite with a walk cycle [0,1,2,3]; delete frame 1 (steps become [0,1,2] pointing at old frames 0,2,3); Ctrl+Z restores all 10 frames but the walk cycle now plays frames 0,1,2 — silently the wrong animation, and it will be written to `<name>_anims.asm` on export.
FIX: Add `steps` to `SpriteSnapshot`/`snap()`/`cloneSpriteSnapshot` (it is a small array; the cost argument that keeps frames deep-cloned does not apply), or make `deleteFrame` not touch `steps` and re-derive them on read.
VERDICT: unverified


########## ERR-A1 [high] Aeon save toasts "Project saved" after the export step silently failed (console.warn only)
file: src/renderer/state/aeon-save.ts:31  confidence: certain
CLAIM: When exportAct() throws, buildAeonSavePlan swallows it into a string field and aeon-save.ts reports it only with console.warn, then calls markClean() and toasts 'Project saved' — the user is told the save succeeded while every export artifact was silently omitted from the write.
EVIDENCE: src/core/project/aeon/save.ts:219-247 wraps the whole export block in try/catch and assigns `exportError = ...` instead of pushing the export files; the plan is still returned as an ordinary plan (save.ts:249). src/renderer/state/aeon-save.ts:27-30 writes plan.files, then :31 `if (plan.exportError) console.warn('[save] Export step failed (non-fatal):', ...)`, :32 markClean(), :34 addToast('Project saved','success'). Failure scenario: the artist edits an act; exportAct throws on any of its inputs; the toast says 'Project saved', the dirty dot clears, and the engine build then assembles the PREVIOUS save's export/act_descriptor.asm, entity_data.asm, vram_bases.asm and section_N.{tiles,art}.bin. The only trace is a renderer devtools warning. Every other failure in this file goes through addToast('Save failed','error') at :39 — the export half is the one exception.
FIX: Surface exportError as a toast ('Project data saved, but the engine export failed: <msg> — the files under export/ are stale') instead of console.warn, and either withhold markClean() or state the caveat, since on-disk state is now half-current.
VERDICT: CONFIRMED — I could not break the claim at any link; every cited line is verbatim and the path traces end-to-end.

1. Renderer glue (src/renderer/state/aeon-save.ts:27-35) is exactly as quoted: it writes `plan.files`, then line 31 `if (plan.exportError) console.warn('[save] Export step failed (non-fatal):', plan.exportError);`, then line 32 `markClean()`, line 34 `addToast('Project saved','success')`, line 35 `return { kind: 'saved' }`. There is no toast, no error state, no return-variant difference on the export-failure path. Every other failure routes to the catch at :36-40 with `addToast('Save failed','error')` — the export half is the sole exception, as claimed.

2. Core plan builder (src/core/project/aeon/save.ts:220-249) wraps the entire export block in try/catch; the catch at :245-246 assigns `exportError = ...` and the function returns an ordinary plan at :249. The header comment at :12-14 confirms this is a deliberate port of the old console.warn behavior into a data field, with the renderer owning the warn — i.e. the design intends exactly the silent path.

3. Consumer search: `grep -rn exportError src/` returns exactly one non-test consumer — aeon-save.ts:31 (console.warn). Nothing else reads it. No stale-export banner, no "Export" command in the Aeon UI (grep over src/renderer/components finds Export surfaces only under sprite/ and classic/), so save is the only place the Aeon export runs and the only place its failure could be reported.

4. Live wiring confirmed: src/renderer/state/project-runtime.ts:50/67/86 sets `aeonImpl = saveAeonProject`, so this is the real Aeon save path, not dead code. `markClean` really clears the dirty flag: editorStore.ts:299 `markClean: () => set({ dirty: false })`.

5. Reachability is proven by the repo's own test, not by my speculation: src/core/project/aeon/__tests__/aeon-save.test.ts:118-158 drives the REAL export path (explicitly "no mocks") into a VRAM overflow and asserts `plan.exportError` matches /VRAM overflow/, that pre-export writes survive (section_0.tiles.bin, project.json present), and that NO export outputs are in files[]. I ran it: 5/5 pass at this SHA. That test is a direct machine-checked reproduction of the half-written on-disk state the finding describes.

Concrete trigger (ordinary authoring mistakes, not contrived): an artist places a 33rd unique object type in one section (src/core/export/entity-data.ts:15 `throw new Error('Section N has X unique object types (max 32)')`), or drags an object past x=$7FF (entity-data.ts:12), or grows a section's tile union past the 1024 FG pool limit (src/core/export/vram-coloring.ts:45) — then presses Ctrl+S. The toast reads "Project saved", the dirty dot clears, the authored data is written, and export/act_descriptor.asm, entity_data.asm, vram_bases.asm and every section_N.{tiles,art}.bin remain at the previous save's contents. The message naming which object or which section overflowed — the only thing that would let the artist fix it — exists only in a renderer devtools warning the artist will never open.

Severity check: "high" is correct and not theatrical. It is NOT critical: the artist's authored work is fully written and uncorrupted; only derived export artifacts are stale. It IS high rather than medium: the export half of save is a main-path feature that silently no-ops while reporting success, and the downstream engine build consumes the stale export/ files, producing a ROM that does not match the editor — wrong output on the main path, with the diagnostic suppressed. The suggested fix (surface exportError as a toast, and either withhold markClean or state the caveat) is the right shape.


########## ERR-A2 [high] A section's objects.json/rings.json that exists but fails to parse loads as [] and the next save writes [] over it
file: src/core/project/aeon/load.ts:262  confidence: certain
CLAIM: The aeon loader cannot tell 'file absent' from 'file present but unreadable/unparseable': both land in the same bare catch and set section.objects = [] / section.rings = [], and buildAeonSavePlan then unconditionally writes those empty arrays back, permanently destroying every object and ring placement in that section.
EVIDENCE: load.ts:258-264 `try { const objRaw = await fa.read(prefix+'.objects.json'); ... JSON.parse(objText) } catch { section.objects = []; }` and :267-273 the identical shape for rings. fa.read is createIpcFileAccess.read (src/renderer/state/classic-file-access.ts:48-53), which throws ENOENT for a genuine miss but also propagates permission/EIO errors — and JSON.parse throws SyntaxError for a corrupt file — all three reach the same catch. The save side is unconditional: src/core/project/aeon/save.ts:83-91 pushes `${prefix}.objects.json` (and :87-91 rings) for every non-null section, with no 'did this load' gate. Failure scenario: a user hand-edits or a tool truncates section_3.objects.json; the project opens with zero visible objects in that section and NO error anywhere (no toast, no console line, no notice — contrast the chunk-library path, which at least warns); the next Ctrl+S writes `[]` over the file. Compare canvas-file.ts:136-141, which solves exactly this by testing /ENOENT/ on the message before treating a read failure as 'absent'.
FIX: Distinguish ENOENT from other failures the way loadCanvasFile does: on a non-ENOENT read error or a JSON.parse failure, record a load notice/warning AND mark the section as 'objects not understood' so buildAeonSavePlan omits that file from the write (do not destroy what you could not read). The same treatment is needed for the .tiles.bin branch at load.ts:196-202, whose failure silently reseeds the nametable from the baked strips and then saves that over the user's edited layout.
VERDICT: CONFIRMED — Traced end-to-end and then executed the real modules; could not refute. (1) load.ts:258-273 has bare catches setting section.objects/rings = [] with no ENOENT discrimination, no notice, no console line. (2) All three failure causes reach it: fa.read throws ENOENT for a miss, main/ipc-handlers.ts:17-27 rethrows any non-ENOENT fs error (only ENOENT becomes the resolve-marker), and JSON.parse throws SyntaxError on a corrupt file. (3) save.ts:84-92 pushes both files unconditionally for every non-null section, with no load gate; aeon-save.ts:27-30 writes every entry via window.api.writeBinaryFile, whose main handler (ipc-handlers.ts:81-93) does tmp-write + renameSync full replace with no mtime guard. (4) The path is live: useProject.ts:27 -> aeon-open.ts:22 -> aeonAdapter.open -> index.ts:115 loadAeonProject. Empirical run with the real loadAeonProject/buildAeonSavePlan and an in-memory FileAccess matching the IPC throw semantics: with section_0.objects.json truncated to '[{"id":"o1","typeId":"ring-mon', the section loads objects: [] with notices: [] and the save plan writes '[]' back to data/ojz/act1/section_0.objects.json; same for an EACCES-style read failure on both objects.json and rings.json. Concrete trigger: a section_N.objects.json containing git merge-conflict markers or a truncated hand-edit — project opens with zero objects in that section, no warning anywhere, next save overwrites the file with []. Notably the same save function already applies exactly this class of guard elsewhere (fa.exists probe for the meta sidecar at save.ts:105; opts.legacyAtlasMerged gate at save.ts:212, whose comment says a load-time parse failure must not lead to destroying data), which corroborates the finding. Partly verified only: the secondary .tiles.bin claim in the fix note — code reading of load.ts:196-202/223-224 plus save.ts:69-70 shows a silent read failure reseeds the nametable from the baked strips and saves that over the user's layout, but that variant requires actConfig.stripPath and I did not execute it.
SEV-CORR: None — high is appropriate. It is silent destruction of user work (which argues for critical), but the trigger requires a file that is already corrupt/unreadable, so the first loss precedes Aurora, and aeon project data normally lives in the engine git repo, leaving history-level recovery. Not theatrical; keep at high.

########## GUARD-A1 [high] Commit-plan block reuse ignoring collision mismatch is untested
file: src/core/art/classic-commit-plan.ts:374  confidence: certain
CLAIM: The guard that forbids reusing a visually-identical pool block whose colind differs (classic-commit-plan.ts:371-374) is pinned by no test — deleting the colind check leaves all 24 classic-commit-plan tests green.
EVIDENCE: PLANT: removed `if ((doc.collision.colind[b] ?? 0) !== colind) continue;` at classic-commit-plan.ts:374 → `npx vitest run src/core/art/__tests__/classic-commit-plan.test.ts` = 24/24 pass. planCanvasCommit is imported by exactly one test file (grep). Failure scenario: a 2C canvas commit whose region overlaps a look-alike block (same 4 tiles/flips/pals, different colind — decorative vs solid variants) reuses the wrong block id; chunk cells save cleanly and the disasm silently gets wrong collision, with no downstream defense — writeS1Level round-trips it fine. Measured S1 fact: one block can reach 55 chunks, so the blast radius is large.
FIX: Add a test: doc with two blocks identical in cells but colind 0 vs N; commit art matching that shape needing colind N (via displaced cell); assert the plan reuses ONLY the colind-matching block / mints a new block rather than reusing the colind-0 one.
VERDICT: CONFIRMED — I attempted to refute and could not; the coverage gap is real and I reproduced it end to end.

WHAT THE GUARD DOES. classic-commit-plan.ts:373-383, inside internBlock, scans doc.blocks ascending for a pool block whose 4 cells match the drawn def; line 374 skips any candidate whose doc.collision.colind[b] differs from the colind inherited from the displaced chunk cell (computed at line 413). This is a DIFFERENT mechanism from the intern-key dedup at line 367 (`...#${colind}`), which only separates blocks minted within this same commit. Line 374 is the only thing protecting reuse of a PRE-EXISTING pool block.

MUTATION SURVIVAL — VERIFIED EMPIRICALLY (repo untouched). I copied src/ + vitest.config.ts to scratchpad with node_modules symlinked, deleted line 374 there, and ran tests: baseline classic-commit-plan.test.ts = 24/24 pass; mutant = 24/24 pass. I then ran the ENTIRE suite against the mutant: 170 files passed / 1 skipped, 2293 tests passed / 1 skipped — byte-identical to baseline. So the guard is unpinned not just by the cited file but by the whole 2294-test suite. `git status --short -- src` afterwards: clean.

ONE SEAT DETAIL IS WRONG, BUT IT CUTS AGAINST THE SEAT. The evidence says "planCanvasCommit is imported by exactly one test file". False: it is imported by src/renderer/state/classicLevelStore.ts, src/renderer/components/canvas/canvas-commit-model.ts and CommitPlanView.tsx, and indirectly exercised by classicLevelStore.test.ts and canvas-commit-model.test.ts. Those suites still pass with the guard deleted, so the conclusion (zero coverage) survives the misstatement.

WHY THE EXISTING "LOOKS IDENTICAL BUT DIFFERENT COLLISION" TEST (test file :300) MISSES IT. That test paints canvasIndex(1,5), which resolves to a brand-new solid tile at pal:1, while pool blocks 1 and 3 hold tiles 1-4 at pal:0. The pool scan at :373 therefore never finds a visual match, so the assertion (two colind values in blockWrites) is satisfied purely by the intern key at :367. Deleting :374 leaves it green — which I confirmed.

BEHAVIORAL PROOF OF THE DEFECT THE GUARD PREVENTS. Concrete triggering state: doc with tile 5 = 0x55 (solid entry 5); block 1 = 4x{tile:5,pal:1} with colind 0; block 3 = identical cells with colind 7; chunk 0 = 256 cells of block 3. Commit a solid canvasIndex(1,5) canvas over target {chunkFileIndex: 0}. With the guard: chunkWrites[0] cells all reference block 3 (colind 7 preserved), blocksReused=1. Without the guard: cells all reference block 1 (colind 0) — the chunk silently loses its collision. Both runs return ok:true with no warning, so nothing downstream flags it.

THE SHAPE IS REAL IN S1, NOT HYPOTHETICAL. Using Aurora's own enigmaDecompress on /home/volence/sonic_hacks/s1disasm/map16/*.eni cross-referenced with decodeS1ColInd over collide/*.bin, groups of byte-identical blocks carrying DIFFERENT colind: GHZ 5 groups, MZ 1 group (LZ, SLZ, SYZ, SBZ 0). Example GHZ: blocks 1 and 393 share cells 4054,4854,4854,4054 with colind 255 vs 251; blocks 325/396 (255 vs 252); 326/397 (255 vs 252); 329/395 (255 vs 253). Because the scan runs ascending from b=1, block 1 is the first hit for its shape, so the unguarded path would pick exactly the wrong variant.

The proposed fix is correct, with one addition: the fixture must make the pool block visually REACHABLE (a solid pool tile plus a matching pal line), which is exactly where the existing :300 test falls short.
SEV-CORR: Downgrade high -> medium. The rubric's "high" is wrong output or a broken main-path feature, but HEAD emits correct plans: the guard at :374 is present, correct, and I verified it selects the colind-matching block. Nothing is currently wrong for a user. This is a mutation-survival/regression-risk finding: an unpinned invariant whose removal WOULD be high (silently wrong collision on a block that can reach up to 55 chunks), but the defect is latent, not live. Medium is the honest rating for "correct code, zero test pinning it".

########## UX-A1 [high] New Canvas defaults to a size that can never be committed into a level
file: src/renderer/shell/new-canvas.ts:76  confidence: certain
CLAIM: The New Canvas dialog opens at 128×128, but the commit path needs whole 256×256 chunks, so the default canvas from the app's primary art-origination flow has literally no route into the game — and the dialog never says so.
EVIDENCE: new-canvas.ts:76-80 `NEW_CANVAS_DEFAULTS = { width: 128, height: 128 }`. canvas-commit-model.ts:33-41 `canvasChunkCapacity` = `Math.floor(width/CHUNK_PX) * Math.floor(height/CHUNK_PX)` with CHUNK_PX=256, so 128×128 → total 0. CommitPlanView.tsx:72-78 renders `total===0` as 'This is smaller than one 256×256 chunk, so there is nothing to commit yet.' Driven live (run2): Explorer ▸ Canvases ▸ New Canvas… ▸ accept defaults ▸ Create → the right column's COMMIT TO LEVEL section reads exactly that sentence and offers no control at all. Same flow in run4 with 256×256 typed in → the section becomes 'COMMIT TO LEVEL / 1 chunk · into GHZ 1' with a target dropdown, a tiles/blocks/chunks report and a 'Commit 1 chunk' button. The dialog's own body copy is 'A free-size drawing surface with its own 64 colours, saved under .aurora/canvas/ in this project.' plus the profile note — 256 is mentioned nowhere. The reason the wrong default survived is a false comment: new-canvas.ts:66-67 says '128x128 is two chunks square', which is off by 4× (two chunks square is 512×512).
FIX: Set NEW_CANVAS_DEFAULTS to 256×256 (one chunk), and put the rule in the dialog beside the Size field — e.g. a live hint under Size reading '256×256 = one chunk; smaller canvases cannot be committed to a level'. Fix the false comment at new-canvas.ts:66-67 while you are there.
VERDICT: CONFIRMED — I tried to refute this on four fronts and each attempt failed on the actual code.

1. Default value — `src/renderer/shell/new-canvas.ts:76-80` really is `NEW_CANVAS_DEFAULTS = { width: 128, height: 128, profileId: 'genesis-level-art' }`, and `NewCanvasDialog.tsx:65-66,84-85` seeds both size fields from it on every open. The default profile is the LEVEL-ART one, so the default canvas is explicitly aimed at the chunk-commit path.

2. Capacity math — `src/renderer/components/canvas/canvas-commit-model.ts:33-41` `canvasChunkCapacity` is `Math.floor(w/CHUNK_PX) * Math.floor(h/CHUNK_PX)`, and `CHUNK_PX = 256` (`src/core/art/canvas-resolve.ts:34`; the classic model agrees — `core/level-classic/model.ts:73` "A 256x256 chunk"). 128/256 → 0, total 0. No rounding-up or partial-chunk branch exists anywhere on the path.

3. Is there a guard/alternate route the seat missed? No.
   - `CommitPlanView.tsx:72-78` returns early on `cap.total === 0` with only the sentence "This is smaller than one 256×256 chunk, so there is nothing to commit yet." — no target select, no plan, no Commit button (the whole `planFromSnapshot` memo is short-circuited at line 57).
   - `CanvasMode.tsx:156-158` shows `CanvasCommitSection` (→ `CommitPlanView`) as the ONLY level-bound control in the canvas pane; the pane has no export, no assign, no sprite route.
   - The canvas has no clipboard at all (`canvasStore.ts:324` records it was removed in Task 12, "nothing wrote it"), so pixels cannot be carried into another editor.
   - A canvas cannot be resized after creation (`CanvasMode.tsx:137` "a canvas cannot be resized (yet)"; `canvasStore.ts:239` names `resizeCanvasDoc` as a future action that does not exist). So the only remedy is create-a-new-canvas-and-redraw.
   - Re-importing the saved PNG does not help: `ImportSheetDialog`/`CanvasCommitSection` both feed the same `CommitPlanView`, so a 128px sheet hits the identical `cap.total === 0` early return.

4. The "false comment" sub-claim also survives: new-canvas.ts:66-67 "128x128 is two chunks square" is wrong under the constant the commit path actually uses (256 → 128×128 is a quarter chunk), and it is still wrong under the only other chunk vocabulary in the repo (docs/ART_SUITE.md defines an editor-only 128×128 chunk, under which 128×128 is exactly ONE chunk, not two square). "Off by 4×" is the right reading for the commit path.

Concrete trigger: with a project open, Explorer ▸ New Canvas… ▸ type a name ▸ accept the 128×128 / Genesis level art defaults ▸ Create. The Commit-to-level section (expanded by default — `CollapsibleSection` `defaultCollapsed = false`, ui/CollapsibleSection.tsx:30) renders only the "smaller than one 256×256 chunk" note, and no size on this document can ever change.

One nuance that cuts against the seat's framing without breaking the finding: `CommitPlanView.tsx:69-71` checks `!levelDoc || !ref` BEFORE the capacity check, so with no act open the artist sees "Open a level act to commit into" and the size rule stays hidden until an act is opened — which is exactly the sequence that makes the late discovery expensive.
SEV-CORR: medium (down from high). Nothing here produces wrong output and nothing is silently lost or corrupted: the commit path itself is correct and works the moment the canvas is ≥256×256, and when it refuses it states the exact rule ("smaller than one 256×256 chunk") in the panel that sits in the same pane the artist draws in, expanded by default. What is defective is a poor default plus a missing hint at the point of choice — a real UX bug an artist pays for by recreating and redrawing (no resize, no clipboard), but not a broken main-path feature. It is above low precisely because the recovery is redraw-from-scratch rather than a setting change.

########## UX-A3 [high] The origination canvas has no keyboard tool loop at all — no tool keys, no brush size, no colour keys
file: src/renderer/components/canvas/CanvasMode.tsx:83  confidence: certain
CLAIM: Against the owner's bar ('strong enough that an artist does not leave for Aseprite'), the canvas implements exactly two keyboard actions (undo/redo). Every one of Aseprite's core-loop keys is dead, there is no brush size at any size, and no tooltip anywhere hints that a shortcut exists.
EVIDENCE: CanvasMode.tsx:83-103 is the only window keydown handler in the pane and it matches only Ctrl/Cmd+Z, Ctrl+Y and Ctrl+Shift+Z. Tool arming is mouse-only: CanvasMode.tsx:191-206 renders the 8 ToolButtons with `onClick={() => setTool(t)}` and no key binding. Driven live (run2), with a fresh canvas focused I dispatched b, e, g, i, l, u, m and x as real Input.dispatchKeyEvent pairs and read the armed tool off the rail after each — all eight returned 'Pencil (paint pixels)'. `]` changed no text in the options bar (30-item before/after dump identical), and canvasStore.ts declares no brush-size field at all (state block, lines 97-145). `3` left paintIndex at 6 (window.__dbg.canvas.paintIndex() before and after). A scan of every button[aria-label] for a shortcut pattern returned an empty list — no label such as 'Pencil (B)' exists. What DOES work: Space-drag and middle-drag pan (use-hand-pan.ts:24-35) and anchored wheel zoom, so the gap is specifically the keyboard tool/colour loop.
FIX: Bind the eight canvas tools to single keys in CanvasMode's existing handler (guarded by the same isTextEntry check it already has), add [ / ] brush size with a size control in the options bar, and put the key in each ToolButton label ('Pencil (B)') so the binding is discoverable without documentation.
VERDICT: CONFIRMED — I tried to break the finding and could not break its facts — but its severity and one of its two sub-claims do not survive.

WHAT I INDEPENDENTLY VERIFIED (every factual assertion holds):

1. CanvasMode.tsx:83-103 is the pane's only keydown registration, and it matches exactly three chords: `(ctrl||meta)+z` without shift → undo (line 91), `(ctrl||meta)+y` or `(ctrl||meta)+shift+z` → redo (line 96). No other branch exists. Nothing bare-key.
2. Tool arming is mouse-only. CanvasMode.tsx:195-203 maps the eight TOOLS through `ToolButton` with `onClick={() => useCanvasStore.getState().setTool(t)}` and no key path. ui/primitives.tsx:55-64 shows ToolButton renders `title={label}`/`aria-label={label}` verbatim, and the eight labels at CanvasMode.tsx:181-188 ('Pencil (paint pixels)', 'Eraser (paint index 0)', …) carry no key hint — so the seat's "empty aria-label shortcut scan" is structurally guaranteed, not a sampling accident.
3. I checked every other `addEventListener('keydown', …)` in the renderer (14 sites) for a handler that could arm a canvas tool from a key. None can. App.tsx:104 short-circuits with `if (!(e.ctrlKey || e.metaKey)) return;` before its s/b/1-9 branches, so bare `b`, `e`, `g`, `l`, `u`, `m`, `x`, `3` reach nothing there — this is the guard that would have refuted the finding, and it does the opposite: it proves the bare keys are unclaimed rather than shadowed. MapViewport.tsx:671-679 is the ONE surface with letter tools (v/s/o/r/t/b/c/k/m), and it bails at line 532 on `!levelKeysEnabled()`, which is false while a canvas tab is active — so those letters are inert here too. CommandPalette is Ctrl+K only, and shell/commands.ts:45-113 builds no tool commands, so there is not even an indirect keyboard route to a tool.
4. No brush size exists anywhere in the stack, not merely in the pane: canvasStore.ts:96-145 declares tool/zoom/mirror/pixelPerfect/dither/paintIndex/visibleGrids/constraints and no size field, its setter list (147-155) has no `setBrushSize`, and the shared `core/art/pixel-edit-controller.ts` has no radius/size concept at all — every stamp is one pixel (setPx at line 34, single-point plot at line 120). So `[`/`]` have nothing to bind to.

WHY THE SEVERITY IS WRONG. Against the panel rubric (high = wrong output or broken main-path feature), nothing here is broken and nothing is wrong. All eight tools arm and work by click, colour arms from the palette grid, and the seat itself confirms space/middle-drag pan and anchored wheel zoom work. This is an absent convenience layer, not a defect: no user work is lost, no output is wrong, no main path fails.

WHY THE FRAMING OVERSTATES. The title says "the origination canvas has no keyboard tool loop", implying a surface-specific deficiency. It is app-wide: SpriteMode.tsx:99-119, the mature sibling pixel editor, binds only undo/redo and Ctrl+C/X/V, and ComposerCanvas.tsx:~642-677 the same. No pixel surface in Aurora has ever had tool keys, so the canvas is at parity with its siblings — there is no regression and no inconsistency between the two pixel editors to point at.

WHERE THE FINDING IS PARTLY REFUTED. The "no brush size at any size" half contradicts an explicit, documented scope decision. The governing spec, docs/superpowers/specs/2026-08-15-in-app-art-authoring-design.md §6 Risk 5, says: "Scope creep toward 'our own Aseprite.' The differentiator is being Genesis-aware, not being a general pixel editor. Layers, brushes and onion skinning stay out until Phase 2 ships and the gap is felt rather than assumed." Brushes are deferred by design, so reporting their absence as a defect argues against the decision on record rather than finding a bug. The seat's bar ("strong enough that an artist does not leave for Aseprite") is an aspirational note, and the spec of record explicitly declines to meet it this phase.

WHAT SURVIVES AS ACTIONABLE. Single-key tool arming is neither implemented nor explicitly deferred anywhere in the specs I searched — a genuine, cheap, uncontested enhancement (the bare-key namespace is provably free, per App.tsx:104), worth doing with the key printed in each ToolButton label. That is a low-severity polish item, not a high-severity defect.
SEV-CORR: low (down from high). Verified factually accurate but it is a missing enhancement, not a defect: no wrong output, no data loss, no broken main path — all eight tools, the palette, undo/redo, pan and zoom work; only the keyboard convenience layer is absent, and it is absent identically on every other pixel surface in the app (SpriteMode.tsx:99-119, ComposerCanvas). The brush-size component should be dropped from the finding entirely: docs/superpowers/specs/2026-08-15-in-app-art-authoring-design.md §6 Risk 5 defers brushes by explicit design decision ("Layers, brushes and onion skinning stay out until Phase 2 ships"). What remains worth filing is the single-key tool binding plus discoverable labels.

########## PERF-A1 [high] Collision overlay issues one beginPath+stroke per 1-px column — ~92k–440k stroke() calls per frame on GHZ1
file: src/renderer/components/classic/classic-overlays.ts:107  confidence: certain
CLAIM: drawCollision draws each 16-px cell's surface line as 16 separate beginPath/moveTo/lineTo/stroke calls, so turning on the collision overlay makes the classic level viewport issue hundreds of thousands of individual canvas stroke ops per repaint; the identical pattern is in aeon's OverlayRenderer.
EVIDENCE: classic-overlays.ts:98-119 — the second pass loops all 256 chunk cells, and for each solid cell loops 16 columns issuing `ctx.beginPath(); ctx.moveTo(...); ctx.lineTo(...); ctx.stroke()` (lines 114-117) for a ONE-PIXEL segment. Worst case 256*16 = 4096 stroke() calls per chunk. The first pass (lines 64-94) similarly issues up to 4096 1x N `fillRect`s per chunk. Call site: ClassicLevelViewport.tsx:485-493 calls drawCollision once per cell in `visibleChunkRange`, inside the dep-less render effect (ClassicLevelViewport.tsx:431-595) that re-runs on every rAF redraw during a pan. Math on real data: /home/volence/sonic_hacks/s1disasm/levels/ghz1.bin header bytes are 47,4 → fg grid 48x5 = 240 chunk cells. At the fit zoom (5*256=1280 world px into an ~800px canvas → zoom ≈0.625) the visible range is ~9x5 = 45 chunks; at viewStore's minimum zoom 0.125 (viewStore.ts:83) the world view is 11200x6400 px = 43x25 chunks, clamped by the grid to 43x5 = 215 chunks. At an assumed 50% solid-cell density that is 45*2048 ≈ 92,000 stroke() calls per frame at fit zoom and 215*2048 ≈ 440,000 at min zoom, plus an equal number of 1-px fillRects. Software-canvas stroke() (this canvas is deliberately `willReadFrequently`, ClassicLevelViewport.tsx:440) is ~1us each, so a single frame is 100ms–500ms — the rAF queue never catches up and panning with collision on runs at single-digit fps. `ctx.getTransform()` (classic-overlays.ts:88) additionally allocates a DOMMatrix per solid cell when showCollisionAngles is on: 215 chunks * 256 cells = up to 55,000 DOMMatrix allocations per frame. Aeon has the same shape at OverlayRenderer.ts:214-223 (16 separate beginPath/stroke per cell), reached per mousemove because MapViewport is not rAF-coalesced (see A3). Only the default hides it: viewStore.ts:68 has showCollision:false, so this is a one-click regression, not a load-time one.
FIX: Batch into one path: hoist `ctx.beginPath()` outside both 16-column loops, emit all moveTo/lineTo pairs for the whole chunk (they share one strokeStyle/lineWidth), and issue a single `ctx.stroke()` per chunk — Skia handles a 4096-segment path far better than 4096 strokes. Same in OverlayRenderer.ts:214-223. Also hoist `1 / ctx.getTransform().a` out of the per-cell branch at classic-overlays.ts:88 (it is constant for the whole call — the caller already has `invZoom`).
VERDICT: REFUTED — The code pattern is real (classic-overlays.ts:107-118 issues one beginPath/moveTo/lineTo/stroke per 1-px column, called per visible chunk from ClassicLevelViewport.tsx:485-493), but the finding's quantitative claim and its user-visible conclusion both break.

(1) Density. The seat "assumed 50% solid-cell density" = 2048 strokes/chunk. I decoded the real data (s1disasm map256/GHZ.kos, collide/GHZ.bin, Collision Array (Normal).bin, levels/ghz1.bin) and ran the exact loop body with the same columnSolidRun and the same early-outs the seat stepped over — `cell.solidity === 0` (line 100) and `shapeIndex === 0` (line 102) and `run === null` (line 111). Measured GHZ1 FG: 160 stroke-eligible columns per chunk on average, 2,447 solid cells and 38,499 strokes for the ENTIRE 48x5 plane; worst 9x5 fit-zoom window = 16,725. So the claimed 92,000 (fit) is 5.5x over and 440,000 (min zoom) is unreachable — the whole level contains only 38.5k.

(2) Cost per stroke. Benchmarked in the actual runtime (Electron/Chromium software canvas with willReadFrequently:true, same scale(zoom) transform and lineWidth=1/zoom, flushed with getImageData): 16,725 unbatched 1-px strokes = 2.5-3.2 ms; 38,499 = 6.5 ms. That is ~0.19us/stroke, ~5x under the assumed 1us. Batched into one path: 0.86 ms / 1.82 ms. So the fix saves ~1-5 ms per frame, not the claimed 100-500 ms, and "panning with collision on runs at single-digit fps" on GHZ1 does not happen. redraw() is rAF-coalesced (ClassicLevelViewport.tsx:181-184), so it is one pass per displayed frame.

(3) The DOMMatrix sub-claim is overstated identically: ctx.getTransform() at line 88 runs once per SOLID cell, and GHZ1's whole plane has 2,447 solid cells, not 55,000; at a measured 0.31us/call that is 0.76 ms with angles on.

The only scenario approaching the claimed severity is a much denser act than the one cited — LZ1 measures 174,290 strokes plane-wide (80,744 in a fit-zoom window), ~44 ms in the same bench. Worth batching, but that is a different level and still an order of magnitude below the stated 100-500 ms.

The proposed fix (hoist beginPath, one stroke() per chunk; hoist 1/ctx.getTransform().a since the caller already has invZoom) is correct and cheap — this is a legitimate micro-optimization filed with a fabricated failure mode and "certain" confidence.
SEV-CORR: low (medium at most). No wrong output, no data loss — the overlay renders correctly; this is a per-frame cost behind a default-off toggle (viewStore.ts:68 showCollision:false). Measured worst case on the cited level is ~3 ms at fit zoom and ~6.5 ms for the whole plane, well inside a 16 ms frame. "high = wrong output or broken main-path feature" is not met; the batching fix is a cleanup, not a bug fix. Only a dense act (LZ1) at extreme zoom-out reaches jank territory (~44 ms), which would justify medium at best.

########## PERF-A2 [high] cappedZoom bounds the canvas EDGE, not its area — 5 wheel notches on the Chunk tab allocates a ~1 GB backing store
file: src/renderer/components/art-shared/zoom-cap.ts:33  confidence: certain
CLAIM: The per-tier zoom change moved the chunk tier's DEFAULT off 24 but added no area bound, so wheel-zooming the Chunk tab still reaches the 151 MB backing store the fix was written to eliminate after 3 notches, and ~1.008 GB at the ceiling.
EVIDENCE: cappedZoom (zoom-cap.ts:33-35) clamps only `contentPx * zoom <= 16000` on the LARGER axis; nothing anywhere bounds width*height*4 (`grep -rn 'maxArea|MAX_AREA|width \* height \* 4' src/renderer src/core` returns only SectionRenderer.ts:93). A classic chunk surface is 32x32 cells of 8 px = 256x256 art px (classic-surface-buffer.ts:110 `const cellsX = 32, cellsY = 32`, composeFrom width = cellsX * TILE_PX at :66). ChunkTab.tsx:195 passes that through cappedZoom, and PixelViewport.tsx:254-255 sets `width = tilesX * width * zoom`. artStore.ts:115-116 `setZoom` clamps to 2..64 for EVERY tier — there is no per-tier ceiling, only a per-tier default (artStore.ts:86 `chunk: 3`). useAnchoredZoom uses factor 2 (use-anchored-zoom.ts:50), so from 3 the wheel reaches 6,12,24,48,64. Backing-store math: zoom 24 → 6144^2*4 = 151 MB (artStore.ts:78-79 names exactly this number as 'a real defect'); zoom 48 → 12288^2*4 = 603 MB; zoom 64 → capped to floor(16000/256)=62 → 15872^2*4 = 1,007,681,536 bytes. Chromium will either fail the allocation (canvas comes back BLANK — the failure mode zoom-cap.ts:1-8 exists to prevent) or drive the renderer to OOM. Aeon's composer is the same class: a 16x16-tile chunk doc (128 px) with the 3x3 repeat preview gives cappedZoom(64, 384)=41 → 15744^2*4 = 991 MB (ComposerCanvas.tsx:157, PixelViewport.tsx:254).
FIX: Add an AREA bound to cappedZoom — e.g. `maxPixels` (default ~64e6 ≈ 256 MB) so it returns `min(edgeCap, floor(sqrt(maxPixels / (w*h))))` — and/or give ART_TIER_DEFAULT_ZOOM a per-tier MAX alongside the default (chunk maxes at ~16, tile at 64) enforced in artStore.setZoom rather than only at the draw site.
VERDICT: REFUTED — The arithmetic is right but the finding is misattributed and its harm is unverified.

TRACED AND TRUE: cappedZoom (zoom-cap.ts:33-35) bounds only the edge (min(zoom, floor(16000/contentPx))), with no area term. buildChunkSurface -> composeFrom(...,32,32) (classic-surface-buffer.ts:108-110, :66-68) yields a 256x256 buffer; ChunkTab.tsx:195 passes 256; ChunkTab.tsx:472-488 mounts PixelViewport with no `repeat` so tilesX/tilesY=1, and PixelViewport.tsx:254-255 sizes the backing store to 256*zoom square. artStore.setZoom (artStore.ts:115-117) clamps 2..64 for every tier, and both use-anchored-zoom.ts:96 and ArtToolOptions.tsx:266 double the STORE value, so chunk 3 -> 6 -> 12 -> 24 -> 48 -> 64, drawn at 62, i.e. 15872^2 ~= 1.008 GB.

WHERE IT BREAKS (1) — not a defect of the change, and the change strictly improved it. `git show aebfa67 -- src/renderer/state/artStore.ts` shows the clamp is byte-identical pre-existing code: `- setZoom: (zoom) => set({ zoom: Math.max(2, Math.min(64, zoom)) })`. `git show aebfa67^:src/renderer/components/classic/ChunkTab.tsx` lines 181/193/200 show the chunk paint surface already read that shared zoom through the same cappedZoom call, with the same 16000px ceiling comment. Pre-change the shared default was 24, so the chunk tab hit the 6144px/151 MB canvas in ZERO notches and reached 62 in TWO; post-change it takes THREE and FIVE. The finding's headline ("still reaches the 151 MB backing store the fix was written to eliminate after 3 notches") inverts the direction of the change: the reported defect (artStore.ts:75-83, commit aebfa67) was OPENING absurdly zoomed, which is fixed and CDP-verified. The diff introduced no guard, removed none, and did not widen the ceiling.

WHERE IT BREAKS (2) — the terminal failure is asserted, not measured, and the cited failure mode does not apply. 15872^2 = 251,918,848 px is BELOW Chromium's canvas-area limit (32768*8192 = 268,435,456), and 15872 is below both the 16384 GPU texture limit and Skia's 32767 dimension limit — which is exactly why MAX_CANVAS_PX is 16000. So "the canvas comes back BLANK, the failure mode zoom-cap.ts:1-8 exists to prevent" is not what this path trips; whether the 1 GB raster allocation itself fails is unverified in both directions, and no harness was run (this repo's convention for runtime-only claims is a CDP harness, e.g. scratchpad/tier-zoom-harness.mjs).

The residual idea — an area bound in cappedZoom, or a per-tier max — is a legitimate hardening suggestion against pre-existing code, but it is not a defect introduced or left unfixed by the reviewed change.
SEV-CORR: Downgrade high -> low (nit/hardening). No user work is lost or corrupted and no main-path feature produces wrong output: all four tier defaults now render at 192-768 px, and reaching the alleged 1 GB canvas requires a user to deliberately zoom a 256px chunk to 62 screen px per art pixel (about four art pixels visible in the viewport) — behavior identical to, and harder to reach than, what shipped before the reviewed change.

########## PERF-A3 [high] MapViewport repaints per mousemove with no rAF coalescing, re-allocating the canvas backing store and forcing layout each time
file: src/renderer/components/MapViewport.tsx:430  confidence: certain
CLAIM: Aeon's level viewport subscribes to vpX/vpY/zoom as zustand selectors and does a full recompose in a dep-driven effect, so a pan drag issues one complete redraw per mousemove (hundreds/sec on a high-poll mouse) — each of which calls getBoundingClientRect and reassigns canvas.width/height — the exact pattern its classic sibling documents as the redraw storm it removed.
EVIDENCE: MapViewport.tsx:122-124 subscribes `vpX`, `vpY`, `zoom` via selectors; handleMouseMove calls `pan(dx, dy)` on every mousemove while dragging (MapViewport.tsx:1257-1262), and object drags call `bumpLiveEdit()` per move (:1252) — `liveEditVersion` is also in the deps. The redraw effect at :430-476 lists vpX, vpY, zoom, liveEditVersion in its dep array, so it runs once per store tick with NO requestAnimationFrame anywhere in the file (`grep -n requestAnimationFrame MapViewport.tsx` → no hits). Inside it: `container.getBoundingClientRect()` (:438) forces a synchronous layout, then `canvas.width = rect.width; canvas.height = rect.height` (:439-440) — the standard clear-the-canvas idiom, i.e. it reinitializes the bitmap even when the size is unchanged: ~4.5 MB alloc+zero for a 1400x800 canvas, per mousemove. Then a full-canvas fillRect + N section drawImages + overlayRenderer.render (:462-472), and overlayRenderer's collision path is A1's per-column stroke loop (OverlayRenderer.ts:214-223). ClassicLevelViewport.tsx:160-199 does the opposite deliberately, and says so: 'Written ONLY by the measure/ResizeObserver path ... the render pass reads it and never resizes the canvas (assigning width/height reinitializes the backing store) nor forces layout via getBoundingClientRect, so drags stay cheap', with rAF coalescing at :181-199 and a guard test at classic-camera.test.ts:9-13. MapViewport has neither half. At 500 events/s that is ~2.2 GB/s of backing-store alloc+memset, 500 forced layouts/s, and ~490 of every 500 recomposites discarded before paint.
FIX: Port ClassicLevelViewport's shape: hold the camera in a ref, publish to viewStore once per painted frame from inside a single rAF, adopt external writes with an imperative `useViewStore.subscribe`, and move the canvas.width/height assignment into the existing ResizeObserver path (:479-520) so the render pass never resizes or measures.
VERDICT: CONFIRMED — I traced the whole path and could not break it. Trigger: open an aeon level tab with an act loaded, pick the `view` tool (or hold middle mouse) and drag. `handleMouseMove` (MapViewport.tsx:1155) falls through to the pan branch and calls `pan(dx, dy)` at :1259 on every dispatched mousemove; `isDragging.current` is armed at :885 (tool==='view' || e.button===1) and at :942 (select-tool click on empty space). viewStore.ts:77-80 `pan` sets new vpX/vpY unconditionally — no equality guard, no throttle. The redraw effect's dep array (MapViewport.tsx:476) contains vpX, vpY, zoom, liveEditVersion, so it re-runs per store tick, and there is no requestAnimationFrame anywhere in the file (grep confirms zero hits). Inside the effect: container.getBoundingClientRect() at :438, then UNGUARDED `canvas.width = rect.width; canvas.height = rect.height` at :439-440 (no `if (canvas.width !== w)`), then the full fillRect + culled drawImages + overlayRenderer.render. No guard the seat missed.

The classic contrast is quoted accurately: ClassicLevelViewport.tsx:159-163 holds the sizeRef comment verbatim, :182-199 is the rAF coalescer, :204-215 the imperative useViewStore.subscribe adopt path, and classic/__tests__/classic-camera.test.ts guards all three. The only thing resembling a defense of MapViewport is that test file's header comment ("a performance bug HERE and not there"), whose stated reason is that classic's render effect has no dep array — that reason does not apply, because MapViewport's dep array *contains the camera fields*, so its effect re-runs on exactly the same ticks. The comment is wrong, not a mitigation.

Two corrections to the seat's evidence. (1) UNDERSTATED: the ResizeObserver effect at :479-520 also lists vpX, vpY, zoom, liveEditVersion in its deps (:520), so every pan mousemove disconnects and re-constructs the observer; a newly observed element always receives an initial notification, and that callback (:484-518) does its own canvas.width/height assignment plus a second complete renderBg/render/overlayRenderer.render. A drag frame therefore costs roughly TWO full composites plus observer churn, which the seat never mentions. (2) OVERSTATED: the arithmetic (500 events/s, ~2.2 GB/s, 500 forced layouts/s, ~490 of 500 composites discarded pre-paint) does not hold in Electron — Chromium coalesces mousemove in the main-thread event queue and dispatches at most one per BeginFrame, so a high-poll mouse produces ~refresh-rate listener calls, not hundreds-to-thousands, and essentially nothing is discarded before paint. The composite is also cheaper than implied: SectionRenderer.render (SectionRenderer.ts:386-412) frustum-culls and drawImages cached 2048px section canvases — a few blits, not a tile recompose.

The proposed fix is the right shape, but it must also strip vpX/vpY/zoom from the ResizeObserver effect's deps, otherwise the per-frame churn is merely relocated.
SEV-CORR: Downgrade high -> medium. Nothing here produces wrong output or loses user work: pans, object drags and the composite all render correctly, and the redundant backing-store reset is invisible because the pass repaints the full canvas anyway. What is real is redundant per-frame work on a main-path gesture (~2 full composites instead of 1, plus one forced layout, one backing-store reinit and one ResizeObserver rebuild per frame). Real cleanup with a known-good sibling implementation to copy, but a perf/quality finding, not "wrong output or broken main-path feature" — and the seat's headline numbers, which are what make it read as high, are inflated by roughly an order of magnitude by Chromium's per-frame mousemove coalescing.

########## PERF-A4 [high] BlockTab/ChunkTab thumbnail strips still key on the coarse chunkEpoch — one block-cell click repaints all ~1400 thumbnails
file: src/renderer/components/classic/BlockTab.tsx:337  confidence: certain
CLAIM: The per-entity thumbnail invalidation that TileTab was fixed to use never landed on BlockTab or ChunkTab: both pass `String(chunkEpoch)` as the versionKey for their whole strips, so a single block edit repaints all 439 BlockThumbs AND all ~965 TileThumbs — including the tile strip, which by the store's own reasoning a block edit cannot invalidate.
EVIDENCE: BlockTab.tsx:337 `const versionKey = String(chunkEpoch);` is passed to every BlockThumb (:455) and every TileThumb (:469); ChunkTab.tsx:431/:543 does the same for its 439 BlockThumbs. TileThumb/BlockThumb repaint whenever versionKey changes (composer-thumbs.tsx:59-64 and :89-93 have versionKey in their effect deps). A block edit commits `commitArt(newDoc, { blocks: true }, { kind: 'all' })` (classicLevelStore.ts:843), and applyCommit bumps chunkEpoch for kind:'all' (classicLevelStore.ts:653-655) — so every BlockTab click invalidates both strips. TileTab was explicitly fixed for this and says so at TileTab.tsx:386-391: 'Keying the whole strip on chunkEpoch repainted all 965 thumbnails on every committed pencil stroke; this repaints the one tile the stroke actually wrote', using `versionKeyFor(id) = `${paletteEpoch}:${tileVersions.get(id) ?? 0}``. Worse, applyCommit's own comment at classicLevelStore.ts:663-665 states 'a block edit bumps NEITHER [paletteEpoch nor tileEpoch]: no object sprite and no tile thumbnail bakes block data, so a block edit must invalidate none of them' — BlockTab defeats that by feeding chunkEpoch to TileThumb anyway. Cost per click: 439 renderBlock + 965 renderTile, each ending in drawBufferScaled, which creates a throwaway <canvas> element per call (see A5) → ~1404 canvas element creations, contexts, ImageData allocations and scaled blits per click.
FIX: BlockTab's tile strip can be fixed today: use TileTab's `versionKeyFor(id)` (`${paletteEpoch}:${tileVersions.get(id) ?? 0}`) for the TileThumbs at BlockTab.tsx:469 — the store already publishes both clocks. For the block strips, add a `blockVersions: Map<number, number>` clock alongside `chunkVersions` in classicLevelStore (bumped from the same `nextVersion()` allocator by classicEditBlock / the surface plan's blockWrites) and key BlockThumb on `${paletteEpoch}:${blockVersions.get(id) ?? 0}:${tileEpoch}`.
VERDICT: CONFIRMED — Traced end to end and could not break the path. (1) BlockTab.tsx:337 `const versionKey = String(chunkEpoch);` (chunkEpoch subscribed at :84) is passed to every BlockThumb (:455, over `doc.blocks.map` — 439 on GHZ) and every TileThumb (:469, over `Array.from({length: tileCount})` where `tileCount = doc.tiles.length/32` = 965); neither strip is virtualized. (2) composer-thumbs.tsx:59-64 (TileThumb) and :89-93 (BlockThumb) both have `versionKey` in the draw-effect deps and are memoized on primitives, so a changed versionKey re-renders AND re-runs drawScaled -> drawBufferScaled, which creates a throwaway <canvas> + context + ImageData per call (composer-shared.tsx:230-237). (3) The commit side matches: BlockTab's editCell (:276-289) calls classicEditBlock -> commitArt(newDoc, {blocks:true}, {kind:'all'}) (classicLevelStore.ts:843) -> applyCommit :653-655 sets chunkEpoch = nextVersion() for kind:'all', while :667-676 deliberately leaves paletteEpoch/tileEpoch/tileVersions untouched for a block edit ("a block edit must invalidate none of them"). So BlockTab defeats the store's own reasoning by feeding chunkEpoch to 965 TileThumbs. (4) TileTab genuinely was fixed and BlockTab/ChunkTab were not: TileTab.tsx:391 uses `${paletteEpoch}:${tileVersions.get(id) ?? 0}` at :510, while grep shows BlockTab :337/:455/:469 and ChunkTab :431/:543 still on String(chunkEpoch). Concrete trigger: GHZ act 1, Block tab Assign mode, click any tile in the Tiles strip (or a flip/priority/palette chip) -> classicEditBlock -> chunkEpoch bump -> 439 renderBlock + 965 renderTile, each with a fresh canvas + scaled blit; likewise on every committed stroke in Block-tab Paint mode (classicPaintSurface also commits {kind:'all'}).

Three corrections that do not refute it: (a) the title's literal trigger is wrong — onCellClick (:305-313) only sets selection and commits nothing (it does change cell.pal, which re-keys the tile strip through the separate `palLine` prop, independent of this bug); the committing triggers are the assign/flip/palette edits and paint strokes. (b) The block-strip half is NOT a straight port of TileTab's fix: chunkEpoch is currently the only published signal that a block changed, so BlockThumb's coarse key is load-bearing for correctness until the proposed `blockVersions` clock exists — only the tile strip at :469 is fixable today, exactly as the fix note says. (c) ChunkTab's exposure is smaller than implied: chunk-cell edits commit {kind:'chunk', id} (classicLevelStore.ts:823), which bumps chunkVersions and not chunkEpoch, so ChunkTab's 439 BlockThumbs do not repaint while painting chunk cells — only on block/tile/palette commits and its own paint-mode strokes.
SEV-CORR: medium — output is always correct (the coarse clock over-invalidates, never under-invalidates); nothing is lost, corrupted, or misdrawn, and no feature is broken. The 9-second freeze cited in composer-thumbs.tsx:14-32 came from React DEV deep-diffing the `doc` OBJECT prop, which no longer applies here since every thumb prop is a primitive. What is left is ~1404 redundant small rasterizes (8x8 / 16x16) plus temp-canvas blits per commit — a per-stroke hitch in Block-tab paint/assign, i.e. a real efficiency defect but not "wrong output or broken main-path feature".

########## STATE-A1 [critical] Aeon Ctrl+S marks the WHOLE project clean while writing only the current act's sections
file: src/renderer/state/aeon-save.ts:33  confidence: certain
CLAIM: `editorStore.dirty` is a single project-wide flag set by every aeon command in every act, but `saveAeonProject` serializes only the CURRENT act's sections and then calls `markClean()` — so edits made in any other act are never written and the app reports itself fully saved.
EVIDENCE: editorStore.ts:243 declares one `dirty: boolean` for the whole aeon project; executeCommand (editorStore.ts:396) and executeAmbientCommand (editorStore.ts:474) both call `markDirty()` regardless of which act the command targeted, and commands mutate `project.zones[].acts[].sections` in place (projectStore.ts:129 getActiveLevel hands the live section array to the history). An aeon act switch is a pure pointer move — tab-activation/level.ts:165 calls only `setCurrentAct(plan.zone, plan.act)`; there is no save, no reload, no per-act dirty bookkeeping. aeon-save.ts:24 then passes `currentZoneId, currentActId` into buildAeonSavePlan, and core/project/aeon/save.ts:51-62 resolves exactly ONE act and loops `act.sections` — only that act's `.tiles.bin` / `.collattr.bin` / `.objects.json` / `.rings.json` are pushed (the only project-wide loop, save.ts:136, writes zone TILESETS, not section data). aeon-save.ts:33 then calls `useEditorStore.getState().markClean()`. Failure scenario: open an aeon project with two acts; on act1 stamp chunks / move an object (dirty=true); click act2's tab; press Ctrl+S. buildAeonSavePlan writes act2's section files only; markClean() sets dirty=false. dirty-tabs.ts:41 (`if (s.aeonOpen) return s.aeonDirty`) drops the dot from every level tab, project-open-guard.ts:58 reads aeonDirty=false so opening another project proceeds with no confirm, and act1's edits are gone. The current-act restriction is not defensive either — save.ts:55 THROWS for a missing act, so it cannot have been intended as a per-act filter over a whole-project write.
FIX: Either make the save whole-project (iterate every zone/act that has resident edits before markClean), or make dirtiness per-document: track a dirty set keyed by the same `level:<zone>:<act>` / `zoneart:<zone>` doc ids the DocumentHistoryHub already uses, have executeCommand/executeAmbientCommand mark the doc they routed to (they already resolve it — editorStore.ts:389/467), and have saveAeonProject clear only the act it actually wrote. dirty-tabs.tsx's aeon branch then becomes per-tab instead of `return s.aeonDirty`.
VERDICT: CONFIRMED — I independently traced every link and none breaks. (1) load.ts:317-335 loads EVERY act of EVERY zone into the resident S4Project, so all acts' sections are live in memory. (2) projectStore.ts:75 `setCurrentAct` is a pure pointer move — no reload, no per-act dirty state; tab-activation/level.ts:54,165 confirms the aeon branch does nothing else (its header comment even asserts act switches are "always safe" because the project is resident). (3) projectStore.ts:129-140 hands the LIVE `act.sections` array to the history and core/editing/history.ts:97-120 mutates `section.tileGrid.nametable` / `section.objects` in place, so act1's edits persist on the resident project after switching to act2. (4) editorStore.ts:242,298-299,396,474 — a single project-wide `dirty` flag that both executeCommand and executeAmbientCommand set regardless of target act. (5) project-runtime.ts:154-167 routes Ctrl+S (and Save All) on any aeon level tab to saveAeonProject; no other writer emits section files. (6) core/project/aeon/save.ts:51-62 resolves exactly ONE act (and THROWS on a missing one, so it is not a defensive filter) and loops only `act.sections`; the sole project-wide loop at save.ts:136 writes zone TILESETS, not section data. (7) aeon-save.ts:32 calls markClean() unconditionally. (8) Downstream is as claimed: dirty-tabs.ts:44 (`if (s.aeonOpen) return s.aeonDirty`) clears the dot on every aeon level tab, and shell/project-open-guard.ts:49,58 reads that same flag, so `planProjectOpen` returns 'proceed' and opening another project replaces the resident project with no confirm — act1's unwritten edits are gone. Triggering state: an aeon project.json with two acts in a zone; edit act1 (stamp tiles / move an object), click act2's tab, Ctrl+S (writes act2's section_*.tiles.bin etc. only, then dirty=false), then open another project or quit. NOTE: verification was read-only — the Bash tool died partway through and never recovered, so I could not run the existing vitest suites; every cited line was read directly.
SEV-CORR: Downgrade critical → high. The mechanism is real and the fix is warranted, but it is LATENT at this SHA: /home/volence/sonic_hacks/aeon/project.json — the only aeon project that exists (no games/*/project.json) — declares one zone with one act, and with a single act buildAeonSavePlan writes everything resident, so no user work is currently lost. "critical = silent loss of user work" requires a reachable losing state; here it requires a multi-act (or multi-zone) project.json that does not yet exist. Multi-act is clearly a designed configuration, not an accident (shell/tabs.ts:100 "a palette edit made from act 1 is undoable from act 2"; session-lifecycle.ts:47 emits a tab per act), so this flips to critical the moment a second act is authored. One link I could not verify because Bash was unusable: whether the Project Setup tab lets a user add an act from inside Aurora — if it does, the bug is one UI action from reachable and the critical rating would be defensible.

########## STATE-A2 [critical] artStore.open (the aeon composer document) is in no project-lifecycle teardown and no dirty guard — unsaved art is destroyed silently on project open
file: src/renderer/shell/project-open-guard.ts:55  confidence: certain
CLAIM: A new/unsaved composer document with painted pixels is dirty only on `artStore.open.dirty`, which `currentOpenDirtySnapshot()` does not read and `endDocumentSession()`/`resetProjectRuntime()` do not tear down — so opening another project neither asks nor warns, and the aeon open path then overwrites the document outright.
EVIDENCE: A New Tile/Block/Chunk document is created with `chunkId: null, liveTileIndex: null` (art-facet.tsx:36-67). Painting it takes ComposerCanvas.tsx's doc-local branch (ComposerCanvas.tsx:255-261): `setPixels(doc, ...)` then `markOpenDirty()` only — NO command is executed, so editorStore.dirty stays false (contrast the chunk-doc branch, ComposerCanvas.tsx:242, which does executeCommand). The work therefore lives solely in `artStore.open` (artStore.ts:122). currentOpenDirtySnapshot (project-open-guard.ts:55-62) reads classicLevelStore.dirty, editorStore.dirty, anySpriteDocDirty(), dirtyCanvasDocIds() — I grepped project-open-guard.ts, dirty-snapshot.ts and project-runtime.ts for `artStore`: zero hits. So planProjectOpen returns `{kind:'proceed'}` and useProject.ts:17 proceeds with no dialog. endDocumentSession (project-open-guard.ts:88-91) closes sprite + canvas documents and not this one, despite its own docblock claiming 'The next document type is added here once'. resetProjectRuntime (project-runtime.ts:223-227) is the same. Failure scenario: aeon project A open, Art facet, New Chunk, paint for ten minutes (no Save clicked), File > Open aeon project B. No confirm appears; openAeonProject then calls `useArtStore.getState().openDocument({doc: docFromChunk(first), ...})` at aeon-open.ts:86, replacing the painted document — the pixels are unrecoverable. Corollary: on the aeon->classic path nothing replaces it, so the document survives into the new project, which falsifies ArtToolOptions.tsx:194's stated invariant '`open` is aeon's open document and is ALWAYS null under a classic open'.
FIX: Add `artDirty: useArtStore.getState().open?.dirty === true` to OpenDirtySnapshot/planProjectOpen (project-open-guard.ts:44/49) so the confirm fires, and add `useArtStore.getState().closeDocument()` to endDocumentSession (project-open-guard.ts:88) and resetProjectRuntime (project-runtime.ts:223) so the document dies with its project on every path. The 'discard' branch (line 165) needs the close too.
VERDICT: CONFIRMED — Traced end-to-end and could not break any link. (1) art-facet.tsx (actual path src/renderer/workspace/facets/art-facet.tsx, not shell/) handleNewTile/Block/Chunk open a doc with liveTileIndex:null, chunkId:null. (2) ComposerCanvas.commitWrites (lines 194-263) falls past the live-tile branch (202) and the chunk branch (209) to the doc-local path at 258-261: adoptPaletteLineForEmptyCells + setPixels + markOpenDirty + bumpDoc, with NO executeCommand — so editorStore.dirty and the history hub never learn about the work. (3) composer-buffer.ts:17-41,115-120 confirms the strokes live in doc.localPixels, held solely by artStore.open, with no undo entry and no file. (4) project-open-guard.ts:55-62 snapshots only classicLevelStore.dirty, editorStore.dirty, anySpriteDocDirty(), dirtyCanvasDocIds(); I read the whole file and it never imports useArtStore. endDocumentSession (88-91) closes sprites + canvases only; project-runtime.ts:223-227 resetProjectRuntime is the same; session-lifecycle.ts:144 calls only resetProjectRuntime on a key change. (5) useProject.ts:17 gates solely on confirmProjectOpen, then aeon-open.ts:86 calls useArtStore.getState().openDocument({doc: docFromChunk(first)…}) — or closeDocument() at line 94 when the library is empty — destroying the painted doc either way. aeon-open.ts's own comment (lines 76-78) states the now-false premise: "nothing can be dirty one statement after the project was committed, so there is no discard to confirm". (6) The guard's test file src/renderer/shell/__tests__/project-open-guard.test.ts has a four-field CLEAN snapshot and zero art cases; its canvas describe-block documents the identical hole one document type earlier. Concrete trigger: aeon project A open → Art facet → "New…" → New Chunk → paint → File > Open project B: no dialog, no toast, pixels gone. Two evidence nits that do not change the verdict: the corollary's citation ArtToolOptions.tsx:194 is wrong (that line is `const pixelPerfect = useArtStore((s) => s.pixelPerfect);` — no such invariant comment exists in the file; the nearest relevant text is the caps docblock at 106-124), and the defect is broader than "new docs only" since the chunk branch at ComposerCanvas.tsx:246-251 also marks artStore-only dirt when strokes land on empty/local cells. Note: Bash was completely unusable in this session (even `echo hi` exited nonzero), so verification is by full reads of the cited files rather than grep/vitest; every relevant file was read in full, so no grep-only link is unverified.


########## STATE-A3 [high] The transform grid is drawn on classic's Chunk/Block Paint tiers, which have no pendingAction consumer — the stale action fires on the next TileTab mount
file: src/renderer/workspace/facets/s1-facets.tsx:438  confidence: certain
CLAIM: `ClassicArtOptions` renders the shared option bar (with the transform grid enabled) for ALL THREE classic pixel tiers, but only TileTab consumes `artStore.pendingAction` — so a flip/rotate clicked on the Chunk or Block Paint surface does nothing, sits in the cross-engine slot, and then applies itself to whatever tile is selected the moment the user switches to the Tile tab.
EVIDENCE: s1-facets.tsx:435-440: `if (!isClassicPixelTier(composerTab, chunkPaintMode, blockPaintMode)) return null; return <ArtToolOptions caps={CLASSIC_TILE_CAPS} />` — and level-presence.ts:59-64 returns true for tab==='tile', tab==='chunk' && chunkMode==='paint', and tab==='block' && blockMode==='paint'. CLASSIC_TILE_CAPS (ArtToolOptions.tsx:162) sets `transforms: 'fixed-square'`, and ArtToolOptions.tsx:248-256 renders TransformGrid with `disabled` computed only for the `'doc'` case — so under 'fixed-square' all seven buttons are live and `onAction={requestAction}` writes artStore.ts:124 `pendingAction`. I grepped every consumer of pendingAction: TileTab.tsx:291-301 and ComposerCanvas.tsx:530-567. ChunkTab.tsx and BlockTab.tsx contain no reference to it. ClassicComposerDock unmounts the inactive tab (`{tab === 'chunk' && <ChunkTab/>}`, per classicLevelStore.ts:222-231), so switching to Tile MOUNTS TileTab, whose effect (TileTab.tsx:292) fires immediately with the leftover string and calls `commitTileBytes(out.bytes, 'Transform')` — a real, dirty-marking classicEditTiles commit. Failure scenario: Chunk tab, Paint mode, click 'Rotate 90'; nothing happens; click the Tile tab; the tile at composerTileIndex (which openAct seeded, art the user never chose) is silently rotated and the tiles domain goes dirty. Given the measured block-tier sharing, that one tile can reach many blocks/chunks. TileTab.tsx:274-281 documents this exact hazard as the reason its `finally { clearAction() }` exists — the hole is that two of the three hosts drawing the button never clear it.
FIX: Either give ChunkTab/BlockTab the same consume-and-always-clear effect TileTab has (over their paint buffer, via classicPaintSurface), or make `transforms` false for the non-tile tiers — the cleanest form is to compute the caps from the active tier in ClassicArtOptions (s1-facets.tsx:435) rather than passing the one CLASSIC_TILE_CAPS constant. Belt-and-braces: have ClassicComposerDock's tab-change effect call `clearAction()` so no string can cross a tier boundary.
VERDICT: CONFIRMED — I tried to break the chain at every link and could not.

1. Host gate — `/home/volence/sonic_hacks/aurora/src/renderer/workspace/facets/s1-facets.tsx:434-440`: `ClassicArtOptions` returns `<ArtToolOptions caps={CLASSIC_TILE_CAPS} />` for any tier where `isClassicPixelTier(composerTab, chunkPaintMode, blockPaintMode)` is true. `level-presence.ts:59-63` returns true for `tab==='tile'`, `tab==='chunk' && chunkMode==='paint'`, and `tab==='block' && blockMode==='paint'`. It is wired into the real facet: `s1ArtFacet` (s1-facets.tsx:537-544) sets `ToolOptions: ClassicArtOptions`, and `LevelWorkspace.tsx:175` mounts it whenever `chrome.toolOptions && pixelToolsLive` — and `usePixelToolsLive` uses the *same* predicate, so on Chunk/Block Paint the bar is drawn, not suppressed.

2. Buttons are live — `ArtToolOptions.tsx:161-163` sets `transforms: 'fixed-square'`, and the render at 246-259 computes `disabled` only for `caps.transforms === 'doc'`. So all seven TransformGrid buttons are enabled and `onAction={requestAction}` writes `artStore.ts:124` `pendingAction`.

3. No consumer on two of the three hosts — I read ChunkTab.tsx and BlockTab.tsx in full: neither imports or reads `pendingAction` (they read `tool/selectedColor/mirror/dither/pixelPerfect/zoom` from artStore only; their write path is diffWrites → planSurfaceEdit → classicPaintSurface). `PixelViewport.tsx` and `PixelEditController` never touch any store. So the click is a no-op with the string left in the slot.

4. No clearer on the tier boundary — `ClassicComposerDock.tsx` (read in full) switches tabs with `{tab === 'chunk' && <ChunkTab/>}` etc. (lines 133-137, unmounting the inactive tab) and its only effects are `setArtTier(tab)` and the selection clamp; neither calls `clearAction()`. `artStore.setArtTier` (line 111) only sets `artTier`. `LevelWorkspace.tsx` (read in full) never touches artStore's action slot.

5. The stale action fires and writes — `TileTab.tsx:291-301`: `const pendingAction = useArtStore(s => s.pendingAction); useEffect(() => { if (!pendingAction) return; ... resolveTileTransform(buffer, pendingAction, selection, locked) ... }, [...])`. On mount `selection` is null and `buffer = tileToBuffer(doc.tiles, composerTileIndex)` (line 161), so `classic-tile-transform.ts:132` clamps the region to the whole 8x8 tile, rule B is satisfied for a square tile (rotate-90 passes), `tileBytesIfChanged` returns bytes for any non-symmetric tile, and `commitTileBytes(out.bytes,'Transform')` runs `classicEditTiles([{tileIndex: composerTileIndex, ...}])` (TileTab.tsx:248-256) — a real undoable, dirty-marking commit against a tile the user did not target.

Concrete trigger: open an S1 act → Art facet → Chunk tab → click the "Paint" chip → click ⇋ Flip horizontal in the option bar (nothing happens) → click the "Tile" tab. TileTab mounts, its effect fires with the leftover 'flip-h', and the tile named in the breadcrumb (composerTileIndex) is flipped and committed. Identical via Block tab + Paint. The only escapes are benign ones that still leave the feature dead: a locked/reserved tile toasts "view-only — transform refused", and a symmetric tile is a genuine no-op.

I found no guard, no alternate consumer, and no caller that prevents the bad value — the finding survives.

Severity: `high` is right, not theatrical, but the framing should be trimmed. Two of three paint tiers draw seven live controls that do nothing (broken main-path feature) and the deferred write lands on the wrong tile (wrong output). It is NOT critical: the misfire targets the tile the dock breadcrumb is already displaying, the result is visible the instant the Tile tab paints, and it is one Ctrl+Z away on the classic undo stack (classicEditTiles is a command), so "silent loss of user work" overstates it.


########## DIR-A1 [high] ROADMAP.md (plan of record) is a month behind actual steering and its sequencing points at the wrong engine
file: docs/ROADMAP.md:342  confidence: certain
CLAIM: The P0-P8 plan of record records nothing after 2026-08-09 (§2.5) and still sequences aeon-first, while every phase actually executed since (UX overhaul stages 1-4 plans 1-6, art authoring 1/2A/2B/2C) was classic-first and off-roadmap.
EVIDENCE: git log master --since=2026-08-15 -- docs/ROADMAP.md is empty while master gained ~190 commits since 2026-08-14. Row-by-row against code: P0 doc hygiene NOT done (ART_SUITE.md still says s4_engine and documents the deleted Toolbar UI); P1 export retirement NOT done (src/core/export/vram-coloring.ts, act-descriptor.ts, entity-data.ts all still present); P2 NOT started (src/main/aether/ contains adapter.ts+protocol.ts only — server side; no outbound client, the suite keystone per empyrean/docs/ROADMAP.md ~line 200 'crown jewel'); P3 open; P4-P6 still engine-gated and unmoved; P7 row says Import pipeline 'None' (ROADMAP.md:161) while src/core/art/png-import.ts + the import path shipped 2026-08-16 (commits 1ba9dd5, f942119, eb370ee); '~83 test files' (ROADMAP.md:57) vs measured 205 files / ~2250 tests (2026-08-14-plan6-handoff.md baseline; 171 *.test.ts in src today). Off-roadmap deliveries not recorded anywhere in it: DocumentHistoryHub undo unification, facet parity/LevelWorkspace unification (LegacyWorkspace/ClassicProjectView/ZoneActTree/Toolbar deleted — confirmed absent from src), art convergence H1-H3, art-authoring phases 1-2C.
FIX: DECISIONS, not options. (a) Add §2.6 recording the August line (UX stages 1-4, art authoring 1-2C) as delivered, same style as §2.5. (b) Re-sequence around the classic spine the owner is actually steering: NEXT PHASE = classic collision authoring — colind/heightmap assignment plus solidity in the classic art facet, reusing aeon's CollisionPalette idiom (this is exactly 'bring aeon's approach into classic'). Why it is the single highest-leverage phase: 2C's own out-of-scope note (phase-2c spec §8) concedes 'authoring collision for brand-new chunks still has no in-app answer' — a committed drawing gets colind 0 + solidity 0 (classic-commit-plan.ts:412-413) and the player falls through it; classicSetColind is reachable only from agent-handler.ts. Every rung of originate→constrain→import→commit→refine now exists in-app; collision is the one rung that still sends the artist out of Aurora, which is the precise failure the 'artists don't leave' bar forbids, at the last step. (c) SECOND = the playtest loop, classic-first (build.lua invoke + emulator launch), then the aeon Aether outbound client per P2 — the suite keystone stays on the board but behind the loop the current product actually needs. (d) Mark P7's classic half DONE, P8 substrate DONE (already noted), and demote P4-P6 to an explicit 'awaiting aeon engine tasks' parking section so cold sessions stop treating them as next.
VERDICT: CONFIRMED — Every factual link verified independently at 2b38e0b. (1) Currency: docs/ROADMAP.md's newest commit is f007245 (2026-08-09); `git log --since=2026-08-14` on master returns 190 commits, none touching the file, while the doc's own header (line 7) mandates in-place updates and line 13 claims "Last full revision: 2026-07-03". (2) Row-by-row: src/core/export/ still holds vram-coloring.ts, act-descriptor.ts, entity-data.ts (P1 retirement §4.2 not done); src/main/aether/ contains exactly adapter.ts + protocol.ts, no client.ts (P2 §4.8 not started); ROADMAP.md:161 says Import pipeline "None" although src/core/art/png-import.ts and the end-to-end import path shipped 2026-08-15/16 (1ba9dd5, eb370ee); line 57's "~83 test files" measures 171 (`find src -name '*.test.ts'`); P0 doc hygiene is unapplied — docs/ART_SUITE.md:12 still says s4_engine and :73 tells the user to click the Art button "in the Toolbar" while no Toolbar/LegacyWorkspace/ClassicProjectView/ZoneActTree file exists under src. (3) The August line (UX stages 1-4, art authoring 1/2A/2B/2C) exists only under docs/superpowers/{specs,plans}/2026-08-1* and is recorded nowhere in ROADMAP.md, whose §5 table stops at the §2.5 row and still sequences P4-P6 behind aeon engine tasks. (4) The fix's cited premise also holds: classic-commit-plan.ts:411-413 gives a cell with no displaced predecessor `solidity = 0` and `colind = 0` (inheritance applies only where `old` exists — correctly scoped by the seat to brand-new chunks); classicSetColind's only non-test caller is agent-handler.ts:713, corroborated by src/core/project/s1/index.ts:76-80 ("Classic has no collision-editing UI… sole collision affordance is a read-only overlay") and by the 2C spec's own out-of-scope note at docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:495. I searched for a superseding plan-of-record that would make the staleness harmless and found none — no newer roadmap exists, and the 2026-08-12 UX and 2026-08-15 art-authoring specs still reference ROADMAP.md. No link in the chain breaks.
SEV-CORR: Downgrade high -> medium. Nothing here is executable: docs/ROADMAP.md produces no wrong runtime output, breaks no main-path feature, and destroys no user work — the panel's "high" bar is not met. The harm is real but indirect and fully reversible by editing one file: a cold session reading the self-declared plan of record is told a shipped feature is "None" (line 161), that the suite has ~83 test files when it has 171 (line 57), that "P0-P3 are unblocked today" (line 344), and is pointed at aeon-gated P4-P6 as next. That is directive-grade misinformation about project state, which warrants medium, not the wrong-output/broken-feature tier.

########## DIR-A2 [high] 2C spec header says BUILT, but D2b's cross-act reach reporting is not built
file: docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:3  confidence: certain
CLAIM: The spec's D2/D2b requirements — palette-adoption dialog names the actual files from paletteSources (SBZ per-act palettes), report names SBZ3-borrows-LZ-art and ending-reuses-GHZ reach, LZ/SBZ3 underwater-palette warning — are all unimplemented, yet the status header declares the phase BUILT with no deviation note.
EVIDENCE: grep for SBZ/borrow/sibling/ending/underwater/paletteSources across src/core/art/classic-commit-plan.ts, src/renderer/components/canvas/canvas-commit-model.ts and CommitPlanView.tsx returns nothing. The only adoption warning is the generic hardcoded sentence at classic-commit-plan.ts:263 ('every act composed from the same palette file is affected') — the exact class of sentence D2 said must be computed from the profile, not hardcoded. The spec §7 test 'the water-palette warning fires for LZ' does not exist (grep water/SBZ in __tests__/classic-commit-plan.test.ts: no hits). The planner takes no profile input at all (CommitPlanInput, classic-commit-plan.ts:49-71), so it structurally cannot compute reach. Failure scenario: an artist commits while editing LZ, adopts a palette, and is never told SBZ3 renders the same art under a palette they never saw — the 'wrong output the artist was not warned about' D2b exists to prevent.
FIX: (none given)
VERDICT: CONFIRMED — Independently verified at SHA 2b38e0b. Spec line 3 declares "Status: BUILT 2026-08-16" with no deviation note, and §8's out-of-scope list does not include D2b or D2's file-naming requirement. Implementation: classic-commit-plan.ts contains exactly two warnings.push sites — line 263 ('the zone palette changed: every act composed from the same palette file is affected') and line 444 (GHZ $51/$28 loop alias) — both hardcoded strings. CommitPlanInput (lines 49-71) has no profile, act ref, or paletteSources field, so the planner structurally cannot compute reach; planFromSnapshot (canvas-commit-model.ts:~200) passes nothing extra; CommitPlanView.tsx holds `ref` (line 35) but uses it only for the "into GHZ 1" label (103) and the applied toast (92), and its adopt-chip tooltip (149) repeats the same hardcoded sentence. Repo-wide grep for paletteSources|underwater|borrow hits only profiles/s1.ts, s1-io.ts:341, s1-levelart-reservations.ts:43 and test fixtures — nothing on the commit path. The §7 test "the water-palette warning fires for LZ" does not exist: classic-commit-plan.test.ts:148 asserts only toMatch(/every act/), and water|SBZ|reach have no hits in that file. The reach itself is real (profiles/s1.ts:481-496: SBZ act 3 takes LZ_TILES, map16/LZ.eni, map256/LZ.kos, collide/LZ.bin while keeping basePalette('SBZ Act 3.bin') plus its own underwater files), so an artist committing chunks while editing LZ does change SBZ3 and is never told. Refutation attempts failed: no deferral/carry-forward note in plans/2026-08-15-2c-foundations.md or 2c-resolver.md, and no separate reach-reporting module anywhere in src. Triggering state: any commit with paletteResolution='adopt-into-zone', or any chunk-replacing commit in LZ/GHZ — the report's warnings array carries at most the generic palette sentence and the $51 alias.
SEV-CORR: Downgrade high -> medium. The finding is anchored to a documentation line and the harm is a missing advisory, not wrong output. Nothing is corrupted or lost: the adoption write goes through doc.palettes -> s1-io decomposition into the act's own paletteSources, so SBZ's per-act palette files are correctly isolated, and the whole commit is one undoable ART_DOMAINS record. The seat also overstates the shipped sentence — "every act composed from the same palette file is affected" is hardcoded but remains TRUE in SBZ (whose acts use different palette files); D2's objection was to the false form "every act of the zone changes". So the artist sees an under-informative true statement, not a misleading one. Main-path commit works and was CDP-verified. That is spec-vs-code drift plus an unbuilt advisory requirement — medium, not "wrong output or broken main-path feature".

########## DIR-A3 [high] The whole art-authoring line shipped with zero MCP parity, against the standing cross-cutting rule
file: src/renderer/agent/agent-handler.ts:1  confidence: certain
CLAIM: Paint-through, canvas documents, canvas commit and PNG import (phases 1-2C) have no MCP tools, violating ROADMAP.md:332-334's rule that every new authoring capability ships its MCP tools in the same phase so agent and human editing stay equivalent.
EVIDENCE: grep commit_canvas/import_art/canvas across agent-handler.ts and docs/MCP.md: no authoring tool hits (only map-canvas screenshot plumbing). classicPaintSurface, classicCommitCanvas and the PNG import path are human-gesture-only. This is a direction breach, not just debt: the suite's agent-equivalence principle is one of Aurora's two structural rules, and 2C's planner is pure and refusal-typed — i.e. it was built to be agent-callable and then wasn't exposed.
FIX: Decide it as scope, not backlog: the collision-authoring phase (A1's next phase) ships paint_collision AND retrofits commit_canvas + import_art_sheet in the same phase, restoring the invariant in one pass.
VERDICT: CONFIRMED — I tried to break the claim on the facts and could not. Verified independently:

(1) The rule exists as cited. docs/ROADMAP.md, section "Cross-cutting (all phases)": "MCP parity rule: every new authoring capability ships with its MCP tools in the same phase (the ~17 new tools across #6–#9), one undo step per mutation, so agent and human editing stay equivalent."

(2) src/main/editor-methods.ts is the SINGLE source of MCP tools — src/main/mcp-server.ts:33 registers exclusively via `for (const m of EDITOR_METHODS)`, there is no second registration site. Its complete 34-entry name list (lines 48–155) has no commit_canvas, no import_art_sheet, no canvas/paint-surface tool. So there is no back door the seat missed.

(3) src/renderer/agent/agent-handler.ts: all 33 `case` labels enumerated; none are canvas/commit/import. The only `canvas` occurrences are the map-canvas screenshot plumbing at lines 545–562 — exactly as the seat characterised it.

(4) docs/MCP.md read in full (169 lines): no canvas, commit, or import tool documented.

(5) The line did ship at this SHA: commits f942119 (png-import), c16f61c (commit panel), b95052b (classicCommitCanvas), 1ba9dd5 (PNG sheet import end to end), e9850ca (marks 2C built); the design doc docs/superpowers/specs/2026-08-15-in-app-art-authoring-design.md carries the banner "phases 1, 2A and 2B built and merged (2026-08-15)".

So the factual core — art-authoring phases 1–2C have zero MCP tools while ROADMAP declares a same-phase parity rule — is true.

Two corrections to the seat's framing, both material:

(a) It is NOT a fresh direction breach. The classic surface has never had any pixel-level agent tool: `write_tiles` is aeon-only (agent-handler.ts:250-252 calls requireProject() then reads ctx.zone.tileset.tiles), and classic pixel editing (TileTab) was human-only before phase 1 ever existed. The gap also runs the other way — the 2C spec itself, docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:495, records "classicSetColind exists but is reachable only from agent-handler.ts", i.e. an agent-only capability with no human editor. The invariant was already unheld on the classic surface in both directions; phases 1–2C continued that state rather than breaking a standing one.

(b) The proposed fix is partly already-done and partly name-colliding. `paint_collision` already exists (editor-methods.ts:70, agent-handler.ts:324) as the aeon section-plane painter, and a classic collision tool cannot reuse that name — editor-methods.ts:145-148 documents that this registry is flat and MCP tool names must be globally unique (the reason classic's palette tool is named set_level_palette rather than set_palette).

Bash stopped returning output partway through the session (echo itself began failing), so I could not run the test suite or grep for the classicPaintSurface/classicCommitCanvas symbol bodies. That link is unverified only as to exact symbol spelling — commit b95052b's subject line names classicCommitCanvas directly, and it is immaterial to the verdict, which rests on the tool registry being demonstrably empty of these capabilities.
SEV-CORR: Downgrade high -> low. The panel rubric reserves "high" for wrong output or a broken main-path feature. Nothing here produces wrong output and nothing is broken: the human authoring path was CDP-verified end to end (commit e9850ca), and no agent call returns bad data because no such agent call exists. There is no triggering input or state to state — the finding describes an ABSENCE of an agent-facing surface on a surface (classic) that already lacked pixel-level agent tools entirely, and which conversely already carried an agent-only tool with no human UI (set_colind). This is roadmap-adherence / scope-planning debt, correctly raised, but it is not a defect. The seat's own words give it away: "This is a direction breach, not just debt" is an editorial framing, not a failure path.

########## SEAM-A1 [high] Aurora's aeon act-descriptor exporter emits a struct layout diverged from engine/structs.emp (34B Act vs 40B, 72B Sec vs 66B) and resurrects a directory the engine deleted
file: src/core/export/act-descriptor.ts:25  confidence: certain
CLAIM: generateActDescriptorAsm emits the pre-2026-06 Act/Sec layouts — fields the aeon engine deleted, missing every field it added, and the wrong total sizes — and saveAeonProject writes them into games/sonic4/data/editor/ojz/act1/export/, the exact directory aeon deleted on 2026-08-01 and which sits inside an auto-commit-daemon watch path.
EVIDENCE: Aurora emits (act-descriptor.ts:26-40): sec_grid_ptr(4) grid_w(2) grid_h(2) start_local_x(2) start_local_y(2) start_sec_x(1) start_sec_y(1) cam_min_x/cam_max_x/cam_min_y/cam_max_y (8, lines 34-37) act_bg_layout(4) act_bg_tiles(4) act_parallax_config(4) = 34 bytes, and its own comment at :25 says "Act descriptor (34 bytes)". aeon/engine/structs.emp:26-51 declares `Act` as 40 bytes ($28): there is NO cam_min/max block at all, act_bg_layout sits at $0E where Aurora writes cam_min_x, and the engine adds act_art_pool_table($1A), act_art_pool_pages($1E), edge_mode($20), pad_21($21), act_sec_local_maps($22), act_art_budget($26) — none of which Aurora emits. Section side: Aurora emits 14×dc.l + 2×dc.w + 4×dc.b + dc.l + 2×dc.w = 72 bytes/section and pads null sections `dcb.b 72, 0` (act-descriptor.ts:59); aeon/engine/structs.emp:107-132 declares `Sec` as 66 bytes ($42). Field-level: Aurora's `sec_reserved_2C` (:77) is the engine's `sec_block_dict`($2C); Aurora's `sec_collision_s4lz` (:79) was renamed `sec_effects`($34) (aeon docs/superpowers/plans/2026-08-14-effects-p3-parcel-c2.md:278); Aurora's `sec_tile_art_s4lz`/`sec_tile_art_vram` (:83-84) no longer exist in the struct; the engine's `sec_camera_lookahead`($3D) and `sec_block_dict_len`($40) are never emitted. Live path: src/core/project/aeon/save.ts:222-243 calls exportAct on every aeon save and writes act_descriptor.asm / entity_data.asm / vram_bases.asm / section_N.*.bin under `${dataPath}export/`. aeon/docs/DEFERRED_WORK.md:988-991 records that commit 46c2e0f (2026-08-01, "Parcel J: delete the parked ojz editor exports") deleted that directory, and DEFERRED_WORK.md:1005 records it "is git-tracked but NOT in the build include graph … it would not even assemble as-is" and "still emits the OLD Act layout: the removed cam_min_x/max_x/min_y/max_y 4-word camera block, no edge_mode byte/pad, and pre-paging art fields". The directory is present on disk again with 2026-08-12 timestamps, i.e. a post-deletion Aurora save recreated it. aeon/docs/superpowers/plans/2026-07-02-art-streaming-phase2.md:16 and 2026-08-08-art-streaming-phase2-v2.md:44 both state games/sonic4/data/editor/ojz/ is auto-committed by a daemon ~60s after edit — so each aeon save silently commits the obsolete descriptor back into the engine repo. Zero tests in aurora compare act-descriptor.ts against engine/structs.emp; test/export/pipeline.test.ts only checks Aurora's own output against itself.
FIX: Either delete the act_descriptor/vram_bases/entity_data emission from saveAeonProject (aeon's recorded direction of travel — DEFERRED_WORK.md:1000 'editor authors JSON, BUILD generates engine format' — Aurora already writes the editor-native section_N.tiles.bin/chunks.json that ojz_strip_gen.py consumes), or regenerate act-descriptor.ts from engine/structs.emp and add a test that fails when Act/Sec field order or sizeof drifts.
VERDICT: CONFIRMED — I independently traced the defect end to end and it is factually real, but the severity framing does not survive.

WHAT I CONFIRMED (concrete trigger): open the OJZ aeon project in Aurora (actConfig.dataPath = games/sonic4/data/editor/ojz/act1/) and press Save. saveAeonProject (src/renderer/state/aeon-save.ts:25) -> buildAeonSavePlan (src/core/project/aeon/save.ts:222 exportAct) -> src/core/export/index.ts:71 generateActDescriptorAsm -> src/core/export/act-descriptor.ts.

Layout divergence verified by hand-counting emitted directives against the engine struct:
- Aurora Act = 4+2+2+2+2+1+1+(2*4)+4+4+4 = 34 bytes, with the cam_min_x/cam_max_x/cam_min_y/cam_max_y block at act-descriptor.ts:34-37. aeon/engine/structs.emp:26-51 declares Act = 40 bytes ($28) with NO cam block; act_bg_layout sits at $0E exactly where Aurora writes cam_min_x, and the engine added act_art_pool_table($1A), act_art_pool_pages($1E), edge_mode($20), pad_21($21), act_sec_local_maps($22), act_art_budget($26) — none emitted.
- Aurora Sec = 14 dc.l (56) + 2 dc.w (4) + 4 dc.b (4) + dc.l (4) + 2 dc.w (4) = 72 bytes, padding nulls `dcb.b 72, 0` (act-descriptor.ts:59). structs.emp:107-132 declares Sec = 66 ($42). Field renames confirmed: Aurora sec_reserved_2C(:77) is the engine's sec_block_dict($2C); Aurora sec_collision_s4lz(:79) is now sec_effects($34); sec_camera_lookahead($3D) and sec_block_dict_len($40) are never emitted.
- Directory resurrection confirmed: games/sonic4/data/editor/ojz/act1/export/{act_descriptor.asm,entity_data.asm,section_N.*.bin} present with 2026-08-12 timestamps, post-dating commit 46c2e0f (2026-08-01, "Parcel J: delete the parked ojz editor exports").
- Test gap confirmed and worse than stated: test/export/act-descriptor.test.ts:60-72 affirmatively asserts `dcb.b 72, 0`, locking the wrong Sec size into the suite. Nothing compares against structs.emp.

WHAT BROKE (why severity is wrong, not the facts):
1. No consumer. aeon/build.sh:79 sets MAIN_ASM="games/sonic4/game_root.asm"; grep for "editor" in game_root.asm returns nothing, and games/sonic4/map.toml has zero ".asm" matches. The engine's live descriptor is games/sonic4/data/levels/ojz/act1/act_descriptor.emp — different directory, different extension, and a TYPED Act literal (its own header: "field-order drift cannot compile"). Aurora's .asm cannot be included, cannot overwrite the .emp, and cannot reach the ROM. The only build-graph grep hit for "act1/export" was a self-match inside Aurora's own emitted comment.
2. The "silently commits the obsolete descriptor back into the engine repo" link is not just unproven, it is contradicted by the current state: `git status` shows `?? games/sonic4/data/editor/ojz/act1/export/`, `git ls-files` on that path is EMPTY, and `git check-ignore` returns rc=1 (not ignored) — i.e. untracked and uncommitted four days after the 2026-08-12 write. No auto-commit process is running (`ps aux | grep commit` empty) and .git/hooks has no non-sample hooks. The daemon claim rests on plan-doc prose, not observed behavior.
3. No user work is lost or corrupted. The editor-native artifact ojz_strip_gen.py actually consumes is ${dataPath}section_N.tiles.bin (save.ts:66-70); the remapped export twin lands in ${dataPath}export/section_N.tiles.bin (save.ts:242). Different directories — no clobber of authored data. Also, the export block is wrapped in try/catch (save.ts:221-247) surfacing as a non-fatal exportError, so it cannot even fail a save.
4. The aeon repo's own record grades it exactly this way: DEFERRED_WORK.md strikes the entry through as "VOID 2026-08-05 (the artifact was deleted)", and its surviving historical text says "So it is no build/runtime risk." The finding cites that same file's evidence while dropping its conclusion.

So: the code defect is CONFIRMED (dead exporter emitting a stale, un-assemblable struct layout, plus recreation of a deliberately deleted directory and a test suite that pins the wrong size), but it is not "wrong output or broken main-path feature" — the output is inert and the aeon build is structurally immune.
SEV-CORR: Downgrade high -> low. "High" requires wrong output or a broken main-path feature; neither holds. The emitted act_descriptor.asm has no consumer: build.sh:79 roots the residual AS at games/sonic4/game_root.asm (zero "editor" references), map.toml has zero .asm references, and the live descriptor is a typed .emp literal at games/sonic4/data/levels/ojz/act1/act_descriptor.emp that Aurora's file cannot be included by or overwrite. The claimed auto-commit consequence did not occur — the directory is untracked and unignored four days after being written. The main aeon save path (editor-native section_N.tiles.bin, chunks.json, project.json) is untouched, and the export block is try/caught as non-fatal. Residual real harm is limited to: dead code emitting an obsolete artifact, git-status noise plus resurrection of a directory the engine deliberately deleted, and a test suite (test/export/act-descriptor.test.ts:60-72) that actively pins the wrong 72-byte Sec size and would resist a future correct rewrite. That is low — cleanup work, not a bug with a victim. A case for "medium" exists only if one weights the latent trap of the pinning tests plus the directory resurrection; nothing above medium is defensible.

########## SEAM-A2 [high] FG_TILE_LIMIT = 1024 mirrors an aeon constant that is 896, and check_budget reports fits/limit from it
file: src/core/export/vram-coloring.ts:5  confidence: certain
CLAIM: Aurora's FG tile-pool ceiling is hardcoded at 1024 with a comment justifying it as 'BG region starts at tile slot 1024'; the aeon engine's declared VRAM contract gives fg_art_pool only 896 tiles (slots 896-1023 belong to dust/character/ring/test windows), so the check_budget MCP tool advertises 128 tiles of headroom that do not exist.
EVIDENCE: src/core/export/vram-coloring.ts:4-5: `// BG region starts at tile slot 1024 ($400); FG group unions must fit below it.` / `export const FG_TILE_LIMIT = 1024;`, used at :44 to throw 'VRAM overflow' and at src/core/agent/budget.ts:81 as `{ limit: FG_TILE_LIMIT, fits: cursor <= FG_TILE_LIMIT }`, which is what the `check_budget` tool returns (src/renderer/agent/agent-handler.ts:218-231; described in src/main/editor-methods.ts:60 as 'per VRAM color group vs the 1024-tile FG pool. fits=false means export will fail'). Ground truth: aeon/games/sonic4/vram.toml declares region fg_art_pool base=0 tiles=896 with the inline note '960 -> 896: the top page surrendered to the dust', then dust_puff@896(16), dust_spindash@912(12), free@924(36), character_window@960(32), test_obj@992(8), ring_placeholder@1000(16), test_marker@1016(4), free@1020(4), and only then bg_region@1024. aeon/tools/vram_map.py:24 `POOL_TILE_CEILING = 896`; aeon/games/demo/vram.toml carries the same 896 with 'follows the engine-global POOL_TILE_CEILING'. The divergence dates to aeon commit c51a4ff9 (2026-08-11 21:47) 'the dust pool carve'. Not academic: the shipped OJZ act1 export on disk is group0=603 tiles (section_0.art.bin = 19296 B) + group1=281 (section_1.art.bin = 8992 B) = 884 tiles, i.e. 12 tiles under the real 896 ceiling, while Aurora reports 884/1024 and fits=true. Adding ~13 unique tiles to the second colour group makes Aurora answer fits=true for a layout whose group-1 base lands on dust_puff. No downstream catch: aeon/tools/ojz_strip_gen.py:1776 records 'identity-residency guard (OJZ_ACT_POOL_TILES <= POOL_TILE_CEILING) is retired' and :1774 'there is no per-act VRAM fit to guard'. No aurora test references POOL_TILE_CEILING or 896; grep for FG_TILE_LIMIT hits only budget.ts, vram-coloring.ts and test/export/vram-coloring.test.ts (which asserts against the same 1024 literal on both sides).
FIX: Stop mirroring: read the ceiling from aeon's declared contract (games/<game>/vram.toml fg_art_pool, or the generated tools/vram_map.py POOL_TILE_CEILING) at project load, and drop 1024 from vram-coloring.ts. If the number must stay hardcoded, add a test that reads aeon's vram.toml when present and fails on inequality. Separately, revisit whether an act-wide union budget is still the right model at all now that the engine pages 64-tile pages (PAGE_TABLE_MAX=256, ojz_strip_gen.py:131).
VERDICT: REFUTED — I traced every link of the claimed path and the failure scenario breaks at the consumer end.

What is true (and only this): `src/core/export/vram-coloring.ts:5` hardcodes `FG_TILE_LIMIT = 1024`; `src/core/agent/budget.ts:81` returns it as `limit`/`fits`; `src/main/editor-methods.ts:60` describes it as "the 1024-tile FG pool"; and `/home/volence/sonic_hacks/aeon/games/sonic4/vram.toml` really does declare `fg_art_pool base=0 tiles=896` with dust/character/ring windows at 896-1023. The two numbers do differ.

What breaks the finding — the "group-1 base lands on dust_puff" harm requires some consumer to place Aurora's cumulative group bases into VRAM. No such consumer exists:

1. `sec_tile_art_vram` — the field that carried a per-section VRAM base — exists NOWHERE in aeon's engine/, games/ or tools/ any more. `grep -rn sec_tile_art_vram engine/ games/ tools/` returns hits only inside Aurora's own stale artifact `games/sonic4/data/editor/ojz/act1/export/act_descriptor.asm`. The live descriptor is `games/sonic4/data/levels/ojz/act1/act_descriptor.emp` and has no such field.

2. Nothing in aeon reads Aurora's `export/` directory. `build.sh` references no editor/export path; grep for `/export/`, `vram_bases`, `.art.bin` across aeon's .py/.emp/.asm/.sh/.toml finds zero consumers, and the exported `.art.bin`/`.tiles.bin` are gitignored (`aeon/.gitignore:2 *.bin`). What the build actually consumes is `editor/ojz/act1/section_N.tiles.bin` + `editor/ojz/chunks_tiles.bin` (`tools/ojz_strip_gen.py:1590`), which Aurora writes at `src/core/project/aeon/save.ts:70` from the RAW nametable — untouched by `assignVramBases`. The base-remapped copies go to `dataPath/export/` (save.ts:230-242) and are dead output.

3. The engine has no 896-tile per-act fit at all. `tools/ojz_strip_gen.py:1770-1776`: "pages land in ALLOCATED frames (dest = frame base, not page_id*64), so there is no per-act VRAM fit to guard … the old identity-residency guard (OJZ_ACT_POOL_TILES <= POOL_TILE_CEILING) is retired." `POOL_TILE_CEILING` is imported but used at exactly one site, the informational print at :1955 ("ceiling {POOL_TILE_CEILING}"). The real hard cap is `PAGE_TABLE_MAX = 256` pages x 64 tiles (:131). The seat quoted line 1776 and still asserted an 896 hard ceiling. So the concrete trigger it offers ("add ~13 unique tiles to the second colour group and group-1's base lands on dust_puff") produces no overlap, no corrupted art, no wrong ROM — the pool is deduped and paged by ojz_strip_gen from the tileset/nametables, and a 900- or 2000-tile act is legal.

4. The tool's operative promise is internally true. budget.ts computes the union exactly as export does (blank-seed + flip-aware) so `fits === !exportThrows` against `assignVramBases` (vram-coloring.ts:44). "fits=false means export will fail" holds; `npx vitest run test/export/vram-coloring.test.ts test/agent/budget.test.ts` passes 10/10.

The 884-vs-896 "12 tiles of headroom" narrative is also inert: those byte counts come from the gitignored export/ blobs, not from the shipped ROM art, which ojz_strip_gen derives independently.

Residual, much smaller issue: 1024 is a stale legacy constant that matches neither aeon's declared fg_art_pool (896) nor the engine's actual paged cap, and `check_budget`'s description narrates it as an engine VRAM fact. That is doc/model staleness in an advisory tool plus dead export output — worth cleaning up, not a defect that produces wrong output.
SEV-CORR: If retained at all, low (stale advisory constant / dead export artifact), not high. High requires wrong output or a broken main path; the only artifact affected by FG_TILE_LIMIT is aurora's export/ directory, which no aeon build step reads, and the engine imposes no per-act VRAM tile ceiling for the value to be wrong against.

########## SEAM-A4 [high] The /mcp endpoint has no Host/Origin validation while /aether does, and the installed MCP SDK provides none by default
file: src/main/mcp-server.ts:65  confidence: probable
CLAIM: protocol.md D8 makes the loopback Origin/Host check Aurora's responsibility; it is applied to /aether and /aether/events but not to /mcp, which serves the same 33 editing tools (including open_project and save_project) with no header validation from Aurora or from the SDK.
EVIDENCE: src/main/mcp-server.ts:65 `exp.post('/mcp', async (req,res) => {...})` — no middleware. The loopbackOnly middleware defined at :83-91 is applied only at :97 (`exp.post('/aether', loopbackOnly, ...)`) and :107 (`exp.get('/aether/events', loopbackOnly, ...)`). The comment at :81-82 states the trust model ('loopback bind + Origin/Host check, never remotely exposed (protocol D8)') beside the endpoint that has it, not the one that doesn't. The SDK is constructed as `new StreamableHTTPServerTransport({ sessionIdGenerator: undefined })` (:68) with no allowedHosts/allowedOrigins/enableDnsRebindingProtection. Verified in the installed SDK (@modelcontextprotocol/sdk ^1.29.0): dist/esm/server/streamableHttp.js contains no occurrence of 'host', 'origin' or 'DnsRebinding' at all (it delegates to webStandardStreamableHttp.js), and dist/esm/server/sse.js:22 shows the family default `this._options = options || { enableDnsRebindingProtection: false }` — protection is opt-in and Aurora does not opt in. Aurora also builds its express app by hand rather than via the SDK's createMcpExpressApp (dist/esm/server/express.js:27-32), which is where hostHeaderValidation would have been wired. Failure scenario: a page served from http://attacker.example:38473/ whose DNS then rebinds to 127.0.0.1 makes same-origin fetches to /mcp (no CORS preflight, no Host check) and can call open_project on an arbitrary path plus every classic mutation tool and save_project against the user's disasm. No test in test/main covers mcp-server.ts at all — aether-adapter.test.ts tests the adapter function, never the express wiring, so loopbackOnly itself is also never exercised.
FIX: Apply `loopbackOnly` to `exp.post('/mcp')` (and the 405 handlers) exactly as /aether does, or pass `{ enableDnsRebindingProtection: true, allowedHosts: ['127.0.0.1:'+port, 'localhost:'+port] }` to StreamableHTTPServerTransport. Add a test that POSTs with `Host: evil.example` and asserts 403 — today no such test exists for either endpoint.
VERDICT: CONFIRMED — CONFIRMED — I tried to break the chain at four points (a missed global guard, SDK-side defaults, CORS, and the MCP initialize handshake) and all four held. I then reproduced the exploit against a live server.

**1. The asymmetry is real (read at pinned SHA 2b38e0b).** /home/volence/sonic_hacks/aurora/src/main/mcp-server.ts:65 `exp.post('/mcp', async (req, res) => {...})` takes no middleware; :77 and :78 (the 405 handlers) likewise. `loopbackOnly` is defined at :83-91 and applied only at :97 (`/aether`) and :107 (`/aether/events`). The only app-wide middleware is `exp.use(express.json({limit:'16mb'}))` at :62 — I grepped all of src/main for `cors|helmet|Access-Control` and got zero hits. `startMcpServer` is called unconditionally at src/main/index.ts:64 (no dev-only gate).

**2. The SDK provides nothing — verified in the installed tree, not from memory.** node_modules/@modelcontextprotocol/sdk is version 1.29.0. `dist/esm/server/streamableHttp.js` is a thin wrapper that passes `options` straight to `WebStandardStreamableHTTPServerTransport`; `dist/esm/server/webStandardStreamableHttp.js:70` is the authoritative default: `this._enableDnsRebindingProtection = options.enableDnsRebindingProtection ?? false`. The Host/Origin validator at :104-127 is entirely gated behind `if (!this._enableDnsRebindingProtection) return;` at :109. Aurora constructs `new StreamableHTTPServerTransport({ sessionIdGenerator: undefined })` (:68) — no allowedHosts, no allowedOrigins, no opt-in. (Minor correction to the seat's evidence: the load-bearing line is webStandardStreamableHttp.js:70, not sse.js:22.) Aurora also hand-builds express rather than using `createMcpExpressApp` (dist/esm/server/express.js:27-38), which is the only place `localhostHostValidation()` gets wired.

**3. The initialize handshake does NOT gate it — this is worse than the seat claimed.** I expected to refute here, since Aurora builds a *fresh* McpServer+transport per POST (:67-68), so no session state survives a request. But webStandardStreamableHttp.js `validateSession` returns `undefined` immediately when `sessionIdGenerator === undefined` (stateless mode), short-circuiting the `if (!this._initialized) reject all requests` check just below it. So a single POST containing only `tools/call`, with no initialize, no Mcp-Session-Id and no Mcp-Protocol-Version, reaches the tool handler.

**4. Reproduced.** I stood up a standalone server replicating mcp-server.ts:65-76 verbatim (scratchpad/rebind.mjs, outside the repo — no files modified) with an `open_project` tool. One POST with `Host: attacker.example:38999`, `Origin: http://attacker.example:38999`, no handshake:
`status=200`, `body={"result":{"content":[{"type":"text","text":"opened /home/victim/secret-disasm"}]}}`, `TOOL RAN WITH: {"path":"/home/victim/secret-disasm"}`.
And the guard Aurora already owns would have stopped exactly that request: `/^(127\.0\.0\.1|localhost|\[::1\]|::1)(:\d+)?$/i.test('attacker.example:38999')` → **false** → 403. Applied to `/aether`, not to `/mcp`.

**Concrete trigger:** attacker serves a page on `http://attacker.example:38473/` with a short-TTL A record, rebinds it to 127.0.0.1, then `fetch('/mcp', {method:'POST', headers:{'content-type':'application/json','accept':'application/json, text/event-stream'}, body: <tools/call open_project>})`. It is same-origin post-rebind, so no preflight; the browser sends `Host: attacker.example:38473`, which Aurora never inspects. All 33 EDITOR_METHODS (confirmed count; `open_project` at editor-methods.ts:108, `save_project` at :155) are reachable.

**Two honest caveats that narrow but do not refute it.** (a) A plain non-rebinding cross-origin POST from evil.com *is* blocked: the transport requires `Content-Type: application/json` (webStandardStreamableHttp.js:382-385), which forces a CORS preflight that Aurora answers without `Access-Control-Allow-Origin`. So DNS rebinding is genuinely required — the loopback bind at :118 does block the naive remote path. (b) Chrome's Private Network Access may interfere in some configurations, but that is a browser-side mitigation, absent in Firefox/Safari, and not a code-side guard.

Seat's test claim also verified: `grep -rln "mcp-server" test/` returns nothing — mcp-server.ts has no coverage, so `loopbackOnly` itself is never exercised either.


########## MAIN-A1 [high] /mcp has no Host/Origin guard while /aether does — the stated D8 protection is bypassable via a sibling route with identical capability
file: src/main/mcp-server.ts:65  confidence: probable
CLAIM: The express MCP route POST /mcp is registered with no middleware, while POST /aether and GET /aether/events are wrapped in `loopbackOnly`; both surfaces dispatch the exact same EDITOR_METHODS registry, so the Host/Origin check is a no-op defense.
EVIDENCE: mcp-server.ts:65 `exp.post('/mcp', async (req, res) => {...})` — no middleware argument. Contrast mcp-server.ts:97 `exp.post('/aether', loopbackOnly, ...)` and :107 `exp.get('/aether/events', loopbackOnly, ...)`. The guard itself is at :83-91 and checks `req.headers.host` / `req.headers.origin` against a loopback regex. The comment at :81-82 claims the Aether surface is protected by "loopback bind + Origin/Host check, never remotely exposed (protocol D8)" — but the MCP route exposes the same methods: buildServer() at :33 iterates `EDITOR_METHODS` (editor-methods.ts:47-157), the same array the Aether adapter maps at aether/adapter.ts:24-25. Transport is constructed at mcp-server.ts:68 with only `{ sessionIdGenerator: undefined }`; the SDK's own DNS-rebinding protection is available and not enabled (node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.d.ts:84 allowedHosts, :90 allowedOrigins, :96 enableDnsRebindingProtection). Failure scenario: user has Aurora open; visits attacker.com, which rebinds to 127.0.0.1. The page is then same-origin with the server (no CORS preflight at all), and POSTs to http://attacker.com:38473/mcp with Host: attacker.com. /aether would return 403 'loopback only (protocol D8)'; /mcp accepts. The page then calls tools open_project{dir:'/home/<user>/sonic_hacks/s1disasm'} → edit_chunk/edit_block/set_layout_region → save_project. save_project reaches agent-handler.ts:729-731 `saveClassicProject()` with no confirmation dialog, writing the mutated act through the guarded channel (the mtime guard passes, because Aurora itself holds the current baselines). Net: the user's disassembly is silently rewritten on disk.
FIX: Apply `loopbackOnly` to the /mcp route the same way as /aether (mcp-server.ts:65), and/or construct StreamableHTTPServerTransport with `enableDnsRebindingProtection: true, allowedHosts: ['127.0.0.1:'+port,'localhost:'+port], allowedOrigins: [...]`. Belt-and-braces: both.
VERDICT: CONFIRMED — Could not refute; every link traced independently. (1) mcp-server.ts:65 registers POST /mcp with no middleware — grep for `loopbackOnly` across src/ yields only the definition (:83) and the two /aether routes (:97, :107); the sole app-level middleware is express.json (:62). (2) The transport does not compensate: mcp-server.ts:68 passes only `{ sessionIdGenerator: undefined }`, and the installed SDK v1.29.0 defaults protection off — node_modules/@modelcontextprotocol/sdk/dist/esm/server/webStandardStreamableHttp.js:70 `this._enableDnsRebindingProtection = options.enableDnsRebindingProtection ?? false`, with validateRequestHeaders (:107-109) short-circuiting when false. (Note: the seat cited the .d.ts; the .js confirms the same defaults.) (3) Identical capability: buildServer() iterates EDITOR_METHODS (mcp-server.ts:33) and aether/adapter.ts:24-25 maps the same import, so edit_chunk/edit_block/set_layout_region/save_project are reachable from /mcp. (4) No handshake gate: server/index.js has no "not initialized" rejection for inbound requests, so a cold tools/call on a per-POST stateless server succeeds; transport preconditions are only Accept: application/json + text/event-stream (:378) and Content-Type: application/json (:382), all freely settable on a same-origin fetch from a rebound page. (5) The write lands unconfirmed: agent-handler.ts:729-731 `case 'classic-save-project': requireClassicProject(); return saveClassicProject();` — no dialog, in contrast to classic-open-project at :574-584 which explicitly fails closed because an agent-driven call "has no UI to confirm through". (6) Unconditional exposure: src/main/index.ts:64 starts the server on app.whenReady() with no flag; mcp-server.ts:118 binds 127.0.0.1:38473. Concrete trigger: Aurora running with an s1disasm project open; attacker serves a page on http://attacker.com:38473 that DNS-rebinds to 127.0.0.1, then same-origin POSTs {jsonrpc:"2.0",method:"tools/call",params:{name:"edit_block",...}} then {name:"save_project"} to /mcp with Host: attacker.com:38473 — /aether would 403 at :89, /mcp accepts. Unverified link (stated honestly): whether a 2026 browser still permits the rebound public-origin -> loopback fetch under Chrome's Local Network Access tightening. That is an external mitigation applying equally to /aether, not a property of this code, so it lowers exploitability without making the missing guard correct.
SEV-CORR: Stated severity "high" is correct — hold it. Not critical: the silent on-disk mutation requires an active attacker plus a browser that still allows rebinding-to-loopback, so it does not fire under normal use the way genuine silent-corruption bugs do. Not lower than high either: a fully write-capable route sits beside one explicitly guarded against exactly this threat, under a comment (mcp-server.ts:81-82) claiming the surface is "never remotely exposed (protocol D8)" — a real hole in a stated protection, not a theoretical one.

########## VIS-VIS1 [high] Ctrl+B and Ctrl+K silently arm a destructive map tool alongside their real action
file: src/renderer/components/MapViewport.tsx:677  confidence: certain
CLAIM: MapViewport's tool-letter switch does not guard `o r t b k m` against the Ctrl/Cmd modifier, so the app-level Ctrl+B (toggle Explorer) and Ctrl+K (command palette) each ALSO change the active editor tool on an aeon level tab.
EVIDENCE: Two window keydown listeners fire for the same event and neither stops propagation. App.tsx:113 `else if ((e.key === 'b' || e.key === 'B') && !e.shiftKey && !e.altKey) { e.preventDefault(); toggleExplorer(); }` — preventDefault only. MapViewport.tsx:662 switches on `e.key` with no modifier test reaching it (the early return at :532 is `levelKeysEnabled()`, at :538 the typing guard; neither looks at ctrlKey), and MapViewport.tsx:677 `case 'b': setToolScoped('paint-block'); break;` — unguarded, unlike :672/:673/:678 which explicitly carry `if (!e.ctrlKey)`. facet-tools.ts:20 lists `paint-block` and `stamp-chunk` in the `layout` facet set, so setToolScoped passes. Ctrl+K is the same shape: CommandPalette.tsx:25 toggles the palette, MapViewport.tsx:679 `case 'k': setToolScoped('stamp-chunk')`. FAILURE: on an aeon level tab, layout facet, user presses Ctrl+B to hide the Explorer; the Explorer hides AND the tool becomes paint-block; their next left-click on the map paints a block into the section instead of selecting. Same for Ctrl+K then dismissing the palette with a click on the map — the click lands with stamp-chunk armed. The author knew this class existed: :670-671 comments about exactly this for Ctrl+V.
FIX: Guard every tool case the same way the v/s/c cases already are: hoist one `if (e.ctrlKey || e.metaKey || e.altKey) return;` immediately before `const step = 64;` at MapViewport.tsx:661 (Ctrl+C, Ctrl+V and Delete all return above it, and no arrow/zoom key wants a modifier), and delete the now-redundant per-case `if (!e.ctrlKey)` on :672/:673/:678.
VERDICT: CONFIRMED — Traced end-to-end at SHA 2b38e0b; could not refute. (1) MapViewport.tsx:702 and App.tsx:120 both register bare window keydown listeners and neither stops propagation — App.tsx:113 calls preventDefault() + toggleExplorer() only. (2) The only gates before the tool switch are MapViewport.tsx:532 levelKeysEnabled() (false only for sprite-doc/canvas-doc tab ids, per workspace/level-keys.ts) and :538's INPUT/TEXTAREA/contentEditable check — on Ctrl+B the target is body; on Ctrl+K the palette input does not exist yet (CommandPalette.tsx:36 focuses it in a later requestAnimationFrame). (3) Every conditional return between :532 and the switch requires either a non-ctrl key or non-empty state: :554 Ctrl+C needs a marquee, :576 Ctrl+V needs a clipboard, :592 'is explicitly !e.ctrlKey, :615 is Delete/Backspace. Ctrl+B/Ctrl+K reach switch(e.key) at :662. (4) :677 case 'b' and :679 case 'k' carry no modifier test, unlike :672/:673/:678. (5) setToolScoped at :651 consults toolsForFacet; facet-tools.ts:21 layout = ['stamp-chunk','select','view','marquee','paint-tile','paint-block'], so both pass and setTool runs. (6) facet-registry.ts:169 Canvas: MapViewport — mounted on every aeon map facet, handler live. (7) The armed tool mutates on the next left-click: MapViewport.tsx:979 builds a set-tiles command and calls executeCommand at :1008. Trigger: aeon level tab, layout facet, tool=select, focus on body, press Ctrl+B — Explorer toggles AND tool becomes paint-block; next left-click writes up to 4 nametable entries. Two evidence corrections that do not break the finding: the Ctrl+K story is off by one click (CommandPalette.tsx:85-88 backdrop is fixed/inset:0/zIndex 1000 with onMouseDown close, so the dismiss-click is absorbed; the FOLLOWING click stamps, and stamp-chunk no-ops at :1026 with no chunk selected), and 'silently' is overstated (MapFacetDock renders the active tool, MapViewport.tsx:1500 switches the cursor to 'cell', and the edit is an undoable executeCommand). One case the seat missed strengthens the fix: :667-669 '=', '+', '-', '0' are also unguarded, so Ctrl+plus/minus/0 browser zoom also changes the map zoom.
