SHA 2b38e0bb8332406d71fc13ee8d95159d10cd3a0c  seats=18 missing=[] totals={"findings":159,"critical":6,"high":25}


===== SEAT ART (fable) — 10 findings, 12 clean =====
COVERAGE: Did not get to: running the app under CDP (commit panel gesture, ImportSheetDialog end-to-end, adopt/use-act-colours dialog flows — all judged from source only); indexed-png.ts decode internals (multi-entry tRNS / non-indexed handling), canvas-file-format.ts, zlib-stream.ts; the sprite modules (sprite-decompose/render/palette, export-sprite beyond the known finding), composer-buffer/composer-collision, pixel-edit-controller, rasterize.ts internals (only its doc contract read), atlas-migration, u
  [CRITICAL/certain/CONFIRMED [sev-corr: Severity critical stands, with one correction to the claim's damage description. The art half is loud-ish (the just-comm]] ART-A1 Block tier lacks the three-state availability: matched reclaimed block ids are overwritten by the same plan
      src/core/art/classic-commit-plan.ts:373 — internBlock's pool-reuse scan matches block ids the same plan reclaims and later hands out from the reclaim queue (and ids already given new defs this gesture), so a replaced chunk's unchanged cells end up pointing at a block id the plan overwrites with different art and colind.
  [MEDIUM/certain/unverified] ART-A2 Pool-block reuse matches def spelling, not identity — real GHZ data shows a 100% miss rate for rendered-identical blocks
      src/core/art/classic-commit-plan.ts:377 — The pool-block match compares raw tile/xf/yf/pal spelling against doc.blocks, so stock data's flip spellings (symmetric tiles stored with xf=1, etc.) defeat reuse and re-committing untouched art mints duplicate blocks.
  [MEDIUM/certain/unverified] ART-A3 Two targets naming the same chunk silently drop one region cell's art and the report lies
      src/core/art/classic-commit-plan.ts:269 — Nothing refuses duplicate chunkFileIndex targets: the planner emits two chunkWrites for one index, the store applies them last-write-wins, and the report counts both as replaced.
  [MEDIUM/certain/unverified] ART-A4 'Use the act's colours' remaps NON-drifted entries to the lowest duplicate colour slot, defeating pool reuse
      src/core/art/classic-commit-plan.ts:153 — remapToActColours builds its map over ALL used entries and picks the FIRST same-colour entry in the line, so an entry that never drifted is rewritten to a lower duplicate index; tile bytes then diverge from the pool's spelling and identical art stops matching.
  [MEDIUM/certain/unverified] ART-A5 Reclaimed-but-unwritten slots are never zeroed; orphaned blocks permanently strand tiles and block ids
      src/core/art/classic-commit-plan.ts:189 — Spec §4 step 9 ('reclaimed ids left unwritten ... zero them and say how many in the report') is unimplemented, and the leak is worse than the spec's 'garbage in BlockTab': orphaned blocks keep their tile references alive so neither the blocks nor their tiles are ever free or reclaimable again.
  [MEDIUM/certain/unverified] ART-A6 Commit ignores the canvas gridOrigin the 2B readout honours — the two describe different 8px cells
      src/renderer/components/canvas/canvas-commit-model.ts:214 — planFromSnapshot pins the region at (0,0) and the planner's clash gate hardcodes origin {0,0}, while the 2B constraint readout/overlay computes cells at doc.gridOrigin (mod-8 phase) — with a non-multiple-of-8 origin, the overlay shows clean while commit refuses cell-clash at boundaries the artist ca
  [MEDIUM/certain/unverified] ART-A10 Palette adoption writes whole lines 1-3 while drift names only USED entries — unnamed entries recolour existing zone art silently
      src/core/art/classic-commit-plan.ts:256 — Drift detection filters to entries the committed art uses, but 'adopt into the zone palette' writes all 48 words of lines 1-3 from the canvas palette — a canvas entry the artist edited but never painted with is written into the zone unnamed, recolouring other level art in every act, violating D2's '
  [LOW/certain/unverified] ART-A7 Anim-slot exclusion is post-hoc on findPoolMatch's first hit; a later identical non-anim tile is never tried
      src/core/art/classic-commit-plan.ts:311 — The planner discards the whole match when the first canonical hit is an animated-overlay slot instead of continuing the scan, allocating a duplicate even when a legitimate non-anim duplicate exists; and phase 1's matcher still matches anim slots at all.
  [LOW/certain/unverified] ART-A8 CRAM words compared raw — the spec's single masking helper does not exist
      src/core/art/classic-commit-plan.ts:237 — The drift gate, the remap, and the PNG importer all compare palette words with raw ===, so a stray bit outside $0EEE in a hand-edited palette file causes spurious palette-drift or colour-not-in-act refusals.
  [LOW/certain/unverified] ART-A9 tiles-exhausted refusal reports free:0 hardcoded and 'available' = tiles already placed
      src/core/art/classic-commit-plan.ts:326 — The one refusal whose job is the numbers carries wrong numbers: `available` is the count of tiles bound so far this plan (not slots available) and `free` is a literal 0, so the dialog renders a misleading sentence.
  CLEAN: Tile-0/block-0 floors (spec D3.4): computeReclaim starts at b=1/t=1 (classic-commit-plan.ts:187,196), blank ce | Tile-tier three-state availability is correct: findPoolMatch skips only `allocated`, matched slots stay matcha | tile-canon: canonical form is lexicographic min over identity/x/y/xy only — no transpose (tile-canon.ts:79-95) | D3 positional inheritance is enforced end to end: chunk cells keep the displaced cell's xf/yf/solidity and its | D5 (whole-chunk minimum) enforced: region must be 256-aligned and target-counted (classic-commit-plan.ts:214-2 | D6 (PNG never becomes a canvas doc) enforced: import-sheet.ts:59-92 goes decodeIndexedPng -> importPngAgainstP | Chunk id spaces are consistent: targets, reclaim, blockToChunks all FILE-indexed; engine ids appear only in la | Ceilings: MAX_ADDRESSABLE_CHUNKS = 0x7f defined once (model.ts:208) and enforced in both planner (classic-comm | Line-0 palette protection is double-doored: planner refuses line-0 drift unconditionally (classic-commit-plan. | classicCommitCanvas is one commitArt across all five ART_DOMAINS with tileLockReason re-validation per tile wr | canvas-doc pixel-space normalization: canvasIndex is the single constructor folding entry-0 spellings and out- | Predicates-unknown refusal fires only when something is actually replaced, allowing additive commits under unk

===== SEAT SAVE (fable) — 9 findings, 12 clean =====
COVERAGE: Did not run the app, tests, or any CDP session — this is a static trace only (per brief the tree is read-only). Not audited: aeon load/save round-trip at the format level (serializeNametable/serializeCollAttr/serializeBgTiles vs their readers — only the write orchestration was reviewed); the aeon export step (exportAct) contents; core/art/canvas-file-format encode/decode internals (relied on the module's own tests existing, not verified byte-level); session-lifecycle.ts / session-persistence res
  [HIGH/certain/CONFIRMED] SAVE-A1 No unsaved-work guard on window close / app quit
      src/main/index.ts:56 — Closing the Aurora window (X button / Ctrl+W / SIGTERM-initiated quit) discards all dirty classic/canvas/sprite/aeon work with no prompt, while every other exit door in the app prompts.
  [HIGH/certain/CONFIRMED [sev-corr: None — 'high' is correct and arguably conservative. By the rubric this is silent loss of user work (critical territory: ]] SAVE-A2 Dirty flags cleared unconditionally after async save — edits landing mid-save are silently marked clean (class: 3 of 4 save paths)
      src/renderer/state/classic-save.ts:205 — Classic, canvas, and aeon saves all clear dirty state captured before the write without checking whether the document changed during the IPC/write await, so an edit committed mid-save is left un-flagged while its bytes are not on disk; the sprite path already fixes this exact race, proving the class
  [MEDIUM/certain/unverified] SAVE-A3 Aeon save path has no mtime guard, no rel-path guard, and unreported partial batches
      src/renderer/state/aeon-save.ts:27 — saveAeonProject writes every plan file through the unguarded writeBinaryFile channel: an externally-edited aeon file is silently clobbered (no conflict check), and a throw mid-loop leaves an undisclosed half-written multi-file act (e.g. section_0.tiles.bin new, its objects.json old).
  [MEDIUM/certain/unverified] SAVE-A4 WRITE_BINARY_FILE IPC has no rel-path guard; reachable with a free-typed sprite name
      src/main/ipc-handlers.ts:81 — The write-binary channel resolves basePath+relativePath with no isRelPathSafe check, so a relativePath containing ../ (or an absolute path) writes anywhere on disk — and exportSprite feeds it a raw user-typed sprite name as a path segment.
  [LOW/probable/unverified] SAVE-A5 Project Setup 'Apply' rewrites a sidecar Aurora failed to read, from an empty config
      src/core/project/s1/index.ts:212 — When .aurora/project.json existed but could not be read at open (fs error path), readSidecarState degrades to config {} — and a later Apply in Project Setup serializes that empty config over the file, destroying contents Aurora never saw; the canvas layer treats this exact case as must-not-overwrite
  [LOW/probable/unverified] SAVE-A6 Rapid double Ctrl+S can produce a false 'changed on disk — reload project' conflict
      src/core/editing/save-coordinator.ts:98 — SaveCoordinator has no reentrancy guard, so two overlapping saveClassicProject runs can both build payloads from the same pre-write mtime baseline; if the second's main-side stat lands after the first's rename, the second reports a conflict against Aurora's own write and the toast tells the user to 
  [LOW/certain/unverified] SAVE-A7 recent-projects.json written non-atomically
      src/main/recent-projects.ts:36 — addRecentProject/removeRecentProject use bare writeFileSync with no tmp+rename, so a crash mid-write corrupts the file; the reader self-heals to [] so the blast radius is only losing the recents list.
  [LOW/certain/unverified] SAVE-A8 Save→undo leaves dirty flags clean while doc differs from disk (documented, accepted)
      src/renderer/state/classicLevelStore.ts:267 — After edit→save→undo, the restored snapshot's dirty={} leaves the doc diverged from disk with a clean indicator, so Ctrl+S no-ops and close prompts stay silent — the code itself documents this as an accepted v1 limitation, recorded here so the overseer knows it is deliberate, not missed.
  [LOW/speculative/unverified] SAVE-A9 Atomic-rename claim excludes durability: no fsync before rename
      src/main/guarded-write.ts:92 — guarded-write's header says a crash mid-write 'can never leave a half-written target', which is true for process crashes but not power loss: writeFile+rename without fsync of the tmp file (and its directory) can leave a zero-length or stale target on some filesystem configurations.
  CLEAN: Guarded classic write is genuinely guarded, not decorative: the exact rejected input is 'on-disk mtimeMs diffe | The mtime baseline is armed in production, not just in tests: readMany returns per-file mtimes (src/main/file- | Per-file write atomicity holds on the classic/canvas path: sibling .tmp + same-dir rename, orphan cleanup, str | Every classic domain writer is self-check-gated against the REAL decoder before bytes are emitted — tiles/bloc | Dirty-domain clearing after save is conservative w.r.t. FILES: a domain clears only when every one of its reso | Canvas sidecar corruption handling is exemplary: an unreadable/unparseable sidecar is never overwritten (R12,  | Sprite in-place art save handles the mid-save-edit race correctly — re-reads the doc after the await and clear | Objpos round-trip: writer sorts by X only when the list actually changed, preserving byte-identity for untouch | .aurora/project.json parsing preserves unknown top-level fields through a round-trip (looseObject + spread, ma | MCP/agent save entry routes through the same guarded classic save, not a side door (editor-methods.ts:155-156  | project.json / bindings sidecar writes via the write-binary channel are tmp+rename atomic (ipc-handlers.ts:86- | No second writer targets classic level files: level bytes go only through saveClassicProject's guarded batch; 

===== SEAT FMT (opus) — 5 findings, 17 clean =====
COVERAGE: "DID NOT GET TO / EXPLICIT GAPS.\n\n1. Environment died mid-sweep. After the intentional OOM repro for A1, the Bash tool started returning exit 1 for every command including `echo`. The unfinished experiment was the A1 reachability sweep: feeding wrong-format real files (all-zero 4KB, all-0xFF 4KB, a .nem, a .eni, an .asm, a layout .bin) to kosinskiDecompress to enumerate which plausible mis-pathed file triggers the runaway. Only the synthetic `new Uint8Array([0x00,0x00])` case ran to completion
  [HIGH/certain/CONFIRMED [sev-corr: No change — "high" is correct as stated. Recording the reasoning since I checked it both directions: it fails the "criti]] FMT-A1 kosinskiDecompress loops without bound on malformed input — exhausts the heap and kills the process
      src/core/formats/kosinski.ts:20 — kosinskiDecompress has no EOF error, no output cap and no iteration guard, so a .kos file whose descriptor words are zero decodes forever, growing an unbounded JS array until V8 aborts the process — which no try/catch can intercept.
  [MEDIUM/certain/unverified] FMT-A2 kosinskiModuledCompress cannot express two size classes; the sprite-export path that uses it has no self-check
      src/core/formats/kosinski.ts:166 — kosinskiModuledCompress writes the decompressed size as a bare 2-byte big-endian field, so an input of exactly 0xA000 bytes collides with the format's 0xA000->0x8000 sentinel (8192 bytes silently lost) and any input >= 0x10000 wraps to a header of 0x0000 (everything lost), and the only caller that c
  [MEDIUM/certain/unverified] FMT-A3 Enigma encoder inflates every re-saved map16 file 1.33x-2.0x, and unlike Nemesis has no size-regression test
      src/core/formats/classic/enigma.ts:123 — enigmaCompress emits every word as a full 16-bit inline raw-copy, never using the incremental/literal/run packets the format exists for, so saving blocks writes a substantially LARGER file back into the user's disasm — the exact ROM-growth hazard the Nemesis golden test was written to prevent, with 
  [LOW/certain/unverified] FMT-A4 nemesisCompress silently wraps the 15-bit tile count above 0x8000 tiles
      src/core/compress/nemesis.ts:333 — encodeNemesis masks the tile count into the header's 15-bit field with no range check, so a >= 1 MiB tile pool produces a header claiming 0 tiles and a stream that decodes to nothing.
  [LOW/certain/unverified] FMT-A5 kosinskiModuledDecompress advances its write cursor by the unclamped module length
      src/core/formats/kosinski.ts:186 — When a module decodes to more bytes than the declared total size leaves, `written` is incremented by the full output length rather than the clamped copy length, so the next out.set() offset can exceed the buffer (RangeError) or the remaining-size arithmetic goes negative and silently drops the rest.
  CLEAN: Nemesis DECODER is byte-identical to the game-accurate oracle (clownnemesis v1.1.1 at /home/volence/sonic_hack | Nemesis ENCODER (the 9579a3b XOR + package-merge work) produces streams the game-accurate oracle decodes back  | Nemesis emitted code-table invariants hold on every one of those 301 stress cases: every code length is 1..8,  | Nemesis edge cases all round-trip through BOTH Aurora and the native oracle: zero-length input (encodes to the | Nemesis XOR mode's cross-tile chain is correct: xorDelta (nemesis.ts:278-291) initialises prev=0 and never res | Nemesis re-encode size across ALL 156 real artnem files: 180889 -> 185824 bytes total (1.0273x). No file excee | Kosinski DECODER matches Clownacy's accurate-kosinski (kosinski-decompress) byte-for-byte on all 9 real .kos f | Kosinski ENCODER is verified bidirectionally against the accurate reference: Aurora's compressed output decode | Kosinski encoder output is slightly SMALLER than Sega's on every real file (e.g. GHZ 8378 vs 8464, MZ REV00 60 | Kosinski-MODULED decoder matches kosinskim-decompress on all 95 real KosinskiM assets in /home/volence/sonic_h | Enigma DECODER matches clownlzss (`kosdec -e -d`) byte-for-byte on all 12 real .eni files (6 map16 + 6 tilemap | The emitCompressed self-check gate at src/core/level-classic/s1-io.ts:398-417 is a REAL gate, not a no-op: it  | S1 layout round-trips byte-identical on all 29 files in s1disasm/levels, including the irregular ones the modu | S1 startpos round-trips byte-identical on all 35 files in s1disasm/startpos; S1 colind round-trips byte-identi | S1 objpos round-trips byte-identical on all 14 real object tables in s1disasm/objpos, with the 0xFF originalLe | Genesis palette codec is an exact inverse for all 8 three-bit levels (encodeGenesisColor(decodeGenesisColor(w) | Codec wall-clock is not a UI-freeze hazard on real data: Nemesis encode 23ms for the largest art file (8x8 - G

===== SEAT CLASSIC (opus) — 7 findings, 19 clean =====
COVERAGE: DID NOT COVER — state these as gaps, they were not reviewed: (1) src/core/collision/** turned out to be the AEON/s4 collision system (Section.collisionEdit, packCollisionCell's 0x3FF/0x400/0x800/12-13 layout, collision-paint, the S&K profile bank), NOT S1's; I read it for cross-checking conventions (that is how I found A3) but did NOT audit its correctness, its adapters, or the aeon `.collattr*` contract. (2) The 2026-08-08 chunk-collision + map-clipboard spec is entirely aeon-scoped (ChunkDef.c
  [CRITICAL/certain/CONFIRMED] CLASSIC-A1 MCP get_classic_level silently destroys unsaved classic edits + undo history
      src/renderer/agent/agent-handler.ts:625 — The agent/MCP `get_classic_level` handler calls `useClassicLevelStore.getState().openAct(ref)` unconditionally, and `openAct` disposes both undo stacks and resets `dirty` to `{}` before re-reading from disk — so any unsaved classic edits are destroyed with no confirmation, no error, and no way back,
  [MEDIUM/certain/unverified] CLASSIC-A2 Layout byte $80 (loop flag over air) passes validation and makes the engine read an odd RAM address
      src/renderer/state/classicLevelStore.ts:772 — `classicSetLayoutCells` validates the MASKED engine id (`chunkId & 0x7f`) against the pool but never rejects the one byte where the loop flag is set over air, $80; the editor draws that cell as air, while S1's FindNearestTile takes the `.specialtile` branch, computes a1 = $FFFFFE01 (odd) and the cal
  [MEDIUM/certain/unverified] CLASSIC-A3 Classic collision-angle needle is drawn mirrored (missing sign negation)
      src/renderer/components/classic/classic-overlays.ts:85 — `drawCollision` converts the S1 angle byte to a needle with `a = (ang/256)*2π` and then draws it in a y-up (CCW) frame, but S1 angle bytes are clockwise-positive; every non-flat angle tick is therefore mirrored about the horizontal, showing a down-right needle over an up-right slope.
  [MEDIUM/certain/unverified] CLASSIC-A4 colind shorter than the block list: unreachable via set-colind, rendered as air, and zero-filled on save over what the ROM reads as full-solid
      src/renderer/state/classicLevelStore.ts:1137 — S1 colind files are legitimately shorter than the block list (GHZ ships 410 bytes for 439 blocks; SBZ 600 for 602), and the ROM resolves the overhang from the adjacent zone's table. Aurora models the overhang as shape 0 everywhere: the overlay draws no collision for it, `classicSetColind` refuses to
  [MEDIUM/certain/unverified] CLASSIC-A5 Object save re-sorts by X on any edit; the justifying comment names one act when five are affected, and GHZ3's sort swaps two respawn slots
      src/core/level-classic/s1-io.ts:531 — `writeS1Level` stable-sorts the object list by X whenever it differs from disk, justified by a comment claiming only GHZ act 3 is non-ascending. Five of the eighteen profiled acts are non-ascending, and for GHZ act 3 the sort reorders two remember-flagged objects, remapping their respawn-table slots
  [LOW/certain/unverified] CLASSIC-A6 classicSetColind allows writing colind[0], which the engine can never consult (block 0 short-circuits)
      src/renderer/state/classicLevelStore.ts:1136 — FindFloor tests the block id BEFORE solidity and treats block 0 as blank, so colind[0] is dead data. `classicSetColind` accepts blockId 0, and the overlay would then draw collision on every chunk cell holding block 0 with non-zero solidity — collision the engine never applies.
  [LOW/certain/unverified] CLASSIC-A7 The colind save self-check is a tautology — it cannot fail
      src/core/level-classic/s1-io.ts:561 — `writeS1Level`'s colind gate re-decodes the buffer it just encoded and compares it to the same source array, but both codecs are `slice()`, so the comparison is `bytesEqual(x.slice().slice(), x)` — always true. The module docblock advertises this domain as gated by a real decoder round-trip.
  CLEAN: map16 / map256 codec shapes match the real disasm exactly: enigma-decoded map16 is 8 bytes per block (0 remain | Chunk-cell and block-cell bit packing is lossless on 100% of real S1 data: I round-tripped every word of all s | The two-tier collision gate itself is modelled correctly for solidity: classic-overlays.ts:66 skips `cell.soli | Chunk-cell flip semantics in the collision overlay are byte-faithful to FindFloor. X-flip: classic-overlays.ts | Layout codec is byte-identical round-trip on all 29 real level files including the three irregular ones (mz3bg | Layout dimension limits match the engine's RAM: `MAX_LAYOUT_W = 64` / `MAX_LAYOUT_H = 8` (model.ts:210-211) eq | Chunk addressability limit is right: `MAX_ADDRESSABLE_CHUNKS = 0x7f` (model.ts:208) is justified by FindNeares | The 1-based chunk-id shift lives in exactly one place and every consumer routes through it: `chunkIndexForId`  | The loop-flag stamp path cannot arm bit 7 over air: ClassicLevelViewport.tsx:904 `s.stampLoop && id >= 1 && id | Ring group expansion is a faithful port: `RING_POS_DATA` (viewport-math.ts:287-294) is byte-for-byte s1disasm/ | objpos entry format matches the engine and round-trips byte-identically on all 18 profiled files (including th | Collision shape tables are decoded with correct sign handling: `new Int8Array(b.subarray(...))` (s1-collision- | Collision inheritance on block cloning is handled deliberately and correctly in both paint paths: `classicPain | Block reclaim is conservative and cannot strand a live reference: `computeReclaim` (classic-commit-plan.ts:180 | Zone-art file paths are consistent per zone in the profile (all three GHZ acts → map16/GHZ.eni, map256/GHZ.kos | Undo stacks are dropped on every act load (`disposeStacksFor` for both the outgoing and incoming ref, classicL | The save path's source of truth is `WriteResult.files`, never `written` (classic-save.ts:59), self-check error | The compression codecs are round-trip verified against the real disasm files by the repo's own golden suite pl | All 205 tests in src/core/formats/classic, src/core/level-classic and classic-commit-plan pass at the pinned c

===== SEAT UNDO (opus) — 11 findings, 10 clean =====
COVERAGE: Read in full: src/core/editing/{history,commands,snapshot-history,canvas-history,sprite-history,classic-domain-history,document-history,bound-edit-history,undo-stack,save-coordinator,section-ops}.ts; src/renderer/state/{editorStore,classicLevelStore,canvasStore,artStore,projectStore,history-factories,history-hub,aeon-save,import-sheet}.ts; spriteStore.ts:250-620; src/renderer/hooks/useHistoryVersion.ts; shell/{dirty-tabs,dirty-snapshot,tab-activation/dispatch,tab-activation/level}.ts; components
  [CRITICAL/certain/CONFIRMED [sev-corr: Severity stands at critical. It meets the bar literally — silent, unrecoverable loss of unsaved user work (the undo stac]] UNDO-A1 MCP `classic-get-level` (a read tool) silently discards the loaded act's unsaved edits and both undo stacks
      src/renderer/agent/agent-handler.ts:625 — `classic-get-level` calls `useClassicLevelStore.openAct(ref)` with no dirty check and no confirm; `openAct` disposes both undo stacks and re-reads the LevelDoc from disk with `dirty: {}`, so every unsaved classic edit in the currently-loaded act is destroyed by what the agent surface presents as a r
  [CRITICAL/certain/CONFIRMED [sev-corr: high — the defect is real and silent, but the magnitude is one placement per occurrence and the on-disk project is never]] UNDO-A2 Leaving the aeon map viewport mid-drag drops an object/ring move: doc already mutated, no command, no dirty flag
      src/renderer/components/MapViewport.tsx:1535 — An object/ring drag mutates `obj.x/obj.y` in place on every mousemove and only becomes a `move-object` command on mouseup; `onMouseLeave` nulls `dragTarget` without committing, so a drag that exits the viewport leaves the placement moved in the document with no undo entry and with `editorStore.dirty
  [HIGH/certain/CONFIRMED [sev-corr: Severity high is correct as stated; explicitly NOT critical — the painted BG data is neither lost nor corrupted (it pers]] UNDO-A3 BG-layer tile painting mutates the layout with no command — un-undoable, and Ctrl+Z reverts an unrelated FG edit instead
      src/renderer/components/MapViewport.tsx:764 — `paintBgTile` writes straight into the resolved BG nametable and calls `markDirty()` + `bumpLiveEdit()` with no command dispatch, so every BG paint stroke is outside history; the next Ctrl+Z on the act tab pops whatever FG/object command preceded it and silently reverts that instead.
  [HIGH/certain/CONFIRMED [sev-corr: medium — no silent loss or corruption (the doc is a detached copy, edits are flagged unsaved, and discarding is confirm-]] UNDO-A4 Aeon composer's tile-space tools and doc-local pixel path mutate the open document with no history entry
      src/renderer/components/art/ComposerCanvas.tsx:342 — `applyTileCell` (tile-stamp, collision paint, palette-apply) and the doc-local branch of `commitWrites` mutate the open ComposerDoc in place and only call `markOpenDirty()` + `bumpDoc()`; neither reaches an undo stack, so Ctrl+Z on the art facet reverts an unrelated `set-tileset-tiles`/`set-chunk` c
  [HIGH/certain/unverified] UNDO-A5 Aeon map paint-tile / paint-collision drags record one undo entry per cell
      src/renderer/components/MapViewport.tsx:1213 — The paint-tile and paint-collision drag handlers call `executeCommand` per crossed cell instead of accumulating the gesture and committing once, so one pencil drag becomes tens of undo entries and, at `MAX_HISTORY = 200`, a couple of drags evict the rest of the session's history.
  [HIGH/certain/unverified] UNDO-A6 Undoing a sprite frame delete restores the frame but not the animation steps it re-indexed
      src/renderer/state/spriteStore.ts:541 — `deleteFrame` records a `SpriteSnapshot` and then writes both `frames` and `steps`, but `steps` is not a snapshot field — so undo restores the frame while every timeline step that referenced a later frame stays shifted down by one, permanently mis-pointing the animation.
  [MEDIUM/probable/unverified] UNDO-A7 Saving a NEW chunk appends tiles undoably but adds the chunk un-undoably — one Ctrl+Z strands the chunk pointing past the tileset
      src/renderer/workspace/facets/art-facet.tsx:137 — The save-chunk path issues `set-tileset-tiles` (an undoable append) and then `useProjectStore.getState().addChunks([saved])`, which is outside history; undoing the tile append leaves a chunk in the library whose nametable references tile indices that no longer exist.
  [MEDIUM/probable/unverified] UNDO-A8 Agent-issued classic art edits record on `zoneart:` while the focused facet still routes Ctrl+Z to the layout stack
      src/renderer/agent/agent-handler.ts:719 — `classicSetPalette` / `classicEditTiles` / `classicEditBlock` / `classicEditChunkCells` invoked from the agent record on `zoneart:<zone>` (commitArt), but nothing on that path claims the `art` facet, so if the user is on the layout facet the next Ctrl+Z drives `level:<zone>:<act>` and reverts an unr
  [MEDIUM/certain/unverified] UNDO-A9 Every sprite undo entry deep-clones the whole frame set (50 deep, per open document)
      src/core/editing/sprite-history.ts:20 — `cloneSpriteSnapshot` copies EVERY frame buffer on record, undo and redo, so a one-pixel stroke on a 100-frame character costs a full copy of all 100 frames, and residency is 50 × (total frame bytes) per open sprite document — the cost canvas-history.ts explicitly budgeted for its own surface but ne
  [MEDIUM/certain/unverified] UNDO-A11 core/editing/history.ts — the aeon command engine with 20 hand-rolled inverses — has no unit test
      src/core/editing/history.ts:1 — There is no test file for `EditHistory`/`applyCommand`/`undoCommand`; the index-sensitive inverses (`add-object` → `pop`, `add-rings` → `splice(len - n, n)`, `delete-objects` → sort-ascending re-insert, `set-tileset-tiles` → backwards splice of appended slots) are entirely unexercised, and 6 of the 
  [LOW/certain/unverified] UNDO-A10 applyCommand/undoCommand silently no-op on a missing target, burning an undo slot that reverts nothing
      src/core/editing/history.ts:98 — `if (!section) return`, `if (arr)`, and the `if (section.objects[cmd.objectIndex])` guards make a command that cannot be applied succeed silently — it is still pushed onto the undo stack and still clears redo, so the user gets a dead undo step and loses their redo branch for nothing.
  CLEAN: Per-document stack routing is sound and single-sourced: `focusedDocId()` (state/editorStore.ts:205-224) is the | No cross-engine stack bleed. The hub is a singleton (state/history-hub.ts:7) but `registerHistoryFactories` di | SnapshotHistory's memory invariant actually holds. `record` pushes then caps (`if (this.undoStack.length > thi | One-gesture-one-entry holds on every CLASSIC editing path I traced. Tile pixels: TileTab.tsx:248-270 funnels s | The classic dirty/undo domain split is enforced, not merely documented. `assertSingleDomain` (classicLevelStor | The aeon zone-palette live preview — the original member of the mutate-the-doc-without-history class — is now  | canvasStore's record-vs-dirty split is complete and each setter is auditable against the list at canvasStore.t | Tab close disposes the right stacks and no more: a level tab drops its own layout stack but keeps `zoneart:<zo | Redo invalidation is correct everywhere: `SnapshotHistory.record` sets `this.redoStack = []` (snapshot-history | `ensureCollisionPlanes` (core/collision/collision-cell-resolve.ts:59-63), called outside command dispatch from

===== SEAT ERR (opus) — 10 findings, 15 clean =====
COVERAGE: "NOT COVERED — stated honestly.\n\n(1) No runtime verification. This is a pure static trace of the pinned tree; I did not launch the app, drive it under CDP, or run the vitest suite, so I did not confirm any of these paths fires in practice nor check which have tests. Per the seat memory ('guards assert nothing'), A3 and A4 in particular deserve a planted-violation check before being treated as settled.\n\n(2) I read every catch/`.catch(`/`void `/toast site on the save, export, import, load, IPC
  [HIGH/certain/CONFIRMED] ERR-A1 Aeon save toasts "Project saved" after the export step silently failed (console.warn only)
      src/renderer/state/aeon-save.ts:31 — When exportAct() throws, buildAeonSavePlan swallows it into a string field and aeon-save.ts reports it only with console.warn, then calls markClean() and toasts 'Project saved' — the user is told the save succeeded while every export artifact was silently omitted from the write.
  [HIGH/certain/CONFIRMED [sev-corr: None — high is appropriate. It is silent destruction of user work (which argues for critical), but the trigger requires ]] ERR-A2 A section's objects.json/rings.json that exists but fails to parse loads as [] and the next save writes [] over it
      src/core/project/aeon/load.ts:262 — The aeon loader cannot tell 'file absent' from 'file present but unreadable/unparseable': both land in the same bare catch and set section.objects = [] / section.rings = [], and buildAeonSavePlan then unconditionally writes those empty arrays back, permanently destroying every object and ring placem
  [MEDIUM/certain/unverified] ERR-A3 An unreadable mappings .asm silently empties the object tile-reservation set, and the 'predicates-unknown' refusal cannot see it
      src/core/project/profiles/s1-levelart-reservations.ts:110 — readMapTextTolerant swallows every read failure into null, buildReservedTileSet skips those requests with `continue`, and the resulting set is non-null-but-empty — so classic-commit-plan's `reservedTiles === null` refusal never fires and the tile allocator will hand out pool tiles that object sprite
  [MEDIUM/probable/unverified] ERR-A4 CommitPlanView.apply is the one classic-command call site with no try/catch, so a throw makes Apply silently do nothing
      src/renderer/components/canvas/CommitPlanView.tsx:88 — Every other classic-command invocation from a React handler explicitly wraps the call ('nothing may escape this handler'), but the canvas/import-sheet Apply button calls classicCommitCanvas bare — a throw from its history-invariant guards escapes the onClick, produces no message, and leaves the user
  [MEDIUM/certain/unverified] ERR-A5 exportSprite writes five files sequentially with no rollback; a mid-sequence failure leaves a mismatched sprite directory
      src/renderer/components/sprite/export-sprite.ts:95 — The five artifact writes plus the index upsert are independent awaits inside one try; a failure after the first write leaves new art.bin/mappings.bin next to the PREVIOUS export's sprite.json and dplc.bin, and the toast says only 'Export failed', never that the directory is now half-updated.
  [MEDIUM/certain/unverified] ERR-A6 classic-save-project returns a FAILED save as a successful MCP tool result
      src/renderer/agent/agent-handler.ts:731 — The agent/MCP save tool returns saveClassicProject()'s outcome variant as the tool's success payload, so a conflict, partial write or self-check failure is delivered as ok:true with no isError flag — an agent that checks call success proceeds as if the level was written.
  [LOW/certain/unverified] ERR-A7 Every PNG decode failure in the sheet importer is relabelled as 'needs an INDEXED PNG'
      src/renderer/state/import-sheet.ts:67 — The catch appends '— the importer needs an INDEXED (paletted) PNG' to every decodeIndexedPng error, including truncated IDAT, interlacing and bad-signature errors, so the UI misdiagnoses the file and sends the user to re-export as indexed when it already is.
  [LOW/probable/unverified] ERR-A8 ImportSheetDialog.pick has try/finally with no catch — a rejection clears 'Reading…' and shows nothing
      src/renderer/components/canvas/ImportSheetDialog.tsx:28 — pick() awaits loadSheetForAct inside try/finally with no catch, so any rejection (the file dialog channel failing, or a throw out of importPngAgainstPalette/flattenActPalette, which sit outside import-sheet's own try) becomes an unhandled promise rejection and the dialog resets to its idle state wit
  [LOW/certain/unverified] ERR-A9 A corrupt recent-projects.json is silently reset to empty and then overwritten
      src/main/recent-projects.ts:19 — getRecentProjects swallows a JSON.parse failure into [], and the very next addRecentProject writes that [] plus one entry back over the file, discarding the user's whole recents list with no notice.
  [LOW/speculative/unverified] ERR-A10 requestCloseTab is fire-and-forget from the tab strip; a rejecting close confirm silently leaves the tab open
      src/renderer/shell/TabStrip.tsx:36 — The close button calls `void requestCloseTab(tab.id)`, and requestCloseTab awaits confirmCloseSpriteDoc/confirmCloseCanvasDoc without guarding them — a rejection (notably the dynamic import of export-sprite failing inside confirmCloseSpriteDoc) becomes an unhandled rejection and the close silently d
  CLEAN: Classic save path is honest end to end: saveClassicWriteResult (src/renderer/state/classic-save.ts:50-91) neve | Canvas save path: canvas-file.ts:282-319 folds mtimes correctly for unwritten/never-queued files (mtimeAfterWr | Guarded write in main (src/main/guarded-write.ts:56-105): rel-path guard throws before any write, the conflict | Close/discard confirms re-read the dirty flag instead of trusting 'it did not throw': confirmCloseSpriteDoc (t | Project-open guard (src/renderer/shell/project-open-guard.ts:145-162) re-snapshots after 'Save & open' and abo | activateLevelTarget's 'Save & switch' (tab-activation/level.ts:187-192) gates openAct on isSaveSuccess(), and  | createCanvasDocument (src/renderer/shell/new-canvas.ts:302-384) writes both files BEFORE opening the tab, and  | The classic level loader does not substitute defaults for unreadable files: readS1Level's readBytes (src/core/ | Write self-checks are real and their failures are surfaced: emitCompressed/emitVerified push to `errors` (s1-i | The three classic art-editing handlers explicitly convert command throws into toasts so nothing escapes a poin | Toasts are actually reachable: ToastContainer is mounted unconditionally at App root (App.tsx:293), outside ev | IPC error semantics survive the preload boundary: ENOENT is returned as a marker (ipc-handlers.ts:19-27) and r | The agent bridge cannot leave a caller hanging: requestAgent has a 30s timeout, a window-closed listener and p | The Aether adapter converts a forwarded throw into a JSON-RPC error rather than swallowing it (src/main/aether | Counts for the sweep: 120 catch sites in non-test src/. 23 of them are silent (13 one-line empty/comment-only,

===== SEAT GUARD (fable) — 8 findings, 14 clean =====
COVERAGE: Did NOT plant in: the aeon backend (aeon-save/aeon-load/aeon-adapter), the s1 adapter/profile resolution layer, layout/objpos/startpos/colind codecs (only their self-check gate), chunk-invalidation and selection-to-chunk (editing), agent-handler, palette drag/copy/apply, sprite decompose/render, session persistence, explorer/tab-activation, canvas-budget, or any compression codec internals — plants were 18 targeted mutations on the brief's named guard areas, not suite-wide mutation testing, so p
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> medium. The rubric's "high" is wrong output or a broken main-path feature, but HEAD emits correct plan]] GUARD-A1 Commit-plan block reuse ignoring collision mismatch is untested
      src/core/art/classic-commit-plan.ts:374 — The guard that forbids reusing a visually-identical pool block whose colind differs (classic-commit-plan.ts:371-374) is pinned by no test — deleting the colind check leaves all 24 classic-commit-plan tests green.
  [MEDIUM/certain/unverified] GUARD-A2 planSurfaceEdit locked-tile refusals untested in both paint modes
      src/core/art/classic-surface-plan.ts:175 — Both 'tile N is not editable' refusals in planSurfaceEdit (link mode :175-177, isolate mode :209-211) can be deleted without failing any test — the Paint path's lock enforcement wiring is unpinned.
  [MEDIUM/certain/unverified] GUARD-A3 Commit-plan freeSlot allocation filter (reserved/anim/locked) untested
      src/core/art/classic-commit-plan.ts:299 — Deleting the `reserved.has(t) || animTiles.has(t) || !isEditableTile(t)` filter from the commit planner's freeSlot (classic-commit-plan.ts:299) leaves all 24 tests green — nothing proves a canvas commit refuses to allocate into object-reserved, animated, or non-editable tile slots.
  [MEDIUM/certain/unverified] GUARD-A4 blocks-exhausted and tiles-exhausted refusals have no core-level test
      src/core/art/classic-commit-plan.ts:388 — Removing the MAX_BLOCKS_TOTAL ceiling (`if (id >= MAX_BLOCKS_TOTAL) return null;` at :388) leaves all tests green; the 'blocks-exhausted' and 'tiles-exhausted' refusal kinds are exercised nowhere against the real planner.
  [MEDIUM/certain/unverified] GUARD-A5 Both save self-check gates: byte-mismatch arm is dead code to the suite
      src/core/level-classic/s1-io.ts:408 — The re-decode-and-compare halves of the two write-path self-checks (s1-io.ts emitCompressed :408 and s1-art-write.ts encodeS1ArtWriteBack :121) can be disabled (`if (false && ...)`) with every test still green — only the decoder-THROWS arm is ever exercised, though both modules' headers claim tests 
  [MEDIUM/certain/unverified] GUARD-A6 performGuardedWrite (main-process fs half of guarded save) has zero tests
      src/main/guarded-write.ts:54 — The fs-effecting half of the guarded save — unsafe-relPath throw, tmp+rename atomic write, orphan-tmp cleanup, and the partial-batch {written,newMtimes,failed,unwritten} contract — is exercised by no test, despite the header (:4-5) saying it was extracted 'so the whole cycle is unit-testable headles
  [LOW/certain/unverified] GUARD-A7 32 test files pin .tsx behavior by regex over source text
      src/renderer/workspace/__tests__/classic-art-dock.test.ts:152 — 32 of 171 test files assert by readFileSync-ing .tsx source and matching text (e.g. classic-art-dock.test.ts:152 pins a literal `finally { ...clearAction(); }` shape; facet-modules 8 such blocks, facet-visibility 6, palette-grid-wiring 6, map-status-model 5) — these pass whenever the text survives, 
  [LOW/probable/unverified] GUARD-A8 Baseline suite is flaky: 2 facet-mount tests failed once, green on rerun
      src/renderer/workspace/__tests__/facet-modules.test.ts:118 — At the pinned commit the first full `npx vitest run` failed 2 tests ('s1ArtFacet mounts the pixel chrome › declares both a ToolDock and a ToolOptions' via dynamic import of s1-facets, plus one sibling) and passed 2914/2914 on two immediate re-runs — a nondeterministic failure mode that trains people
  CLEAN: save-guard decision table (src/core/project/__tests__/save-guard.test.ts): plants CAUGHT — 'edited externally' | Collision carry D3 (classic-commit-plan.ts:411,413): plants CAUGHT — forcing colind=0 → 2 failures; forcing so | Slot double-booking regression (classic-surface-plan.ts:106): plant CAUGHT — removing the `taken` check → 1 fa | canvas-resolve flip canonicalization (canvas-resolve.ts:120): plant CAUGHT — dropping canon.xf/yf propagation  | Zoom-per-tier (artStore.ts:90 selectArtZoom): plant CAUGHT — hardwiring the 'tile' tier → 4 failures in art-zo | PNG import validation (png-import.ts:146 cell-needs-two-lines): plant CAUGHT — swallowing the unexpressible ce | Locked-tile PREDICATE (editable-tiles.ts:37 animRanges): plant CAUGHT — 2 failures across editable-tiles.test. | Canvas sidecar version gate (canvas-file-format.ts:127): plant CAUGHT — disabling the version check → 3 failur | free-tile-slots readout parity (free-tile-slots.ts:31): plant CAUGHT — dropping the reserved exclusion → 1 fai | Commit-plan refusal tests use `expect(r.ok).toBe(false)` BEFORE the `if (!r.ok)` narrow (classic-commit-plan.t | Static sweep of all 171 files / ~2138 it-blocks: true zero-assertion tests ≈ 0 — 11 regex flags all resolved t | Mock-assertion flags (12 blocks) are legitimate interaction tests, not mocks of the unit under test — e.g. cla | The 'legacy' re-implementations in zoom-cap.test.ts:21 and zoom-anchor.test.ts:42-43 are extraction-parity / n | PLANT SCOREBOARD: 18 planted violations, 11 caught / 7 missed (39% miss). The misses concentrate in exactly tw

===== SEAT CDP-A (opus) — 4 findings, 15 clean =====
COVERAGE: "NOT COVERED — state honestly:\n1) The Import Art Sheet path BEYOND the chooser (decode -> palette map -> plan -> commit for a real sheet) is unverified in the app; see finding A4 for why it is unreachable. I verified reachability + the OS-dialog handoff only, same ceiling as the prior session.\n2) Aeon backend: nothing. Every row ran against classic / s1disasm GHZ act 1 only — no other zone, no other act, no act switch mid-edit, no aeon composer (whose 'composer' zoom tier I only read in source
  [MEDIUM/certain/unverified] CDP-A-A1 A canvas→level commit is undoable only from the level tab's Art facet; the reflexive Ctrl+Z eats the drawing instead
      src/renderer/components/canvas/CommitPlanView.tsx:174 — The commit chip promises "one undoable step", but that step lives on the ZONE-ART history — unreachable from the canvas tab (where Ctrl+Z undoes the drawing) and from the level tab's default Layout facet (where the Undo chip renders disabled and Ctrl+Z does nothing).
  [MEDIUM/certain/unverified] CDP-A-A4 The Import Art Sheet path beyond the file chooser is unreachable from any CDP harness
      src/preload/index.ts:59 — window.api is a frozen, non-configurable contextBridge object, so no CDP harness can stub selectFile — which makes decode → palette-mapping → CommitPlanView → commit for a real sheet impossible to verify in the running app, on what the dialog's own docblock calls the dominant art-entry path in the f
  [LOW/certain/unverified] CDP-A-A2 The importer reports a file-read failure as "not an INDEXED PNG"
      src/renderer/state/import-sheet.ts:65 — readAbsolute() sits inside the try whose catch appends "— the importer needs an INDEXED (paletted) PNG", so an fs failure on the chosen file is shown to the artist as a format complaint about a file that may be perfectly valid.
  [LOW/certain/unverified] CDP-A-A3 Commit refusal text is hardcoded plural while its entry list can be singular
      src/renderer/components/canvas/canvas-commit-model.ts:140 — The palette-drift refusals read "…line 0 entry 6, which belong to Sonic's palette" and "Restore those entries" for a single entry — plural verb and plural noun over a singular list.
  CLEAN: Per-tier zoom defaults (fix aebfa67) hold at 2b38e0b: chunk Paint canvas opens 768x768, tile 192x192, block Pa | Zoom is genuinely PER TIER, not just per default: one wheel notch out on the chunk Paint surface took it 768-> | Assign survives a Paint round-trip on BOTH tabs (fix 2b38e0b): chunk Assign ink fraction 0.646 -> 0.646 across | Chunk-tier paint-through is sound with live negative controls: scratchpad/paint-through-harness.mjs 7/7 checks | Tile-tier paint-through reaches the level doc and undoes exactly: a single click on tile $10 moved its FNV has | Block-tier paint-through diverges correctly: a drag on block $D8's 16x16 Paint surface moved its TL cell from  | Locked-tile refusal is real and states itself: tile $33E (one of 135 gap/appended tiles in GHZ 1) shows "lock  | New Canvas -> draw -> plan works end to end: the Ctrl+K dialog creates a 256x256 genesis-level-art doc backed  | The commit panel plans before it applies, and its numbers match reality: meta "1 chunk into GHZ 1", report "ti | The default commit target is the safe one: options[0] is "Append as new chunk" and the select's value is empty | The panel REFUSES rather than half-writing when the pool cannot fit the art: two crossing strokes needing 24 t | Palette drift onto line 0 is caught with the right, specific reason and correctly offers no way out: moving ca | A canvas commit IS one undoable step on the stack it is recorded to: from the level tab's Art facet, a single  | Import Art Sheet is reachable and states its contract: the command is offered for a classic project, the dialo | Nothing these runs did touched tracked disasm files: /home/volence/sonic_hacks/s1disasm git status shows only 

===== SEAT CDP-B (opus) — 3 findings, 22 clean =====
COVERAGE: NOT COVERED — aeon-only rows. Marquee selection, map clipboard (chunk copy/paste), save-as-chunk and the ring-pattern placement tool are AEON features, not classic ones: FACET_TOOLS in src/renderer/workspace/facet-tools.ts gives layout ['stamp-chunk','select','view','marquee','paint-tile','paint-block'] and rings ['place-ring','select','view'], but the s1 manifest declares layout as ['view','stamp-chunk','select'] (src/core/project/s1/index.ts:489) and grants facets ['layout','objects','palette'
  [MEDIUM/certain/unverified] CDP-B-B1 A layout stamp that writes the byte already there still dirties the act and pushes an undo step
      src/renderer/state/classicLevelStore.ts:746 — classicSetLayoutCells has no identical-value short-circuit, so stamping a chunk id onto a cell that already holds it commits a real command: the tab gets the unsaved dot, an undo entry is pushed, and the next act switch pops the destructive "Unsaved changes … discards unsaved edits and undo history"
  [MEDIUM/certain/unverified] CDP-B-B2 The active level tab's live camera is never snapshotted, so a relaunch reopens it at a stale or default viewport
      src/renderer/shell/tab-activation/level.ts:83 — workspaceStore.setView is only ever called for the OUTGOING act during a tab/act switch, so the tab you are actually looking at when the app exits has its camera persisted as of the last time you switched AWAY from it (or not at all, if you never left it) — the relaunch then restores that stale valu
  [LOW/certain/unverified] CDP-B-B3 Collision overlay's surface line is stroked on the cell boundary, so it bleeds one pixel row into the air cell above
      src/renderer/components/classic/classic-overlays.ts:114 — drawCollision strokes the per-column surface line at an integer world y with lineWidth 1/scale, so a shape whose surface sits at y=0 of its 16px cell renders half inside that cell and half inside the neighbouring cell above — painting collision ink over chunk cells whose solidity is 0, and rendering
  CLEAN: Open project + zone/act navigation: __dbg.openDir('/home/volence/sonic_hacks/s1disasm') -> projStatus {status: | Layout stamping round-trip: pick 'Chunk $20' in the picker -> Stamp Chunk tool -> click layout cell (1,1) -> r | Drag-stamp is ONE undo step: press on cell (0,0), 8 interpolated mousemoves to cell (2,0), release -> all thre | Stamping AIR erases: arming the 'Air ($00) — stamp to erase a cell' swatch (after scrolling it into view) and  | Stamping outside the layout grid is a clean no-op: with the below-grid band on screen (camera y=600, zoom 0.5) | BG plane is a separate grid: switching the FG/BG chip to BG and stamping cell (0,0) left the FG cell (0,0) rea | S1 loop flag (bit 7) authoring round-trips: arm the '∞ Loop' chip (title 'Stamp this chunk with S1's loop flag | Object placement: arming '$1FCrabmeat' in the object library and clicking the FG map placed one object at exac | Properties-panel edit commits and is undoable: select-all + type 512 + Enter in the inspector's X field moved  | Entity selection + drag + delete: clicking the ring group $25 at (324,864) selects it and the inspector shows  | Destructive-key guard is real: with the inspector's X number field focused, Backspace and Delete leave the obj | Collision overlay on/off is non-destructive and honours the two-tier gate: toggling 'Collision (path A)' in th | The block-0 short-circuit ordering in drawCollision is moot for this data: a full scan of all 82 GHZ act-1 chu | Collision angles is an independent toggle: 'Collision angles' checks/unchecks without disturbing 'Collision (p | Undo/redo chips route by FOCUSED FACET, not by tab: after a layout stamp the Undo chip is enabled on Layout; s | Per-tab viewport AND facet restore across an in-session tab switch: act 1 set to cam {1024,300,2} + facet Obje | Session restore across a REAL relaunch (SIGKILL of my own instance, fresh process): the stored aurora.session. | Per-tab dirty indicator does not lie: with two act tabs open and act 1 dirty, only 'Green Hill Zone Act 1' car | Dirty act switch is safely gated: clicking the act-2 tab while act 1 is dirty raises role=alertdialog aria-lab | openAct disposes BOTH stacks for BOTH acts: classicLevelStore.ts:331-335 disposeStacksFor drops the layout doc | Classic offers no marquee/clipboard/ring/collision-paint surface at all, and its keys are inert rather than ha | No disk writes to the live disasm from this seat: /home/volence/sonic_hacks/s1disasm still shows only the two 

===== SEAT UX (opus) — 12 findings, 10 clean =====
COVERAGE: WHAT I DID: drove the real pre-built app at 2b38e0bb under xvfb+CDP on PORT=9368 across four scripted sessions (harness discipline reused from scratchpad/canvas-cdp-harness.mjs — one node process owns the app for its whole life; the shell kills anything that outlives its spawning command). Real mouse/key events only; the app was read through the DOM and window.__dbg. Covered: cold boot with no project, opening s1disasm, the Explorer tree and its Canvases group, the New Canvas dialog (valid + two
  [HIGH/certain/CONFIRMED [sev-corr: medium (down from high). Nothing here produces wrong output and nothing is silently lost or corrupted: the commit path i]] UX-A1 New Canvas defaults to a size that can never be committed into a level
      src/renderer/shell/new-canvas.ts:76 — The New Canvas dialog opens at 128×128, but the commit path needs whole 256×256 chunks, so the default canvas from the app's primary art-origination flow has literally no route into the game — and the dialog never says so.
  [HIGH/certain/CONFIRMED [sev-corr: low (down from high). Verified factually accurate but it is a missing enhancement, not a defect: no wrong output, no dat]] UX-A3 The origination canvas has no keyboard tool loop at all — no tool keys, no brush size, no colour keys
      src/renderer/components/canvas/CanvasMode.tsx:83 — Against the owner's bar ('strong enough that an artist does not leave for Aseprite'), the canvas implements exactly two keyboard actions (undo/redo). Every one of Aseprite's core-loop keys is dead, there is no brush size at any size, and no tooltip anywhere hints that a shortcut exists.
  [MEDIUM/certain/unverified] UX-A2 Import Art Sheet is a second-class modal: no Escape, no role, and the backdrop discards a configured plan
      src/renderer/components/canvas/ImportSheetDialog.tsx:40 — The Import Art Sheet dialog — the other half of the newly surfaced art path — has no role=dialog, no aria-label, no focus trap and no Escape handler, and its backdrop dismisses on any click, silently throwing away a loaded sheet and every per-chunk commit target the user set.
  [MEDIUM/certain/unverified] UX-A4 Explorer group counts include the action rows, so 'CANVASES 3' means one canvas
      src/renderer/shell/Explorer.tsx:252 — The count badge on every Explorer group is items.length, and the Canvases and Object Library groups inject 'New Canvas…' / 'Import Art Sheet…' / 'New Sprite…' as items — so a collapsed group's only piece of information is inflated by 1-2 and an empty Canvases group reads '2'.
  [MEDIUM/certain/unverified] UX-A5 Zoom controls run in opposite directions on the level view and the pixel canvases
      src/renderer/components/art-shared/ToolColumnParts.tsx:146 — The shared ZoomControl draws + · value · −, while the level status bar draws − · value · +, so the same spatial gesture zooms opposite ways depending on which surface you are on — and the canvas one is backwards relative to the near-universal convention.
  [MEDIUM/certain/unverified] UX-A6 Three different words for 'stop this edit being shared', and the instruction points at a control that needs an unmentioned mode switch
      src/renderer/components/classic/ChunkTab.tsx:491 — The one concept a S1 art editor must teach — how to change art in one place when it is shared — is spelled 'Duplicate', 'Duplicate chunk', 'Diverge' and 'Isolate' on the same screen, and the banner that tells you what to do omits the mode switch the control lives behind.
  [MEDIUM/certain/unverified] UX-A7 The Blocks panel header says 'paint block $X' in both modes, colliding with the Paint mode chip beside it
      src/renderer/components/classic/ChunkTab.tsx:539 — On the Art facet's Chunk tier the block picker's header reads 'Blocks (439) · paint block $1' regardless of mode, 30px below a mode row whose other chip is literally called 'Paint' — so the header reads as a mode indicator and contradicts the actual mode.
  [MEDIUM/certain/unverified] UX-A8 Solidity tooltips print the internal enum name instead of explaining the collision type
      src/renderer/components/classic/composer-shared.tsx:22 — The four chips a first-time ROM hacker uses to set collision are labelled None / Top / LRB / All, and their tooltips are 'NotSolid' / 'TopSolid' / 'LRBSolid' / 'AllSolid' — the SonLVL C# identifiers, which restate the abbreviation and explain nothing.
  [LOW/certain/unverified] UX-A9 The tier strip renders two controls that do the same thing, one of them styled as static text
      src/renderer/components/classic/ClassicComposerDock.tsx:44 — The Art facet's tier row shows 'Chunk | Block | Tile' as pills and, 20px to their right, 'Chunk $1 › Block $1 › Tile $1' — both are buttons calling the identical setTab, but the second is styled as a 10px monospace grey breadcrumb so it does not read as a control.
  [LOW/certain/unverified] UX-A10 Explorer group headers cannot be opened from the keyboard
      src/renderer/components/ui/CollapsibleSection.tsx:61 — Every Explorer group is a <div onClick> with no role, tabindex or key handler, and every group renders defaultCollapsed — so a keyboard-only user cannot expand Canvases to reach New Canvas… except by discovering that typing in the Filter box force-expands everything.
  [LOW/probable/unverified] UX-A11 The level-art pixel tiers have no Fit control and open a tile filling ~5% of the pane
      src/renderer/components/canvas/CanvasMode.tsx:232 — 'Fit' exists only on the canvas and sprite surfaces; the classic Chunk/Block/Tile pixel editors — the surfaces a ROM hacker actually spends the day in — have only +/− stepping, and the Tile tier opens an 8×8 tile at 24× (192px) inside an ~890×680 empty pane.
  [LOW/certain/unverified] UX-A12 The canvas status bar reports a level tile budget for a canvas that cannot reach that level
      src/renderer/shell/ArtStatusBar.tsx:1 — A 128×128 canvas's footer reads 'tiles 1 unique · 17 free in GHZ 1 · pool 948/965' while the Commit panel two inches away says there is nothing to commit — and the readout silently follows whichever act happens to be loaded, although a canvas belongs to no level.
  CLEAN: Tier vocabulary (tile/block/chunk) is genuinely consistent everywhere it surfaces, and the canvas actively tea | New Canvas dialog error states are exemplary and were exercised live: typing '9 bad name!' produced the inline | NewCanvasDialog is a properly built modal: role="dialog" + aria-label='New Canvas' (read off the live DOM), a  | The chunk $00 empty state names the next action instead of just refusing: 'The blank chunk (engine id $00 = ai | Un-openable canvas files are listed as disabled rows with a reason rather than silently dropped (explorer-data | The commit flow's defaults are the safe ones and it names its destination: defaultTargets returns 'Append as n | Canvas navigation gestures work and match convention: Space-drag and middle-drag pan (use-hand-pan.ts:24-35, a | Both new art entry points are reachable two ways, not one: the Explorer's Canvases group (explorer-data.ts:68/ | Facet and mode selection are visually unambiguous where it matters: the active tier chip and the active mode c | The classic art tool rail exposes every tool through a title/aria-label that says what it does, not what it is

===== SEAT PERF (opus) — 10 findings, 10 clean =====
COVERAGE: MEASUREMENT: zero runtime measurement — I honoured the "do not run dev servers" pin, so every number here is static call-count / byte arithmetic (buffer dims read from source, layout dims read from s1disasm/levels/ghz1.bin, canvas sizes from the width/height expressions). I did NOT profile a real frame, so the per-stroke() and per-canvas-creation microsecond costs in A1/A5 are estimates; the CALL COUNTS and BYTE FIGURES are exact. In particular I did not measure real GHZ chunk solid-cell density
  [HIGH/certain/REFUTED [sev-corr: low (medium at most). No wrong output, no data loss — the overlay renders correctly; this is a per-frame cost behind a d]] PERF-A1 Collision overlay issues one beginPath+stroke per 1-px column — ~92k–440k stroke() calls per frame on GHZ1
      src/renderer/components/classic/classic-overlays.ts:107 — drawCollision draws each 16-px cell's surface line as 16 separate beginPath/moveTo/lineTo/stroke calls, so turning on the collision overlay makes the classic level viewport issue hundreds of thousands of individual canvas stroke ops per repaint; the identical pattern is in aeon's OverlayRenderer.
  [HIGH/certain/REFUTED [sev-corr: Downgrade high -> low (nit/hardening). No user work is lost or corrupted and no main-path feature produces wrong output:]] PERF-A2 cappedZoom bounds the canvas EDGE, not its area — 5 wheel notches on the Chunk tab allocates a ~1 GB backing store
      src/renderer/components/art-shared/zoom-cap.ts:33 — The per-tier zoom change moved the chunk tier's DEFAULT off 24 but added no area bound, so wheel-zooming the Chunk tab still reaches the 151 MB backing store the fix was written to eliminate after 3 notches, and ~1.008 GB at the ceiling.
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> medium. Nothing here produces wrong output or loses user work: pans, object drags and the composite al]] PERF-A3 MapViewport repaints per mousemove with no rAF coalescing, re-allocating the canvas backing store and forcing layout each time
      src/renderer/components/MapViewport.tsx:430 — Aeon's level viewport subscribes to vpX/vpY/zoom as zustand selectors and does a full recompose in a dep-driven effect, so a pan drag issues one complete redraw per mousemove (hundreds/sec on a high-poll mouse) — each of which calls getBoundingClientRect and reassigns canvas.width/height — the exact
  [HIGH/certain/CONFIRMED [sev-corr: medium — output is always correct (the coarse clock over-invalidates, never under-invalidates); nothing is lost, corrupt]] PERF-A4 BlockTab/ChunkTab thumbnail strips still key on the coarse chunkEpoch — one block-cell click repaints all ~1400 thumbnails
      src/renderer/components/classic/BlockTab.tsx:337 — The per-entity thumbnail invalidation that TileTab was fixed to use never landed on BlockTab or ChunkTab: both pass `String(chunkEpoch)` as the versionKey for their whole strips, so a single block edit repaints all 439 BlockThumbs AND all ~965 TileThumbs — including the tile strip, which by the stor
  [MEDIUM/certain/unverified] PERF-A5 drawBufferScaled creates and discards a whole <canvas> element on every thumbnail paint
      src/renderer/components/classic/composer-shared.tsx:230 — Every composer thumbnail repaint allocates a fresh DOM canvas element plus a 2D context as its upscale source, so opening the Tile tab creates ~965 throwaway canvases and one BlockTab click creates ~1400 — even though the codebase already uses a shared scratch canvas for exactly this job elsewhere.
  [MEDIUM/certain/unverified] PERF-A6 PixelViewport recomposes the entire buffer to RGBA on every render, including every pointermove of a stroke
      src/renderer/components/art-shared/PixelViewport.tsx:112 — The draw effect has no dependency array and allocates a fresh OffscreenCanvas + ImageData and walks every pixel each time it runs, and `rerender()` fires it on every pointermove — so dragging in ChunkTab paint mode does a 65,536-iteration recompose plus a 256 KB ImageData allocation per mouse event,
  [MEDIUM/certain/unverified] PERF-A7 findPoolMatch re-canonicalises the whole 965-tile pool for every wanted tile — O(unique x pool) with two allocations per inner step
      src/core/art/tile-pool-match.ts:126 — The flip-aware pool match rebuilds every pool tile's canonical form from scratch on each call, and the canvas commit calls it once per unique resolved tile, so a commit is quadratic in (unique tiles x pool size) and allocates a 64-byte array plus a 64-character string per inner iteration.
  [MEDIUM/probable/unverified] PERF-A8 The sprite editor is the one PixelViewport host that does not apply cappedZoom
      src/renderer/components/sprite/SpriteCanvasHost.tsx:73 — SpriteCanvasHost hands PixelViewport the raw store zoom, so a large sprite frame at high zoom can produce a canvas past the platform edge limit — which renders blank with no error, the exact failure zoom-cap.ts was written to prevent — and an unbounded backing store on the way there.
  [MEDIUM/certain/unverified] PERF-A9 chunkEpoch is bumped by every tile, block and palette edit, wiping the whole level-viewport chunk-canvas cache
      src/renderer/state/classicLevelStore.ts:653 — Any art edit that is not a single-chunk edit bumps the coarse chunkEpoch, which is part of the classic viewport's cache key, so one composer pencil stroke forces every visible chunk canvas to be re-composed from scratch even though the reverse indices needed to narrow it already exist.
  [LOW/certain/unverified] PERF-A10 objectIdSig walks every object and builds a sorted joined string on every render, including every rAF redraw
      src/renderer/components/classic/ClassicLevelViewport.tsx:347 — The object-sprite refresh signature is computed inline in the component body rather than memoised, so it re-walks the act's object list, allocates a key string per object, builds a Set, sorts and joins on every render — and this component re-renders once per painted frame throughout a pan or object 
  CLEAN: zustand selector discipline is clean across the whole renderer: `grep -rEn 'use[A-Za-z]*Store\(\)' src/rendere | TileTab's claim that the tile strip repaints only the edited tile is TRUE end to end, and I traced every link: | ClassicLevelViewport's camera and repaint discipline is the strongest thing in the tree and I could not break  | buildUsageIndex is linear, not quadratic: one pass over blocks (4 cells each) plus chunks (256 cells each) plu | No synchronous IPC anywhere: `grep -rn 'sendSync' src` returns nothing, and every main-process handler is `ipc | canvas-resolve is properly interned and linear in cells, not quadratic: internTile keys a `Map<string, number> | cachedConstraints is a genuine cache with a stated failure mode I checked: single entry keyed on PixelBuffer i | SectionRenderer is well optimised for aeon's per-act work (independently of MapViewport's driver problem in A3 | ChunkGrid's chunk-picker thumbnails do the right things: React.memo on ChunkCell with a per-chunk `port.versio | The classic object-sprite cache has real eviction rather than unbounded growth: EpochCache keyed on `objectArt

===== SEAT STATE (opus) — 8 findings, 13 clean =====
COVERAGE: WHAT I DID NOT GET TO. (1) src/renderer/state/classic-save.ts (262 lines) and canvas-save.ts/canvas-file.ts (465 lines) — I read only their call contracts from project-runtime.ts; the guarded-write mtime/conflict logic and the domain-clearing in classic-save are UNREVIEWED, and A1's shape (a flag cleared more broadly than the write) is worth checking there too. (2) classicLevelStore lines 745-1282 (the eight classic commands) I read for commit/dirty routing only — I did not verify the per-comman
  [CRITICAL/certain/CONFIRMED [sev-corr: Downgrade critical → high. The mechanism is real and the fix is warranted, but it is LATENT at this SHA: /home/volence/s]] STATE-A1 Aeon Ctrl+S marks the WHOLE project clean while writing only the current act's sections
      src/renderer/state/aeon-save.ts:33 — `editorStore.dirty` is a single project-wide flag set by every aeon command in every act, but `saveAeonProject` serializes only the CURRENT act's sections and then calls `markClean()` — so edits made in any other act are never written and the app reports itself fully saved.
  [CRITICAL/certain/CONFIRMED] STATE-A2 artStore.open (the aeon composer document) is in no project-lifecycle teardown and no dirty guard — unsaved art is destroyed silently on project open
      src/renderer/shell/project-open-guard.ts:55 — A new/unsaved composer document with painted pixels is dirty only on `artStore.open.dirty`, which `currentOpenDirtySnapshot()` does not read and `endDocumentSession()`/`resetProjectRuntime()` do not tear down — so opening another project neither asks nor warns, and the aeon open path then overwrites
  [HIGH/certain/CONFIRMED] STATE-A3 The transform grid is drawn on classic's Chunk/Block Paint tiers, which have no pendingAction consumer — the stale action fires on the next TileTab mount
      src/renderer/workspace/facets/s1-facets.tsx:438 — `ClassicArtOptions` renders the shared option bar (with the transform grid enabled) for ALL THREE classic pixel tiers, but only TileTab consumes `artStore.pendingAction` — so a flip/rotate clicked on the Chunk or Block Paint surface does nothing, sits in the cross-engine slot, and then applies itsel
  [MEDIUM/certain/unverified] STATE-A4 openAeonProject writes its fitted composer zoom into whatever tier happens to be active — usually the tile tier
      src/renderer/state/aeon-open.ts:85 — `setZoom(fitComposerZoom(...))` is called before any composer has mounted, so `artTier` is still its default `'tile'` (or a classic tier left over from the previous project); the chunk-fit zoom is written to that tier instead of `'composer'`, discarding the fit and leaving the tile editor at a chunk
  [MEDIUM/certain/unverified] STATE-A5 editorStore.selection / activeSectionIndex / marquee are never cleared on an aeon act switch — the inspector binds to a phantom object in the new act
      src/renderer/state/editorStore.ts:88 — `selection` carries `{sectionIndex, index}` and `activeSectionIndex` is a bare number, both resolved against whatever act is CURRENT; an aeon act switch changes only projectStore's zone/act, so after switching, the object inspector shows — and edits — the object sitting at those coordinates in the n
  [LOW/certain/unverified] STATE-A6 toolStore is a dead duplicate of four artStore fields
      src/renderer/state/toolStore.ts:14 — `useToolStore` holds mirror / ditherPattern / ditherSecondary / pixelPerfect — the same four fields artStore already owns and every surface actually reads — and has zero importers anywhere in src.
  [LOW/probable/unverified] STATE-A7 The persisted per-tab workspace record is never pruned, so it grows without bound in localStorage
      src/renderer/shell/session-lifecycle.ts:173 — Tabs are pruned on restore via `restoredTabIsValid`, but the workspace record (facet + viewport, keyed by tab id) is seeded wholesale and re-serialized every change, so entries for closed/deleted/renamed tabs accumulate forever under each project key.
  [LOW/certain/unverified] STATE-A8 useHistoryVersion re-subscribes to the history hub on every render
      src/renderer/hooks/useHistoryVersion.ts:60 — The `subscribe` argument to useSyncExternalStore is an inline arrow, so its identity changes every render and React tears down and re-registers the hub listener on each one.
  CLEAN: spriteStore multi-document model (spriteStore.ts:244-373): the park/unpark is written field-by-field on purpos | canvasStore per-document isolation (canvasStore.ts:363-370 patch, 522-530 closeCanvasDoc, 453-465 loadCanvasDo | canvasStore.openCanvasDoc clears the hub stack for a reused id (canvasStore.ts:438) — this is a real guard wit | DocumentHistoryHub prefix dispatch and disposal (core/editing/document-history.ts:29-63): longest-prefix-wins  | classicLevelStore act-load safety: openAct's `loadToken` (classicLevelStore.ts:345,402,443) drops a superseded | classicLevelStore's two-document commit split: assertSingleDomain (line 582-596) genuinely throws on a cross-d | The late-mount listener class is closed for the two shared hooks: use-attached-effect.ts:44-55 runs unconditio | ResizeObserver hosts are not members of the late-mount class: composer-shared.tsx:194-206 takes the ELEMENT (c | Subscription hygiene: all five imperative store subscriptions (session-lifecycle.ts:135,136,190,197 and Classi | classicObjectArtStore's cache lifecycle (classicObjectArtStore.ts:236-311): dir change wipes, refreshGen drops | canvas-constraints-cache.ts: single-entry cache keyed by buffer IDENTITY (sound because canvasStore.setPixels  | Session persistence validation (core/shell/session-persistence.ts:16-87): tabs are zod-strict-parsed with Home | openEngine()/openProjectDir() (open-project.ts:21-44) really are the single answer — classic wins the tie cons

===== SEAT DIR (fable) — 9 findings, 8 clean =====
COVERAGE: Did not run the test suite, tsc, or the app — all verification is static reads of the pinned tree; the spec's 'CDP-verified 7/7 and 4/4' claims were accepted on the strength of the checked-in harness reports (2026-08-15-*-cdp-report.md) without re-driving CDP. Did not line-verify canvas-resolve.ts/tile-canon.ts math (mirrorBlock, canonical orientation XOR) beyond confirming their test suites exist and the composition comments match tile-pool-match's; a correctness seat should own that. Did not a
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> medium. Nothing here is executable: docs/ROADMAP.md produces no wrong runtime output, breaks no main-p]] DIR-A1 ROADMAP.md (plan of record) is a month behind actual steering and its sequencing points at the wrong engine
      docs/ROADMAP.md:342 — The P0-P8 plan of record records nothing after 2026-08-09 (§2.5) and still sequences aeon-first, while every phase actually executed since (UX overhaul stages 1-4 plans 1-6, art authoring 1/2A/2B/2C) was classic-first and off-roadmap.
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> medium. The finding is anchored to a documentation line and the harm is a missing advisory, not wrong ]] DIR-A2 2C spec header says BUILT, but D2b's cross-act reach reporting is not built
      docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:3 — The spec's D2/D2b requirements — palette-adoption dialog names the actual files from paletteSources (SBZ per-act palettes), report names SBZ3-borrows-LZ-art and ending-reuses-GHZ reach, LZ/SBZ3 underwater-palette warning — are all unimplemented, yet the status header declares the phase BUILT with no
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> low. The panel rubric reserves "high" for wrong output or a broken main-path feature. Nothing here pro]] DIR-A3 The whole art-authoring line shipped with zero MCP parity, against the standing cross-cutting rule
      src/renderer/agent/agent-handler.ts:1 — Paint-through, canvas documents, canvas commit and PNG import (phases 1-2C) have no MCP tools, violating ROADMAP.md:332-334's rule that every new authoring capability ships its MCP tools in the same phase so agent and human editing stay equivalent.
  [MEDIUM/certain/unverified] DIR-A4 2C's shipped tests skip three named spec §7 disciplines
      src/core/art/__tests__/classic-commit-plan.test.ts:221 — Spec §7 requires reclaim guards planted against real s1disasm GHZ data, an id-space off-by-one plant (engine id vs file index freeing a neighbour's art), and pipeline step 9's zeroing of stale reclaimed block ids with a report count; none of the three exists.
  [MEDIUM/certain/unverified] DIR-A5 ART_SUITE.md, the user-facing guide, documents a UI stage 4 deleted
      docs/ART_SUITE.md:72 — The guide instructs 'Click the Art button in the Toolbar' and homes classic art editing in the ClassicComposerDock/ZoneActTree world, but Toolbar.tsx, LegacyWorkspace, ClassicProjectView and ZoneActTree were deleted in plan 5 (merged 2f1db2b) and paint-through/facets replaced the described flows; it
  [MEDIUM/certain/unverified] DIR-A6 The art spec's three §7 open decisions are already decided by shipped code — ratify them so they stop being re-litigated
      docs/superpowers/specs/2026-08-15-in-app-art-authoring-design.md:339 — All three 'open' decisions were settled de facto during the build; the spec still lists them open, inviting a cold session to redesign shipped behaviour.
  [MEDIUM/certain/unverified] DIR-A7 The three dead aeon export modules flagged for retirement on 2026-07-03 are still in the tree
      src/core/export/vram-coloring.ts:1 — vram-coloring.ts, act-descriptor.ts and entity-data.ts encode the retired per-section engine model (PLAN_AUDIT §1 called them dead-on-arrival) and remain in src/core/export six weeks after the roadmap ordered retire-don't-extend — a standing trap for any session that greps for 'export'.
  [LOW/certain/unverified] DIR-A8 Phase-1 spec status not annotated for the 'Make unique' descope
      docs/superpowers/specs/2026-08-15-in-app-art-authoring-design.md:214 — §3.6 promises the linkage banner ships a 'Make unique' action and the header claims phase 1 fully built, but the action was descoped (recorded only in the plan, A8) and a test now asserts its absence.
  [LOW/certain/unverified] DIR-A9 26 commits on master are unpushed, including all of 2C
      docs/superpowers/specs/2026-08-15-phase-2c-resolve-and-commit-design.md:3 — The entire 2C line (import path, commit panel, Assign-black fix) exists only on this machine; git log origin/master..master = 26 commits.
  CLEAN: SPOT-CHECK 1 HOLDS — 2C core pipeline exists and matches the spec's hard rules: floors defended at BOTH doors  | SPOT-CHECK 2 HOLDS — D4 three-state availability shipped exactly as specced (tile-pool-match.ts:34-48: matched | SPOT-CHECK 3 HOLDS — D3 collision inheritance: displaced cell's flips kept and block pre-mirrored (classic-com | SPOT-CHECK 4 HOLDS — phase 1 §3.6 seam preview: classic TileTab wires PixelViewport's repeat prop behind the s | SPOT-CHECK 5 HOLDS — D6/PNG path: import bypasses CanvasDoc and its 1024px cap (png-import.ts header states an | 2B budget readout is as claimed: unique-count/free-slots/pool with the deliberate refusal to compare (use-canv | Remap-to-act-colours is line-preserving as the spec ordering note requires (classic-commit-plan.ts:144-167 sea | Stage-4 plan-5 deletions verified real: Toolbar.tsx / LegacyWorkspace / ClassicProjectView / ZoneActTree absen

===== SEAT ARCH (opus) — 11 findings, 11 clean =====
COVERAGE: WHAT I DID NOT GET TO — read all of this as a real gap, not hedging.

(1) DYNAMIC-IMPORT EDGES ARE ABSENT FROM MY GRAPH. My cycle detection and orphan detection resolve static relative specifiers only. I enumerated dynamic imports by grep and found few (renderer/index.tsx:19 → debug-hooks, ProjectSetupTab.tsx:174 → classic-save, ipc-handlers.ts:82-83 → node builtins, 4 in core tests) and hand-accounted for each, BUT tab-activation/sprite.ts:104-110 claims export-sprite is pulled in by dynamic im
  [MEDIUM/certain/unverified] ARCH-A1 core/formats/sprite-mappings.ts is dead code AND the one core module that constructs a DOM ImageData
      src/core/formats/sprite-mappings.ts:127 — renderSpriteFrame (src/core/formats/sprite-mappings.ts:106) has zero callers anywhere in src/, yet it is the sole place src/core constructs a browser `ImageData` (line 127) and returns it in its public signature (line 111) — a renderer-type leak sitting in the layer that is supposed to be renderer-f
  [MEDIUM/certain/unverified] ARCH-A2 The core-layering invariant has zero automated enforcement
      package.json:6 — "src/core imports nothing from src/renderer / react / zustand / electron" is the load-bearing invariant of this codebase's structure, and nothing enforces it: there is no ESLint config, no lint script, and no test that scans core's imports. The only import-layering guard in the repo covers a single 
  [MEDIUM/certain/unverified] ARCH-A3 ProjectAdapter's "engine-agnostic seam" carries both engines' concrete document types, and writes are not on the seam at all
      src/core/project/adapter.ts:192 — core/project/adapter.ts is documented as "the engine-agnostic seam" (line 1) but imports both engines' concrete models (LoadedS4Config, S4Project, CollisionProfileSet at :16-18; LevelDoc at :213), names engines in its capability enum (`levels: 'chunk-hierarchy' | 'aeon' | null`, :135), and expresses
  [LOW/certain/unverified] ARCH-A4 aeon bypasses the adapter registry's open(): openProject() can never open an aeon project
      src/renderer/state/classic-bridge.ts:68 — Both adapters are registered (classic-bridge.ts:33), but the bridge's dispatch explicitly excludes aeon from `openProject()` — `if (match && match.type !== 'aeon')` at :68 — and routes an aeon match back out as `{ kind: 'not-classic', aeon: true }` (:72) so a renderer module calls `aeonAdapter.open(
  [LOW/probable/unverified] ARCH-A5 dirty-snapshot bypasses openEngine(), the module that exists to be the single answer to "which engine is open"
      src/renderer/shell/dirty-snapshot.ts:28 — src/renderer/state/open-project.ts was created because "[f]our call sites used to derive this independently and TWO OF THEM DISAGREED" (open-project.ts:3-4), yet dirty-snapshot.ts:28 re-derives aeon-openness as `useProjectStore.getState().project !== null` instead of calling `openEngine()`. Since a 
  [LOW/certain/unverified] ARCH-A6 ~640 LOC of unreachable modules in core and renderer, including a live-looking zustand store
      src/renderer/state/toolStore.ts:14 — Eight non-test modules have no importer anywhere in src/: src/core/formats/object-defs.ts (88), object-names.ts (134), s4-objects.ts (75), s4-rings.ts (30), src/core/import/color-quantize.ts (87), src/core/formats/sprite-mappings.ts (175, see A1), src/renderer/components/SectionList.tsx (77), src/re
  [LOW/certain/unverified] ARCH-A7 components/ taxonomy is historical, not predictable: classic has a directory, aeon does not
      src/renderer/components/MapViewport.tsx:19 — 21 classic components live under src/renderer/components/classic/, but the equivalent aeon components sit loose at src/renderer/components/ root with no components/aeon/ — and seven of them are named so that nothing identifies them as aeon-only. Placement is therefore not derivable from the file's r
  [LOW/certain/unverified] ARCH-A8 Three unrelated meanings of "canvas" as sibling paths, one of which is a global theme module
      src/renderer/canvas/canvas-colors.ts:1 — src/renderer/canvas/ (aeon level-map renderers), src/renderer/components/canvas/ (the origination-canvas art document UI) and src/core/art/canvas-*.ts (the origination-canvas model) are three different subjects sharing one word. The collision is not merely cosmetic: renderer/canvas/canvas-colors.ts 
  [LOW/certain/unverified] ARCH-A9 Three import cycles, all providers↔components or store↔store; benign today only because every use site is deferred
      src/renderer/providers/chunk-grid-aeon.ts:152 — The static import graph has exactly three cycles: (1) components/AeonChunkActions.tsx → providers/chunk-library-import.ts → providers/chunk-grid-aeon.ts → back; (2) components/classic/ChunkPickerHeader.tsx ↔ providers/chunk-grid-classic.ts; (3) state/classicLevelStore.ts ↔ state/classicProjectStore.
  [LOW/certain/unverified] ARCH-A10 src/shared depends on src/core, inverting the layer it is named for
      src/shared/agent-protocol.ts:6 — src/shared is the module preload and main both consume, i.e. the bottom of the stack, but shared/agent-protocol.ts imports two core types — making core a build dependency of the preload bundle.
  [LOW/certain/unverified] ARCH-A11 Two parallel 4bpp codec pairs for the same on-ROM format
      src/core/formats/tiles.ts:19 — The Genesis 4bpp tile encoding has two independent decoder/encoder pairs in core — parseTiles/serializeTiles (Tile{pixels} shape, the aeon path) and unpack4bppTile/packTilePixels (PixelBuffer shape, the classic art-edit path) — plus a third decode inlined in the dead renderSpriteFrame. Both live pai
  CLEAN: src/core imports NOTHING from react, react-dom, zustand, electron, or src/renderer — exact count: 0 violations | Cycle count for the whole of src/ is 3, and they are the same 3 with and without type-only edges (listed in A9 | There is ONE palette colour model, not two. Every RGB↔CRAM conversion in the app funnels through core/formats/ | components/shared/ purity is a real guard, not a decorative one. shared-purity.test.ts:53 matches the SPECIFIE | The engine-openness facade is well factored and honest about its own history. src/renderer/state/open-project. | Engine branching in the renderer is modest and concentrated, not smeared: 32 total occurrences of engine discr | The port/provider pattern is applied consistently for the surfaces it covers. providers/ holds matched per-eng | The sprite editor's aeon-store coupling does NOT produce a broken classic path, despite four components readin | No geometry-helper duplication of the kind the brief suspected: there is no canvasGeom module in the tree at a | Path aliases are inert: `@core` and `@shared` are declared in vitest.config.ts:6-7 but grep finds ZERO import  | src/main is a genuinely thin layer with one core dependency, and it is the right one: src/main/guarded-write.t

===== SEAT SEAM (opus) — 9 findings, 13 clean =====
COVERAGE: "WHAT I DID NOT GET TO — the mirror sweep was NOT exhaustive. I audited the VRAM/BG/engine-struct/layout-geometry/file-path classes and stopped there. Completely un-audited mirror candidates, any of which could hold the same class of defect: src/core/compress/* and src/core/formats/kosinski.ts + formats/classic/enigma.ts (compression format magic, header shapes, the 0x7ff inline-bits constant at enigma.ts:141); src/core/formats/s4-strips.ts, s4-collattr.ts, s4-rings.ts, s4-nametable.ts, section-
  [HIGH/certain/CONFIRMED [sev-corr: Downgrade high -> low. "High" requires wrong output or a broken main-path feature; neither holds. The emitted act_descri]] SEAM-A1 Aurora's aeon act-descriptor exporter emits a struct layout diverged from engine/structs.emp (34B Act vs 40B, 72B Sec vs 66B) and resurrects a directory the engine deleted
      src/core/export/act-descriptor.ts:25 — generateActDescriptorAsm emits the pre-2026-06 Act/Sec layouts — fields the aeon engine deleted, missing every field it added, and the wrong total sizes — and saveAeonProject writes them into games/sonic4/data/editor/ojz/act1/export/, the exact directory aeon deleted on 2026-08-01 and which sits ins
  [HIGH/certain/REFUTED [sev-corr: If retained at all, low (stale advisory constant / dead export artifact), not high. High requires wrong output or a brok]] SEAM-A2 FG_TILE_LIMIT = 1024 mirrors an aeon constant that is 896, and check_budget reports fits/limit from it
      src/core/export/vram-coloring.ts:5 — Aurora's FG tile-pool ceiling is hardcoded at 1024 with a comment justifying it as 'BG region starts at tile slot 1024'; the aeon engine's declared VRAM contract gives fg_art_pool only 896 tiles (slots 896-1023 belong to dust/character/ring/test windows), so the check_budget MCP tool advertises 128 
  [HIGH/probable/CONFIRMED] SEAM-A4 The /mcp endpoint has no Host/Origin validation while /aether does, and the installed MCP SDK provides none by default
      src/main/mcp-server.ts:65 — protocol.md D8 makes the loopback Origin/Host check Aurora's responsibility; it is applied to /aether and /aether/events but not to /mcp, which serves the same 33 editing tools (including open_project and save_project) with no header validation from Aurora or from the SDK.
  [MEDIUM/certain/unverified] SEAM-A3 BG_MAX_TILES = 512 is the last unreconciled copy of a constant aeon fixed to 448 in 2026-06
      src/renderer/agent/agent-handler.ts:44 — The set_bg tool accepts up to 512 BG tiles; aeon's BG region is 448 tiles ($8000-$B7FF, bounded by the relocated sprite attribute table), and aeon explicitly finished reconciling every other copy of that number away from 512 on 2026-06-23.
  [MEDIUM/certain/unverified] SEAM-A5 The Aether initialize result omits two schema-REQUIRED keys, and no reply carries the required envelope fields
      src/main/aether/adapter.ts:31 — capabilities() returns none of timingBasis, limits, frame, mclk, running or droppedEvents, all of which empyrean/contract/schema/bus-protocol.schema.json marks required on the initialize result, and no success/error reply carries the machine stamp or dropped-event counter that §2.2/§2.3 make envelop
  [MEDIUM/certain/unverified] SEAM-A6 The SSE channel pushes editor/ready before any initialize/initialized, and broadcast() — the only stamped-event path — is dead code
      src/main/mcp-server.ts:107 — protocol.md §2.1 and §8 item 3 require a server to reject/withhold events before the client's `initialized` notification; Aurora's /aether/events handler has no session state and writes an editor/ready notification the instant a socket connects, with no machine stamp in its params.
  [MEDIUM/certain/unverified] SEAM-A7 get_bg hardcodes height 32 while list_bgs derives it, so the documented get_bg→set_bg round-trip breaks on any non-32-row background
      src/renderer/agent/agent-handler.ts:443 — get_bg reports `height: BG_TILES_HIGH` (a constant 32) while returning the whole bgLayout array, list_bgs computes height as bgLayout.length/64, and set_bg accepts exactly 2048 words — three different answers to the same question, and the engine's Plane B is 64x64 with all 64 rows live.
  [LOW/certain/unverified] SEAM-A8 The MCP discovery file restates protocolVersion as a literal instead of importing PROTOCOL_VERSION
      src/main/mcp-server.ts:140 — ~/.aurora/mcp.json (and the legacy ~/.sonic-level-editor/mcp.json) writes `protocolVersion: 1` as a hand-typed literal while the adapter answers initialize from src/main/aether/protocol.ts's PROTOCOL_VERSION, so a version bump makes the two disagree silently.
  [LOW/certain/unverified] SEAM-A9 Oracle integration is an approved but wholly unimplemented contract — no emulator client exists in Aurora
      docs/specs/2026-07-03-aether-client-playtest-design.md:33 — Aurora's approved Aether-client playtest design specifies an emulator/* client (method calls, event subscription, symbol resolution, a build→load_symbols→reload_rom loop); there is no implementation anywhere in src, so this is a dormant contract with nothing to drift against yet.
  CLEAN: Tool registry ↔ IPC union ↔ handler switch are in exact three-way agreement: scripted cross-check of src/main/ | Single-source MCP/Aether surface holds: src/main/mcp-server.ts:33 and src/main/aether/adapter.ts:24-25,37 both | The bundled S1 profile's file paths are accurate and existence-guarded: I extracted all 111 distinct paths fro | The S1 profile matches the SonLVL rev01 INI field-for-field where I checked: base palette composition Sonic.bi | S1 word bit layouts are verbatim-correct against SonLVLAPI: src/core/level-classic/model.ts:37-44 (TILE_MASK 0 | src/core/formats/classic/s1-layout.ts's header claims are true of the real data, which I re-measured across al | The bit-7 loop-flag interpretation (s1-layout.ts:5, usage-index.ts:59, model.ts:200-208) is confirmed by the d | MAX_LAYOUT_W=64 / MAX_LAYOUT_H=8 (model.ts:210-211, validated at :238-239) are correct and non-binding: the wi | The s1 object-art profile is existence-guarded against the real disasm: src/core/project/profiles/__tests__/s1 | The `emulator/romReloaded` event-name mirror is reconciled: empyrean/contract/protocol.md §10 decision 4 recor | The seat brief's premise that 'the aeon adapter is a marker with a deferred loader' is STALE — it is a real ad | The /aether loopback guard does assert something: src/main/mcp-server.ts:83-91 rejects a Host of `evil.example | Aether error-code constants match protocol.md §5 exactly: src/main/aether/protocol.ts:34-42 (PARSE -32700, INV

===== SEAT MAIN (opus) — 9 findings, 13 clean =====
COVERAGE: "READ IN FULL (whole seat scope): src/main/index.ts, ipc-handlers.ts, file-io.ts, guarded-write.ts, recent-projects.ts, agent-bridge.ts, mcp-server.ts, editor-methods.ts, aether/adapter.ts, aether/protocol.ts, src/preload/index.ts, src/shared/ipc-types.ts, src/shared/rel-path.ts, electron.vite.config, package.json, src/renderer/index.html. Every IPC channel in IPC_CHANNELS was traced handler-by-handler.\n\nNOT DONE / GAPS:\n(1) My shell broke roughly two-thirds through (grep/find/echo began retu
  [HIGH/probable/CONFIRMED [sev-corr: Stated severity "high" is correct — hold it. Not critical: the silent on-disk mutation requires an active attacker plus ]] MAIN-A1 /mcp has no Host/Origin guard while /aether does — the stated D8 protection is bypassable via a sibling route with identical capability
      src/main/mcp-server.ts:65 — The express MCP route POST /mcp is registered with no middleware, while POST /aether and GET /aether/events are wrapped in `loopbackOnly`; both surfaces dispatch the exact same EDITOR_METHODS registry, so the Host/Origin check is a no-op defense.
  [MEDIUM/certain/unverified] MAIN-A2 file:write-binary is the only write channel with no rel-path guard, and one caller interpolates an unsanitized free-text name into the path
      src/main/ipc-handlers.ts:81 — WRITE_BINARY_FILE resolves the renderer-supplied relativePath against basePath with no isRelPathSafe check and mkdirs parents recursively, so an absolute or `..`-bearing path writes anywhere the user can write — and the sprite export path builds that string from an unsanitized UI text field.
  [MEDIUM/certain/unverified] MAIN-A3 The editor bus has no authentication at all; the discovery file publishes the port to every local process
      src/main/mcp-server.ts:132 — Neither /mcp nor /aether requires any token, and Aurora advertises its port in a world-readable file in the user's home directory, so any local process can drive the full editor surface and rewrite the user's disassembly unattended.
  [LOW/certain/unverified] MAIN-A4 file:read-binary and file:list-project-files are unguarded — the renderer has arbitrary filesystem read
      src/main/file-io.ts:5 — readBinaryFile does no path validation, so an absolute or `..` path escapes the project root; listProjectFiles likewise walks any basePath. The rel-path guard is therefore a hygiene convention, not a security boundary.
  [LOW/probable/unverified] MAIN-A5 No setWindowOpenHandler / will-navigate handler — a child window would inherit the preload and expose window.api on a remote origin
      src/main/index.ts:32 — createWindow installs no navigation or window-open policy, so Electron's defaults apply: window.open to an arbitrary URL is allowed and the child window inherits the parent's webPreferences including the preload that exposes arbitrary file read/write.
  [LOW/certain/unverified] MAIN-A6 Second Aurora instance clobbers then deletes the discovery file, orphaning the first instance
      src/main/mcp-server.ts:143 — The discovery file is written unconditionally and deleted unconditionally on quit, so running two Aurora instances leaves the surviving one undiscoverable.
  [LOW/certain/unverified] MAIN-A7 recent-projects.json shape is never validated; a non-array file breaks the recents API with a TypeError
      src/main/recent-projects.ts:12 — getRecentProjects catches parse errors but not shape errors, so a corrupt store containing valid JSON that is not an array makes addRecentProject/removeRecentProject throw, rejecting the IPC invoke.
  [LOW/probable/unverified] MAIN-A8 Bus-facing paint/stamp methods have unbounded size params while their siblings are capped
      src/main/editor-methods.ts:68 — paint_region's w/h/entries and set_layout_region's chunkIds carry no upper bound, unlike every neighbouring method, so a local bus client can hand the renderer an arbitrarily large region.
  [LOW/certain/unverified] MAIN-A9 SSE subscriber set is unbounded, and the advertised event channel is never actually pushed to
      src/main/aether/adapter.ts:83 — addSubscriber accepts unlimited SSE connections with no cap and no auth, and `broadcast` — the only thing that would use them — is never called anywhere in the codebase, so the advertised editor/ready event stream emits exactly one message at connect time and nothing thereafter.
  CLEAN: BrowserWindow flags, stated plainly (src/main/index.ts:40-47): contextIsolation TRUE (explicit, :42), nodeInte | Express server bind address is loopback-only and explicit: mcp-server.ts:118 `srv.listen(port, '127.0.0.1', .. | No process spawning anywhere in the privileged surface: grep for `child_process|execSync|spawn(` across all of | No shell.openExternal, no custom protocol registration, no registerFileProtocol/interceptFileProtocol, no <web | A real CSP ships with the renderer: src/renderer/index.html has `default-src 'self'; script-src 'self'; style- | The /aether loopbackOnly middleware is genuinely restrictive for the route it covers (mcp-server.ts:83-91): th | guarded-write.ts is the one write path that is both atomic and genuinely guarded, and its guard is tested with | isRelPathSafe (src/shared/rel-path.ts) rejects the real escape shapes: leading `/`, leading `\`, `^[a-zA-Z]:`  | agent-bridge.ts request lifecycle is leak-free on all three exits: resolve/reject via the response listener cl | Both dialog handlers scope themselves to the sender's own window (ipc-handlers.ts:57, :97, :111 via BrowserWin | The MCP tool surface and the Aether method surface cannot drift: both are generated from the single EDITOR_MET | file:read-binary's missing-file marker round-trip is correct end to end: main resolves `{__missing}` for ENOEN | The CDP debug port is env-gated and loopback-bound (index.ts:25-28): nothing opens a remote-debugging port unl

===== SEAT DEAD (opus) — 9 findings, 9 clean =====
COVERAGE: SIGNIFICANT GAP — the shell died partway through. Around the two-thirds mark every Bash call began returning exit 1 with no output; the cause is disk-quota exhaustion (a Write to the scratchpad returned EDQUOT), and clearing ~/.npm/_cacache and ~/.npm/_npx did not restore it. No Grep/Glob tool is exposed in this seat, so from that point I could only use Read. Everything reported above was verified BEFORE the shell died or via Read afterwards; nothing is asserted on unverified grounds.

NOT COVER
  [MEDIUM/certain/unverified] DEAD-A1 toolStore.ts is a zero-importer duplicate of artStore's tool-modifier slice
      src/renderer/state/toolStore.ts:14 — `useToolStore` is a complete zustand store (mirror / ditherPattern / ditherSecondary / pixelPerfect plus setMirror/setDither/setPixelPerfect) with ZERO importers anywhere in src/, and every field and setter name is duplicated verbatim by the live `useArtStore`.
  [MEDIUM/certain/unverified] DEAD-A2 Aether advertises an `editor/ready` event it can never emit — `broadcast` has no callers
      src/main/aether/adapter.ts:94 — The SSE event channel is fully built and wired for subscribers, but `broadcast()` is never called from anywhere, so the `events: ['editor/ready']` capability returned by the `initialize` handshake is a promise the server cannot keep.
  [MEDIUM/certain/unverified] DEAD-A4 src/core/formats/sprite-mappings.ts is a stale duplicate of the live sprite-mapping parser
      src/core/formats/sprite-mappings.ts:36 — This 175-line S2 sprite-mapping parser (`parseSpriteMappings`, `SpriteMappings`, `SpritePiece`) has zero importers; the live parser of the same name is src/core/import/sprite-mappings-import.ts, and every real consumer imports THAT one.
  [LOW/certain/unverified] DEAD-A3 src/core/art/camera.ts: 8 of its 10 exports are dead; the migration its header promises never happened
      src/core/art/camera.ts:1 — Only `screenToWorld` is consumed; `clampZoom`, `worldToScreen`, `pan`, `zoomAtPoint`, `fit`, `zoomToSelection`, `clampPan` and `ZoomBounds` have zero consumers, and the module header's stated purpose (the Map viewport migrating onto this camera) was never carried out.
  [LOW/certain/unverified] DEAD-A5 src/core/formats/object-names.ts is an S2-era object-name table superseded by the S1 profile
      src/core/formats/object-names.ts:5 — `OBJECT_NAMES` (134 lines of Sonic 2 object IDs) and its lookup helper have zero importers; the live name table for the classic backend is `S1_OBJECT_NAMES` in src/core/project/profiles/s1-objects.ts.
  [LOW/certain/unverified] DEAD-A6 Four more source files have zero importers and are unreachable from every entry point
      src/renderer/components/SectionList.tsx:6 — SectionList.tsx, core/formats/object-defs.ts, core/formats/s4-objects.ts, core/formats/s4-rings.ts and core/import/color-quantize.ts are all unreachable from src/renderer/index.tsx, src/main/index.ts and src/preload/index.ts, and none is imported by any test.
  [LOW/certain/unverified] DEAD-A7 Seven exported symbols in otherwise-live files have zero callers
      src/core/art/classic-surface-buffer.ts:153 — These exports are dead while their host modules are live, so file-level tools and the reachability walk both miss them: buildChunkSurfaceById, decodeObjectArt, TileDeduplicator, ToolButtonGrid, resetClassicObjectArt, IconUndo, IconRedo.
  [LOW/certain/unverified] DEAD-A8 The design system's entire typography scale is generated but never consumed
      src/renderer/styles/theme.css:1 — 25 of the 55 CSS custom properties emitted by scripts/gen-theme.mjs are never referenced by any `var()` in src/renderer, including all 12 type-scale tokens and all 3 weight tokens — while 227 inline `fontSize` sites hardcode 13 distinct pixel values.
  [LOW/certain/unverified] DEAD-A9 core/model/types.ts is a compat barrel whose value re-exports are all dead
      src/core/model/types.ts:19 — The barrel re-exports 16 runtime symbols and 15 types from s4-types, but its five importers consume only Tile, Color, Palette, PaletteLine and TileRef — every value re-export (createSection, createSectionTileGrid, createChunkDef, packNametableWord, unpackNametableWord, SECTION_TILES_*, BLOCK_*, BLOC
  CLEAN: scripts/gen-theme.mjs is NOT stale: I ran it and it resolved its token source through /home/volence/sonic_hack | src/renderer/providers/properties-classic.ts is dead-by-design, NOT an oversight — do not put it on a deletion | src/renderer/components/art-shared/ToolColumnParts.tsx is live despite six knip hits against it — the classic  | src/core/import/tile-dedup.ts must be kept: `flipTile` (tile-dedup.ts:6) has three live importers (core/export | scripts/render-classic-act.mjs and scripts/verify-s1-roundtrip.mjs are deliberate standalone verification harn | The `screenToWorld` single-symbol re-export at src/renderer/components/classic/viewport-math.ts:145 is genuine | Twenty more knip 'unused export' hits are over-exports with a real internal caller, not dead code — each verif | The three source entry points are all real and reachable: src/renderer/index.tsx (which imports './styles/them | knip's 'unused file' report for scratchpad/*.mjs (26 entries) is noise, not a finding — those are per-investig

===== SEAT VIS (opus) — 15 findings, 8 clean =====
COVERAGE: SHELL FAILURE MID-SWEEP — state this plainly: the Bash tool died with EDQUOT (disk quota exhausted on the machine) roughly halfway through, after the theme/colour/spacing/font-size measurements but BEFORE I could grep for `outline`, `tabIndex`, `aria-`, `role=`, `.focus()` and `autoFocus` across the tree. Everything I report about focus rings, focus traps, tabIndex and ARIA was established by READING individual files, not by an exhaustive scan. Treat those counts as lower bounds: there may be mo
  [HIGH/certain/CONFIRMED] VIS-VIS1 Ctrl+B and Ctrl+K silently arm a destructive map tool alongside their real action
      src/renderer/components/MapViewport.tsx:677 — MapViewport's tool-letter switch does not guard `o r t b k m` against the Ctrl/Cmd modifier, so the app-level Ctrl+B (toggle Explorer) and Ctrl+K (command palette) each ALSO change the active editor tool on an aeon level tab.
  [MEDIUM/certain/unverified] VIS-VIS2 Six different "is the user typing?" predicates across eight window keydown registrations; two have none
      src/renderer/components/MapViewport.tsx:538 — Every editor shortcut is a window-level listener with a hand-rolled text-entry guard, and the six copies have diverged — MapViewport's and ComposerCanvas's both miss `<select>`, so native select typeahead fires editor shortcuts.
  [MEDIUM/certain/unverified] VIS-VIS3 The shared `Chip` primitive is a <span> with onClick — every action built from it is mouse-only
      src/renderer/components/ui/primitives.tsx:91 — `Chip` renders `<span title onClick>` with no tabIndex, no role and no key handler, so chips cannot be reached by Tab or activated by Enter/Space anywhere in the app — including primary actions with no alternative control.
  [MEDIUM/certain/unverified] VIS-VIS4 Three modals, three different dismissal/focus contracts
      src/renderer/components/canvas/ImportSheetDialog.tsx:41 — ImportSheetDialog has no Escape handler, no focus trap and no initial focus, while it dismisses on backdrop click — the exact combination NewCanvasDialog deliberately rejected, and the only modal in the app where Esc does nothing.
  [MEDIUM/probable/unverified] VIS-VIS5 Ctrl+K opens the command palette BEHIND an open modal and steals focus to an invisible input
      src/renderer/components/CommandPalette.tsx:25 — CommandPalette's Ctrl+K listener is ungated by modal state and its backdrop sits at z-index 1000 while ConfirmDialog and NewCanvasDialog sit at 1100, so pressing Ctrl+K while either is open renders the palette underneath them and focuses its hidden search field.
  [MEDIUM/certain/unverified] VIS-VIS6 Focus rings are suppressed and never replaced; no focus token exists at all
      src/renderer/components/canvas/NewCanvasDialog.tsx:277 — Interactive controls set `outline: 'none'` with no substitute, and neither index.html nor theme.css defines any :focus/:focus-visible rule or a --focus-ring token, so the one surface with a deliberate Tab trap cycles focus invisibly.
  [MEDIUM/certain/unverified] VIS-VIS7 The generated type scale has zero consumers; 227 hardcoded font sizes span 13 values against a 7-step contract
      src/renderer/styles/theme.css:33 — gen-theme.mjs emits --text-*-size/--text-*-line and --weight-* from the Empyrean contract, and nothing in the renderer reads them except one `body` fallback — the app's actual typography is 227 inline numeric fontSize values, most of them below the scale's floor.
  [MEDIUM/certain/unverified] VIS-VIS14 Tool hotkeys exist on aeon's level canvas and on no other surface in the app
      src/renderer/components/classic/ClassicLevelViewport.tsx:955 — Nine single-letter tool hotkeys are bound on aeon's MapViewport; the classic level canvas binds none, and none of the five pixel-art surfaces binds a single tool letter — so the same named facet has a different keyboard depending on which engine is open.
  [LOW/certain/unverified] VIS-VIS8 canvas-colors.ts comments claim token mirroring that is false; the tile browser draws selection and hover in two different greens
      src/renderer/canvas/canvas-colors.ts:120 — Several canvas constants are stale Catppuccin values whose comments assert they mirror an Empyrean token they do not match, and the visible consequence is that TilesetPanel/ArtBrowser outline the SELECTED tile in one green and the HOVERED tile in a different green.
  [LOW/certain/unverified] VIS-VIS9 Selected palette line wears an emerald ring over a Catppuccin-blue wash
      src/renderer/components/PaletteViewer.tsx:95 — The palette-line selected state combines `T.accent` (emerald) for the border with a hardcoded blue tint for the background — two accent hues on one element.
  [LOW/certain/unverified] VIS-VIS10 Floating HUD chrome is on two different near-blacks
      src/renderer/components/MapViewport.tsx:1585 — The map hover bar and the composer collision HUD sit on rgba(17,17,27,…) (#11111B) while every other floating overlay uses rgba(10,12,18,…) — the value of --void.
  [LOW/certain/unverified] VIS-VIS11 `ToolButton` and `Divider` each exist twice with divergent APIs and opposite visuals
      src/renderer/components/art-shared/ToolColumnParts.tsx:155 — Two components named `Divider` render perpendicular rules, and two named `ToolButton` take incompatible props and differ in size and accessibility — swapping an import silently changes the layout.
  [LOW/certain/unverified] VIS-VIS12 Four readouts render in the OS default mono instead of the token font
      src/renderer/components/art-shared/ToolColumnParts.tsx:181 — `fontFamily: 'monospace'` is written raw in several numeric readouts, so they render in the browser default mono while every other mono readout uses JetBrains Mono via T.fontMono.
  [LOW/certain/unverified] VIS-VIS13 The classic composer uses a 3px radius that is not on the token scale
      src/renderer/components/classic/composer-shared.tsx:253 — Six shared composer styles hardcode `borderRadius: 3`, a value the emitted radius scale does not contain, so the classic art tiers are visibly rounder-by-one than the rest of the app.
  [LOW/probable/unverified] VIS-VIS15 Space cannot activate any focused button while a pixel surface is mounted
      src/renderer/components/art-shared/use-hand-pan.ts:63 — The hand-pan hook calls preventDefault on every Space keydown that is not in a text field, which suppresses Space-activation of focused buttons app-wide, not just over the canvas.
  CLEAN: Colour-token discipline is genuinely strong: only 25 hex literals exist in all of src/renderer (excluding __te | Cross-facet layout consistency is structurally enforced rather than conventional: LevelWorkspace.tsx:126-133 c | The four classic art tiers really do share their substrate rather than re-implementing panels: TileTab.tsx:15- | Keep-alive keyboard exclusivity is correct and its reasoning has been corrected against a real measurement: le | The canvas grid alphas have a real cross-file guard with a stated failure mode: canvas-colors.ts:84-99 pins CA | NewCanvasDialog's focus trap is real, not decorative: the index arithmetic is extracted to ui/focus-trap.ts (n | ui/primitives.tsx:55-78 gets button accessibility right where it uses real buttons — ToolButton and IconButton | MapViewport's tool hotkeys are correctly scoped to the active facet's declared tool set (MapViewport.tsx:646-6