[
  { "clickAt": "const b=[...document.querySelectorAll('button')].find(e=>e.innerText.trim()==='Oracle Jungle Zone · act1'&&e.getBoundingClientRect().left<240); const r=b.getBoundingClientRect(); return {x:Math.round(r.left+r.width/2),y:Math.round(r.top+r.height/2)};", "after": 2500 },
  { "sleep": 1200 },
  { "clickAt": "const b=[...document.querySelectorAll('button')].find(e=>e.innerText.trim()==='Undo'); const r=b.getBoundingClientRect(); return {x:Math.round(r.left+r.width/2),y:Math.round(r.top+r.height/2)};", "after": 1200 },
  { "clickAt": "const b=[...document.querySelectorAll('button')].find(e=>e.innerText.trim()==='Effects'); const r=b.getBoundingClientRect(); return {x:Math.round(r.left+r.width/2),y:Math.round(r.top+r.height/2)};", "after": 2500 },
  { "sleep": 1500 },
  { "eval": "const vis=(el)=>{const r=el.getBoundingClientRect();return r.width>0&&r.height>0&&r.bottom>0&&r.right>0&&r.top<innerHeight&&r.left<innerWidth};const L=[];for(const el of document.querySelectorAll('button,input,select,textarea,[role=tab]')){if(!vis(el))continue;const r=el.getBoundingClientRect();L.push(`${el.tagName.toLowerCase()}${el.disabled?' DIS':''} ${Math.round(r.left+r.width/2)},${Math.round(r.top+r.height/2)} :: ${((el.innerText||el.value||el.placeholder||el.title||'').trim().replace(/\\s+/g,' ')).slice(0,44)}`)}return L.join('\\n')+`\\n=== ${L.length}`;" },
  { "shot": "25-facet-effects" }
]
