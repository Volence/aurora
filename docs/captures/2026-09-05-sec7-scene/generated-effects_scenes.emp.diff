diff --git a/games/sonic4/data/generated/ojz/act1/effects_scenes.emp b/games/sonic4/data/generated/ojz/act1/effects_scenes.emp
index 318c9b04..319ea3ec 100644
--- a/games/sonic4/data/generated/ojz/act1/effects_scenes.emp
+++ b/games/sonic4/data/generated/ojz/act1/effects_scenes.emp
@@ -5,8 +5,8 @@
 // tools/EFFECTS_CONSUMER_CONTRACT.md §2 and the design is
 // docs/superpowers/specs/2026-08-22-aurora-effects-wave1-design.md §3.
 //
-// STATE OF THIS BAKE: 3 editor scene(s) reached by an assignment,
-// 3 binding(s), 9 act sections. Authored but unassigned: none.
+// STATE OF THIS BAKE: 4 editor scene(s) reached by an assignment,
+// 4 binding(s), 9 act sections. Authored but unassigned: none.
 //
 // EVERY `pub comptime fn` AT THE BOTTOM IS EMITTED FOR EVERY ACT, ALWAYS — the act
 // default, the section scene, and one per `EffectsPreset` channel (raster, patched,
@@ -90,6 +90,26 @@ pub const Scene_Editor_ojz_act1_floor: Scene = scene(
     v_offset: 288,
     transition: TRANS_INSTANT)
 
+pub const Scene_Editor_ojz_act1_sec7_worldwater: Scene = scene(
+    layers: [ layer(world_y: 0, fa: FACTOR_1_8, fb: FACTOR_1_8),
+              layer(world_y: 3, fa: FACTOR_1, fb: FACTOR_1_2, curve: SceneCurve.To(FACTOR_1_4)),
+              layer(world_y: 162, fa: FACTOR_3_4, fb: FACTOR_1_2, curve: SceneCurve.To(FACTOR_1_8), vsplit: SceneVSplit.At(67)),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer(),
+              no_layer() ],
+    count: 3,
+    v_factor: 15)
+
 pub const Scene_Editor_ojz_act1_start: Scene = scene(
     layers: [ layer(world_y: 0, fa: FACTOR_1, fb: FACTOR_1_16, dsa: 15, dsb: 15),
               layer(world_y: 32, fa: FACTOR_1, fb: FACTOR_1_16, dsa: 15, dsb: 15),
@@ -114,9 +134,10 @@ pub const Scene_Editor_ojz_act1_start: Scene = scene(
     transition: TRANS_INSTANT,
     anchor: SceneAnchor.At(0, 15, 2))
 
-pub const EditorScenes_OJZ_Act1: [Scene; 3] = [
+pub const EditorScenes_OJZ_Act1: [Scene; 4] = [
     Scene_Editor_ojz_act1_depth,
     Scene_Editor_ojz_act1_floor,
+    Scene_Editor_ojz_act1_sec7_worldwater,
     Scene_Editor_ojz_act1_start,
 ]
 
@@ -129,8 +150,8 @@ pub const EditorScenes_OJZ_Act1: [Scene; 3] = [
 // unreferenced top-level `const X = f(..)` is comptime-INERT and the fold would
 // never run (docs/EMP_PITFALLS.md §3).
 pub const EditorScenes_OJZ_Act1_BudgetChecked = scene_budget_enforce(EditorScenes_OJZ_Act1)
-ensure(EditorScenes_OJZ_Act1_BudgetChecked == 3,
-       "editor scenes: the budget fold checked {EditorScenes_OJZ_Act1_BudgetChecked} scenes, not the 3 bound by this act's assignments — a fold that examines fewer scenes than it was handed is gating a subset")
+ensure(EditorScenes_OJZ_Act1_BudgetChecked == 4,
+       "editor scenes: the budget fold checked {EditorScenes_OJZ_Act1_BudgetChecked} scenes, not the 4 bound by this act's assignments — a fold that examines fewer scenes than it was handed is gating a subset")
 
 // THE CAPABILITY SUBSET TEST, one-sided exactly as the registry's is. An editor
 // scene that demands a scanline service the game does not declare would have its
@@ -142,8 +163,9 @@ ensure((EditorScenes_OJZ_Act1_CapsFolded & ~Game.SCANLINE_CAPS) == 0,
        "editor scenes: the folded capability mask {EditorScenes_OJZ_Act1_CapsFolded} is NOT a subset of Game.SCANLINE_CAPS {Game.SCANLINE_CAPS}; the UNDECLARED bits are {EditorScenes_OJZ_Act1_CapsFolded & ~Game.SCANLINE_CAPS} — an Aurora-authored scene demands a scanline service this game does not declare. Either widen SCANLINE_CAPS in games/sonic4/config/game.emp, or stop authoring the capability in the scene that raises it")
 
 // ---- the lowered records, under STABLE binding names ----
-pub data EditorSceneBinding_OJZ_Act1_Sec0: SceneCfg5 = lower5(EditorScenes_OJZ_Act1[2])
+pub data EditorSceneBinding_OJZ_Act1_Sec0: SceneCfg5 = lower5(EditorScenes_OJZ_Act1[3])
 pub data EditorSceneBinding_OJZ_Act1_Sec4: SceneCfg5 = lower5(EditorScenes_OJZ_Act1[0])
+pub data EditorSceneBinding_OJZ_Act1_Sec7: SceneCfg3 = lower3(EditorScenes_OJZ_Act1[2])
 pub data EditorSceneBinding_OJZ_Act1_Sec8: SceneCfg3 = lower3(EditorScenes_OJZ_Act1[1])
 
 // ---- AURORA-AUTHORED REEL RATES + THE BINDING TABLE (item 10, DEBUG TIER) ----
@@ -288,8 +310,8 @@ pub data EditorVariant_OJZ_Act1_ojz_sec3_shimmer_0: pal_variant = variant(shift_
 // below, including the budget fold, builds green while asserting nothing. Measured
 // on this very module (2026-08-22): with the descriptor's `use` line removed, an
 // `ensure(1 == 0)` here built GREEN with an unchanged CRC.
-pub equ EditorScenes_OJZ_Act1_Count = 3
-pub equ EditorScenes_OJZ_Act1_Bindings = 3
+pub equ EditorScenes_OJZ_Act1_Count = 4
+pub equ EditorScenes_OJZ_Act1_Bindings = 4
 pub equ EditorRaster_OJZ_Act1_Bindings = 2
 pub equ EditorCycle_OJZ_Act1_Bindings = 0
 pub equ EditorVariant_OJZ_Act1_Bindings = 0
@@ -321,6 +343,7 @@ pub comptime fn ojz_act1_sec_scene(sec: int, hand: Label = 0) -> Label {
     comptime var out = hand
     if sec == 0 { out = EditorSceneBinding_OJZ_Act1_Sec0 }
     if sec == 4 { out = EditorSceneBinding_OJZ_Act1_Sec4 }
+    if sec == 7 { out = EditorSceneBinding_OJZ_Act1_Sec7 }
     if sec == 8 { out = EditorSceneBinding_OJZ_Act1_Sec8 }
     return out
 }
