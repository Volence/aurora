<!--
Authored 2026-07-03 by Claude Fable 5. This is Aurora's forward-looking master plan,
written to be executed cold by later (smaller) model sessions: every phase names its
contract documents, file paths, and acceptance gates. Companion: PLAN_AUDIT_2026-07-03.md
(what was stale and why). Grounding: aurora/src survey, empyrean/docs + contract/ + design/,
aeon/structs.asm + tools/ + docs/superpowers/{specs,plans}/2026-07-02-* (the design week).
Update this doc as phases complete — mark DONE in place, log discoveries under each phase.
-->

# Aurora — Art-Suite Roadmap

*The plan of record for making Aurora the best-in-class visual authoring tool for the
Aeon engine. Last full revision: 2026-07-03; **re-sequenced 2026-08-19** (§2.6 + §5).*

> **Read §2.6 before §2, §3 or §5.** Two lines of work were delivered out of this plan's
> sequence — §2.5 (disasm-as-project, 2026-08-09) and §2.6 (the August line, 2026-08-12 →
> 08-19) — and where they disagree with the older sections, **they win**. §5.1 is the
> short list of what is open right now.

## 1. Mission

Empyrean's charter (`empyrean/docs/ROADMAP.md`) makes Aurora **the single home for
everything visual**: level layout, level art, collision, sprites, animation, screens/
menus/HUD, parallax/raster effects, and object-behavior authoring. No separate sprite
app, no separate menu tool. The quality bar is unchanged: *"did a modern studio ship
this for their own engine?"*

Two structural rules frame everything below:

- **Aurora authors documents; generators bake them.** The pattern proven by collision
  (`.collattr.bin` → `ojz_strip_gen.py` overlay) is now the suite standard: Aurora
  writes editor-owned files under `aeon/games/<game>/data/editor/`, a Python generator
  in `aeon/tools/` validates them with hard build gates and emits `data/generated/`.
  Aurora never emits engine assembly directly (the TS asm exporters are being retired —
  see §4.2, §4.7). Compression is Crucible's (ZX0/S4LZ), never Aurora's.
- **Aurora is becoming a live tool, not just a file editor.** The Aether bus
  (`empyrean/contract/protocol.md`) is how Aurora talks to Oracle (the emulator):
  live palette pushes, play-from-cursor, and design #8's real-engine 60fps parallax
  preview. Aurora already *serves* Aether; the missing half is the **outbound client**
  (§4.8) — named by the 2026-07-01 suite audit as the whole suite's keystone gap.

## 2. Ground truth (2026-07-03)

> Dated, and partly overtaken — §2.5 and §2.6 are the current picture. Kept because the
> engine-contract half below is still the contract.

**Shipped and solid** (verified against src + git):
- **Map mode** — 256×256-tile sections, sparse act grids, tile/block/chunk stamping,
  BG (Plane B) assignment per act + per-section override, object/ring *display*,
  section management (add/remove/move/copy, undoable), zone-tileset prerender.
- **Art mode** — full pixel suite (pencil/fill/line/rect/select/dither/mirror/
  transforms), tile/block/chunk documents, flip-aware tileset dedup, edit-in-place,
  chunk library, palette editor with live preview.
- **Sprite mode** — whole-frame bitmap editing, multi-game adapters (S1/S2/S3K/S4)
  for mappings+DPLC read, animation *playback* timeline, zone/standalone palette
  modes + copy bridge, cross-sprite pixel clipboard, snapshot undo merged with the
  shared history.
- **Collision authoring** — the real engine model end-to-end: 16-bit cell words
  (S&K-derived shape 9:0 | xflip 10 | yflip 11 | solidity 13:12), dual planes A/B,
  block-keyed reuse painting, kind-tabbed shape palette, sticky flips,
  `.collattr.bin`/`.collattrb.bin` sidecars **consumed authoritatively by the engine
  build** (`aeon/tools/ojz_strip_gen.py:1065`). WYSIWYG confirmed in-game.
- **Agent surface** — MCP server (:38473, ~16 `editor/*` tools) + Aether *server*
  adapter (`/aether` + SSE), shared undo with human edits, loopback-only.
- Health: ~83 test files (vitest), tsc clean, electron-vite build.

**The engine contract that matters now** (supersedes `aeon/docs/LEVEL_EDITOR_SPEC.md`
until that doc is refreshed — trust `aeon/structs.asm` + the 2026-07-02 specs):
- `Sec` is **66 bytes ($42)**; per-section tile art is gone. Art is an **act-wide,
  globally-deduped, ZX0-paged pool** (`act_art_pool_table`/`act_art_pool_pages`,
  256-tile pages; manifest at `data/generated/ojz/act1/ojz_act_pool_manifest.asm`).
  Blocks are 768-byte units (512 nametable + 128 collision A + 128 collision B),
  S4LZ v3 with per-section dictionaries. Camera bounds are grid-derived, not authored.
- Editor-owned inputs the build already consumes: `section_{N}.tiles.bin`,
  `.collattr.bin`/`.collattrb.bin`, `objects.json`, `rings.json`, `meta.json`,
  `data/editor/{zone}_tiles.bin`, BG library. ⚠ **Corrected 2026-08-22:**
  `editor_bg_override.json` was listed here and is NOT Aurora-owned — it has
  **zero references in `src/`** and is produced by aeon's own
  `tools/png_to_bg_override.py`. Load-bearing for the effects arc: wave 1's
  BgAnim bands ride that file's `anims` key, so Aurora must **become a writer of
  a file it has never touched** — not extend a path it already owns.
- Coming from the design week: `section_id` widens byte→word (floating origin, #2);
  per-act ROM budget gates (#1); an engine/game split with a game manifest (#5).

**The four Aurora-facing engine designs — all approved, execution-ready**
(specs+plans in `aeon/docs/superpowers/{specs,plans}/2026-07-02-*`):

| # | Design | Aurora's half |
|---|---|---|
| 6 | Editor collision authoring | Chunks carry dual-plane 16-bit collision; stamps place art+collision atomically; legacy encodings retired; `paint_collision` MCP tool |
| 7 | Screens/HUD | 4th AppMode `'screen'`: visual authoring of title/menus/results/game-over as JSON documents → `screens_gen.py`; 7 MCP tools |
| 8 | Raster + parallax | 5th AppMode `'raster'`: band/curve/raster-timeline editors → `parallax_gen.py`+`raster_gen.py`; **Aurora's first outbound Aether client** for live 60fps preview via DEBUG RAM overrides; 6 MCP tools |
| 9 | Object behaviors | Map-mode properties panel: typed per-placement params + behavior picker → `behavior_gen.py`; subtype-as-bundle-index; retire TS entity exporter; 4 MCP tools |

## 2.5 Delivered out of sequence — Disassembly-as-Project (2026-08-09)

**Status: SHIPPED (v1)** — branch `feature/disasm-project`, ~35 double-reviewed
commits. Spec: `specs/2026-08-09-disasm-project-abstraction-design.md`; plan (with
per-task deviation notes): `plans/2026-08-09-disasm-project-abstraction.md`. This
was a motivated pull-forward, not part of the P0–P8 line (see §5) — it proves the
engine-agnostic project abstraction by opening a **Sonic 1 disassembly and editing
it in place**.

What landed:
- **Engine-agnostic `ProjectAdapter` layer** (`core/project/`): fingerprint
  `detect` → bundled profile → **loud resolution report** (per-entry
  resolved/missing/ambiguous) → capability manifest → domain accessors. Single
  `detectProject` registry with disjoint fingerprints; `.aurora/project.json`
  override sidecar.
- **S1 in-place editing**: hierarchical `LevelDoc` (tiles → blocks → chunks →
  chunk-id layout, never flattened) on the shared undo history; levels
  (layout/chunks/blocks/tiles/palette/colind), object placement, and object
  sprite art all edit and **save back S1's native formats** so its stock
  `build.lua` produces the modified ROM. Round-trip is byte-identical
  (uncompressed) / decompressed-identical (compressed) across all six zones.
- **Pure codecs** (`core/formats/classic/`): Enigma (the one missing codec) +
  S1 plain-binary layout/objpos/startpos/colind/collision-shape codecs; Nemesis
  and Kosinski encoders already existed and were reused.
- **Guarded save**: atomic tmp+rename, mtime-conflict refusal, encoder self-check
  (re-decode + structural compare before any file is written).
- **12 MCP tools** for the classic surface (`open_project` … `save_project`),
  MCP-parity one-undo-step-per-mutation (`docs/MCP.md` → *Classic project tools*).
- **Aeon detection unified**: aeon now routes through the same `detectProject`
  registry (`AeonProjectAdapter`).

**Relationship to the P0–P8 plan:**
- **Pulls P8 Phase A/D forward.** P8's neutral `LevelDoc` model and its Phase D
  classic codecs (§4.10) are now built and battle-tested against real S1 data —
  P8 inherits them and its "donor disasms are import-only" assumption is
  superseded (edit-in-place proven). See the P8 note in §4.10 and §5.
- **Shares P1's placement vocabulary.** Classic object placement
  (select/move/Del/drag-from-library, inspector for id/subtype/flips/respawn)
  deliberately mirrors the P1 aeon entity-placement interaction (§4.1) so the
  later aeon work reuses the same UI gestures behind a different backing command.

**Deferrals — status after v1.1** (2026-08-09 afternoon, Tasks B1–B4 on the same
branch; plan `plans/2026-08-09-classic-v1.1-batch.md`):
- **Aeon adapter is a routing marker** — *CLOSED 2026-08-13 (`4782e86`); this entry was
  STALE for nine days and is corrected 2026-08-22.* `AeonProjectAdapter.open()` performs
  the **real project load** — `index.ts:115` calls `loadAeonProject(fa, …)` and returns it
  on `handle.aeon` — and `useProject.loadFromPath` **no longer exists anywhere in `src/`**
  (the renderer's aeon branch goes through `state/aeon-open.ts` → `aeonAdapter.open()`).
  What IS still deferred is narrower and should be described as such: the aeon `report` is
  not populated (`buildReport([])`), and `AeonProjectData` carries no scene/preset/band or
  budget concepts. Extending that model is a data-shape change, **not** "building a loader".
  This stale text was handed across the fence into aeon's 2026-08-22 effects assessment and
  refuted there; see `reviews/2026-08-22-aeon-effects-survey-verification.md`.
- **Composer wiring for classic content editing** — *CLOSED (v1.1 B3).* A
  self-contained `ClassicComposerDock` (Chunk/Block/Tile tabs) edits tile pixels,
  blocks, and chunks in the classic view with shared-structure UX (usage counts +
  shared-edit warnings + duplicate-then-edit) — deliberately NOT wired into aeon's
  Art mode.
- **Palette editing UI + MCP** — *CLOSED (v1.1 B4).* A `ClassicPalettePanel` (4×16
  swatch grid) edits the act palette through a reusable Genesis RGB-slider control
  (extracted from `PaletteEditor`'s idiom, decoupled from the aeon stores); each
  color commits one `classicSetPalette`, whose epoch bump refreshes chunk art +
  object sprites + thumbnails live. MCP now wires `set_level_palette` (classic
  `set_palette` — renamed to avoid the aeon `set_palette` name in the flat tool
  registry) and `set_start`.
- **Object art + loop authoring** — *CLOSED (v1.1 B1/B4).* Placed objects render
  their real S1 sprite art in the viewport (B1); loop-de-loop layout cells are
  authorable (the chunk picker's Loop toggle stamps S1's bit-7 flag; the viewport
  draws a corner glyph; the eyedropper preserves the flag) (B4).

## 2.6 Delivered out of sequence — the August line (2026-08-12 → 2026-08-19)

**Status: SHIPPED — all on `master` and pushed** (`ae5d6a2` … `8efda9d`, ~540 commits).
Recorded 2026-08-19. Where this section disagrees with §2, §3 or §5, **this section wins**;
every row was re-checked against source on that date.

Health at `8efda9d`: 296 test files, **3291 passed / 3 skipped**, `tsc --noEmit` clean.

Like §2.5 this was *steered*, not taken from §5's order, and for the same reason: the
motivating spine is the **classic (S1 disassembly) editor**, the one engine where a whole
level can be authored end-to-end today. Three lines landed, each on its own spec, plus a
full-surface review.

### A. UX overhaul — one shell, facet parity (spec stages 1–4)

Specs: `specs/2026-08-12-aurora-ux-overhaul-design.md` (§12 is the staging) and
`specs/2026-08-13-ux-overhaul-stage4-design.md` (**read its §2 and §3.0 first** — seven
claims from earlier documents were investigated and found wrong; four changed the design).
Merges `ae5d6a2` → `2f1db2b`, `a662e99`.

- **Stages 1–3** — document/session model (tabs, per-document undo, project-wide save),
  facet/tool/panel registries, tab strip + explorer + Home + Project Setup, and aeon's
  Map/Art/Sprite modes re-homed as facets of one `LevelWorkspace`.
- **Stage 4** — classic re-homed into that same workspace, and the exit criterion met:
  **both engines render through one `LevelWorkspace`**, through one `(engine, facetId)`
  module registry. `LegacyWorkspace`, `ClassicProjectView`, `ZoneActTree` and the old
  `Toolbar` are **deleted**. Undo is unified on `DocumentHistoryHub` (per-document stacks;
  undo-bus, sprite-undo, edit-seq and classic-history all gone). Step H converged the art
  tiers three ways instead of the spec's one shared Art facet — only one of classic's three
  tiers is a pixel surface — so classic's Tile tier now runs on aeon's
  `PixelEditController`/`PixelViewport`, one `art-shared/PaletteGrid.tsx` serves all four
  palette sites, and `s1ArtFacet`/`artFacet` stay separate modules.
- **Classic facets today** (`src/core/project/s1/index.ts`):
  `layout · objects · collision · palette · art`. Rings are absent by design (S1 rings are
  objpos objects, not a layer).
- **Not built from that spec:** stage 5's **typed cross-tab/cross-window clipboard** and the
  **Converter tab** (clipboards are still per-store; no converter module in-tree), and stage
  6's polish pass beyond what the lens sweep (D below) fixed. PNG import — stage 5's other
  half — shipped through line B instead.

### B. In-app art authoring — originate, constrain, commit (phases 1, 2A, 2B, 2C)

Specs: `specs/2026-08-15-in-app-art-authoring-design.md` (its **§0 · Corrections** is
authoritative over everything after it) and
`specs/2026-08-15-phase-2c-resolve-and-commit-design.md` (supersedes §4.4).
Merges `4427c2f`, `42ff186`, `d5f44d6`, `9a77e5b`, `697d409`.

The bar this line was built to: *Aurora must be good enough that artists don't leave for
Aseprite to make the art in the first place.*

- **Phase 1 — paint-through.** Paint pixels on a composed block/chunk surface; the stroke
  resolves down the tile→block→chunk reference ladder without silently damaging the other
  places sharing that tile.
- **Phase 2A/2B — the origination canvas.** A free canvas with a configurable constraint
  profile; clashes, colours-per-line and the flip-aware tile count stay live in front of the
  artist while they draw. Canvas documents are named sidecar files under `.aurora/canvas`.
- **Phase 2C — resolve and commit.** `canvas-resolve` (pure geometry) → `classic-commit-plan`
  → `classicCommitCanvas` (one store command), with a **PNG import path** at any size feeding
  the same resolver. A canvas commit therefore covers at most **16 chunks** — that is a
  derivation, not a literal in source (`CANVAS_MAX_SIDE` 1024px ÷ `CHUNK_PX` 256 = 4, so
  4×4); an agent auditing this went looking for a `16` and reasonably could not find one.
  The PNG path has no such cap. CDP-verified 7/7 (commit panel) and 4/4 (import).
- **Phase 3 (tile-pool growth) was measured and DECLINED** — see §0 of the phase-1/2 spec.
  Measured across all six S1 zones: **blocks** are both the dangerous tier and the variant
  tier; chunks have no near-duplicates at all; GHZ has 2 spare tiles.
- **Doc debt:** the 2C header says BUILT, but **§D2b cross-act reach reporting is not built**
  (SBZ palette sources, LZ/SBZ3 shared-file reach, the underwater-palette warning). Annotate
  the header rather than letting BUILT mean "except §D2b".

### C. Classic collision authoring + the agent surface (spec stages 1–5)

Spec: `specs/2026-08-16-classic-collision-authoring-design.md`. Merges `81a51fc`, `e981908`,
`a39afce`, `13734af`, `3217fab`, `9f734f4`, `4df618f`, `a814b09`, `eb138e2`, `92a7a00`,
`8efda9d`. **Complete and closed** — nothing it booked is outstanding; the packet is
`docs/reviews/2026-08-19-handoff-after-collision.md`.

Before it, `classicSetColind` was reachable **only** from the agent handler: the agent could
assign collision and a person could not. Now:

- A **Collision facet** for classic, over the real engine lookup verified against
  `s1disasm/_incObj/sub FindNearestTile & FindFloor & FindWall.asm`: chunk-cell solidity
  gates the block-id `colind`, and block 0 short-circuits both. Granted read-only at stage
  3a, write at stage 3b (a shape picker over `paint-collision`); solidity itself stays
  ChunkTab's Assign mode.
- **Committed art gets collision** (stage 4) — closing 2C's own out-of-scope note that a
  committed drawing landed at colind 0 / solidity 0, i.e. the player fell through new art.
- **Human gestures** — drag to paint, Shift-drag for a rectangle — and refusals that
  *guarantee* instead of hedging: link-equivalence carries `linkEquivalent`, a drag crossing
  a loop-flagged cell says so, and a partial paint names **which** cells were skipped.
- **Agent parity restored** (stage 5, two plans): `commit_canvas` + `import_art_sheet` (the
  whole art line had shipped UI-only) and `set_block_collision`. Each is one `EDITOR_METHODS`
  entry, so it lights up on **both** MCP and Aether at once; that registry now carries **36
  tools**. Spec `specs/2026-08-18-art-agent-surface-design.md` §1 corrects the "MCP parity"
  framing: it is registry work, with no MCP-specific and no Aether-specific half.

**Runtime proof lives outside vitest** — a node-only suite cannot see React or canvas.
`scratchpad/collision-agent-harness.mjs` (**8/8**),
`scratchpad/collision-gesture-harness.mjs` (**9/9**) and
`scratchpad/commit-collision-harness.mjs` (**8/8**) drive the real app under CDP; all three
must stay green, and each carries planted-defect notes in its footer.

The commit harness used to report 5/6, recorded here as "stage 4's row-4 *expectation* is what
is wrong". **Confirmed and closed in `c005f57`**, and the reason is worth keeping: row 4 wanted
the toggle to stamp `$FF` into *every* new block, but `withCollision` skips block ids past the
end of the zone's colind table (spec §5 / CLASSIC-A4 — in ROM they read the *adjacent* zone's
table). GHZ ships **439 blocks against a 410-entry** `collide/GHZ.bin`, so in GHZ that skip
covers every id a commit can append and the shape half of the toggle is a designed no-op — which
the preview stated on screen all along. The harness now runs GHZ *and* SLZ (414 blocks against a
500-entry table, where the write is permitted) and proves both halves. No app change was needed
for row 4 itself — but the preview's statement was half-silent: it said "0 will get flat ($FF)"
without saying blocks were skipped or why (`reportLines` dropped `skippedOverhang`). Closed
2026-08-20 (`fix/report-skipped-overhang`): the preview now carries the count and the overhang
reason, guarded by harness row 8 (planted red first) — hence 8/8.

### D. The 2026-08-16 lens sweep

`docs/reviews/2026-08-16-aurora-lens-sweep.md`, a full-surface review. Its R1–R14 defect
campaign landed (`de7fb4e`, `780d311`), the micro type tier `2xs: 10px/14px` landed
(`3a129f5`), and three findings were REFUTED and recorded so they are not re-found. Its §7
*direction* items are the open list in §5.1.

## 2.7 P2 delivered — the playtest loop (2026-08-19)

**Status: SHIPPED.** Aurora is now Aether's first outbound client, which the
2026-07-01 suite audit named as the whole suite's keystone gap. Aeon-first, not
classic-first as the lens sweep recommended — that advice predated the switch to
`oracle-next`, and aeon's path was measurably working while classic's had two
unverified links — measured 2026-08-19 in
`docs/reviews/2026-08-19-classic-playtest-links.md`: they were **symbols** (oracle's
listing parser hard-refuses an AS `.lst`; gates everything, since Aurora resolves only
by symbol) and **play-from-cursor** (S1 has no warp mailbox and no facility to co-opt).
Build and live palette were false worries — 600 ms native build, palette strictly
simpler than aeon's.

Health: 3383 passed / 3 skipped, `tsc` clean.

- **Client** (`src/main/aether/client.ts`) — unix socket, NDJSON JSON-RPC,
  four-step socket-path resolution, symbol resolution with a cache dropped on
  `romReloaded`. **The handshake is two messages**: `initialize` advertising
  `events: true`, then an `initialized` notification. Subscription happens on the
  second; a client sending only the first gets a healthy connection that silently
  never receives an event.
- **Live palette** — writes `Pal_Base` (96 bytes, **lines 1–3 only**) then sets
  `Pal_Base_Dirty`, payload-then-flag. NOT a `write_cram`: both engines rebuild
  CRAM from a RAM source every frame, so a CRAM write is a one-frame flash. Proven
  end to end by a harness driving the real app, with an independent observer client
  reading `Palette_Buffer` out of the machine.
- **Play-from-cursor** (F7) — through aeon's `Warp_Req_*` mailbox rather than a
  camera poke. Measured: a bare poke leaves 19/1120 visible-window nametable words
  wrong at +30f; the mailbox leaves 0. DEBUG-shape only, so a release ROM greys it
  out.
- **Build & Run** (Ctrl+Shift+B — Ctrl+B was already the Explorer) — save →
  `FAST=1 ./build.sh` → reload the ROM the emulator is actually running → restore
  the player's position. The panel opens on start and closes on success; errors are
  pulled to the top of its output.
- **Agent surface** — five `EDITOR_METHODS` entries, so all of it is live on MCP
  and Aether at once. The art line had to be retrofitted for this; this phase did
  not repeat that.

**Loop cost, attributed** (the interesting number was never the build): re-bake
7–12s · build 1.3s · position restore 1.5s · reload 64ms · save ~0. Aeon has an
incremental content-addressed re-bake in flight targeting <2s. **Updated 2026-08-20:**
aeon's re-bake landed at 0.99s, and position restore moved to the engine's boot-position
override (aeon `a2a24eb9`, ARCH §4.12b; consumed here in `src/main/aether/boot-restore.ts`)
— measured 643ms vs the warp-retry loop's 1442ms, and the first painted frame IS the
destination (the draw-then-jump is deleted, not shortened). The warp loop survives only
as a fallback for pre-override DEBUG ROMs; `restoredTo` now reports the engine-published
clamped pair on both paths. Aurora's own
contribution to the wall was a save that rewrote every file whether it had changed
or not, which bumped ~40 mtimes and marked aeon's level tree stale on **every**
build — fixed by comparing before writing.

### 2.7b The classic half — links 1 and 3 (2026-08-20)

**Status: SHIPPED** for s1disasm projects, per the split the measurement report
recommended (`2026-08-19-classic-playtest-links.md` §7). The gate that report
named — oracle-next's parser refusing AS listings — opened with oracle's
`a380830` drop; the report's §9 foreground acceptance and this parcel's own
harness both confirm `sonic.lst` loads (accepted unverified, 35-method server).

- **Build & Run** routes through the OPEN project (`state/build-and-run.ts`,
  ONE dispatch site for UI + agent + debug hooks): classic saves via
  `saveClassicProject` then runs `lua build.lua` in the disasm root. No env
  requirements, no FAST/DEBUG flavour (the plan never claims one), listing is
  the source-named `sonic.lst` — never a ROM-stem derivation. The three build
  fields are seeded fill-only into `.aurora/project.json` at open. **Measured
  end-to-end wall: ~650 ms** (save ~0 · build 0.6s · reload ~0) — the fastest
  Build & Run in the suite, as the report predicted.
- **Live palette** — classic is SIMPLER than aeon and is not a port: S1's
  VBlank DMAs all four `v_palette_line_N` lines to CRAM unconditionally every
  frame, so a push is ONE 32-byte write, no dirty flag (none exists; none was
  invented). The mapping is 1-indexed — editor line N ↔ `v_palette_line_(N+1)`
  — derived from Aurora's own S1 profile against `Palette Index.asm`, and the
  harness plants the off-by-one to prove the observer catches it. All four
  lines push (classic line 0 is an ordinary act line). A push PERSISTS until
  the next level transition/fade; only `PaletteCycle`'s few entries repaint.
  The connection probe reports **which family** the ROM carries
  (`paletteKind`), so neither engine's panel lights up against the other's ROM.
- **Play-from-cursor stays gated OFF** — link 4, not shipped and not faked: S1
  has no warp mailbox in any flavour, so F7 gates on symbol detection exactly
  as a release aeon ROM does, with a classic-worded reason. Position restore
  after Build & Run is likewise absent by design (`restoredVia` omitted): the
  only alternative is poking `v_player` on a running machine, which is link
  4's unmeasured spike. Both wait on the s1disasm-side mailbox decision the
  report assigned across the fence (§6 items 8–9).

Proof: `scratchpad/classic-playtest-harness.mjs`, 19/19 — real app under CDP on
the real s1disasm, independent observer client, anti-vacuous controls (stale-
dist refusal, pre-push sentinel absence, untouched-line checks), and the tree
restored byte-identically (git status equal) at the end.

## 3. The domain map

Where each art domain stands and where it goes. ★ = new capability, ☆ = upgrade.

| Domain | Today | Target | Contract | Phase |
|---|---|---|---|---|
| Level layout (map) | Mature | ☆ in-viewport object/ring placement + section/act properties | `specs/2026-07-03-entity-placement-properties-design.md` | P1 |
| Level art (tiles/chunks) | Mature; **authoring shipped** — paint-through, origination canvas, resolve-and-commit (§2.6 B) | ☆ export realignment to act-pool pipeline; VRAM budget v2 | §4.2 | P1/P6 |
| Collision | Mature (aeon map paint); **classic authoring shipped** — Collision facet, gestures, committed art gets collision (§2.6 C) | ☆ chunk-carried collision (design #6, **aeon**) | aeon spec #6 | P1 |
| Sprite art | Mature (draw/read) | ☆ finish the S4 export spine (decompose→mappings→DPLC→anim) | `plans/2026-06-17-sprite-*.md` | P3 |
| Animation | Playback only; **S1 `_anim` scripts auto-load** into the timeline on object art checkout, flips included (Parcel 1, 2026-08-20 — `docs/reviews/2026-08-20-s1-animation-audit.md`) | ★ authoring timeline + event tags | `specs/2026-07-03-animation-authoring-design.md` | P3 |
| Screens/menus/HUD | Nothing | ★ Screen mode (design #7) | aeon spec #7 | P4 |
| Parallax/raster | Config path in project.json only | ★ Raster mode + live preview (design #8) | aeon spec #8 | P5 |
| Object behaviors | Static JSON placement | ★ properties panel + behavior picker (design #9) | aeon spec #9 | P6 |
| Playtest loop | None | ★ Aether client: palette→CRAM, build→reload, play-from-cursor | `specs/2026-07-03-aether-client-playtest-design.md` | **P2 — next** |
| Import pipeline | **PNG import shipped for classic** on 2C's resolver (§2.6 B); **slicing + auto-palette exist as core algorithms** (2026-08-22), reachable by nothing yet | ★ wire slicing/suggestion into the import dialog + agent surface; sprite-frame targets | `specs/2026-07-03-png-import-design.md` + `specs/2026-08-15-phase-2c-resolve-and-commit-design.md` | P7 |
| Multi-game levels | Sprites only; **S1 read+write in place shipped** (§2.5) | ★ level adapters (S1/S2/S3K ⇄ each other ⇄ aeon, both directions) | `specs/2026-07-03-multi-game-level-interop-design.md` | P8 (Phase A unblocked now; **Phase A/D substrate landed by §2.5**) |

### 4.1 Level layout — close the last authoring gaps

Objects and rings render but are edited as raw JSON. Fix:
- **Place/drag/delete in-viewport**: drag from the object library palette onto the
  map; drag to move; Del to remove; ring line/arc/rect pattern tools (classic
  SonLVL patterns). One undo step per gesture via new `set-entities` command.
- Enforce the exporter constraints at edit time (section-local 0–$7FF, type < 32,
  subtype < 256, X-sorted on save) rather than at bake.
- **Section properties inspector**: surface the authored `Sec` fields Aurora hides
  today — `sec_flags` (water/underground/no-Y-wrap/preserve-state), `sec_music`,
  `sec_camera_lookahead`, per-section BG override, per-section parallax ref — in a
  right-dock panel writing `meta.json`. (Water flag pairs with the #8 water raster
  track later.)
- **Act/zone management**: "New Act / New Zone" wizard writing `project.json` +
  skeleton editor files, so creating a level never means hand-editing JSON. Grid
  resize already exists; expose start-position drag on the map.

This is also the substrate design #9 needs (its properties panel extends this
inspector), which is why it's Phase 1.

### 4.2 Level art — realign export to the real pipeline

The engine moved to the act-pool model; Aurora's `core/export/{vram-coloring,
act-descriptor,entity-data}.ts` target the retired one (see PLAN_AUDIT §1). Direction:
- **Retire, don't extend.** Aurora's save path (editor files) is the real interface; the
  Python generators own baking.

  > **Corrected scope, measured 2026-08-19 — the 2026-07-03 order and the 2026-08-19
  > handoff packet are BOTH wrong about this, in opposite directions. Re-measure before
  > you cut.** What is actually in-tree:
  >
  > | Module | Reality |
  > |---|---|
  > | `core/export/index.ts` | **Not dead.** `src/core/project/aeon/save.ts:27` imports `exportAct` and calls it at `:271` — the aeon save's export step. (The handoff packet says "ZERO importers"; that is wrong.) |
  > | `act-descriptor.ts`, `entity-data.ts` | Reached only through that barrel. |
  > | `vram-coloring.ts` | Reached through that barrel **and** by `src/core/agent/budget.ts:4` (`computeVramColoring`, `FG_TILE_LIMIT`), which `agent-handler.ts` uses for the **live** `check_budget` tool. Deleting it wholesale **breaks `check_budget`**. |
  >
  > What the export step emits — `{dataPath}export/{act_descriptor,entity_data,vram_bases}.asm`
  > and `section_N.{tiles,art}.bin` — is consumed by **nothing**. The directory *does*
  > exist and is populated (`aeon/games/sonic4/data/editor/ojz/act1/export/`, 30 files,
  > last written 2026-08-12) — an earlier revision of this note wrongly said it did not —
  > but nothing in aeon reads it: the build's `act_descriptor.emp` is authored under
  > `games/*/data/levels/`, and `tools/ojz_entity_gen.py` builds its own
  > `data/generated/ojz/act1/entity_data.emp` from the editor JSONs. Same filename,
  > different producer.
  >
  > So the order is: (1) delete the export step from `buildAeonSavePlan` — which also kills
  > sweep finding **R8**'s misleading "Project saved" after a failed export; (2) then
  > `export/index.ts`, `act-descriptor.ts`, `entity-data.ts` are genuinely dead — delete
  > them; (3) **keep** `vram-coloring.ts`, or move the two symbols `budget.ts` needs
  > somewhere honest and retire only the ASM generators.

  > **DONE 2026-08-19.** Executed exactly as ordered above. `buildAeonSavePlan` no longer
  > emits anything under `export/` and `AeonSavePlan.exportError` is gone with the R8 toast
  > branch it fed; `export/index.ts`, `act-descriptor.ts`, `entity-data.ts` and four test
  > files are deleted; `vram-coloring.ts` kept, trimmed to the two symbols `budget.ts` uses
  > (`assignVramBases`, `generateVramBasesAsm`, `VramBaseAssignment` deleted). Two guards
  > replace the deleted export tests and were both proven red against a planted
  > `export/act_descriptor.asm` push. Suite 291 files / 3271 passed / 3 skipped, tsc clean.
  > **Left alone deliberately:** the 30 stale files already on disk in the aeon tree —
  > deleting another repo's data is not this change's business.
- **Whole-level tile view stays** (the editor's flat-tile model is unaffected); what
  changes is bookkeeping: the VRAM budget readout should count **act-pool pages**
  (612 distinct tiles ⇒ 3 pages of 256) against the 1472-tile FG pool + 448-tile BG
  region, using `ojz_act_pool_manifest.asm` as ground truth, not per-section 300-tile
  caps. (`check_budget` MCP tool gets the same update.)
- Later (P6): a **budget visualizer** — per-page occupancy, shared-vs-unique tiles,
  what stamping this chunk would add — the "green/red before build" idea from the
  vision doc, rebuilt on the paged model.

### 4.3 Collision — execute design #6

Aurora-side only; aeon needs zero changes. Follow
`aeon/docs/superpowers/plans/2026-07-02-editor-collision-authoring.md` exactly:
`ChunkDef.collisionA/B: Uint16Array` (64 words per 16×16-tile chunk), atomic
art+collision stamping (one undo step), chunk composer paints collision with the
existing tool + palette, capture-from-map carries collision, paint default flips to
"just here" (propagation becomes the modifier), legacy `tileGrid.collision` nibble
plane + `ChunkDef.collision` byte + `.coll.bin` all deleted, `paint_collision` MCP
tool added. This *completes* the 2026-06 collision lineage.

### 4.4 Sprites & animation — finish the spine, then author

The read side is done (four game adapters); the S4 *write* side is the open queue
(plans `2026-06-17-sprite-{decomposition,mappings-export,animation-export}.md`):
1. **Auto-decomposition** — whole frame → hardware pieces (≤4×4 tiles, one palette
   line each) + flip-aware tile pool. This unlocks everything downstream.
2. **Mappings + DPLC export** to aeon's formats (`games/sonic4/data/{mappings,dplc}`;
   VDP-order pieces, 8 bytes/piece).
3. **Animation authoring**: upgrade the timeline from playback to editing — add/
   remove/reorder frames, per-frame 1/60s durations, loop/jump control codes, and
   **event tags as inline timeline markers** targeting the engine's `AF_*` codes
   (incl. `AF_CALLBACK`, which design #9 consumes as `EV_ANIM`). Onion-skinning.
4. **Registry payoff**: object library entries reference a sprite + preview frame, so
   placed objects render their real art in the map viewport (replaces markers).

Craft backlog (pull in opportunistically, each small: pixel-perfect pencil stroke,
symmetry painting, RotSprite rotation, boolean selection modes, numeric transforms —
research already done in `ideas/2026-06-16-art-suite-vision.md`).

### 4.5 Screens/menus/HUD — execute design #7 (Aurora half)

The engine plan sequences its own tasks first (font/text, fade engine, interpreter,
`screens_gen.py`); Aurora's mode is that plan's last stage. Build per the spec:
4th AppMode `'screen'`, `core/formats/screen.ts` (Zod mirror of the generator schema),
`screenStore` on shared undo, `ScreenViewport` (40×28 cell grid, font-tile text,
menu rows + cursor, images, live palette + fade preview), tool dock (text / menu /
image / properties: palette, music cue, fades, timeout+input actions), atomic save to
`games/sonic4/data/editor/screens/<name>.screen.json`, 7 MCP tools
(`list_screens`…`remove_widget`). Menu art itself is just Art mode (tiles) + this
mode (layout) — no separate "menu art" tool needed.

### 4.6 Parallax/raster — execute design #8 (Aurora half)

> ⚠ **SUPERSEDED IN ITS DATA CONTRACT (2026-08-22).** The incoming effects-authoring arc
> (§5.2) ruled **Option B**, owner-confirmed: Aurora authors `games/sonic4/data/editor/`
> **`effects/`**`*.json` and aeon's **`tools/effects_gen.py`** bakes generated `.emp`.
> The paths, packer and generators named below — `data/editor/{parallax,raster}/*.json`,
> `core/formats/parallax-pack.ts`, `parallax_gen.py`/`raster_gen.py` — are **NOT the
> contract**; neither generator was ever built, so nothing is lost by dropping them.
> Ruled here rather than left ambiguous so Aurora does not carry two contradictory
> contracts (`reviews/2026-08-22-aeon-effects-survey-verification.md`). The *editor
> surface* described below — band editor, curve editor, raster timeline, sequencer —
> stands and is what waves 1–2 build. Ruling 4 targeted the **act-level** ref, not the
> section-level one; **both are settled as of 2026-08-22** (§5.1 item 11): the act-level
> field is now **`Act.sceneRef`**, and the dead `Section.parallaxRef` was measured and
> deleted. Cite by symbol — item 11 records why a line number here went wrong as two
> different types inside one day.

The flagship "modern studio" feature: author per-scanline effects visually and see
them **in the running engine at 60fps** instead of hand-tuning asm tables. Per the
spec: 5th AppMode `'raster'` with band editor (drag boundaries over the rendered BG,
factor dropdowns constrained to representable shift-add fractions), curve editor
(sine params or 256-point freehand), raster timeline (224-line strip, split markers,
palette stops), sequencer step list; documents at
`data/editor/{parallax,raster}/*.json`; shared packer `core/formats/parallax-pack.ts`
golden-tested against `parallax_gen.py`; live preview = pack → debounce 50ms →
`emulator/write_memory` payload-then-flag into the engine's DEBUG override blocks;
fallback save→build→`reload_rom` stays supported. 6 MCP tools. Gated on engine tasks
1–4 of its plan (trampoline/walker, water effect, sequencer, generators + byte-equal
migration golden).

### 4.7 Object behaviors — execute design #9 (Aurora half)

Map-mode PropertiesPanel becomes a typed form driven by the archetype's param schema
(from `objects.json`, which gains `behaviors:{move,act,events}` + param declarations);
instance overrides via `set-object-props` command; behavior picker on library entries;
4 MCP tools; **retire `core/export/entity-data.ts`** (Python `behavior_gen.py` +
`ojz_entity_gen.py` become sole authority). Gated on the engine's sequencer + generator
landing (its plan's tasks 1–5).

### 4.8 The playtest loop — become Aether's first outbound client

Suite-designated priority (`SUITE_PLAN_AUDIT_2026-07-01.md` §3.1). Build one small
client module and three features on it:
- **Client**: `src/main/aether/client.ts` — connect to Oracle's socket
  (`$XDG_RUNTIME_DIR/oracle.sock`, NDJSON JSON-RPC 2.0, `initialize` handshake per
  `empyrean/contract/protocol.md`), event subscription, **symbol resolution only**
  (`emulator/lookup_symbol` — never hardcode addresses; they provably drift +$24
  between builds). Status-bar `Aether ◇ connected` indicator (the design contract's
  chrome convention). Consider making it the seed of
  `empyrean/clients/typescript/` so the suite gets its shared TS lib.
- **A1 — live palette→CRAM** (tiny): palette slider drag → `emulator/write_cram`;
  the running game recolors next frame. The suite's designated "one product" demo.
- **A3 — Build & Run** (medium): invoke `aeon/build.sh` (child_process now;
  `build/invoke` when Crucible joins the bus), then `emulator/load_symbols` +
  `emulator/reload_rom` on success; surface build errors in a toast/panel.
- **A2 — play-from-cursor** (small, the crown jewel): keypress warps the running
  game to the map cursor — resolve `Player_1`/`Camera_X`/`Camera_Y` by symbol, write
  positions, let section streaming re-run. Within-act only until the engine grows
  parameterized boot (engine #5 delivers `Game_Entry`, which eventually unlocks
  cross-act).
This phase also builds exactly the plumbing design #8's live preview needs
(`write_memory`/`reload_rom`), which is why it precedes P5.

### 4.9 Import pipeline ★ (new proposal — not previously specced)

Most real art starts life outside Aurora. Add:
- **PNG import** to tile/chunk/sprite-frame documents: nearest-palette-line
  quantization with error reporting (which pixels exceeded the line), optional
  auto-palette suggestion (median-cut to 15 colors + transparent), flip-aware dedup
  into the tileset on accept. Import targets both level art and sprite frames.
- **Sprite-sheet slicing** (grid or auto-bounds) → frames.
- Defer Aseprite native format; PNG covers the pipeline. Defer shadow/highlight
  authoring until an accuracy study (sample ground truth via Oracle per the vision
  doc's research note).

### 4.10 Multi-game levels

> **Substrate landed early (§2.5, 2026-08-09).** The disasm-as-project work built
> and battle-tested the neutral hierarchical `LevelDoc`, the S1 classic codecs
> (Phase D), and the engine-agnostic `ProjectAdapter`/registry against real S1 data,
> and proved **edit-in-place** (superseding this design's original import-only
> assumption for donor disasms). P8 now inherits that layer; the remaining P8 work is
> the *cross-game* adapters (S2/S3K ⇄ each other ⇄ aeon, both directions), collision
> best-match into the shared shape set, object mapping tables, and world assembly.

**Fully designed** — see `specs/2026-07-03-multi-game-level-interop-design.md`:
hub-and-spoke `LevelDoc` neutral model, per-game adapters driven by SonLVL INI
manifests (SonLVL's API source in `programs/SonLVL/SonLVLAPI/` is the definitive
format reference), collision best-match into the S&K shape set (which IS aeon's
vocabulary — S3K maps ~1:1), object placements via editable mapping tables with
marker entities for the unmapped, both directions (donor→aeon AND aeon→classic, plus
cross-classic through the hub), and **world assembly**: import multiple levels — any
mix of games — into one aeon act placed adjacent in the section grid, flowing
continuously in-game via per-section palette/BG/parallax/music. Phased A–E; Phase A
(S2→aeon end-to-end + into-act placement + chunk-borrow mode) is unblocked today.
Sequenced late only because earlier phases raise its value — pull it forward freely
if it's the motivating feature.

### Cross-cutting (all phases)

- **Design tokens**: consume `empyrean/design/tokens.json` (emit CSS custom props via
  `gen:theme`), accent `#34D399`, window title `Aurora — <context>`, command palette
  Ctrl/Cmd-K. Most exists from polish Plan A — close the gaps as the modes multiply.
- **Trust model**: keep loopback-only + Origin/Host checks on every HTTP surface.
- **MCP parity rule**: every new authoring capability ships with its MCP tools in the
  same phase (the ~17 new tools across #6–#9), one undo step per mutation, so agent
  and human editing stay equivalent.
- **New-mode pattern**: every new AppMode follows the established recipe — Zod format
  module in `core/formats/`, store on the shared undo history, viewport component,
  dock, `EDITOR_METHODS` descriptors. Designs #7/#8 name this explicitly.
- **project.json growth**: as screens/parallax/raster/behavior documents appear,
  extend `s4-config.ts` (paths, per-game manifest awareness from engine #5) with Zod
  validation and sane defaults, keeping paths relative to the engine repo root.

## 5. Recommended sequencing

Two things invalidated the original table: §2.5 and §2.6 delivered ~700 commits it never
recorded, and the steering moved to the **classic spine**. Re-sequenced 2026-08-19 around
what is actually being built. **P2 — the playtest loop — is next.**

### 5.1 Open now, in order

| # | Work | Size | Source |
|---|---|---|---|
| 1 | ~~The playtest loop (= P2, §4.8)~~ — **DELIVERED 2026-08-19, aeon-first.** Client core, live palette, play-from-cursor and Build & Run all ship, with the agent surface (`aether_status`, `aether_connect`, `push_palette`, `warp`, `build_and_run`). See §2.7. | M | sweep §7.2, `SUITE_PLAN_AUDIT_2026-07-01.md` §3.1 |
| 2 | ~~Retire the dead export path~~ — **DONE 2026-08-19**, see §4.2 | S | sweep §7.6 |
| 3 | ~~`docs/ART_SUITE.md` teaches a deleted UI~~ — **DONE 2026-08-19**, rewritten, not deleted. Measured section by section first: the mechanics core (presets, brush spaces, tools, flip keys, edit-in-place, save/dedup flows, palette editor, atlas note) was still true against source and is kept; the Toolbar navigation, the "separate classic surface" section and the shared-stack undo claim were false and are replaced with the facet-pill shell, the five classic facets, and per-document undo; paint-through, the origination canvas, resolve-and-commit and the 36-tool agent registry are now covered with spec pointers. | XS | sweep §7.5 |
| 4 | ~~Annotate the 2C spec header~~ — **DONE 2026-08-19.** Re-measured first: `CommitReport.warnings` is fed by exactly two pushes (shared-palette file, engine chunk $51), so none of D2b's three reaches is reported. Header now reads "BUILT — EXCEPT §D2b". | XS | sweep §7.3 |
| 5 | ~~Write three already-settled decisions into the art spec~~ — **DONE 2026-08-19.** All three verified against source, then written into §7 of `2026-08-15-in-app-art-authoring-design.md` (retitled *Settled decisions*) with citations: `ChunkTab.tsx:441`/`BlockTab.tsx:347`, `canvas-file.ts:36`, `use-canvas-constraints.ts` + `canvas-budget.test.ts`. | XS | sweep §7.4 |
| 6 | ~~`ChunkGrid`'s status hint needs 213px in a 157px slot~~ — **DONE 2026-08-19**, and the framing was wrong. Measured under CDP: classic loses 58px, but **aeon loses 158px** (a chunk-NAME badge plus the S/M/L control leave the hint 29px), so no rewrite could have fixed it. The hint now takes its own line. `scratchpad/chunkgrid-hint-harness.mjs`, 20/20, six rows proven red first. | XS | handoff §4 |

**Open (added 2026-08-22, from the effects-arc verification):**

| # | Work | Size | Source |
|---|---|---|---|
| 8 | **The 448-tile BG ceiling has no expression in Aurora.** `BG_TILES_HIGH = 32` / `BG_MAX_TILES = 512` in `src/renderer/agent/agent-handler.ts` still cap it *(cited by SYMBOL: this row read `src/main/mcp/agent-handler.ts:51-52` until 2026-08-22, and that path **does not exist** — `ls src/main/mcp/` is ENOENT — while the line numbers had separately rotted to :56-57. A directory and a line number both went stale in one cite; the constant names did not. Re-verified firsthand: the cap is still 32/512.)*, so the agent path cannot author a 64-row BG — the stage-4 design claimed this "Fixed here"; it was not. The 448 ceiling the BgAnim band invariants depend on is represented **nowhere** in this repo. Ruled 2026-08-22 with the aeon overseer as **NOT in effects wave-1 scope** (wave 1 needs a human authoring surface first; agent parity on bands is a distinct capability). Booked anyway: it is a real invariant with no local expression, and re-rule if agent-authored bands become how the first act's content gets produced. | S | `reviews/2026-08-22-aeon-effects-survey-verification.md` |
| 9 | **No aeon-viewport performance datum exists, and the OJZ showcase runs there.** `MapViewport.tsx` has **zero** `requestAnimationFrame` calls (vs. four in `ClassicLevelViewport.tsx`), and `viewStore.ts:52-56` scopes `playAnimatedArt`/`occludeSprites` to `s1` only — so the play-clock + overlay-pass machinery the effects arc was assumed to ride is **classic-only**. **MEASURED 2026-08-22** (`scratchpad/mapviewport-baseline-harness.mjs`, foreground; 21/23 rows): with the page provably still painting — **1602 rAF ticks against 0 MapViewport repaints** over 5.0s idle, probe still bound, React root alive — **the aeon viewport has no clock**; it repaints only on pan/zoom/overlay/state/edit. **Headroom is ample**: worst configuration (zoom 0.25) p95 **1.000ms = 6.0% of a 16.69ms frame**. **RE-RUN 2026-08-22 after the resolution fix: 37/37, harness landed (`253cb0e`).** The first run's confound was real — `performance.now()` is clamped to 0.1ms — and is now beaten by **amortised batching** (12 repaints per bracket, **0.0167ms effective resolution, 6× finer than the tick**), verified by two self-checks that MEASURE the claim: c1 reads the actual quantum, c2 pushes known 1:2:4 sub-tick workloads through the same machinery and recovers **2.00 / 4.00**. Cross-cell comparison is now valid. Amortised per-repaint medians: zoom 4 **0.0250ms**, zoom 2 / zoom 1 **0.0333ms**, Tile Grid ON **0.0833ms**, zoom 0.25 **0.7167ms ±0.0225** — spread 96.5%, gap 41.5× the resolution. **The arc-relevant number: one extra full-viewport line pass (Tile Grid) costs ~+0.050ms**, the only per-pass evidence available until an effects prototype exists (a 224-line pass is a different shape of work). An unclamped CDP `Performance.getMetrics` cross-check corroborates the ORDERING (0.82ms vs 0.13ms script/repaint for the same two cells) and is reported, never asserted. The fix also repaired a real tripwire bug: `(hi-lo)/lo` fell through its zero branch and announced "0.00% spread" about a set running 0.000→0.800ms. ⚠ **Caveat that must still travel with these numbers:** The bracket excludes React commit, compositor and GPU upload, so these are an **upper** bound on available headroom. **What it does NOT settle:** whether a loop must be ADDED is undecidable from this — nothing in the viewport animates, so "needs none" and "needs one that doesn't exist" predict the identical zero. **RULED 2026-08-22 — `reviews/2026-08-22-preview-posture-ruling.md`** (the measurement licensed only the affordability half; the design half is now closed). The fork was drawn wrongly: **BgAnim bands are not uniformly time-driven.** `aeon/engine/level/bg_anim.emp` gives three drivers — `camera_x` (0), `camera_y` (1), `timer` (2, `Logic_Tick`) — and `camera_x` is the schema default, so **two of three are functions of camera position, not time**. Ruled shape is therefore a fourth option nobody named: **driver-faithful preview** — camera bands ride the existing pan repaints *clocklessly* (`MapViewport.tsx:574` already carries `vpX, vpY` in its draw-effect deps), and **only `timer` bands get a clock**, because `rate_shift` is the one parameter judgeable solely in motion. A wall-clock preview of a camera band would teach the wrong driver model, which "labeled-approximate" does not license. **Ordering: concurrent, not a prerequisite** — the wave-1 design §5's "wave-1 prerequisite" is overdrawn and corrected; no band editor/writer work reads a clock, so the clock is its own parcel with one intra-wave edge (land no later than the band-preview parcel). **Shape:** classic's pattern copied locally — `playAnimatedArt` promoted into `OVERLAY_KEYS_BY_ENGINE.aeon`, **off by default**, `ClassicLevelViewport.tsx:401-431`'s early-return + `t0`-deterministic + step-keyed repaint (~15/s at `rate_shift` 2, not 60); the zero-repaint idle property is **conditioned, not spent**. No shared clock service — §5's "shared play-clock" is aspirational; classic's is component-local. **Overseer's addition (§4 of the ruling): the coordinate derivation is the real correctness risk** — `vpX`/`vpY` are an editor pan, `Camera_X`/`Camera_Y` are engine world px; the band-preview parcel must DERIVE the mapping and test it, or the preview is confidently wrong about phase and rate. **Palette-cycling cost is UNMEASURED** (the stamp stays under `SectionRenderer.ts:13`'s `RECOMPOSE_DIRTY_THRESHOLD = 2000` and takes the per-cell flush path; a palette change takes full-recompose) — booked with aeon as a **wave-2 prerequisite**. | S–M | same |

| 10 | ~~**`resolvePlaneWords` returns a short `edit` array as-is, ignoring its own `length` argument**~~ — **DONE 2026-08-22**, branch `fix/resolve-plane-words-length`. Re-measured first, and the booking under-described it in two ways. (a) The consumers do **not** iterate to `length`: `OverlayRenderer.drawCollisionOverlay` indexes `(cr*2)*SECTION_TILES_WIDE + cc*2` and `MapViewport`'s hover readout indexes `cellRow*SECTION_TILES_WIDE + cellCol`, so the real bound is the **section geometry**, not any array's `.length` — and both call sites were *deriving* `len` from array lengths (`OverlayRenderer.ts:59-61` from plane **A**, then reusing it for plane **B**; `MapViewport.tsx:1546`), which is the drift vector, not just the missing pad. (b) The consequence is not a crash: `undefined & 0x3FF === 0`, so a missing region renders as **air, silently**, while the A/B diff reads `undefined !== word` as "these planes disagree" and outlines every one of those cells. **Fix:** exported `SECTION_PLANE_WORDS` (the same `SECTION_TILES_WIDE * SECTION_TILES_HIGH` expression the consumers index with, so bound and buffer cannot drift); `resolvePlaneWords` now pads a short `edit` to `length` with `AIR_CELL` (its own documented absent-plane fill) and reports every mismatch once via `[COLLISION_PLANE_LENGTH]` on `console.error` — it does **not** throw, deliberately: `render()` draws all sections' grids/rings/objects in one call and the hover readout runs inside a mousemove handler, so a throw would destroy far more than the bad plane. Refusing bad input stays the LOADER's job (`6fc7359`); this is the last line, not the gate. Call sites re-pointed to `SECTION_PLANE_WORDS`: `OverlayRenderer.ts`, `MapViewport.tsx`, and `CollisionPalette.tsx`'s `resetToEngine` (which sized `engineWords` from `engine.length` and would have pushed `newColl: undefined` into an undoable command). **Verified:** `test/collision/collision-cell-resolve.test.ts`, +8 tests, suite **3919 passed / 3 skipped / 0 failed** (was 3911/3, `npm test` → `vitest run`); six proven red on the unfixed body, and the index-space derivation additionally proven by a planted violation (`SECTION_PLANE_WORDS` halved → *"expected 65278 to be less than 32768"*). **STILL OPEN — foreground CDP pass:** the node suite drives `OverlayRenderer` through a recording 2D-context stand-in, so it proves the draw **calls**, not the pixels; and `MapViewport`'s hover readout is React and unreachable from node, so *"a cell past a short plane reports its real shape instead of `air`"* is asserted **nowhere**. **Overseer's landing check (2026-08-22), which narrows that residual to near-nothing:** the change is **behaviour-preserving on every well-formed path**. The old bound's three fallbacks all already evaluated to 65536 — `collisionEdit?.length`, `engineCollision?.length` (the loader allocates exactly `SECTION_TILES_WIDE * SECTION_TILES_HIGH`, `load.ts:258-259`) and `tileGrid.nametable.length` (same geometry) — so the fix replaces three fragile routes to the right number with one derived route to it, and changes behaviour **only** on the malformed path. No allocation change, no live-behaviour risk; the earlier worry that a baseline-less section would start allocating 128KB per repaint is refuted by that third fallback. **And the residual CDP proof has a structural obstacle worth naming rather than chasing:** exercising this end-to-end requires a short plane to reach the renderer, but the loader guard (`6fc7359`) now refuses one at load — so the harness would have to deliberately defeat the guard that makes the path unreachable. Ruled: leave the residual booked, do **not** build that harness; revisit only if a producer outside the loader path ever writes a plane. | S | collattr fix parcel, 2026-08-22 |

| 11 | ~~**Re-point the `parallaxRef` reader to the effects contract**~~ — schema §4's carry (`parallaxRef` → scene id). **GATED on aeon landing its `project.json` edit first**: at aeon `00607dd5`, the act entry still read `"parallax": ".../ojz_default.asm"`, so re-pointing now would target a key that does not exist. aeon has booked that edit as a wave-1 lane item and an explicit prerequisite (aeon `5d0be056`; empyrean half LANDED at `b0d5b00`). **Contract re-pin due:** the sceneRef parcel pinned empyrean `1326ceb`; the schema doc has since moved to `2f3b6fd` (six→thirteen ref-site relabel + the §4 ordering fix). Verified firsthand that `contract/schema/aurora-effects-scene.schema.json` is **byte-unchanged** across that range and the normative `sceneRef` lines only shifted line numbers — so nothing landed is invalidated — but **re-pin the writer-side golden against `2f3b6fd` when the next parcel is cut** (schema §8). ~~re-pin against `2f3b6fd`~~ — **DONE differently, and better, in item 12**: the schema-codec parcel pins the schema JSON's **git blob hash** (`2d7a9fee37d85334103ca1a3e03e1a40466d6d9c`), which is stable across `1326ceb → 2f3b6fd → 069cf59 → c2c81e2` and cannot be invalidated by a doc-only commit. Cite the blob, not a commit, for anything that depends on the wire shape. **UNGATED 2026-08-22 — aeon landed it. Code anchor `98100905`** (class-checked: a merge, `project.json | 2 +-` = the one line, plus three doc syncs; the narrower edit commit is `7bff8488`). aeon's act entry now reads `"sceneRef": null` — explicit null, not an absent key. aeon verified on the merged tree from a clean checkout, all four build shapes byte-matching their pins, and their reader enumeration came back **DEAD** (four read-only Python readers, all naming fields explicitly, no generic iteration; a full `regenerate-level.sh` re-baked byte-identical, which a consumer of the key could not have done). **Aurora's own half LANDED — code anchor `91afb64c`** (`feat(aeon): re-point the act-level ref to the effects contract`), derived with `git log -S'sceneRef?: string | null' -- src/core/config/s4-config.ts` rather than off `git log --oneline` (bar 9b). `Act.sceneRef` / `S4ActConfig.sceneRef` read the act entry's `sceneRef`, absent and `null` collapse, and Aurora never writes the key. Re-confirmed 2026-08-22 against aeon's **pushed** master `0c26bbb2` (`ls-remote`-resolved, not the sibling working tree): `project.json` carries `"sceneRef": null` and no `parallax` key. Vendored-schema drift gate green (5/5).

**DELIVERED 2026-08-22**, branch `fix/act-scene-ref-repoint` (`83bb635` the dead-field deletion, `91afb64` the re-point). `Act.parallaxRef` → **`Act.sceneRef`**, populated from the act entry's `sceneRef` with `?? null` so **null and ABSENT collapse before the model sees them** (§4 gives them one meaning, and absent is the real case for any `project.json` predating aeon's edit — which is also why `S4ActConfig.sceneRef` is declared OPTIONAL where `parallax` was required). Renamed, not re-valued, for the contract's two reasons: no reader can mistake a scene id for a path, and act-level and section-level assignment now share one name and one semantics. The typed lie is closed — the reader was reading a key aeon had deleted, so the field held `undefined` against a `string | null`.

**⚠ NO LINE NUMBERS IN THIS ROW, DELIBERATELY — anchor to the symbol.** The previous revision of this row *corrected* a stale cite by supplying a fresh one, and **the correction went stale inside a single day**: the line it warned about (`s4-types.ts:227`) was `export interface Tileset` when the warning was written and `export interface Palette` by that evening's master — wrong as **two different types in one day** — and a peer overseer re-cited it that afternoon inside an otherwise carefully SHA-pinned message, so the pin certified a coordinate the pinned revision itself refutes. A correction that carries a line number inherits the defect it was correcting. Cite the **symbol** (`Act.sceneRef`, `loadAeonProject`, `buildAeonSavePlan`); let a SHA date the **claim**, never the coordinate. *A path has a time — and so does a line number, but a symbol does not.*

**§(b) — the save-path claim: PROVED, and it was already half-tested.** `buildAeonSavePlan` re-serialises `LoadedS4Config.raw`, the raw parsed document, and only when a pointer retarget actually changed something — so `sceneRef` round-trips untouched and no `parallax` key returns. The pre-existing gate is `test/config/editor-dest-fields.test.ts`, *"carries unmodelled keys through at top, zone and act level"*: `expect(out.zones[0].acts[0].unknownActKey).toEqual([1, 2, 3])`. That covers **unmodelled** keys; `sceneRef` is now modelled-on-read and still never written, which nothing pinned. **+7 tests** in the same file: the reader's three cases plus *"a pre-change project.json (parallax, no sceneRef) gives that same null"* (which fails with a **path** in the message if anything is re-wired to the old key), and on the save side sceneRef-survives / no-parallax-returns, explicit-null-stays-a-PRESENT-key, and the general form — **every act key the save does not own comes back unchanged, derived from the input object rather than a copied literal**. Three planted defects proved them red; the hand-enumerated-save plant failed **6** tests, the three new ones plus three pre-existing baseline/verbatim ones.

**§(c) — the dead `Section.parallaxRef`: DELETED** (`83bb635`). Enumerated **by symbol**, over every constructor/copier/serialiser of a `Section`, not by grepping the field's own name: `createSection` (wrote null) and `cloneSection` (copied it) were the only writers, `cloneSection` the only reader — reading it solely to hand it back to itself. `loadAeonProject` never assigned it; `buildAeonSavePlan`'s meta-sidecar literal enumerates exactly `bgLayoutRef`/`paletteRef`/`sceneRef`; the `SectionMeta` codec has three keys and this is not one; `resizeGrid`/`moveSection` copy with `{ ...sec }` **spreads** (field-agnostic, not enumeration sites at all); `pasteSection` and `SectionGridNav`'s clipboard delegate to `cloneSection`; the history commands assign `sceneRef`/`bgLayoutRef` and `set-sections` stores arrays by reference; `EffectsScenePanel`, `effects-aeon`, `properties-aeon`, `agent-handler` and `debug-hooks` all read `sceneRef`/`bgLayoutRef` and none reads this. It was **not inert clutter but an active trap** — the effects survey wired ruling Q4 to it by mistake, because a per-section `parallaxRef` is exactly what a per-section scene assignment would be named, and the contract had to grow a correction saying "not that one". **What replaced it is the point:** `cloneSection`'s ref coverage was three hand-written lists, which can only catch a drop someone remembered to add a line for. A fourth test now derives the expectation from the source section's own key set. Proven by planting a new `weatherRef` on `Section` that `cloneSection` does not copy: **all three hand-list tests stayed GREEN and only the new one failed.**

**Cross-repo consequence — TAG for the controller.** empyrean's `AURORA_EFFECTS_SCHEMA.md` §4 warns readers away from `Section.parallaxRef` *by name and by line*. That field no longer exists, so the warning is now moot rather than wrong, and both of §4's coordinates into this repo are dead. Worth a ping when the next contract edit is cut; nothing is broken by leaving it.

**Fossil, re-checked at delivery and still dead:** an *untracked* aeon `games/sonic4/data/editor/ojz/act1/export/act_descriptor.asm` carries `dc.l games/sonic4/data/parallax/ojz_default.asm ; act_parallax_config`. Untracked, dated before the export step was retired (§4.2), syntactically invalid as 68000 asm (a bare path as a `dc.l` operand could never assemble), and **`grep -rn act_parallax_config src/` still returns nothing** — confirmed 2026-08-22. Left alone. **But if the export path ever grows an `act_parallax_config` emitter it needs this same re-point** — a site a search for `parallaxRef` would never surface.

**Verified:** suite **4040 passed / 3 skipped / 0 failed** (4032 on master; +8), `npx tsc --noEmit` clean — noting that `tsconfig.json` includes only `src/**/*`, so `test/` fixtures are NOT typechecked and tsc alone is not a completeness proof for anything under `test/`. **Explicitly still open, and out of scope here:** no UI assigns an act-level scene and no consumer of `Act.sceneRef` exists — the field is read from disk, held, and written back unchanged. | S | effects arc wave 1 |

| 12 | ~~**Wave-1 surface 1 — the scene-definition codec**~~ — **DELIVERED**, branch `feat/effects-scene-codec`. Reader/writer/library-load/golden for `games/sonic4/data/editor/effects/<scene_id>.json` in `src/core/formats/effects/` (schema §2/§6/§8; aeon consumer contract §2.1/§2.3 at `00607dd5`). No UI — this parcel is core/format only. **Pin:** the contract schema is vendored byte-identical and held to blob `2d7a9fee37d85334103ca1a3e03e1a40466d6d9c` by `test/formats/effects-schema-drift.test.ts`; the doc half read at `069cf59` (§2 unchanged since `0ea8734`). **Not ajv:** Aurora's core runs in the renderer under `script-src 'self'` (`src/renderer/index.html:6`) and ajv compiles schemas with `new Function`; the subset evaluator refuses any keyword it does not implement, and a coverage gate walks the committed schema to prove the implemented set covers it. **Design:** the codec never enumerates fields — parse hands back the parsed document untouched, serialize reorders keys from the schema's own `properties` order and refuses to drop anything — so §6 hazard 1 is answered structurally rather than by a fourth hostile comment. **Rulings made here:** `layer_mask_raw`/`v_deform_shift_raw` are REJECTED on read (closed schema + consumer's NOT-read list; refusing at read is the loud half of "round-trip what you do not understand, or refuse the file"), and the `deform.own` two-sources guard is **not** enforced — §2.2 assigns it to sigil, so it ships as `advisoryLayerDeformConflicts`, a pure function nothing in the read or write path calls. **Still open:** ~~surface 3 (`project.json` act-level `sceneRef`) is item 11 and stays aeon-gated~~ — surface 3 **DELIVERED 2026-08-22**, see item 11 (reader re-pointed, save proven to round-trip it, no act-level UI); surface 4 (BgAnim `anims`) is a separate parcel; no caller wires `loadEffectsSceneLibrary` into the aeon project load yet — that arrives with the UI parcel. Surface 2 (`sceneRef` in the meta sidecar) landed earlier at master `61d4b80`. **Overseer's landing verification (2026-08-22):** merged and re-verified on the MERGED tree, not branch-side — **3959 passed / 3 skipped / 0 failed**, `tsc` clean (exactly `3919 + 40`, so this parcel and item 10 compose without interference). Three claims checked independently rather than accepted: (a) the `unevaluatedProperties` → `additionalProperties` equivalence is **exact** — all **19** occurrences in the committed file sit beside `{type, properties, required}` with **zero** in-place applicators (`allOf`/`anyOf`/`if`/`dependentSchemas`/`not` counts are all 0; the 12 `$ref`s and 8 `oneOf`s all sit under `properties`, which is not in-place), and the evaluator re-asserts the precondition per node; (b) the CSP blocker is real and decisive — `renderer/index.html:6` is `script-src 'self'` with no `unsafe-eval`, so an ajv validator would have passed every node test and thrown in the running app; (c) `oneOf` is implemented with correct **exactly-one** semantics including the >1 branch, which is the failure mode a hand-rolled evaluator would most plausibly get wrong. **Cross-repo reconciliation, verified at landing:** empyrean's committed `contract/schema/aurora-effects-scene.schema.json`, Aurora's vendored copy and the hash pinned in the drift test are all `2d7a9fee…` and the two files are **byte-identical** on disk. ⚠ **This reconciliation is an OVERSEER RITUAL, not a gate** — the drift test proves only that *our copy is the blob we pinned*; nothing inside this repo can notice empyrean changing the schema, and that is deliberate (a sibling-repo path probe is wrong from a worktree, wrong from a lone clone, and absent from a packaged build, and would degrade to "not found → skip validation"). **Re-run the three-way compare by hand whenever a wave parcel is cut**; last reconciled 2026-08-22 against empyrean `8e55475` — schema JSON **byte-identical, blob `2d7a9fee…`, pin holds**. Note that `8e55475` changed the schema *doc* (§5.1/§5.2/§6) without touching the schema *file*, and the empyrean overseer said so explicitly **because silence from that side is not evidence either way** — this repo's drift test structurally cannot observe theirs. That is the ritual working as designed. **RATIFIED by the contract owner 2026-08-22** (empyrean overseer): both rulings above confirmed as *what the contract requires, not merely permits* — rejecting the raw fields on read is mandated by the closed schema plus §2.1's exclusion (a carrying file fails validation outright), and the unenforced two-sources guard is the normativity split as designed, with the advisory-nothing-calls-it shape called a textbook implementation of it. **Do not re-litigate either without new evidence.** The reconciliation gap is closed from their side too: empyrean `152a30c` (docs-only, 9 lines to their `OVERSEER.md`, class-checked here) makes pinging the Aurora lane a **standing obligation** on any change to the schema file, recording both our blob pin and the no-in-place-applicators precondition so an amendment introducing `allOf`/`$ref` gets coordinated rather than silently breaking the `additionalProperties` equivalence. §8's re-pin remains the deliberate step; that commit names who pulls the trigger. | M | effects arc wave 1 |

| 13 | ~~**Wave-1 scene AUTHORING UI + caller wiring + agent parity**~~ — **DELIVERED 2026-08-22**, branch `feat/effects-scene-ui` (`d017b8f` model+load+save, `5184668` scene-ui+undo command, `17b34e6` the facet, `4977f61` agent parity, `640277a` harness). The codec (item 12) had no caller and no UI; an author can now create, edit, assign and persist a scene. **What shipped:** (a) `S4Project.effectsScenes` + `AeonProjectData.scenes` — the SAME object, aliased not copied, pinned with `toBe`; required rather than optional because the tree has exactly one S4Project constructor and an optional field would let a second one omit it, which reads downstream as "no scenes" and writes nothing. (b) `loadAeonProject` calls `loadEffectsSceneLibrary`; `buildAeonSavePlan` writes one canonicalised JSON per scene. (c) A new **Effects facet** on the `parallax` capability (which already existed in `FACET_CAPABILITIES`, declared ahead exactly for this), order 15 — `mapFacet`, so the act stays in view; tools `['view']`, plane-gated like `palette`. Factor pickers, precision/transition, layer list with `world_y`/`fa`/`fb`, add/remove layer, and the per-section `sceneRef` assignment. (d) `set-section-scene` is its OWN command, not a field on `set-section-bg`: folding them would make changing a background undo a scene assignment made three steps earlier. (e) Agent parity as REGISTRY work — four `EDITOR_METHODS` entries (`list_effects_scenes`, `get_effects_scene`, `set_effects_scene`, `assign_section_scene`), kinds, handler cases; `docs/MCP.md` updated. (f) `set-effects-scene` is ONE command for create/edit/delete, a whole-document swap, so the one-undo-step bar holds by construction however many controls a gesture touched. (g) `scratchpad/effects-scene-harness.mjs`, **RUN: 26/26** under xvfb-run against the real aeon tree. **Design decisions worth not re-litigating:** the wire and the undo command both carry WHOLE DOCUMENTS, never field patches — a patch API needs the field enumeration `core/formats/effects` is built around not having, and `cloneEffectsScene` is `structuredClone` for the same reason. `scene-ui.ts` derives every UI constraint from the vendored schema and its `at()` THROWS naming the path, because a quiet `undefined` renders as an empty dropdown that looks exactly like a panel still loading. `WAVE1_PRECISION_VALUES` is the schema's list with `"line"` filtered out, not a `['cell']` literal — and it is a UI policy, not a validation rule: a document carrying `"line"` still loads, round-trips and saves. **A guard that asserted nothing, caught and fixed:** the save path's first unreadable-file rule was a by-path SKIP, which can never fire — an unparsable file yields no id, so nothing on a load→save aims at its path. The reachable collision is a scene AUTHORED under the broken file's stem (easy, because that scene is invisible in every list), and a skip there would lose the author's work AND leave the file. It now THROWS naming the file; the UI and the agent refuse the id earlier. **Measured, not assumed:** 0 map repaints over 3.0s idle on the Effects facet against 962 rAF ticks — item 9's zero-idle property survives wave 1, as `reviews/2026-08-22-preview-posture-ruling.md` requires; planting an rAF loop in the panel takes that row to 1786 repaints. **Verification:** node suite **4032 passed / 3 skipped / 0 failed**, `tsc` clean; 11 red-first proofs across core, provider, agent and harness, each planted and restored. **Still open, deliberately:** no PREVIEW of scene output (its own parcel); the BgAnim `anims` writer (surface 4); item 11's act-level `sceneRef` (aeon-gated). **Two gaps found while building, NOT closed here:** a scene DELETED in the editor leaves its file on disk (the save plan has no delete channel at all — `bgLibrary` has the same hole), and nothing authored here reaches a ROM, because aeon's `tools/effects_gen.py` still does not exist. | M–L | effects arc wave 1 |

| 14 | **`editor_bg_override.json` has two writers and one of them destroys the other's work — parked 2026-08-22 — and the PARKED-FOR-THE-OWNER label was wrong; see the correction at the end of this row.** Found 2026-08-22 while weighing BgAnim bands (arc surface 4) as the next cut. `aeon/tools/png_to_bg_override.py` is a **whole-file overwrite that never reads the file**: `out = {"layout": …, "tiles": …}` is a fresh dict at `:175` (plus `palette`/`palette_line` conditionally), then `open(OVERRIDE, "w")` + `json.dump` at `:179-180` — and the token `OVERRIDE` appears **exactly twice in the entire file**, the path constant at `:44` and that write. Verified by reading around the cited lines, not by trusting them (bar 17). **So the moment Aurora writes `anims`, the next `png_to_bg_override.py` run silently deletes it** — no merge, no warning — and `anims` is the expensive key (`phases` = 8 banks × `cols*rows` tiles × 64 pixel values). An author could lose a hand-authored band set to a routine BG re-import and only discover it when the bands stopped reaching the ROM. **Same defect class as bar 12's silent ref erasure**, which is why the ownership question is downstream of this rather than the reverse. This is the per-key-ownership fork that was named as a design obligation when the arc was greenlit, deliberately left unanswered, with **last-writer-wins explicitly rejected**; both overseers have refused to settle it unilaterally. Decision agent dispatched with the mechanism as grounding. **RULED 2026-08-22 — `reviews/2026-08-22-bg-override-ownership-ruling.md`; the fork is CLOSED and this row's own sequencing call is REVERSED.** Three findings overturned the framing, all re-verified firsthand: (i) **the collision already fired.** `anims` existed at aeon `b76576ea`/`b0e5a661` and is gone at `dd93a840` (2026-07-21) — the commit that introduced `png_to_bg_override.py` — destroying two authored bands (192 animated slots of 340 tiles); `bg_anim.emp` has read `BgAnim_Table: u16 = 0 (disabled)` for **one month** and nobody noticed, because the `--stat` shows one line on minified JSON. *No act authors bands BECAUSE the collision fired* — the prior premise inverted cause and effect. (ii) **Three writers, not two** — `forest_bg_gen.py:420-431` dumps the same path with `anims` and no read, destroying `palette` in the other direction; bar 12 again, the enumeration counted format-definers and missed a data-toucher. (iii) **`anims`/`tiles`/`layout` are not separable** — bands pack from slot 0 and `tiles[0:n] == phases[0]` (measured True for both bands), so animated slots are a **prefix** of `tiles` and adding/removing a band renumbers the whole blob. **Ruled:** ownership is **per-document, not per-key**; Aurora is sole writer of record; `png_to_bg_override.py` is reclassified writer→**importer/seeder** that refuses loudly on keys it does not produce, gains `--out`, and asserts retained `palette` against `GEN_PALETTE`; `forest_bg_gen.py` gets the same refusal or is deleted (art judgement → owner). **The overseer's "refusal is a floor that becomes merge in one edit" was WRONG** — a merge would pass every consumer assert and ship silent visual corruption, strictly worse than the deletion it replaces. And the fork was **already answered in schema §6 hazard 1** (refuse-or-round-trip binds *every* wave-1 writer, aeon's included); three sessions failed to cite it. **SEQUENCING REVERSED — BgAnim is unblocked and cuttable now**, since nothing Aurora writes is wrong absent the aeon fix; exposure is to a third party running an aeon tool afterward. **Hard gate:** the refusal must be on aeon master *before the first commit carrying a non-empty `anims` lands there*. **DISCHARGED 2026-08-22 — aeon `bd31e133`**, `--stat`-verified here as a genuine code merge (727 insertions): a shared `tools/bg_override_io.py`, BOTH writers converted (`png_to_bg_override.py` gains `--out`, `forest_bg_gen.py` keeps `BG_OUT`), `inject_editor_bg.py`, a 295-line `tools/test_bg_override_no_clobber.py`, and TOOL-01 in `docs/BUGS.md`. **BgAnim (wave-1 surface 4) is UNBLOCKED.** **⚠ CORRECTION 2026-08-22 (audit prompted by the empyrean lane): this row said PARKED FOR THE OWNER, and the thing that closed it was not an owner act.** The ownership fork was settled by a **dispatched decision agent** — the ruling doc says so in its own header, and says the fork had been parked precisely because *two overseers each refused to settle it unilaterally*. What makes the outcome defensible is not the agent: it is that every load-bearing fact was re-verified firsthand before banking, and that aeon then **discharged it in code** at `bd31e133`, `--stat`-verified here as a genuine 727-insertion merge. A decision ratified by implementation across the fence is stronger than one merely asserted; it is still **not** the owner's, and this row must not read as though it were. **The half that genuinely IS the owner's was never closed — the ruling reserves it explicitly (*whether that art loss was intended is the owner's call, not one to assume*) — and it is now item 01 on the owner's decision board, merged with the tile-famine question per item 24.** Never record an approval whose granting act you have not seen.

⚠ **The gate's own vacuity, raised by the empyrean overseer and RETIRED here rather than at first-band time.** Their caveat was accurate: the shipped `editor_bg_override.json` carries only `layout`+`tiles` (verified — `anims` absent, `bg_anim.emp` still `BgAnim_Table: u16 = 0`), so every refusal check runs against *fixtures*, and the first band Aurora authors would have been the first time the gate fired on live data. That is a real "first green is newly meaningful" exposure, and it does not need to wait for a band editor to exist. `scratchpad/bg-override-live-shape-refusal-probe.py` closes it now: it copies **today's shipped file** (448 tiles, 4096 layout entries — not the 340-tile `b0e5a661` fixture), adds one real band at `b0e5a661`'s band-0 geometry (`32×4, camera_x, rate_shift 2, slot_base 0`, 8 phases), and runs the tool through the acceptance suite's own `tool.OVERRIDE` monkeypatch. **Result: refusal fires, names `anims` in its exit message, and leaves the file byte-intact** — and the *paired control*, the same live file with no `anims`, is **accepted (exit 0)**, so the refusal is attributable to the key and not to the tool disliking the shipped shape. The aeon tree is byte-identical afterwards (asserted, not assumed). What this does NOT retire: the composition with Aurora's own writer output, which cannot be tested until surface 4 exists — a first-band green is still newly meaningful about *Aurora's* bytes, just no longer about the gate. Superseded call below, kept for the record: ~~**BgAnim is NOT the next cut.**~~ aeon's overseer is right that it is the shorter road on the engine side — its consumer `inject_editor_bg.py` ships, unlike `effects_gen.py` — but it is the road that crosses this parked call, and the scene road does not. The fix, whatever shape it takes, is aeon's to land: Aurora builds in that tree but never lands there. Note it is genuinely arguable that "regenerate the BG from source art" *should* be authoritative over the whole file — in which case the answer is a **loud refusal** on unknown keys, not a silent clobber. **EXTENDED 2026-08-22 by the aeon overseer, verified firsthand here — it is worse, and it is ALREADY FIRING with no Aurora writer involved.** Read `:175-178` as a unit, not `:175` alone: `palette`/`palette_line` are written **conditionally** (`if stamp_palette:`), and `inject_editor_bg.py:206-215` **consumes them** (`if 'palette' in data:` → 16 CRAM words asserted → mapped to a line → stamped into `ojz_palette.bin`, whose own comment says the colours revert without it). So run the tool in EXTRACT+stamp mode, then again in lock mode to re-import art, and **the stamped BG palette is silently gone** — one tool forgetting its OWN key, across its own two documented modes, on a live consumer path, in a workflow those modes make natural. Confirmed the only `.read()` in the tool is `GEN_PALETTE` at `:75`. **This kills the "regenerate from source art should be authoritative over the whole file" defense** that was booked above as arguable: the tool is not authoritative over `palette`, it just forgets it, and authority you cannot exercise consistently across your own invocations is not authority — it is an unread file. Ruled a bug on the aeon side **independent of the ownership question**, and being fixed now with the line drawn deliberately: (i) the tool's OWN keys survive its own modes — uncontroversial, resolves nothing about ownership; (ii) unknown keys including `anims` get a **LOUD REFUSAL**, explicitly not read-merge-write, because *a tool that silently preserves unknown keys has in effect ruled the file shared and multi-writer-safe — one of the two answers to the fork, adopted by implementation rather than by decision.* Refusal takes neither side, converts silent destruction into a visible stop, and leaves both "shared file" and "split the files" equally cheap. **The refusal is the FLOOR, not a decision taken**; if the ruling comes back shared-ownership, refusal becomes merge in one edit. Decision agent re-dispatched (Fable limit hit) and sent this mechanism as a grounding correction. | S–M (aeon) | Aurora overseer, 2026-08-22 |

| 15 | ~~**Effects panel polish + a harness evidence gap**~~ — **DELIVERED 2026-08-22**, branch `fix/effects-panel-polish` (`5713f21` the two panel fixes, `ab72d58` the 13 new CDP rows, `940bb6b` this row — **the row originally cited `6777ef5` for itself, which is an ORPHAN**: a pre-rebase copy of the same content, still in the object store so it resolves cleanly, but `git merge-base --is-ancestor 6777ef5 master` says NO. Caught at landing. This is today's anchor precedent in its purest form — *locally every SHA resolves and `git log` looks finished* — and the self-referential case is the one nobody re-checks, because a row citing itself reads as bookkeeping rather than as a claim.). Harness **39/39** (was 26/26), node suite **unchanged at 4032 passed / 3 skipped**, `tsc` clean, and the aeon tree hashes identically before and after the run. **(a) FIXED — and derived, not eyeballed.** The shell had exactly ONE horizontal dock inset, the `T.s4` typed into `PanelHeader`'s padding, and nothing else could reach it — which is precisely why a panel that renders its own body content had nothing to line up with. It is now `PANEL_INSET` in `ui/primitives`, `PanelHeader` reads it, and a new opt-in `SectionBody` applies it to a section's body; `EffectsScenePanel` wraps all four of its sections in one. Deliberately NOT folded into `CollapsibleSection`: `CollisionPalette`, `RingPatternPalette`, `ArtBrowser` and `PropertiesPanel` already pad themselves, so a section-applied inset would double up on every one of them. Red-first on master, all four flagged controls at rightInset 0: `new_scene_id L+0 R+45.2 | Name L+72 R+0 | V factor L+72 R+0 | Section select L+72 R+0 (header inset 8/8)`; green after at `L+8 R+8` on the three fields and `L+8` on the id input. The harness row reads the header's COMPUTED padding rather than a literal, so the two cannot drift apart. **(b) NOT OURS — ATTRIBUTED, AND THE CODE LEFT ALONE.** The red `soli…` badge is the generic object box `canvas/OverlayRenderer.drawObjects` draws for a placement with no sprite preview (fill `rgba(255,100,100,.7)`, stroke `#ff4444`, `obj.typeId` centred in 8px monospace, `canvas-colors.ts:80-82`), and aeon's `ojz/act1/section_0.objects.json` holds exactly one placement: `typeId: "solid"` at world (808,210). Measured on the map canvas's own pixels on the Effects facet AND on Layout: the SAME 16x16 cluster at canvas `[800,202,815,217]`, at the SAME screen x 1084..1100, on both. What differs is the canvas — **816px wide on Effects vs 876px on Layout, because the Effects dock is 300px and every other map facet's is 240px** — so clearance from the badge to the canvas's own right edge is **60px on Layout and 0px on Effects**. And the label is genuinely wider than its badge: `"solid"` measures **19.9998px in the app's own 2D context inside a 16px box**, so it overflows by ~2px each side at any window size. The Effects facet does not cause it and does not expose anything a pan would not; it merely has a narrower canvas, and this act's one object happens to sit at its edge. **⚠ BOOKING (not this parcel's):** `OverlayRenderer.drawObjects` draws `obj.typeId` untruncated and unclipped centred on a fixed 16px box — any typeId over ~4 characters spills, and any object near a viewport edge shows a half glyph. Reproduces on every map facet at every window size; needs a measured-and-elided label (or a box sized to its text). **(c) EVIDENCE PRODUCED — and it uncovered a real defect on the way.** The harness now drives the app into the expanded-layers-with-packed-spinners state at two layers AND at the schema maximum of eight, proves the section is genuinely expanded and the forms genuinely open before it shoots (rows 11a/11b/11d — a collapsed section photographs calm and asserts nothing, which is exactly what item 13's shots did), and captures both. **THE MEASURED DENSITY, reported and NOT judged:** a fully expanded layer card with both packed-factor forms open is **154px**. The Layers section's share of a 774px dock is **306px**, so **exactly two cards fit**; at 2/8 the stack is 308px (18px beyond the window), at 8/8 it is **1232px, 966px of it beyond**. Whether that is acceptable is the owner's call — this is the evidence for making it. **The defect found doing it, and FIXED here:** both of this panel's `variant="list"` sections declared the variant and never supplied the scroller the variant's own model requires (`ui/CollapsibleSection`: a list "takes an equal share … and scrolls inside it"), so with `overflow: visible` **954px of layer cards were painted straight over the SECTION ASSIGNMENT rows** — overlapping text, photographed on master, pre-existing and not introduced by (a). One style prop per list section. **⚠ BOOKING (why no node test caught it):** `panel-scrollers.test.ts` and `panel-headings.test.ts` derive their panel list by walking `<CollapsibleSection><Child` inside **facet modules**, and `effects-facet.tsx` mounts `<EffectsScenePanel />` straight under `<Panel>` — so **all four of this panel's titled sections, both list variants included, are invisible to both guards**. Any future panel that groups its own sections escapes the same way. The derivation in `components/__tests__/helpers/section-panels.ts` needs to walk sections declared inside panels, not only inside facets. | XS–S | overseer landing pass, item 13 |

| 16 | ~~**`s1-io` zero-edit round-trip times out under CPU contention — a flaky gate, reported honestly rather than buried.**~~ — **DONE 2026-08-22**, branch `fix/s1-io-roundtrip-timeout`. **Fixed structurally, not by raising a number: the 18-act loop is now 18 `it.each` cases and the 5000ms default is untouched.** The booking's other option (a raised timeout) was measured and **rejected**: the overage is unbounded by anything this repo controls — the same act took **1653–1900ms at ~2× oversubscription, 11349–12993ms at ~8–10×, and 976ms idle** — so any constant either still flakes at the next load level or is large enough that a genuine 10× regression on the repo's heaviest round trip passes silently. That trades a flaky gate for a blind one. **The derivation for the split:** per-act cost measured across all 18 acts (idle, load ~5.4) is **40.0ms (mz act1) – 71.0ms (ghz act1)**, summing to 986ms; the spread tracks tile and block counts and is genuine, not a confound (`chunks=82` IS identical for all 18 — every `map256/*.kos` decompresses to exactly 41984 B, a fixed-size S1 table, verified). So headroom against the SAME 5000ms goes from **5.1×** for the merged loop to **70–125×** per case — a ~14× gain in contention tolerance, derived, with no invented constant. **Reproduced both states before and after:** master fails with the literal `Error: Test timed out in 5000ms.` at `s1-io.test.ts:228:25` under ~10× oversubscription; the split passed 8/8 at load 128–137 (slowest case 160–693ms, and the slowest act *varies*, so it is contention and not one heavy act). ⚠ **Residual, stated rather than hidden:** one case hit **4815ms at load ~158** (≈10× oversubscription) in 16 branch runs, not reproduced in 8 further runs — the split raises the threshold by ~an order of magnitude, it does not make a wall-clock budget immune at arbitrary starvation. **Also closed a silent-gate hole the split exposed:** master's version iterated `result.files` with no non-vacuity check, so a writer that silently dropped a whole domain made it report **green** (proven: gating `dirty.colind` off left `(c)` passing at 976ms). Each case now asserts the emitted path set equals the set derived from `ResolvedLevelPaths`, and a coverage gate asserts all 18 bodies ran. **Suite count +18** (18 act cases + 1 coverage gate − 1 merged loop) — **4289 → 4307 passed, 3 skipped both sides**, measured against base `a2712a0`; master moved twice mid-parcel, so the delta is the stable figure and the absolutes are pinned to that base. Three red-first plants, all restored. **Independently reproduced on master by the overseer** at a stated load — see the caveat in that report about whose load it was. Original booking: Found by the item-11 parcel, which saw a single `1 failed | 4039 passed` run that did not reproduce in 11 subsequent runs. Rather than shrug it off as noise it hunted the cause under **2× CPU oversubscription** and caught it: `src/core/level-classic/__tests__/s1-io.test.ts › s1-io (c) zero-edit round-trip (all 18 acts) › re-encodes every domain identically` — `Error: Test timed out in 5000ms`. **A wall-clock timeout, not an assertion failure**, on an 18-act ROM round trip. **Proven pre-existing, not introduced**: the parcel put master's content in the working tree and reproduced the identical timeout there, 2 of 3 starved runs, at master's own 4031/3 count; the branch touches nothing under `src/core/level-classic/`. ~~Not reproduced independently by the overseer~~ (**discharged 2026-08-22** — reproduced on master by the overseer and again by the fix parcel) — recorded as the parcel's finding, and it is the *method* that earns the booking (reproduce-before-concluding on something it would have been cheaper to call noise). **Why it matters beyond tidiness:** a 5s budget on the repo's heaviest round trip means the suite's verdict depends on machine load, and this workspace routinely runs several agent worktrees at once — so a red run here is ambiguous between a real regression and a busy box, which is exactly the ambiguity that trains people to re-run until green. Fix is a raised/removed timeout on that test specifically, or splitting the 18 acts. | XS | item-11 parcel, 2026-08-22 | **DELIVERED 2026-08-22**, branch `fix/s1-io-roundtrip-timeout` — **split, not a raised timeout**, and the measurement says the other road is not merely cheaper but actively harmful. Measured across three regimes on the same content: idle (load ~5.4) **976 ms**; ~2× oversubscription (load 21–26) **1653–1900 ms**; ~8–10× (load 42–157) **11349–12993 ms, timed out**. **There is no constant to derive** — a value safe at load 150 is ≥15 s, and at 15 s a genuine 10× regression on the repo's heaviest round trip passes silently, trading a flaky gate for a blind one. The split moves *work per budget* instead: per-act cost is **40.0 ms (mz act1) – 71.0 ms (ghz act1)** against the **untouched** 5000 ms default, so headroom goes **5.1× → 70–125×** with no invented number anywhere. **A SILENT GATE WAS FOUND WHILE SPLITTING, and it is worse than the flake it was dispatched for:** master's `(c)` iterated `result.files` with **no assertion that the set was the right one**, so with `dirty.colind` gated off **master passed green at 976 ms with a whole domain silently dropped** — proven, not theorised. Each case now asserts the emitted path set equals the set **derived from `ResolvedLevelPaths`**, plus a coverage gate that all 18 bodies ran, counted at the *top* of the body so a genuine act failure shows as the failing acts rather than a false 'act dropped'. **Overseer verification on the merged tree:** planted an *extra* emitted file (the superset direction the parcel did not test) — all 18 acts red by name. **⚠ That plant does NOT discriminate**, and the overseer says so rather than counting it: master's version catches it too, incidentally, because it `readDisk`s the bogus path and the bytes mismatch. **The discriminating evidence is the parcel's own dropped-`colind` plant**, where master is green. **CORRECTION TO THIS ROW'S OWN CORROBORATION:** the overseer's independent full-suite reproduction (12,893 ms at load 147) was reported as evidence that three agent worktrees reach that load. The parcel refuted it: **the load was the parcel's own instrument** — 128 userspace spinners running 16:48–16:52 — and the overseer's figure sits between the parcel's two same-window master measurements (11,349 and 12,993 ms). So the run **does** independently reproduce the defect on master, and it is **not** independent of the instrument, and it is **not** evidence about three worktrees. The overseer had inferred the cause of the load rather than observing it. **Residual, stated not hidden:** one case hit **4815 ms at load ~158** in 16 branch runs, not reproduced in 8 further runs at load 128–137. The split raises the contention threshold by about an order of magnitude; it does not make a wall-clock budget immune at arbitrary starvation, and if this box routinely reaches load 150 that is a separate booking. `(b) read() succeeds on every act` remains a merged 18-act loop (43 ms, 116× headroom) — deliberately untouched.

| 17 | ~~**Object labels are drawn untruncated into a fixed 16px box — every map facet, every window size.**~~ — **DELIVERED 2026-08-22**, branch `fix/object-label-elision` (`d84ec53` the fix + 37 node rows, `d85a76e` the budget correction, `c1b7d73` the CDP harness). Node suite **4242 passed / 3 skipped** (master baseline 4204 / 3), `tsc` clean, CDP harness **23/23** and **15/23 red on master** — every instrument and anti-vacuous row green there, exactly the eight claim rows red. **(a) THE BOOKING IS CONFIRMED TO THE DIGIT, re-measured from scratch in the app's own 2D context:** `"solid"` is **19.999771118164062px** in a **16px** box (`scratchpad/label-measure-probe.mjs`). Two things the booking did not have. This build's `monospace` advance is **exactly half the em** across a 4x size range (0.4999943 / 0.5000001 / 0.5000001 at 8/16/32px), which is *where* the 20px comes from; and the **ellipsis costs TWO cells** (7.9998932 / 3.9999542 = 2.0000) because U+2026 is not in the mono face and the fallback is double-width — so any budget arithmetic that counted characters would have been wrong here, which is why the fix measures the candidate instead. **(b) THE ROW NAMED ONE SITE; THERE WERE THREE, and the one it did not name is worse.** `classic-overlays`' ghost marker labels its box with an object NAME: `"Conveyor Belt Controller"` ($68) is **96px of text through a 24px box**, a 4x overflow, and its hex-fallback sibling overflows too (two digits are 16px in a 16px box at zoom 0.5, double it at 0.25). Fixed together through one measured helper (`canvas/label-fit.ts`); a fix to only the booked site would have left the larger overflow one file away. **(c) THE DESIGN CALLS, argued not defaulted.** *Elide with an ellipsis, never a bare prefix* — `"sol"` is a plausible typeId, so a silent cut is a lie the author cannot detect; the marker costs two cells on a 16px box and that is the price of honesty. *Suppress entirely* when not even one character plus the marker fits — the alternatives are a bare `…` (says nothing the box did not) or a half glyph (the defect) — and the criterion is MEASUREMENT, not a second hand-picked size threshold. *No clip region*: elision bounds the label inside its own box by construction, so a per-object `save/clip/restore` would only mask an arithmetic error at a real per-repaint cost; an object at the CANVAS edge still shows a half box, exactly as a half tile, half sprite and half ring already do. *The aeon label is now SCREEN-sized* (`8 / zoom` world px, the convention classic already used): under the old world-space 8px the fit was zoom-INVARIANT, so an id too wide at zoom 1 was too wide at every zoom and nothing could ever reveal it — now zoom is the affordance (3 cells at zoom 1 => `s…`, 7 at zoom 2 where the whole `solid` reads), and at zoom 1 the drawn glyph size is unchanged. **(d) THE BUDGET WAS WRONG ONCE, and the app said so.** The first cut kept half a pixel of breathing room between glyph and frame. The classic viewport opens **fit-to-window — 0.5796875** on an 856px canvas — where `"0E"` is 13.8 world px in a 16 world px box whose 1-screen-px border reaches 0.86 in: it clears the frame, and the politeness deleted it. The budget is now `boxWidth - strokeWidth` and `labelBudget` lost its `gap` parameter rather than taking a zero. It costs nothing at zoom 1 — every marker fits the same number of cells either way. **(e) BOTH CONFIGURATIONS HOLD, and they are the point.** Layout (876px canvas, **60px** clearance) and Effects (816px canvas, **0px**, because that dock is 300px where every other map facet's is 240px): same marker, same canvas bbox `[800,202,815,217]`, and on both the object overlay's painted span is inside the 18px ceiling a 16px fill plus a half-pixel-overhanging 1px border may light up. **⚠ The Effects row does NOT discriminate on its own** — run against master it reads 18 and PASSES, because the canvas' right edge at 816 clips the right half of the overflow before the diff can see it (master bbox ends at 815). That is item 15's finding restated as a measurement, and it is why the discriminating row is the **Layout** one: master 20px span, fixed 18px. **(f) HOW IT IS VERIFIED, both halves.** Node (+38): `label-fit.test.ts` is the arithmetic with expectations derived from the measured metric (`src/test/mono-measure.ts`, raw readings in its header) and the box geometry IMPORTED from the draw sites; `object-label-draw.test.ts` drives the REAL draw functions through a recording context, because a perfectly-tested helper nobody calls reproduces the defect exactly. Eight red-first perturbations of the SUBJECT: raw typeId restored (4 red), raw ghost name (3), raw hex (1), ellipsis dropped (11), the `w > max` refactor that accepts NaN (1), an exclusive budget (1), a search that stops one short (1), the world-space font restored (3). CDP (`scratchpad/object-label-harness.mjs`, 23 rows): a `fillText`/`fillRect` prototype spy reads the exact string and font the overlay hands the rasteriser, `measureText` measures it in the app's own context, and a **pixel diff of the map canvas with the object overlay toggled through the real View-menu checkbox** bounds its ink without guessing which pixels are art. Anti-vacuous named per row: the fixture placement is still there (1a), the canvas repainted while the spy was live (2a), the checkbox was found and really flipped (5b), the diff has non-zero ink before it is bounded (5c), the object probe reads real data — 82 ring placements — before a zero ghost count is believed (C2a), and every suppression row also asserts the BOX was drawn. **(g) FOUND ON THE WAY.** `P5` proved the early `Number.isFinite(full)` guard was indistinguishable from its own absence (NaN fails `<=` unaided), so it is gone; the in-loop check stays because the equivalent-looking `if (w > maxWidth) hi = mid - 1; else accept` DOES accept NaN. And the first classic scan reported "no invisible/trigger object in ANY act" — that was the INSTRUMENT (`__dbg.listObjects` does not exist; the probe is `__dbg.classic.listObjects`, and a `.catch(() => [])` swallowed it). GHZ1 holds three $49 Waterfall Sound Effects, which is what C3–C6 measure: `"Wat…"` at 20.47px in a 23px budget, where master drew the whole 88.00px name. The control row that catches that class now runs first. | XS–S | item-15 parcel, 2026-08-22 |

| 18 | ~~**Two panel guards derive their subject list from FACET modules, so any panel that groups its own sections is invisible to them.**~~ — **DELIVERED 2026-08-22**, branch `fix/panel-guard-enumeration` (`900a8e6` the two guards + the shared derivation, `ab5f68a` the third guard nobody had counted; **this row cites no SHA for itself** — item 15's orphan self-citation is the standing precedent, and a row cannot name its own commit before that commit exists without the number being invented). Suite **4116 passed / 3 skipped** (**4040** before — the row first said 4032, transcribed from item 11's snapshot of a master that had since moved; measured on the merged tree at landing, delta **+76**), `tsc` clean. **THE COUNT, which was the parcel and not the fix.** Enumerated by the PRIMITIVE and its consumers — every `.tsx` under `src/renderer` holding a `<CollapsibleSection>` call site — because walking facet modules is the exact frame that failed. **Section call sites 25 → 43. Files under the rules 22 → 39. Files DECLARING a section 8 → 14**, and the six new ones are `EffectsScenePanel` (4 sections), `SpriteMode` (7), `S1ObjectSection` (2), `CanvasMode` (3), `Explorer` (1), `ProjectSetupTab` (1) — 18 sections, **every one reachable in the app** (App.tsx mounts all four surfaces; S1ObjectSection is inside SpriteMode). **Three separate things hid them, and the directory was only one:** (a) `<CollapsibleSection([^>]*)>` stops at the first `>` in the source, and two of the effects panel's four carry `right={<IconButton icon={<span>Delete</span>}…}` — **those two were unreadable even to a scan aimed at the right file**; the opening tag is now found by balancing braces. (b) Comments are now blanked, quote-aware: EffectsScenePanel's own docblock quotes `<CollapsibleSection><Child` while describing this bug, and a raw scan enrolls that sentence as a fifth, id-less section. (c) A section whose body is inline JSX has no child panel file to resolve, so it is checked through the declaring file's own per-section style objects. **PER-SECTION, NOT PER-FILE — the distinction is load-bearing and cost a rewrite.** The first widening flagged six panels; **five were false**, each the surface's OWN container (Explorer's tree scroller, the setup tab's page scroller, SpriteMode's and CanvasMode's canvas wrappers, `s1-facets`' canvas fill) — all correct, all outside every section in their file. Buying coverage with false positives is the tripwire-reason failure in another costume, so an owner file is now checked through its sections' bodies and only a panel mounted INSIDE a section is checked whole. The sixth was `<SectionBody style={LIST_BODY}>`, which is genuinely column-bounded because the wrapper carries the `minHeight: 0`: read off `ui/primitives` by `shrinkableWrappers()` rather than exempted by name, and **asserted** — a SectionBody that stops declaring it fails the file loudly rather than silently exempting nothing. **NEW DERIVED RULE, the one the 954px defect actually broke:** a `variant="list"` section must supply the scroller its variant promises (`ui/CollapsibleSection`'s own docblock: "takes an equal share … **and scrolls inside it**"). Enrolled by the call site's own declaration, so there is no list to forget something in. **RED-FIRST, each plant grepped to its call site first:** `<SectionBody style={LIST_BODY}>` → `<SectionBody>` at both list sections (the pre-`5713f21` shape) — new guards **FAIL 2** ("aeon.effects.scenes declares variant="list" — a share of the column — but nothing in it scrolls"), **old guards PASS 65/65**; heading type planted in EffectsScenePanel — new FAIL 1, old PASS 27/27; an uncapped `overflowY` in the assign section's body — new FAIL 1 on that section alone, old PASS 38/38; `minHeight: 0` removed from SectionBody — the file fails loudly. **NEWLY CAUGHT IN APP CODE: nothing.** All four new surfaces sit in a `<Panel scroll>` or their own scroll container and **none declares `variant="list"`**, so no section there promises a share it does not deliver — a checked fact now, not an unexamined one. **⚠ THE RESIDUAL BLIND SPOT, named rather than implied:** the scan is textual and keys on the literal tag, so a section rendered via `React.createElement`, re-exported under another name, or generated at runtime is still invisible; a style object assembled by a helper or spread in from another module is not resolved; and for a list section the scroller may be found anywhere in a reached panel file rather than on the path that actually renders. `deriveSections()` carries a tripwire for the specific way this regressed (every call site back inside `workspace/facets`), not for those. **And the instrument's own reach:** none of this sees rendered geometry — the 954px overhang is only observable under CDP (`scratchpad/effects-scene-harness.mjs`). The node guards check the DECLARATION the geometry depended on, which is the strongest form available in node; a geometric assertion belongs in the harness. Original booking:  The defect item 15 actually found — 954px of layer cards painting over the SECTION ASSIGNMENT rows — was invisible to all ~4,000 node tests, and this is why. `panel-scrollers.test.ts` and `panel-headings.test.ts` build their panel list by walking `<CollapsibleSection><Child` **inside facet modules**; `effects-facet.tsx` mounts `<EffectsScenePanel />` straight under `<Panel>`, so all four of that panel's titled sections — both `variant="list"` ones included — are enumerated by neither. **This is review bar 12 pointed at a test's own subject derivation**: the guards enumerate by what *defines* a panel (the facet module) rather than by what *is* one, so a panel escapes simply by being composed one level deeper. The two sections here are the known instances; the class is "every panel that groups its own sections", which nobody has counted. **Counting it is the parcel** — the fix is worthless without knowing what else was never guarded. | S | item-15 parcel, 2026-08-22 |

| 19 | ~~**The four non-facet section columns have never been under the flex-column model, and nobody has measured whether they need to be.**~~ — **MEASURED AND RATIFIED 2026-08-22: LEAVE ALL FOUR ALONE.** The recommendation rests on the measurements below, which are in-repo and were re-run. The row also records that **the owner ran the harness and confirmed it on its own numbers** — reported by the session that wrote the sentence (`a0c0e59`), leaving no repo artifact, so a later session CANNOT verify it. Flagged rather than deleted or re-asserted, and it changes nothing either way, because **the load-bearing authority here is the measurement, not the approval**: `S9.r4` measured every other section at **1202px** against a **528px** column, a **-674px** share against the 160px floor, so the floor engages and the flex model is a no-op exactly where it would matter. A **second run at two genuinely different viewports** strengthened it past what the source argument alone could claim: **every surface overflows at BOTH sizes** — window heights needed to lose the scrollbar are ProjectSetupTab **~6326px**, SpriteMode-9 **~2071px**, SpriteMode-7 **~1780px**, CanvasMode **~1306px**, SpriteMode-6 **~1201px** — so the floor engages across the whole range these columns are ever seen at, not merely at today's size. (Stated precisely: that is every display in common use; only a maximised 4K panel would clear the bottom four, and ProjectSetupTab clears at no size at all.) **The two runs also found FOUR HARNESS DEFECTS, all fixed here, and the row taxonomy is re-stated twice. (1) `SCREEN` sized the xvfb DISPLAY, not the Electron window** — both invocations produced 1400x872, so `--compare` would have called run-to-run noise a size property and the `PLANT=list-no-scroller` split could never occur. The six `i4` rows caught it and failed loudly, which is the anti-vacuous discipline working. SCREEN now drives the PAGE VIEWPORT, set via `Browser.setWindowBounds` then `Emulation.setDeviceMetricsOverride`, **verified by reading `window.innerHeight` back**, and **BLOCKED** on the size axis if neither takes; new row **`V.set`** catches it once at setup instead of as six scattered failures, `--compare` now REFUSES when both summaries share a viewport height, and an emulated size axis is disclosed as emulated. **(2) `C.i0` and `C.c4` share ONE cause and it is not nested scroll.** `NEW_CANVAS_DEFAULTS` has no default NAME on purpose (`new-canvas.ts:77` — Create is disabled until the artist types one) and the harness set only width/height, so **no canvas was ever created** and the block measured SpriteMode's column a second time under CanvasMode's name. Independently of that diagnosis: **there is no `overflow: auto` anywhere inside CanvasMode's three sections** (grep of `components/canvas`: only `canvasWrap`, outside the Panel, and the import dialog; `PaletteGrid.tsx:173` documents it), so **when CanvasMode is genuinely on screen `C.c4` MUST be NOT MEASURED — a red `C.c4` is proof the surface was not CanvasMode.** The two are NOT independent and the CanvasMode recommendation is unchanged. **(3) THE WORST ONE, invisible from the run and mine: `expect.titles` was read OFF THE SCREEN and then asserted AGAINST the screen** — a `C.i1` that compared the subject to itself, could not go red for any reason, and reported green against SpriteMode's titles while CanvasMode was nowhere on screen. The dominant defect class in this repo, one level inside the instrument built to catch it. Fixed three ways: titles are now LITERALS transcribed from the call sites; every surface carries a **STORE-level sentinel (`i0`)** no leftover paint can satisfy; and **THE GATE** — if any instrument row fails, every claim row and report for that surface is NOT MEASURED, never red, never green, so the owner's inference about `C.c4` is enforced rather than reconstructed. Also: `C.setup2`/`C.setup3` catch the dialog defect at its own site, `c4` now reports the armed element, wheel-event count and cancellation and spaces its ticks 400ms apart (Chromium latches a scroll sequence to the element the first event hit — this row's most likely false red, disclosed), and `armInnerScroller` arms the TALLEST overflowing scroller so two configurations of one column cannot disagree by accident. **(4) `PLANT=list-no-scroller` COULD NOT FAIL AT ANY SIZE, and the reason was that `c1` was aimed at the wrong geometry.** The plant strips inner scrollers and `c4` judges inner-scroller behaviour, so the plant deleted its own judge and both sizes reported a clean 65/65 with six honest NOT-MEASURED notes underneath — the "passes for the wrong reason" shape wearing a disclosure as cover. But the deeper fault was mine: **`c1` compared section BORDER BOXES, and the 954px shape it exists for does not move them** — a section sized by the column paints its children below its own box while the box stays where flexbox put it. So c1 was blind to the one defect it was written for. `c1` now compares **PAINTED EXTENT** (`contentBottom`, stopping at any box that clips, since descending into a capped list would report every inner list in the app as an overlap), and a border-box overlap is reported separately as `c1box`. Plus two run-level invariants: **`P.invariant`** — a run with PLANT set and NO red row is a FAILURE whatever the disclosure says, which protects every future plant — and **`P.invariant2`** — the plant's NAMED judge (`clip`->c2, `nested`->c3, `contain`->c4, `list-no-scroller`->c1) is what must go red, since another row failing does not make a plant coverage for the property it names. The plant is **re-aimed, not retired**. **RE-STATED DISCRIMINATING ROWS:** `V.set`, `c1` (list-no-scroller — now a functioning judge), `c2` (clip), `c3` (nested), `c4` (contain — and NOT MEASURED by source-derived necessity on CanvasMode/Explorer/setup), **`c5` EARNED BACK** (V.set passes at two real viewports, so it compares cross-size data rather than a run against itself), `S6.doc`/`S7.seven`/`S9.nine`, `i0`/`i0b`/`i1`, `C.setup2`/`C.setup3`, `P.invariant`/`P.invariant2`. **`c1` still cannot go red UNPLANTED** (every section is `flexShrink: 0` in an `overflow: auto` box). **PREDICTION WITHDRAWN:** the earlier "red at 1280x800, possibly green at 1680x1050" split does not exist — free space is negative on every one of these columns at BOTH viewports, so the plant is expected red at both. The flexbox reasoning was right; the premise that these columns ever have slack was wrong. Branch `measure/non-facet-section-columns`; full record in `docs/reviews/2026-08-22-non-facet-section-columns.md`. **NO PRODUCT SOURCE CHANGED** — suite **4242 passed / 3 skipped**, unchanged from the master baseline at `a82d3db`; `tsc` clean. **RECOMMENDATION: LEAVE ALL FOUR ALONE**, on three lines of evidence, the first of which is decidable without launching anything. **(1) The model's own floor makes the refactor a no-op exactly where it would matter.** `LIST_SECTION` grants `container.clientHeight` minus every other section's natural height and stops shrinking at `SECTION_LIST_MIN_HEIGHT = 160`; on an over-subscribed column that share is below the floor by construction, the floor engages, and `CollapsibleSection.tsx:87` hands the deficit straight back to `Panel`'s scrollbar — *"the pre-existing behaviour of an over-subscribed column, restored for the case that actually is one."* Which is what these columns do now. **(2) Two of the four are not columns.** `ProjectSetupTab` is an 860px centred document page with a pinned Apply footer outside its scroller; `Explorer` is a file tree whose groups are `defaultCollapsed` and whose filter force-expands them — per-group scrollbars in a 240px rail is a different and worse tree, and item 18 already ruled that scroller a false positive on the same reasoning. **(3) The unbounded-list worry does not hold where the booking put it.** Every data-driven body in SpriteMode is capped in its own source AT AUTHORSHIP, before the model existed: `scanList` `maxHeight: 220` + `.slice(0, 200)` (`ef6c613`, 2026-06-17), `S1ObjectSection`'s two row lists `maxHeight: 240` (`5e0d493`, 2026-08-12). **THE BOOKING'S THREE ASKS, ANSWERED.** **(a) The "six" is not stale — it is UNQUALIFIED.** SpriteMode has THREE counts because its two gates are independent (`:49` reads `projectStore.project`, `:54` the classic store; neither goes through `state/open-project.ts`): 4 always + 2 with an aeon project = **6**, + 3 with classic = **7** or **9**. Six is correct for the aeon-only session, and was correct the day the docblock was written — SpriteMode.tsx already had seven call sites at `5399202`. The repair is one clause, not a new number. **(b) REFUTED for SpriteMode, and relocated.** The two genuinely uncapped data-driven bodies are elsewhere: `Explorer`'s `styles.items` (~102 rows — `S1_OBJECT_LIST`, 102 named ids — the largest content section in the app, bounded only by `treeScroll`) and `CanvasMode`'s `canvas.commit` (`CommitPlanView.tsx:128`, one row per 256x256 chunk, capped at 16 only by arithmetic — `CANVAS_MAX_SIDE = 1024`). Both argued correct at their sites. **(c) THE BOOKING'S PREMISE ON THE GUARDS IS STALE.** Item 18 already enrolled all four: `deriveSections()` reports **43 sections / 14 owners / 25 panels**, including all 14 content sections across the five declaring files. And the coverage is real, not nominal — **RED-FIRST, both plants made, run and reverted**: dropping `maxHeight: 220` from SpriteMode's `scanList` fails `panel-scrollers` **1/94** on `sprite.open`; adding an uncapped `overflowY` to Explorer's `styles.items` fails **1/94** on `explorer.${g.id}`; baseline 140/140. What the guards correctly do NOT cover: `UNBOUNDED_BY_NATURE` (adding `Explorer.tsx` would assert it must be `variant="list"`, which is the thing this parcel concludes it must not be), the list-scroller rule (vacuous — none declares `list`), and anything geometric. **No guard change recommended.** **ONE DATUM THAT REFRAMES THE ESCAPE HATCH: all 15 `<Panel>` mounts in the renderer declare `scroll`** — every facet column, both engines, the effects panel and both non-facet ones. The container scrollbar is not a fallback these four took while the facet columns did better; it is what every column declares. **THE HARNESS: `scratchpad/section-column-harness.mjs`**, committed, controller-run (`VITE_AURORA_DEBUG=1 npm run build`, then `SCREEN=1680x1050` and `SCREEN=1280x800`, then `--compare`). It enumerates sections STRUCTURALLY — `PanelHeader` is rendered by `CollapsibleSection` and by nothing else in the renderer, so its computed signature is one-to-one with call sites — bounds every measurement to one x-range (the Explorer is persistent; an unbounded scan would measure it four times and report four green columns), and drives all three SpriteMode configurations including **aeon-first-then-classic**, which is how nine is reached: `open-project.ts` records that a classic open LEAVES a resident aeon project, and `useProjectStore.reset()` has no app-code caller. **WHICH ROWS DISCRIMINATE, STATED UP FRONT:** `c2` (reachability, red under `PLANT=clip`), `c3` (<=2 scrollbars deep, red under `PLANT=nested`), `c4` (the wheel chains out of an exhausted inner list — the only place nested-scroll confusion is observable — red under `PLANT=contain`), `c5` (cross-size: the natural stack height must be a CONTENT property, NOT MEASURED from one run), and the count rows `S6.doc`/`S7.seven`/`S9.nine`. **WHICH DOES NOT: `c1`.** "No section paints over the one below it" CANNOT go red unplanted here — every section is `flexShrink: 0` in an `overflow: auto` box, so overlap is structurally impossible. It is the regression tripwire for the shape that DID ship (the effects panel's 954px) and is red only under `PLANT=list-no-scroller`, which itself is expected red at 1280x800 and possibly GREEN at 1680x1050 because flexbox only squeezes when free space is negative — that split is a finding, not a fault. **STILL OPEN, and NOT a layout defect:** in an aeon-then-classic session SpriteMode renders "Export to project" and "Load engine character" against a resident aeon project the user has navigated away from. `state/open-project.ts` exists to be the one answer to "which engine is open" and its docblock records that four sites once derived it independently and two disagreed; `SpriteMode.tsx:49`/`:54` is a fifth. Whether that state is a feature or an accident is a product call nobody has made — **book it separately, do not fold it into a layout decision.** **NO FIFTH SURFACE.** Counted by what MOUNTS a section (bar 12): 43 call sites, 15 Panel mounts, no aliasing (`ui/index.ts:5` is a plain re-export; no `React.createElement` form). The one non-facet owner the booking omits is `EffectsScenePanel`, correctly — two of its four are `variant="list"`, so it fails the booking's own predicate. **WHAT REMAINS UNMEASURABLE UNTIL THE HARNESS IS RUN:** every per-surface px number (finding (1) above is arithmetic from source constants and does not depend on it); whether nested scrolling is CONFUSING as opposed to DEEP; and whether nine sections in 240px is the right information architecture at all — this measures the layout MODEL, not the IA. Original booking:  **The four non-facet section columns have never been under the flex-column model, and nobody has measured whether they need to be.** Surfaced by item 18's count. `SpriteMode` mounts **nine** `CollapsibleSection`s in one `<Panel width={240} scroll>`, `CanvasMode` three, and `Explorer` / `ProjectSetupTab` declare one per data group in their own scroll containers. **Every one of them is `variant="content"`** — the default — so `LIST_SECTION`'s share-of-the-column machinery never engages on any of these surfaces and each degrades to a container scrollbar. That is coherent and is exactly what `Panel`'s own docblock calls the escape hatch, so **nothing here is asserted to be broken**; what is missing is the measurement. Three concrete things to settle: (a) the docblock says "SpriteMode mounts six" and it mounts nine — stale, and the number was the evidence for the posture; (b) `sprite.open`'s project-scan result list and Explorer's group lists ARE data-driven, and a data-driven list in a content section is the shape `UNBOUNDED_BY_NATURE` exists to forbid in a facet column — it is only fine here because the container scrolls, which is an argument nobody has written down; (c) whether a 240px column with nine sections is usable at all is a CDP question, not a source one. **Needs a foreground harness** (`scratchpad/*-harness.mjs`); the node guards deliberately do not judge it. | S | item-18 count, 2026-08-22 | **OVERSEER VERIFICATION — the harness was run in the foreground across three rounds, and each round found a defect the previous one hid.** (i) `SCREEN` sized the xvfb display, not the Electron window: 1400×872 at *both* settings, so the cross-size row `c5` was comparing a run against itself. The harness's own `i4` instrument rows caught it and failed loudly rather than letting geometry rows report numbers from a window nobody asked for. Fixed via `Browser.setWindowBounds` + `Emulation.setDeviceMetricsOverride`, **each verified by reading `innerHeight` back** — nothing trusts a CDP call that merely returned. `V.set` now confirms it once at setup: **PASS at both 1680×1050 and 1280×800.** (ii) The parcel's own catch, and the worst of the three: `C.i1` **read the expected titles off the screen and asserted them against the screen** — it could not go red for any reason, and reported green against the wrong surface entirely. The dominant defect class in this repo, found one level inside the instrument built to catch it. (iii) The overseer's catch: `PLANT=list-no-scroller` reported **65/65 green with the poison installed**, because stripping inner scrollers marks the `c4` rows NOT MEASURED — the plant deleted its own judge. **The repair went deeper than asked:** `c4` was never that plant's judge, `c1` was, and `c1` compared **border-box** positions when the 954px defect paints *children* below a box that never moves — so `c1` was **non-functional**, not merely non-discriminating as §7 had disclosed. Now compares painted extent, descending for the lowest painted pixel and **stopping at any clipping box** (descending into an `overflow:auto` list would red-flag every capped list in the app — the opposite false verdict). **Re-verified after the fix: `PLANT=list-no-scroller` goes RED at both viewports via 4 `c1` rows, with `P.invariant` (a plant is installed so something MUST be red) and `P.invariant2` (the plant's NAMED judge is what went red) both firing.** An unknown `PLANT=` value is refused loudly, so a typo cannot install nothing and pass. **⚠ NOT verified: `P.invariant`'s own RED path** — all four plants now work, so the overseer could not construct a plant-with-no-red to make it fire, and says so rather than counting it. **The conclusion is RATIFIED and better evidenced than at first return:** every surface overflows at both viewports — window heights needed to lose the scrollbar are ProjectSetupTab **~6326px**, S9 ~2071px, S7 ~1780px, CanvasMode ~1306px, S6 ~1201px. **Correction to the overseer's own overstatement**, made by the parcel: *"no window size a human owns"* holds outright for ProjectSetupTab and for the whole set at 1080p and 1440p, but a maximised 4K panel (~2160px) would clear the bottom four. The honest form is decisive without the overshoot. **The parcel also withdrew its own predicted cross-size split** — free space is negative on every column at both viewports, so there is no slack to find and the plant is correctly red at both; the flexbox reasoning was right and the premise that these columns ever have room to spare was wrong.

| 20 | ~~**Wave-1 surface 4, part 1 — the `editor_bg_override.json` codec**~~ — **DELIVERED 2026-08-22**, branch `feat/bganim-override-codec` (`19481e2` the vendored contract + fixture, `170ba1a` the codec, `24df448` the empty-`anims` ruling). Read/write/validate in `src/core/formats/bg-override/`; **no UI, no editing command, no caller — nothing imports it yet**, deliberately, as the scene codec (item 12) was cut the same way. Suite **4204 passed / 3 skipped** on the merged tree (4116 before; +88), `tsc` clean. **Every constant is read out of a vendored contract JSON** whose values came from aeon SOURCE with the authority cited inline, through a loud accessor that throws naming the path — nothing hand-pinned. `BGANIM_MAX_BANDS` derived from **all three** of its authorities (`engine/system/constants.emp` sizing `ram.emp`'s `BgAnim_LastStep`, `bg_anim.emp`'s deliberate module-local mirror, and `inject_editor_bg.py`'s emitter cap), the `rows*32` power-of-two from the emitter's own `col_shift` derivation rather than restated. **THE POSTURE INVERTS AEON'S, and that is the parcel's real content:** aeon's tools *refuse* unowned keys because each owns a slice; a **sole writer of record cannot refuse its own file**, so empyrean schema §6 hazard 1 (*round-trip what you do not understand, or refuse the file*) resolves to the **other half of the pair** — `palette`, `palette_line` and every unknown key, at top level **and inside a band**, round-trip untouched **and deliberately UNVALIDATED**, because judging a key you do not own means refusing a file its owner considers fine over a constraint the drift rule lets aeon change without telling us. Pinned by a test that plants a *deliberately illegal* palette and asserts it passes through unjudged. The round-trip is a **loop over `Object.keys`**, not an enumeration, and `cloneBgOverride` is `structuredClone` — so bar 12 has no surface here, checked by grepping the **type** (`BgOverrideDocument`/`BgOverrideBand` appear only in the codec and its tests; there is no second copier to miss). **PREFIX IDENTITY MEASURED, NOT ASSUMED:** on the two real bands at aeon `b0e5a661`, `phases[0] == tiles[slot_base:slot_base+cols*rows]` is **TRUE for both** (`tiles[0:128]`, `tiles[128:192]`) — 192 animated slots sitting **inside** a 340-entry array, which is simultaneously the counterexample to the superseded `tiles + animated <= 448` budget rule, since it scores a real shipped document at 532. Fixture vendored byte-identical from blob `33892d82c95d61a9214cb449fa7c67f683247ad3`, **re-hashed at landing against `b0e5a661:games/sonic4/data/editor_bg_override.json`** rather than trusted from the report. **Empty `anims` — RULED at review, and the delivery was changed:** the parcel refused it in both directions; refusing on *read* makes Aurora unable to open a document the consumer accepts, leaving the user hand-editing JSON, which is the outcome sole-writer ownership exists to prevent. Now **unauthored, not unreadable** — dropped on read with a notice, refused on write (aeon's `test_bg_emit.py` asserts `assertNotIn("anims", live)`, so the no-bands document has *no key*), and refused outright only **beside a legacy `anim`**, where the consumer's `if anims is None` fallback does not fire and the band would be silently dropped. Verified by the overseer as **behaviour**, independent of the parcel's own tests: benign-reads-with-notice / what-opens-is-saveable / raw-write-still-refused / carve-out-refuses / no-key-parses-silently / unowned-keys-survive, 6/6. **14 red-first plants total**, each grepped for site uniqueness first — which mattered: the empty-`anims` condition occurs **twice** after the ruling, and a plant on the bare condition would have hit the wrong branch and looked convincing. **Five places the contract doc and `inject_editor_bg.py` disagree, found by reading the consumer rather than the doc:** tile length is unenforced (a long tile is **silently truncated** to 64 values, a short one raises `IndexError`); pixel range is **masked, not checked**, so a `16` bakes silently as `0`; §1.1's "absent/empty → disabled stub" contradicts aeon's own `test_bg_emit.py`; an empty `anims` beside a legacy `anim` silently drops the band; and two §1.1 line cites went stale when `bd31e133` landed. **⚠ STILL OPEN, and it is the honest limit:** no writer has yet produced `anims` bytes that aeon's gate has judged — **composition is unproven**, and proving it needs a real save + `python3 tools/inject_editor_bg.py` + a build, which is foreground work. **MOSTLY CLOSED 2026-08-22 by the overseer in the foreground** (`scratchpad/bganim-writer-vs-aeon-gate.{py,emit.ts}`): Aurora's `serializeBgOverride` output on the real 2-band `b0e5a661` document is **ACCEPTED by aeon's entire `inject_editor_bg.main()`** — the whole injection path, not one function — loaded from their file at a revision resolved by `ls-remote` rather than restated or read out of their working tree. It emits **`BgAnim_Table: u16 = 2`**, i.e. real bands rather than the disabled stub the ROM has carried for a month; an anti-vacuous row fails the accept if the table comes back 0, because a disabled stub succeeds just as quietly. **Stronger still: all four generated artifacts (`bg_anim.emp`, `bg_anim_banks.bin`, `bg_tiles.bin`, `zone_bg.bin`) are BYTE-IDENTICAL** whether the input came from Aurora's writer or from aeon's own file — under a **determinism control** (the same input run twice must digest identically) without which the byte-identity claim would be measuring nondeterminism rather than transparency. **The first six of aeon's band asserts were reached by code-reading first and it nearly produced a wrong booking:** a grep for `col_shift` returned 0 hits in Aurora's codec and I almost recorded the power-of-two constraint as unenforced — it is enforced at `bg-override.ts:370-375` under the name `colBytes`, citing the consumer's assert verbatim. Running the real path instead of grepping for it is what settled all six. The gate **discriminates** — a prefix-identity poison is REJECTED naming band 0 — and the fixture's untouched bytes run as a control, so a failure would have been attributable to Aurora's writer rather than to the document. Note **Aurora's writer independently refuses the same poison**, so the poison had to bypass `serializeBgOverride` to reach aeon's gate unshielded; that refusal is a second, separate green. **What remains open is the harder half and it is stated in the script's own docstring: this is a ROUND TRIP, not an ORIGINATION.** The bands come from aeon-generated content that already satisfied the invariant, so a band *composed by the add-band command* is still unjudged. And `validate_band_coherence` is ONE function — no bank emit, no palette stamp, no build, no ROM. Next parts: the band model + the single undoable add/remove command (bands renumber the whole blob), then the band editor UI + agent parity + a CDP harness. | M | effects arc wave 1, surface 4 |

| 21 | **The scene/raster road is complete on Aurora's side and stops at aeon's fence — PARKED WITH THE OWNER as a SCHEDULING call, not an engineering one.** Established 2026-08-22 with the aeon overseer, every claim verified firsthand against their **pushed** master (`ls-remote`, never the sibling working tree). **Verified-at anchors, since the revision you checked at is an anchor too:** first established at aeon `1ee8f8e6`, **re-verified at `b16ec612`** after their master moved — `effects_gen.py` still absent, `7bff8488` still reachable. The intervening commit is theirs widening the staleness hazard to its real input set, i.e. the correction below landing on their side. Aurora ships the whole authoring half: scene codec (item 12), per-section `sceneRef` sidecar, scene authoring UI + caller wiring + agent parity (item 13), act-level `parallaxRef`→`sceneRef` repoint (item 11). **A user can author and save a scene today and nothing carries it into the ROM**, because `tools/effects_gen.py` — the baker turning Aurora's neutral JSON into generated `.emp` — does not exist on aeon master and has no branch in flight (they enumerated local branches and counted `master..<branch>` for each, deliberately, having read a *merged* branch's empty diff as an empty branch earlier the same day; the four genuinely-ahead branches are unrelated). **Aurora owes them nothing for it** — their wave-1 design §7 puts `effects_gen.py`, the per-act binding module, the `act_descriptor.emp` seam and the `project.json` re-point on their side. **The answer is better than "blocked on P5" sounds: it is UNSCHEDULED, not blocked** — no missing dependency, no open design question, no sigil involvement. Its one ordering prerequisite already landed at aeon `7bff8488`, **class-checked here** (`project.json | 2 +-` — a data-file anchor for a data-file claim, which is the shape that has been wrong before and this time is not) and confirmed reachable from their pushed master, with the remote blob showing `sceneRef` present and `parallax` absent; P5 now *consumes* that key rather than creating it. The honest blocker is their queue: an owner hold, with a probe migration and two doc fixes ahead. **They declined to quietly reorder, which was correct** — this is the owner's call and reordering another lane's queue is not Aurora's to ask for. **SUPERSEDED BY EVENTS, same day — aeon STARTED P5 unprompted while this row was being written.** At aeon `251a94ec` three slices are on their master: `ce10277e` slice 1 (scene discovery + SHAPE validation), `5f18b5a8` slice 2 (render the `scene()`/`layer()` call text), `251a94ec` slice 3 (the real attachment spellings, plus two defects slices 1–2 shipped). **So the owner's scheduling call was not needed** and this row should not be read as still asking for one — the honest record is that the question was live for about an hour and their queue resolved it. The remaining gate on the scene road is P5 *finishing*, not P5 *starting*. Note their validation posture is deliberately SHAPE-only (schema version, id, unknown keys) with authored VALUES left to sigil's constructor `ensure`s, so Aurora should not expect value diagnostics from that tool. **The prior recommendation, kept for the record since it was made and the reasoning still holds if a similar call recurs: jump it.** The scene road is the larger prize, its inputs are ready, and it is the only thing between a finished Aurora authoring surface and the ROM; the BgAnim road (items 20, 22) proceeds in parallel either way and is independent of this decision. **⚠ UNPARKED 2026-08-22 — THE FENCE IS OPEN. An Aurora-authored scene can now reach a ROM.** aeon landed the binding seam at **`e0fe9499`**, verified here firsthand and against their **remote** (`ls-remote` == `e0fe9499`, `--is-ancestor` of `origin/master`) and **class-checked as a genuine code merge**: `tools/effects_gen.py` +497, a new `tools/effects_seam_gate.py` (181), `act_descriptor.emp`, `build.sh` gates, `test_effects_gen.py` +249. **The joint I checked rather than accepted** — because a conditional import would leave the reachability trap live at zero content — is that the import edge is **unconditional**: `act_descriptor.emp:55` is a bare `use games.sonic4.ojz_effects_editor_act1.{…}` with no guard, and the block above it states the owner's Q-c ruling (always-emitted default binding). One path, always live, at zero content or full. **The scheduling question this row parked is therefore MOOT** — it asked whether to wait on aeon's fence, and the fence opened. **The trap aeon measured on this exact seam, worth carrying because it will present as our bug:** an unreached `.emp` module gets **zero body elaboration**, so every guard inside it builds green while asserting nothing — with the descriptor's `use` removed, an `ensure(1 == 0)` built clean **with an unchanged CRC**. If an authored scene ever *builds fine and does not appear*, look at the seam gate first, not the generator. **And their byte-identical CRCs are the correct ZERO-CONTENT shape, not evidence the build skipped** — the gate reports `Count=0, Bindings=0, 9 section call sites, 696 equates`; this is the byte-neutral-CRC bar, where a matching CRC cannot witness that anything ran. **Does NOT change the tile famine (item 24):** the seam is about scenes; band insertion stays impossible until an art-side pass. aeon's `band_reserve` (`93c436ec`, `vram.toml`, default 0) binds the NEXT import and **creates no space in the current blob**, so promotion-first remains the working route. **Their two self-corrections, recorded so this repo does not carry the wrong version:** the historical band shape was **not** 192-of-448-leaving-256 — each band's `phases[0]` **is** `tiles[slot_base:slot_base+n]`, so animated slots are a **subset** of the blob and never an addition; the real split at `b0e5a661` was 192 animated + 148 purely static + **108 never spent**. And aeon's tool tests have been wired since 2026-08-16. **Aurora's remaining deliverable is item 31.** | — (aeon) | Aurora overseer, 2026-08-22 |

| 22 | **Aurora's writer and aeon's `json.dump` disagree on JSON separators, so every alternation rewrites 100% of `editor_bg_override.json` — invisibly.** Measured 2026-08-22 by the item-20 composition probe (`86d9fb1`), as a side effect rather than as its object. Identical semantics (`a == c` is True), **not byte-identical**: Aurora emits **282,867 B** where aeon's `json.dump` emits **407,055 B** for the same document — 30% smaller, both a single line, differing only in the `,`/`:` separators (python defaults to `", "`/`": "`). **Not a correctness bug** and not asserted to be one. What makes it worth booking is the interaction with a hazard this arc already ruled on: the ownership ruling established that `--stat` reads `editor_bg_override.json | 2 +-` on this file *because the JSON is minified*, which is how two authored bands were destroyed for a month with nobody noticing. Format churn means **the first Aurora save rewrites the entire file and still shows as one line**, and so does every subsequent alternation between the two writers. The review discipline that failed once — reading `--stat` and seeing a one-line change — fails identically here, now on a file with a sole writer of record whose whole justification is that the previous arrangement destroyed data quietly. **RESOLVED 2026-08-22, same day, and the option I called most likely was REFUTED by the aeon overseer.** I had offered three readings and flagged the third — *correctly ignored, because a sole writer means no alternation happens* — as the probable answer. It is wrong: **`tools/bg_override_io.py` writes this file via `json.dumps(obj).encode()`**, Python's default separators, which is the other half of the 407,055 vs 282,867 B delta. The document has **two writers with different serializations today**, so alternation is not hypothetical and the sole-writer-of-record ruling governs *authority over content*, not *serialization*. Two distinct things that the phrase 'sole writer' silently merges. **Ruled: canonicalize, don't match.** Matching separators fixes the churn that happened to be measured and leaves **key order** waiting to produce the identical surprise later; a *determined* serialization is the property worth having (agreed separators + `sort_keys`), after which any diff at all is semantic. Pretty-printing was considered and **dropped on their scale argument** — a hundred-thousand-line diff across tile arrays is the same unreviewability in a different costume, and canonical-and-minified already delivers the property that matters: a diff appears only when something semantic changed, which is the actual defense against the month-long silent death. **aeon carries it** into `tools/EFFECTS_CONSUMER_CONTRACT.md` as a serialization clause and changes `bg_override_io.py`; **Aurora adopts on its own schedule** — it costs us a writer-golden re-pin (small, ours, gates nobody), and `sort_keys` reorders the unknown keys we round-trip untouched, which stays semantically faithful but should be said out loud in the posture note so a future reader does not read reordering as editing. **Transition caveat, raised by this lane and the one part that is not about the destination:** adopting canonical form churns the whole file **once** per side, and that one-time churn has exactly the shape of the hazard the clause exists to prevent. **Land the reserialization as its own commit that changes nothing else**, on both sides, with a message saying the diff is format-only — otherwise the first thing the new clause does is hide a real edit. **ADOPTED 2026-08-22 — see item 25**, which discharges this row. Two things in the summary above were superseded by aeon's own amendment before Aurora landed it: the clause is **recursive** (nested band and layer objects sort too, not just the top level), and pretty-print was **not** dropped outright — it was ruled per document class, so the scene files pretty-print and only the tile-array documents minify. The transition caveat this lane raised became contract text, along with a fixture exception this lane raised unprompted. | XS | overseer, 2026-08-22 |
| 20 | ~~**Wave-1 surface 4, part 1 — the `editor_bg_override.json` codec**~~ — **DELIVERED 2026-08-22**, branch `feat/bganim-override-codec` (`19481e2` the vendored contract + fixture, `170ba1a` the codec, `24df448` the empty-`anims` ruling). Read/write/validate in `src/core/formats/bg-override/`; **no UI, no editing command, no caller — nothing imports it yet**, deliberately, as the scene codec (item 12) was cut the same way. Suite **4204 passed / 3 skipped** on the merged tree (4116 before; +88), `tsc` clean. **Every constant is read out of a vendored contract JSON** whose values came from aeon SOURCE with the authority cited inline, through a loud accessor that throws naming the path — nothing hand-pinned. `BGANIM_MAX_BANDS` derived from **all three** of its authorities (`engine/system/constants.emp` sizing `ram.emp`'s `BgAnim_LastStep`, `bg_anim.emp`'s deliberate module-local mirror, and `inject_editor_bg.py`'s emitter cap), the `rows*32` power-of-two from the emitter's own `col_shift` derivation rather than restated. **THE POSTURE INVERTS AEON'S, and that is the parcel's real content:** aeon's tools *refuse* unowned keys because each owns a slice; a **sole writer of record cannot refuse its own file**, so empyrean schema §6 hazard 1 (*round-trip what you do not understand, or refuse the file*) resolves to the **other half of the pair** — `palette`, `palette_line` and every unknown key, at top level **and inside a band**, round-trip untouched **and deliberately UNVALIDATED**, because judging a key you do not own means refusing a file its owner considers fine over a constraint the drift rule lets aeon change without telling us. Pinned by a test that plants a *deliberately illegal* palette and asserts it passes through unjudged. The round-trip is a **loop over `Object.keys`**, not an enumeration, and `cloneBgOverride` is `structuredClone` — so bar 12 has no surface here, checked by grepping the **type** (`BgOverrideDocument`/`BgOverrideBand` appear only in the codec and its tests; there is no second copier to miss). **PREFIX IDENTITY MEASURED, NOT ASSUMED:** on the two real bands at aeon `b0e5a661`, `phases[0] == tiles[slot_base:slot_base+cols*rows]` is **TRUE for both** (`tiles[0:128]`, `tiles[128:192]`) — 192 animated slots sitting **inside** a 340-entry array, which is simultaneously the counterexample to the superseded `tiles + animated <= 448` budget rule, since it scores a real shipped document at 532. Fixture vendored byte-identical from blob `33892d82c95d61a9214cb449fa7c67f683247ad3`, **re-hashed at landing against `b0e5a661:games/sonic4/data/editor_bg_override.json`** rather than trusted from the report. **Empty `anims` — RULED at review, and the delivery was changed:** the parcel refused it in both directions; refusing on *read* makes Aurora unable to open a document the consumer accepts, leaving the user hand-editing JSON, which is the outcome sole-writer ownership exists to prevent. Now **unauthored, not unreadable** — dropped on read with a notice, refused on write (aeon's `test_bg_emit.py` asserts `assertNotIn("anims", live)`, so the no-bands document has *no key*), and refused outright only **beside a legacy `anim`**, where the consumer's `if anims is None` fallback does not fire and the band would be silently dropped. Verified by the overseer as **behaviour**, independent of the parcel's own tests: benign-reads-with-notice / what-opens-is-saveable / raw-write-still-refused / carve-out-refuses / no-key-parses-silently / unowned-keys-survive, 6/6. **14 red-first plants total**, each grepped for site uniqueness first — which mattered: the empty-`anims` condition occurs **twice** after the ruling, and a plant on the bare condition would have hit the wrong branch and looked convincing. **Five places the contract doc and `inject_editor_bg.py` disagree, found by reading the consumer rather than the doc:** tile length is unenforced (a long tile is **silently truncated** to 64 values, a short one raises `IndexError`); pixel range is **masked, not checked**, so a `16` bakes silently as `0`; §1.1's "absent/empty → disabled stub" contradicts aeon's own `test_bg_emit.py`; an empty `anims` beside a legacy `anim` silently drops the band; and two §1.1 line cites went stale when `bd31e133` landed. **⚠ STILL OPEN, and it is the honest limit:** no writer has yet produced `anims` bytes that aeon's gate has judged — **composition is unproven**, and proving it needs a real save + `python3 tools/inject_editor_bg.py` + a build, which is foreground work. Next parts: the band model + the single undoable add/remove command (bands renumber the whole blob), then the band editor UI + agent parity + a CDP harness. | M | effects arc wave 1, surface 4 |
| 23 | ~~**Wave-1 surface 4, part 2 — the BgAnim band model + the one undoable add/remove-band command**~~ — **DELIVERED 2026-08-22**, branch `feat/bganim-band-command` (`0e66a79` the contract extension, `d20c94d` the model + command, `5aa5c35` a vacuity fix). `src/core/formats/bg-override/bg-anim-band.ts` (model, plan, insert/remove), `src/core/editing/bg-override-band.ts` (the command builders), `SetBgOverrideBandCommand` + `S4Level.bgOverride` + both `history.ts` branches. Suite **4289 passed / 3 skipped** (4242 before; +47), `tsc` clean. **Still no UI and no caller** — part 3. **THE PLAN IS THE PARCEL.** Adding a band inserts its phase-0 art as tiles at the front-ward end of the blob, which renumbers every static tile after it and invalidates every layout word that named one; doing any two of those three bakes cleanly and ships corrupt art (TOOL-01). So the unit of work is a **symmetric plan** computed before anything is touched — each layout entry records BOTH its with-band and its without-band word, so `insertBand`/`removeBand` are literal inverses and history's apply and undo are those same two functions with arguments swapped, not two implementations that can drift. **THE RENDERED PICTURE IS THE INVARIANT, not "it still validates"** (the corrupt case validates): image invariance is asserted **cell by cell over all 4096 nametable words** of the real `b0e5a661` document, through a resolver written from aeon's own nametable loop rather than from the code under test, for appends AND for an insert at position 0 (where every index moves). Round trips are **byte-identical through `serializeBgOverride`**, with `serialize(golden) === serialize(golden)` run first as the nondeterminism control. **ONE NEW CONTRACT CONSTANT, extracted not remembered:** `LAYOUT_TILE_INDEX_MASK`, from `inject_editor_bg.py`'s `if word != 0: idx = word & 0x7FF; word = (word & ~0x7FF) | ...`. The half a reader forgets is not the mask but the **ESCAPE** — a word of exactly 0 draws VRAM tile 0 and is **not** a reference to `tiles[0]` — and the corollary: a renumbering that would turn a nonzero attr-free word INTO 0 is **unrepresentable**, so it refuses rather than blanking a drawn cell. Contract hash re-pinned; the drift gate's own coverage assertion forced the export. **REMOVAL WILL NOT SILENTLY DELETE ART:** the real document draws its two bands in **2560 of its 4096 cells**, so removal refuses while any cell names the band, says how many, and takes `blankReferencingCells` as the caller stating it meant to lose that art. (Demoting a band to static art — image-preserving but *not* a byte-identical inverse, so a different operation — is deliberately **not** in this parcel.) **Bar 12 handled at the two copiers this parcel introduces** (the `slot_base` resync and the owned-key rebuild): both are spreads, never field lists, and both are pinned with unknown top-level AND unknown band keys plus the codec's deliberately illegal palette — the hand-enumerated-literal plant on the resync fails with `expected undefined to be 'a tool that does not exist yet'`. **19 red-first plants, each grepped for site uniqueness first** — which mattered again: `history.ts` now carries the `set-bg-override-band` dispatch **twice** (apply and undo are near-identical, differing only in which way the ternary points), and the undo-direction plant had to hit line 295 and not 151. **ONE PLANT CAME BACK GREEN AND THAT IS THE FINDING:** deleting the `word === 0` escape left the whole suite passing, because the row poisoned a cell and then APPENDED, and on an append tile index 0 does not move — the row was vacuous. Re-cut at position 0 it now fails `expected 8 to be +0` (`5aa5c35`). **⚠ STILL OPEN, unchanged from part 1 and untouchable from a background agent:** no writer has yet produced `anims` bytes aeon's gate has judged — composition needs a real save + `python3 tools/inject_editor_bg.py` + a build, which is foreground work. Next: the band editor UI + agent parity + a CDP harness. | M | effects arc wave 1, surface 4 | **OVERSEER VERIFICATION, on the MERGED tree, not the branch:** `tsc` clean, **4289 passed / 0 failed / 3 skipped** at load average 149 (the generous `--testTimeout=60000` deliberately keeps item 16's flake out of the verdict; master's own baseline that hour was 4241 passed **+1 failed**, and the failure was item 16 reproducing — see that row, and see the correction there: **that sample was NOT independent of the instrument that caused it**). **Two violations planted by the overseer that the parcel did NOT list**, each grepped for site uniqueness first, to test the guard rather than replay its proof: (a) an off-by-one in the renumber (`idx + n` → `idx + n - 1`) and (b) a boundary error (`idx < slotBase` → `idx <= slotBase`). **Both went red, 6 rows each, and by the RIGHT rows** — *renders every cell identically* fired by name in both the appended and inserted-first forms, plus *keeps the attribute bits and only rewrites the index half*. So image invariance is genuinely guarded rather than incidentally passing. **Plant (b) also fired the re-cut *leaves the consumer BLANK escape alone* row** — the one the parcel self-reported as vacuous and re-cut in `5aa5c35` — against a defect it was not designed for, which is what makes the vacuity fix verified rather than merely claimed. Both plants restored, tree confirmed clean. **DESIGN CALL RATIFIED:** removal refuses by default when layout cells draw the band, with `blankReferencingCells` as the caller's explicit statement of intent. Correct, and for the arc's own reason — the real document draws its two bands in 2560 of 4096 cells, so silent blanking would be an unrequested edit to the author's picture (this arc's entire defect class), while a bare refusal would make the command useless for the only case that occurs. Undo-of-add does not hit the refusal, because adding a band creates no referencing cells; that is why the round-trip rows pass. **Demoting a band to static art is image-preserving but is NOT a byte-identical inverse** — correctly ruled a different operation and out of scope. **Renumbered from the parcel's `21` to `23` at merge:** it branched before items 21 and 22 existed on master, so the collision was the overseer's to resolve, not the parcel's error. **AND THE ORIGINATION LIMIT IS NOW CLOSED TOO** (`scratchpad/bganim-origination.{py,emit.ts}`), which item 20 named as the honest gap and which this parcel made reachable without any UI: the command is a pure function on the document, so the overseer composed a band **that has never existed** — 8×4, `timer`, `rate_shift` 3, **non-zero art in all 8 banks** (blank art would bake to all-zero banks and pass the consumer trivially) — appended at slot 192, rewriting 1536 layout words. **aeon's `inject_editor_bg.main()` ACCEPTED it**, `BgAnim_Table` 2 → 3, with both artifact deltas matching figures **derived from the band's geometry** rather than typed: banks `+cols*rows*BANKS*32 = +8192 B`, tiles `+cols*rows*32 = +1024 B`. **The strongest row is image invariance measured on AEON'S OWN EMITTED ARTIFACTS, not on the document:** all 4096 nametable cells resolved through to `(attrs, tile bytes)` before and after — **1536 raw words differ** (the indices move, as they must) and **ZERO resolved cells differ**, with an anti-vacuous row confirming 4096/4096 cells draw a tile across 203 distinct images. That is a strictly stronger statement than the unit test makes. **What remains, and it is now genuinely all that remains: no build, no ROM, no staleness gate.** Nothing assembles the emitted `.emp`; the band has never run. That is part 3's proof and this must not stand in for it.

| 24 | **aeon's LIVE `editor_bg_override.json` is at 448/448 tiles, so Aurora cannot add ANY band to it — not even 1×1. The BgAnim road's real blocker is the tile budget, not the missing UI.** Measured 2026-08-22 against aeon `e087af9c` (`ls-remote`-resolved, their pushed file, not the sibling working tree): the shipped document carries **only `layout` + `tiles`** — no `anims`, no legacy `anim` — with **448 tiles against `BG_TILE_CAPACITY` 448** and 4096 layout words. `tileSlotsRemaining` is **0** while `bandsRemaining` is **4**: the binding constraint is tiles, and all four band slots are free and unreachable. Aurora's own command refuses correctly and loudly at both sizes tested — *"the band needs 1 slot(s) at the front of a 448-tile blob, which would make 449 — over the BG tile capacity of 448"* — so this is the guard working, not a defect, and the refusal is non-vacuous on live data. **Why it matters: every band figure in items 20 and 23 was measured against the `b0e5a661` FIXTURE (340 tiles, 108 free), which is a historical document, not the one that ships.** The aeon overseer flagged the missing `anims` proactively; the capacity consequence is this lane's, found by running the command against their live bytes rather than reading them. **The mechanism is the prefix rule doing what it always did:** animated slots are a *prefix of* `tiles`, so `insertBand` grows the blob — and a full blob has nowhere to grow. **What the live document actually needs is the operation part 2 deliberately scoped OUT**: promoting an existing static tile range *into* a band, which does not grow the blob at all (a band's phase 0 IS the static art of the slots it covers). Because bands pack from slot 0, promotion also has to move the chosen tiles to the front — the same whole-blob renumber plus layout rewrite `insertBand` already implements, entered from the other end. So this is a new entry point on existing machinery, not new machinery. **Alternative, and it is a content decision rather than an engineering one: shed static tiles.** At 448/448 the OJZ background spends its entire budget on static art, so any band at all costs picture. That is the owner's call and is exactly the trade item 8 booked with no local expression — note the codec now DOES express the real ceiling (`BG_TILE_CAPACITY` = 448, derived from aeon), while `agent-handler.ts` still caps at a wrong 512. **Sequencing consequence: part 3's composition proof cannot use the live document as its subject** unless promotion lands or tiles are shed; it will have to state which document it ran against, and why. **DISCHARGED 2026-08-22 by item 27, which took route 2 (promotion) — not route 1 (shedding static tiles), which remains the owner's content call and is still unmade. Part 3's composition proof CAN now use the live document: promotion has been run against it end to end.** | M **RULED 2026-08-22 — THERE IS NO TILE RECOVERY, and promotion-first is the road, not a workaround.** The aeon overseer answered the ask and went past it; every measure re-derived firsthand HERE under a deliberately changed frame, because we vendored their constants and agreement between us is cheap by construction. **(a) 448 is not a budget, it is a hard VRAM boundary** — the BG tile region is `$8000..$B7FF` because the sprite attribute table sits at `$B800`; `(0xB800-0x8000)/32 = 448` exactly, reproduced here. Tile 449 sprays into the SAT. It cannot be raised by decision. **(b) Zero slack inside it**, three ways, all reproduced against their pin `e087af9c` (`git cat-file -p`, not the working tree): 448/448 distinct bitmaps (sha1 per tile, a different method than theirs); **0** flip-redundant pairs over orig/H/V/HV **with a control**, since a flip check whose transforms are no-ops finds zero pairs and looks like a finding — `H(H(t))==t` and **all 448 tiles change under both H and V**, i.e. not one tile in the blob is symmetric; 0 uniform-colour tiles. So dedup, blank-reclaim and flip-reclaim are all exhausted and recovering even 8 tiles means removing or redrawing art. **(c) The joint nobody cited, checked and CLEAR — recorded as a negative result rather than left implied.** Their reference count masks the low 11 bits off each layout word, but the consumer contract's escape clause says the consumer rebases only `if word != 0`, so a layout word of exactly zero renders VRAM tile 0 and does **not** mean `tiles[0]` — under a naive mask, `tiles[0]` could be dead slack counted as referenced. Measured: **zero layout words are exactly 0**, so naive and escape-aware counts agree at 448 distinct, 0..447, zero unreferenced. That was the one step that could have refuted *not one unreferenced slot*, and it took one command. **(d) Not mask-sensitive**, which was the real hunt (bar 19's wrong-alphabet shape): masks `0x3FF` and `0x7FF` both give 448 distinct in 0..447; only masks wide enough to swallow attribute bits push the max out of range. The parameter that had to differ was the mask WIDTH, not the repo. **(e) The lever is the GENERATOR, not dedup — and this collapses two open questions into one.** `png_to_bg_override.py` has no `--max-tiles`; it only refuses *above* the ceiling, so it packed to exactly 448 because nothing asked it not to. `forest_bg_gen.py`, which authored the original bands, emits 340 tiles (192 animated + 148 static) — 108 tiles of headroom **by construction**, because it authors `layout`/`tiles`/`anims` as one coherent owned set. The current blob is the output of the very run that destroyed the two authored bands on 2026-07-21. So *can aeon recover tiles* and *was the colonnade→Deep Forest art replacement meant to take the BG animation with it* are *the same question*, it is a CONTENT/ART call for the owner, and it is on their desk as ONE thread with these numbers attached — deliberately not a second thread. **Consequence for Aurora: unchanged, and that is the point.** Under a saturated blob, promotion (`tiles.length` unchanged) is the only route that functions, and it landed before insertion. If the owner rules for a re-generation with reserved band space, insertion lights up for free and nothing built is wasted; if the art stands, promotion was always the whole road. **⚠ OWNER RULED 2026-08-22 — THE SATURATION IS NOT CONTENT, AND THE PREMISE ABOVE IS SUPERSEDED (the wall is not).** Relayed by the empyrean lane from their own session, transcribed from the owner's words; **recorded as RELAYED, not witnessed here** — this lane did not see the granting act, per the owner-state rule in `OVERSEER.md`. The ruling: the OJZ background is a **generator experiment** (a constrained-art import trial), **explicitly non-final and explicitly moldable**, and the owner specifically wants tiles freed to make room for parallax and animation experiments *on this very art*. It used all 448 unique tiles as a **side effect** of the import, not as an art decision. aeon is adding a band-tile reserve to `png_to_bg_override.py` (config-held, default 0), and the empyrean lane ruled the reserve number is aeon's to choose rather than an owner park. **What survives from the measurement above: the 448 ceiling is a hard VRAM boundary and every reclamation avenue inside it is exhausted. What does NOT survive: that the full document is a durable constraint to design around.** **Consequence for item 28, corrected to the in-flight agent mid-parcel:** promotion stays first-class — it costs no tiles ever and is the only gesture that works on a saturated document — but **insertion is no longer the exotic case and must be a peer in the interface, not a fallback path**. Capacity surfacing matters MORE now, for a different reason than originally given: not *insertion is unreachable*, but *capacity is a live quantity the author is actively managing*. The harness must cover both gestures against BOTH document states. **Method note, and it is the lesson of the whole item:** three lanes independently measured the wall correctly and the measurement was never the weak step — the false premise was the unexamined assumption that a measured constraint is a DURABLE one. Nobody enumerated *whose constraint is this and is it meant to hold*. Measuring a constraint precisely tells you nothing about whether it is load-bearing. | overseer, 2026-08-22 |
| 25 | ~~**Adopt aeon's §5 canonical JSON serialization**~~ — **DELIVERED 2026-08-22**, branch `feat/canonical-json-serialization` (`0376039` the chokepoint, `b8e5ca5` bg-override, `f36b3a4` scene files). **This discharges item 22**, and the clause it adopts is not the one item 22 anticipated: aeon's §5 was AMENDED mid-parcel. Read at their pushed tip `768eb2d8` (`ls-remote`-resolved; `--stat` shows `tools/EFFECTS_CONSUMER_CONTRACT.md` alone, 42 insertions), the clause now splits — **DETERMINISM binds universally, no document classes; COMPACTNESS is per class.** So tile-array documents minify with separators `(",", ":")` and scalar documents **pretty-print at indent 2**, and both sort keys **alphabetically and RECURSIVELY** (Python's `sort_keys` is recursive; nested band and layer objects sort too). Item 22's summary said pretty-print was "dropped on their scale argument" — that argument is *from scale*, so removing the scale inverts it, which is what the ruling found. Suite **4309 passed / 0 failed / 3 skipped** (4289 before; +20), `tsc` clean, both re-measured on the branch with `--testTimeout=60000` (item 16's flake kept out of the verdict). **ONE CHOKEPOINT, `src/core/formats/canonical-json.ts`**, mirroring aeon's own `bg_override_io.atomic_write_json` — a rule spelled once cannot be adopted inconsistently. **EQUIVALENCE WITH THE REFERENCE IMPLEMENTATION IS MEASURED, NOT CLAIMED:** four documents (synthetic, scene-shaped with an em dash, the real `canopy_dusk` golden, the real 407 KB `editor_bg_override` fixture) render **BYTE-IDENTICAL** to `json.dumps(d, sort_keys=True, separators=(",",":"), ensure_ascii=False)` and to the `indent=2` spelling; the BG fixture lands at **282,867 B**, the exact figure §5 cites. Kept out of the suite deliberately — a `python3`-dependent test degrades to skip, which renders "could not measure" as green. **TWO POINTS THE CLAUSE IMPLIES RATHER THAN STATES, both gated:** arrays keep their order (`layout`/`tiles`/`phases`/`layers` are positional — sorting them is corruption, not canonicalization), and keys sort by **CODE POINT**, not JS's default UTF-16 code-unit order, which disagree above the BMP (a leading surrogate is `0xD800`, below `U+FFFF`) — and the BG codec is a sole writer of record round-tripping keys it did not author, so its key set is not ours to bound. **ALPHABETICAL, NOT CONTRACT ORDER, and the ruling's reason lands exactly on this codec:** `canonicalize()` ordered by `TOP_LEVEL_KEYS` then appended unknown keys in *insertion order*, which is not reproducible across writers — contract order has no answer at all for the keys we round-trip untouched. `TOP_LEVEL_KEYS`/`BAND_KEYS` survive as the contract's DECLARATION order (reader, validator, drift gate) and their comments now say so. **ROUND-TRIP POSTURE SURVIVES:** sorting REORDERS `palette`, `palette_line` and every unknown key at top level and inside a band; the test planting a *deliberately illegal* palette still passes it through unjudged, and the "refuse to drop" assertion now covers BANDS as well as the top level — proven live, a planted `continue` producing *canonicalization would drop palette* and *would drop anims[0].authored_by*. **NO BG GOLDEN NEEDED RE-PINNING** — every byte assertion in the BG suites compares serializer output to serializer output; the only pinned hashes belong to the vendored fixture and the vendored contract, neither written by this codec. **SEMANTIC IDENTITY MEASURED against master's serializer** on the real document: 282,867 B → 282,867 B (`f6175db63dfbc3c2…` → `e0401a220d81305c…`), decorated 283,047 → 283,047, deep-compare of the parsed values **IDENTICAL** both times — identical length with a different hash is the signature of a pure reordering. **THE SCENE GOLDEN DID MOVE, and was re-sorted by the REFERENCE implementation rather than by our writer** (`json.dumps(..., sort_keys=True, indent=2, ensure_ascii=False)`), preserving the fixture's stated value — a serializer-built golden drifts in lockstep with a broken serializer — and improving it: the byte round-trip is now cross-implementation evidence. Proven inert four ways: 2,524 → 2,524 B, 140 → 140 lines, `git --stat` 89/89, and the **multiset of content lines (indent + trailing commas stripped) IDENTICAL**, nothing only-in-before or only-in-after. **THE VENDORED BG FIXTURE IS UNTOUCHED** — §5 now says so in contract text: "Canonicalization governs what a tool WRITES as output, never what it VENDORS as evidence." `canopy_dusk.json` is NOT covered by that exception and the distinction is the point: it was authored in this repo (`3ddfa8e`), so there is no capture provenance to destroy. **TWO NEW SCENE ROWS DO NOT DISCRIMINATE AND SAY SO IN THEIR OWN COMMENTS:** the scene DETERMINISM row and the pretty-print row both pass against the pre-§5 writer, and the determinism one *has to* — a scene cannot carry a key the schema does not declare, so schema order was already total over its key set. That is precisely why the BG codec needed this ruling and the scene codec did not; both rows are kept as regression guards, since the ruling lives in another repo. **A BG determinism row ALSO came back green at first and was re-cut:** `b0e5a661` carries only DECLARED keys, so a contract-order writer already renders it deterministically — the row passed against the very code it was meant to refute. It now decorates the fixture with `palette`, `palette_line`, an unknown top-level key and an unknown BAND key before reshuffling. **10 red-first gates total**, each failing assertion recorded, each plant grepped for site uniqueness first. **DEVIATION, FLAGGED FOR RATIFICATION:** §5 says to land a reserialization as its own commit that changes nothing else. Not reachable for `canopy_dusk.json` without landing a RED commit — it is byte-pinned to the writer under test, so the two must move together. The rule's purpose (a wholesale rewrite showing as `1 +-` hiding a real edit) is discharged by the four inertness proofs and by the diff being 89/89 attributable pretty lines. **⚠ STILL OPEN — the rest of §5's reach, deliberately not acted on:** §2.2 names `section_N.meta.json` and root `project.json` as documents of this contract, and Aurora writes both pretty-printed with UNSORTED keys (`section-meta.ts:35`, `save.ts:145`, `save.ts:303`). `serializeSectionMeta` is *accidentally* compliant — `bgLayoutRef` < `paletteRef` < `sceneRef` is already alphabetical — which is exactly the kind of luck a fourth ref would end. `project.json` is NOT compliant and Aurora is not its sole writer, so canonicalizing it rewrites aeon's file wholesale; that needs the owner's word, not a parcel's. | S | overseer, 2026-08-22; aeon `tools/EFFECTS_CONSUMER_CONTRACT.md` §5 at `768eb2d8` | **OVERSEER VERIFICATION on the merged tree:** `tsc` clean, **4327 passed / 0 failed / 3 skipped**. Vendored BG fixture confirmed untouched (`git diff master..HEAD -- test/fixtures/bg-override/` empty), per §5's own vendored-evidence clause. **`canopy_dusk.json` inertness re-verified independently of the parcel's four proofs:** parsed both revisions and deep-compared — **equal** — and walked the new document for sortedness at every depth: **18 unsorted objects before, 0 after**, with an anti-vacuous check confirming the subject is genuinely nested (6 levels, 31 objects), so "sorted at every depth" is a real claim rather than one a flat document would satisfy for free. **DEVIATION RATIFIED:** §5 asks a reserialization to land as its own commit changing nothing else, and that was unreachable for `canopy_dusk.json` without landing a deliberately RED commit, since the fixture is byte-pinned to the writer under test. Green-at-every-commit was the right call: the rule's *purpose* is that a wholesale rewrite must not hide a real edit, and that purpose is discharged by the inertness proofs plus an 89/89 attributable pretty diff — a red commit would have bought literal compliance at the cost of a broken bisect. **The parcel's own two green rows are the other thing worth recording:** it found that the BG golden determinism row and both new scene rows PASS against the pre-§5 writer, and refused to count them as refutations — the BG one re-cut to decorate the fixture with unknown top-level and unknown *band* keys before reshuffling, the scene ones annotated in-file as regression guards. That asymmetry is the actual argument for why the BG codec needed this ruling and the scene codec did not.

| 26 | **`section_N.meta.json` is §5-canonical BY LUCK, with no gate — and `project.json` is not canonical at all.** Left open by the item-25 parcel as out of its scope, then verified firsthand here. `serializeSectionMeta` (`src/core/formats/section-meta.ts:34-40`) writes a **hand-enumerated object literal** — `bgLayoutRef`, `paletteRef`, `sceneRef` — which happens to be alphabetical, so it satisfies §5's universal determinism clause today **by coincidence**. A fourth ref added in the natural place (say `effectsRef`, which this arc's own wave 2 could plausibly want) breaks canonical compliance silently, and **nothing asserts the sidecar's key order**. Contract §2.2 names this file as a document of the contract, so §5 binds it. **The adjacent and scarier hazard is NOT present, and that is worth recording as a negative result rather than leaving it implied:** a hand-enumerated literal over `SectionMeta` is the exact shape that once let a dropped ref survive a 3,909-test suite here (`cloneSection`, the sidecar erasure arc). I planted it — deleted `sceneRef` from this literal, site grepped unique first — and it fails **6 tests across 3 files**. So the enumeration is guarded even though the ordering is not; the two hazards live in the same three lines and only one has a gate. Fix is small: route both writers through `src/core/formats/canonical-json.ts` (which item 25 just built) and add a sortedness gate with a poison. **`project.json` is the harder half and is NOT Aurora's call**: `src/core/project/aeon/save.ts:145` and `:303` write it pretty-printed and unsorted, and **Aurora is not its sole writer** — canonicalizing it rewrites aeon's repo-owned file wholesale, which is the owner's sequencing decision and carries §5's own one-time-churn caveat. Book, do not act. | S (sidecar) / owner-gated (project.json) | overseer, from the item-25 parcel's open list, 2026-08-22 |
| 27 | ~~**Wave-1 surface 4, part 2b — band PROMOTION and DEMOTION, the pair that does not change `tiles.length`**~~ — **DELIVERED 2026-08-22**, branch `feat/bganim-band-promotion` (`55be1d1` the model + its tests, `5e347fe` the two commands, `ee16b30` the live-document acceptance probe). **This discharges item 24 by its route 2.** Suite **4371 passed / 0 failed / 3 skipped** (4327 before; +44), `tsc` clean, both measured on the branch with `--testTimeout=60000` (item 16's flake kept out of the verdict). **Still no UI** — that is part 3. **THE ACCEPTANCE IS THE PARCEL AND IT IS NOT VACUOUS: promotion runs on aeon's LIVE `editor_bg_override.json`**, read at their pushed tip `9b3f11f6` (`ls-remote`-resolved, blob `a1d187ba`, **re-hashed by the probe itself** rather than trusted from the export). That document carries only `layout` + `tiles` — **448/448 tiles, zero bands, all 4096 cells drawn, 448 distinct resolved cells** — and `makeAddBandCommand` refuses on it at **1x1, 2x1, 1x2 and 8x4**, quoted live. **26/26 checks pass:** promoting tiles 200..232 into an 8x4 band rewrites **2464 layout words**, `tiles.length` goes **448 -> 448**, **all 4096 resolved cells are identical** through a resolver written from aeon's own nametable loop while **2464 RAW words change**, the **tile multiset is unchanged** (a permutation, not a rewrite), prefix identity holds for every band, promote -> demote is **byte-identical** under a `serialize(x) === serialize(x)` control, and demotion blanks **not one cell** where removal would have had to. The probe is proved non-vacuous the same way its rows are: with the slide-up defeated it reports 3 failures and **2144 of 4096 cells differing**. **IT IS A PROBE, NOT A FIXTURE, AND THAT WAS A DELIBERATE REFUSAL:** vendoring a second copy of another repo's moving 89 KB file is a maintenance trap, and §5 says canonicalization governs what a tool WRITES, never what it VENDORS as evidence. So the SUITE asserts the **property** — *promotion succeeds on a document with zero free tile slots* — against a document **padded to `BG_TILE_CAPACITY`** rather than pinned to 448, which survives aeon taking item 24's route 1. **NEW MACHINERY: NONE. A NEW ENTRY POINT ON THE OLD.** `planLayoutRemap` is reused unchanged; `planBandArrival` factors the four refusals insertion and promotion share (position, band ceiling, band geometry, a `slot_base` that tries to place rather than agree); and `applyWithBand`/`applyWithoutBand` are **ONE implementation for both plan kinds, differing in a single line each**. `BandSlotPlan` gains **`staticBase: number | null` — REQUIRED, not optional**, so every construction site must say whether the band's slots are created/destroyed (insert/remove, the blob grows) or moved (promote/demote, the length is fixed), and a site that forgets fails to compile. `insertBand`/`removeBand`/`promoteBand`/`demoteBand` are mode-asserting wrappers, so a plan of one kind cannot be applied through the other door. **History gains NO new branch** — it now calls the mode-agnostic pair; a second dispatch would be a third place slot arithmetic could disagree, in a file that already carries the same ternary twice. **DEMOTION DESTROYS NOTHING, and that is the pair's whole point over insert/remove.** `removeBand` deletes the band's slots, so on the real document it must refuse or blank **2560 of 4096 cells**; `demoteBand` hands the same slots back to `tiles` as static art, so the picture is unchanged in BOTH directions and there is no `blankReferencingCells` here — there is nothing to blank. The discriminating row asserts exactly that contrast on the same band. Demoting the LAST band to the default target moves **no tile at all** (`plan.layout` is empty), because the default is the first slot the shortened animated prefix no longer covers. **THE PHASE-FILL DESIGN CALL, AND IT WAS SETTLED BY MEASUREMENT RATHER THAN TASTE.** Banks 1..7 of a promoted band are a **copy of phase 0**, so the band is *visually inert until authored* — it draws at every step what those slots already drew. Blank banks were rejected because the picture would break on the band's second phase, an unrequested edit to the author's background, which is this arc's entire defect class. The third option — **generating banks 1..7 as pre-shifted art**, which is literally what the contract calls `phases` ("pre-shifted art 1px apart, selected by step & 7") — looked strictly better and **was killed by the data**: over BOTH real `b0e5a661` bands, BOTH tile orderings (column-major and row-major) and **every** horizontal offset, **zero** exact reproductions of any bank from bank 0; band 0's banks differ by at least **1369/8192 pixels (17%)** at their best offset. The shipped banks are hand-drawn frames, not shifts, so a generated shift would be **art Aurora invented rather than art the author drew**. The measurement also produced a second, unasked-for finding: **band 1's banks are 99.24% identical to its bank 0** (31 of 4096 pixels differ at offset 0), so the real shipped bands are themselves close to inert — the chosen fill is the closest thing to what the document already does. **AUTHOR-FACING CONSEQUENCE, stated plainly:** promoting costs zero tiles and changes nothing on screen, at rest or in motion, until the author writes banks 1.. — the band editor's job. It also writes 8x the range's art into the JSON where blanks would compress; the ROM cost is identical either way, because the banks exist at fixed size whatever is in them. **18 RED-FIRST PLANTS, each grepped for site uniqueness first, and the grep mattered twice:** `return idx - n;` occurs in BOTH `planBandRemoval` and `planBandDemotion`, and the demotion index guard is a near-verbatim twin of the removal one — both plants were disambiguated by anchoring on adjacent lines and confirmed to have landed on the intended site (the demotion-index plant left the identically-named removal row **passing**, which is the proof). The `history.ts` undo-direction plant had to hit line 300 and not 156. Headline failures: the losslessness line defeated gives *expected [ ...(212) ] to have a length of 340 but got 212* across **13 rows**; the tiles-move line in the other direction gives *expected [ ...(348) ] to have a length of 340 but got 348* across **14**; the promotion slide-up gives image-invariance failures across **8**; the undo-direction plant gives *expected '{"anims":[{"cols":8,...' to be '{"layout":[16576,...'* across **9**. **ONE ROW CAME BACK GREEN AND THAT IS THE FINDING.** *refuses a range that reaches past the end of the blob* passed with its guard deleted, because it matched `/has only \d+ tiles/` — and the CODEC's prefix check says *"the static tile blob has only N tiles"* too. The row was catching `refusing to create a BgAnim band` from a completely different rule. Re-cut to match this guard's own words (`cannot promote tiles 338..342` plus *it never adds any*), plus a boundary row that the exactly-fitting range is allowed, it now fails correctly. **Two errors sharing a phrase is a subtler trap than two call sites sharing a line**, and no uniqueness grep over the source would have caught it. **⚠ STILL OPEN, unchanged from parts 1 and 2 and untouchable from a background agent:** no build, no ROM, no staleness gate — nothing assembles the emitted `.emp` and no promoted band has ever run. The probe exercises Aurora's writer, not aeon's injector: it does not call `inject_editor_bg.main()` (the item 20 and 23 probes did, and that composition should be re-run against a PROMOTED band in the foreground). Item 24's route 1 — shedding static tiles to buy band budget — is still the owner's unmade content call; promotion sidesteps it rather than answering it, since a promoted range stops being independently addressable static art. Next: the band editor UI + agent parity + a CDP harness. | M | ROADMAP item 24 route 2 | **DELIVERED AND LANDED 2026-08-22.** Merged-tree verification: `tsc` clean, **4371 passed / 0 failed / 3 skipped** (+44). **THE LAST LINK IS CLOSED ON THE SHIPPING DOCUMENT** (`scratchpad/bganim-promoted-vs-aeon-injector.{py,emit.ts}`, overseer foreground — the row the parcel tagged): a band promoted on aeon's **live 448/448** `editor_bg_override.json` was fed to **aeon's own `inject_editor_bg.main()`** and **accepted**. `BgAnim_Table` goes from the disabled stub the ROM has carried since `dd93a840` (2026-07-21) to **`u16 = 1`**. `bg_anim_banks.bin` **0 → 8192 B** = `cols*rows*BANKS*32`, derived from geometry — **and that `0` needs a footnote the aeon overseer supplied and this lane verified.** It is the size the injector *emits into a clean output directory*, and it agrees with what reaches the ROM (the disabled stub emits `pub data BgAnim_Banks = Data.empty`, so zero bytes ship). It is **NOT** a statement about aeon's tree: the on-disk `games/sonic4/data/generated/ojz/act1/bg_anim_banks.bin` there is **49,152 bytes** of stale banks from when the document had bands, because the injector writes that file only inside its `if anims:` branch and the disabled-stub path leaves whatever was present. Verified firsthand at aeon `9b3f11f6`: 49,152 B, the committed `bg_anim.emp` is the stub, and the file sits in `tools/verify_level_bin.py`'s `_ORPHAN_ALLOWLIST` with a written reason from their 2026-08-13 tools lens sweep (D10) — *a deliberate ruling, not an oversight*, since `verify_level_bin` checks embed→file and never file→embed. The `0` in this row came from the probe's fresh tempdir, where the file does not exist; it happens to coincide with the ROM-side truth for a different reason than a reader would assume. **It will look like a contradiction the first time the promotion path writes that file for real**, which is exactly why it is written down here; **`bg_tiles.bin` delta ZERO**, which is the entire reason promotion exists. **Image invariance on aeon's emitted artifacts: 2464 raw nametable words move, ZERO resolved `(attrs, tile bytes)` cells differ**, anti-vacuous at 4096/4096 cells across 448 distinct images, with the emit refusing outright if the source is not full or already carries bands. **DEVIATION RATIFIED:** `history.ts` now calls `applyWithBand`/`applyWithoutBand` rather than `insertBand`/`removeBand`. Accepted, and it is the better design rather than a concession — one dispatch serving both plan kinds means promote/demote **cannot drift** from insert/remove, and the pair adds zero dispatch sites; the cost, that the four public door names are no longer what history calls, is documented and small. **PHASE-FILL CALL RATIFIED: copy phase 0 into banks 1..7**, and the parcel killed the more attractive option with data rather than argument — generating *pre-shifted* art (literally what the contract calls `phases`) was tested against both real `b0e5a661` bands, both tile orderings and every horizontal offset: **zero** exact reproductions, band 0 differing by ≥1369/8192 pixels (17%) at its best offset. The shipped banks are hand-drawn frames, so a generated shift would be art **Aurora invented**. Fallout finding: band 1's banks are **99.24% identical to bank 0**, so the real bands are themselves near-inert. Author-facing consequence, stated: promoting costs zero tiles and changes nothing on screen at rest *or* in motion until banks 1.. are authored; ROM cost is identical either way since the banks exist at fixed size regardless of content. **THE PARCEL'S GREEN ROW IS THE MOST TRANSFERABLE THING IN IT — see `docs/OVERSEER.md` bar 2c.** **Overseer's own independent plant** (phase 0 sourced from offset 0 instead of `staticBase`) went red across 10+ rows including image invariance and prefix identity — **but the overseer's uniqueness grep returned 2, not 1, and it proceeded anyway**, landing on the producer (`:643`) while the guard (`:677`) stayed intact. That happens to be the right experiment — it proves the guard catches a bad producer — but the step was skipped, not passed, and is recorded as such. **STILL OPEN, and it is now genuinely the whole remainder: no build, no ROM, no staleness gate.**

| 28 | ~~**Wave-1 surface 4, part 3 — the band editor UI + agent parity + a CDP harness**~~ — **DELIVERED 2026-08-22**, branch `feat/bganim-band-ui`. **The arc is now reachable by a human.** Before this, `grep -rl bg-override src/renderer/` returned **zero** files and nothing loaded or saved `editor_bg_override.json` at all. Suite **4422 passed / 0 failed / 3 skipped** (4371 before; **+51**), `tsc` clean, CDP harness **35/35** on the LIVE aeon tree. Commits: `c7d7df7` load/save + the write-back, `19ef005` the panel + provider, `6983fa8` agent parity, `084eee6` the `__dbg` probes, `0a30311` the harness, `7e29d27` the peer reshape. **⚠ THE PARCEL'S BIGGEST FINDING IS A DEFECT THE MODEL SHIPPED WITH AND NOTHING COULD SEE.** `getActiveLevel` builds a fresh `S4Level` **view** per gesture and again per undo (`BoundEditHistory` calls its supplier each time). Every other command mutates *through* that view into an object the project owns; a band command **cannot**, because the plan appliers are pure and `history.ts` writes `level.bgOverride = applyWithBand(...)`. On a plain data field that assignment lands in a throwaway object — **the store keeps the pre-edit document, the panel never changes, the save writes the file it loaded, and every unit test of the codec, the plans and the four commands stays green.** On screen it is indistinguishable from a correct refusal. The field is now an **accessor** that reads the project's holder and writes back into it; the command layer is untouched. `bg-override-writeback.test.ts` is the only place in `vitest run` where that wiring is the subject, and it carries a **negative control** that drives the same command through a plain-field level and watches the edit vanish. **LOAD/SAVE:** `bg-override-io.ts` owns none of the format — parse and serialize stay the sole reader and writer — only the three facts a *file* has: where it is, whether it was there, whether it parsed. Absent is silent, unreadable is loud and **never written over**. It writes **only what changed**, comparing the re-serialization against the exact loaded text: **measured, aeon's live 88,993-byte document round-trips BYTE-IDENTICALLY**, so an untouched project adds no write at all; a `json.dumps`-defaulted file (the b0e5a661 fixture) is normalized once and that is its own row rather than a surprise. **⚠ OWNER RULING LANDED MID-PARCEL AND THE SHAPE CHANGED FOR IT.** The dispatch said aeon's 448/448 document makes insertion useless on real content, so promote should be primary and insert the fallback — and that shipped first, as a collapsed section titled *"Add a band (needs free tiles)"*. **The 448 ceiling is real and immovable** (`(0xB800-0x8000)/32`, the BG region below the sprite attribute table) **but the saturation is not**: the owner ruled that background a non-final generator experiment and the aeon lane is adding a band-tile reserve. So the two art sources are now **PEERS** in one *New band* section — geometry and driver asked once, then *From existing tiles* (promote; costs no slots at any capacity) and *From new art* (insert; costs `cols*rows`) side by side, neither collapsed, neither reached by the other failing. **The capacity readout stayed, for a better reason than the brief gave**: not "insertion is exotic" but "capacity is a live quantity the author manages", so `tileSlotsRemaining`/`bandsRemaining` sit beside the control that spends them and a refused Add carries the codec's reason. **A DRIVER IS A SCALAR SOURCE, NEVER AN AXIS** — options come from `BGANIM_DRIVER_NAMES` (read out of the vendored contract, so no stale list can exist here), every option's title carries the correction, and a test asserts **no option names a direction**. The empty option **leaves the key out**, so a document tracking the engine default goes on tracking it. **Removal's confirmation is the COMMAND'S refusal**, not a count the panel invented: first press asks for a non-blanking removal and the refusal — which names the cells at stake — *is* the prompt. **AGENT PARITY:** five methods on the `EDITOR_METHODS` registry (`list/promote/demote/add/remove_bg_anim_band`), one entry lighting each up on **both** MCP and Aether. Refusals **throw** rather than returning `{ok:false}` — an MCP client cannot tell a cheerful no-op from success. **⚠ STALE ROADMAP PATH, REPORTED NOT WORKED AROUND: rows 8 and 28 both send this work to `src/main/mcp/agent-handler.ts`, which does not exist** (`ls src/main/mcp/` → ENOENT). The real surface is `src/main/editor-methods.ts` + `src/renderer/agent/agent-handler.ts` + `src/shared/agent-protocol.ts`. **THE CDP HARNESS (`scratchpad/bganim-band-harness.mjs`) EARNED ITS KEEP THREE TIMES.** Every bound it asserts is **parsed out of the vendored contract JSON at startup** — the rows picker is checked against a list *computed* from `TILE_BYTES`, and on-screen and derived both come back `[1,2,4,8,16,32,64,128,256]`. It drives the whole arc on the live tree: promote (448→448, blob unchanged) → remove through the real two-click blank confirmation (**448→446, 2 slots freed**) → add (446→448) → three Ctrl+Z to a **byte-identical** document (FNV-1a over the serialization, not a band count). **3s on the facet with the panel open = ZERO map repaints against 962 rAF ticks**, so the viewport's measured no-idle-repaint property is intact and the preview ruling's posture is honoured. **RED-FIRST, run twice** (poison `getActiveLevel`'s setter, **rebuild**, re-run), and each run found a bug **in the harness**: (1) three rows passing **vacuously** — *"the blob did not grow"*, *"Ctrl+Z removes the band again"* (0→0→0) and *"byte-for-byte what it started as"* are all trivially true of a no-op, and each now requires the gesture to have LANDED; (2) section 6 **CRASHED** on the band that was never created, silently taking the no-clock rows with it — 6/7 are now gated on 5c and their absence is a **failing row that names itself**; (3) `clickByText` searches `text + ' ' + aria-label`, so an `IconButton` rendering *"Remove"* with label *"Remove band 0"* yields the haystack *"Remove Remove band 0"* that **no anchored pattern against either half can match** — two clicks had been silent no-ops. **BAR 2b, FOUND BY PLANTING:** the project-level *"an unreadable file is never a write"* row **does not discriminate** `saveFileFor`'s unreadable branch — the load leaves `doc` null, so the first branch returns and the second is unreachable from there. Deleting it left that row green. It now has a direct row that reaches the state. **BAR 2c, ONE MATCHER RE-CUT:** the add-at-capacity row matched `/over the BG tile capacity/`, which the codec's **document-level** `tiles.length` validator also says — re-cut against `planBandInsertion`'s own words, `slot(s) at the front of a N-tile blob`. **Node poisons, all restored:** the write-back setter (2 rows), the unreadable guard (1), the unchanged-comparison (2), the save-plan call (2), the load call (5), the insert capacity check (1), the `rowChoices` power-of-two filter (2), an **empty driver derivation** (1 — the empty-dropdown defect a derivation test cannot see), the promote dispatch (3, aimed **by line number** because four near-identical `executeAmbientCommand` lines sit within 32 lines), and removal defaulting to blank (1). **PREVIEW DELIBERATELY NOT BUILT** — concurrent, not a prerequisite, per `reviews/2026-08-22-preview-posture-ruling.md`; nothing here starts a clock, schedules a frame or touches `MapViewport`. **⚠ STILL OPEN, unchanged and untouchable from a background agent:** no build, no ROM. The harness drives Electron, never an emulator, and no band authored through this UI has ever run — the promote→inject composition that item 27 closed should be re-run in the foreground against a band the **UI** made. The tile budget the panel prints is Aurora's arithmetic, not aeon's injector's. | L | effects arc wave 1, surface 4 | **DELIVERED 2026-08-22 on `feat/bganim-band-ui`; awaiting overseer review and land.** |

| 29 | **FOREGROUND FOLLOW-UP, tagged by the item-28 parcel: re-run the promote→`inject_editor_bg` composition against a band the UI actually made.** Item 27's acceptance probe composed against a band the *model* made; the panel prints **Aurora's** tile arithmetic, not aeon's injector's, and the two agreeing has never been observed on a UI-authored band. Runtime work, so it is the overseer's in the foreground — not an agent's. **Two traps recorded in `OVERSEER.md` apply and both produce a false green:** `editor_bg_override.json` is on the INPUT side of the level-staleness mtime gate, so writing it makes a staleness stop **certain**, and that stop presents as the `anims` refusal gate rejecting Aurora's bytes when it never judged them — attribute the failure to a STAGE before reading it as a verdict. Run `tools/regenerate-level.sh` after the save and before the build, and `rm -f` the ROMs first, or four leftover CRCs greet you from a build that never ran. | S | item 28's tag |

| 30 | **`npm install` FAILS on this repo — a front-door defect, found while auditing the README and confirmed firsthand by the overseer.** `package.json` carries `vite@^8.0.8` while `electron-vite@5` declares `vite: ^5.0.0 || ^6.0.0 || ^7.0.0`, and `@vitejs/plugin-react@6` wants `vite@^8` — so npm cannot satisfy the graph. Verified non-mutating with `npm install --dry-run` (exit **1**, `ERESOLVE`, `Conflicting peer dependency: vite@7.3.6`), reading the exit status **directly and not through a pipe**, because the discovering agent's first two runs reported exit 0 through `| tail` — a pipeline's status is the LAST command's, which is how a failed install then silently resolved against the parent checkout's `node_modules` and produced two green-looking runs. `npm ci` fails identically; `npm install --legacy-peer-deps` works and is now what the README documents. **That is the right README fix and it is not the fix.** A new contributor's first command failing is a real defect; the options are to hold `vite@7` until `electron-vite@6` supports 8, or to pin an override deliberately. Not urgent, genuinely open, and it should not be closed by documentation alone. | S | README audit, 2026-08-22 |

| 31 | **NEXT UP — emit a WRITER-ORIGINATED effects scene from a real authoring session, and hand it to aeon as a fixture.** aeon asked for this twice and its value rose when their seam landed (item 21): the generator is now wired end to end, so a fixture from a real session exercises **read → validate → emit → lower through the real `scene()`/`layer()` constructors → link → witnessed by the seam gate**, where an hour earlier it would have tested only the reader. **Why it is not the fixture we already shipped.** `test/fixtures/effects/canopy_dusk.json` is writer-**certified** (hand-written for shape coverage, then proven byte-identical through `serializeEffectsScene(parseEffectsScene(GOLDEN))`, with §5 key order asserted against the document rather than the serializer). That is an *implementation* cross-check — TypeScript writer bytes against aeon's Python-side inference, which is exactly the axis their slices 1–3 got wrong. It is **not** corroboration of the values, because a hand-written fixture shares the schema-read frame with the schema. A writer-**originated** artifact was enumerated over a parameter nobody chose deliberately, which is the only thing that breaks a shared frame. **Shape:** drive the real app under CDP through the scene authoring UI, author a scene, save, and commit exactly what the writer emitted — no hand-editing, or it stops being the thing that makes it valuable. **Bar 1 applies and is the point**: a node-suite fixture would be writer-certified again. **No dependency either way** — aeon has no gate on it and said so twice. | S | aeon's ask, 2026-08-22 |

**Open (added 2026-08-21, owner-named):**

| # | Work | Size | Source |
|---|---|---|---|
| 7 | ~~**Priority-aware object previews + occlusion ghost**~~ — **DONE 2026-08-21 (classic/S1)**, merge `feat/s1-priority-occlusion`: per-pixel compositor (map wins iff hi tile ∧ opaque ∧ low sprite piece; piece pri = attrs bit 15), violet-wash ghost at 0.4α riding the same "Sprite occlusion (game order)" toggle (default ON), MZ animated hi-pri cells re-patched per play-tick. Harness 30/30, and what it **guards** is `avg < 5ms` (`s1-priority-occlusion-harness.mjs:620`) — the 0.18ms figure once quoted here was a single observation, never an assertion, and is not a held property (corrected 2026-08-22) (found the owner's exact monitor, GHZ1 $26). **Still open: the aeon viewport half** — no priority-mask derivation exists there yet; re-book when aeon previews grow occlusion needs. Original booking:  In the map viewport, object previews composite above ALL map tiles; the VDP draws high-priority plane tiles in front of low-priority sprites (owner screenshot: an invincibility monitor rendered in front of GHZ tree leaves it should sit behind). Add a third compositing pass — high-priority map-tile pixels re-drawn above the object layer, honoring each sprite piece's own priority bit from its mappings — and a ghost affordance (occluded portion drawn translucent/outlined) so hidden objects stay discoverable and selectable. The priority lens (`337d2d3`) already derives the per-tile priority mask; reuse it. Applies to both engines' viewports where object previews exist. | S–M | owner, 2026-08-21 |

⚠ **Item 1 is the first phase that needs the emulator.** Background agents must never call
`mcp__oracle__*` — they deadlock. Runtime work goes in a CDP / foreground harness the
controller runs, exactly as the two collision harnesses do.

Deliberately **not** open: sweep finding U6 (argued at the site, left unchanged on purpose —
don't re-find it), and the sweep's three REFUTED findings (§6 of the review).

### 5.2 The phase table

| Phase | Work | Gate/dependency | Status |
|---|---|---|---|
| **P0** | Doc hygiene: apply PLAN_AUDIT (status banners, naming pass, README); annotate vision-doc entries superseded by design-week specs | none | **PARTLY DONE** — this revision closes the ROADMAP half; §5.1 items 3–5 are the rest |
| **P1** | Design #6 collision-in-chunks + retirements; in-viewport object/ring placement; section/act properties inspector; act/zone wizard; export realignment (retire vram-coloring path, budget → act-pool math) | none | **PARTLY DONE, and re-cut.** Classic collision authoring shipped (§2.6 C) — but that is *classic*, not aeon design #6; classic object place/move/delete shipped (§2.5); the export **retirement** is done (§4.2, 2026-08-19). Still open: **aeon** design #6, the section/act properties inspector, the act/zone wizard, and the budget readout's act-pool math (the retirement removed the old path; it did not build the new count) |
| **P2** | Aether outbound client + A1 palette→CRAM + A3 Build & Run + A2 warp | Oracle running (exists) | ✅ **DONE 2026-08-19** — see §2.7 |
| **P3** | Sprite export spine (decompose → mappings → DPLC) + animation authoring timeline w/ event tags + object-art previews in map | engine consumer + sigil `.emp` anim question are cross-fence | **RE-CUT 2026-08-20** (audit: `docs/reviews/2026-08-20-p3-plan-audit.md`) — the three 2026-06-17 plans were **executed in June** (154 sprite tests pass; a real `pitcher_plant/` export exists); their checkboxes were never ticked. Actually open: the animation *authoring* timeline (2026-07-03 spec: event tags, reorder, onion-skin) + agent parity; an **engine-side consumer** — nothing in aeon's build reads `data/sprites/`, so the spine ends at the editor's doorstep; and plan 3's premise collapsed and is now RULED (aeon `336c53ea`, 2026-08-20): Aurora emits NEITHER `.asm` nor `.emp` — the neutral export (json + bin) IS the contract, aeon's bake-step consumer (booked their side) owns flatten/dedup/`.emp` generation, and Aurora's golden pins json+bin bytes once aeon hands over the field list its consumer reads. The June `.asm` anim exporter is therefore pipeline-dead and retires when the neutral anim export replaces it. Previews exist on BOTH sides (aeon's `object-previews.ts` survived the re-home; bindings file empty), not classic-only |
| **P4** | Screen mode (design #7 Aurora half) | aeon #7 tasks 1–4 (interpreter + `screens_gen.py`) | open, engine-gated |
| **P5** | Raster mode + live preview (design #8 Aurora half) | aeon #8 tasks 1–4; P2's client | open, engine-gated |
| **P6** | Behaviors properties panel (design #9 Aurora half) + entity-exporter retirement; VRAM budget visualizer v2 | aeon #9 tasks 1–4 | open, engine-gated |
| **P7** | Import pipeline (PNG/sheet quantization) + craft backlog pulls | none | **PARTLY DONE** — the PNG import path shipped on 2C's resolver for classic (§2.6 B). **Core algorithms landed 2026-08-22** (`core/art/sheet-slice.ts`: grid + auto-bounds; `core/art/palette-suggest.ts`: median cut to 15 + transparent, cut in the Genesis 3-bit space): pure functions, 26 node tests, **no caller** — `ImportSheetDialog` still takes a whole sheet against a fixed act palette. Remaining: (a) UI + agent wiring for both, which only a CDP harness can prove; (b) **sprite-frame targets** — measured 2026-08-22, `CommitTarget` is `{ chunkFileIndex }` and nothing else, so a frame target is a commit-planner change, not an import one |
| **P8** | Multi-game level adapters (own design cycle) | none hard | substrate delivered by §2.5; remaining work is the cross-game adapters + world assembly (§4.10) |
| **UX 5–6** | The UX overhaul's own leftovers: typed cross-tab/cross-window clipboard, Converter tab, and the stage-6 polish pass | none | open (§2.6 A) |
| **§2.5** | ✅ **DONE** — Disassembly-as-Project: engine-agnostic `ProjectAdapter` + S1 in-place editing + guarded save + 12 MCP tools + aeon detection unified. Aeon adapter's `open()` performs the real load (`4782e86`, 2026-08-13) — the long-standing "routing marker" claim was stale and is corrected in §2.5; what remains deferred is the unpopulated aeon `report` and `AeonProjectData`'s missing scene/budget concepts. | delivered 2026-08-09 | done |
| **§2.6** | ✅ **DONE** — the August line: UX overhaul stages 1–4, art authoring 1/2A/2B/2C, classic collision authoring + the agent surface, the lens-sweep defect campaign. | delivered 2026-08-12 → 08-19 | done |

Rationale: P2 is the suite's keystone gap — Aurora already *serves* Aether and the outbound
half is what the 2026-07-01 suite audit named — and it builds exactly the plumbing design
#8's live preview needs, which is why it still precedes P5. The classic spine has overtaken
P1's aeon half, so P1's remainder should be re-cut against what facet parity already gave for
free rather than executed as written. P3 and P7 stay pure-Aurora work that can interleave any
time engine work stalls.

**Incoming arc (agreed 2026-08-22 with the aeon overseer): parallax/raster effects authoring view** — multi-band parallax + raster effects + BgAnim tile bands, Aurora-authored (the OJZ BG showcase direction). Sequence: aeon's read-only survey of this repo (pinned at `4cffe45`) → owner design review → aeon's S4LZ-slicing engine parcel → Aurora parcels cut HERE against aeon's committed briefs/contracts (SHAs to be pinged across). Lane split: Aurora's overseer dispatches and lands all Aurora parcels; aeon ships committed briefs; cross-tool contract material goes to empyrean. ~~Known load-bearing caveat handed to their survey: the aeon ProjectAdapter is still a routing marker (§2.5)~~ — **THAT CAVEAT WAS FALSE and Aurora is the source of the error** (corrected 2026-08-22). `open()` has performed the real load since `4782e86` (2026-08-13); `useProject.loadFromPath` no longer exists. The stale §2.5 text propagated into aeon's effects assessment and was refuted there — see `reviews/2026-08-22-aeon-effects-survey-verification.md`. The real gap is narrower: `AeonProjectData` names no scene, preset, band or budget, so that model must be **extended**; it is not a loader that must be built, and any estimate priced as such is wrong.

Watch items from the engine side: `section_id` byte→word (floating origin) — check Aurora's
section keying when it lands; engine/game split (#5) — `project.json` may gain a
game-manifest pointer (the split happened on 2026-07-07: `games/demo` now boots on the
agnostic engine); per-act ROM budget gate (#1) — surface its numbers in the budget UI when
the manifest exists.

## 6. Acceptance bar (per phase)

A phase is done when: tsc + vitest green with new coverage for every format codec;
every new mutation is one undo step and has an agent tool; formats are Zod-validated
with build-gate parity (what the generator rejects, Aurora flags at edit time); the
feature is GUI-verified by the user (Electron; no headless); relevant docs updated
(this file's phase table + MCP.md tool list); and — for generator-coupled work — a
golden test proving Aurora's writer and the Python reader agree byte-for-byte.

Two amendments from the August line (§2.6), both learned the hard way:

- **"an MCP tool" is the wrong phrasing** — one `EDITOR_METHODS` entry in
  `src/main/editor-methods.ts` serves **both** MCP and Aether, so the parity obligation is
  *registry* work: schema entry + `AgentRequest` kind (`src/shared/agent-protocol.ts`) +
  a case in `src/renderer/agent/agent-handler.ts`. There is no MCP-specific half.
- **vitest cannot see React or canvas.** Anything whose behaviour is a rendered surface, a
  mouse gesture or a live store round-trip needs a CDP harness driving the real app
  (`scratchpad/*-harness.mjs` — the collision pair are the reference), run in the
  foreground by the controller. Background agents must never call `mcp__oracle__*`; they
  deadlock. And **plant a violation before believing any guard**: a guard that asserts
  nothing is the dominant defect class here, and a defect planted in the wrong function —
  there are usually two near-identical dispatch lines — passes a full build-and-run cycle
  looking convincing.
