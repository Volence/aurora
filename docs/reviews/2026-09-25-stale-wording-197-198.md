# Stale wording, ROADMAP rows 197 and 198 (2026-09-25)

Branch `stale-wording-197-198`, cut from master `667c3582`. Both rows were re-measured on
today's tree before any change; both were still true. No emulator and no `mcp__oracle__*`
tool was used. Every harness run below: `VITE_AURORA_DEBUG=1 npx electron-vite build` in the
worktree, `ELECTRON_BIN` = the main checkout's electron, `AURORA_BUILT_TREE` = the worktree
(the `root:`/`pinned:` lines named the worktree on every run), `AEON_DIR` = a fresh
`git archive` extract of aeon `origin/master` `7c0baa73` (re-extracted per band-preset run,
because that harness saves into it).

## Row 197: BAND-PRESET-3A-WORDING-DRIFT

**Still true.** `npm run harness:band-preset` on the unmodified tree: 44 rows, 1 failed, and
the one was 3a:

```
FAIL  [3a] LIMIT 1: the author-length sentence is PAINTED and the contract wording is on its hover
        painted(301B): mustBind=false aeonWired=false namesTheControl=false; hover(8678B): rasterRef=true
        sec5Wired=false othersReachNothing=false splitPlusLine=false costsROM=true
```

Every other row green, so the panel was right and the row was wrong. **The row's text was half
right about the cause.** 3a had copied eight phrases from two strings. The five hover phrases
came from `RASTER_SECTION_BINDING_LIMIT` and left it at 3962cc77 (09-10), as the row says. The
three painted phrases came from `SHORT_BODIES.unbound` and left it a week later, at 3f985513
(09-17, ruling b1), which the row does not mention. So both halves were red, for two separate
rewrites.

**Change (8b73aafe).** `scratchpad/band-preset-harness.mjs` bundles
`src/renderer/providers/effects-preset.ts` in memory with esbuild and imports it from a
`data:` URL. 3a now asserts that the painted div's `` innerText === `${title}. ${body}` `` and
its `title === full`, where those values come from `presetLimitsShort()`, the function
`LimitBlock` maps over. On a red it prints the first character where the two differ. The
lookup key `LEADS` (rows 2c and 3a) is derived the same way. The run refuses to start if the
import yields no `unbound` limit or empty strings. It also now calls
`assertFreshBuild(RUN)`, because comparing the bundle against `src/` means nothing when
`dist/` is stale. Checking whether the wording is correct stays with the node suite; this row
only checks that the app paints and hovers the source's wording.

**Proofs** (each mutation was applied with `sed`, shown with `git diff`, rebuilt, and restored
with `git checkout --` from the committed 8b73aafe):

| run | mutation on disk | 3a | total |
|---|---|---|---|
| fix | none | `painted(301B vs source 301B): identical; hover(8678B vs source 8678B): identical` | 44 rows, 0 failed |
| A: source reworded | `'MUTATION-197 painted. Saving writes the document; …'` in `SHORT_BODIES.unbound` and `'MUTATION-197 hovered. Saving a preset does not install it; …'` in `RASTER_SECTION_BINDING_LIMIT` | PASS, `painted(323B vs source 323B): identical; hover(8700B vs source 8700B): identical` (+22 B each: the row followed the source) | 44 rows, 0 failed |
| B1: UI hovers something else | `BandPresetPanel.tsx`: `title={l.full}` → `title={l.full.slice(0, -1)}` | FAIL, `hover(8677B vs source 8678B): first difference at char 8677: got "" want "."` | 44 rows, 1 failed |
| B2: UI paints something else | `BandPresetPanel.tsx`: `{l.body}` → `{l.body.replace('BIND', 'bind')}` | FAIL, `painted(301B vs source 301B): first difference at char 125: got "bind it, …" want "BIND it, …"` | 44 rows, 1 failed |
| restored | none (rebuilt from committed baseline) | PASS, identical / identical | 44 rows, 0 failed |

One run after B2 was discarded. Its restore command was refused by the shell, so it measured
the B2 build again and was red with B2's line. The restore was then done as a separate
command, `git status` came back clean, and the tree was rebuilt before the restored run above.

## Row 198: EFFECTS-GUIDE-RETIRED-RULE

**Still true.** `docs/guides/effects-first-run.md` §6 (the markdown the Guide tab imports with
`?raw`) taught three things:

- the rule "only if it binds a preset that no other section binds";
- condition 1 cleared by "a programmer splits a preset record";
- condition 2 as a `preset()` that "threads the raster chooser on this index".

The strip on today's tree says otherwise (`src/renderer/components/effects/SectionPicker.tsx`
ConditionRow titles, backed by `sectionWiringConditions` in
`src/core/formats/effects/section-wiring.ts`):

- condition 1 is only that the section binds a preset record, and a shared record is `yes`;
- sharers "install the same band", and aeon refuses only sections of one record that name
  different preset documents;
- condition 2 is the record's own `preset()` passing `<chooser>(preset: <that record>_KEY)`
  with its own key.

Since aeon bcd844aa the chooser is keyed on the record. **One reading, not two:** the code
comments, the tooltip wording, the returned verdicts and `sharingSentence` all agree. §6 also
never mentioned region mode. On an act whose regions.json exists (OJZ act 1), 3f985513 and
`regionModeRasterNotice` show a single notice in place of the condition rows, and both section
dropdowns refuse a new binding.

**Change (cc70d682).** In §6:

- rewrote the rule blockquote, the sharing paragraph, and the table rows for conditions 1 and 2;
- added one paragraph saying the `own preset` label is older than its rule;
- "owns its preset" became "whose record is not threaded";
- added one region-mode paragraph;
- "bind a scene to any section" is now scoped to acts with no regions file.

§9 "a section aeon has not threaded" now says "whose record aeon has not threaded". The
guide-text gate's `OJZ_Preset_Plain` NOT_A_LABEL row was deleted because the gate itself
demanded it: the span left the guide along with the retired sharing example. No new
backticked spans were added.

**The test that pinned the wording.** `scratchpad/effects-guide-harness.mjs` row 3b typed the
old §6 rule as a probe. No vitest file pinned §6 wording.

**Change (10a21123).** Each 3b probe is now the first plain-prose line (40 or more characters,
no markdown syntax) under `## 3.`, `## 6.` and `## 8.` of the guide on disk. The run refuses
rather than use an empty probe.

**Proofs:**

| run | state | 3b | total |
|---|---|---|---|
| red: typed probe vs new guide | committed old harness (copied from `HEAD:` to a temp file and deleted after), new guide built | FAIL, `on disk 2/3; rendered [true,true,false]` | 12/13 |
| fix | derived probes | PASS, `on disk 3/3; rendered [true,true,true]` | 13/13 |
| M1: app renders a different doc | guide line `three sub-tab buttons. It stays put …` changed to `… MUTATION198 it stays put …` on disk, NOT rebuilt | FAIL, `rendered [true,false,true]` | 12/13 |
| M2: source reworded | same edit, rebuilt | PASS, probe reads `… MUTATION198 it stays put …`, `rendered [true,true,true]` | 13/13 |
| restored | `git checkout --` from committed cc70d682, rebuilt | PASS | 13/13 |

## Re-run (overseer)

```
VITE_AURORA_DEBUG=1 npx electron-vite build
AEON_DIR=<fresh git archive of aeon origin/master> ELECTRON_BIN=<main>/node_modules/.bin/electron \
  AURORA_BUILT_TREE=<this tree> npm run harness:band-preset     # 44 rows, 0 failed
AEON_DIR=<same kind of copy> ELECTRON_BIN=… AURORA_BUILT_TREE=… npm run harness:effects-guide   # 13/13
```

## Left open

- `effects-guide-harness.mjs` still opens the guide with `.click()` (row 3) and clicks the
  rail with `.click()` (row 4). Those are synthetic clicks, not CDP Input events. This is
  outside this parcel and was not changed.
- The other band-preset rows (3b, 3c, 3e) still assert copied phrases. They are green today,
  but they have the same shape that rotted 3a.
