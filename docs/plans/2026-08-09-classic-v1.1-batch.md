# Classic v1.1 Batch — Object Art, Composer Dock, Polish

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. User-approved 2026-08-09 (morning session). Builds directly on `docs/plans/2026-08-09-disasm-project-abstraction.md` (COMPLETE) — its ground rules (TDD, pure core, skipIf goldens, no Co-Authored-By, conventional commits) apply verbatim. Branch: `feature/disasm-project`.

**Goal:** Close the gap to the full "HyperSonic" feature set for S1 projects: objects render their real sprites, all level-art layers (tiles/blocks/chunks) are editable in a purpose-built composer dock with shared-structure UX, sprite editing is reachable from the object UI, plus the approved polish list.

**Key design decision (user-ratified):** level-art editing lives in a **self-contained composer dock inside the classic project view** — NOT wired into aeon's Art mode. S1's strict shared tile/block/chunk semantics get purpose-built UX: usage counts everywhere, shared-edit warnings, duplicate-then-edit, capacity-aware creation (chunks ≤ 127 — layout byte masks bit 7 for loops; blocks ≤ $400; tiles bounded by the act pool).

## Task B1 — Object art rendering (registry payoff)

- `src/core/project/profiles/s1-object-art.ts`: id → {artFile, mapAsm, frame, pal} transcribed at authoring time from the disasm's SonLVL objdefs (`_ObjDef/*.ini` direct entries; `xmlfile=` XMLs; `codetype=` C# sources — read all three flavors, transcribe what each declares; curate rather than exhaustively port edge cases; unlinked ids keep the hex-box fallback). Include zone-scoped overrides where the per-zone INIs redefine ids.
- Preview pipeline: render the declared frame via the existing S1 sprite adapter (mappings parse + nemesis decode) against the act's live palette (`pal` field = palette line). Cache per (id, palette-epoch). Pure core render helper + renderer-side canvas cache following the ChunkPicker pattern.
- Viewport: placements draw their sprite (respect obj xflip/yflip; anchor at obj x/y matching engine sprite origin per the mappings' signed offsets), selection box sized to the frame bounds; hit-testing upgraded from anchor-radius to frame bounds (this also subsumes the ring-footprint gap for $25 via its existing expansion). Library panel rows gain thumbnails.
- Tests: linkage-table shape test; preview render golden (skipIf) for 2-3 known objects (Crabmeat frame 0 non-empty, correct dims); hit-test bounds tests. Gate: full suite + a regenerated GHZ1 render with object overlay for visual check.

## Task B2 — Edit-art handoff (small)

- "Edit art" button on ObjectInspector + library rows (linked ids only): opens Sprite mode preloaded with that object's art+mappings via the existing open path (reuse the S1ArtSource capture — same code the manual pick uses), keeping guarded save-back intact.
- Test: handoff invocation wiring (store-level), no Sprite-mode regressions.

## Task B3 — Composer dock

- `ClassicComposerDock` in the classic view, three tabs sharing one selection context:
  - **Chunk tab**: 16×16 block-grid editor for the selected chunk (from picker/eyedrop); paint = pick block (mini block-palette with thumbnails + usage counts) + flips + solidity (S1 single-plane, 2 bits); one `classicEditChunkCells` per gesture. "Duplicate chunk" (new id ≤ $7F or refuse with message), "New blank chunk".
  - **Block tab**: 4-tile composer (tile picker from the act pool with usage counts, flips, palette line, priority); one `classicEditBlock` per commit. Duplicate/new block (≤ $400).
  - **Tile tab**: 8×8 pixel editor (16-color row from the act palette; pencil only v1 — no full aeon tool suite); one `classicEditTiles` per stroke; anim/gap tiles shown locked with the existing rejection reasons surfaced pre-emptively.
- Shared-structure UX: usage counts on every entity ("used in N chunks / M cells"); an inline warning banner when editing anything used >1 place; counts computed by a pure indexed helper (tested) with invalidation on the relevant commands.
- Viewport/picker/thumbnails all refresh via the existing chunkVersions/epoch keying (tile/block edits already bump the epoch).
- Tests: usage-index helper; per-tab gesture→command wiring (one undo step); duplicate/new capacity refusals; composer honors engine-id space (chunk $00 not editable).

## Task B4 — Polish batch

- Loop-chunk authoring: picker gains a "loop" toggle on the armed chunk (stamps id | $80 where the chunk id ≤ $7F; display loop cells with a corner glyph); eyedrop preserves the flag into the armed state (replaces the v1 toast).
- Palette panel: 4×16 swatch grid in the classic right dock; click → the repo's color-picker idiom → one `classicSetPalette` per line-commit gesture. MCP tools `set_palette {line, colors}` + `set_start {x, y}` (engine-id/batched conventions; docs/MCP.md).
- Consolidation: `screenToWorld` → reuse `core/art/camera.ts`; single `clamp` helper; `CHUNK_PX` single export. Stale test-only export audit (keep `clearAdapters`, document).
- Docs: spec header v1.1 note; ROADMAP §2.5 updated (deferrals closed: composer, palette, loop authoring, object art); MCP.md.

## Sequencing & gates

B1 → B2 → B3 → B4, sequential (same repo). Every task: implement → spec review → quality review → fixes, per subagent-driven-development. Batch gates: full suite + tsc + build green after every task; `verify-s1-roundtrip.mjs` PASS after B3/B4; regenerated renders (GHZ1 FG + objects overlay) eyeballed by the controller; GUI verification by the user at the end.

## Completion status

- [x] **B1 — Object art rendering** (commit `822b2e0`, fix `c88793b`). Placed objects
  render their real S1 sprite art; frame-bounds hit-testing; library thumbnails.
- [x] **B2 — Edit-art handoff** (commit `8bfd1b9`, fix `1c32f70`). "Edit art" opens
  Sprite mode preloaded with the object's art+mappings.
- [x] **B3 — Composer dock** (commit `1cae5ea`). Chunk/Block/Tile tabs with shared
  selection context + usage-count / shared-edit UX.
- [x] **B4 — Polish batch** (this batch — commits: features / consolidation / docs).
  Loop-chunk authoring, palette panel, MCP `set_start` + classic palette tool,
  unconditional Duplicate buttons, nibble-helper extract+tests, Escape gesture
  cancel, `screenToWorld`/`clamp`/`CHUNK_PX`/blit-helper dedup, dead-code removal,
  composer split into tab files, hidden aeon-only sprite panels, docs.

**B4 deviation notes:**
- **MCP palette tool named `set_level_palette`, not `set_palette`.** The aeon
  `set_palette` already owns that name in the flat `EDITOR_METHODS` registry and MCP
  `registerTool` throws on a duplicate name, so the classic palette tool takes a
  distinct role name (it also legitimately differs: it allows palette line 0). `set_start`
  kept its planned name (unique). Documented in editor-methods, MCP.md, and the ROADMAP.
- **Loop-flag layout-cell validation was tightened, not merely "confirmed".** The plan
  expected `classicSetLayoutCells` to already accept a raw loop-flagged byte; in fact
  only `validateLevelDoc` (structural) did — the command's per-cell guard capped at
  `doc.chunks.length` and would have rejected `id | $80`. The guard now validates the
  MASKED engine id (`& 0x7f`) against the pool while accepting the full 0..$FF byte, so
  the loop bit rides through to save (render/collision already mask `& 0x7f`). A
  byte-level store test pins that the raw flagged byte is stored.
- **Reusable color picker extracted to `GenesisColorSliders`** (art-shared) rather than
  refactoring the aeon `PaletteEditor` — reuses the CONTROL idiom decoupled from the
  aeon stores without risking aeon regressions (as the plan directed: "reuse the picker
  CONTROL only, not the aeon-coupled editor").
- **Store `classic-commands.ts` extraction intentionally NOT done** (kept B4 scoped, per
  the controller's instruction; noted for a later batch).
