{"at": "2026-08-24T01:59:38Z", "headline": "Nothing about the game changed tonight. This was record keeping, plus one finding about our own tools that is worth knowing.", "matters": "Two things you asked for are now in place: the questions this lane needs you to answer are written out as real choices with costs, and finished work now publishes without asking you first. The finding is less comfortable. The way this editor describes how it finds the emulator looks like it was copied from another tool's documentation rather than measured against our own code, and that description has been handed to other lanes as fact for months. Nobody has checked it yet, and it is now written down as an open question instead of being quietly fixed.", "detail": "Seven commits pushed 58ff2ae..2448969, verified by ls-remote. decisions.jsonl opened with three entries; d-2 and d-3 answered same day and recorded as d-4/d-5. ROADMAP item 36 booked, narrowed, then reopened as fully open after the oracle lane withdrew a transferring result. OVERSEER.md: retracted half this lane's own cutover framing (the shim is a client; evidence about it is not evidence about the server) and banked oracle's binary-freshness answer with their stated limit. No src/ changes. Item 35 dispatched after this push.", "refs": {"commits": ["a352a8e", "de6d31f", "5670a6f", "44bf53d", "7fc5e52", "79b307b", "2448969"], "queue": ["35", "36"], "project": null}}
{"at": "2026-08-24T02:19:59Z", "headline": "The editor was writing a background drift value the game could not read, and now it writes one it can. Every scene made in this editor carried it.", "matters": "When you set how far a background drifts up and down, the editor used to offer a list of named choices, and the name it started on was one the game reads as nonsense. The game did something that looked almost exactly like pinning the background still, with a one pixel wobble, which is why it went unnoticed. The setting is now a plain number box from 0 to 15, the saved test scenes that carried the bad value are corrected, and the scene built by hand in the editor was rebuilt in the editor rather than patched by hand. One thing is still unproven: nobody has watched the fix on real hardware or in the emulator, so what is certain is that we now write a value the game accepts, not yet that it looks right on screen.", "detail": "ROADMAP item 35. Schema re-vendored by extraction from empyrean a32bcb03, hash-gated at cab3ca58 rather than hand-edited; $defs/factor untouched, so fa/fb/curve.to are unaffected. Old blob 2d7a9fee was cited in 7 files/10 lines, not the 4 predicted; three doc citations left alone deliberately as historical record. Brief was wrong twice: canopy_dusk's own v_factor and writer_session's v_factor were also live defects, both ratified. Writer fixture RE-ORIGINATED under CDP per its provenance, not edited; exactly one line moved (FACTOR_3_4 to 8). Overseer re-verified on the merged tree: 4468 passed/3 skipped/0 failed across 350 files, tsc clean, harness 25/25 in foreground with emitted output byte-identical to the committed fixture, and the drift gate poisoned red-first and restored. Engine half unrun.", "refs": {"commits": ["e6434a1", "44cad84", "7eda34f", "427cbd1", "e4705d9", "2400ba8"], "queue": ["35"], "project": "EFFECTS-W1"}}
{"at": "2026-08-24T13:08:34Z", "headline": "A moving background band built by hand in the editor now survives the whole trip into the engine's own tooling \u2014 and the test that was supposed to be checking this was broken.", "matters": "Until today the band path had only ever been proven with a band the code built for itself. Now one made the way an author would make it \u2014 typing the size, clicking Promote, pressing Ctrl+S \u2014 goes through the engine's own converter and comes out accepted, with the background picture pixel-for-pixel unchanged. The uncomfortable half: the check that judged this had a number typed into it that only matched one particular band size, so it called a perfectly correct result a failure, and would have done that for every band except the one it made itself. That is fixed and the fix is proven not to have made the check softer. Still not proven: none of this has been assembled into a ROM or run on the console, so the band has never actually moved on screen. Separately, the engine side has quietly fixed the thing that was blocking new bands: their art tool now keeps 128 tile slots back automatically. The existing background art has not been redone, so nothing is free yet, but the road you approved is now paved.", "detail": "ROADMAP items 29 and 24. New scratchpad/bganim-ui-authored-composition-harness.mjs (CDP, 18/18) feeding the existing bganim-promoted-vs-aeon-injector.py (all rows as expected). Hermetic: temp copy of aeon's project, override taken from ls-remote-resolved 81f6ce7d, aeon's tree never written, so the staleness and anims-refusal traps could not fire. Results: BgAnim_Table 0 -> u16 = 1, tiles delta 0, bg_anim_banks.bin 512 B = cols*rows*PHASE_BANKS*TILE_BYTES, 0 raw nametable words and 0 resolved cells differ. Instrument defect: exp = 8*4*8*32 hardcoded; repaired to derive from the band under test plus the vendored contract; poisoned red-first (PHASE_BANKS=4 -> expected 256 got 512) and restored; control re-run of the original 8x4 model band still yields 8192 and 2464 nametable words. Currency: inject_editor_bg.py byte-identical between pinned 9b3f11f6 and tip; vram_map.py moved but BG_TILE_BASE_SLOT still 1024. Row 24: aeon added BG_BAND_RESERVE=128 / BG_STATIC_TILE_BUDGET=320 and png_to_bg_override.py refuses above the budget; shipped override still 448 tiles, no anims.", "refs": {"commits": ["5bbd3f9"], "queue": ["29", "24"], "project": "EFFECTS-W1"}}
{"at": "2026-08-24T13:12:55Z", "headline": "Buttons in a panel's title bar no longer fold the panel shut when you click them.", "matters": "Adding a layer or deleting a scene used to also collapse the panel you were working in, so on an odd-numbered click the panel would slam shut on the thing you had just made \u2014 the heading would say eight layers over an empty box. That is fixed for every button in every panel title bar, including ones nobody has written yet, rather than patched on the two that were misbehaving. Plain text counters in a title bar still toggle the panel as before, which is deliberate. I checked it both ways on the merged code: with the fix every one of the thirty-one checks passes, and with the fix taken back out the exact failure you originally saw comes straight back. One thing I chose not to do: these panel headings still cannot be operated by keyboard at all, which is a separate and older problem, and I have written it down rather than quietly widening this fix into it.", "detail": "ROADMAP item 32, merged 8a3bbd7 (fix 5041f6e, harness 18bf2fc). onHeaderClick + isHeaderAction walks target->header, stopping at the header rather than closest() so a nested section cannot get a dead toggle; portal targets refused. Overseer re-verified on the merged tree, both frames: 31/31 green, 24/31 red with only the onClick reverted, same 7 rows, 4b reproducing 'Layers (8/8)' over 0 cards. Suite 4468 passed/3 skipped/0 failed, tsc clean. Load-bearing observable is the persisted panel-state write, not the DOM (unmount re-reads state; nothing un-writes localStorage). Harness asserts after every click because in the red frame click 2 passes - the even toggle cancels the odd one. Row's own blast radius corrected 3->4 call sites after independent re-derivation; Explorer was missed and is also the live collapsedOverride example. Keyboard accessibility and the unmeasured portal branch both booked, not done.", "refs": {"commits": ["8a3bbd7", "5041f6e", "18bf2fc", "c9a0dad"], "queue": ["32"], "project": "EFFECTS-W1"}}
{"at": "2026-08-24T14:45:37Z", "headline": "The editor was telling agents the wrong size limits for a level's background \u2014 one too generous, one too tight.", "matters": "A background picture has a hard ceiling on how many unique tiles it can use, set by where the console keeps its sprite table. The editor told agents that ceiling was higher than it really is, so an agent could build a background the console physically cannot hold and only find out later, at bake time or as garbage sprayed over the sprites. In the same breath it was too strict about height: it insisted every background be half the height the engine actually supports, which made a full-height background impossible to author through the agent path at all \u2014 even though backgrounds that size already exist in the library. Both numbers now come from the shared file that records what the engine really does, so the limit the editor enforces, the limit it advertises and the limit the engine holds are one number that cannot drift apart. Before this there was not a single test anywhere covering reading or writing a background through the agent path; there are now, and I checked them by breaking the code on purpose and confirming the right ones failed. The one thing left unproven is a full-height background making the whole trip through a real save and into the engine's own tooling \u2014 that needs the app running, so I have written it down rather than implying the tests covered it.", "detail": "ROADMAP item 8, merged 06bb119 (fix 1d0a770, first tests 258ebab, docs 18f0574, two more stale comments 1d7f4f1, count correction 484254d). Six sites; the gate with teeth was src/main/editor-methods.ts's zod .length(2048)/.max(512) in the MAIN process, one layer before the handler and never named in the row. Now derives BG_TILE_CAPACITY/BG_LAYOUT_WORDS/BG_LAYOUT_WORDS_LEGACY/LAYOUT_WORD_MAX/LAYOUT_TILE_INDEX_MASK/TILE_PIXELS/TILE_PIXEL_MAX from bganim-consumer-contract.json via bg-override.ts. get_bg measures height off the act's layout, null when absent. Height widening NOT blocked: every downstream stage already derives height; agent-handler was the only module believing 32 rows. Overseer verification on the merged tree: baseline on master 99bc74c measured firsthand at 4468 passed/3 skipped, merged 4488/3/0 (+20), tsc clean, build clean. Independently re-planted the tile ceiling (448->512): 3 rows red incl. one that discriminates 448 from the old 512, exact assertion 'promise resolved { tiles: 449 } instead of rejecting'; restored clean. Ran a DIFFERENT enumeration frame than the agent's (contract numbers as raw literals, any file type) to test their completeness claim: surfaced 0x7FF in bg-tiles.ts as a candidate duplicate, then dismissed it \u2014 0x7FF is the VDP-wide 11-bit index field used at 17 sites incl. sprites and FG, not a BG-contract number, so leaving it unpinned is correct and pinning it would over-couple. bg-tiles.ts's BG_TILE_BASE_SLOT=1024 IS a genuine second copy and is pinned by drift test rather than imported, deliberately, since bg-tiles.ts is also the classic/S1 loader. Cross-repo sweep for get_bg consumers outside aurora: none read the reply's height field, so making it nullable breaks nothing (only hit is an empyrean status-audit doc listing the method name).", "refs": {"commits": ["06bb119", "1d0a770", "258ebab", "18f0574", "1d7f4f1", "484254d"], "queue": ["8"], "project": null}}
{"at": "2026-08-24T16:27:04Z", "headline": "I tried to build a ROM with a moving background band. It doesn't build \u2014 and finding out why was the point.", "matters": "Nobody had ever put an editor-made animated background into an actual ROM, so nobody knew what would happen. I did, and it stops in two places, both on the engine side. First, the engine's tool writes a reference to the animation artwork in a form the assembler won't accept \u2014 I found the correct form by looking at a neighbouring file doing the same job, and confirmed it fixes that step. Second, the table that decides how much room each piece of the ROM gets allots this feature exactly two bytes, because two bytes is what the switched-off version takes up; a real band needs about eight thousand. Neither problem is ours and neither could have been found any other way than by trying. Along the way I told you two things that were wrong: that there was spare room nearby (there isn't \u2014 what looked like spare room is other artwork, including the very background tiles this whole effort has been about), and that the knock-on effect was contained (it isn't, though it turned out smaller than feared \u2014 five pieces shift, not fifteen, because a sound-related address is pinned in hardware and absorbs the rest). The engine lane is doing both fixes as one job. When it lands I run this again and you finally get to watch the background move.", "detail": "ROADMAP 29-ROM. Isolated worktree of aeon at origin/master 5349bea4; aeon's live tree never touched. Band via the item-29 path on the real 448/448 doc: promote 8x4 timer, tiles.length unchanged; regenerate-level.sh re-baked + verify_level_bin OK; injector wrote BgAnim_Table=1 and an 8192B bank blob = cols*rows*BANKS*32 derived. DEFECT 1: bg_anim.emp:10 emits bare-name `BgAnim_Banks + N` -> 8x unknown-name. NOT forward-ref (tested: reordering reproduces identically). sec_local_maps.emp:20 uses extern(\"NAME\"); rewriting to extern(\"BgAnim_Banks\")+off resolves. bg_anim.emp:10 is the only bare-name data-symbol arithmetic in the tree. Discharges the injector's own :152-159 booking (FORMAT-FAITHFUL BUT NOT BYTE-PROVEN). DEFECT 2: ojz_bg_anim [0x3B270,0x3D29E) overlaps test_mappings [0x3B672,0x3B6A2). NOT the deferred map.toml row (order keys on head-labels, map.toml:73; BgAnim_Table present at :120; the reserved slot at :106-111 names ojz_effects_editor_act1/effects_gen.py). Cause: golden offcanonical_sizes/s4_debug.txt gives BgAnim_Table 0x27e70 -> Map_TestObj 0x27e72, exactly 2 bytes = the stub (u16=0 + Data.empty); real band ~8250. Fix = re-derive via derive_offcanonical_sizes.sh, not relocate. MY ERRORS, both corrected: (a) quoted 22664/3082 as free space -- bases-only table, gaps are allotments; OJZ_Palette's 22664 holds 22658 of content incl. the 14338 BG tile blob, 6 bytes slack; my own 2-byte proof was the refutation and I read past it. (b) withdrew the cascade prediction on a VACUOUS query -- grepped golden bases for a span taken from build output; golden has zero labels in 0x38000..0x40000, so it could only return 0. Right observable: BgAnim_Table is label 65/80, 15 follow. Cascade settled by aeon reading map.toml:166-176: [[anchor]] dac_banks at=0x48000 hardware-pinned (Z80 SetBank latches LMA), so 5 shift not 15; ~119KB genuine slack in the HeightMaps->Dac_Temp_Blip span. Bounded re-derivation still writes sigil's golden fixture, so the three-lane question is priced down, not withdrawn. Nobody contacts sigil until aeon's parcel lands.", "refs": {"commits": ["f6c568f"], "queue": ["29-ROM"], "project": "EFFECTS-W1"}}
{"at": "2026-08-26T01:26:51Z", "headline": "A background parallax scene you build in Aurora now reaches the game running in the emulator.", "matters": "You asked more than once to see the effects work in the emulator. Tonight the whole chain ran for the first time: five scrolling depth bands were authored in Aurora's Effects panel for the start of act 1, saved, built into the ROM with one Build & Run press, and reloaded into the window you were watching. The engine measurably picked the authored scene, and each band scrolls at exactly the speed typed in. Two engine-side gaps were found and fixed on the way (a missing layout-table row for the first real scene, and the engine preferring its built-in preset over the editor's choice) \u2014 both aeon's, both done tonight.", "detail": "scene ojz_act1_start (fb 1/16,1/16,1/8,1/4,1/2, anchor ch0), sceneRef on ojz/act1 section_0; aeon content/ojz-act1-start-scene @ 7562fe3d incl. parcel/parallax-section-precedence; verified Parallax_Current_Config==EditorSceneBinding_OJZ_Act1_Sec0 (0x12C96), Scroll_B -25,-25,-51,-103,-207 @ Camera_X 414; ROM s4.debug.bin 715114 B; Build & Run driven via editor/build_and_run over the running Aurora debug session; no Aurora code changed"}
{"at": "2026-08-26T02:22:36Z", "headline": "When the editor can't reach the emulator it now says why \u2014 a stale socket file versus no socket at all \u2014 instead of a bare 'disconnected'.", "matters": "Three lanes spent an evening reading different symptoms off one machine because the editor threw the real error away. The steps other lanes are told about were also checked against the code: the list was right, the implied 'try the next one' was not, and the docs now say so.", "detail": "code 25d2da4; socket-dead-link.test.ts red-first verified by overseer; suite 4490/0/3; opened ROADMAP 39 (attach vs protocol \u00a77.1 own-instance default)"}
{"at": "2026-08-26T02:50:35Z", "headline": "The small file that records which background, palette and effects scene each level section uses now always comes out in the agreed layout, and a test would catch it changing.", "matters": "It was in the agreed layout by coincidence \u2014 adding one more field in the obvious place would have quietly broken the rule the engine's tools rely on. Existing files come out byte-for-byte the same.", "detail": "code 2af977f; suite 4492/0/3; one test row disclosed as non-discriminating in the ROADMAP row"}
{"at": "2026-08-26T02:51:40Z", "headline": "The two effects-panel number boxes that let you type a value the game could not hold now stop at the game's real limits.", "matters": "Before, a scene could save with a vertical offset the ROM build would reject much later with an error pointing at generated code. The limits come straight from the shared schema aeon ruled tonight, so the editor and the engine cannot disagree about them.", "detail": "code b6355d5; schema blob d4345af5 from empyrean 5c930d6; suite 4495/0/3; two NumberField sites elsewhere flagged as candidates (ObjectInspector:128, BgAnimBandPanel:311), not fixed"}
{"at": "2026-08-26T04:05:34Z", "headline": "A brand-new animated band can now be added to the background in the editor, once the background art leaves room for it \u2014 proven on a test copy of the real art simplified to fit, with the engine's own tooling accepting the result.", "matters": "Before, the real background used every tile slot, so the editor could only animate art already there. This shows the whole insert path works the moment the shipped art is regenerated with room kept back (the engine's generator now does that automatically). The shipped art itself is untouched and still yours to decide. One catch found on the engine side: its band injector still refuses anything bigger than three tiles wide because of a leftover limit from before the assembler learned to place things itself; aeon is removing it now.", "detail": "merge c04ff20 (parcel/24-roomy-bg-insert: 74410b4 fixture+simplify, add8c43 tests, 504471b harness, 15090b4 probe flag, ca33778 roadmap). ojz_forest_flowers.png reproduces the live 448-tile blob byte-for-byte; bg-simplify.py greedy flip-canonical merge -> generator at a840d68f prints 'unique tiles: 320/320'. Fixture test/fixtures/bg-override/editor_bg_override.roomy.json sha256 a21c9505... with provenance sidecar. Merged tree: tsc clean, vitest 4502/0/3. Foreground: bganim-insert-roomy-harness 36/36 (BAND=3x1, both states); injector probe AEON_REV=a840d68f all rows as expected (banks 768, tiles +96, 0/4096 resolved diffs). Finding sent to aeon-f2: inject_editor_bg.py:111 BGANIM_PLACER_CEILING=1026 still refuses >3 slots; they are retiring it (parcel/bganim-room-retire)."}
{"at": "2026-08-26T05:32:35Z", "headline": "A background band saved from the editor can now actually move \u2014 you pick how its animation frames are made, and 'shift' generates them as one-pixel steps of the art.", "matters": "Until now every band the editor saved had eight identical frames, so it would have sat perfectly still in the game however it was set up \u2014 the missing piece before you can watch one move. The old behaviour stays the default, so nothing that already exists changes.", "detail": "merge f45d67d (parcel/29-band-phase-shift, 5 commits) plus overseer control 0394d4c. phaseFill copy|blank|shift on both doors, provider, both agent methods and a panel selector. Direction/wrap/bank count derived from aeon a840d68f (bg_anim.emp header; forest_bg_gen pat_pixel (v+ph)%PAT_W) and re-verified firsthand by the overseer. Merged tree: tsc clean, vitest 4516/0/3 (+14). Foreground CDP harness 18/18 on the live 448/448 document. Independent control scratchpad/bganim-phase-roll-control.py: REAL pass, copy-forgery rejected, opposite direction rejected."}
{"at": "2026-08-26T06:21:52Z", "headline": "A moving background band built in the editor now runs in the game. It was put into a real ROM, and the animation was watched frame by frame in the emulator.", "matters": "This is the last piece of the background-effects road. Until tonight a band had never actually run: the editor could save one and the tooling would accept it, but nothing had ever assembled it into a ROM or seen it move. Now the canopy in the jungle level animates, all eight of its frames play in order, and the picture underneath is untouched. You can look at it when you wake \u2014 and the same route now works for any band you author.", "detail": "Overseer foreground probe at aeon pushed bc95e32e in an isolated checkout (no collision with their live tree). 8x4 band promoted from tile 2, phaseFill 'shift', timer/rate_shift 3. Baseline s4.bin crc b96319e3 699408 B; with band crc 4b85b9ce 699444 B. ROM grew only 36 B because the 8238 B section expands into pre-existing $00 fill under the dac_banks anchor \u2014 CRC alone therefore proves nothing, so the section size was confirmed by aeon's own bganim_room.py against the built listing ('8238 B the section already holds') and by bg_anim_banks.bin = 8192 B, matching 2+44+32*256 derived from their constants. Animation verified against all 64 (bank,rotation) pairs \u2014 64 distinct images, 28 samples, 0 unmatched, all 8 banks visited, coarse rotation stepping exactly as the fine phase wraps. Packing established by control first (40/40 static tiles under hi_lo, 0/40 lo_hi); the initial 'art nowhere in VRAM' result was an unverified-packing artifact, not a finding. Caveats: FAST=1 skips the verification lanes and sigil reports 0.1.0 ac84536f-dirty, so this is a proof artifact, not a landing artifact. Commit e0f5745, pushed. Finding relayed to aeon: planBandInsertion never points a layout cell at new slots, so an inserted band is invisible; promotion is what is watchable."}
{"at": "2026-08-26T06:36:11Z", "headline": "The band you'd actually build in the editor \u2014 clicked together in the real panel and saved \u2014 now runs and moves in the game. It is ready for the engine side to put in the game for good.", "matters": "This is the whole background-effects road finished end to end. The band was made the way you would make it, through the panel, not by a script standing in for you \u2014 and the file the panel saved turned out to be identical, byte for byte, to what the internal path produces, which is the strongest form of 'the editor and the engine agree'. It is committed and the engine lane is expecting it.", "detail": "Merge 54a7b2c (parcel/handover-band); suite 4516 passed / 3 skipped / 0 failed on the MERGED tree (same count as before the merge \u2014 the deliverables are a CDP harness and a data fixture, not vitest rows). Fixture editor_bg_override.handover-band.json, 110,660 B, sha256 9d05f512..., byte-identical to the model path verified with an independently written emitter. Built at aeon's pin in an isolated checkout: verify_level_bin OK, crc 7e889eca, banks 8192 B, section 8238 B. Animation: 28 samples, 0 unmatched, all 8 banks, rotations [0,6,7]. RATIFIED deviation: rate_shift 3 unreachable in the UI, key left out, aeon default 2 applies; opened ROADMAP 44 for the missing panel control. CAUGHT AND FIXED: the probe first committed was the naive bank-only version and produced a FALSE NEGATIVE against the handover band while the write-up beside it described the rigorous 64-pair method; replaced at 703da75 with the control + pair model + vacuity refusal. Also caught mid-landing: merging the worktree-agent-* branch was a silent no-op that exited 0 (the work was on parcel/handover-band), so the first 'merged-tree' suite run was actually an unmerged baseline."}
{"at": "2026-08-26T13:30:52Z", "headline": "Six items on the plan were still advertising work that is already finished and shipped. Fixed the plan, not the code.", "matters": "A finished item that still reads as open is worse than one nobody wrote down: it is written to be picked up cold, so the next person to look \u2014 or the next session I start \u2014 would have gone and redone work that was already on master. One of them told a reader the editor cannot add a background band at all, which stopped being true days ago. Six rows also rendered as broken table lines. Nothing about the app changed.", "detail": "docs/ROADMAP.md sec 5.1 rows 9, 21, 22, 24, 32 had stale FRONTS (the repo's OVERSEER.md notes the front is what a scanning session reads, and later corrections get appended to the back where nobody looks); rows 22 and 32 carried no delivery marker at all. Every cited SHA was --stat-checked as a code/artifact commit and confirmed an ancestor of master before being written, per bar 9b: 0376039 (canonical-json chokepoint, discharges 22), 5041f6e + harness 18bf2fc (discharges 32), 55be1d1 + 5e347fe (promotion costs zero slots, discharges 24 structurally; owner d-10 discharges it materially), 253cb0e (MapViewport 37/37, closes 9's measurement half; the ruling closes its design half; item 42 is the build half). No row body altered - each old tail verified preserved verbatim by string containment before commit. Rows 14/24/32 were missing trailing table cells; added. Row 14's reserved owner half deliberately LEFT OPEN with a scope note: d-10 answers the tile-famine question it was merged with, but that is a different art loss from the 2026-07-21 collision this row reserves. Commits 71e03e1, dbf440f; pushed, remote confirmed by ls-remote."}
{"at": "2026-08-26T13:46:25Z", "headline": "You can now set how fast a background band moves. Until today every band the editor made ran at exactly one speed.", "matters": "This was the one gap left by the band you watched animating in the game last night \u2014 that band was supposed to move at a chosen speed and the panel had no way to say so, so it took the default. The control warns you that the number runs backwards: a higher setting is a SLOWER band, which is the opposite of what a speed field usually means. Leaving it alone keeps the key out of the file entirely, so a band you never thought about the speed of keeps following the engine's own default instead of being frozen to today's value.", "detail": "ROADMAP item 44, branch feat/bganim-rate-shift-control, merged e085514 (c022f40 control + 9 unit rows, a3b1681 CDP harness, afc5cc9 tsc fix). The row was HALF STALE and the parcel says so: BandSpec.rateShift was already threaded into both command doors, into createBand, the codec's BAND_DEFAULTS, both agent doors, and the panel's read-only display \u2014 only the form control was missing, which is why ~4,400 green tests never noticed (bar 1). Two-part control copying the driver picker: '(default \u2014 N)' omits the key, 'custom\u2026' reveals a clamped box. DEFAULT_RATE_SHIFT = BAND_DEFAULTS.rate_shift, no literal 2 on the surface. clampRateShift is the real bound (item 37: min stops nothing typed); NO upper bound invented \u2014 contract says nonNegativeInt with no max. Agent DECLINED to invent a 'too slow' threshold because the contract carries no driver width to derive one from: correct call, ratified. VERIFIED BY THE OVERSEER ON THE MERGED TREE, not taken on report: full suite 4523 passed / 3 skipped, tsc exit 0, and the CDP harness re-run at 30/30 under xvfb. Independent poison by a DIFFERENT enumeration parameter than the agent's \u2014 they poisoned the implementation, I moved the vendored contract's default 2->5 \u2014 which produced exactly one failure, the contract drift gate firing correctly, proving no test pins the literal 2 and that the drift gate is not vacuous. Contract restored, suite green. NOT VERIFIED ON HARDWARE and tagged: no ROM built, so 'rate_shift 3 is half the speed of 2' rests on aeon's cited step = driver >> rate_shift, not a watched band."}
{"at": "2026-08-26T14:20:30Z", "headline": "Bands now move inside the editor \u2014 and building it turned up that the editor has been showing a different background than the game builds.", "matters": "You asked to preview how the bands move without going to the emulator. That works now: switch on 'Play bands' and a timer band animates, while a camera-driven band correctly does NOT move on a clock \u2014 it moves when you pan, because that is what drives it in the game. With the preview off the viewport costs nothing, exactly as before. The catch is the finding underneath: the picture on the map canvas and the background the ROM is built from are two different files that nothing has ever connected, so on your real project the preview refuses rather than guessing. It says why on screen. Which of the two backgrounds should win is filed as d-11 and is yours to answer.", "detail": "ROADMAP item 42, branch feat/bganim-motion-preview, merged fa2660f. Governed by docs/reviews/2026-08-22-preview-posture-ruling.md: playAnimatedArt promoted into OVERLAY_KEYS_BY_ENGINE.aeon OFF by default, clock local to the viewport, camera bands clockless. THE PARCEL REFUTED ITS OWN BRIEF \u2014 I briefed 'the background is already on screen, you only change which phase is painted'; MapViewport paints act.bgTiles/bgLibrary while a band indexes bgOverride.doc.tiles and nothing relates them. Verified independently by me: 0/320 by index, 320/320 as a set, decoded past the 2-byte BE header (my first pass was byte-offset and would have falsely contradicted the agent); no library entry matches by index at all; and no aeon tool reads the library, only inject_editor_bg.py reading the override. Agent correctly refused to build the unguarded substitution \u2014 every band is licensed by the contract's own prefixIdentity against the DISPLAYED blob, a refused band claims no cells and keeps no clock alive. Also corrected the ruling: the editor pan maps to Camera_X by IDENTITY, not zoom-scaled with its own origin (viewStore.ts:116 divides on the way in; SECTION_PIXEL_SIZE 2048 = aeon's $800); amended in place at the point of the error since item 43 reads that doc next. VERIFIED ON THE MERGED TREE BY THE OVERSEER: suite 4558 passed / 3 skipped (baseline 4523), tsc clean, and the CDP harness re-run at 23/23 under xvfb \u2014 row 2a 0 repaints over 4s idle against 1280 rAF ticks with the zero EARNED by row 3a reading 90 on the same counter; 3b 15.0 repaints/s matching the derived rate, not 60; 3d the camera band holding 1 pixel state while the timer band shows 17 in the same window on the same instrument; 4a/4b/4c/4d pan 4px changes phase, 3px does not, a full pattern period returns to phase, vertical does not move a camera_x band. 18 red-first plants restored, and two GREEN poisons reported with the alternative path ruled out. NOTE: the harness runs on a fixture, because the live project cannot exercise the feature until d-11 is answered. Incidental fix folded in: MapViewport's ResizeObserver held a drifted near-copy of the draw body passing undefined for object sprites, so a window resize silently downgraded every object preview to a box."}
{"at": "2026-08-26T14:39:09Z", "headline": "The messy right-hand panel is tidied \u2014 one label column, one rhythm, and the creation form now starts closed instead of eating a third of the space.", "matters": "This is the panel you pointed at. The three lists in it were each pinned at their minimum height with their own little scrollbars inside a column that was already too tall \u2014 four scrollbars, three of them stubs, which is what 'messy' actually was. Now one list gets the room, the rest are plain sections, and the New band form arrives closed because it alone was 38% of the column. One honest caveat: the motion preview built alongside this added its own section, so the column still runs over by about a screen-inch and I have booked the fix rather than papering over it.", "detail": "ROADMAP item 41, branch fix/effects-panel-tidy, merged 3127acc. New effects/column-layout.tsx declares the column geometry once for both panels (Field/Row/Hint/Group/Card); LABEL_W 64 derived from a DOM-Range measurement of every rendered label. Layers keeps variant='list'; Scenes and Bands demoted to content; New band defaultCollapsed. MY DIAGNOSIS HELD HALF: all three list sections sat at exactly the 160px floor inside a 292px overflow \u2014 confirmed. But 'mixed label widths' was wrong: every row already placed its control at exactly 72px; what is ragged is four rows carrying a SECOND label mid-row, which no shared width can reach. The agent added L3 for that and it was the red row. VERIFIED BY THE OVERSEER ON THE MERGED TREE: suite 4558 passed / 3 skipped, tsc clean, effects-column 19/19, and the cross-parcel checks \u2014 bganim-motion 23/23 (42's preview survives the restructure) and section-header-action 31/31. LANDING CORRECTION I MADE: the harness's A1 arrival guard FAILED on the merged tree at 18/19, correctly \u2014 42 added an eighth section after 41 branched, so the intended arrival state had changed under the guard. Ruled Band preview arrives OPEN and recorded the reasoning in the harness rather than silently editing the expectation; annotated row 41 that its 0px overflow is branch-true and master is 285px; booked item 45 to fold the duplicated per-band cards together, gated on d-11. TWO SILENT GREENS THE PARCEL EXPOSED BUT DID NOT CAUSE: the item-32 harness had been 22/31 since aeon's tree gained a scene, because it addressed its fixture as scenes[0] \u2014 nine rows reading a different scene's layer count while the panel behaved perfectly; repaired to address by id, verified here. And effects-scene [4a] had been passing because its /fa/ matcher matches the word 'default' (bar 2c). Agent also self-reported two sed -i edits that briefly landed in the shared checkout; I verified the shared tree is byte-identical to HEAD."}
{"at": "2026-08-26T18:40:00Z", "headline": "The map now shows you the background the game actually uses \u2014 and painting it finally changes the game.", "matters": "You had two copies of the Oracle Jungle background and they were not the same picture. The editor drew one; the ROM was built from the other. About 1,200 of the map's 4,096 background squares showed different artwork from what the console would show, and nobody could have spotted it by looking. That is now over: the map paints what ships. The moving-band preview you asked for, which refused to run on your real project for exactly this reason, now runs on it and the band moves. The uncomfortable half is what we found on the way in: painting the background in the editor has never reached the game. It edited a picture file the engine does not read, so every background stroke anyone has ever made was thrown away at build time. That is fixed in the same change \u2014 a stroke now edits the file the ROM is built from, and you can see it land. And one thing is now refused out loud instead of quietly going wrong: the tile chooser in background mode is showing you the FOREGROUND tiles, which are different art, so about two thirds of the tiles you could pick do not exist in the background at all. Picking one of those used to write a number the console would turn into garbage; it now paints nothing and tells you why. Giving background mode its own tile chooser is written down as the next job rather than bodged in here.", "detail": "Branch feat/canvas-paints-rom-background, five commits (c1095b8, 2480d02, e6fb5e7, b8eed7f, 1dc46e0) plus docs. Implements owner decision d-12 (supersedes d-11). Binding DERIVED: aeon's inject_editor_bg.py hardcodes one output directory; actBindsBgOverride matches that vendored literal against project.json's per-act stripPath, so every other act keeps its library/act-default background. Seam reported to aeon: two hardcodes, one per repo. Paint retargeted to a new set-bg-override-layout command; apply and undo share one writer so the document and the canvas mirror cannot disagree. Verification: suite 4593 passed / 3 skipped (baseline 4558), tsc clean; new CDP harness scratchpad/bg-override-paints-harness.mjs 20/20 across three trees (live read-only, an act retargeted off the injector's directory, a writable copy read back off disk after a real Ctrl+S); AEON_LIVE=1 bganim-motion-harness 18/18 with 2 camera-only rows noted, fixture run unchanged at 23/23. Five red-first plants, all restored. One alternative green path found and closed: reverting the command target left four paint rows green because the live stroke writes the document directly, so row 3e2 (the act's own binaries) was added and it goes red. ROADMAP items 46 (done) and 47 (the picker mismatch) added; item 45's d-11 gate discharged.", "refs": {"queue": ["46", "47"], "project": "EFFECTS-W1"}}
{"at": "2026-08-26T15:28:35Z", "headline": "The map now shows the background the game actually builds \u2014 and it turned out background painting had never reached the game at all.", "matters": "This is the call you made today. The editor had been drawing a stale copy of the jungle background while the game was built from a different one, with about 30% of the tiles disagreeing. It now draws the real one, so the band preview works on your actual level instead of refusing \u2014 I ran it and the band animates. The unplanned part: because painting wrote to whichever background was on screen, every background tile anyone has ever painted in Aurora went into a file the game does not read. That is now fixed in the same change. One thing is deliberately refused rather than quietly fixed: the tile picker still offers the whole zone tileset while this background only has 320 tiles, so picking one outside that range paints nothing and says so on screen, naming the file and the fix. Booked as its own item because it was equally wrong before today and is not this change's to hide.", "detail": "Owner decision d-12, branch feat/canvas-paints-rom-background, merged and pushed. THE BINDING IS DERIVED, NOT HARDCODED TWICE: actBindsBgOverride compares the act's stripPath from project.json against BG_OVERRIDE_CONSUMER_OUT_DIR, read from the vendored contract by the same accessor every other constant uses \u2014 no 'ojz' or 'act1' literal in Aurora's logic, and it fails closed, so a renamed act, second zone, other game or moved output binds nothing. I verified the contract addition's provenance myself at the pinned aeon commit rather than the working tree: 1ee8f8e6 is an ancestor of aeon's origin/master, the cited blob 5cef80aa matches git rev-parse exactly, and OUT_DIR at that revision is the recorded value (line 55 there, 203 in the working tree \u2014 cited by blob not line, which is what makes the drift harmless). VERIFIED BY THE OVERSEER ON THE MERGED TREE: suite 4593 passed / 3 skipped (baseline 4558), tsc clean. Acceptance test \u2014 bganim-motion-harness against the LIVE project, 18/18 with 2 honest NOTEs (the live document has no camera_x band so the driver-contrast rows cannot be measured there and say so): row 1c reads the band as PREVIEWING with no refusal, where it refused this morning; 15.0 repaints/s, its own step rate. bg-override-paints-harness 20/20: canvas paints the override's pixels at 24 discriminating cells and NOT the library's; an unbound act still paints its library background unchanged; a stroke reaches the model, reaches the file on disk after Ctrl+S, changes exactly one cell, and leaves BOTH the library binaries and the act's own binaries byte-identical. That last row (3e2) exists because the agent planted the exact hazard I warned about and found rows 3b-3e ALL STAYED GREEN \u2014 the stroke writes the document directly, so the file is right before the wrong command commits; without 3e2 the paint half would have rested on the undo rows alone. Regression sweep all green: fixture motion 23/23, effects column 19/19, rate shift 30/30. Agent also found __dbg.aeon.bgAt was a SECOND copy of the background resolution that would have reported the library's word for a cell painted from the override \u2014 a probe that lies; now routed through the viewport's own resolver. Safety: aeon's live editor_bg_override.json is unmodified (mtime unchanged, git clean) \u2014 the harness hardlinks copies and unlinks before writing so the link breaks. Merge conflict on docs/decisions.jsonl resolved keeping MY d-12 entry: the agent had authored a duplicate timestamped 18:40:00Z, about four hours in the future and ending :00, the signature of a timestamp written from the model's head rather than read from the clock."}
{"at": "2026-08-26T15:51:15Z", "headline": "Caught this lane contradicting itself to the hub, and fixed the reason it could.", "matters": "An earlier session told the hub which four projects Aurora would stand behind. That reply was never written down anywhere in Aurora, so after a session restart the next one told the hub something different an hour later, while both were sitting in front of your ratification list. The four projects stand; the record now lives in Aurora's own docs so a fresh session finds it.", "detail": "Nominations sent 2026-08-26T14:40:35Z (EFFECTS-W2, SPRITE-EXPORT, SCREENS, OBJECT-BEHAVIORS; mega-act declined; PNG import as horizon). Contradicting second reply 15:49Z withdrawn in full. Banked in docs/2026-08-26-project-nominations-aurora.md. EFFECTS-W2 amended, not folded: aurora half landed (d-12 + item 46, merge 2533489), aeon half untouched -- inject_editor_bg.py:203 still hardcodes OUT_DIR .../ojz/act1, verified read-only at aeon 15037c74. Link 4 ruled an aurora queue item, not a project. Shared-protocol bar 20 in the sending direction."}
{"at": "2026-08-26T16:16:19Z", "headline": "The tile picker now shows the background art your stroke actually puts down.", "matters": "In background mode the picker was showing the foreground tileset -- 919 tiles of the wrong art -- while a stroke wrote that number into a 320-tile background blob. You saw one picture and painted another, and most of the picker's range named no background tile at all. It also no longer throws away your foreground pick when you switch layers: the two are remembered separately, because one number cannot mean two things.", "detail": "ROADMAP item 47, merged a2463dd (code f30a681, harness 2277355). New providers/tile-picker-source.ts delegates to resolveDisplayedBg -- the same resolver the canvas and paintBgTile use, so no second resolution path can drift. Per-layer selection (selectedBgTileIndex); clamping and clearing both rejected as silently moving the author's pick. Item 46's refusal toast KEPT as the last line of defence (a pick survives a blob that shrinks under it, and __dbg.setSelectedTile arms one directly), wording corrected. Verified on the MERGED tree: 4612 passed / 3 skipped / 0 failed, tsc clean, bg-tile-picker-harness 28/28 (1 row honestly marked NOTE, not counted), bg-override-paints-harness 20/20 unregressed. Overseer re-planted the original defect independently: row 4d red with 'on-disk layout[0] = 0xd (tile 13); picked 17, fg decoy was 13', restored byte-identically. Disclosed limit: the thumbnail cache key is pinned in node only -- on this tree the arrays are 919 vs 320, so even the old length-based key would rebuild, and no CDP row can discriminate it."}
{"at": "2026-08-26T17:36:47Z", "headline": "One band drew two cards in the effects panel; now it draws one, and the column fits the screen again.", "matters": "The effects column stopped overflowing at 1680x1050 \u2014 every section header is reachable without scrolling for the first time. It still overflows on an 800px-tall screen, which is a separate piece of work.", "detail": "ROADMAP item 45, merged eb6ad7b (branch fix/effects-band-card-fold, 4 commits), correction 52d6eb2, pushed 7e4146b..52d6eb2. 'Band preview' is no longer a CollapsibleSection: the per-band resolved rate and preview verdict fold into each band card via bandStatus() in providers/bganim-preview-aeon.ts (10 unit tests, red-first); the playback chips, two column-wide warnings and the four named approximations (behind a 'why approximate?' chip) are a strip inside 'BG animation bands'. Sections 8->7, open on arrival 6->5. Measured on the merged tree from a clean 7e4146b checkout for the before side: column overflow 235->0px at 1680x1050, 485->214px at 1280x800; all-open worst case 1132->861 / 1382->1111. Suite 4612->4622 passed / 3 skipped / 0 failed, tsc clean, effects-column 19/19->22/22 at both frames with both new plants (D1 duplicate, D2 approx) fired on the merged tree, bganim-motion 23/23 fixture and 18/18+2 noted on AEON_LIVE. Six of the agent's after-figures were corrected at landing: they reproduce nowhere, including in its own worktree against its own dist/, so they were measured on an earlier revision of its branch and carried forward into the report, f1dde85's message and the row."}
{"at": "2026-08-26T18:42:25Z", "headline": "The map preview stopped being decorative \u2014 you can drag a line on it to say where a parallax layer starts.", "matters": "Until now the only way to place a parallax layer was to type a number into a spinner with no relationship to the act on screen beside it. Now the layers are lines drawn across the map, and you drag one to move it; the whole drag is a single undo. Half of what you asked for \u2014 the band marquee is still to come.", "detail": "ROADMAP item 43 part 1. Merged to master: feature e9749e6, CDP harness ba162b6, gesture-ordering fix f1fa56f, harness aim fix 6b81f91. Selected scene lifted from EffectsScenePanel React.useState into editorStore.selectedEffectsSceneId, with the stale-id fallback lifted alongside as resolveSelectedScene so canvas and form resolve one id one way. World-Y/world_y axis identity derived from sectionWorldOffset, not assumed. Verified on the MERGED tree by the overseer: vitest 4645 passed / 3 skipped / 0 failures, tsc clean, effects-guides-harness 30/30 across three runs. The +1 transform plant was re-run by the overseer independently: 5b/5c/6a/6b go red, restored to 30/30. The harness first reported 28/28 and was wrong -- fractional canvas rect under Xvfb dpr 1.35 meant CDP delivered the previous device row; feature was never at fault, harness now aims at integer client pixels with no tolerance anywhere. Overseer error worth recording: the 'sub-pixel theory is dead' ruling was made by reading row 4a from one run and the failing rows from another."}
{"at": "2026-08-26T19:14:05Z", "headline": "The visual band tool you asked for cannot work the way it was specced, and we found that out in an afternoon instead of after building it.", "matters": "Boxing a region of the background and turning it into an animated band was going to be the other half of the map-preview work. It cannot resolve: the background's tiles are stored across-then-down, while an animated band needs them down-then-across, so a box you draw almost never names a range the engine can animate. At the size both your shipped bands use, it never does. The measurement is banked with the script that produced it, and the shape of the feature is out for a decision rather than being quietly changed into something buildable.", "detail": "ROADMAP item 43 part 2, merged to master 66ffb47 (docs/reviews/2026-08-26-bganim-marquee-resolution.md + probe; NO src/ change). Band slots are column-major (bg-anim-band.ts:217, cited to aeon engine header/forest_bg_gen.py/injector); static picture tiles are row-major. Overseer re-derived the adjacency independently: 31.9% across a static row vs 1.3% down a column, matching the agent exactly. 1.71% of 177,776 marquees promotable; 18/143,088 multi-row, all 1 cell wide; 0 at h=4. promoteBandCommand cannot detect the mismatch (no marquee parameter) so a picture-region gesture must compute its own refusal. Also verified firsthand and relayed to aeon hedged: their live editor_bg_override.json's 8x4 band is not drawn as a column-major block and its slot 3 paints 964 cells. Shape decision routed to a fable agent."}
{"at": "2026-08-26T19:17:08Z", "headline": "Ruled what the band tool becomes: the map shows you what a band actually paints, before you commit to it.", "matters": "Boxing a region can't work, so the gesture was re-decided against what you actually said -- 'I mark somewhere' is a mark, not a box. You click a spot on the background and the panel aims a band there; the map lights up every cell that band would set in motion, live, while you adjust it. Clicking a cell that's already animated jumps to that band. This matters more than it sounds: promotion animates a tile everywhere the picture uses it, so a band can quietly own 964 cells of sky -- today nobody can see that until it moves.", "detail": "ROADMAP item 43 part 2 ruled 2026-08-26 by a delegated design agent, banked in row 43. Ships (d)+(c) fused plus click-to-seed: pure providers/band-coverage.ts, candidate lifted to editorStore as selectedEffectsSceneId was, clockless MapViewport overlay beside item 42's preview, writes only via existing promoteBandCommand/addBandCommand, no entry into bg-anim-band.ts. Rejected (a) 98.3% refusal rate, (b) resolves 4-37% falling with width, (e) 25% of failures are de-duplication which no permutation fixes. Aeon's 'injector accepts non-contiguous promotion' was checked against validate_band_coherence at aeon 5289a0a3 (base == cursor; phase0 == tiles[base:base+n]) -- contradicted and withdrawn by that lane; set-promotion is (e) relabeled. Aeon confirmed the scattered live band is intended at the mechanism level. Overseer verified all four scoping symbols exist (firstPromotableSlot, activeGuideScene, bandPreview, tile-picker-source). Build not yet dispatched -- serialized behind the effects-column parcel, which owns BgAnimBandPanel.tsx."}
{"at": "2026-08-26T19:38:03Z", "headline": "The effects column now fits an 800px-tall screen, and the big screen went from barely fitting to comfortably fitting.", "matters": "On a short laptop screen the column needed about 240px of scrolling and two of its seven section headers were unreachable; now nothing scrolls and all seven are reachable. On your 1050px screen it already fit, but only by 0-17px -- one wrapped line from breaking. It now has 321px of room, and the Layers list finally came off its minimum height. The cost: the BG animation bands section arrives closed. Its header still names it and shows the band count, and everything inside is one click away.", "detail": "ROADMAP item 45 tail. Merged: src 7748c71 (2 effects panels, +110/-16), harnesses fbd26df, docs 9c73fa5 (row 45 tail + new row 49). 1280x800 overflow ~234-252px -> 0px; 1680x1050 0-2px -> 0px with real margin 0-17px -> 321px; live scrollbars 2 -> 1 both frames; headers reachable 5/7 and 6-7/7 -> 7/7. Re-verified by the overseer on the MERGED tree: effects-column 25/25 at BOTH frames, two runs each; vitest 4645 passed / 3 skipped / 0 failures; tsc clean. Overseer independently ran PLANT=visible and confirmed [H1] answers COULD NOT MEASURE A FIT rather than 'fits' -- the loud-on-unmeasurable bar, and the shape of the section-column c1 defect avoided. The agent's two booked pre-existing failures (bganim-motion [4c], bganim-ui-authored-composition) were attributed to master rather than re-run at the old revision: the parcel touches only BgAnimBandPanel.tsx and EffectsScenePanel.tsx, which no hash of aeon background data can depend on. Rejected height-conditional collapse (defaultCollapsed is read once at mount and would not follow a resize)."}
{"at": "2026-08-26T21:02:45Z", "headline": "You can now see what a band actually paints before you commit to it -- and the highlight says what it is.", "matters": "Those magenta cells you asked about are the feature: every background cell the selected band animates. Band 0 turns out to paint 1,244 cells, and one single slot of it -- the sky tile -- accounts for 964 of them. Promotion animates a tile everywhere the picture uses it, so until now that footprint was invisible until it moved. Clicking a cell now aims a band there, or jumps to the band already animating it. The caption names the highlight and carries a swatch of its own colour, because you saw it and could not tell what it was.", "detail": "ROADMAP item 43 part 2 (A + B), merged 0236ef7. New pure providers/band-coverage.ts + canvas/band-lens.ts; candidate lifted to editorStore (bandCandidate/bandLensTarget) resolved once by resolveBandLens for both panel and canvas; writes nothing -- document writes remain promoteBandCommand/addBandCommand. Mark commits at mouseup with a witness (band list + blob size) so panning survives and a stale mark is dropped. Re-verified by the overseer on the MERGED tree: vitest 4680 passed / 3 skipped / 0 failures, tsc clean, CDP harness 37/38 + 1 NOT MEASURABLE across four runs, and all 10 poisons re-run independently -- every one reddened its named row, collateral named, tree restored clean and re-verified green after. One poison (alarm-candidate) enforces the neutral-information ruling as a gate. Colour: the agent changed it on my retracted instruction, measured the replacement losing on figure/ground against OJZ grey block art (covered cell read rgb(65,68,72)), and changed it back -- owner had confirmed magenta read as information, not alarm. Caveat carried: [11b] went red once in seventeen observations pre-merge, unpinned, green in all four merged-tree runs. Wave 2 (blob-strip drag) still open; the ROM-stepping question stays tagged and is blocked on a session relaunch."}
{"at": "2026-08-26T23:17:28Z", "headline": "Your nine points on the effects tools were triaged into a plan, and the survey of what Sonic 1, 2 and 3&K backgrounds do that ours can't is written", "matters": "Four of the gaps are engine work and are now booked with the engine lane; Starlight-style backgrounds are already possible; the one question left for you is where band art gets drawn.", "detail": "docs/reviews/2026-08-26-effects-feedback-triage.md (525ef5f merged 7387346..), docs/reviews/2026-08-26-bg-capability-survey-s1-s2-s3k.md (53b0662, merged 077a589); decision d-13; aeon booked gaps 1\u20134 in their DEFERRED_WORK.md"}
{"at": "2026-08-26T23:17:28Z", "headline": "Five fixes from that feedback are live: the band highlight you couldn't get rid of now clears with Escape or a Hide button, the map can show a game-screen-sized frame you drag around, the layer panel says 'screen line' on locked scenes and counts layers per scene, the curve a scene carries is shown on its layer card, and the band highlight and card say what a band actually does", "matters": "The things you asked 'how do I remove this', 'what can I see on screen', 'why 8 layers' and 'what is fa/fb' about are answered in the app rather than in a doc; the mark-band tool and curve controls are still coming.", "detail": "parcels A b9b3dee, E b15fcf1, G 328f1ea, C 72383e2, D 100d812 + 72792d4; merged tree tsc clean, vitest 4745 passed / 3 skipped; scroll-direction word gated on BAND_SCROLL_DIRECTION pending ROM confirmation; FOREGROUND items: effects-column harness at 1280x800/1680x1050, guides harness re-aim for screen space, zero-idle-repaint with frame on"}
{"at": "2026-08-26T23:47:05Z", "headline": "The whole road to trunks in front of an animated background is now in Aurora: you can mark a band with its own tool, stamp a region at a band in one drag, draw the band's tiles and its eight frames, regenerate the frames with one button, and the layer panel has real curve and vertical-split controls", "matters": "What you asked for on point 8 is authorable in one app without leaving for aeon's generator, and the file it writes is checked against the engine's own injector rule before it ever reaches a build.", "detail": "parcels B e90f028, H 4e8dff4, J 4dc23b5, I daf23d8, label alignment 12d1650, scene newline 87c9ea8 (hub ruling e1ebd20 \u00a78), band direction measured 04e0f04; first live-app pass docs/reviews/2026-08-26-effects-foreground-checks.md (all rows pass; label column booked and fixed); merged tree tsc clean, vitest 4849 passed / 3 skipped; second live-app pass over H/I/J in flight"}
{"at": "2026-08-27T00:17:54Z", "headline": "The effects feedback arc is closed: the last two fixes landed \u2014 a stroke on band art now undoes from any facet, and every file the editor writes into the engine tree ends the same way so a save with no edits changes nothing", "matters": "Saving no longer churns files you didn't touch, and undo behaves the same wherever you drew.", "detail": "undo fix 4017727; newline rule at the canonical-json chokepoint (empyrean 7a51e11 \u00a78) merged after; live-app pass 2 docs/reviews/2026-08-26-effects-foreground-checks-2.md all rows pass; merged tree tsc clean, vitest 4866 passed / 3 skipped. Known one-time effect: the first save after this adds the trailing byte to aeon files that lack it (override, bglib, chunks, objects/rings, section_0.meta, project.json), then identity resumes. FOREGROUND left: 'Ctrl+Z from Effects undoes the stroke' harness row."}
{"at": "2026-08-27T00:32:34Z", "headline": "Closed the July band-loss question you answered \u2014 the tool that caused it refuses today \u2014 and found the same trap now pointing the other way.", "matters": "You said not to worry if it wasn't still happening, so I checked before closing rather than after: aeon's importer now exits with a named refusal rather than eating a band set. But moving band art into Aurora last night gave that file a second author, and aeon's OTHER background tool still claims band data as its own \u2014 so a routine background regeneration over there would now eat art you made here. Told that lane; it is their tool and it is not blocking anything.", "detail": "aurora ROADMAP item 14 CLOSED by the owner 2026-08-27; residual booked as item 53. Verified by behaviour not presence against aeon origin/master 89d73e0c (ls-remote-resolved, read at a revision, not through the sibling working tree): read_existing_override called with each writer's declared OWNED_KEYS over a doc carrying `anims` -> png_to_bg_override exit 1 (OWNED_KEYS :77, call :211), forest_bg_gen exit 0 (OWNED_KEYS = layout,tiles,anims :20; OUT defaults to the live path :22-24). Dependency named per the notify-on-the-dependency bar: owner decision d-14. Commits 4717dd1 (incl. c449c4d), probe scratchpad/item14-refusal-probe/, relayed to aeon-f2."}
{"at": "2026-08-27T01:39:47Z", "headline": "The band tool is finished: drag a run on the tile strip and the map lights exactly what that band would take.", "matters": "This was the last piece of the band arc. You aim a band by dragging along the strip of background tiles instead of typing numbers at it, the length snaps to something the hardware can actually run, and nothing is written until you promote \u2014 so you see the footprint first. Two things I caught on review: a message that told you the one slot you CAN use was taken, and the roadmap entry for this item had silently doubled itself, with both copies still saying the work was unstarted.", "detail": "ROADMAP item 43 wave 2 (option C) CLOSED. Branch feat/bganim-band-strip-range merged 98329fe; overseer fix d7ec678 (prefix-refusal off-by-one: firstPromotableSlot is a count, so owned slots are 0..fps-1; the guarding row pinned the literal /0\\.\\.32/ and passed against the defect, re-cut to derive both halves; red-first confirmed). Verified on the MERGED tree: node 4900 passed / 3 skipped / 0 failed (branch-point 4866, delta = the parcel's +34), tsc clean, CDP bganim-strip-range-harness 34/34 on five overseer runs (3 at dpr 1, 2 at forced 1.35, fractional rect confirmed). Parcel-level discriminator run: killing the range branch gives 29/34 with [6c][6e][6f][6g][10a] red, restored green; [7a] disclosed as non-discriminating. Separately: item 43's roadmap row carried 17,225 duplicated characters inside one line, both copies stating wave 2 was still open; de-duplicated under a fragment-set loss check (0 lost). grep -c '^| 43 |' was 1 throughout, so the known two-row detection could not see it."}
{"at": "2026-08-27T03:46:48Z", "headline": "Drew a tree trunk into the animated jungle background, in the real editor, and the trunks appeared across the level straight away.", "matters": "This is the thing you asked to see: band art is drawn where you are looking at it, not made elsewhere and imported. One drag of the rectangle tool put a full-height trunk into the band, the background repainted every place that band is used, and one button turned the single drawing into the eight frames the animation needs. Nothing was saved and the engine tree was never touched. It also caught a real slip: the band card tells you the band owns one more tile than it does, in three places -- the same off-by-one I fixed yesterday in a fourth place, so yesterday's fix was too narrow. Booked, not yet fixed. Your vertical-scrolling bands are booked too, and deliberately not sent to the engine lane yet.", "detail": "scratchpad/band-trunk-demo.mjs, 8/8, commit bff6179. Opens scratchpad/fixtures/aeon-bg-writable (a copy; the live aeon path is refused) with its override refreshed from live, so the art drawn on is what ships; D8 asserts the copy's file is byte-identical after the run. D4 derives the expected slot set from the band's geometry and matches changed slots exactly ([12..19], cols 3-4, all 4 rows). Trunk colour derived from the app's own render, not a literal. ROADMAP 54 = the three off-by-one readouts (bandRows slotRange string, blob-budget Hint, promote-form Hint; base..base+count in inclusive punctuation; slotRange FUNCTION and rangeCovers are correct and are not this). ROADMAP 55 = vertical bands, engine-side, hold until the owner says. Band scroll direction corroborated from a second instrument: measured leftward ~1px/4 frames on the ROM."}
{"at": "2026-08-27T04:39:31Z", "headline": "Backgrounds can now have sixteen parallax layers instead of eight, everywhere you'd see it in the editor.", "matters": "You asked the engine lane to raise that ceiling; this is the editor half, and it took one file swap because every place that shows the limit already reads it from the shared contract rather than having the number typed in. The one exception was worth catching: the description an AI agent reads to learn the rules still said 'layers 1 to 8' \u2014 the one place a wrong bound would never be argued with, because it is what an agent believes before it tries anything. The panel now reads 5 of 16 layers; I checked that on screen, not just in tests. Safe to land before the engine catches up: their build refuses a too-long scene by name rather than quietly dropping layers.", "detail": "ROADMAP 56, commit d54d386. Re-vendored by extraction from empyrean 277bc15, pin d4345af5 -> 0f661b70, hash of extracted bytes equals what the revision holds; currency also checked at empyrean origin/main tip (same blob). Aeon's unverified booking that Aurora derives the cap held, verified by enumerating the four EFFECTS_LAYER_COUNT consumers. Literal found in src/main/editor-methods.ts edit_effects_scene description; four prose sites de-numbered. effects-scene.test.ts re-derived + anti-vacuous accept-at-max row; red-first via disabling the evaluator's maxItems branch. 4900 passed / 3 skipped, tsc clean, CDP shot shows LAYERS (5/16 PER SCENE). aeon pushed master d5fb9778 still asserts MAX_PARALLAX_BANDS == 8 with a named build refusal."}
{"at": "2026-08-27T05:46:07Z", "headline": "Your background band is proven to actually animate inside a real ROM \u2014 and we can now point at a pixel and say which band and which slot it came from.", "matters": "That was the last unproven link in the effects road: the editor could build a band and the ROM could carry it, but nobody had shown the thing moving in the machine rather than in a preview. It moves.", "detail": "Tagged band-lens question closed. Freshly spawned oracle-aether advertises 46 methods incl. get_layer_states/set_layer_enabled (implementation oracle-rs, serverBuild d285ecbc6c3a, source vcs), verified by executing. band-lens-harness.mjs: planeA hidden and the hide measured, 27/28 sample points on planeB, 13 rebasing into the band's slot range via BG_TILE_BASE_SLOT, 0 outside the blob (reported as unexercised, not counted as earned). band-step-proof.mjs: screenshot diff cannot answer stepping (0/27 tile ids moved over 90 frames while every screenshot differed); VRAM tile bytes at band slots 0..31 gave 6 distinct contents over 6 captures against a byte-stable control run at slots 96..127. Also corrected a banked cross-lane contract: pixel_attribution's cell is a sibling of winner, not a child \u2014 silent failure mode. Commits 5f91e4a, d9e0a2b; pushed, origin/master moved cc891e9 -> d9e0a2b."}
{"at": "2026-08-27T06:05:14Z", "headline": "The band cards stop claiming a slot they don't own, in all eight places \u2014 and the 320x224 screen frame is confirmed working on screen.", "matters": "You can now read a band's slot range off the card and trust it, which matters because the very next slot is the one a promotion takes. And the screen frame answers the question you asked it for: how much of the level a player actually sees at once.", "detail": "Item 54 closed: three named readouts + a fourth found ten lines away + four more in band-coverage.ts/band-strip-range.ts. One helper, slotSpanPhrase(base,count), with the empty case decided so 0..-1 never renders. Suite 4922 passing, tsc clean. Foreground: slot-range-onscreen-harness.mjs 4/4 x3, band card renders 'slots 0..31 \u00b7 32 tiles', defect literal absent everywhere. Item 50 closed: screen-frame-harness.mjs 12/12 x3, frame drawn at exactly one screen at live zoom, edge pixel distinct from interior and from above, toggle off drives a repaint, no idle repaints over 3s. Screenshot sent to the owner. Merges 9b27869 + tail; pushed through 458808d."}
{"at": "2026-08-27T06:29:05Z", "headline": "The readout that was cutting itself off now fits, the number boxes enforce the limits they show, and we finally know what it costs to drop Sonic somewhere in a running Sonic 1.", "matters": "The strip readout is the only thing on screen telling you what a drag aimed at, and 40% of it was being cut \u2014 including on the old wording, so this was not a new problem. And the Sonic 1 measurement is the thing that was blocking anyone from costing the last playtest link: the engine does not fight the poke, the camera catches up on its own, and the one real trap is poking too early.", "detail": "Merged: item 40 (clampFieldValue / clampStaticBase, each derived from the same source as its displayed props), item 43 tail (readout 173px->85px in an 85px box; widening excluded on measurement \u2014 224px row with a 122px flexShrink:0 label, ~48px gutter against a 71px deficit), item 53 closed by behaviour against aeon origin/master 79385a2a (refuses by name, file byte-identical, exit 1). Suite 4941 passing; strip harness 36/36 incl. new [6g3] over all 2090 legal (base,rows) pairs. Item 48 gate: poke holds drift 0 over 60 frames, camera pans 0->432 in ~30 and centres; poking at the mode transition is silently discarded by level init, byte-identical to control. Row 48 stays open. Pushed through 535940a."}
{"at": "2026-08-27T06:56:41Z", "headline": "Clearing a number box no longer silently writes a zero into your scene \u2014 and the band label in the tile picker stops cutting itself off.", "matters": "Select-all-and-delete in any of twelve boxes used to commit a real 0: v_center, v_offset, a band's static base. You would have seen the value change and had no idea why. Typing a number into a box with a floor also no longer snaps mid-keystroke.", "detail": "NumberField now keeps a local text buffer and commits only finite text; empty/whitespace/lone-minus/lone-dot commit nothing. Twelve call sites, derived (the ui barrel hid four from a module-path grep). NaN-and-refuse rejected: 8 of 12 clamps map non-finite to a substitute number and commit it. Commit-on-blur rejected: spinner arrows would appear inert and the suite has no DOM to verify it. Foreground harness 11/11 x3 after fixing my own vacuous first version (cleared a field holding 0, which is what the defect writes). tilePickerBandLabel 155px/106px -> 75px/75px as 'b0 \u00b7 0..31', detail verbatim on the title; worst case over 796916 legal tuples clears by 16px without eating [6g3]'s 1px. Suite 4965 passing; strip harness 40/40. Pushed through 90c5de9."}
{"at": "2026-08-27T07:00:05Z", "headline": "You can now click a spot in a Sonic 1 level and have the running game put Sonic there \u2014 and it tells you where he actually ended up, not where you asked.", "matters": "That was the last missing piece of the classic playtest loop. The 'where he actually ended up' half matters because nobody has checked whether the spot you clicked is inside a wall, so instead of guessing we read the machine back and report the truth. One known gap, written down rather than hidden: doing this outside a level looks like it worked."}
{"at": "2026-08-27T07:00:05Z", "headline": "Overnight run summary: nine items closed, all pushed.", "matters": "Everything from tonight is on master and verified on the merged tree, so a fresh session loses nothing.", "detail": "Closed: band lens (tagged question), 54 (eight readouts), 50 (screen frame), 40 (+ the emptied-box zero and the typing snap), 43-tail (strip readout + picker label), 53 (aeon's tool, verified by behaviour), 48 (S1 play-from-cursor, live-confirmed). Suite 4910 -> 5020. d-15 filed and ruled by the hub under delegation; recorded as the hub's act, not the owner's, for his review."}
{"at": "2026-08-27T07:05:51Z", "headline": "The band speed control is now something we have watched rather than something we believed: setting it one notch higher really does halve the speed.", "matters": "That number was shipped in the editor on the strength of a formula in aeon's source that nobody had ever seen run. Now it is measured \u2014 4 frames per step at one setting, exactly 8 at the next \u2014 so when you set a band's speed you are setting something we know behaves the way the box says.", "detail": "Row 43's NOT-VERIFIED-ON-HARDWARE tag discharged. rate_shift 2 -> 12 changes in 48 frames, all gaps [4], mean 4.00; rate_shift 3 -> 6 changes, all gaps [8], mean 8.00; ratio exactly 2.00. Two ROMs differing by exactly one byte (cmp -l = 1) at BgAnim_Table+4, so zero build variance \u2014 better evidence than two builds. Record identity-checked against the document before either measurement (tile_count 32, step_mask 63, vram_dest 32768) and the patched ROM reads back 3. First attempt patched memory; server refused ROM writes and the probe reported BLOCKED rather than emitting four identical cadences that would have looked like a refutation. Row 14's residual also closed (aeon 79385a2a, verified by behaviour). Pushed through 75777ec."}
{"at": "2026-08-27T07:39:03Z", "headline": "Band art drawn in the editor, regenerated by the editor, and watched moving in a real ROM \u2014 row 51 is closed end to end.", "matters": "That is the whole authoring loop for animated background art proven in one pass: you draw it, the editor derives the in-between frames, it bakes, and it moves on screen. The picture of it is in your messages.", "detail": "Phase 0 edited, banks 1-7 via the shipped regeneratedShiftPhases (not a reimplementation), prefix mirrored, FAST build in the pinned fixture: crc 4b4f1b5b -> f6f41a67 at identical length. band-step-proof: 6 distinct band contents over 6 captures, control run byte-stable, all hashes differing from the pre-edit ROM. Also closed row 51's CDP half (36/36 x3 plus a forced fractional-dpr run) and booked row 57 (openBgTileDocument has zero callers outside its own definition \u2014 an unreachable door, not a broken one)."}
{"at": "2026-08-27T07:39:03Z", "headline": "A pinned copy of the engine stopped being able to rebuild its own ROM, and nothing in it looked wrong.", "matters": "We measure things by building a pinned copy and watching the result. That method quietly assumed the pin still builds to the pin, and it does not \u2014 so a future session could have compared against tonight's numbers, found a mismatch, and had nowhere to look. Now it is written down with a cheap defence.", "detail": "Clean tree throughout, every tracked file byte-perfect including the injector outputs the re-bake reproduced exactly. Drift is in gitignored artifacts every build regenerates (compression dictionary, sound blob) that the checkout shipped in a state the tools do not reproduce. 1273 bytes differ, first at the header checksum, scattered over 168KB \u2014 a re-pack signature, not a content change. Relayed to aeon with what I did NOT establish stated. docs/reviews/2026-08-27-fixture-build-drift.md."}
{"at": "2026-08-27T07:49:13Z", "headline": "A finding I sent another lane last night was wrong about its own cause, and chasing that down found the real one: our test builds are not as repeatable as everyone assumed.", "matters": "When we pin a copy of the game's code to measure against, we do not pin the tool that builds it \u2014 so the same unchanged code can build into a different ROM hours later, with nothing on screen to say why. It has not corrupted any result we relied on, and there is now a free check that catches it.", "detail": "docs/reviews/2026-08-27-fixture-build-drift.md, corrected at aurora b1c15d0 (pushed). My packet's premise ('the checkout SHIPPED gitignored artifacts') was false and uncited; the aeon lane accepted it and built a correct-but-inapplicable cp -r explanation. Reflog shows the fixture is a git clone; git ls-tree dcc692d1 tracks no s4.bin and no engine/{debug,sound}/generated. Real varying input: build.sh takes SIGIL_EMIT (:317, writes engine/sound/generated) and SIGIL_BUILD (:558, the assembler) from the environment, both in sigil's live tree, neither pinned. Both rewritten inside the build window (emit_sound_blob 01:41:19, sigil 03:14:00) between crc 4b4f1b5b and f33b157e. Not closed: mtime proves a rewrite, not a behaviour change, and the old binary is gone. Defence adopted: record build.sh's own 'Assembler: sigil <rev>' banner beside every CRC. Relayed to aeon (whose queue it is) and sigil (whose binary it is)."}
{"at": "2026-08-27T07:54:50Z", "headline": "The build-tool finding closed out across three repos: aeon owns the gap, sigil fixed the false alarm my defence relied on, and all three of us corrected a wrong call.", "matters": "Our test builds can now be compared honestly between sessions, and the check that makes that possible was caught being decorative before anyone built on it.", "detail": "aurora 8e2056a6 (pushed). Bar in docs/OVERSEER.md: quote build.sh's 'Assembler: sigil <rev>' banner (match text, not line number) beside every CRC; revision: is load-bearing, tree:/-dirty asserts nothing. aeon banked the landing bar at fd6ccc8e and owns the unpinned-toolchain gap. sigil removed the stuck-dirt cause at e5bd4a4f (docs/lane-status.json ignored; verified reachable + their tree clean) but the VALUE is stale until relink -- tell is a revision != fbf60abd, not the flag clearing; amended after they flagged it, because my first wording would have made a future session conclude they never fixed it. Cross-lane enumeration of docs/lane-status.json paid out in three directions (sigil stuck flag; aurora ignored; aeon tracked -> main tree always dirty -> golden freezes need a clean worktree). Durable outputs: 'a vigilance rule protects the people who least need it' (from aeon's 3-incidents-2-caught datum), and 'a provenance record is several claims on different clocks'."}
{"at": "2026-08-27T08:31:18Z", "headline": "You can now open a background tile's art by double-clicking it in the tile strip \u2014 the art tools could always edit a band's animation frames, but never its still tiles.", "matters": "That was the last piece of the background-art road that had no way in: the code to do it existed and shipped, and nothing in the app could reach it. Double-click a tile in the strip and it opens in the composer.", "detail": "aurora 8683339 (merged + pushed), branch parcel/bg-tile-door: b35840f door + node rows, fd1270f CDP harness, f5ff3fd ROADMAP row 57. Door is a double-click on the blob tile strip in ArtBrowser, gated on origin==='override', mirroring BgAnimBandPanel's openBank contract (openDocumentGuarded + switchFacet). Rule is a pure provider resolveStripOpen; refusal bounds derived from doc.tiles.length, never pinned. Re-verified by the overseer on the MERGED tree: tsc clean, suite 5026 passed / 7 skipped / 0 failed (387 files), harness 19/19 on three of my own runs. I re-planted the discriminating violation myself (door repointed at openBandBankDocument) and [4b] went red with report kind:'open', openedTileIndex:37, gestures:1 -- proving a row asserting only 'something opened' would have been GREEN on the bank door; restored byte-identical, 19/19 returns. Left open and disclosed, not implied: the refusal branch has no CDP row (this act paints the override in every section, so nothing can drive it) and FG-strip double-click is deliberately inert."}
{"at": "2026-08-27T09:45:42Z", "headline": "Backgrounds can now be given a wobble from the editor \u2014 the file format always carried it and nothing in the app could set it.", "matters": "This is the shimmer-and-ripple half of the background effects work. Until today an author could set how a background scrolls but not how it waves, even though the engine has always been able to do it; the setting existed only for someone hand-editing a file. It also caught a trap on the way in: one of the new controls could have produced a background the game refuses to build, with no way to fix it from inside the editor, so the fix shipped alongside it rather than being written down for later.", "detail": "branch feat/effects-deform-authoring merged to master. deform_fg/deform_bg/v_deform/layer deform + left_column_mask policy; six tableRef forms derived from the schema oneOf; no v_factor_fg control (RESERVED in v1 runtime). vDeformToggleCommand clears the mutually-required pair in one undo step (aeon scene_dsl.emp:1288 and :1293, poison-pinned at emp_expect_fail.py:282). sprite_mask disabled (:1354); factor0_lock selectable because Aurora's check is stricter than the engine's (no packer). Verified on the MERGED tree by the overseer, not on report: tsc clean, vitest 5069 passed/7 skipped/5076 total across 387 files, effects-deform-harness.mjs 37/37 on five clean runs with dpr=1 and identical integer aim; one back-to-back looped run gave no output and is recorded inconclusive (suspected xvfb-run -a display contention). 17 red-first plants, five alternative green-paths ruled out, three rows disclosed non-discriminating. The own()-layer dsb fold (scene_dsl.emp:558, 'THE FOLD') verified firsthand. NOT seen on hardware. ROADMAP rows 58 (delivered) + 59 (precision removal, hub-ruled)."}
{"at": "2026-08-27T10:18:33Z", "headline": "The Effects panel had a setting the engine deleted a day earlier \u2014 it is gone, and the example scene beside it was re-recorded rather than hand-edited.", "matters": "An author could pick a 'Precision' value that changed nothing in the game, because the engine had removed the field's storage the day before. What made it more than a delete: one of the two example scene files was recorded off a real editing session, and its own note warns that patching it converts it into an ordinary hand-written file and silently destroys the only reason it exists. So the session was replayed in the running app and the bytes taken off disk again \u2014 exactly one line different, which is what proves the replay faithful. The replay was the first in four days and found the recording script had quietly stopped driving half its own gestures, which nothing else would ever have surfaced.", "detail": "ROADMAP row 59 DELIVERED; merged 5c67880, plus overseer correction 15ee95f; pushed, origin/master moved b02cb13 -> 15ee95f (ls-remote confirmed). Branch feat/retire-precision, 4 agent commits. Vendored schema re-pinned to empyrean 0bd4753 (delta exactly one line; verified identical to the hub blob). canopy_dusk edited (writer-CERTIFIED, so legitimate) 2505->2482 B; writer_session_ojz RE-RUN under CDP (writer-ORIGINATED) 956->933 B, blob 2c4104e4->893cd055, sha256 c28b6db0..., delta exactly `- \"precision\": \"cell\",` on both. VERIFIED FIRSTHAND ON THE MERGED TREE, not taken on report: vitest 5070 passed / 7 skipped / 0 failed / 387 files (aggregate), tsc exit 0, and an INDEPENDENT harness re-run by the overseer against its own writable aeon copy on a different port produced output BYTE-IDENTICAL to the committed fixture (29/29), with md5sum over aeon's live editor/effects/*.json unchanged across it. One overseer plant: precision re-added to the vendored schema reddens the drift gate (`expected '0f661b70...' to be 'dd972cf0...'`), restored green. Agent's own work: 3 red-first plants with 5 quoted failures, 6 alternative green-paths ruled out, 3 rows disclosed as non-discriminating. Row 60 booked (harness rot + the layer count pinned at 8 against an app ceiling of 16, printed on every run as a visible debt). Overseer correction 15ee95f: the provenance's aeon-tree row welded a live safety claim to an emptiness claim that had gone false.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T11:04:00Z", "headline": "The example background scene is re-recorded at the editor's real 16-layer limit and now exercises the wobble controls \u2014 and a check that had been passing on luck was found and fixed.", "matters": "Two things. The recorded example scene had drifted: it was recorded when the editor allowed 8 layers and the limit is now 16, and it could not exercise any of the wobble controls that shipped this morning. Both are fixed, so the example now reflects what the editor actually offers. And reviewing it turned up something worse in the recording script \u2014 a workaround for a bug that had since been fixed had quietly become a bug itself, and every green run for months had been green by coincidence rather than because the thing it checked was true. That one was found by reading; no amount of running could have surfaced it.", "detail": "ROADMAP rows 60 DELIVERED and 61 booked; merged, plus overseer commits 2748dd4 (row 61) and 93691c6 (the harness fix). origin/master 71f8925 -> 93691c6, ls-remote confirmed. Agent delivered commits a451a9d (ceiling) / 6bab090 (deform gestures) / afdcbbb (packet), deliberately split so each has an interpretable delta: the one-line corroboration every previous re-origination used is unavailable at 43 lines, so each commit carries a delta PREDICTED IN WRITING BEFORE THE RUN (scratchpad/predict-commit{A,B}.py) and matched with no diff. I verified the predictors are non-circular (they read nothing from the app or the fixture) and that PREDICTED-commitB.json is byte-identical to the shipped fixture. Fixture 933 -> 2411 B, blob 893cd055 -> 4022f564, sha256 073283c6. VERIFIED FIRSTHAND ON THE MERGED TREE: vitest 5070 passed / 7 skipped / 0 failed / 387 files, tsc exit 0, and an INDEPENDENT harness run by the overseer (own aeon copy, own port) 37/37 byte-identical to the committed fixture, aeon live scenes md5-unchanged. TWO FINDINGS THE AGENT DID NOT ABSORB: my brief's own prediction of v_factor was WRONG (R7's 'v_factor = layer count' is unsatisfiable at 16 \u2014 range 0..15, clamp lands on 15 which is the lock sentinel AND the new-scene default R7 forbids; proven by plant emitting v_factor:15, amended to the modulo R5/R6 already use), and a FIFTH rot (R4 had been driving nothing since tooltips landed, byte-invisible because addLayerCommand's default IS i*32, so row 5b was non-discriminating and always was). SIXTH ROT, mine, found in review: the CollapsibleSection workaround outlived its fix (5041f6e), its probe was end-anchored so open() could only return false, it therefore clicked every call and the section ended open only on even parity, and row 4b accepted 'clicked' as proof of openness. Red-first: restoring the old probe reddens 4b at 36/37 with every gesture row still green. AEON CROSS-LANE: aeon confirmed the v_factor sentinel is a genuine trap in THEIR contract (parallax.emp:1661, s1==15 returns 0 = LOCKED, so a clamping producer authors a band that does not move), booked in this lane's name, and accepted the argument that moving the sentinel beats a note-to-producers because a forgotten rule yields a silently wrong ROM while a moved sentinel fails loudly at build time.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T11:16:37Z", "headline": "Checked that a scene the editor accepts is one the ROM build actually accepts \u2014 and found one place where it isn't.", "matters": "The editor blocks or warns about certain background scenes because the ROM build would reject them. Those warnings were written by reading the engine's source, and nobody had ever checked them against a real build. Good news first: every rule is still exactly where the editor thinks it is, so nothing has silently drifted. The gap is in the direction nobody had tested \u2014 one setting the build rejects outright, where the editor says nothing at all. It's an unusual case (the value has to arrive in a file rather than be picked in the app), but an author hitting it gets a broken build and no explanation. Booked with two smaller ones.", "detail": "Verification parcel, merged and pushed; origin/master 6dd5a55 -> d1617ae (ls-remote confirmed). Agent commit f702be4 (packet + rows 62/63), overseer commit for row 64 + a provenance correction. NO SOURCE CHANGED \u2014 verification parcel by design. All five guards verified in place against aeon HEAD; I re-read three of them firsthand in aeon's source rather than accepting the table. FRAMING CORRECTION that changes how the result reads: four of five guards are ADVISORY by row 58's deliberate design (a warned scene still saves), so 'Aurora refuses' means the check fired, not that the editor prevented anything; only sprite_mask is a prevention. DIRECTION 2 LICENSED BY A POSITIVE CONTROL (aeon's warning, relayed mid-parcel): hand-written JSON bypassing Aurora entirely, built rc=1 quoting guard 1's own interpolated signature \u2014 without it every clean cell would have been an empty-test pass. Row 62: sprite_mask is the one genuine permissive gap. Row 63: the period control accepts 1..256 where only the 9 divisors of 256 are legal; two parcels had independently worked around it. Row 64 (mine): guard 5 is FOUR ensures and Aurora covers three \u2014 scene_dsl.emp:1251 has no apparent counterpart, booked at READ-not-measured status because the packet had left it as an UNVERIFIED cell, which is invisible to a later session in a way a row is not. Corrected in the fixture provenance: the period rule's owner is the ENGINE (parallax_dsl.emp:52/:87) not sigil, and the divisor is a literal 256 not a derived table length; emitted values unaffected. factor0_lock divergence re-checked and still stricter-not-permissive ({15,15,0} builds green, crc=e9e07375, while Aurora warns). Discipline: banner revision fbf60abd on all 27 invocations, no cargo in sigil, aeon md5-identical before and after, green baseline first and restore-green after each of 13 refusals. Suite 5070 passed / 0 failed on the merged tree.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T11:52:59Z", "headline": "The three gaps between what the editor allows and what the ROM build accepts are closed \u2014 including a second silent one that turned out to be real when measured.", "matters": "The editor now speaks up in every case where the build would reject your background scene. One of the three was booked as a suspicion rather than a fact, and measuring it confirmed it: a scene combining a curve with a moving anchor builds broken while the editor said nothing. Also, the box for the wobble's period used to offer 256 values when only 9 of them are legal \u2014 two separate pieces of work had quietly steered around that without anyone writing it down. Separately, this parcel caught the shared assembler being rebuilt underneath it by another part of the project; that turned out not to have damaged anything, but it changed how the three repos pin that tool.", "detail": "ROADMAP rows 62/63/64 DELIVERED, replaced in place; merged and pushed, origin/master f2cfeba -> c1addb9 (ls-remote confirmed). Agent commits a845770 (62) / d58fb6d (64) / 1b82251 (63) / e0b1ec5 (packet + harness). VERIFIED FIRSTHAND ON THE MERGED TREE: vitest 5074 passed / 7 skipped / 0 failed / 387 files, tsc exit 0, and I re-ran scratchpad/guard-surface-harness.mjs independently for 22/22 (dpr 1, load 2.69, uptime 1d23:41). aeon effects md5 unchanged. ROW 64 MEASURED, not assumed: curve on layer 3 + anchor dsa 3 / dsb 2 builds rc=1 quoting scene_dsl.emp:1251 verbatim while Aurora returned []; the 15/15 anchor (the case the guard PERMITS) builds green at crc=4362a054 and the no-curve control at 55e6ebbc, so the sentinel trap was real and a saturating fixture would have authored the permitted case while believing it authored the refused one. Why it had been dropped: guard 5's other three ensures are per-layer, this one has its halves on different objects (curve on a layer, shifts on the scene's anchor). ROW 63: select over computed divisors, carried non-divisor rendered DISABLED rather than silently displayed as legal; schema read first and it is the looser document (1..256), so this is deliberately stricter than the contract on the codebase's own test. ROW 62: advisory added, disabled option KEPT \u2014 they cover authoring versus arriving. 9 red-first node plants with distinct quoted failures, 4 in-app plants each taking only their own rows red, non-discriminating rows disclosed with catchers named. CROSS-LANE INCIDENT: the shared sigil binary was relinked twice mid-parcel (fbf60abd -> 52882e2e -> 537869e6, mtime 07:20:23) by another lane's cargo test --release --workspace; verified firsthand here and relayed to aeon and sigil unprompted. aeon confirmed no straddle (all seven goldens captured and restored six minutes before the first relink) and their attest incidentally ran a seven-shape equivalence check between the two revisions, 4023 passed. Both lanes adopted an md5-of-the-binary pin alongside the revision, on the argument that a revision names a property of the SOURCE and cannot detect a relink of the ARTIFACT; sigil also widened its announce rule from bilateral to all-lanes because this lane was mid-build and in none of that conversation.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T12:20:00Z", "headline": "Proved you can actually reach the last two background controls in the app \u2014 and did it with a test instead of a second recorded scene that would have billed us forever.", "matters": "This repo has shipped a feature nobody could reach before: fully tested, working code with nothing in the app that called it. So 'can an author actually get to this control' is worth proving rather than assuming. Both remaining controls check out. The way it was proved matters as much: the item as booked called for recording a second example scene, which sounds cheap but obliges us to re-record it every single time the file format changes \u2014 that happened twice yesterday. A test carries the same claim once. The test is only worth anything because unwiring the control makes it fail while 'the control is on screen' keeps passing \u2014 which is the difference between proving reachability and proving a box exists.", "detail": "ROADMAP row 61 DELIVERED in the RULED shape, replaced in place and leading with the shape so nobody reads it as the booked version having been done; merged and pushed, origin/master 1330a77 -> pushed (ls-remote confirmed). Agent commits 523d08d (harness + select-key probe) / a2fc9ba (packet + row). git diff master -- src/ test/ is EMPTY. VERIFIED FIRSTHAND ON THE MERGED TREE: tsc exit 0, vitest 5074 passed / 7 skipped / 0 failed, and I re-ran scratchpad/curve-vsplit-reachable-harness.mjs independently against my own aeon copy for 30/30 (dpr 1, load 4.78, uptime 2d 8min), aeon effects md5 unchanged. THE LOAD-BEARING PLANT: curve picker's onChange unwired -> 25/30 with 3c/5a/5d/6b/8a red, while 3a (control rendered and enabled, 18 options) and 7a (blanket no-missed-gesture) STAYED GREEN \u2014 if the rows were proving 'a control is on screen' that plant would have been all-green. Four more plants incl. the vsplit spinner pointed at world_y (27/30, 'layer top unchanged at 170') which rules out the measures-the-wrong-quantity cause, and two instrument plants reproducing the exact end-anchored-selector shape of the five real rots. Every asserted value is one the defaults cannot produce, including through the seed trap one field over: vsplitFromToggle seeds at = world_y (the i*32 trap), so the seed is read out of the document BETWEEN gestures and rows assert movement from it; the vsplit values are strictly interior to 0..511 so no clamp and no 15-family sentinel can land there. Non-discriminating disclosed: the 'and is ENABLED' clause of 3a/4a cannot fail today because the panel never passes disabled to either control; catchers named. Measured instrument limit reported rather than hidden: a <select> cannot be driven by arrow keys through CDP here (Chromium's menulist opens a native popup outside the page); typeahead works and is path 2's gesture, and the probe is committed so it is re-derivable. Nothing tagged for foreground follow-up.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T14:51:12Z", "headline": "The build console no longer covers the properties panel \u2014 with it open, 94 of 126 controls could not be clicked.", "matters": "Every time a build failed the console appeared over the right-hand panel and hid the bottom of it, so controls you could see could not be clicked and scrolling would not reach them. That is exactly when you most need the panel. It now takes real space instead of floating over everything, so the column shrinks and everything stays reachable. Also landed earlier: the layer guides now sit on the background instead of sliding with your view, and adding a band selects and scrolls to it.", "detail": "ROADMAP row 68 DELIVERED; merged and pushed, origin/master dee1274 -> pushed (ls-remote confirmed). REPRODUCED ON MASTER FIRST, which the previous attempt at this skipped: 13/17 rows with 5a/5b/5c/8a red, 94 of 126 enabled column controls unreachable with the console open vs 0 shut, column box height 742 in BOTH states because the console was position:absolute left:0 right:0 height:260 zIndex:40 reserving no space. CAUSE FIX: BuildPanel becomes a flow child of the app root (flexShrink:0, maxHeight:50vh); column 742 -> 482 -> 742, canvas bottom 848 -> 588. The magic bottom:22 removed \u2014 measured wrong by 2px and underivable. Rejected: bounding the overlay to the column's edge (no width constant; Panel width is per-facet, and it would still cover canvas and Explorer) and padding the scrollers (workaround, two of them). VERIFIED FIRSTHAND ON THE MERGED TREE: tsc clean, vitest 5108 passed / 7 skipped / 0 failed, and I re-ran the harness independently for 21/21 \u2014 at dpr 1.35 where all the agent's nine runs were dpr 1, so the fix is now covered on both device scales this environment produces. Plants: A (defect restored on both the 17-row and 21-row instruments) reds the four catchers verbatim plus the Home-tab row; B (delete maxHeight) reds 9a/9b, without which those rows assert only 260<=260. Five alternative green-paths ruled out on 5a. Console's OWN usability asserted separately (close button, pulled-to-top error line, 140th output line). One row DELETED rather than weakened: 'the lowest control sits above the console top' is false of a healthy app. Trade disclosed: console now flush with the window bottom, status bar above it. Booked not fixed: minHeight:0 still missing from App.tsx's flex chain \u2014 a real latent hazard, deliberately not bundled because no row here can see it and bundling it is what produced my reverted branch.", "project": "EFFECTS-W1"}
{"at": "2026-08-27T15:52:32Z", "headline": "The canvas now shows what the game would show \u2014 the frame is the camera, and each band slides at its own real speed.", "matters": "You can drag the screen frame and watch the background bands separate the way they will in the ROM, without leaving the surface you edit on. Two things you reported are fixed with it: the guide lines no longer get dragged around when you move the frame (they belong to the artwork, not to the screen), and the frame's vertical position is now a real scene value you can move \u2014 which is what was stopping you placing a layer down in the flowers. Arrow keys nudge the camera a pixel at a time, because a slow band only moves one pixel per sixteen and a mouse drag jumps straight past it.", "detail": "ROADMAP rows 69-71; merged and pushed, origin/master 8105673 -> pushed (ls-remote confirmed). Agent branch feat/camera-preview, six commits. VERIFIED FIRSTHAND ON THE MERGED TREE: tsc clean, vitest 5174 passed / 7 skipped / 0 failed (65 new rows), and I re-ran scratchpad/camera-preview-harness.mjs independently for 26/26 at dpr 1.35 under load 13.2. FOUNDATION CORRECTION I sent mid-parcel, three of four points confirmed by the agent against source: .v_locked does move.w pcfg_v_offset -> Vscroll_BG and Camera_Y is never read on that arm, so a locked scene's layer tops are PLANE ROWS with guide origin 0 \u2014 guides no longer ride the frame. MY POINT 3 WAS WRONG and the agent said so rather than implementing it: the clamp was never a fixed 3..223, row 66 already shipped max(0,3+vo)..min(511,223+vo); the actual blocker was the guides plus v_offset having no gesture on the canvas at all. Decoder transcribed from Decode_Factor_A term for term, 16/16 agreement with the published FACTOR_* names, with the honest non-agreement disclosed: asr.w truncates PER TERM, so camX 7 through FACTOR_3_4 gives 6 rather than 5.25 or its floor 5. The 15-sentinel is numerically invisible for camX 0..32767 (x>>15 == 0), so it is caught structurally and via the vertical sentinel instead \u2014 stated rather than papered over. Arrow keys 1px, 16 with Shift, derived: 16 is the camera move that displaces FACTOR_1_16 by exactly one pixel. A row the agent had labelled a catcher came back GREEN under a plant (it moved the camera horizontally while the rule reads the frame's Y \u2014 failure mode iii verbatim); relabelled non-discriminating with 5e named as the catcher. NOT REPRODUCED and printed on the canvas per scene: curve ramps, deform, transitions, Plane A factors, sprites, priority, the left partial column. Two carry-forwards: Input.dispatchKeyEvent never replies on this CDP target and without a per-call timeout yields a process that exits 0 having reported nothing; and the aeon-tree md5 this role reports was walking other agents' nested worktrees because -path ./.git -prune only prunes the top level.", "project": "EFFECTS-W2"}
{"at": "2026-08-28T19:29:33Z", "headline": "The editor can now show you which foreground art draws in front of Sonic.", "matters": "The lens already existed and worked \u2014 it had simply never been switched on for the engine you use, so the answer was one line away and nobody could see it.", "detail": "aurora master 121521e2; showPriority added to OVERLAY_KEYS_BY_ENGINE.aeon; depiction lifted into tile-lens.ts shared with classic; CDP harness 24/24 re-run by the overseer on the merged tree; veil density measured at 24% of painted art at 100% zoom."}
{"at": "2026-08-28T19:29:33Z", "headline": "Collision angle marks were drawn MIRRORED on the aeon map, and now lie along the slope with a barb showing the solid side.", "matters": "Every sloped angle mark pointed across the surface it described instead of along it, so the view was actively misleading while looking authoritative. 'Collision angles view is just bad' was a correct bug report, not a matter of taste.", "detail": "aurora master 121521e2; one mark module drawn by all five surfaces (both map overlays, picker thumbnails, big preview, paint ghost, legend); tangent from the angle byte, open side from the height sign; 6 unit guards red-first, CDP harness 18/18 re-run on the merged tree."}
{"at": "2026-08-28T19:29:33Z", "headline": "Pasting now shows the art you copied, selections can be made per tile, and undo actually redraws the map.", "matters": "Undo after a paste was reverting the data but never repainting, so the screen kept showing art that was no longer there \u2014 you would have been editing against a map that disagreed with the file. It affected chunk stamps too, not just paste.", "detail": "aurora master 121521e2; batch commands were skipped by the renderer-cache listener while the sibling store-version path recursed; manual per-callsite workarounds deleted; tile-granular selections carry art only, keyed on rectangle alignment because collision is stored per 16px cell; CDP harness 34/34 re-run on the merged tree."}
{"at": "2026-08-28T19:37:10Z", "headline": "A parallax layer guide now says why it stops moving, and dragging one is no longer trapped by the window.", "matters": "Two separate things were happening at once: an invisible legality limit that moved when you moved the view box, and a drag that really was imprisoned by the canvas edge. Your reading of it was half right and I had called it wrong.", "detail": "aurora master 1f65b3a; floor measured as EFFECTS_FIRE_LINE_MIN + v_offset at two offsets (67 at 64, 138 at 135) matching the field report; window mousemove added for guide drags only (a control layer left at vp.y+10=210 instead of 150); the v_offset path can still strand a placed layer below its floor and now surfaces it rather than silently re-clamping; CDP harness 46/46 re-run by the overseer on the merged tree, suite 5277 passing."}
{"at": "2026-08-28T19:57:54Z", "headline": "The test suite is fully green, and the one failure everybody had been calling harmless was ours.", "matters": "A test was comparing our file-writing against a document sitting half-edited in aeon's folder, so it changed colour as someone else typed. Before today it had been green for its whole life while proving nothing, because it had no fixed thing to compare against. Four readers in a row waved it through.", "detail": "aurora master 84c23bf3; fixture vendored at aeon 88909bb3 with a provenance sidecar and a separate currency check that compares content not commit ids; verified by the overseer with no aeon present at all (passes, currency skips loudly) while aeon's file was still dirty, and by planting a one-token drift (fires, naming revision and blob). Suite 5281 passed / 0 failed. A second identical live-tree read was found that nobody had noticed, and two long-silent skipped blocks made audible. 37 reads of the s1disasm tree reported and deliberately left open rather than half-fixed."}
{"at": "2026-08-28T20:00:36Z", "headline": "Painting a tile no longer wipes its priority or its flips, and you can now set them on purpose.", "matters": "Every tile you painted was silently losing its depth and its mirroring \u2014 so art you had set to draw in front of Sonic quietly dropped behind him, with nothing on screen to show it had happened. Four places in the app did it, not the two I found.", "detail": "aurora master 7c9090a; new brush-word rule module \u2014 flips come from the brush (a flip is which picture you picked), priority is tri-state defaulting to keep, chosen because a wrongly-kept priority shows up under the new lens immediately while a wrongly-cleared one is invisible until a player walks behind something. Controls copied from the classic facet's existing chips rather than invented. Harness 21/37 on the broken build, 37/37 fixed, re-run by the overseer on the merged tree; suite 5300 passing, zero failures. Open: the agent-facing paint-region call still flattens omitted attributes."}
{"at": "2026-08-28T20:19:03Z", "headline": "A band's curve is now something you can see \u2014 and the camera preview had been painting itself black under a common setting.", "matters": "You could set a curve and the preview kept drawing the band flat, so the value was authorable and invisible. Worse, the whole in-frame composite was being drawn and then covered over on every frame whenever the Bg Plane overlay was off \u2014 it reported itself as working, with thousands of tests green, and showed nothing.", "detail": "aurora master 02f0117e; the deferral reason on record ('needs a clock') was wrong \u2014 read the engine source, the ramp depends on position not time, while deform genuinely does need a clock and had been lumped with it. Ramp is transcribed from the engine rather than interpolated, because it ramps a negated scroll word and floor division is not symmetric under negation. Roadmap's 'sine params or 256-point freehand' corrected \u2014 that text described a different feature's tables. Harness 29/29 re-run on the merged tree, suite 5310 passing, zero failures."}
{"at": "2026-08-28T20:47:15Z", "headline": "You can now see where a vertical split actually lands down the frame, instead of typing a number into a spinner.", "matters": "A split's number was never a position \u2014 it is the background row you scroll TO, not the line it fires at. The two were easy to confuse and nothing on screen distinguished them; the strip now shows both, and refuses to place a split at all on a scene whose background scrolls, because there the line does not exist until the game runs.", "detail": "aurora master a2faf2cc; three coordinate spaces separated (act / plane / screen), commensurable only when v_factor is locked, verified from aeon 0bee83c6 read at a committed revision; unlocked scenes drawn hatched with the engine's own sentence; splits drawn as one-edged boundaries, palette bands not drawn and said so, because bands are intervals with two edges and drawing one as the other would misrepresent the hardware. No number from aeon's still-moving design is compiled in, and a test asserts that. Suite 5341 passing, harness 25/25 re-run by the overseer, red-first twice on the running app including a reproduction of the drawn-then-painted-black defect."}
{"at": "2026-08-28T21:11:51Z", "headline": "The editor now warns you when a split cannot legally exist on a scrolling background \u2014 and explains why, in the engine's own terms.", "matters": "You could build a scene the game flatly rejects and nothing said so. The code that was supposed to warn you had a comment claiming another warning already covered it; that other warning never existed, and the claim was false for its entire 30-hour life.", "detail": "aurora master f704decf; sentence names the mechanism (two writers to one word \u2014 the parallax step writes the scroll at frame top, the split writes an absolute constant to the same word mid-frame) and carries both engine-named remedies rather than one. Clauses declared once, composed in three places. New currency test pins seven load-bearing claims against the engine's own refusal, identified by condition rather than line number, and skips loudly when aeon is unreachable. Suite 5356 passing; harness 36/36 re-run by the overseer; the panel rows proven discriminating by running against a build carrying master's panel (29/36, exactly the seven panel rows red)."}
{"at": "2026-08-28T23:51:33Z", "headline": "Hold Ctrl while dragging a selection and it snaps the other way \u2014 and middle-click panning works in paste mode again.", "matters": "Your idea, built as an invert rather than literally, because block was already the default so forcing block would have done nothing. The panning bug was the mousedown handler promising something the mousemove handler threw away \u2014 the third time today one handler enforced a rule its sibling ignored.", "detail": "aurora master 84d64c42; one writer feeds preview and commit so they cannot disagree, sampled live including bare Ctrl press and release against the last cursor tile; Snap control shows the effective mode and what release returns to. Pan fix pans first then falls through so the ghost tracks the post-pan cell rather than freezing. No modifier conflict, checked two ways. Suite 5362 passing; harnesses 33/33 and 17/17 re-run by the overseer on the merged tree."}
{"at": "2026-08-29T00:11:45Z", "headline": "Painting collision no longer wipes parts of a cell it does not own \u2014 and the bits everyone believed were spare turned out to already mean something.", "matters": "The loop feature was about to be designed on the assumption that the top two bits of a collision cell were free. They are the second path's solidity. Nobody saw it because the build code never writes the number 14 as a literal \u2014 it refers to it by name, so a search for the value finds nothing.", "detail": "aurora master d5ae5a24; verified independently by the overseer at aeon b76576ea (PATH_B_SOL_SHIFT = 14, used in bake_cell, self-tested there). Aurora's own data is unaffected because it feeds the per-plane encoding which reads only path A \u2014 a narrower claim than 'the bits are free', and the currency test now pins both halves so nobody re-derives the wrong one from empty content. Writer sweep found 15 sites where reading found 3; three were broken, including the art facet's chunk brush that an enumeration by tool would miss. Suite 5383 passing; harness 12/12 re-run on the merged tree."}
{"at": "2026-08-29T00:20:38Z", "headline": "Press X or Y to mirror a selection, or to mirror what you are about to paste.", "matters": "Mirrored tiles cost nothing against the tile budget \u2014 the hardware carries a flip bit per tile \u2014 so mirroring terrain was free in the data and unreachable from the editor until now.", "detail": "aurora master 41d303fe; one transform used at both moments, so the in-place path is literally copy-flip-write and cannot drift from the clipboard path. Mirroring is two operations \u2014 reverse the order along the axis AND toggle each word's own flip bit \u2014 and both half-transforms were planted and measured red (9/27 and 7/27 node, 32/40 CDP each). Collision flips with its own bit positions, which sit at different offsets from the art's; the crossing trap is pinned by a test. Carries the correction that bits 15:14 are path-B solidity, not spare. Suite 5421 passing; harness 40/40 re-run by the overseer on the merged tree."}
{"at": "2026-08-29T01:32:26Z", "headline": "The collision direction marks now lead with which way is OUT, and flipping has buttons instead of only hidden keys.", "matters": "The marks were geometrically correct the whole time \u2014 a flat surface really did draw a flat bar with a tiny up-tick. They spent their ink on the surface direction, which is not the thing you were asking them. At 0 degrees the coloured ink went from an 11-wide-by-5-tall bar to a 1-wide-by-7-tall stem pointing up.", "detail": "aurora master 7da27efd; the tangent is kept, not deleted, because the silhouette is quantised to 16 integer heights and cannot distinguish 26 from 30 degrees \u2014 the bar is the only carrier of precision. Detail threshold derived from that blind band (3.576 degrees) rather than chosen: 57 screen px per cell. An undecidable wall draws a double-ended stem rather than a confident arrow. A pre-existing clip in the big preview was found and fixed. Flip buttons call the same single action the keys call; with them deleted the button harness drops to 10/25 while the key harness stays 40/40, which is what proves they are testing the buttons. Suite 5442 passing; harnesses 16/16 and 25/25 re-run by the overseer."}
{"at": "2026-08-29T05:55:12Z", "headline": "An agent painting a big patch of tiles no longer knocks scenery out from in front of Sonic \u2014 and the same bug was hiding in the Art facet's stamp.", "matters": "You fixed this for the mouse last night. The same mistake had two more roads: the tool an agent uses to paint a region, and stamping a tile in the Art facet over art captured from a real section. Both silently cleared the bit that says 'this square draws in front of the player'. The Art one was invisible to a search, because it builds a cell rather than a word.", "detail": "O12. master 93f7870..90465c6 (pushed, remote verified). Fix routes both roads through the existing single decider core/editing/brush-word.ts: new brushPriorityFromOptional maps the wire's absent/true/false to keep/on/off, and resolveBrushPriority was split out so the cell road (composer-buffer stampTile) shares the definition of 'keep' rather than respelling it. agent-handler.ts no longer imports packNametableWord. save_chunk is a CREATOR and passes undefined as the destination, byte-identical to before. Shared entrySchema resolved by making one description true for both consumers; hf/vf now state they are NOT tri-state. Sweep: 21 writers, 2 broken (the dispatch's enumeration of 2 candidate sites was refuted \u2014 ComposerCanvas.applyTileCell was a third decider, hard-coded pri:false against docs seeded with pri: e.priority at composer-buffer.ts:57,:86). Verified by the overseer on the merged tree, not taken on report: tsc clean; suite 5,468 passed / 0 failed / 7 skipped (405 files) run twice; harness npm run harness:tile-attributes 37->49 rows, 49/49 on three independent runs (dpr=1, rect 284,74,876x721 all three, host up 3d 17h). Red-first re-run by the overseer: planting the !!spec.pri collapse back reddens exactly [w1] and [w4] (47/49); planting the composer's hard false reddens exactly 2 node rows. Non-discriminating rows [w2] [w3] [w5] [w6] are named in the harness's own output, not just the packet. Packet docs/reviews/2026-08-29-agent-paint-priority.md. Half the queue row was wrong and is corrected in the packet: clearing FLIPS on the agent road is correct under the shipped rule, and [w5] now pins it."}
{"at": "2026-08-29T08:14:26Z", "headline": "An agent can now READ collision back out of the editor, so it can check a loop's shape from the data instead of squinting at a screenshot.", "matters": "The paint tool could write collision and nothing could read it. Two things the proposal assumed turned out false: the read could not be written back (the write filled a whole rectangle with one value, and a loop is never uniform), and a 16px cell is really four 8px cells underneath that can disagree \u2014 so the read now says 'mixed' and shows all four rather than picking one and calling it the cell."}
{"at": "2026-08-29T08:40:41Z", "headline": "A brand-new background effect \u2014 layers that drift on their own, like clouds \u2014 can now be read and written without the editor rejecting the file.", "matters": "The surprise is that nothing was ever at risk of being lost. I expected the editor to quietly drop the new setting; it actually refused the whole file, because the format is closed. So a scene carrying it was unopenable rather than damaged. You still cannot AUTHOR drift \u2014 the engine side refuses it until aeon ships their half \u2014 and there is deliberately no control for it, because we already have one complaint open about a dropdown offering values the build rejects."}
{"at": "2026-08-29T08:47:24Z", "headline": "Our own test harnesses were running a kill command that could take down a DIFFERENT Aurora than their own \u2014 including another agent's \u2014 while leaving their own stray process alive.", "matters": "Twenty-eight of them did this, from one shared file. It was actually seen happening on 2026-08-16 ('killed MY Electron mid-run three times') and written down as an environment quirk rather than a bug, so it sat for two weeks. They now identify their own process by descent instead of by name-matching, put your ~/.aurora/mcp.json back exactly as they found it, and refuse to talk to any Aurora they did not start. A check enforces it so the next harness written cannot skip it.", "detail": "O16. master 2838284..1b2e08e (merge bb5fcc5), pushed. Third hazard beyond the two dispatched: pkill -f 'aurora/dist/main/inde[x].mjs' in 28 files, living in canvas-cdp-harness.mjs session() which 15 harnesses import. It matches any Aurora whose ARGV carries the main tree's dist path and MISSES a worktree orphan -- backwards both ways. Overseer narrowed the agent's claim that it matches the owner's editor: pkill -f matches argv, and his electron-vite dev process is 'electron .', so what is established is cross-harness killing (witnessed 2026-08-16 in scratchpad/lens-sweep-2026-08-16/seat-CDP-B.json) plus a production launch from the main tree. Fixed as one shared module scratchpad/lib/harness-guard.mjs, not 98 copies; killTree gets a grace period before SIGKILL (bare SIGKILL loses Chromium's localStorage flush). Gate npm run check:harness-guards, derived from the directory. Launcher count 92->98 after the agent found two of its OWN guards vacuous under plants. Three further gate defects found by the overseer at landing, all from worktree blindness: ELOOP crash on an untracked self-referential symlink, four unguarded untracked launchers no worktree could see, and a headline counting them as clean. Verified: 141->150 classified, tracked plant exits 1, restored exits 0, tsc clean, suite 5527."}
{"at": "2026-08-29T08:56:57Z", "headline": "You can now paint a loop's layer crossover with the same brush that draws the collision \u2014 and painting ground that is solid on both paths is one stroke instead of two.", "matters": "A loop needs the player to swap which collision layer they are on. Until now that was an invisible object someone had to place by hand next to every loop. Now it is part of the drawing. The brush cannot express the illegal case at all, rather than warning you about it. Two honest limits: the engine does not yet read what you paint, so a painted loop does nothing in game; and nothing writes into your real level files until aeon lands a guard against their own repaint tool erasing it."}
{"at": "2026-08-29T09:12:58Z", "headline": "Re-checked every claim in the background-capability survey against the engine's current code. Six had drifted, including two where the fix I proposed would not have worked.", "matters": "That document is what gets read when we plan background work, and it had already sent two people down wrong paths in one night. Every row now carries the exact code symbol it rests on, so the next reader re-checks in one command instead of trusting a three-day-old snapshot. It also turned out the table has 25 rows, not the 21 I had written in my own note \u2014 four rows nobody was counting.", "detail": "O19, master 8b716f5..d17f95b, pushed. 25/25 re-measured at pinned aeon 4f846e25 and empyrean 86925008, 0 unmeasurable; 6 moved (4, 5, 6, 15, 16, plus row 11's context), 19 checked-and-unchanged recorded WITH symbols rather than by silence. Two of my own proposals corrected rather than preserved: row 6's per-band accumulator in the shadow view (Step 4a re-copies from ROM every frame and rotates, so it would build green and accumulate nothing) replaced with aeon's Parallax_Drift_Acc; row 15's bob sized the wrong mechanism entirely (whole-plane, scene-level Step-5 term, not per-band). Per-row 'measured at' markers replace the doc-wide banner. Agent found the most stale content was \u00a7E7 -- about THIS repo, no sibling read needed -- correcting the banner's premise that a peer's source has the shortest half-life. Two live-decision flags: preset binding (an authored palette band costs ROM and shows nothing) and the BG height x depth budget (OJZ act 1 is 2,048 px past its own wrap ceiling, unchecked by any build rule). Two errors found in aeon's own docs, relayed not edited."}
{"at": "2026-08-29T09:23:35Z", "headline": "Our type checker had never looked at 127 of the test files. It does now \u2014 and turning it on immediately found a test that was passing for the wrong reason.", "matters": "Every 'types are clean' this project has reported, including several of mine tonight, silently skipped one directory. The nasty part is that it skipped only 30% of the tests, so the check was right often enough to keep trusting. One test was building a collision plane at half the real width and passing only because the numbers it happened to use were small enough to fit.", "detail": "O18, master c498506..4f347f9, pushed. Blast radius measured before fixing: 15 errors, 10 files, 5 codes; 132 files newly entered the program (782 -> 914). The gap was exactly one directory -- ~289 colocated src/**/__tests__ files were already covered by src/**/*, which is why it survived: a repo where NO tests were checked would have failed loudly the first time anyone looked. One config, not a second tsconfig: measured that test/ needs nothing extra (all 127 import vitest explicitly, no path aliases in use anywhere, no lib divergence). Compiler options byte-identical -- the entire diff is the include array plus a comment. ZERO suppressions: no ts-expect-error, no ts-ignore, no any, no excluded subtree. Real bug found: set-collision-edit.test.ts built Section.collisionEdit as Uint8Array where the field is Uint16Array; green only because its values fit in a byte. Near-miss avoided: widening isSelfMark would have made it answer false for the reserved crossover value, destroying the property that value exists to give; fixed test-side with a narrowing that throws instead. New script npm run typecheck -- cite it, never a hand-typed npx tsc, since inheriting a narrow include is the whole defect. Overseer verified: planted a type error in a test file, typecheck names it and exits non-zero, restored clean; suite 5,637/0 unchanged. Disclosed gap: scripts/ and scratchpad/ .mjs still unchecked (needs allowJs, separate decision). Overseer settled the flagged src contract: S4ActConfig.bgLayout is required non-nullable and correct -- those configs are code literals in profiles/s1.ts, all supplying real paths, so load.ts's guard is harmless defensive code, not evidence of a wrong type."}
{"at": "2026-08-29T09:49:06Z", "headline": "A test file can no longer sit in this repo without anything running it \u2014 the suite now refuses to start if one does.", "matters": "The green total counted only the files the runner happened to look at. A test written in a slightly different place or with a slightly different name was type-checked, looked finished, and never ran once \u2014 and nothing anywhere would have told you.", "detail": "master ef00e5e (merge of worktree-agent-aac33dc49b0c7e647, 5525afd). New scripts/check-test-collection.mjs + npm run check:test-collection, chained into npm test (~0.27s). Compares two live enumerations: `vitest list --filesOnly` (asked, not re-implemented) against a walk for vitest's OWN default test-shaped glob **/*.{test,spec}.?(c|m)[jt]s?(x), deliberately wider than the configured include or the check is circular. Both directions error. Empty collection = COULD NOT MEASURE, exit 2, never green. 416 on disk / 416 collected at landing. Agent proved 3 plants red; overseer independently planted a .test.mts at repo root (shape AND location untested by the agent) -> red naming the file, and independently ran the empty-include vacuity probe -> exit 2. Re-verified on the MERGED tree: 5,637 passed / 0 failed / 7 skipped, typecheck clean. O22 was booked as 'unify the two test locations'; declined as churn with no property (the move leaves the include patterns exactly as narrow) and re-shaped to this gate; the convention question goes to the owner as ergonomics. Booked O24 from the agent's own disclosed limit: collection is not execution, and 2 skipped files / 7 skipped tests are a silent zero one layer deeper."}
{"at": "2026-08-29T10:08:01Z", "headline": "A loop crossover painted in the editor reaches the saved file exactly, and then vanishes \u2014 the ROM never sees it, and nothing would have told you.", "matters": "You could paint loop crossovers across a whole act, save, build, and get a ROM identical to the one you started with. Every check we have would say the build was fine, because it was. The engine half that reads the field simply is not written yet \u2014 so the editor can author something today that has no way to reach the game.", "detail": "LP-3, aurora 2e24094, docs/reviews/2026-08-29-crossover-paint-loop.md, instrument scratchpad/crossover-paint-harness.mjs. Throwaway aeon worktree cut from 4d86f5db, reset clean; nothing landed. document->file HOLDS (13 rows, 2 fixtures): cells written back with their own word + crossover:'hand-off' so geometry is constant by construction; exactly 16 words/plane changed in the whole 131072B file, low 14 bits identical, A=2 B=1, 2 runs of 8. Values derived from LOOP_CROSSOVER_ENCODING.md 3.2/4 at aeon 4f846e25, not from the hub's prose relay (aeon flagged their own number unverified; it was right). file->bake FAILS: collision_pipeline.py defines XOVER_* (57-75) but bake_plane_cell (229) never reads bits 15:14 \u2014 its docstring enumerates 9:0,10,11,13:12. s4.bin 06d2ccf6/719205 both sides, assembler md5 504b0c0a identical on both sides. Units finding: collattr is per-8px-subtile so a 4-cell rect moves 16 words; caught by painted:16 vs an assertion expecting 4. Instrument error kept in the packet: plane 'both' + words writes ONE plane's geometry to both (documented behaviour); row 5b caught it, and ONLY because the fixture had moved to cells with real geometry \u2014 at all-zero cells the same broken call passed 11/11. No section-0 fixture has both planes non-zero AND differing (198 runs, all identical), so two frames were run and each row's discriminating status reported."}
{"at": "2026-08-29T10:12:58Z", "headline": "Three of our own comments promised that the engine would catch a mistake it has no code to catch \u2014 including one an AI assistant reads as a tool description.", "matters": "Someone reading our code would reasonably decide a bad value was safe to let through, because something downstream would reject it. Nothing does. The claims were all true of the plan and none was true of what was built.", "detail": "aurora d8c23f2. crossover-audit.ts ('Aeon's bake refuses these (R2)' / 'The bake hard-errors (R1)'), collision-word.ts ('a self-mark is a hard build error in aeon's bake'), editor-methods.ts ('the illegal value 3 that aeon's bake hard-errors on'). Measured: bake_plane_cell never reads bits 15:14 (docs/reviews/2026-08-29-crossover-paint-loop.md). Each now names the rule, says specified-not-implemented, and cites the measurement. Also corrected a premise relayed TO us: our audit REPORTS (agent-handler.ts:515 puts severity in the reply), it does not refuse \u2014 and Aurora cannot author value 3 at all, since crossoverFor derives it from (brush, plane) and the brush enum has no such value. That is a stronger property than a refusal and was being described as a weaker one. Fourth instance of perishable-claim-in-a-code-comment; OVERSEER.md's standing instruction was to tell the hub at a fourth, done. Shape common to all four: the comment describes an OBLIGATION on another repo and is read as a REPORT about it. Suite 5,637/0, typecheck clean."}
{"at": "2026-08-29T10:15:09Z", "headline": "A test that quietly opts out now has to say why, or the suite fails \u2014 and on a machine without the reference game data, 165 of them were saying nothing.", "matters": "A skipped test and a passing test look identical in the summary line. On this machine only 7 tests skip, so the problem looked tiny; on a machine without the reference data 168 skip, and almost all of them were silent. Anyone else running our suite was reading a green number that had a large hole in it.", "detail": "O24, aurora master 50bc78f (merge of worktree-agent-afaa3a0949767a118). scripts/skip-report-reporter.mjs wired into vitest.config.ts reporters, so bare `npx vitest run` gets it too; 58 skipIf/skip sites across 31+ files converted to carry a reason; unexplained skip sets exit 1. OVERSEER RATIFIED the agent's ruling that silence fails rather than warns, on evidence I re-measured myself rather than accepting: with s1disasm pointed at an absent path (34 files sed'd, restored after) the tree skips 168 and skip-report says OK, every skip named \u2014 so it cannot fire spuriously on a machine lacking fixtures. MY BRIEF WAS WRONG and the agent corrected it: I called the repaired s4_engine rows exemplary; ctx.skip(reason)'s message reaches NO shipped reporter \u2014 verified here independently, the JSON reporter emits meta:{} and the marker string appears nowhere in the output. Reachable only via the Reporter API, which is why this is a reporter and not a pass over --reporter=json. Overseer plant: a reasonless describe.skip (a shape the agent's three plants did not use) -> red, with vitest's own total showing 0 failures, so the gate was the only thing catching it. Merged tree: typecheck 0, check:test-collection 418/418, npm test 5,652 passed / 0 failed / 7 skipped."}
{"at": "2026-08-29T10:35:18Z", "headline": "Swept about 1,200 dead X-server leftovers off the machine; the harnesses that leak them are still unfixed.", "matters": "Test runs had been leaking a virtual display each and never cleaning up, so the machine was carrying 20 dead servers and roughly 1,200 orphaned lock and socket files, and each new run had to climb past them to find a free number. Nothing was lost and your own desktop session was never touched. This removed the mess, not the cause: four launcher scripts still leak on every run, so it will come back.", "detail": "O20 first half, foreground (agents cannot do process work). All 20 Xvfb had been reparented to systemd --user, were 8.5-35 h old, and every one had zero client fds; killed by PID individually, never pkill. Residue was far larger than the booked row: 943 locks / 264 sockets against ONE live X server (Xwayland :0), hence display numbers climbing to :1030. Removed 941 locks + 263 sockets with a live-PID guard, keeping :0. The guard held back :151, whose recorded PID had been RECYCLED onto a Vivaldi renderer thread - so a PID-liveness check on a lock file is two-valued ('that PID is in use' != 'that X server runs'), the same family as [ -S socket ] reporting a corpse as a server. Removed it on the stronger instrument: wrong process class plus no socket. Cause (4 unguarded launchers) open."}
{"at": "2026-08-29T11:08:21Z", "headline": "Six tests had been reporting PASSED while touching nothing at all, and no run could ever have told you.", "matters": "On any machine without the reference game data our suite was lying three different ways: 59 tests said Aurora was broken when the truth was just that the data is not there, three files died on the spot and took 29 more tests with them, and six sat in the green column having never looked at their subject. That last group is the one worth knowing about: two of them could only ever have worked inside a temporary folder an assistant creates, so on your machine they had been measuring nothing for their whole life, and there was no input in the world that could have turned them red. Run the suite with the reference data hidden now and it reports 269 honest skips and zero failures.", "detail": "O25, merge 0c3e73c (branch o25-fixture-absent-honesty, 5 commits). Class A 59 -> 0, Class B 3 files/29 tests -> 0, Class C 6 -> 0, plus 58 rows that never registered at all. Cause of class C: a bare `return` from a test body IS a pass, and console.warn reaches no reporter, total or exit code, so the skip-reason gate landed at 50bc78f is structurally blind to it -- failure and success emit the same artifact. Worst case aeon-json-trailing-newline.test.ts:233 resolve(__dirname,'../../../../../../aeon') = six levels: /aeon from the main checkout, the real path only from a linked worktree (verified by arithmetic, both directions). New gate scripts/check-pseudo-skip.mjs wired into npm test ahead of vitest. OVERSEER-VERIFIED FIRSTHAND, not branch-side: gate red on a clean extraction of 1e7c288 (exit 1, 9 sites); matcher poisoned -> 'scanned 0 bodies in 419 files ... COULD NOT MEASURE' exit 2, ruling out the alternative green path; merged tree tsc exit 0 and 5,656 passed/7 skipped/0 failed; true absence via tmpfs-over-sibling-root in a private namespace 5,332 passed/269 reasoned skips/0 failed exit 0. OPEN, agent-disclosed and I reproduced their exact numbers by an independent condition (empty dirs rather than absent): a PARTIAL checkout still fails 27 files/135 tests, because every guard tests directory existence."}
{"at": "2026-08-29T12:27:27Z", "headline": "The curve dropdown no longer offers you a setting the build will reject \u2014 it is greyed out with the reason on it.", "matters": "Picking a curve target equal to Plane B's own speed makes a ramp that goes nowhere, and the build refuses it. Until now you could pick it and only afterwards be told. It is greyed now, labelled '(engine refuses)', with the full sentence underneath \u2014 deliberately NOT hidden, because a missing option teaches nothing and reads as a bug. Screenshots are in scratchpad/shots-curve-option-disabled/. One rough edge left alone: the closed box clips the last bracket at the current column width, and widening it would move four other rows that are measured against it.", "detail": "O13, merge 7609750 (branch o13-curve-option-disabled). curveGoesNowhere/curveFlatReason are the single source; curveAdvisory and the new curveFieldOptions both read them and the component holds NO fb comparison (verified here by grep on the merged tree), so picker and advisory cannot drift. Merged-tree re-verify: tsc clean, 5,661 passed / 7 skipped / 0 failed, all three gates green; captures viewed firsthand, not accepted by description. TWO PLANTS CAME BACK GREEN AND WERE DIAGNOSED: the wiring test's slice ran to end-of-file and swept in two OTHER pickers sharing the identical disabled={o.disabled} line, so deleting the flag left them holding it green (cause ii, two paths one observable); slice now bounded and the bound itself asserted. Harness row 3f 'no other option is disabled' was trivially true when none was; it names which one now. OVERSEER RULING: do NOT widen to vsplitLockAdvisory. The repo's own test (tableRefParamOptions vs factor0_lock) is disable only when no document content can make the value legal; vsplit's precondition is another control's v_factor in the same document, so it stays advise-don't-disable. Idiom was already here twice; tableRefParamOptions is the banner the drift and band parcels should copy, not this file."}
{"at": "2026-08-29T14:24:08Z", "headline": "You can now write a colour band in the editor \u2014 and the panel says plainly that saving does not put it in the game.", "matters": "This is the authoring half of the parallax/raster work: a band you write here becomes real ROM data through aeon's generator. Three things it still cannot do are printed at the top of the panel, above every control, not hidden in a tooltip: saving does not install the band (a programmer binds it by hand); seeing it at all is a debug key combination whose list your document does not join by itself; and nothing anywhere checks that a band is VISIBLE, so a legal band over an unused palette colour builds fine and shows nothing. It also says outright that nobody in the suite has ever looked at one of these on screen, so there is no preview \u2014 an unfaithful one would be worse than none.", "detail": "EW-BAND, merge a44d91c (branch ew-band-preset-panel). Codec + vendored schema + provenance, full plumbing, provider owning every rule, BandPresetPanel mounted in the effects column. Anchors both re-derived by the agent AND by me: aeon docs/EDITOR_RASTER_PRESETS.md blob 94db6b3a (read past the revert window at 23006094), empyrean schema blob 29c1c5ee at 6664b61, identical at origin/main. Page and schema agree on all seven machine-checked rows. SCOPE HELD: document shape is settled so it is built; band anchoring to layout/camera is out (design absent from aeon's queue; owner sequenced moving bands after). SHARED-MODULE CHANGE, the risky part: json-schema-subset.ts refused unevaluatedProperties beside ANY in-place applicator; the preset schema is the first to pair it with a required-only oneOf, which annotates nothing, so that was a false positive. Now a prover over a WHITELIST of provably-inert keywords ($ref still unconditional refusal). I verified it BY CONSTRUCTION rather than by reading the rationale: required-only ACCEPTS; properties, additionalProperties, an unrecognised keyword, and an annotator nested under `not` each REFUSE with UnsupportedSchemaError, and I printed which path each took rather than trusting a bare toThrow \u2014 my own first probe passed 3 rows vacuously by calling a non-existent export. Merged tree: tsc clean, 5,724 passed / 7 skipped / 0 failed, 422 files collected. Agent self-caught two defects incl. a harness that polluted its own fixture and passed on it. TAGGED: still nobody has seen a band on screen; aeon's cycle-table lint unexercised from this side."}
{"at": "2026-08-29T17:38:58Z", "headline": "Cleared out the development leftovers: four abandoned working copies and forty-one finished branches.", "matters": "Every one of them was work already merged into the main line, so nothing was lost, and I checked that before deleting rather than after. Two files pointing at a dead Aurora process are gone too. If you go looking for an old branch by name it will not be there any more, so I saved a list of every name with its exact position, which restores any of them in one command.", "detail": "Worktrees removed: three under .claude/worktrees/ (ew-band-preset-panel ab8ec1d, o13-curve-option-disabled 597bcb0, o25-fixture-absent-honesty 414fba7) plus a detached gatecheck tree under /tmp at 1e7c288. All four verified clean (git status --porcelain empty; the gatecheck tree's one untracked file, scripts/check-pseudo-skip.mjs, was byte-identical to master's tracked copy) and every HEAD verified an ancestor of master. 41 local branches deleted with `git branch -d` (safe form: refuses unmerged; 41 deleted, 0 refused, which is itself the merge proof); `git branch --no-merged master` was empty beforehand. Name->SHA map saved at scratchpad/2026-08-29-branches-before-sweep.txt (42 lines); all objects still present, spot-checked on three. HEAD unchanged at 97a18dc, no tracked modifications. Origin still carries master + feat/aurora-polish-shell; no remote ref was touched. Discovery files: see d-19 closure in decisions.jsonl. X-server leak (O20) re-measured: 0 Xvfb, 0 locks, 0 sockets, so the 10:35Z sweep held and the unfixed cause has not re-triggered."}
{"at": "2026-08-29T18:07:55Z", "headline": "A stamped chunk now remembers where it came from \u2014 the groundwork for fixing a chunk once and having every copy follow.", "matters": "This is the shape you asked for: remembering is the default, and detaching is the deliberate act, so a wrong guess is always recoverable. It survives closing and reopening the project. Nothing has changed on screen yet \u2014 there is no checkbox and no live updating \u2014 because anything visible has to be proven by driving the real app, and that is the next piece. The one thing worth knowing: the classic half of the editor could not be copied, for a reason in the data rather than the code, so this is a second mechanism and not a shared one.", "detail": "Owner ruling d-18c, phase 1. Merge 39e4905 (merge commit e831caf, branch feat/aeon-chunk-identity, 3 agent commits). Section.chunkLinks = per-TILE Uint32Array plane of stable placement ids beside the nametable + ChunkPlacementLink records; persisted as a new section_N.chunklinks.json sidecar. Classic's approach (level IS a grid of chunk ids) is unavailable because aeon's tileGrid.nametable IS the exported artifact and a ChunkDef is a variable widthTiles x heightTiles footprint, so one id per cell cannot say which PART of a chunk a cell holds. OVERSEER VERIFICATION on the merged tree: tsc clean; 5783 passed / 7 skipped vs base c4534d3 at 5724/7 measured in the SAME tree (+59, skips unchanged). A first base run in a separate worktree read 5717/14 and was discarded as an environment difference, not code -- both sides re-measured in one tree rather than stitched across two. Two violations planted independently of the agent's 13: (a) dropping the chunkLinks carry from cloneSection -> exactly one row red, 'cloneSection must carry chunkLinks: expected undefined to be defined', while the pre-existing key-set guard stayed GREEN, confirming the agent's own disclosure that it derives from createSection and cannot see an optional field (bar 12); (b) defeating the understood() gate in buildAeonSavePlan -> the row asserting a refused sidecar is NOT overwritten went red. Both restored. Two existing map-stamp rows changed expectation on purpose and both are STRENGTHENINGS: (f) keeps the surviving half of the old property and adds the detached-form-is-null discriminator; (g) trades toHaveLength(1) for an explicit no-collision-child assertion. Persistence handles the section-meta erasure defect class explicitly: a refused sidecar is left on disk, and the test asserts the refusal stayed SCOPED (other section files still saved). OVERSEER ADDITION at landing (39e4905): row 91 now names the ORDERING CONSTRAINT -- wire the renderer brush link-breaks (seam 4) before or with the propagation call (seam 3), never after. buildActPropagationCommand verified to have no caller in src/ (tests only), so the gap is latent today; wiring propagation first would silently overwrite hand-painted art with library tiles."}
{"at": "2026-08-29T18:58:00Z", "headline": "Fix a chunk once and every copy of it in the level fixes itself \u2014 with a checkbox for the copies you want left alone.", "matters": "This finishes what you asked for. Stamping remembers by default, so correcting a chunk corrects the whole level; ticking the box makes a stamp a plain copy that nothing will touch again, and you can detach one you already placed. Hand-painting over part of a stamp detaches only the tiles you painted, so your touch-ups are never overwritten. Proven by driving the real editor and clicking the real controls, not by unit tests \u2014 the tests cannot see the screen. One limit worth knowing: copies stamped in OTHER acts keep their link but are not updated yet.", "detail": "Owner ruling d-18c phase 2. Merge 84e3092f (branch feat/chunk-identity-ui; 6656e747 brush link-breaks, 4f88ac9f checkbox+detach, 5e7f9b5f propagation). ORDERING CONSTRAINT (overseer's finding at phase-1 landing) honoured and verified, not trusted: link-breaking landed two commits BEFORE propagation. VERIFICATION on the merged tree: tsc clean; 5799 passed / 7 skipped vs master 5789/7 measured in the same tree (+10, skips unchanged). CDP harness re-run by the overseer in the foreground FOUR times across two rebuilds, 10/10 each, dpr + canvas rect printed per run (dpr=1 observed; the agent additionally ran it at 1.35, so both regimes this box produces are covered). OVERSEER'S OWN PLANT, independent of the agent's seven: cutting link-breaking at the endPaintStroke site ONLY -> row 6 RED, row 7 GREEN, proving the three near-identical set-tiles dispatches are genuinely distinguished (the documented hazard where a plant in the wrong one survives a full cycle looking convincing). Row 10 also reddened, catching the exact damage the ordering constraint exists to prevent. Restored, 10/10. Agent's own disclosures verified as honest: row 1 is the anti-vacuous gate only, and row 10 stays green under poison F (with nothing propagating, 'it did not touch the hand-painted tiles' is vacuously true) - recorded in the poison file rather than counted. Agent also corrected a wrong prediction of its own in place (poison C reddens rows 2/3, not 2/8). OPEN, documented not glossed: cross-act propagation (buildActPropagationCommand is indexed by flat act slot and one command belongs to one act's undo stack); the harness never saves, so the sidecar round trip of gesture-written links rests on phase 1's node tests."}
{"at": "2026-08-30T00:34:46Z", "headline": "A comment in our own code was telling every session not to build the timeline editing you just approved \u2014 the reason it gave stopped being true a day ago.", "matters": "Nothing was broken on screen, but the cost was real: the raster timeline is read-only, and the file said that was a deliberate ruling because a piece of aeon's design had not landed. That piece landed on the 28th and the comment did not move, so for a day the code was forbidding work nobody was actually blocked on. It only surfaced because the hub proposed the exact parcel the comment ruled out. The header now leads with the correction and keeps the old paragraph underneath as the record, and the roadmap rows say the same thing, so a fresh session cannot read the old version and stop. Alongside it: your effects half is now priced row by row, and every row that genuinely has to wait names the FILE it is waiting for instead of 'aeon'. Two smaller finds are booked too, one of which is worth knowing \u2014 per-section backgrounds work on your machine and look broken from any fresh copy of the repo, because the assignment is saved but the picture it points at is not.", "detail": "master 40d75b23, pushed (ls-remote verified). ROADMAP \u00a75.1 rows 92-101 price DoD item 12: 92 preset MCP tools (S, startable), 93 assign_section_preset split out (XS, gated on aeon's effectsRef + the CR \u2014 no SectionMeta field exists), 94 timeline split/edge drag (M, startable), 95 the clock split out (M, gated on aeon item 4; scoped to the anchor mover since camera bands preview clocklessly), 96 drift (S), 97 variants/cycles (M), 98 per-line authoring as a NEW field via schema CR never a widened curve (L; curve is pinned 'none'|{to} with unevaluatedProperties:false, re-verified at empyrean origin/main), 99 controls for aeon items 7-11 (L, absorbs old row 55), 100 docs/MCP.md stale in two paragraphs, 101 tracked-ref/untracked-body on the BG library. RasterTimelineStrip.tsx header corrected; tsc clean. Owner's go verified firsthand at empyrean origin/main 7149b39, not from the relay."}
{"at": "2026-08-30T01:21:32Z", "headline": "An agent can now read and write the raster presets an author builds by hand \u2014 the first of the two effects pieces you approved is on master.", "matters": "The band presets that drive the raster effects could only be edited by a person clicking in the panel. Now a Claude agent can list them, read one whole, and write one back, going through the exact same rules the panel does \u2014 so an agent and a person cannot disagree about what is refused. Two things are worth knowing. The queue row promised this was pure wiring over a finished provider; the agent building it found that was true of every EDIT and false of whole-document REPLACE, and said so instead of quietly filling the gap \u2014 the row now records its own error. And the deliberately missing fourth tool, the one that would bind a preset to a place in the level, is still missing on purpose: there is no field on a section to write it into until aeon lands one.", "detail": "ROADMAP row 92 / DoD item 12. Branch parcel/preset-mcp-tools, merged 3b6710fe, pushed. list_effects_presets (plural, resolving the spec's factored shorthand, ruled by this lane) / get_effects_preset / set_effects_preset in agent-handler.ts + editor-methods.ts + agent-protocol.ts; replacePresetCommand ADDED to providers/effects-preset.ts (the gap, mirroring replaceSceneCommand at effects-aeon.ts:2178, verified absent at base 178e3223). 15 new rows; suite re-run on the MERGED tree 5814 passed / 7 skipped / 0 failed / 426 files, tsc clean. Overseer independently re-planted the agent's P8 poison (disabled the codec's reserved-key check): row went red with the quoted assertion and the fallback was the generic schema refusal, confirming its cause-(ii) analysis rather than adopting it. assign_section_preset absent and verified absent (comments only, SectionMeta untouched); the two stale docs/MCP.md claims deliberately untouched (row O30)."}
{"at": "2026-08-30T01:47:42Z", "headline": "The wall of orange text in the effects panel is down to a third of its height, and every field it was burying is back on screen.", "matters": "The warning that fires when a scene asks Plane B to do two contradictory things was correct and enormous \u2014 21 lines, about half the panel, pushing five controls out of sight along with two hints that were cut off mid-sentence. It now shows what is wrong and what to do about it, with the paragraph explaining WHY behind a 'Why this happens' line you click only if you want it. Not a word was rewritten. Two things worth knowing. The old checks could not have caught this going wrong: with the explanation permanently welded shut, three of them still passed \u2014 I welded it shut myself to confirm that rather than take it on report, and nine new checks now watch whether the text is actually painted rather than merely present. And I had this row's own measurement wrong: my pixel figures were in a different unit than the sentence claimed. The proportion, which is the part that mattered, held.", "detail": "O15. Branch o15-advisory-shape, merge on master, pushed. vsplitLockAdvisoryParts/sceneVsplitLockAdvisoryParts split diagnosis/mechanism/remedies; joinAdvisory keeps the one-string form byte-identical so splitRefusal and inherited tests are untouched; column-layout.tsx gains Advisory. Harness 45/45 at SCREEN=1920x1080 re-run on the MERGED tree; node 5817/0/7 on the merged tree (5814 after the preset parcel + 3). Overseer re-planted the welded-shut poison independently: 43/45 with [5a],[5b],[9d] green, confirming the hazard. Relabelled [5a]/[5b] (they said 'ON SCREEN' while being text-presence rows) with the measurement recorded at the rows. Agent contradicted the brief on six points, all verified and all correct: 36/36 was unachievable as written (3 rows re-aimed), splitRefusal is a THIRD site, my crop offsets were device px at dpr 1.35, the shot framing depended on scrollIntoView, its own first node row was vacuous, and there is no ROADMAP row O15 (rows end at 101). Packet section 2 corrected for unit; ratio 0.48 stands by two independent methods. Before AND after committed with -f; the reversal on the after is recorded."}
{"at": "2026-08-30T02:13:54Z", "headline": "The tool manual told other agents that nothing they author here reaches the game. It has not been true since the 22nd.", "matters": "An agent reading Aurora's manual before authoring a parallax scene was told to expect the work to stop at a file on disk. It reaches the ROM, and a normal Build & Run carries it there. The same page also promised a background export step that was deleted two weeks ago, and understated how many scroll layers a scene may have.", "detail": "docs/MCP.md, merge f0df2e2f pushed. Three claims re-measured against source, not repaired from the queue row's assertions. (a) the {zone}_BG_{id}/BINCLUDE promise: exportAct and its export/ dump went at 2026-08-19 (src/core/project/aeon/save.ts:11-23) and no generator reads bgLayoutRef; the shipped act's single templated sec_bg_layout is `default`, so per-section BG is editor/preview only and the act-wide editor_bg_override.json is what bakes. (b) effects_gen.py BUILT AND WIRED 2026-08-22, both binding fns called from act_descriptor.emp, FAST=1 re-bake on editor staleness, canonical build refuses on a stale bake. (c) layers 1-8 -> 1-16 per the vendored schema. Overseer re-verified all citations independently, aeon's by text not line number. Merged tree 5817/0/7, tsc clean. Two handoffs: aurora row O33 (assign_section_bg's reply does not disclose the unbaked binding); aeon's tools/EFFECTS_CONSUMER_CONTRACT.md:101 still says effects_gen.py 'does not exist yet', contradicting its own status table twelve lines above and the likely origin of the false claim removed here."}
{"at": "2026-08-30T02:51:15Z", "headline": "Our test harnesses have been quietly burning display numbers for five days, and the guard we added last week is what made it certain.", "matters": "1,500 stale directories and 100 dead lock files had built up since the 25th, and every leaked lock permanently burns an X display number so every later test run scans longer before it can start. It is now cleaned up automatically as each run ends. The residue already on the machine is still there and still growing \u2014 from other projects' test runs, not this one.", "detail": "O20+O23, merge 055bff40 pushed. Cause: /usr/bin/xvfb-run has no trap, so its whole cleanup sits after the command line and is skipped whenever the wrapper is signalled \u2014 which is what killTree does on every teardown, so O16's orphan fix guaranteed this leak. Two leak rates, the uncounted one 17x the counted: tempdirs leak on every teardown, locks/sockets only on abrupt ones. Both rows were wrong: no .sh starts an X server (the four unguarded launchers are .mjs), and two .sh DO launch by proxy. reapDisplays() is scoped to artifacts named by processes inside a tree the harness launched. THE PARCEL'S REAL LESSON: boundSocketPaths() failed OPEN against its own docstring \u2014 an empty Set meant both 'I could not look' and 'nothing is bound', so gate 3 would invert from a refusal into a permission and leave the never-reap list as the only thing protecting the owner's desktop socket; latent, not live. Found by an overseer poison that came back GREEN because gate 3 was covering for gate 1. Now a null sentinel read as gate 0, refuse everything loudly. Two further poisons re-planted independently by the overseer post-fix, each firing in exactly one row. A third silently no-op'd against a changed declaration and returned a clean 20/20 \u2014 proof-the-edit-landed is now the rule before any plant is believed. Merged tree: xvfb-reap 20/20, guard checker 166 classified / 0 failures / 0 unmeasurable, node 5817/0/7, tsc clean."}
{"at": "2026-08-30T02:55:41Z", "headline": "Correction to tonight's leak note: the leftover files are our own, not another project's, and I said otherwise.", "matters": "I told you the leak was still growing from someone else's test runs. It was growing from mine \u2014 I had deliberately broken the cleanup three times to prove the fix works, and the files that appeared were the proof. The cleanup itself is real and still landed. I also called the leftover files a speed hazard; measured, the cost is under a millisecond per run, so that was overstated.", "detail": "Corrects the 02:51Z entry and re-scopes queue row O34; nothing in merge 055bff40 changes. Seraph challenged the suite-wide claim and was right to; its own relayed figure (locks zero) was wrong \u2014 locks are 108, sockets 91, tempdirs 1522, 0 live Xvfb, re-measured with find. THE ERROR WAS INSTRUMENTAL: I reasoned from a DELTA (+17 in every class across two rounds) where an ABSOLUTE with timestamps was available the whole time, and a delta cannot see its own author. The fingerprint settles it \u2014 every lock today falls in a burst of exactly 17 at 9-10s intervals with a ~25s tail, six identical bursts, one inside my own session window. One producer, aurora's own harness. Two traps banked: find -printf %TY is LOCAL time so the '08-29' bucket is today (02:54 UTC = 22:54 EDT); and my launch-count grep counted string mentions, so the CHECKER \u2014 which greps for those strings \u2014 topped it at exactly 17 and matched the burst size by coincidence. Still open: the sweep (foreground, held while peers are live) and 6 unguarded-untracked launchers in the owner's tree that the landed fix does not reach."}
{"at": "2026-08-30T03:50:10Z", "headline": "Four things in the editor claimed something was alive when they could not know it \u2014 including a test that rejected every correct emulator.", "matters": "A test guarding your playtest runs had been aborting on any up-to-date emulator and passing only on a stale one. The editor also could not tell you which emulator answered it, so a session against the wrong one failed later and further away, looking like features that stopped working. And a tool reported success for a background assignment that never reaches the game.", "detail": "O26+O28+O32+O33, merge 6da97049 pushed. One class: an artifact asserting a liveness it cannot know. O26 pinned the served method COUNT at 35 and threw otherwise; the real count moved 35->52->53->55 in a day, twice while the agent worked, so the check was inverted against its purpose \u2014 now derived from the methods actually called. O28 the discovery file outlived every process it named (verified live at dispatch: a dead pid on disk); writer cleans up on signals, each re-raised so the app stays killable, and readers annotate liveness. O32 there is no port and no server selector, so 'default to the new core' is not expressible as client config \u2014 the honest row is an assertion: reads implementation (registry-closed), never serverName (a deployment label the contract forbids discriminating on), never serverBuild.id for equality. Denylist not allowlist so a future registry amendment warns rather than becoming an outage. O33 reply shape only, storage unchanged. Overseer checks before landing: confirmed the owner's live emulator would NOT be falsely refused (his frontend holds /run/user/1000/oracle.sock, on Aurora's default chain; implementation shipped at oracle 0a4313ea 2026-08-25, his process started 2026-08-29), then planted an independent poison making 'cannot tell which server' proceed silently \u2014 3 rows fired naming that verdict, 11 correctly unmoved. Agent's own disclosures kept: its [w1] row went green over a plant because it matched a variable NAME not the mistake, and its O33 wording rows all survived a plant that stopped writing entirely. Merged tree 5848/0/7 (baseline 5817), tsc + build clean, guard checker 170 classified / 0 failures. Parked: O35 the window icon does not render-verify (decode proven, render unmeasurable \u2014 no WM under Xvfb); O36 a Wayland detach hypothesis explicitly not measured."}
{"at": "2026-08-30T04:35:44Z", "headline": "The background library lists seventeen backgrounds and ships none of the actual images \u2014 and saving would have deleted sixteen of the names.", "matters": "Anyone opening this project on a fresh machine gets a library promising seventeen backgrounds with no image data behind any of them, and a section already assigned to one. Everything looks right on your machine, which is why nobody caught it. Worse: adding one background on such a machine would have silently rewritten the list down to that one name. The editor now says which pictures it could not open instead of quietly showing you the default, and never blocks you over a missing one.", "detail": "O31, merge 3725bce7 pushed. Manifest TRACKED with 17 entries, 0 of 34 bodies tracked; section_0.meta.json tracked holding bgLayoutRef into untracked bytes; section_4 the control (same tracked shape, ref null), so the sidecar is not the fault. Honest statement corrected against the row: after f0df2e2f nothing bakes per-section BG at all, so the cost is to the EDITOR not the ROM \u2014 authoring against a picture that is not the one the project names. The unnamed defect: buildAeonSavePlan wrote the manifest from resolved entries only; mergeBgLibraryIndex now keeps unopenable names in manifest order. Overseer: aeon untouched (34 bodies intact, 315,394 B by du); poisoned the merge back to narrowing with the edit proven landed first, 4 rows fired across 2 files. Correction to the agent that STRENGTHENS the ask: git check-ignore -v names .gitignore:2 (blanket *.bin for build outputs), not the :118 experiments rule \u2014 and aeon's own file says source .bin must be tracked, citing ojz_tiles.bin failing the same way. Booked O37 (the ask, with both options priced) and O38 (aeon-open toasts failure notices as 'success'). Merged tree 5870/0/7, tsc clean, guard checker 171 classified / 0 failures."}
{"at": "2026-08-30T05:07:53Z", "headline": "Thirty-six tests could only ever run on your machine, and a wrong folder path made forty-three of them lie about why they failed.", "matters": "The test suite had your home directory typed into it in three dozen places, so on any other machine those tests could only skip. Worse: if the reference folder was present but empty \u2014 a half-finished clone, a wrong path \u2014 forty-three tests failed with messages that never mentioned the folder at all, sending the next person to debug a parser or a symbol that was fine. They now say what is actually wrong. A cleanup bug that had been dropping copies of the disassembly into the repo is fixed too.", "detail": "O11, merge 9f3e4d22 pushed. Row stale both halves: 306 rows in 36 files depend on the tree, not 37, and one file's single literal gates 51 rows \u2014 counting literals measures editing effort, never coverage. Absent-input audit, three numbers, two scenarios: tree GONE 306/306 noticed, 0 misdirected; tree PRESENT-BUT-EMPTY 43 misdirected, now 0 via a marker check rather than a bare directory test. Scope disclosed, not implied: does NOT cover a real checkout missing what a row reads (20 rows, booked O39). Two rows used walk-up finders that looked correct and still opened the real tree, caught by an fs-level trace. Also fixed: s1-saveback-roundtrip mkdtemped into the REPO ROOT and restored the api before rmSync, so a failing run left nine untracked un-gitignored copies of the disassembly one `git add -A` from being committed; removal moved to finally. Overseer: sibling checkout untouched (f6ece65, same 4 dirty entries); planted a hardcoded path back into a real test file with the edit proven landed first, gate fired naming file, line, text, derivation and its comment exemption. Three empty husks dated 08-29 removed by hand. Merged tree 5874/0/7, tsc clean. Framing corrected in the code comment: the two classes differ in urgency and self-correction, not seriousness \u2014 the 'worse than' clause was mine and was wrong."}
{"at": "2026-08-30T05:38:59Z", "headline": "A test file could vanish from the suite entirely and the totals would not say so \u2014 ten tests were doing exactly that.", "matters": "When the reference disassembly is missing one folder, twenty-two tests used to fail blaming the image codec, the project store, or node's path module \u2014 never the missing folder. They now name it. And one whole test file was dying before it could register, so ten of its tests silently left the count, four of which did not need the folder at all. A run that loses tests reports the same shape as a run that passes them.", "detail": "O39, merge 2b0f610d pushed. The disclosed tail of 9f3e4d22. List was 20, is 22 in 13 files, via TWELVE incomplete scenarios: which rows misdirect is a function of which subtree is missing (no-artnem 14, no-artunc 5, no-maps-sub 3, markers-only 20), and only the union finds 22 \u2014 the two extra never misdirect on markers-only, the tree the first audit used. 19 rows skip naming the file, derived from the adapter's own enumeration not a hand-list; 3 KEEP FAILING and name the tree because their subject is the checkout's completeness, so skipping them would make an incomplete tree entirely green (needed S1_FINGERPRINT exported, the only shipped-code change). Outside the brief: priority-mask.test.ts died at COLLECTION under no-map16 because its describe BODY read a file \u2014 a collection death produces NO failing rows, so a failure-message audit is structurally blind to it; and s1-open-refusal's 'animations stay ABSENT' passed vacuously, an empty-timeline assertion satisfied by an open that read nothing. No loud-to-silent trade measured across all twelve: 0 failed->passed, 0 failed->gone. Anchor coverage measured not asserted: red in 9/12, 0/3 under no-anim, limit recorded. Overseer: peer untouched (f6ece65, same 4 dirty); planted a real early-return bug in renderChunk against the COMPLETE tree with the edit proven landed first \u2014 guarded goldens went RED, not skipped. Agent's pushback accepted: the fork was skip-vs-keep-failing, not vendor-vs-derive; no row here pins peer values. Merged tree 5874/0/7, tsc clean."}
{"at": "2026-08-30T06:00:40Z", "headline": "Our automated tests have been running on your actual desktop this whole time, not on the invisible screen they set up for themselves.", "matters": "Every automated UI test starts a hidden off-screen display, then launches the app. The app was ignoring that display and connecting to your real desktop instead \u2014 the hidden one was created, paid for, and never used. It explains a measurement oddity we had been recording as a quirk for weeks, and it means these tests have been able to put windows on your screen. Fixed in one place, covering all hundred-odd launchers.", "detail": "O40+O36+O27, merge 3c1639f2 pushed. O36 CONFIRMED by measurement: Electron under the old harness shape, DISPLAY deleted, Xvfb asked for 1001x777, reports 2844x1600@1.35 and 1920x1080@1 \u2014 the owner's two monitors. Cause: the session exports ELECTRON_OZONE_PLATFORM_HINT=auto, inherited by every {...process.env}. The previously-proposed remedy is REFUTED: deleting WAYLAND_DISPLAY still lands on his compositor (wl_display_connect(NULL) falls back to literal wayland-0 under XDG_RUNTIME_DIR; removing XDG_RUNTIME_DIR segfaults instead of falling back), and both hint-style flags fail on Electron 41.2.1. Only --ozone-platform=x11 works, pinned once in spawnGuarded. RETIRES the banked 'dpr varies run-to-run' anomaly \u2014 it varied by which monitor Chromium called primary; two memories corrected. O40: the gate argued its exclusion from a cost it was already paying and the two copies had diverged, so under an override it printed a confident OK while forbidding a string no test could use; now exits 2 saying the run is not evidence. O27: row wrong, no diff, 40 occurrences and zero defects. Overseer ran the ozone proof independently 4/4 and read the RED row, not only the green; confirmed the premise in this session's own env. NOT measured deliberately: the windowed case, because these harnesses do create windows and he is logged in. Merged tree 5874/0/7, tsc clean."}
{"at": "2026-08-30T06:21:41Z", "headline": "Opening a project reported \"this file could not be read, fix it by hand\" as a green success message.", "matters": "Every message shown while opening a project was painted green, including outright failures, on a two-second dwell too short to read. A failure now arrives as an error and stays up; the one genuine success stays green. The inversion was the real risk \u2014 making everything a warning would train you to ignore the channel, so two tests fail if the good news ever stops looking like good news.", "detail": "O38, merge 68fc5c84 pushed. Severity carried from the producer, the only place that knows what it did: 5 error (read/parse failures), 2 warning (legacy bg-override upgrades \u2014 nothing failed, but the file is not what it looks like and the next save rewrites it), 1 success (atlas unification). No 'info' level, deliberately. MY DISPATCH FIGURE WAS WRONG: 25 sites in 9 files is really 12 statements in 5 files; my grep was an OR counting declarations, and two 'files' use 'notices' as an English verb in a comment \u2014 a grep counts a spelling, not a population, the rule I relayed to the hub the same night. The type change is NOT a complete backstop: Array.join accepts an object array, so `.join(' ')` kept typechecking and would have produced '[object Object]'; tsc caught `.some(...)` and missed `.join()`, 9 sites failing at runtime instead of compile time. Overseer planted the property the agent claimed but did not plant \u2014 severity made optional plus an unclassified push \u2014 proving the required field is the sole guard (TS2345 when required; production compiles clean when optional). Not proven and tagged: that the toast PAINTS amber/red; the node suite cannot see paint. Merged tree 5883/0/7, tsc clean. Booked O43 (no toast cap \u2014 7 ten-second toasts where there were 7 two-second ones) and O44 (the join() hole)."}
{"at": "2026-08-30T07:05:00Z", "headline": "The background repeats before the act ends \u2014 and the number everyone was quoting for it, 2,048, was on the wrong axis and was not a wrap width at all.", "matters": "Backgrounds are meant to repeat sideways; that is how a forest tiles forever, and the editor now just tells you how often, in a tooltip that costs no space. Repeating UP AND DOWN is a tear \u2014 sky reappearing below the ground \u2014 and until now the editor let you cause one with a single spinner and said nothing until the game ran. It now says so, and only then: it is silent on every background that exists today, and it goes quiet again the moment you take the fix it suggests. A warning you would see on every level is a warning you would learn to ignore.", "detail": "O21, branch fix/bg-wrap-visibility. THE ARITHMETIC FIRST: 64 is PLANE CELLS, 512 is PLANE PIXELS (the wrap modulus, both axes: BG_WIDTH 64 x TILE_WIDTH_PX 8; aeon PLANE_H_CELLS = PLANE_V_CELLS = 64), 2048 is WORLD PIXELS (SECTION_PIXEL_SIZE, aeon SECTION_SIZE $800). A world px and a plane px are different units and the exchange rate is the parallax factor, per band horizontally and per scene vertically -- so comparing 2048 to 512 is a category error, not an arithmetic one. The row's 2,048 is the VERTICAL OVERSHOOT, traced to this repo's own BG capability survey row 11 (6144 act height - 4096 ceiling at v_factor 3), and it equals SECTION_PIXEL_SIZE BY ACCIDENT, which is why it reads as a wrap width once relayed without its axis. CORRECTION TO THAT SURVEY'S HEADLINE, relayed not edited: it compares act EXTENT; the camera never reaches the last screenful, so the seam-predicting quantity is TRAVEL (5,920) and the figure is 1,824. AGREEMENT, labelled as such rather than dressed as a derivation: Aurora derives travel 5,920 from SECTION_PIXEL_SIZE and SCREEN_HEIGHT alone, and aeon read Camera_Y_Max = 0x1720 = 5920 live off the ROM (d-31). PREMISE: 'wraps 2,048 px' NO; 'before the act ends' YES (aeon witnessed the seam on the running ROM); 'nothing checks it' YES, fully -- aeon's DEFERRED_WORK says no build rule does, and Aurora's renderBg draws the plane once at world origin with no parallax while cameraPreviewPlan only answers at one anchor. BUILT: src/renderer/canvas/bg-wrap.ts (PLANE_COLUMN_SPAN, actReach, bandReach, verticalReach, verticalWrapAdvisory), the fb tooltip readout, the V-factor advisory, aeon.state() now reporting the act grid so a harness derives expectations from the act instead of copying the app's answer. NOT BUILT AND NAMED: canvas marks on the map at each repeat's world position -- the geometry now exists for it. NO LITERAL 512, 2048 OR 5920 IS AN EXPECTATION anywhere: the vitest file spells each figure as the expression producing it and the harness rebuilds the span from the vendored contract plus bg-tiles.ts. RED-FIRST UNDER FOUR PLANTS, including the row's own category error (expected 2048 to be 512) and the advisory-rendered-to-nobody (17/27) -- under which [4h] 'taking the remedy silences it' STAYED GREEN, which is exactly why it is paired with [4a]. Node 5,904 passed / 0 failed / 7 skipped, base 5,883/0/7; rows COLLECTED 5,890 -> 5,911, +21 = exactly the new file. Harness bg-wrap 27/27. tsc clean. Aeon read only, at origin/master, cited by text; d-31 is superseded twice and its recommended option names Aurora's BG canvas as the bigger half -- it does not change this parcel, because verticalReach measures the RELATION rather than asserting the constant, so the advisory retires itself the day the plane stops being the ceiling. No emulator; nothing on the owner's display."}
{"at": "2026-08-30T06:48:33Z", "headline": "The background repeats partway up a tall act, and the number everyone had been quoting for it was measuring something else.", "matters": "A tall level's background runs out and starts over before the level does, and nothing told the author where the seam falls. The editor now says so \u2014 but only when it can actually happen, so it stays silent on every scene that exists today rather than becoming another warning you learn to ignore. The figure in our own survey was also measuring the level's full length where it should have measured how far the camera can travel.", "detail": "O21, merge c47322c0 pushed. Three numbers on three axes: 64 plane-B cells, 512 plane-B px (the wrap modulus), 2048 world px (one section). The row's '2,048' is the vertical OVERSHOOT and equals SECTION_PIXEL_SIZE by coincidence \u2014 which is how it came to be read as a wrap width. Derived: extent 6,144, travel 5,920, v_factor 3 ceiling 4,096, seam at camera Y 4,096 with 1,824 px over it; no wrap at v_factor 4. Survey correction: extent 2,048 vs travel 1,824, and only travel predicts the seam. Horizontal repetition is the invariant so it gets a tooltip readout at zero panel height; vertical is a tear so it gets an advisory. Fires on zero existing scenes. Overseer recomputed every figure and planted the survey's own error: 3 rows fire, one cross-checking against Camera_Y_Max 0x1720 read live off the ROM by aeon. Merged tree 5904/0/7, tsc clean."}
{"at": "2026-08-30T06:48:33Z", "headline": "Cleared 1,731 leftover files our test runs had been dropping in temporary storage since the 25th.", "matters": "These were the leftovers from the display bug fixed earlier tonight. Your own desktop session was excluded by name and verified intact afterwards. Nothing else on the machine was touched.", "detail": "O34. Removed 111 locks, 94 sockets, 1526 tmpdirs; 1/1/2 remain and are correct (the owner's Xwayland :0, plus two tmpdirs newer than the threshold). Method: an AGE THRESHOLD of 30 minutes eliminates the lock-before-bind race entirely \u2014 xvfb-run creates the lock BEFORE the server binds, so a bound-socket check alone cannot see a display mid-startup, but nothing 30 minutes old is mid-startup \u2014 plus explicit :0 exclusion and a bound-socket check from /proc/net/unix. Taken at a boundary with nothing in flight, as recorded in the row beforehand. Overseer counting error on the way, third of that family tonight: read grep -c over /proc/net/unix as 61 bound sockets and nearly aborted; it was 61 connection rows for ONE socket."}
{"at": "2026-08-30T07:30:27Z", "headline": "The art composer could set three of a tile's four placement flags and not the fourth, with no way to even see it.", "matters": "Placing a tile in the composer lets you set its palette line and both flips, but not whether it draws in front of or behind the player \u2014 and nothing showed you which tiles already had that set. Both now exist: chips to set it, and a shaded overlay to see it, matching the ones the map editor already uses.", "detail": "O17, merge 7a8ee569 pushed. MY DISPATCH NAMED THE WRONG FILE: I identified the Art facet as ArtBrowser.tsx, which is a tile picker in a section TITLED 'Art' inside the LAYOUT facet \u2014 I grepped a string and found a section title, not a facet. The Art facet is art-facet.tsx -> ComposerCanvas.tsx. My own framing also inverted: I asked whether priority being a nametable attribute of a placement made the facet's silence a correct boundary; the composer document IS a nametable, ComposerCell carries pal/hf/vf/pri and the composer already authored three of four, so the reasoning proves the opposite. Row's 'at all' corrected: true of the composer, false of aeon (TileBrushOptions has had chips since O12). Lens toggle is a chip not a View menu because facet-chrome gates that on mapOverlays the composer does not claim; writes the SAME showPriority key. Classic deliberately excluded (BlockTab already authors it). Overseer planted the SAVE path \u2014 authored priority never reaching the packed word \u2014 and one PRE-EXISTING end-to-end row fires. Merged tree 438 files all collected, tsc clean. Tagged for foreground: the option bar's readability at a narrow window width, which the harness cannot judge."}
{"at": "2026-08-30T08:08:17Z", "headline": "You can now split a raster band and drag its edges \u2014 and the engine turned out to forbid the obvious way of doing it.", "matters": "Bands on the effects timeline were read-only. You can now drag either edge and split one in two, each as a single undo step. The interesting part: two bands touching edge-to-edge do not build \u2014 the engine refuses that program, and one level up it silently kills every later colour change in the frame. So a split leaves one line clear between the halves. That is the engine's rule, read out of its source, not a limitation of the editor. This is the split-and-drag half only; moving bands over time needs an engine piece that does not exist yet. CORRECTED 2026-08-30: I wrote \"that is the engine's rule\" as if it were permanent. It is the engine's rule TODAY. aeon has a complete, banked design for letting bands overlap (effects tail Part A, banked 2026-08-17, owner: aeon's lane), and their own source says OVERLAP IS DESIGNED, NOT IMPOSSIBLE. Nothing in the editor changed \u2014 the refusal is still right \u2014 but the sentence now carries its date and its owner.", "detail": "EW-TIMELINE-EDGE / ROADMAP row 94, merge 1c8f01aa pushed. Model derived from aeon's shipped raster_dsl.emp, verified verbatim by the overseer before landing: compose merges every fire on one screen line into ONE record and raster_program refuses it by name; two records on a fire line store an arm gap of -1 whose byte is RASTER_ARM_PARK, killing every later fire SILENTLY. So abutting bands do not build; a split cuts at L and starts the lower half at L+1, and a band needs 3 lines to be splittable (derived, not transcribed). Edge bounds are four rules of two kinds: CLAMPED (fire bound 3..223, imported from effects-aeon rather than re-typed since a band edge and a vsplit fire are the same ensure; top < bot) and ADVISED (no two bands per fire line; colour sharing only without vertical overlap) \u2014 advised deliberately, because two bands over disjoint CRAM spans may legally nest and a hard wall would refuse a program the engine builds. CORRECTION TO MY DISPATCH: I said the stale read-only ruling was discharged and not to touch it; true of the .tsx which I had read, FALSE of canvas/raster-timeline.ts which I had not \u2014 I verified one file and generalised to the parcel. Overseer planted the engine constraint itself (split producing ABUTTING halves), restored from a byte copy not git checkout: 6 rows fire, one swept over every legal cut. Merged tree 439 files all collected, 5991/0/7, tsc clean. Disclosed as O49: two hint strings have no gate that they reach the render, and four rules are not plant-tested. \u26a0 CORRECTION APPENDED 2026-08-30 (no behaviour changed, comments and messages only). \"So abutting bands do not build\" was banked here, in ROADMAP row 94, in a code comment, in the poison list and in the CDP harness as an ENGINE LAW with no date and no expiry. It is an INTERIM POSITION. Re-verified at aeon 2e976223 (their origin/master, read with git -C ../aeon show): OVERLAP IS DESIGNED, NOT IMPOSSIBLE heads their check_intervals guard, and effects tail Part A \u2014 runtime patchable-overlap resolution, design complete and three-times swept \u2014 is BANKED 2026-08-17 by owner ruling, revival condition \"a real program that needs overlapping patchable bands\". OWNER: aeon's lane. Two qualifications found while re-verifying, both of which the original write-up lacked: Part A deletes the disjointness wall for PATCHABLE-vs-PATCHABLE pairs ONLY (statics stay sacrosanct) and a preset document lowers to band(), which is static by construction and not handable to patchable; and the ensure an abutting PAIR actually reaches is raster_program's restore-alone guard (spec 4.2a, claim D-B), which Part A does not name. So Part A landing alone would NOT make these bands abut. The claim is now stated ONCE, with date/owner/expiry/re-read list, in the GAP RULE block of src/renderer/providers/effects-preset.ts; eight sites are enumerated and gated in effects-preset-timeline.test.ts. NOT relaxed anywhere: aeon's guard says every naive relaxation was proven unsound across three adjudications."}
{"at": "2026-08-30T08:33:05Z", "headline": "A corrupted project would have thrown sixty-three ten-second error messages at you, one after another. Now it says it once.", "matters": "Making genuine failures visible last night had a side effect: they stay on screen ten times longer than a success, and nothing limited how many could pile up. A project with every section file unreadable produced sixty-three of them. It now reports one sentence naming the first few and counting the rest, and the on-screen stack is capped at four \u2014 but nothing is dropped: failures are always shown ahead of successes, the hidden ones are counted honestly, one click shows them all, and every single one is also written to the developer console where it survives everything.", "detail": "O43, merge 1b990156 pushed. MY SCENARIO WAS WRONG TWICE. First: I claimed a clean clone stacks 17 error toasts from untracked bg bodies \u2014 it does not, those never reach notices and O31 had already coalesced them; I had built the case on the one scenario the codebase had solved, and caught it only by checking the flow before writing the brief. Second, the agent's: I said 5 files per section, worst case 45. It is SEVEN, worst case 63, MEASURED on a 3x3 act \u2014 I counted CALL SITES, and two suffixes reach markUnreadable through one readPlane closure invoked twice. Call sites are not invocations; third unit error of mine tonight. Agent also found two producers I never named that flood over a DIRECTORY LISTING (booked O51). Both halves shipped because each alone provably fails: coalescing cannot bound saveAllDirty or ~96 other addToast sites; a cap alone would put 59 of 63 failures behind a number. Inversion guarded three ways (errors selected first and newest-first; hidden counts derived from the hidden array so an unmeasured overflow cannot read as zero; click-to-expand plus console.error mirror surviving expiry, dismissal and the cap). dwellMs untouched. Overseer re-derived the 63 and planted the inversion by a DIFFERENT mechanism than the agent's, restoring from a byte copy with md5 checked: 5 rows fire. Merged tree 442 files all collected, 6018/0/7, tsc clean."}
{"at": "2026-08-30T09:03:13Z", "headline": "A single line in the test config was the only thing keeping test output visible, and nothing protected it.", "matters": "Delete or reorder that line and our test runs go quiet about anything a passing test prints \u2014 silently, with nothing failing. It now has two guards, and the second one matters: a check that the line is PRESENT cannot tell you it still WORKS. Proven by leaving the line untouched and breaking visibility another way \u2014 the first guard passed all nine checks while the second caught it and named the line to look at.", "detail": "O42+O44, merge a7039e65 pushed. vitest 4 auto-selects a silent reporter whenever CLAUDECODE is set, and a config-level silent:false does NOT override it (AgentReporter passes silent:'passed-only' into super() before ...options, so the ctx fallback never fires). Both halves shipped: a text assertion over the config (structural proxy, catches deletion/reordering) and a canary that spawns a real child vitest against the real config and greps its output, with a forced-agent control that must show markers ABSENT so it cannot pass vacuously. It guards BOTH local properties \u2014 console visibility AND the skip reporter naming a skip with its reason, which is why this repo's pin exists at all. Overseer reproduced the load-bearing case independently (pin untouched + silent:true: text 9/9 green, canary red), restored from a byte copy with md5 verified. THE AGENT DECLINED A ROW I SUGGESTED, on measurement and rightly: 'minimal' is not a reporter in 4.1.4 so that clause could never fire, and with 'default' asserted first a trailing 'agent' cannot mute \u2014 a vacuous gate inside a parcel about vacuous gates. O44: live half already closed; a hand-rolled lint flagged 7 sites, ALL 7 false positives, so instead a type-checker gate watching a NAMED LIST OF TYPES, zero false positives by construction, exits 2 COULD NOT MEASURE when an entry stops resolving. Agent's own expectation refuted by its probe: n + '' is ALLOWED by tsc. Merged tree 443 files all collected, 6030/0/7, tsc clean."}
{"at": "2026-08-30T09:25:44Z", "headline": "The engine lane was waiting on us to ship one key. It is shipped, and the check everyone trusts turned out to be blind to part of it.", "matters": "Assigning a raster preset to a level section needs a key in the section's sidecar file, and the engine could not ship its half until ours landed \u2014 an older Aurora would have erased the key on the next save. Ours is on master. Separately: our standard test command does not typecheck, so one of the places that would silently erase data passes it completely green while the compiler reports twenty-three errors.", "detail": "EW-SECTIONMETA-RASTERREF, merge 7b1d15a0 pushed, SHA sent to aeon. Spec read at empyrean da91abce \u00a73.1. THE COUNT TRAP RESOLVED, both numbers right: 16 = non-test src/ FILES containing `sceneRef`; 13 = SITES by aeon's own governing definition ('a place that hardcodes the ref SET'), in 5 files \u2014 re-derived against the type and checked against the sceneRef parcel's own diff, not carried forward. MY DISPATCH METHOD HAD A HOLE: 'follow sceneRef' finds the code that HANDLES the set and misses 6 sites that DESCRIBE it in prose, one of them author-facing with ZERO occurrences of sceneRef (booked O53). ONE SITE IS INVISIBLE TO npm test: dropping the interface field leaves vitest 6041 green, tsc 23 errors \u2014 planted by the overseer to verify (booked O52). Aeon must expect a one-line `\"rasterRef\": null` diff on existing sidecars; F2 narrowed to its real property, not deleted. \u00a78 re-pin half-done BY DESIGN (rasterRef appears zero times in aeon's consumer contract \u2014 the precondition working). No fixture touched; goldens are inline hand-written strings. Merged tree 443 files all collected, 6041/0/7, tsc clean."}
{"at": "2026-08-30T09:36:00Z", "headline": "The command everyone quotes as proof the tree is green could not see a type error. Now it can, for 4.4 extra seconds.", "matters": "Every claim in our review packets and this log that the merged tree is green comes from someone typing `npm test`. That command ran the tests but never ran the compiler, and at least one place where the editor could silently erase a level author's data was guarded by the compiler alone. The gap was covered only by the overseer remembering to run a second command by hand at each landing, which is a habit, and the next session does not inherit a habit.", "detail": "O52 CLOSED. branch gate/npm-test-typechecks, commit 0a9ee3b0. `npm test` now runs `npm run typecheck` between the check:* scripts and vitest. THE BOOKED PREMISE WAS FALSE AND WAS MEASURED, NOT ASSUMED: the queue row weighed 'tsc is slower than the suite, so a naive add may make the common loop painful' and offered a separate `check` script as the other shape. Measured in a clean worktree \u2014 `time npx tsc --noEmit` 12.1s cold then 6.1s/6.1s warm; `time npm test` 13.6s before, 18.0s after. tsc is under HALF the suite's wall, so the shape that protects the existing claims (the one people actually type) is also the cheap one; the fallback shape, which would have had to PRINT that it does not typecheck, was not needed. ORDER IS DELIBERATE: typecheck BEFORE vitest \u2014 fails in ~6s instead of after the suite's ~11s, and a tree that does not compile makes the suite's verdict meaningless. Composes `npm run typecheck` rather than a second `tsc --noEmit` literal, so one definition. RED FIRST with the real documented erasure (rasterRef deleted from the SectionMeta interface): vitest run ALONE exits 0 at 441 files / 6041 passed / 7 skipped, ZERO red rows \u2014 that is the number that makes the case; the new `npm test` exits 2 at the typecheck step with 23 error TS2339/TS2353 rows and vitest never runs. Restored: exit 0, 441 files, 6041/0/7, tsc clean. PRE-EXISTING RUNNERS: none \u2014 no .github/, no CI config, no installed git hooks, no core.hooksPath; the only definition was the uncomposed `typecheck` script plus two scratchpad harnesses shelling out to tsc for their own purposes. README `Other scripts` block corrected \u2014 it said ``npm test  # vitest, single run``, and it now names the typecheck step and flags that test:watch is NOT the gate."}
{"at": "2026-08-30T09:37:35Z", "headline": "The command everyone runs to check the code could not see a whole class of mistake. Now it can.", "matters": "For months, deleting a required field from the project file format would have sailed through with every one of the six thousand checks reporting success. The check that catches it existed and nothing ever ran it. It runs now, before the slow part, and costs about four and a half seconds.", "detail": "O52. Proven by plant both by the agent and independently on the merged tree: deleting rasterRef from the SectionMeta interface left vitest at 6041 passed with zero red rows while tsc --noEmit reported 23 errors. `npm test` now composes `npm run typecheck` between the check:* scripts and `vitest run`. The booked premise that tsc would be too slow to add was measured false (4.5s against a ~15s suite); a separate check script was therefore rejected because every 'merged tree green' claim in the packets was made by someone typing `npm test`. Merge 939c2551, note correction 20ff1d1-ish, pushed 9588f429..eebe682f. Overseer re-verification found the durable note attributed the whole fail-fast stop to tsc alone when the check:* prefix runs first; corrected with measured splits. Remaining uncovered: scripts/ and scratchpad/ .mjs (needs allowJs, separately booked in tsconfig), and test:watch deliberately."}
{"at": "2026-08-30T09:48:45Z", "headline": "You can now point a section at a raster preset \u2014 and the editor tells you outright that nothing plays it back yet.", "matters": "The binding the engine work has been building toward is finally writable from Aurora's side. The part worth knowing is the honesty: every reply that makes one of these assignments says, in the same breath, that no consumer reads it and the effect still has to be hand-written on the engine side. Nobody will spend an afternoon assigning presets and wondering why the screen never changes.", "detail": "EW-ASSIGN-PRESET + O53 rider. assign_section_preset mirrors assign_section_scene through providers/effects-preset's sectionPresetCommand (one function behind both the agent door and the future UI door), refusals thrown, '' sentinel shared with the panel. Writes rasterRef, never effectsRef (reservation kept unspent, asserted). The binding-limit sentence is ONE constant, core/formats/raster-binding.ts, quoted by the panel block, the agent reply and the published tool description; PRESET_LIMITS.unbound folded into it rather than forked. Two existing gates reversed deliberately with reasons in place. 19 new rows, 11 plants all diagnostic. Merged tree 444 files / 6061 passed / 7 skipped, rc=0 with tsc now in the chain; pushed 88031728..f98824ac. Overseer checks beyond the agent's: confirmed nothing writes effectsRef outside comments, confirmed the fixture's undefined-presetId bug cannot be reproduced by the real loader (parseSectionMeta normalizes non-strings to null, createSection defaults null), confirmed the source-reading guard is anti-vacuous, confirmed end-to-end publication (protocol type, handler, main tool table, docs/MCP.md). The agent corrected my brief's prose-site count from six to seven, and it is right: commit 32ad0341's subject says six while its own diff touches seven prose files. UI half booked as O54."}
{"at": "2026-08-30T10:09:49Z", "headline": "The engine started reading the preset assignments overnight, so the warning we show authors was wrong by morning. It is corrected \u2014 and it still is not good news.", "matters": "Assigning a preset to a section now reaches the build instead of stopping at the file. But the last connection is not made: the build prepares the effect and nothing plugs it in, so the section keeps running its old hand-written one and the screen looks the same. An author is told exactly that, plus what shrank \u2014 the remaining hand-work is one line per section rather than writing the effect. The old warning carried a dated expiry naming the two engine files that would prove it wrong, which is the only reason this was caught the same morning rather than misleading people for months.", "detail": "Aeon landed the consumer at their 4aa2abc0 and reported the expiry fired. Verified here at that revision, read-only: real merge, ancestor of their origin/master, effects_gen.py +138, ACT_RASTER_REF_KEY defined and resolved, contract \u00a72.2 amended. \u26a0 CORRECTION TO THE PEER'S OWN FRAMING, verified twice (agent, then me): they said our sentence told authors to hand-write 'something the pipeline does for them'. It does not. The generator emits an always-present zero-byte chooser and NOTHING CALLS IT \u2014 every raster: argument in games/sonic4/data/effects/ojz_effects.emp is a hand-authored label, the generated module's witness reads EditorRaster_OJZ_Act1_Bindings = 0, and git grep sec_raster( at that revision hits only the design spec and the generated module. Their own DEFERRED_WORK calls it step 5, blocked on an owner's section choice. So the replacement sentence is not 'it works now'. Bonus falsification found in passing: PRESET_LIMITS.debug_chord claimed the build fails when a preset has no table row; at 4aa2abc0 the reachability lint accepts a row OR a binding. Fold preserved (one constant, four consumers); identity row and the effectsRef-reservation negative both survive; new sentence carries its own dated expiry. 20 prose sites across 11 files, enumerated. A matcher trap was caught pre-commit: /aeon 4aa2abc0/ also matched LIMIT 2, re-pointed. 444 files / 6062 passed / 7 skipped, tsc clean. Merged e8bb5ea9."}
{"at": "2026-08-30T10:37:23Z", "headline": "You can now point a section at a raster preset with a dropdown, instead of asking the assistant to do it.", "matters": "This was the last half of a feature that had a writer but no control. The dropdown sits directly under the paragraph explaining what binding does and does not do, so an author cannot reach it without that paragraph having rendered first \u2014 and the paragraph is now accurate in the other direction too, because it used to say no control here writes one. Pick a preset and the section is bound; pick 'Hand-authored raster' and it is unbound, one undo step either way. A section pointed at a preset that is not there says so by name instead of quietly drawing as unbound. What has NOT changed: nothing plays it back. The screen is unchanged, in the editor and in a built ROM, because aeon still has not plugged the chooser into their effects source \u2014 re-checked this morning at their current master, not taken from yesterday's note.", "detail": "ROADMAP row 93's remaining half, branch feat/section-raster-select. The select is in BandPresetPanel, inside the presets section rather than beside the scene panel's assignment row: a collapsed CollapsibleSection renders no children, so the limit block cannot be bypassed, and the options ARE the documents in the picker above it. It calls sectionPresetCommand and never assigns rasterRef \u2014 that function owns the '' sentinel, so the empty option and assign_section_preset's explicit null are ONE unbind. New provider members: RASTER_REF_ROW, presetRefOptions, unassignablePresetRef. \u26a0 THE LIMIT'S FOURTH CLAIM EXPIRED and band-preset-wording's LIMIT 1 row was RED before it was edited; the four prose sites repeating the retired half were followed. \u26a0 THE PLANT THAT JUSTIFIES THE HARNESS: one character of JSX (value={o.label} on the option) left the node suite at 57/57 and tsc clean while turning nine harness rows red \u2014 the select offered display labels as values and nothing could be bound at all. A source row now covers that instance. Suite 6080/7 in 445 files (base 6062/7 in 444); 10 poison plants, 10 diagnostic; scratchpad/section-raster-select-harness.mjs 23/23 in a real Electron under xvfb, including the sidecar bytes after a real Ctrl+S, with PLANT=rot-select aborting at exit 2 and PLANT=no-model measured at 20/23 still green. Re-verified at aeon origin/master bff4d3d3: still no call site. No emulator; what a bound band LOOKS like is still unmeasured anywhere in the suite."}
{"at": "2026-08-30T11:05:00Z", "headline": "A person can now point a section at a raster preset by clicking, not just by asking an agent to do it.", "matters": "This was the last missing half of that feature. The control sits inside the preset panel deliberately: a collapsed panel draws none of its contents, so putting the dropdown anywhere else would have handed someone the control with the warning about what it does not do scrolled out of sight \u2014 and that warning is currently the only thing in the entire toolchain that tells them. Binding a section to a preset that does not exist now says so by name instead of silently showing the wrong thing.", "detail": "O54, ROADMAP row 93 closed on Aurora's side. Control lives in BandPresetPanel's raster section below the create row; reads activeSectionIndex off the existing store, no second source of truth; calls sectionPresetCommand so the '' sentinel keeps a select's empty option and an agent's explicit null one state; no new sentence, RASTER_REF_ROW is label+title only and a test asserts no phrase of the constant is retyped. The CDP harness earned itself immediately: value={o.label} on the option left the node suite at 57/57 with tsc clean while the select offered display labels as values so nothing could ever bind; nine harness rows red. 23/23 harness rows, real Electron under xvfb against aeon exported by git archive to a scratch copy. Ghost-id case verified painted via checkVisibility/getClientRects/elementFromPoint. 445 files / 6080 passed / 7 skipped. Merged fa9f5dd6. Follow-up 3fa74454: two instruments this lane's own commits rotted today, both reported by the agent rather than silently fixed - the poison's anchor text had moved to raster-binding.ts (reported NOT PLANTED, loud), and harness row 3a still demanded the RESERVED key be on screen, which the unit gate forbids, so the two instruments contradicted each other. Both re-anchored; poison 11/11 caught 0 NOT PLANTED; harness 29 rows 0 failed with row 3a shown red on the stale phrases and green on the current ones."}
{"at": "2026-08-30T11:45:00Z", "headline": "The engine wired exactly one section, so the editor stopped speaking in general and started naming the number.", "matters": "Until this morning the answer to 'what does assigning a preset do' was the same for every section: nothing. One section is now wired and the rest are not, so a single sentence could only be right about one of them. Picking section 5 does something; picking any other writes a key nothing follows. The engine's build will refuse that mistake by name \u2014 but only on the slow build, not the fast one people iterate with, and nothing in the editor warns at all. All of that is now said plainly instead of averaged into one reassuring line.", "detail": "O55. Aeon step 5 at their 9cdf32d8 threaded exactly one call site (ojz_effects.emp:1072, sec: 5, hand: Raster_Program_None); verified here at that revision, read-only. Sentence retired from universal to a three-case split naming the section number, per aeon's drafting rule. THREE QUALIFIERS the agent established beyond aeon's own report and I re-verified the load-bearing one: effects_seam_gate really does refuse the unwired case by section and id (sys.exit(1)); build.sh runs it only under FAST=0 and the FAST banner names it among skipped lanes; the arm is vacuous today (no sidecar carries the key) and says so in its own output rather than reading green. 'One line per section' retired as WRONG not merely stale: sections 6-8 share one record and threading a section-keyed chooser into a shared record is itself a seam-gate refusal, so the old phrasing sent an author to an edit the build rejects. Viewport clause kept and strengthened with 'for section 5 as much as for any other', separately pinned, so the threading cannot drift into 'you can see it'. New three-way expiry. 4 gate rows red before any was edited; matcher-trap row proved by leaking the new revision string into LIMIT 2 and catching it. Poisons 11/11, 0 NOT PLANTED, re-run on the merged tree. Harness 29 rows 0 failed, row 3a red-first on the old matchers. 445 files / 6081 passed / 7 skipped, tsc clean. Merged 6352debe."}
{"at": "2026-08-30T12:40:00Z", "headline": "The rule that abutting bands do not build was written down as a law. It is a dated position, and it now says so in eight places.", "matters": "Nothing you can do in the editor changed. What changed is what the code, the roadmap and the instruments CLAIM about the engine. \"Abutting bands do not build\" landed with the timeline parcel as if it were physics; it is true today, and aeon has a complete, banked design for letting bands overlap. Their own source says OVERLAP IS DESIGNED, NOT IMPOSSIBLE. Every place we said it now carries the date it was true, who owns changing it, what would end it, and what to re-read at their current master rather than at a pinned revision. The refusal itself was not loosened by a single character, deliberately: their guard says every naive relaxation was proven unsound.", "detail": "Comments, docs and instrument descriptions only - the diff shows ZERO non-comment changed lines in src/renderer/providers/effects-preset.ts and src/renderer/canvas/raster-timeline.ts, so every exported string is byte-identical and no condition, comparison or test expectation about when bands may abut was touched. Eight sites enumerated and gated: effects-preset.ts (the canonical GAP RULE block, plus four docblocks pointing at it), canvas/raster-timeline.ts, the two test files, timeline-edit-poisons.mjs, timeline-edit-harness.mjs, ROADMAP row 94, and this ledger's 08:08:17Z entry (corrected in place, marked as a correction). The merge message 1c8f01aa says it too and is NOT on the list - history is not rewritten; recorded here so nobody re-finds it as a gap. Re-verified at aeon 2e976223, read at that committed revision and never from a working tree: DEFERRED_WORK.md \"Effects tail Part A\" (BANKED 2026-08-17, owner ruling demand-pull, revival condition \"a real program that needs overlapping patchable bands\") and raster_dsl.emp's check_intervals GUARD 2. TWO CORRECTIONS THE ORIGINAL WRITE-UP LACKED, both found by re-reading rather than by relaying the dispatch: (1) Part A deletes the disjointness wall for PATCHABLE-vs-PATCHABLE pairs ONLY - statics stay sacrosanct - and a preset document lowers to band(), which is \"Static by construction ... not handable to patchable\", so Part A landing alone would NOT let these bands abut; (2) the ensure an abutting PAIR actually reaches is raster_program's restore-alone guard (spec 4.2a, claim D-B), not check_intervals, and Part A does not name it. A reader waiting for Part A was waiting for the wrong event. FOUR new gate rows, red-first at 4 failed / 32 passed before the edits, and poisoned again after (removing EVALUATE, DO NOT OBEY, and removing aeon's sentence from raster-timeline.ts, gave 3 red; restored from byte copies). The anchors are asserted to appear EXACTLY ONCE in the file, because rules 1, 2 and 4 sit in the same comment and a looser phrase would be satisfiable by a neighbouring rule's wording. npm test: 443 files passed / 2 skipped, 6085 passed / 7 skipped, tsc clean. poisons:rasterref 11/11 caught, 0 NOT PLANTED. band-preset-harness 29 rows / 0 failed against a writable aeon copy at 2e976223 - run even though the byte-identical-strings proof already covered it."}
{"at": "2026-08-30T12:39:57Z", "headline": "We had written a temporary engine restriction into the code as though it were permanent \u2014 and what we thought would lift it turned out to be wrong too.", "matters": "Anyone reading that comment learned a false thing about the engine: that two effect bands touching each other can never be built. The engine team has the opposite designed and signed off, waiting only for someone to need it. The comment now says when it was true, who can change it, what would actually end it, and what to go read \u2014 and it explicitly tells a future reader not to take it as permission to weaken the check, because every casual attempt at that has been proven unsound. Nothing about how the editor behaves changed.", "detail": "O50. 8 sites, gated by a count so the number moves when the list does; found by grepping the CLAIM (gap rule, one clear line, does not build, check_intervals, tight fit, cannot share, plus every consumer of BAND_GAP_LAW/BAND_SPLIT_LAW) rather than the word - 'abut' alone would have found 5 of 8. \u26a0 TWO CORRECTIONS TO MY OWN BRIEF'S PREMISE, found by the agent reading aeon rather than relaying, and verified by me at aeon 2e976223: (1) effects-tail Part A deletes the disjointness wall for PATCHABLE-VS-PATCHABLE pairs ONLY - statics stay sacrosanct - and band() is 'Static by construction, not handable to patchable' while a preset document lowers to band(), so Part A landing ALONE would not lift this, which is exactly what my brief said would end the claim; (2) the ensure an abutting pair actually reaches is raster_program's restore-alone guard (raster_dsl.emp:3067), not check_intervals. The dated block names the real condition. NO BEHAVIOUR CHANGED, verified independently by me: git show -U0 over the two shipped source files has zero non-comment changed lines, so every exported constant is byte-identical. 4 gate rows red before any edit, then poisoned again after; one matcher bug of the agent's own caught that way (flatten stripped // but not block markers). 445 files / 6085 passed / 7 skipped; poisons 11/11 0 NOT PLANTED; band-preset harness 29 rows 0 failed. Merged a32232d2."}
{"at": "2026-08-30T13:00:20Z", "headline": "Saving several documents at once could throw one ten-second error message per document. Now it says it once.", "matters": "Two spots still produced an error message for every item they touched \u2014 one per open sprite tab, and one on the canvas side that fired on EVERY save, not just failed ones, because a flag meant to mark a problem was never cleared after a good save. The second is the one you would have noticed: four sentences, ten seconds on screen, every time. Nine other places that looked like the same bug are not, and the reasons are written down so nobody re-investigates them.", "detail": "O51. Population re-derived: 112 non-test addToast lines (row said 98), 96 actual call expressions by AST. Method: proximity heuristic discarded for a TypeScript-compiler containment scan following local aliases (11 aliased sites invisible to a name grep), then a fixed-point interprocedural pass - which is where BOTH real defects were, since the toasting function is called from inside the loop rather than containing one. Real: saveAllSpriteArt (saveSpriteArt toasts on every exit path, once per dirty sprite tab) and the canvas saver (saveCanvasDocument has two paths that report WITHOUT throwing, and sidecarRejected survives a successful save). Bounded with reasons: 9. False positives of the callback rule: 7 (onClick handlers defined inside a JSX map, invoked one at a time). debug-hooks ruled out of scope - unbounded is its job as the cap's own instrument. \u26a0 CORRECTED THIS OVERSEER'S BRIEF: project-runtime saveAllDirty is per SAVER (4 registered by fixed id, verified here), not per document as I wrote. Expectations derive from each fixture's ids.length. Agent's own near-miss, chased not trusted: first canvas run returned 0 toasts not 9 because ensureSaversRegistered latches and beforeEach cleared the coordinator - a green over an empty loop. 447 files / 6092 passed / 7 skipped, tsc clean. Merged 645429c5. Render side TAGGED unverified, booked as O57."}
{"at": "2026-08-30T13:07:00Z", "headline": "Decided NOT to build a test for the toast fix, and wrote down why \u2014 where the next person will be standing.", "matters": "The tempting move was to build an instrument proving the fix works on screen. It would have re-proved something an existing instrument already covers: a flood cannot reach the screen even if the code regresses, because the message stack is capped and that cap is already driven against the real app. What is genuinely unproven is narrower \u2014 nobody has watched a real Save All over several documents end to end \u2014 and that sentence is now in the file itself, so 'we fixed the toast flood' cannot get quoted as more than it is.", "detail": "O57 closed by ruling, not by silence and not by building. Checked toast-overflow-harness.mjs first: it drives the real app under CDP and asserts the screen paints fewer than the store holds, that it is a CAP AND NOT A DROP (two numbers from two places), that the overflow count is rebuilt from store and screen rather than compared to a literal, and that the state is escapable. So the painting side was already covered by an existing instrument, and the production side is covered by rows deriving from fixture ids.length. The residual gap is gesture -> coordinator -> saver -> fold wiring, which O51 did not change. Recorded in save-outcome-report.ts's header as a three-way split (proven producer-side / proven painting-side BY ANOTHER INSTRUMENT / not proven end-to-end), with the declined harness and its reason so nobody rebuilds the argument, and with the judgement carrying its own expiry: if a future change moves where the fold is called from, the declined gap is over code that has moved. Merged 0d4d9bf3."}
{"at": "2026-08-30T14:05:00Z", "headline": "The chunk panel promised that editing a chunk updates every copy. It only ever updated the act you had open. Now it says so, and tells you where the other copies are.", "matters": "Stamped chunks remember where they came from, so editing the chunk in the library updates the stamps. The panel said it updated every copy. It updated every copy IN THE ACT YOU HAD OPEN \u2014 the chunk library is shared by the whole project, so the same chunk stamped in another act quietly kept showing the old art while still claiming to follow the library. Making it reach across acts was rejected for reasons written down in the code: an undo step cannot reach an act you are not looking at, and in another ZONE the same chunk does not even mean the same tiles. So the act you have open still updates, and saving now tells you exactly which other acts still hold un-updated copies and how many, as a warning that stays on screen long enough to read.", "detail": "Branch chunk-link-scope-honesty. FIRST ESTABLISHED THE DEFECT IS REAL rather than assumed: config.chunkLibrary is ONE project-level path -> LoadedS4Config.chunkLibraryPath -> ONE project.chunkLibrary array, while sections hang off zones[].acts[]. So a copy in another act is the same ChunkDef and the divergence is genuine. Also confirmed art-facet.tsx is the ONLY producer of set-chunk (the agent's save-chunk only ADDS, minting a fresh id), so there is no second propagation path. DESIGN CALL: report, do not propagate. Three grounds, at the head of chunk-links.ts's new section \u2014 (1) AnyCommand.sectionIndex is a flat index resolved as level.sections[i] against the FOCUSED act, and undo stacks are per-document (documentHistoryHub.historyFor), so a cross-act child would write the wrong sections on undo; (2) ChunkDef.nametable words are indices into the ZONE tileset (sliceForSave mints them), so cross-zone propagation is corruption; (3) action at a distance in an act the author cannot see. New: findOutOfActChunkCopies (excludes the current act by OBJECT IDENTITY \u2014 act ids repeat across zones, an id compare drops a whole act's copies) + describeOutOfActChunkCopies. Panel sentences moved to CHUNK_LINK_LINKED_BLURB / CHUNK_LINK_DETACHED_BLURB in chunk-links.ts beside the mechanism, because the sentence IS a claim about the scope. Toast on the edit path becomes 'warning' when copies survive; byte-identical to before when none do. VERIFICATION: chunk-links-cross-act.test.ts, 10 rows, fixture = same chunk in THREE acts across TWO zones with a chunk-b discriminator (a one-act fixture cannot see this bug), counts re-derived by an independent traversal rather than pinned. 6 node plants, each red on its own row: act.id-instead-of-identity exclusion (expected 2 to be 3), counting all placements not the named chunk (expected 4 to be 1), propagation reaching nothing (expected null not to be null), blurb losing IN THIS ACT, panel re-inlining a literal, save path dropping describeOutOfActChunkCopies. HARNESS: JUDGED WARRANTED and BUILT, as one row on the existing instrument rather than a new one \u2014 chunk-links-harness.mjs row 2b, real Electron under xvfb against the real aeon project, asserting the panel PAINTS the act-scoped sentence and that it is the live paragraph (real checkbox click changes it and changes back, so a static string elsewhere in the DOM fails). 11/11 rows, existing 10 unaffected; poisoned red on 2b alone by reverting the blurb constant and rebuilding. NOT BUILT, deliberately: a harness for the warning TOAST \u2014 it needs the same chunk stamped in two acts of a real project, which neither a fixture nor the live tree has, and building that state through the app is its own parcel. The exact sentence is asserted word-for-word in node and a source-grep guard binds the save path to it; that residual is stated in the ROADMAP row rather than left implied. Suite 6102 passed / 7 skipped in 448 files, tsc --noEmit clean."}
{"at": "2026-08-30T13:59:55Z", "headline": "The chunk panel told you editing a chunk updates every copy of it. It updates the act you are looking at \u2014 so it now says so, and tells you where the others are.", "matters": "Stamped chunks remember where they came from, and editing the original re-stamps them. That works within one act. A copy in another act kept its link, was never updated, and quietly drifted from the chunk it claimed to remember \u2014 while the panel promised otherwise. Making it update everything was not an option: the editor's undo cannot address another act, and across zones the tile numbers mean different things, so it would have corrupted the other act rather than propagating to it. Instead the promise now matches what happens, and after a chunk edit you are told exactly which copies elsewhere were left alone.", "detail": "O46. Cross-act defect CONFIRMED real: config.chunkLibrary is one project-wide string loaded once into one ChunkDef[], so a copy in another act is the SAME chunk. ROADMAP row 91 had recorded cross-act as unbuilt on purpose all along - the panel sentence was the thing out of step, not the roadmap. Three grounds for rejecting cross-act propagation, first two verified by me: (1) NOT EXPRESSIBLE - a command's sectionIndex is a flat index resolved as level.sections[cmd.sectionIndex] against the focused act with per-document undo stacks, so a child meant for act 2 slot 3 writes act 1 slot 3 on undo; (2) across zones a ChunkDef nametable word indexes the ZONE's tileset, so it is corruption not propagation; (3) action at a distance on an unreachable undo step. Chose propagate-in-act plus REPORT, which keeps the capability narrowing alone would have dropped; save toast becomes warning when copies survive, byte-identical when none do. Blurbs moved beside the mechanism because a sentence is a claim about a scope. Out-of-act copies excluded by object IDENTITY not act id - ids repeat across zones and an id compare would silently drop a whole act. 6 node plants each restored, fixture is the same chunk in three acts across two zones with a discriminator, all counts re-derived by independent traversal. Agent caught a live hole in its own guards: the two source-greps now strip comments first, without which its own new docblocks would have satisfied them. CDP harness judged warranted and added as ONE row to the existing chunk-links harness, asserting the panel PAINTS the sentence and that it is the LIVE paragraph (a real checkbox click flips it), poisoned red on that row alone. 448 files / 6102 passed / 7 skipped. Merged 7267ca51. Warning toast TAGGED unseen on screen, booked as O58."}
{"at": "2026-08-30T15:05:00Z", "headline": "There is now a control for making the background sway \u2014 and its 'off' setting is off, which was harder than it sounds.", "matters": "The engine's numbers for this run backwards in three separate ways: a bigger number means a SMALLER sway, some numbers in the middle of the range are illegal, and 'no sway at all' is the number at the very top rather than zero. A control built the obvious way would have had an off switch that meant maximum. You now pick a sway in pixels from a short list, and a duration in seconds and minutes, and off is its own switch rather than one end of a slider.", "detail": "EW-7-11 first split, plus the schema re-pin it required. bob_shift is an inverse amplitude (1=128px, 8=1px), domain is exactly {15} union 1..8, and 15 is the no-bob sentinel while the packed wire byte's off is 0. Amplitude is a closed 8-rung select in PIXELS ascending - shift order would leak the inversion back into the place it was being hidden - and the LIST IS THE GUARD rather than a bound, because min/max on a NumberField only style the spinner and never stop a typed value, so a bounded spinner makes 0 unusual not unreachable. Off is a distinct state, not a ninth rung, and OMITS both keys rather than writing 15 (the schema default IS the sentinel; writing it would diff every scene file to restate its silence). Ladder derived from the schema's own anyOf numeric arm, sentinel derived three ways, disjointness asserted at module load. Schema re-vendored by EXTRACTION at empyrean bc639a10: extracted bytes hash to c73d5b42 and are byte-identical to the contract, both re-verified by me. anyOf implemented as anyOf and not as its neighbour oneOf, with an overlap row that fails if the semantics are swapped. 8 poisons all restored, byte-identical round-trip proven on aeon's shipped scene including on-set-off. CDP harness 13/13 with four DOM plants; column geometry given a CONTROL RUN (stash, rebuild, re-run) showing all seven failing rows fail on the control tree too. 449 files / 6132 passed / 7 skipped. Merged."}
{"at": "2026-08-30T15:35:00Z", "headline": "The two files for the first effect you will actually see are written and handed to the engine lane.", "matters": "A band that turns the void behind the forest from black to bright azure, across the colour that makes up half the background, over a stripe of the screen whose edges stay visible however the camera scrolls \u2014 so it reads as a band rather than the whole screen changing tint. Both files were produced by driving the editor for real rather than typed by hand, because the point of this step is to prove the editor-to-engine path works, and a hand-typed file would have proved the file format while quietly skipping the part being tested. What is proven stops at the engine's generator accepting it; whether it actually draws is the engine lane's measurement, not a claim made here.", "detail": "O59. Files: presets/ojz_sec5_showcase.json (sha256 c2e9b897, 309B) and ojz/act1/section_5.meta.json (sha256 b599f780, 104B), both one trailing newline. Produced via loadAeonProject -> set-effects-preset -> assign-section-preset -> saveAeonProject -> reload; only substitution is window.api backed by node fs instead of Electron IPC, which replaces the transport and nothing above it. CRAM addr 80 = 0x50 derived from aeon's stream_pal_region agreement rules (80>>5==2, (80>>1)&15==8), re-derived by me. Colour 3748 = $0EA4 = RGB 2/5/7 approx #48B4FC, replacing $0000 pure black; nothing else on palette line 2 is blue-dominant so the region cannot read as an existing colour bleeding, and it is deliberately not a pure primary which would look like a diagnostic stripe. Band top 32 bot 80 = 48 of 224 lines, both edges inside screen lines 0..95, the intersection where section 5's foreground is unoccluded at EVERY camera position (foreground opaque 25-31% over world rows 0-127, empty 128-255, 61-81% over 256-383, content ends 383, camera top confined 0..160). Aeon's coverage figures reproduced independently to all four decimals. Verified against aeon's own tooling on a scratch copy archived at their 1cbb6660: load_preset shape OK, load_section_raster_refs {5: ...}, raster_seam_faults NO FAULTS with its section arm no longer vacuous, emit moves EditorRaster_OJZ_Act1_Bindings 0->1; control plant binding section 6 produced their fault by name. Expiry ruled NOT FIRED - handing files is not a sidecar in their tree, every clause still true word for word at their tip; fires on their commit. Two obligations relayed: effects_gen.py emit must be committed in the same commit or their drift gate fails, and this lands before/with authored_probe's deletion. 450 files / 6139 passed / 7 skipped. Merged 40e7ec1e."}
{"at": "2026-08-30T16:41:39Z", "headline": "The editor's sentence about which sections can carry a colour band said the engine had none bound yet. The engine now has one, so the sentence was rewritten to match, not deleted.", "matters": "This is the sentence an author (and any agent) reads when binding a preset to a section, and it had just become false in one clause: the engine lane committed the section-5 sidecar this lane handed over. Re-derived against the engine's committed tip rather than anyone's description of it: section 5 is bound and the engine's build carries it; binding any other section still reaches nothing and only the engine's slow full build refuses it; nothing draws in the editor. Whether the band shows on screen was deliberately left unclaimed here, because the engine's own commit said nothing had been seen yet. The wording tests went red on the old anchor before they were moved, so the pin is real. Also found while landing: 27 finished agent worktrees were sitting under the repo and made the main tree's test gate refuse to run; all were clean and merged, and are gone.", "detail": "O56. Merge 2fccda21 (agent branch parcel/o56-binding-limit-expiry, 5 commits 3ffee5a3..490b76bf). Anchor 9cdf32d8 -> aeon 6e2495a5 (c9a462be ancestor; sidecar games/sonic4/data/editor/ojz/act1/section_5.meta.json rasterRef ojz_sec5_showcase; effects_scenes.emp:163 Bindings=1, :213 chooser arm; build.sh:658/684 seam gate under FAST==0 only, banner :224; effects_seam_gate.py:384 VACUOUS notice only on empty set). Red-first: 4 matchers red (band-preset-wording expiry row, anchor-uniqueness row, agent-handler.assign-section-preset), 11 kept matchers green, no vacuous row. Merged tree measured here: vitest 448 passed/2 skipped (450 files), 6139 passed/7 skipped; typecheck clean; pseudo-skip, peer-path-literals, object-stringify, harness-guards all exit 0. band-preset-harness 29 rows/0 failed/21.2s at dpr 1 against git-archive 6e2495a5 (AEON_DIR scratch copy), run in the foreground; the wrapping shell then hung to the 10-min cap on a child holding the pipe, cleaned by the kill, no Electron/Xvfb left. check-test-collection FAILED on master before landing: 12393 test-shaped files under .claude/worktrees (27 clean, merged agent worktrees) not collected, despite .git/info/exclude:11 listing **/.claude/worktrees/ - the check's git check-ignore pass did not drop nested-worktree paths; worktrees removed, check OK 450/450; durable fix booked as O61. New expiry already imminent: aeon reports the band SEEN and measured (CRAM line 2 entry 8 = $0EA4 on lines 40/56/72, $0000 on 8/20/96/150, control black) with captures landing under docs/research/reference_captures/2026-08-30-sec5-band/ - retire the 'nothing has been seen' clause only once that is on their origin/master (O63)."}
{"at": "2026-08-30T16:52:18Z", "headline": "The test suite refused to run on the main tree whenever finished agent checkouts were lying around. It now steps over them and says so.", "matters": "Every parcel is checked by running the whole suite on the merged tree before it is pushed. Today that first check refused to even start, because twenty-seven finished agent checkouts were still sitting inside the repo and the check counted their test files as uncollected. The cause was not what it looked like: git was answering correctly, but the answer for twenty-seven trees was bigger than the buffer the script reads it through, and the script took a cut-off answer for 'git is unavailable'. The check now refuses to enter any nested checkout and prints which ones it skipped, so an agent mid-parcel no longer stops the main tree's suite from running - and it turned out a pinned fixture copy of the engine had been silently walked all along.", "detail": "O61. Merge e1445215 (agent branch parcel/o61-collection-nested-trees: 02ab78dc walk skip + loud line, eacd13bb 64 MiB maxBuffer + named error). Mechanism measured by the agent, both brief hypotheses refuted: git check-ignore -v answers for nested-worktree paths (exclude:18 .claude/) and a worktree consults the common dir's info/exclude; the failure was ENOBUFS - Node spawnSync default maxBuffer 1 MiB, 27 x 450 paths at agent-<17hex>/ prefix = 1,118,556 B, cliff at ~26 trees of that name length (11-char-shorter probe names passed at 972 KB). Red-first: 27 throwaway worktrees -> FAIL 12150 uncollected with the 'git check-ignore unavailable' note; after the fix OK 450/450 plus 'skipped 27 nested git tree(s)'. Residual proven separately: 40 ignored fixture copies with no .git (1,423,120 B) still false-FAILed with the walk skip alone, fixed by the buffer raise - deviation from the brief, ratified. Merged tree here: full npm test chain exit 0, skip line names .claude/worktrees/agent-a4a1b0caaa51fc17a, agent-a7c2319249a91263c (O63, live) and scratchpad/fixtures/aeon-build-pin; vitest 448 passed/2 skipped (450), 6139 passed/7 skipped; typecheck clean. No test exists for the script; the transcript in the agent report and the header's dated NESTED GIT TREES paragraph are the record."}
{"at": "2026-08-30T17:03:19Z", "headline": "The editor's binding sentence no longer says nobody has seen a colour band on screen. The engine lane has, and the sentence now quotes their measurement and says where it was seen.", "matters": "An author binding a preset to a section reads this sentence. This morning it said, truthfully, that nothing had been seen on screen; by noon the engine lane had committed a measurement - the band's colour sampled inside and outside its stripe, on two independent runs that agree byte for byte, against a control build with the binding removed that shows black everywhere. The sentence now cites that record and is careful about the one thing that has not changed: the band was seen in the engine's emulator capture, in the engine's tree, and this editor still draws nothing for it. A test row that was supposed to catch 'seen on screen' creeping into the editor's own claims turned out to stay green no matter what; it was re-cut and proven with two deliberate breakages. A second sentence in the same panel, the one that says no band has ever been looked at anywhere in the suite, is now also false and is booked for its own pass.", "detail": "O63. Merge edd778ea (agent branch parcel/o63-band-seen-expiry: ada54128 constant+header, b780fafa tests, 9d42a000 prose sites MCP.md + effects-preset.ts, c3aea880 dated lines in review packet + ROADMAP row 93). Artifact: aeon 4a4d3474 (2026-08-30 16:35:26Z, ancestor of their origin/master e6405428, --diff-filter=A; later 778c0f67/e6405428 touch README prose only, data blobs identical) docs/research/reference_captures/2026-08-30-sec5-band/: README 'VERDICT: BAND SEEN', CRAM line 2 entry 8 = $0EA4 at lines 40/56/72, $0000 at 8/20/96/150, frame 259, bound A/B byte-identical, control (rasterRef null, crc32 3ceb094d vs 476e220f) $0000 everywhere, Raster_Program $013ACE vs 0, instrument oracle-aether private headless. Read here firsthand before the agent cited it. Anchors 6e2495a5 -> e6405428; c9a462be kept as sidecar provenance. Red-first: 4 rows red on the constant edit (band-preset-wording LIMIT 1, expiry, anchor-uniqueness; agent-handler.assign-section-preset), secondary pins probed individually; non-discriminating negative row not.toMatch(/band (was|has been) (seen|measured|observed)/) re-cut as a per-occurrence loop, plants P1 (inserted 'You can see it on screen.') and P2 (control clause dropped) each red only where declared. Merged tree here: full npm test chain exit 0, vitest 448/2 skipped (450), 6139/7; band-preset-harness 29 rows/0 failed/21.2s dpr 1 in the foreground (AEON_DIR git-archive 6e2495a5; wrapper timeout 124 after completion, a child holds stdout - booked O65). TAGGED by the agent and booked as O64: NO_PREVIEW (providers/effects-preset.ts:181 'A raster band has never been looked at on screen anywhere in this suite') now false at 4a4d3474, its own harness rows, needs its own red-first retirement."}
{"at": "2026-08-30T17:21:48Z", "headline": "Removing the only section binding - or binding a different section - now tells you that the engine's full build will refuse the result, and why. The control itself is untouched.", "matters": "The engine's own tests pin today's content: section 5 bound to its showcase band is the one state its canonical build accepts. Unbind it from the editor, or bind another section, and the next full build fails on three named tests - while the quick build (the one authors run most) says nothing. Until now the editor let you do that in silence. It still lets you (nothing is greyed out, no confirm dialog), but the reply an agent gets and the text beside the select now say exactly which build refuses, which tests, and that the quick build does not. The agent that built this read the engine's tests and build script itself and corrected three things the brief had relayed: which switch actually skips the tests, that an unbind trips all three tests rather than two, and that deleting the preset document does not clear it. It also found an existing test row that could not see a conditional mount, and covered it.", "detail": "O62. Merge 3172b859 (agent branch parcel/o62-unbind-disclosure: 2ac13cb6 tests red-first, ed69041c constant clause + header, 52619f32 prose sites by reference: agent-handler.ts:1099/1119, editor-methods.ts, effects-preset.ts PRESET_LIMITS.unbound -> LimitBlock, docs/MCP.md, ROADMAP row 93 one dated sentence). aeon pinned 027ec162 (ancestor of their origin/master, verified here): test_effects_seam_gate.py :331 assertTrue(bound)/bound<=threaded, :358 assertEqual(sorted(bound),[5]), test_raster_cycle_table_lint.py :228 reachable-by-nothing; pytest lane build.sh:493 under `if [[ ${NO_LINT:-0} == 0 ]]` at :447, FAST=1 sets NO_LINT=1 at :234 (verified here; brief said 'FAST==0 block', corrected from source). Disclosure is a NEW CLAUSE in RASTER_SECTION_BINDING_LIMIT (one sentence, three audiences, no fork), anchored 'read at aeon 027ec162', with its own EXPIRES list. Red-first: 5 rows red on the untouched constant (3 in agent-handler.assign-section-preset, 2 in band-preset-wording incl. anchor-uniqueness); plants (a) null-reply trim -> only the unbind row, (b) near-copy in editor-methods.ts -> only the by-reference row, (c) LimitBlock guarded on !rasterRef -> only the new placement row, with the pre-existing 'mounts LimitBlock with no guard' row staying GREEN (its hole, now covered). Merged tree here: full chain exit 0, vitest 448/2 skipped (450), 6144 passed/7 skipped (+5 rows vs 6139); band-preset-harness 29 rows/0 failed (exit 124 after completion, O65), section-raster-select-harness 23/23 exit 0. TAGGED for aeon and relayed: README :39-40 swaps the two seam-gate messages' test names; 'delete the document and the first test alone decides' is one test short."}
{"at": "2026-08-30T17:36:38Z", "headline": "The panel's reason for having no preview - 'nobody has ever looked at a band on screen' - was rewritten now that someone has, and says plainly that this editor still draws nothing.", "matters": "The band-preset panel tells the author why there is no preview. Its reason had been that no colour band had ever been seen on screen anywhere in the suite, so there was nothing to preview faithfully against. The engine lane has now measured one frame - one section, one camera position, in their emulator - so the sentence leads with what is still true (this editor draws no band and has sampled nothing), names that one frame and where it lives, says a preview here could at most be checked against it and is not built, and carries a dated expiry. Exactly one test row went red on the change; six replace it, including a loop that catches 'you can preview it here' creeping in. The on-screen harness row that pinned the old words was moved and re-run for real.", "detail": "O64. Merge 2dc16c61 (agent branch parcel/o64-no-preview-expiry: 054c9c28 constant, dba37c95 tests, b74cdd1b prose sites incl. band-preset-harness row 3e matcher + section-raster-select comment + MCP.md + review packet line, c2fcb742 ROADMAP row 93 one dated sentence, 8eab24af effects-facet.tsx JSX comment found by a wider grep). aeon pinned 0b6eaf6f (ancestor of their origin/master, verified here); 4a4d3474 ancestor; README quoted with its not-established list (one entry, one camera position, one frame, no hardware). Red-first: 1 row red on the constant edit (band-preset-wording.test.ts:505 /never been looked at on screen/), no vacuous row; plants (a) 'You can preview it here.' -> drift loop only after dropping a $ anchor on the expiry row that made it position-sensitive, (b) drop the 4a4d3474 citation -> anchor row only. Merged tree here: full chain exit 0, vitest 448/2 skipped (450), 6149 passed/7 skipped (+5 vs 6144); band-preset-harness 29 rows/0 failed in the foreground with row 3e printing drawsNone=true noneBuilt=true anchor=true retiredPhraseGone=true. Harness still hangs after its summary (O65, in flight with the cause found: killTree was handed a bare pid and silently no-op'd)."}
{"at": "2026-08-30T17:59:43Z", "headline": "The on-screen harness that finished its rows but never exited now exits on its own in a fraction of a second, and no longer crashes the app on the way out.", "matters": "Every visual check of the band panel runs the real editor under a virtual display. It had been printing its result and then sitting there until killed, which meant a four-minute wrapper around every run and a reader who had to trust the summary line over the exit code. The cause was a one-token slip: the cleanup handed the kill helper a bare number where it expected the process, and the helper quietly did nothing. On the way to fixing it the agent found that the shared cleanup also shut the display down in the same instant as the app, which made the app crash at exit and leave a core file on the machine on every run that needed the hard kill - the crash loop a peer lane noticed and reported. The cleanup now stops the app first and the display last, every harness that shares the helper inherits it, and the proof is a count of core files before and after: one on the old order, zero across five runs on the new.", "detail": "O65. Merge 0868c1a8 (agent branch parcel/o65-harness-exit: 001a587d helper accepts pid / NOTHING KILLED loud / inFlight capture to the exit net, 205f301d + a22280af OVERSEER.md Instruments bullet, 9b2838f6 ordered teardown app-first then wrapper group, proof rows k6-k9). Holder measured: killTree(child.pid) -> helper read .pid off a number -> silent no-op; 12-process tree held node's stdio pipes. Ordered teardown proven by coredumpctl: old order red-d SIGKILLed 5 -> +1 core (SIGTRAP SI_KERNEL, timestamp = SIGTERM instant), 17/17 correlation SIGKILLed>0 <-> core; new order grn-a..d + srs-grn +0 each, exit 0 at 14 ms after summary, xvfb-run.* 21->21. Outside-observer proof k9: old order VIOLATION at +15 ms (X dead, app alive), new 'hold observed +106 ms, no violation', guard-proof 16/16. Merged tree here: full chain exit 0, 448 files passed/2 skipped; Tests  6149 passed | 7 skipped (6156); plain `npm run harness:band-preset` (no timeout wrapper) exit 0 at 21.6 s wall, ORDERED line printed, SIGKILLed 0, cores 0->0, no .X96-lock; harness:guard-proof 16/16. Incident with aeon closed: the agent's signal accounting showed no cross-group signal (none at 17:20:2x; own group + 5 pre-captured pids at 17:24:4x); no OOM; aeon attributes their two kills to their own harness on abnormal foreground calls. Booked O66 from the agent's tail: section-raster-select, bg-wrap, chunk-links process.exit over an un-awaited killTree (reap correctly via inFlight but skip the grace); section-raster-select needs a fresh AEON_DIR per run (row 1c)."}
{"at": "2026-08-30T18:20:57Z", "headline": "The band timeline's two on-screen hints are now checked on screen, and four rules that had never been deliberately broken were broken on purpose to prove their tests can see them.", "matters": "The timeline strip tells the author how to drag, split and undo bands. Those two sentences were painted, but nothing checked they were - the unit tests cannot see the rendered panel and the on-screen harness looked only at a different sentence. Now the harness reads each sentence back from the painted panel, hit-tests it, hides it by collapsing the section and finds it again on reopening, and a deliberately forked or deleted sentence turns exactly those rows red while every unit test stays green - proof that the harness, not the unit test, is the witness. Separately, four rules the timeline parcel had flagged as 'green but never poisoned' were each broken in the rule itself; each break turned exactly one existing test red, so none of the four was a hollow check.", "detail": "O49. Merge c1f1a3cc (agent branch parcel/o49-timeline-hints-and-poisons: 76604003 poisons P11-P14 + node identity row for RASTER_TIMELINE_GESTURES, 61283198 harness rows 3y/3c/3d/3e/3f + ROADMAP row 94 dated sentence). Render sites RasterTimelineStrip.tsx:431-432 (Hint leaves under preset!==null; :383 is a title attribute, not paint). resolveSelectedPreset falls back to presets[0] so 'no preset' is unreachable; the live flip is the CollapsibleSection header. Plants H1 (forked literal) and H2 (deleted Hint) -> harness 37/39 with exactly the declared rows red, node 84/84 green; N1 -> exactly the new identity row red. Four rules: bandCramSpan pal_region arm, presetBandAt gap, seed band splittable, no-cycle-figure prose - each plant reddened exactly one pre-existing row (failure lines in the agent report), 14/14 diagnostic in poisons:timeline-edit. Harness moved to display :95 port 9439 (unique), await killTree(child), process.exitCode. Three agent runs 39/39 dpr 1; merged tree here: 39/39 exit 0 23.7 s wall, cleanup ORDERED, 0 cores. npm test on the merged tree: 1 failed / 6149 passed / 7 skipped - the one red is test/formats/effects-preset-schema-drift.test.ts CURRENCY row (empyrean origin/main moved with item 5, 12aecd5); NOT a regression, re-vendor parcel EW-REVENDOR-PRESET in flight."}
{"at": "2026-08-30T18:20:57Z", "headline": "The last three on-screen harnesses that were exiting without letting the app close cleanly now wait for it, and one of them refuses to run on a copy a previous run already changed instead of quietly failing two rows.", "matters": "After the teardown fix earlier today, three harnesses were still cutting the app off by exiting over the cleanup they had started - the shape that had been leaving crash dumps. Each now waits for the ordered cleanup, and a source check inside the test suite refuses that shape from coming back (it turned red on exactly those three files first). The section-binding harness had another trap: a run changes twenty-five files of the project copy it opens, so a second run on the same copy looked like two failing rows. Restoring twenty-five files would have been a fragile re-implementation of the app's save; instead the harness now checks the copy before launching anything and refuses, by name, with the recipe - a fresh copy per run.", "detail": "O66. Merge c6268cde (agent branch parcel/o66-siblings-ordered-teardown: 20da9f09 G5 in check-harness-guards.mjs, a768fb28 bg-wrap + chunk-links await killTree + chunk-links takes AEON_DIR env instead of the live aeon path, 614d9826 section-raster-select await + leftover refusal exit 2, f64be235 OVERSEER O65 bullet dated O66 sentence). killTree census: 3 fixed, 2 already correct in effect via process.exitCode (bg-dangling-ref, effects-bob; G5 deliberately does not flag), the rest awaited/captured. G5 red-first 3 -> clean 168/168 -> plant 1 -> clean. Before/after per harness with coredumpctl deltas all 0 (three runs each after; ORDERED line present; exit 3-6 ms after summary, which now prints after the teardown). Write set measured by sha256 manifest diff: 25 files (whole-project save plan) -> option (b) refuse: second run on a consumed copy aborts at +26 ms, exit 2, '0/0 rows had run'. Merged tree here: section-raster-select 23/23 exit 0 on a fresh git-archive 6e2495a5 copy, ORDERED, 0 cores; guard-proof 16/16; npm test 1 failed / 6149 passed / 7 skipped with the single red being the schema-drift currency row (see O49 entry)."}
{"at": "2026-09-02T03:48:08Z", "headline": "Effects presets can now carry palette cycles and palette variants, exactly as the contract spells them, and the contract's own test cases run here by name.", "matters": "This is the first piece of the effects push you asked us to continue: the file format is caught up with the shared contract, so the cycle and variant controls can be built next. Two things worth knowing. Nothing you author with these keys reaches a ROM yet, because the engine side still refuses them; the controls will say so on screen. And a deliberate trip-wire is set: the day the engine catches up, one test here goes red on purpose to say the controls are unblocked, so a red run with that name is news, not a break. The catch-up also found that our schema checker was reporting full coverage while actually unable to read one of the new constructs; it reads it now and a walker proves every node.", "detail": "master 33a19fc1 (4 commits c02d90c2, 6bc22d41, 2cea72aa, 33a19fc1), branch parcel/ew-revendor-preset-12aecd5-r2, salvaged from the dead agent's uncommitted draft in .claude/worktrees/agent-a80dffc8f3fae764e. Schema blob f85dde134325676a6c701ca1f77e90dbc958bca2 = empyrean 12aecd58825de5c830b4bbe33fe9f80cf01bea83 = origin/main 6092f784 at landing; vectors blob cc3f0f96cf572b1731e0a21ad3e9188b2bfd8958. Overseer's own run on the merged tree: vitest 449 files passed | 2 skipped, 6180 tests passed | 7 skipped (03:46:38Z-03:46:52Z); tsc clean; a one-key poison of the vendored schema went red on 7 rows naming blob, byte count and the failing vector. Side fix: test/formats/aeon-fixture-currency.test.ts checked every sidecar revision against aeon, so the new empyrean sidecar failed as 'unpublished aeon SHA'; sweep is now per-repo block. ROADMAP row 97 replaced. Lag trip-wire: effects-preset-schema-drift.test.ts row 'the contract-leads-consumer lag at aeon \u2026 is exactly the item-5 keys'."}
{"at": "2026-09-02T04:24:25Z", "headline": "The effects preset panel now has controls for palette cycles and palette variants, with a warning above them that says the engine does not read these yet.", "matters": "You can author a cycle script or a palette variant per preset, keep a slot's hand-made value, clear it, or switch cycling off, and each of those is a distinct choice the file records differently, so nothing you leave alone gets rewritten. The warning is honest about the gap: what you set is saved to the preset file and goes no further until the engine side lands its half, and it is built so it disappears by itself the day that happens rather than lingering. Nothing clamps or invents a range; the engine's own checks remain the authority. Verified in the running app on two screen sizes, reading the saved bytes back.", "detail": "master 17ef3e50 (commits 72dc988f, effd72a5, 17ef3e50), branch feat/variant-cycle-controls. Premise module src/core/formats/effects/preset-lag.ts (PRESET_KEYS_AWAITING_AEON, the one hand-typed list; drift test's last row measures it against aeon docs/EDITOR_RASTER_PRESETS.md at origin/master d78f9090; PresetLagDisclosure renders null when empty; coupling row refuses a hand-typed copy). Provider commands in src/renderer/providers/effects-preset.ts via editPresetCommand (one undo, one set-effects-preset). Harness scratchpad/variant-cycle-harness.mjs, npm run harness:variant-cycle: 31/31 at 1680x1050 and 1920x1080, copy file bytes cycles:null / variants:[{lines:11,shift_r:3},null], untouched authored_probe.json byte-identical 484 B; disclosure 198 px inside a collapsed section (37 px shut). Overseer's own run on merged tree: vitest 451 files / 6223 tests passed, 7 pre-existing skips (04:23:39Z-04:23:53Z), tsc clean; own poison: premise emptied -> 5 rows red across drift + disclosure tests. Packet docs/reviews/2026-09-02-variant-cycle-controls.md; ROADMAP row 97 replaced in place. Agent reported its P1-P4 plant diffs lost to context compaction; failing-row logs in the packet are the record. TAGGED: nothing here reaches a ROM until aeon's generator lowers the keys (DoD item 5, chain 196)."}
{"at": "2026-09-02T04:33:53Z", "headline": "Measured why a full save touched section files nobody edited: the writer fills in the raster binding key as empty where the file had none, and the shared contract says that is the same thing, so it stays.", "matters": "You will see this exactly once per section file on the engine side, two files today, the same way the palette binding key appeared once before; after that a save that changes nothing writes nothing. It is not a bug and nothing was changed, but four tests now pin the behaviour so a move in either direction is loud. The rest of the thirty-three files the engine lane is discarding are not this key and were not looked at here.", "detail": "master 68aef6b6, branch fix/save-rasterref-absent. Path: load.ts:421-425 -> section-meta.ts:145 (absent folds to null on parse; Section.rasterRef is string|null, s4-types.ts:263) -> save.ts:144-153 -> section-meta.ts:122-127 (canonicalJsonPretty, four keys). Contract empyrean origin/main AURORA_EFFECTS_SCHEMA.md \u00a73.1 line 363: 'null / absent = keeps its hand-authored raster channel. Absent and explicit-null are the same state, exactly as for sceneRef.' At aeon origin/master d78f9090 only section_0 and section_4 lack the key; section_5 binds ojz_sec5_showcase. Ruled A (leave the writer) by the overseer on the contract's own sentence; no owner card. Tests src/core/project/aeon/__tests__/aeon-save.rasterref-absent.test.ts (4 rows, 3/4 red on each of two poisons). Packet docs/reviews/2026-09-02-rasterref-absent-save.md. Overseer run on merged tree: 452 files / 6227 tests passed, 7 pre-existing skips (04:33:11Z-04:33:23Z), tsc clean. Loose end booked: aeon-save.test.ts:83-97 SCENE_META_ON_DISK no longer resembles aeon's real files since 3674d85a."}
{"at": "2026-09-02T04:42:02Z", "headline": "The seventeen authored backgrounds are now safely in the engine's history \u2014 the exposure we flagged on the 30th is closed.", "matters": "Opening the project on a fresh machine now brings every background image along, not just the list of names; nothing authored lives on one disk any more.", "detail": "O37 retired. Verified at aeon origin/master d78f9090 through git objects: .gitignore lines 18-19 negate ojz_bg_*.bin and ojz_act1_bg*.bin (aeon commit befbae65, 2026-08-30T08:46:46-04:00); 17 bodies + 17 _tiles + ojz_bglib.json tracked. No further relay sent; the hub's 2026-09-02 instruction to relay O37 to aeon-ca was against a board row that had not been retired."}
{"at": "2026-09-02T08:23:23Z", "headline": "The test that proves a saved level keeps its effects assignment was checking against an invented file; it now checks against the engine's real one.", "matters": "That test is the only thing standing between an author's scene assignment and being silently dropped on the next save. It had drifted into describing a file the engine never writes, so it could have passed while the real thing broke. It now reads the engine's actual saved file, byte for byte, and states exactly the one line the editor is allowed to add.", "detail": "O67 CLOSED. aurora master 8f40f87c (merge of fix/o67-scene-meta-fixture: 6b72ea49, a3ba15d6). SCENE_META_ON_DISK re-originated from aeon origin/master d78f9090 games/sonic4/data/editor/ojz/act1/section_0.meta.json, read through git objects; overseer re-verified byte-identity independently (108 B, sha256 3b375c4e..8227b9). The round-trip expectation is DERIVED by canonicalisedByAurora() from the fixture text (insert the sorted rasterRef line, add the \u00a78 tail), not typed; a new row pins that the canonicalisation happens exactly once. Overseer re-planted two of the agent's three poisons on the merged tree: omit-rasterRef-when-null -> 3 failed/26 passed, drop-sceneRef -> 5 failed/24 passed, both matching the agent's report; git status clean after restore. RASTER_META_ON_DISK kept synthetic (aeon's only real rasterRef body, section_5, has no non-null sibling and would delete the did-not-eat-its-sibling assertions) but its bgLayoutRef corrected to empyrean da91abce \u00a73.1's actual example value, verified at that revision. Merged tree: Test Files 452 passed | 2 skipped, Tests 6228 passed | 7 skipped, full npm test chain, 25s wall."}
{"at": "2026-09-02T08:48:03Z", "headline": "The editor's tests and tools no longer have this machine's home directory typed into them \u2014 they find the other projects by asking, and say so out loud.", "matters": "Two hundred and fifty-two lines across a hundred and thirty-eight files named one specific folder on one specific computer. On anyone else's machine those rows could only skip, silently, while still reporting green. They now resolve the same paths by rule, refuse loudly when pointed somewhere wrong instead of quietly falling back, and a test that used to read the engine's half-finished working files now reads a committed version instead.", "detail": "SUITE-PATHS CLOSED. aurora master 70c672dd (merge of parcel/suite-paths: 311672d1, 67384196, 69e654f2, 5a6803fb, 28ad9ce9, c53d1bff; 154 files, +1805/-413). Adopts empyrean contract/SUITE_PATHS.md at origin/main 82982b7f: canonical <NAME>_DIR / EMPYREAN_SUITE_ROOT with AURORA_*_REPO, AURORA_PEER_ROOT, LIVE_AEON, S1_DIR as announced transitional aliases; four-step precedence; set-but-wrong throws SuitePathError at that step; canonical-vs-alias disagreement refuses naming both; siblingRootSource()/siblingPathSource() name the answering step. New siblingDefaultPath* so the 7 live-tree guards compare against the DEFAULT rather than the override. Gate now covers scratchpad/ and .py/.sh (1164 files scanned, 4351 git-ignored excluded and counted in its own output). screen.test.ts reads aeon through git objects at origin/master instead of the live tree. OVERSEER VERIFICATION on the merged tree, not taken from the report: planted a home literal in a .mjs (rc=1, named bganim-preview-fixture.mjs:6) and in a .py (rc=1, named find-curved-slope.py:4), and a same-path COMMENT correctly stayed green; exercised the resolver live \u2014 step-1 set-but-wrong throws, AEON_DIR-vs-LIVE_AEON disagreement refuses naming both, LIVE_AEON prints exactly one alias line while AEON_DIR prints none, siblingDefaultPathOrUnresolved('aeon') ignores AEON_DIR=/tmp and still answers the live path; git status clean after every plant. Full npm test on the merged tree: Test Files 453 passed | 2 skipped (455), Tests 6250 passed | 8 skipped (6258), 41s wall. TAGGED item CLOSED by the overseer rather than deferred: VITE_AURORA_DEBUG=1 npm run build then npm run harness:toast-overflow drove the real app through the migrated electron path \u2014 15/15 rows, so the ELECTRON_BIN substitution shared by ~60 CDP harnesses launches. Two follow-ups booked (O68, O69)."}
{"at": "2026-09-02T09:05:58Z", "headline": "A test that was supposed to prove the editor finds its sibling projects correctly was quietly measuring nothing wherever anyone actually ran it; it now builds the conditions it needs and refuses when it cannot.", "matters": "The check exists because there are two ways to ask git 'where am I', and one of them answers wrongly from a secondary copy of the repo. But the two answers only differ inside such a copy, and the tests normally run in the main one \u2014 so the check skipped every ordinary run and had been proving nothing for its whole life. It now creates the copy it needs, measures there, and prints both answers side by side on every run. More importantly, when it cannot tell the two apart it fails saying FIX THE BED, NOT THE RESOLVER \u2014 so a future change that quietly breaks the setup gets caught instead of sliding back into a green that means nothing, which is the exact failure this work existed to close.", "detail": "O68 CLOSED. aurora master 6566677d (merge of fix/o68-step3-worktree-proof: cdab2f42, a1ea3024, a5656114; one file, +246/-18; the subject sibling-root.mjs blob is byte-identical to master, verified by rev-parse). Row runs rather than skips: 24/24 in that file, pair printed on green. FULL SUITE ON THE MERGED TREE, measured here not branch-side: 453 files passed / 2 skipped (455), 6252 tests passed / 7 skipped (6259), zero failures; all 7 skips name reasons per skip-report. Agent supplied five red-first plants. I ran an INDEPENDENT sixth aimed at the question its five did not ask -- is the announce refusal load-bearing or decorative. Plant A (bed executes the MAIN copy, announce refusal intact) => UNMEASURABLE, correct. Plant B (same inert bed, announce refusal neutered to `false &&`) => GREEN, and it printed a pair that looks entirely convincing: correct wrong-derivation path, correct right-derivation path, correct resolver answer. So the measured pair the suite contract asks for is NOT self-validating -- an inert bed prints an identical-looking one. The announce line naming the worktree is the ONLY check separating a real bed from a vacuous one. Relayed to the hub for sigil and aeon, who are building the same clause. Leak check independently reproduced: worktree count 9 before and after, zero /tmp/aurora-step3-* left behind, across both failing plants. Ratified the agent's one flagged deviation: an optional 4th cwd param on the run() helper, defaulting to dirname(subject), so prior behaviour is unchanged and only the cwd-pin row varies it. Contract clauses this closed against: empyrean 08dd3f6 (original, from this lane's finding), 923cfd4 (module anchoring), 9c86639 (bed placement, sigil), f17940b (UNMEASURABLE refusal + report the pair, sigil), 90c4cf6 (which announce artifact, from this lane's cache grep)."}
{"at": "2026-09-02T09:17:36Z", "headline": "The tools now agree on what to call the editor's own folder, and a harness that must never touch the engine's live files stops guessing and refuses.", "matters": "Ninety-three files each worked out where the editor lives, in two different spellings and two different ways, so pointing a run at another copy moved two thirds of them and quietly left the rest behind. Separately, three tools defaulted to a temporary folder from a session that ended weeks ago; the worst of them is the one whose whole job is to work on a throwaway copy of the engine and never the real one, and its dead default sailed straight past that safety check. It now stops and says what to set. The checker that is supposed to catch this kind of thing could see neither problem, because neither of them mentions a home directory; it can now, and finding out taught it about eight more places.", "detail": "O69 CLOSED. branch fix/o69-suite-paths-residue, three commits (c020d497, 8f9a89ae, 66f0703d) off master b0581478. (a) AURORA_DIR is now the one derivation for \"which aurora tree\", on test/support/sibling-root.mjs and mirrored in scratchpad/lib/suite_paths.py: step 1 the explicit variable with AURORA_ROOT and AURORA_REPO as announced transitional aliases, step 3 the module's own file location. STEP 2 DELIBERATELY SKIPPED for the repo asking the question -- consulting EMPYREAN_SUITE_ROOT for aurora itself would make the documented poison recipe EMPYREAN_SUITE_ROOT=$(mktemp -d) npm test relocate the repo under test, and would make a linked worktree resolve to the MAIN checkout. ENUMERATION, two greps neither of which contains the other: 64 files read process.env.AURORA_ROOT (2 preferring AURORA_REPO), 29 MORE wrote dirname(dirname(fileURLToPath(import.meta.url))) with no override at all, 34 already imported the const. 133 files touched, 10 new resolver rows (33 in the file), all in child processes with AURORA_ROOT/AURORA_REPO added to the env scrub. (b) Three defaults named a dead /tmp/claude-<uid>/<slug>/<session uuid>/scratchpad path. guard-surface-harness's was its AEON_DIR, so the dead path never tripped the live-tree guard and an unset variable died later and further away; it now REFUSES at import via checkoutOverride('aeon'), naming why and printing the cp -r recipe. The live-tree refusal was PROVEN FIRING with AEON_DIR set to the live aeon tree ('refusing to run against the real aeon tree -- use the throwaway copy') and PROVEN NON-VACUOUS with a throwaway directory, which passes both refusals. capture-harness (SHOTS re-created by mkdirSync so screenshots landed unseen) and verify-s1-roundtrip (SCRATCH hard-wired, no override, holding the s1disasm working copy and both ROMs) fixed likewise. (c) check-peer-path-literals gained session-scratchpad (derived from os.tmpdir()) and unratified-env (variable list built from checkoutEnv/checkoutEnvAliases over a new exported SUITE_PEERS roster, read out of the resolver rather than retyped). Found 8 more: two harnesses deriving the sibling by string-surgery on the worktree path, the Python injector's parents[2]/'aeon', two hand-taken overrides, screen-frame-guides-harness (message said 'never the live one' and never checked -- now has the real refusal, proven firing), and bg-override-art-injector-gate.test.ts reading AEON_ROOT, not the spelling anything else honours. AEON_ROOT added to checkoutEnvAliases so that override keeps working. Rule 3 does NOT cover .sh, stated in source: a shell script cannot import a resolver. A $VAR shape was tried and dropped after it flagged a template interpolation of a local const. RED FIRST throughout: both new rules planted in .mjs and .py with a comment holding the same text (correctly exempt); rule 2's pattern broken -> guard 1 exit 2; rule 2's canary LINES deleted -> new guard 4 'never fired on either canary' exit 2; an exemption path renamed -> new guard 5 exit 2. Suite 453 files passed / 2 skipped (455), 6261 passed / 7 skipped (6268), every skip naming its reason. Contract read at empyrean 08dd3f6. [at CORRECTED at landing: the returned entry carried 2026-09-02T09:20:00Z, which was 2.5 minutes in the FUTURE of the merge clock and ended in :00 seconds \u2014 the estimate signature the LANE_LOG contract names. A future timestamp is rejected outright by the reader, so every true thing in the entry would have been lost with it. Reset from `date -u`; nothing else in the entry touched.]"}
{"at": "2026-09-02T09:41:38Z", "headline": "The editor stopped letting anyone tell it where it lives, and split that question away from the one it was really being asked: which built copy should this run use.", "matters": "One setting was doing two jobs. It was meant to say which folder the editor lives in \u2014 something the code can just look up, and can only be wrong about if someone types it wrong \u2014 but it was also the only way to say 'run against the build over there', which is a real need because a second working copy of the repo has no build in it at all. Because the two jobs named the same folder for everyone, nobody could see they were different jobs; the first time somebody used the setting for the second job, it silently moved the whole repo under test. The lookup is now just a lookup, the setting is checked against it and refuses if they disagree, and the second job has its own name. The part that took the longest was how to prove callers were sorted into the right job: a check that the two names AGREE only ever runs where the mistake is invisible, so instead the test points them at different folders on purpose \u2014 with the mistake planted, everything else in the suite stays green and only that test goes red.", "detail": "O70. branch fix/o70-own-checkout-observed, three commits off master cbfa22cf. RULING IMPLEMENTED: empyrean contract/SUITE_PATHS.md @ fba68d5 'A resolver's OWN checkout is observed, not resolved', read at revision. AURORA_DIR is now the module's own file location, step-source `own` (not `step 3`), and AURORA_DIR / AURORA_ROOT / AURORA_REPO are a CONSISTENCY CHECK that throws on disagreement; a symlinked spelling of the same directory counts as agreement. Mirrored in scratchpad/lib/suite_paths.py. RED FIRST IN BOTH DIRECTIONS, run against cbfa22cf's subject copied into place: DISAGREEING -> old subject 'expected a non-zero exit; got 0 with stdout /tmp/aurora-not-this-tree-VtbydC' (it obeyed); AGREEING, the anti-vacuous half -> old subject source 'step 1: AURORA_DIR=...'; symlink row -> old subject answered with the LINK path. ALTERNATIVE GREEN PATH RULED OUT: pickEnv's two-spellings-DISAGREE refusal also throws SuitePathError and also names a variable, so the refusal rows set exactly ONE spelling, assert the consistency check's own sentence, and assert the word DISAGREE is ABSENT. NEW VARIABLE AURORA_BUILT_TREE for the second question (which built tree a run executes against), named after the contract's artifact-directory case (oracle's ORACLE_AEON_DIR) and deliberately not *_DIR, which is the ratified CHECKOUT spelling. No aliases: AURORA_ROOT stays a transitional alias of AURORA_DIR for ~64 instruments and is still announced once on stderr. SPLIT LANDING, sigil's caution @ 8be3a16: sigil proposed a gate asserting the two names AGREE; declined for the inverse, a row that points them APART, because an agreement gate fires on the one person doing the legitimate thing and can only ever test the configuration in which a misassignment is invisible. MEASURED with the misassignment planted (run-root's pin branch answering question 1 only): pointed apart, 2 of 5 rows red ('the run target must follow the variable it was assigned: expected /tmp/aurora-lives-in-9Q5AVJ to be /tmp/aurora-pinned-build-sp8dGx'); not pointed apart, sibling-root.test.ts 38/38 GREEN with the same plant in the tree. The walk moved out of mapviewport-baseline-harness.mjs (unrunnable in a test \u2014 it spawns Electron against dist/ on import) into scratchpad/lib/run-root.mjs with the caller's own location passed IN, plus a fifth row asserting the wiring structurally, since none of the other four can see the harness keeping a private copy. CONSUMER SURFACE CORRECTED: the hub's note said that harness is the only run-target consumer; it is the only consumer of the override ACCESSOR. 105 of the 123 scratchpad/*.mjs instruments that import AURORA_DIR use it to reach node_modules/ or dist/ \u2014 question 2 wearing question 1's answer, benign from the main checkout, NOT converted here, recorded in run-root.mjs's header as the migration surface. SCOPE recorded in both module headers: the fold is safe precisely while the build is in-tree AND the artifacts are revision-stamped; sigil is correctly not splitting, aurora fails the first condition because a linked worktree has no dist/ of its own. This is a conditional, not 'split every <TOOL>_DIR'. GATE: rule 3's variable list is now the resolver's exported OWNED_ENV, imported whole \u2014 the gate's own claim that 'a variable added there is policed here without anyone remembering to edit this file' was false for a variable under a NEW constant. PROVEN BOTH WAYS with process.env.AURORA_BUILT_TREE planted in a real tracked file (scratchpad/mapviewport-baseline-harness.mjs): the pre-O70 gate printed OK with 20 variables policed, blind to it; this gate FAILs naming scratchpad/mapviewport-baseline-harness.mjs:222 with 21 policed. Plant removed, gate green. FULL SUITE: 454 files passed / 2 skipped (456), 6271 passed / 7 skipped (6278), zero failures, all four check:* gates OK, typecheck clean, every skip naming its reason. NOT ATTEMPTED, tagged for foreground: executing mapviewport-baseline-harness.mjs itself, which needs a built dist/ and a live app."}
{"at": "2026-09-02T09:46:43Z", "headline": "The harness that borrows another copy's build now says so in a line a test can actually read, instead of one only a running app could print.", "matters": "When a second working copy of the repo has no build in it, the harness climbs up and uses the nearest copy that does. That is fine \u2014 the two are genuinely different things and it is allowed to pick \u2014 but only if it tells you which one it picked, because a speed measurement taken against somebody else's build and reported as yours is worthless. It did print that. The problem was where: inside the part of the script that only runs once a real app has launched, so nothing could ever check the wording, and a change that quietly dropped the warning would have looked identical. The message now comes from a small shared piece that a test drives directly against fake folders, and two tests hold it from both sides \u2014 one fails if it stops warning, the other fails if it warns every time.", "detail": "O70 follow-up, commit 4/4 on fix/o70-own-checkout-observed, answering empyrean contract/SUITE_PATHS.md @ c9bc05f ('Two rows, not one', the third shape). SHAPE DECLARED, as the contract requires of a split: AURORA_BUILT_TREE is NO DEFAULT (unset, the resolver reports none), but the consumer of that none is an INDEPENDENT SECOND DERIVATION \u2014 the runnable-tree walk \u2014 in the sub-case that is MEANT to differ, since it searches for a BUILD not a checkout and a linked worktree is a real checkout and an unrunnable one. Artifacts carve-out, so the owed assertion is the ANNOUNCEMENT, not equality. NOT TESTABLE UNTIL ONE EDIT: the announcement was composed inline in mapviewport-baseline-harness.mjs's main(), which needs a built dist/ and an electron binary, so no row could reach it \u2014 leaving it there would have been the untestable half of the conformant shape, and 'differs silently' is the defect the clause exists to prevent. The render moved into scratchpad/lib/run-root.mjs as describeRunRoot(), taking the RETURNED value (recomputed per call, not a memoised stderr nag, which the contract forbids as the proof artifact); the harness prints describeRunRoot(RUN_ROOT) and the structural row asserts it does. So the outcome was the first of the coordinator's three (factor it out and drive it in-process), not the BLOCKED tag. TWO ROWS, BOTH PLANTED RED, since either alone is satisfiable by a subject that tells a reader nothing: PLANT A, differs silently (`false && borrowed`) -> the announce row red, 'and MARK it, because those two are not the same tree: expected root: /tmp/aurora-announce-ancestor-m... to contain BORROWED', control green, 1 of 7 red. PLANT B, marks unconditionally (`true || borrowed`) -> the CONTROL red, 'nothing was borrowed, so nothing may say so: expected root: /tmp/aurora-announce-in-tree-Kq... not to contain BORROWED', announce row green, 1 of 7 red. CONSTRUCTED NOT AMBIENT AND PROVED: the child prints back what it saw in its own environment for both names and the row asserts both are '(unset)', so a green arriving from an ambient override or from the scrub regex ceasing to match is unreachable. CONTRACT CITATION NOW STALE, for the two lanes reading this as reference: c9bc05f cites the walk at 'scratchpad/mapviewport-baseline-harness.mjs (at 3bcefa3c, line 223)'; that was accurate at 3bcefa3c, but da6f08eb moved the walk into scratchpad/lib/run-root.mjs and 4/4 moved the announcement with it. FULL SUITE: 454 files passed / 2 skipped (456), 6273 passed / 7 skipped (6280), zero failures, all four check:* gates OK, typecheck clean. STILL NOT ATTEMPTED, tagged for foreground: executing the harness end-to-end, which needs a built dist/ and a live app."}
{"at": "2026-09-02T10:02:52Z", "headline": "The check that the editor can find the other projects was only ever run in the one situation where it cannot go wrong, so it now runs in the ordinary one too", "matters": "The editor works out where its neighbouring projects live by asking the version-control tool where its own project folder is. That tool gives a different kind of answer depending on which sort of copy you ask from: from the temporary side copies that helper sessions work in, it always answers with a full address, and from an ordinary everyday copy it answers with a short one that means nothing on its own. Every check we had was written from a side copy, so the short answer had never once been tried, and a version of the editor that got it badly wrong would have looked perfectly healthy in every check we run before merging. A neighbouring project shipped exactly that and only found out after merging. New checks now build a throwaway project of their own, in each of the ordinary shapes, and read the answer back. Each one refuses loudly, in its own words, if the throwaway project it built is not actually capable of telling a right answer from a wrong one, so a check that quietly stops testing anything cannot pass unnoticed. Both were confirmed by deliberately breaking the editor first and watching them go red, and the old checks stayed green through that same break, which is the whole reason the new ones exist.", "detail": "O74, branch fix/o74-step3-main-checkout, two commits. THE GAP: test/support/sibling-root.mjs step 3 derives the suite root as dirname(dirname(<git rev-parse --git-common-dir>)) with cwd=AURORA_DIR. `git rev-parse --git-common-dir` has THREE output shapes, measured here on git 2.55.0 by the new rows themselves: from a linked worktree an absolute path whether asked or not; from a main-checkout root `.git`; from a main-checkout subdirectory two levels down `../../.git`. The pre-existing step-3 bed is a linked worktree and exercises only the absolute shape; sigil's merged tree went red on the relative one (its mechanism was resolving git's relative output against the process cwd rather than against the directory git ran in \u2014 NOT the same mechanism as the mutation below, but the same class of observable). MEASURED BLINDNESS, by the controller before this parcel: with `--path-format=absolute` deleted from our own resolver, the resolver's 38 rows went 38/38 green (the step-3 row asserted only the source-string PREFIX, built from the command name, never from the result; the worktree bed is unaffected; the cwd-pin row asserts stability and non-null, never correctness), the full suite reported ZERO failures at 443 files / 5935 rows against a normal 454 / 6273 because 270 rows became SKIPS not failures (277 skipped against 7), and the literal gate failed for a fabricated reason. Only the announce line named the bug. WHAT LANDED: two rows in test/support/sibling-root.test.ts, `derives the suite root from a MAIN CHECKOUT ROOT, where git answers `.git`` and `derives it from a MAIN-CHECKOUT SUBDIRECTORY, where git answers `../../.git``, sharing makeMainCheckout(nest) / proveStep3FromMainCheckout(). The bed is a scratch `git init` under mkdtemp with the subject COPIED IN at <anchor>/test/support/, since AURORA_DIR is the module's own file location and is what git is handed as cwd; nest=[] gives shape 1 and nest=['packages','editor'] gives shape 3. THE REAL MAIN CHECKOUT WAS DECLINED AS THE BED, deliberately: its expected suite root is exactly what a bed-ignoring resolver answers, so that row cannot discriminate \u2014 and these rows run inside a linked worktree half the time anyway. Expectation = dirname(dirname(<absolute --git-common-dir>)) read out of the bed, never typed. FOUR UNMEASURABLE BED REFUSALS before any resolver assertion, following the existing file's refusal shape: (1) git's default answer must be relative \u2014 an absolute one means the bed is the linked-worktree case renamed; (2) it must be the shape the row names, so the two rows cannot silently become one; (3) the derivation must differ with and without the flag, else a flag-less resolver passes; (4) the bed's suite root must not equal what the resolver derives from the REAL tree, measured live in the same run by running the real subject \u2014 this is the hub's second question answered inside the row. Then the returned step-source (recomputed per call, on stdout, embedding git's cwd) must name the bed's anchor. RED-FIRST TWICE, mutation shown from disk via git diff before each red run. PLANT 1, `--path-format=absolute` deleted from sibling-root.mjs:479 (verified from source that :487 is a purely lexical dirname(dirname(common)) with no resolve(), so a relative common can only yield a relative root): both new rows RED \u2014 `expected '.' to be '/tmp/aurora-step3-main-aBBkFL/suite'` and `expected '..' to be '/tmp/aurora-step3-main-0Tzk22/suite'` \u2014 and 38 passed, i.e. the linked-worktree row and every other row stayed GREEN, the blindness reproduced in one run. PLANT 2, the inert bed: derive(bed.subject) -> derive() so the row runs the MAIN copy: both rows RED at bed refusal (4)/(5), naming `it derived from /home/volence/sonic_hacks/aurora/.claude/worktrees/agent-... , not from the main checkout /tmp/aurora-step3-main-fsJrW9/suite/aurora this row built`. Both restored with `git checkout HEAD --` from the committed baseline and re-run green. SUITE: 454 files passed / 2 skipped (456), 6275 passed / 7 skipped (6282), zero failures, against the baseline recorded in the previous lane-log entry (454/2, 6273/7): +2 rows and the SKIP COUNT DID NOT MOVE, which the plant above proved is the number that hides a broken resolver. All five gates exit 0 (check-test-collection, check-pseudo-skip, check-peer-path-literals, check-object-stringify, check-harness-guards); typecheck clean. Contract read at empyrean origin/main contract/SUITE_PATHS.md, which governs and agrees with the brief. LEFT OPEN, MEASURED NOT GUESSED: scratchpad/lib/suite_paths.py is the Python twin, passes --path-format=absolute at line 211 and does the same lexical Path(common).parent.parent, so it is immune to this mechanism \u2014 but there is NO test file for it anywhere in the repo (find over test/ src/ scripts/ scratchpad/ returns only the module itself), so it has zero step-3 rows in either configuration while 7 instruments import it. Booked for the controller, not built tonight. NOT ATTEMPTED, no emulator was touched: nothing here needs runtime confirmation."}
{"at": "2026-09-02T10:10:31Z", "headline": "The check now also runs in the copy of the project the code is really checked out as, and says out loud when it could not", "matters": "The earlier checks each build a throwaway project of their own and read the answer back. That proves the editor can work out where its neighbours live in a project someone assembled for the purpose, and it says nothing about the project the code is actually checked out as, which is where the trouble showed up in a neighbouring team. A further check now asks the question in this copy itself. It was left out at first for a reason that was true but was about a different thing: that check cannot tell whether the editor really looked here or merely gave the right answer from habit, because in this one case the two answers are the same. The reviewer's ruling, and it is right, is that this makes the check narrower rather than empty, and the honest move is to keep it and write the limitation into the check itself, where the next reader will see it, instead of using it as grounds to delete the check. Helper sessions work in a side copy where the situation being checked does not exist, so there it stands down and says in the run's own output that it measured nothing, rather than passing quietly. It was still confirmed by deliberately breaking the editor, in a throwaway ordinary copy assembled for that purpose, and watching it go red.", "detail": "O74 follow-up, third commit on fix/o74-step3-main-checkout, after the coordinator REJECTED the deletion of the real-main-checkout row. The rejected reasoning was mine and the rejection is correct: the row's expectation is the real tree's own suite root, which is exactly what a bed-ignoring resolver answers, so it does not discriminate THAT property \u2014 but it is fully discriminating for the flag defect, and from a main-checkout root without --path-format=absolute the derivation yields '.', not the suite root. Narrower, not inert, and narrower on the axis a constructed bed cannot reach: empyrean contract/SUITE_PATHS.md, 'a derivation verified where it could be constructed says nothing about where it runs' and 'a lane whose step-3 row is a bed and nothing else has an untested production path invisible in a green run'. ROW: `and THIS checkout, when it is a main one \u2014 the configuration production runs in`. Anchor is R.AURORA_DIR read back from the subject, not recomputed in the test. Detector is the SAME one the bed refusals use \u2014 isAbsolute(`git rev-parse --git-common-dir`) means linked worktree \u2014 deliberately, so there are not two ways of asking that can drift apart. Two skip paths, both ctx.skip('SKIPPED, NOT PASSED: \u2026'): not a git repository at all, and standing in a linked worktree, the latter naming the production configuration it did not measure AND the one it found instead. Assert path: expected = dirname(dirname(<absolute --git-common-dir>)) measured live from this tree in the same run, plus .not.toBe(withoutTheFlag) and a source-names-the-anchor check; console.log prints the verdict including the line '(this row does NOT discriminate a bed-ignoring resolver \u2014 see its header)'. The header states the non-discrimination and tables which row covers which property. RED-FIRST, AND NOT DEFERRED DESPITE THE WORKTREE: this session runs in a linked worktree, where the row skips, so the committed tree was copied to a scratch `git init` main checkout under the session scratchpad (SKIP .git/node_modules/dist/.claude; node_modules symlinked to the aurora checkout's, since this worktree has no vitest of its own and npx walks up to find one \u2014 third-party packages only, every source file, config and the subject from the copy). From there: 40 passed | 1 skipped (41), the row's ASSERT branch executed and green, announce naming \u2026/mainck/suite/aurora. Plant applied in the worktree (--path-format=absolute deleted, git diff --stat + line quoted from disk) and the checkout rebuilt from it, line re-quoted from the clone's disk: 3 failed | 37 passed | 1 skipped, the new row RED with `run in this repo's own main checkout at \u2026/mainck/suite/aurora, the resolver answered .; the directory holding this checkout \u2026 is \u2026/mainck/suite: expected '.' to be '/tmp/claude-1000/\u2026'`. Restored with git checkout HEAD -- from the committed baseline, rebuilt, 40 passed | 1 skipped again. HONEST LIMIT, STATED NOT BURIED: that scratch checkout is a STAND-IN for the owner's main checkout, so the row AS IT WILL RUN AT LANDING is unproven here; the coordinator runs it in the main checkout at landing. COMPLEMENTARITY, visible in both runs and worth noting: in a worktree this row skips and the linked-worktree row measures; in the main checkout the reverse (there the worktree row skips because the scratch repo has no commits, `git worktree add HEAD` -> `fatal: invalid reference: HEAD`, and it says so). SUITE in this worktree: 454 files passed / 2 skipped (456), 6275 passed / 8 skipped (6283), zero failures; against the previous entry's 6275/7 the SKIP COUNT MOVED BY EXACTLY ONE and that one is this row declining to fake a measurement \u2014 the number the earlier plant proved is load-bearing, moving for the reason the run itself prints. Five gates exit 0, typecheck clean. CORRECTION TO THE PREVIOUS ENTRY, from the coordinator: the Python twin has 6 importers, not 7 \u2014 scripts/check-peer-path-literals.mjs NAMES it rather than importing it. The gap itself (no test file, so no step-3 row in either configuration) is unchanged and already booked. No emulator was touched."}
{"at": "2026-09-02T10:34:07Z", "headline": "The written rule that every entry in our work diary must carry a time read off the clock is now checked by the machine, for entries added from today onward", "matters": "Two of our shared records are meant to be written to and never edited, and each line is supposed to say exactly when it was written, taken from the clock at that moment. Some were not. People typed a time from memory, or rounded it to a tidy number, or filled one in afterwards for something they had done earlier. A measurement built last week showed the difference plainly: lines written properly sit right on top of the moment they were saved, and the others sit minutes or hours away. But it was a tool someone had to remember to run, so nobody did. It now runs with the rest of the checks, every time, and stops the run when a newly added line has a time it could not have read off a clock. The old lines are left exactly as they are. Editing them is forbidden - the whole point of these records is that they are never rewritten - and a check that was red every single day would teach everyone to ignore it. So the new check draws a line at the moment it came into existence: everything older is counted, reported out loud with the reason, and let be; everything from that moment on has to hold up. The line is drawn on when a line was SAVED, not on the date the line claims for itself. That distinction is the whole thing. If it were drawn on the claimed date, then somebody filling in a time for last week's work - one of the two problems this exists to catch - would fall on the old side of the line and never be looked at, and the check would quietly be half blind while appearing to work. Building the proof turned up a real fault in the first version and it is fixed: someone whose bad line had already been shared with the team had no way to put it right, because the old time stayed findable in the history forever and the check stayed red no matter what they did next. Correcting the line now clears it, and the correction itself is held to the same rule, so it cannot be used to slip anything through. Before it was believed, the check was deliberately broken in four different ways and watched to go red each time, including by adding a deliberately wrong line to the diary and taking it out again afterwards - both steps left in the record on purpose, so anyone can see the proof rather than taking my word for it.", "detail": "O76, branch fix/o76-ledger-timestamp-gate. WHAT LANDED: scripts/check-ledger-timestamps.mjs, fifth in the `npm test` && chain (after check-object-stringify, before check-harness-guards) and also `npm run check:ledger-timestamps`. It shells out to scratchpad/ledger-timestamp-audit.py, which is unchanged in measurement and gains three OPT-IN flags: --since (the ratchet), --strict-ahead, --only-present. WITHOUT THEM the instrument's exit statuses and every finding it prints are identical to master's copy - verified after each method change by running `git show master:` of the file side by side over both ledgers; two message templates gained a sentence naming the new flags and nothing else moved. THE CUTOFF IS ON COMMITTER TIME, NOT ON `at`: IN_FORCE = 2026-09-02T10:22:28Z, read off `date -u` seconds before the commit that added the gate (d39f0fc5, committed 10:22:57Z), so the gate binds its own landing and nothing earlier. It is a pinned constant, not a runtime derivation, because deriving it from the add-commit would move it LATER under any squash or rebase and a cutoff that drifts later grandfathers more; what IS derived is crossCheckInForce(), which reads `git log --diff-filter=A -- <self>` and exits 2 if IN_FORCE is later than it - the landing spot for an edit-it-forward. Today: 135 lane-log + 21 decisions grandfathered, printed with the reason on every run. ELEVEN CANARY CASES run before this repo is judged at all, on `git init` repositories under mkdtemp with GIT_COMMITTER_DATE set, through gateArgs() - the SAME flag list the real run uses, so the canaries cannot drift into testing a different command line. Each names its expected exit status, required substrings and FORBIDDEN substrings; RULES_EXERCISED requires every failure rule to have fired or the run exits 2. THE FIRST CANARY SET LOOKED COMPLETE AND WAS BLIND: every case behaved identically whether the cutoff read the commit or the `at`, so the set would have printed OK with the cutoff moved onto `at`. K4b (committed after the cutoff, `at` claiming before it) is the only discriminator; it asserts '1 entry IN SCOPE' and '1 GRANDFATHERED' as well as the status. RED-FIRST, FOUR MUTATIONS, EACH `git diff`-ed FROM DISK BEFORE ITS RED RUN: (1) in_scope() moved onto `at` -> K4b RED, 'exit 0, expected 1; missing \"1 entry IN SCOPE\" ... must not contain \"0 entries IN SCOPE\"', gate exit 2. (2) still_there() -> False, the new method's vacuity risk -> K2 RED, 'missing \"1 entry IN SCOPE\"', gate exit 2. (3) a lane-log entry planted 20 min adrift and COMMITTED (816c1f90) -> gate exit 1, 'OVER THRESHOLD (1) 1205s 2026-09-02T10:08:52Z', RATCHET line reading 1 entry IN SCOPE / 135 GRANDFATHERED; `npm test` rc=1 with NO 'Test Files' line, i.e. the chain stopped at the gate and vitest never ran. Measured aside: with the same line in the working tree but UNCOMMITTED the gate exits 0, because the audit reads git history - which is why the plant had to be committed. (4) LEDGERS pointed at a tracked-by-nothing path that EXISTS on disk -> audit exit 2, gate exit 2, `npm test` rc=2. (A non-existent path hits the earlier readable guard instead, also exit 2.) All four restored from committed baselines - `git checkout HEAD --`, never on a dirty tree - and re-run green. THE RATCHET IN BOTH DIRECTIONS ON THE REAL LEDGERS, same flags, cutoff moved back to 2026-08-01T00:00:00Z: docs/lane-log.jsonl exit 1 with STAMPED AHEAD (6) + OVER THRESHOLD (4) + TWO IN-SCOPE ENTRIES SHARE ONE STAMP (8); docs/decisions.jsonl exit 1 with 1 + 1 + 4. At IN_FORCE both exit 0. So the grandfathered entries are genuinely bad and it is the ratchet, not an inert check, that spares them. A DEFECT THE PROOF ITSELF FOUND AND THE THIRD FLAG THAT FIXES IT: removing plant 1 by a follow-up commit did NOT clear the gate. First appearance keys on the stamp, so a bad `at` stays in its commit's diff forever - meaning a PUSHED bad entry had no remedy short of rewriting history, i.e. a permanently red suite, the exact failure the ratchet exists to prevent, reintroduced elsewhere. --only-present judges only stamps the file still carries at HEAD and COUNTS AND PRINTS the withdrawn ones (today 2, both my plants). K6b proves the remedy clears the gate; K6c proves it cannot launder a stamp, since the replacement is judged at its own commit. Because that was a METHOD change, mutations 1 and 3 were BOTH re-run from scratch afterwards (plant 2 = 816c1f90, unplanted by ad58db23) rather than left resting on the pre-change proof. ALSO FIXED, found by the exit-2 proof: the gate's header documented exit 2 for an unmeasurable run while the code exited 1 for every non-zero status and printed 'take the time from date -u' guidance underneath - a right verdict with a fabricated reason. It now exits 2 and says nothing here claims an entry is wrong. SUITE: 454 files passed / 2 skipped (456), 6275 passed / 8 skipped (6283), zero failures, all six gates exit 0, typecheck clean, 21.2s. Against the controller's main-checkout baseline of 454/2 and 6276/7 the TOTAL IS IDENTICAL (6283) and one row moved passed->skipped: row 108's 'and THIS checkout, when it is a main one' row, which skips in a linked worktree. Confirmed rather than assumed by running that file alone here: 40 passed | 1 skipped, the skip naming this worktree. This parcel adds no vitest rows. Gate cost measured: 1.4s wall for 11 canary git repositories plus both real ledgers. LEFT OPEN, NOT GUESSED: whether the audit's permanent home is one copy in empyrean or one per lane is parked with the owner, and nothing written here assumes an answer - the script still takes ledger and repo as arguments and bakes in no path, so it lifts unedited. --strict-ahead is the GATE's setting and not the instrument's default, so a human running the bare tool still sees a sub-threshold ahead-stamp reported and gets exit 0; that asymmetry is deliberate (the instrument's verdicts must not move) and is stated in its docstring. No emulator was touched and nothing here wants runtime confirmation."}
{"at": "2026-09-02T10:55:43Z", "headline": "The second copy of the code that works out where our neighbouring projects live had never been tested at all, and now it is — in both of the situations it has to work in", "matters": "The editor has to work out where the other projects sitting beside it live on disk. There are two separate pieces of code doing that, one for each of the two programming languages this project is written in, and they have to agree. One of them was thoroughly checked. The other — the one six of our small standalone tools rely on — had no checks whatsoever, in any language, anywhere in the project. That is worse than a check that is wrong, because a wrong check at least shows up somewhere: a piece of code with no checks adds nothing at all to the tally at the end of a run, so a run that never looked at it and a run that looked and found nothing print exactly the same thing. It now has seven checks, and they run every time anyone runs the checks for the whole project. They cover both of the situations that matter: the temporary side copies that helper sessions work in, and the ordinary everyday copy — which give different sorts of answer, and only one of them had ever been tried anywhere in this project until last night. Each check builds its own throwaway project, puts a copy of the code inside it, and then refuses out loud if the throwaway it built is not actually able to tell a right answer from a wrong one, so a check that has quietly stopped testing anything cannot pass unnoticed. Six deliberate breakages were made and each was watched to go red first, including breaking the checks themselves rather than only the code they judge. One real disagreement was found and fixed along the way: the two pieces of code accepted different lists of names for the same setting, so somebody using one of those names got a working answer from one half of the editor and a refusal from the other. Making the two lists match is not the fix — a check that goes red the next time either list changes is, and that is what shipped.", "detail": "O75, branch fix/o75-python-resolver-tests, three commits. THE GAP, from docs/reviews/2026-09-02-python-twin-untested.md: aurora resolves peer checkouts through two twins, test/support/sibling-root.mjs (rows 106-108) and scratchpad/lib/suite_paths.py, and the Python one had NO test file in any language at any path while 6 instruments import it, so it had a step-3 row in neither configuration. Flattest member of the disjoint-population class four lanes hit on 2026-09-02: the others shipped a green artifact that looked like coverage, this one shipped no artifact, because a module with no test contributes no skip, no failure and no line to any suite total. ARITHMETIC RE-VERIFIED RATHER THAN ASSUMED: _suite_root derives with pathlib.Path(common).parent.parent, which resolves the string against nothing exactly as Node's lexical dirname does, so both twins agree on all three git output shapes ('.git'->'.', '../../.git'->'..', '/a/b/aurora/.git'->'/a/b') and the flag-deletion plant discriminates here for the same reason it does there. WHAT LANDED: scratchpad/lib/test_suite_paths.py, 7 rows, shaped after sibling-root.test.ts's step-3 block rather than invented - a LINKED WORKTREE (the only place --git-common-dir and --show-toplevel disagree), a MAIN-CHECKOUT ROOT ('.git'), a MAIN-CHECKOUT SUBDIRECTORY two levels down ('../../.git'), THIS checkout's own real main checkout (a loud skip naming the production configuration it did not measure, since agent sessions run in a worktree), the CWD PIN that makes copying the subject into each bed the only bed that works, the announce line, and the cross-twin alias comparison. Every bed is CREATED BY THE ROW and the subject is COPIED INTO IT, because _AURORA_DIR_DERIVED is Path(__file__).resolve().parents[2] and is what git is handed as cwd; expectations are mkdtemp paths DERIVED from the bed, asserted to differ both from a flag-less derivation and from what the resolver answers ignoring the bed entirely, the latter MEASURED live in the same run by running the real module. Five bed refusals fire before any resolver assertion and each says FIX THE BED, NOT THE RESOLVER. O73 FOLDED IN AND SETTLED: AEON_ROOT was accepted by the JS twin (sibling-root.mjs checkoutEnvAliases) and refused by the Python one, so one environment resolved for a JS instrument and refused for a Python one; Python accepts it now, but the DELIVERABLE IS THE CROSS-TWIN ROW, which reads both tables live over the JS twin's own SUITE_PEERS plus a non-peer name and compares them IN ORDER, since the first spelling that answers wins. Both files now carry a note where the lists live saying which row will catch the next divergence. RUNNER: scripts/check-python-resolver.mjs, sixth in the `npm test` chain and `npm run check:python-resolver`. python3 -m unittest, NOT pytest - pytest 9.1.1 is installed on this machine and that is exactly the reason not to depend on it; python3 is not a new dependency, since check-ledger-timestamps in this same chain already shells out to a .py and six scratchpad instruments are Python. The gate exists rather than a bare command in package.json because unittest prints `Ran 0 tests ... OK` with exit 0 when discovery matches nothing, which is this parcel's own absence surface one level up: it NAMES the seven rows it requires and exits 2 if any is missing, and prints every skip reason. RED-FIRST, SIX MUTATIONS, EACH `git diff --stat`-ED AND THE LINE QUOTED BACK FROM DISK BEFORE ITS RED RUN: (1) '--path-format=absolute' deleted from _suite_root -> both main-checkout rows RED (\"'.' != '/tmp/aurora-py-step3-main-vqjti8md/suite'\" and \"'..' != '/tmp/aurora-py-step3-main-50v0n2d3/suite'\") while the LINKED-WORKTREE ROW STAYED GREEN, the blindness reproduced in one run, and `npm test` rc=1 with ZERO `Test Files` lines, i.e. the chain stopped at the gate and vitest never ran; (2) the main-checkout bed made inert (_probe(bed.subject.parent) -> _probe(SUBJECT.parent)) -> both rows RED at the announce refusal, naming this worktree as the tree actually measured; (2b) the same for the linked-worktree bed -> RED the same way; (3) a required row renamed out of collection -> unittest reports `Ran 6 tests ... OK` and the gate exits 2 naming the absent row; (4a) AEON_ROOT removed from the Python table -> cross-twin row RED; (4b) the JS table REORDERED -> RED naming the first differing element. All restored with `git checkout HEAD --` from a committed baseline and re-run green. The worktree bed's loud-skip path is reachable via AURORA_PY_STEP3_REPO_FOR_TEST and was exercised (=/tmp -> `git worktree add` exit 128, skip printed). NOT DESCRIBED AS ANY OTHER LANE'S BUG, anywhere in the code, the comments, the ROADMAP or here: the sibling defect was resolving git's relative output against the process cwd rather than against the directory git ran in; dropping the flag is a DIFFERENT MECHANISM producing the same class of observable, and the file's header states the fourth reading of a green-under-plant that the distinction exists for. SUITE: 454 files passed / 2 skipped (456), 6275 passed / 8 skipped (6283), zero failures, seven gates exit 0, typecheck clean. THIS PARCEL ADDS NO VITEST ROWS - its seven are counted by the gate, which prints them - so against the controller's main-checkout baseline of 454/2 and 6276/7 the TOTAL IS IDENTICAL (6283) and the one row that moved passed->skipped is row 108's, which skips in a linked worktree and says so. LEFT OPEN: the real-main-checkout row skips here for the same reason its JavaScript counterpart does, so the production configuration is measured on a repository the rows build and NOT YET where the code lives - closed by one run of `npm test` from the main checkout, which the controller can do at landing. Python has no SUITE_PEERS roster of its own and did not gain one: the cross-twin row takes its domain from the JS side, so a peer added there is compared automatically, and that asymmetry is stated in the row rather than left to be discovered. No emulator was touched and nothing here wants runtime confirmation."}
{"at": "2026-09-02T11:07:49Z", "headline": "The new checker could report all-clear while the thing it was checking had already been changed on disk, and it can no longer do that", "matters": "The checks added yesterday for the second half of our project-finding code turned out to have a flaw, not in the checks themselves but in the thing that runs them. To save time, the language they are written in keeps a pre-digested copy of each file and only re-digests it when the file's timestamp or its length changes. So an edit that keeps both — and swapping two words in a list keeps both — leaves the old pre-digested copy in place, and the checks then run against yesterday's version of the file while the changed version sits on disk. The reviewer hit this on his very first attempt at reproducing one of my deliberate breakages: the run came back all-clear, he read it as a fault in my check, and only found the real cause by throwing away the pre-digested copies. My own breakages had happened to avoid the collision, so their results still stand, but the method could have fooled the next person. The runner now hands the language a brand-new empty scratch area for those copies on every run, so nothing left over from any earlier run can be reached, whoever ran what beforehand. Two neater-sounding options were considered and rejected because they only stop new copies being made and do nothing about old ones already lying around, which is exactly the situation this arises in. Proving the fix turned up a second, smaller version of the same fault in my own work: with the fix taken out, the run not only went all-clear over a changed file, it went on printing a line claiming it had read the file fresh. That line was a statement of intent that nothing checked. The runner now counts what the language actually wrote into the scratch area and refuses outright, rather than reporting anything at all, if the count is zero. Both halves were demonstrated by deliberate breakage: with the fix in, the run goes red and names the difference; with it taken out, the identical situation goes all-clear on the version that shipped, and now refuses loudly on the current one.", "detail": "O75 follow-up, three commits on fix/o75-python-resolver-tests, branch fast-forwarded onto master 99b49066 (the O75 merge) first. RAISED BY THE COORDINATOR AT LANDING, and his verification of the parcel proper is recorded there: in the main checkout all seven rows ran with 0 skipped, the real-main-checkout row ASSERTING rather than skipping, and the flag plant went three-red including AssertionError: '.' != '/home/volence/sonic_hacks' from the real checkout while the linked-worktree row stayed green. THE HOLE: CPython invalidates a cached .pyc on the source's (mtime, size). A mutation preserving BOTH is invisible to it, so the run executes stale bytecode while the changed file sits on disk. This parcel's own alias plants (4 and 4b) are exactly that shape - short edits to a tuple, run seconds apart - and the coordinator hit the FALSE GREEN on his first attempt at the reorder plant, read it as a defect in the cross-twin row, and found the cause only by clearing the cache. My plants happened not to collide, so their results stand; the method could have fooled the next person, which is the thing this parcel exists to end. MEASURED AS A CLEAN A/B ON IDENTICAL DISK STATE, cache first populated the way a human running the rows by hand populates it (`python3 -m unittest discover ...`, which writes scratchpad/lib/__pycache__), then scripts/collide: same-size aeon alias reorder ((\"LIVE_AEON\", \"AEON_ROOT\") -> (\"AEON_ROOT\", \"LIVE_AEON\"), 58 bytes both) plus os.utime back to the original mtime_ns, both re-read from stat and printed as same=True. SHIPPED PRE-FIX GATE, checked out from merge 99b49066: exit 0, `Ran 7 tests ... OK (skipped=1)`, and its own output printed py=['AEON_DIR', 'AURORA_AEON_REPO', 'LIVE_AEON', 'AEON_ROOT'] as fact while grep of the file on disk said (\"AEON_ROOT\", \"LIVE_AEON\"). FIXED GATE, same tree, same cache: exit 1, `Lists differ: [... 'AEON_ROOT', 'LIVE_AEON'] != [... 'LIVE_AEON', 'AEON_ROOT'] / First differing element 2`. THE FIX: PYTHONPYCACHEPREFIX pointed at a fresh mkdtemp the gate creates and removes, passed in the child's env so it also governs the processes the rows spawn (the copies of the subject in each bed). NOT -B and NOT PYTHONDONTWRITEBYTECODE, which stop a cache being WRITTEN and do nothing about READING one that already exists - they would leave the hazard armed for everyone who has ever run the rows by hand, which is how the stale cache gets there. Removal registered on `exit` rather than after the run, because die() calls process.exit and a gate that tidies only its happy path litters exactly the runs someone is debugging. A SECOND DEFECT, MINE, FOUND BY PLANTING THE REMOVAL OF THAT FIX: the run went green over the mutation AND WENT ON PRINTING \"compiled from source - PYTHONPYCACHEPREFIX was a fresh empty ...\", because the sentence was prose about an intention and nothing read anything back. A gate that names the hazard it closed while not closing it is worse than one that says nothing. So the gate now COUNTS the bytecode files Python actually wrote under the prefix (85 on this machine) - the child's own evidence that it compiled there and could reach no earlier cache - and an empty prefix is exit 2, not a caveat. PYTHONDONTWRITEBYTECODE is deleted from the child's env for the same reason: inherited, it would suppress the writes this check reads and turn a real measurement into a false alarm. Third commit moves that refusal AFTER the run's output is printed, because when it fires the rows have usually just printed a confident OK and the two lines together are the whole lesson; refusing first hid the green that is the reason to refuse and read as a crash. RETROACTIVE, since the method changed twice mid-proof: the collision was re-run from the final committed baseline both ways - fix in place -> exit 1 naming the differing element; fix removed -> exit 2, `COULD NOT MEASURE - nothing was compiled under the per-run bytecode prefix ...`, with `Ran 7 tests ... OK (skipped=1)` still visible above it. All restores were `git checkout HEAD --` or `git checkout 99b49066 --` from committed baselines. NOT DONE, and it is the coordinator's call already made: the cross-twin row's docstring gains no note about same-size same-mtime edits, because after this fix the row's plain claim - it fails when either side gains, loses or reorders an alias - is simply true. SUITE: npm test rc=0, 454 files passed / 2 skipped (456), 6275 passed / 8 skipped (6283), seven gates exit 0, typecheck clean; unchanged from the parcel's own landing figures, since this touches only the runner. No emulator was touched."}
{"at":"2026-09-02T11:40:26Z","headline":"A hundred and four of our testing tools were asking where their own copy of the project sits when what they needed to know was where the built, runnable copy sits — the same answer at the main desk, a different one in every side workspace","matters":"When one of us works on a change, we do it in a side workspace: a second view of the project, sharing its history but holding only the text files. The heavy machinery — the installed parts the program needs to start, and the assembled program itself — lives only at the main copy, because it would be wasteful to duplicate. Our testing tools have to launch the real program to watch it work, so each of them has to answer a question: where is the assembled program? For years they answered it by asking a different question — \"which copy of the project am I standing in?\" — and then assuming the machinery sat inside it. At the main desk that assumption is right, which is why nobody ever saw the mistake; in a side workspace it points at a program that is not there, and the tool dies with a message about a connection that never appeared, which sounds like a timing problem and is not one. Two days ago we gave the two questions two separate names and converted exactly one tool. This finishes it: a hundred and four tools now ask for the built copy by its own name, and say out loud at the top of every run which copy they measured and whether they had to borrow it from somewhere else — because a number measured against somebody else's build and reported as yours is the failure underneath all of this. Three things are worth saying plainly. First, the count we started from was wrong in both directions, and the way it was wrong is the lesson: eight of the tools could not be seen by the search that defined the job at all — seven work out where they are by a private method that never mentions the shared one, and one sits in a folder the search did not reach. A search that finds nothing and a world with nothing in it produce the same silence. All eight were found afterwards, by the automatic check, not by any search. Second, that automatic check is the part that actually stops this coming back: it now refuses any line that builds a path to the machinery out of the answer to the wrong question, and it runs with every test run, so the next person who writes it the old way is told at once, by line. Third, and this is the honest limit: at the main desk this whole change alters nothing, because both answers name the same folder there. So the tests passing is a check that nothing broke, and it is not evidence that the fix works. The evidence had to come from a real side workspace, and it did: one of the converted tools, run from the workspace this was written in, correctly reported that it was borrowing the main desk's build three folders up, where the old form pointed at two files that are not there.","detail":"O72, five commits on fix/o72-built-tree-consumers, cut from master 0bc48156. POPULATION RE-DERIVED, and it disagrees with the brief in both directions. scratchpad/*.mjs at depth 1: 163 files (the brief said 173). Importing AURORA_DIR: 122. Composing a build path off it IN CODE: 104 — one already converted (mapviewport-baseline-harness), so 103 to do, which matches the brief's 107 only after subtracting the 4 files whose only \"dist\" is prose (distance, distinct, distM); a loose grep counted those. The remaining 18 of the 122 are question 1 and untouched. PLUS EIGHT THE SURVEY COULD NOT SEE: seven derive the checkout from import.meta.url and never name the resolver, so a population defined as \"mentions AURORA_DIR\" excluded them by construction; and scratchpad/handover/handover-band-harness.mjs is one directory below the *.mjs glob every count in the brief was taken over. All eight found by the new gate rule AFTER the conversion. Total migrated: 104. THE HELPER: scratchpad/lib/run-root.mjs gains electronBin(root), distMain(root), runTarget(here) -> {root,here,borrowed,source,electron,main} and announceRunRoot(run), which writes describeRunRoot to stderr and returns the value. The two artifact paths are now two constants that isRunnableTree also uses, so the predicate that approves a tree and the paths a harness spawns cannot drift. ELECTRON_BIN is read there and only there — 61 files read it before — and it stays out of the resolver's OWNED_ENV on purpose: it names an executable, not a suite checkout. WHAT ROOT STILL MEANS: unchanged. Every migrated file keeps const ROOT = AURORA_DIR for src/ reads, scratchpad/shots-* writes and test/fixtures/ opens — about 135 sites. Only the build paths moved. Rebinding ROOT to the run target (the exemplar's shape) would have sent shot output into the borrowed tree. THE 21 THAT NEEDED A DECISION: six private walks (bg-dangling-ref, bg-tile-picker, s1-nonlevel-families, s1-sonic-preview, composer-priority, chunk-links) climbed for the BINARY only and so could approve a tree with no dist/ and fail one step later; four composed inline at the spawn with no ELECTRON binding; seven derive ROOT from import.meta.url; three import ROOT from canvas-cdp-harness and printed a provenance line naming the wrong tree, one of them reading join(ROOT,'dist','renderer','assets') for real (canvas-cdp now exports MAIN and RUN); one is the handover file. Two vacuous \"if (!ELECTRON) throw\" guards removed: the old [...].find(existsSync) can be undefined, RUN.electron never is. THE ONE NOT MIGRATED: ozone-x11-proof.mjs drives a synthetic probe app, not Aurora, so a tree with electron and no dist/ is good enough and requiring a build would make it refuse to run on an unbuilt clone. It takes electronBin and keeps its createRequire fallback; a row asserts that, so the exclusion cannot rot into a file resolving nothing. THE GATE: check-peer-path-literals rule 4, checkout-as-build-tree. Rules 1-3 are structurally blind to this class — no home-directory literal, no session scratchpad, no process.env read; the name, the import and the module are all right and only the question is wrong. It is the one FILE-SCOPED rule there, and that is forced: the composition goes through a local alias, so a line-local grep for AURORA_DIR beside an artifact returns ZERO and reads as an empty world. Four alias seeds — the identifier; anything transitively bound to it (a fixpoint, or the second hop reports a clean file); anything bound to an expression over import.meta.url, which is AURORA_DIR hand-rolled and is how seven instruments hid; and by spelling, a binding imported as ROOT, which is how three more did. Five canary lines, two of which must NOT fire: join(ROOT,'scratchpad/shots') is the checkout answering its own question, and ROOT + '/distance-table.json' is why the dist match carries a not-a-word guard. Canary lines are interpolated rather than typed, as rules 2 and 3 already are, so the gate does not fail on its own source — it did, on the first attempt, and exempting the file was the wrong fix because it would have switched rule 4 off for the parts of the gate that legitimately bind const ROOT = AURORA_DIR. RED-FIRST, THREE PLANTS, ONE PER SEED SHAPE, EACH SHOWN ON DISK BEFORE ITS RED RUN: ${ROOT}/dist/main/index.mjs in loop-paint-harness (AURORA_DIR alias), ${ROOT}/node_modules/.bin/electron in priority-lens-harness (import.meta.url-derived ROOT), join(ROOT,'dist','renderer','assets') in art-agent-harness (imported ROOT). Gate exit 1, all three lines named with file and line. Restored with git checkout -- from the committed baseline, re-run exit 0. SIX VITEST ROWS in test/support/run-root.test.ts, all with the two trees pointed apart: the helpers compose off the tree they are GIVEN (checked against a checkout that is a different directory, so a helper reaching for AURORA_DIR cannot pass); ELECTRON_BIN still wins and does not leak into the dist half; THE WORKTREE SHAPE — an unbuilt directory nested under a built one, asserting BOTH that the pre-migration composition names files that are not there and that runTarget names files that are; the announcement actually leaves the process, on stderr and not into stdout; the launcher population is wired, as a set difference rather than a count; and the ozone exception takes electronBin. run() moved from execFileSync to spawnSync for the announcement row — execFileSync surfaces stderr only when the child fails, so that row could not have failed for the right reason. ELECTRON_BIN joined the child-env scrub list. TWO PREDICATE FAILURES RECORDED IN THE FILES THEY BIT, because both are the same shape as the parcel: the first population row asked \"names an artifact path in code\", which collapses to 11 files AFTER the migration because a migrated harness never spells either path again — a predicate the fix itself invalidates measures the fix, not the property; and its comment stripper was a naive block-comment regex, which found an opening /* inside a // line mentioning src/main/aether/*.ts and deleted that file's run-root import, so the row named the one migrated harness it could not see as the one that had not been migrated. LIVE EVIDENCE FROM A REAL LINKED WORKTREE (this branch's own, which has neither artifact): node scratchpad/band-preset-harness.mjs printed \"root: /home/volence/sonic_hacks/aurora BORROWED - this script lives in .../agent-a156100978926c3ae, which has no built app\" and \"walked up 3 level(s)\", then stopped at its own unrelated AEON_DIR refusal. In the same directory the pre-migration composition resolves to two paths existsSync reports false. SUITE: 454 files passed / 2 skipped (456), 6281 passed / 8 skipped (6289), zero failures, seven gates exit 0, typecheck clean. Against the controller's main-checkout baseline of 454/2 and 6276/7 that is +6 passed — exactly the six rows added — and +1 skipped, which is row 108's step-3 row skipping because this run stands in a linked worktree, as it says. GREEN HERE IS A REGRESSION CHECK AND NOT EVIDENCE FOR THE MIGRATION: in the main checkout both names resolve to the same directory, so every harness behaves identically either way. LEFT OPEN: rule 4 does not read .py/.sh (no Python instrument spawns the app today; if one ever does, the row belongs in check-python-resolver.mjs), and the imported-alias seed is a convention keyed on the spelling ROOT — the one seed that could produce a false positive if a module ever exported a run target under that name. cwd: ROOT was left alone at the spawns: it is a valid directory either way and changing it is a behaviour change outside this parcel. No harness was run to completion (none needs to be for this property, and runtime confirmation is the controller's to schedule)."}
{"at":"2026-09-02T11:44:12Z","headline":"A correction to the entry I wrote twenty minutes ago: the number of tools I changed is a hundred and fourteen, not a hundred and four, and I got it wrong the same way the brief did","matters":"The entry above this one says a hundred and four. That was the count as it stood before I had finished — it does not include ten tools I found and fixed later in the same run, and it is off by one more in a way I only saw when I counted the finished work instead of my own notes. This matters more than the arithmetic. The whole point of the entry above is that a search which cannot reach something reports the same silence as a world where that thing does not exist — and the number I published was itself an example: it was my own earlier note, carried forward, not a fresh count of what had actually changed. Nobody checked it because there was nothing to check it against. So: a hundred and fourteen tools changed. A hundred and eleven of them now ask the shared helper directly; the other three get the answer passed along by a tool they already lean on. This entry is added rather than the earlier one edited, because the diary is written to and not rewritten — a record that quietly corrects itself is a record you cannot trust about anything else either.","detail":"O72 correction, same branch fix/o72-built-tree-consumers. THE ARITHMETIC, re-derived from the finished branch rather than from my own running notes: `git diff --name-only master..HEAD -- scratchpad` minus `lib/run-root.mjs` = 114 instruments changed; of those 111 contain `lib/run-root.mjs` and 3 (art-agent, collision-agent, collision-gesture) take `MAIN`/`RUN` re-exported from canvas-cdp-harness. Adding mapviewport-baseline-harness, which was converted before O72, the module has 112 importers, 110 of which call `runTarget`; ozone-x11-proof and mapviewport call `resolveRunRoot` for reasons each states in place. WHERE 104 CAME FROM: 103 (the depth-1 AURORA_DIR surface, minus the one already converted) + 1 (the handover file). It was written before rule 4 had been widened and run, and so predates the seven import.meta.url-derived instruments and the three that import ROOT — exactly the eleven the entry above is ABOUT. The count and the lesson were in the same entry and the count had not learned the lesson. CORRECTED IN PLACE where the document is a living one: docs/ROADMAP.md row 111, scratchpad/lib/run-root.mjs's docblock (which now carries the full 163/122/104/103 + 1 + 7 + 3 derivation), test/support/sibling-root.mjs and scripts/check-peer-path-literals.mjs's header. NOT corrected in place in the ledger, which is append-only; this entry is the correction. Nothing else in the entry above changes: the suite figures, the red-first proof, the worktree evidence and the left-open items all stand as written."}
