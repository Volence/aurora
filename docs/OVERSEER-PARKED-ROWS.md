# Aurora rows parked out of `lane-status.json`

**2026-09-06.** The board is for rows a fresh session could START or that wait on the owner
(`contract/LANE_STATUS.md` rule 7). These are neither. **Row text is verbatim as it stood on the
board**, so nothing is lost in the move and a successor can put any of them back unchanged.

## The instrument rows, behind LENS-PASS

Ruled 2026-09-06 by the hub: no bulk re-run, and a lens finding that rests on a harness re-runs
that harness in the same parcel and reports its state beside the finding. **One pointer row on the
board names this file; these five are the contents.**

- **`PICKER-HARNESS-SHARED-SECTION`** (size S, blockedBy nothing) — The sibling picker instrument asserts three sections share one preset record; aeon has had none since 09-05, measured not relayed. It has no fixture machinery, so repairing it is a shape decision, not an edit.
- **`HARNESS-STALE-VACUITY-ROWS`** (size S, blockedBy nothing) — Seven rows across three collision harnesses are red on stale vacuity premises and were red for the previous parcel too, unseen because it ran only its own harness. Ruled out as aeon drift by counting bytes at both revisions.
- **`HARNESS-PORT-DEFAULT`** (size XS, blockedBy nothing) — Every harness defaults to the same debug port, so two agents running at once collide and the second exits on a target it did not start. A port derived from the process id would end it.
- **`BGANIM-HARNESS-REPAIR`** (size S, blockedBy nothing) — The blind band harness opens panel sections by two titles the vocabulary sweep renamed. Repairing it makes dozens of rows run for the first time in weeks against strings that also moved, so it is a parcel, not a side effect.
- **`HARNESS-NEVER-RERUN`** (size L, blockedBy LENS-PASS opening) — 165 harness scripts exist and not one runs in the test suite. The hub ruled it becomes LENS-PASS's first rule: a lens finding resting on a harness re-runs it and reports its state beside the finding.

## Closed or answered today, kept as a record

- **`BAND-ORDER-REORDER`** — CLOSED by measurement: aeon's reorder is emission-only and never returns to us, and we read the override rather than the emission. The one caveat is recorded: never learn band order from the generated file.
- **`OBJECT-VOCAB-ASK`** — DELIVERED: the ask is committed as a review packet. Identity is a string on one side and a number on the other; a spawn's coordinate space must be explicit, because a world position is a valid local one and fails silently.
- **`REFUSAL-BYTES-NAME-SHAPE`** — Subtler than booked: adding a band can change the SHAPE as well as the size, so the after figure can move the opposite way from what adding implies, and the sentence never says a shape moved.
- **`DEAD-SIZE-ARM`** — A size result's failure arm is unreachable from any document now, leaving two dead panel branches. The type is deliberate safe-direction machinery, so collapsing it is a parcel and not a tidy-up.

## What is NOT here

Nothing blocked on the owner or on a peer was moved: those stay on the board, which is where he
reads them. Nothing was deleted, condensed or reworded on the way out.

