{
  "_generated_by": "GENERATED by tools/effects_gen.py. DO NOT EDIT; run `effects_gen.py emit`.",
  "channels": {
    "0": {
      "hi": 220,
      "lines": 218,
      "lo": 3,
      "source": "games/sonic4/data/effects/ojz_effects.emp#OJZ_TC_PROG"
    },
    "1": {
      "hi": 223,
      "lines": 2,
      "lo": 222,
      "source": "games/sonic4/data/effects/ojz_effects.emp#OJZ_TC_PROG"
    },
    "2": {
      "hi": 160,
      "lines": 158,
      "lo": 3,
      "source": "games/sonic4/data/effects/ojz_effects.emp#OJZ_WORLD_WATER_PROG"
    },
    "3": {
      "hi": 223,
      "lines": 62,
      "lo": 162,
      "source": "games/sonic4/data/effects/ojz_effects.emp#OJZ_WORLD_WATER_PROG"
    }
  },
  "edges": {
    "hi": {
      "behaviour": "drop",
      "engine": "engine/effects/raster.emp:1980",
      "note": "Past hi the record is NOT EMITTED this frame: no boundary is drawn anywhere and the band vanishes until the latched line re-enters [lo, hi]. It does NOT pin to hi."
    },
    "lo": {
      "behaviour": "clamp_up",
      "engine": "engine/effects/raster.emp:1983",
      "note": "Below lo the record IS still emitted, clamped UP to lo, so the boundary pins at the top of the band and stays visible. The frame-top ship covers what is above it."
    }
  },
  "game": "sonic4",
  "how_to_use": "A sweep on channel c is a CERTAIN REFUSAL when its PEAK-TO-PEAK TRAVEL (2 * (256 >> amp_shift), whole pixels) EXCEEDS channels[c].lines. It is travel, not peak excursion: the engine's own ladder ensure (engine/effects/raster.emp:397) compares 2 * (SINE_AMPLITUDE >> shift) against the screen -- an earlier cut of THIS SENTENCE had the excursion and was wrong by 2x IN THE PERMISSIVE DIRECTION, which is why the formula is stated here to be parsed and never remembered. `lines` is an INCLUSIVE COUNT of lines in [lo, hi], and travel is a DISTANCE. THE TEST IS ONE-DIRECTIONAL: travel > lines is a CERTAIN refusal and worth warning on; travel <= lines is CANNOT TELL, never a clearance -- the latched line is (anchor - Camera_Y), so where the sweep sits inside [lo, hi] is camera-dependent and unknowable at author time. There is deliberately NO SENTENCE HERE STATING WHEN A SWEEP FITS: the widest-that-fits phrasing this string used to open with was implementable as a green clearance, and a consumer reading only the first sentence would ship one. Leaving the band is NOT symmetric: past `hi` the record is NOT EMITTED and the band vanishes; below `lo` it is EMITTED CLAMPED UP and stays visible. Read `edges` below before writing the warning.",
  "schema": "aeon-effects-channel-bands/1",
  "units": "SCREEN LINES, 1:1 with the authored patchable(lo:, hi:). Not fire lines: the engine subtracts 1 once, in Raster_BuildSchedule. Do not convert."
}
