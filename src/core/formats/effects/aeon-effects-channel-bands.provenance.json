{
  "$what": "Provenance of aeon-effects-channel-bands.json, aeon's GENERATED declaration of the screen band each raster patch channel is confined to (`patchable(lo:, hi:)`), vendored into Aurora at a named aeon revision. src/core/formats/effects/channel-bands.ts imports it; src/renderer/providers/effects-preset.ts turns it into the ONE sentence an author sees when a sweep's peak-to-peak travel cannot fit its channel's band.",
  "$why_vendored_into_src_and_not_test_fixtures": "It is not a test fixture. This file is READ AT RUNTIME by the editor, the warning under the `Travel` select is computed from it, so it must sit where the renderer bundle can import it, beside the other two contract documents this repo vendors (aurora-effects-preset.schema.json, aurora-effects-scene.schema.json) and for the same reason. Its drift gate is therefore the SCHEMA's shape (test/formats/effects-channel-bands-drift.test.ts, modelled on effects-preset-schema-drift.test.ts) and not test/formats/aeon-fixture-currency.test.ts, whose table is scoped to test/fixtures.",
  "$why_a_sidecar_at_all": "Bar 19. A test that reads a peer's WORKING TREE has its colour decided by that peer's uncommitted edits, see docs/reviews/2026-08-28-golden-live-tree.md. The bytes are copied here at a COMMITTED revision, the peer's own git blob id is recorded so the pin is CHECKABLE rather than merely copied, and the separate question 'is that pin still what aeon ships' gets its own check that reads aeon at a committed revision, names it, fails on drift, and skips loudly when it cannot run.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "path": "games/sonic4/data/generated/effects_channel_bands.json",
    "revision": "2be6020ae6340e734d2e2c419fe0de50408b226b",
    "revision_subject": "contract(channel-bands): state the fit test ONLY in the refusal direction",
    "blob": "170556fca0701d8255c51ea7f014136442cc7b98",
    "last_changed_by": "b8913cda7ad752cc311556735e070a9fb25d9fef, the revision pinned above IS the commit that changed the file. THE CHANGE IS EXACTLY ONE LEAF, `how_to_use`, re-stated in the REFUSAL DIRECTION: the string used to OPEN `A sweep on channel c fits when its PEAK-TO-PEAK TRAVEL (…) is <= channels[c].lines`, a clearance-shaped sentence, while a later sentence in the SAME string said `travel <= lines is CANNOT TELL, never a clearance`. Two consumers could implement the document accurately and build opposite warnings. It now opens `A sweep on channel c is a CERTAIN REFUSAL when its PEAK-TO-PEAK TRAVEL (…) EXCEEDS channels[c].lines`, and aeon added an explicit note that there is deliberately NO SENTENCE STATING WHEN A SWEEP FITS. A PARSED LEAF DIFF against the previous pin, run here rather than taken on anyone's word, found 1 changed leaf of 27: every `lo`/`hi`/`lines`/`source`, all six `edges` leaves, `units`, `schema`, `game` and `_generated_by` compare BYTE-IDENTICAL. The multiplier and base the parser reads out of the sentence are unchanged (2 and 256), which is why the direction of Aurora's comparison did not move and did not need to.",
    "resolved_by": "git -C <AEON_DIR> show b8913cda:games/sonic4/data/generated/effects_channel_bands.json, read through git OBJECTS at a NAMED revision, never through the sibling working tree, which on this machine is a live lane checkout. The blob id above was read back two independent ways and they agree: `git -C <AEON_DIR> rev-parse b8913cda:<path>` in aeon, and `git hash-object` on the bytes that landed here. sha256 of the extracted bytes on disk: d7f4d40a63947f403cd3b6e706003938b89229b7c99861991c362fa8acffea76. AEON_DIR was resolved through test/support/sibling-root.mjs and TYPED in an interactive shell command; no absolute peer path entered a committed file.",
    "revision_published": "b8913cda IS aeon origin/master's tip at the moment of vendoring, not merely an ancestor of it, checked with `git rev-parse origin/master`; and origin/master:<path> resolves to the SAME blob 170556fc. Both facts are re-asserted on every run by test/formats/effects-channel-bands-drift.test.ts rather than left as prose here.",
    "branch_that_answers_currency": "origin/master"
  },
  "vendored": {
    "path": "src/core/formats/effects/aeon-effects-channel-bands.json",
    "bytes": 2717,
    "git_blob": "170556fca0701d8255c51ea7f014136442cc7b98",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object aeon stores, and test/formats/effects-channel-bands-drift.test.ts recomputes it from these bytes on every run"
  },
  "what_this_document_is_authoritative_FOR_and_what_it_is_not": {
    "authoritative_for": "The per-channel band (`lo`, `hi`, `lines`) and the ASYMMETRIC edge behaviour, which no other artifact in the suite states in machine-readable form.",
    "one_directional_only": "`how_to_use` is explicit IN BOTH ITS SENTENCES as of aeon b8913cda, and the code obeys it. The fit test is stated ONLY in the refusal direction, `A sweep on channel c is a CERTAIN REFUSAL when its PEAK-TO-PEAK TRAVEL (2 * (256 >> amp_shift), whole pixels) EXCEEDS channels[c].lines`, and the document says outright that it deliberately contains NO SENTENCE STATING WHEN A SWEEP FITS. travel <= lines is CANNOT TELL and NEVER a clearance, because the latched line is (anchor - Camera_Y) and where the sweep sits inside [lo, hi] is camera-dependent and unknowable at author time. ⚠ THE PHRASING MATTERS AND IS NOT COSMETIC: until b8913cda this string OPENED with a clearance-shaped `fits when … is <=` while closing with `never a clearance`, so a consumer reading only the first sentence would ship the green verdict the document forbids. Aurora never did, `AnchorBandFit` in channel-bands.ts has no `fits` member AT THE TYPE LEVEL, so a future edit cannot paint one by accident, and the comparison direction is written exactly once, as `travelPx > band.lines`, rather than inferred from this prose. The parser reads ONLY the multiplier and the base out of the sentence (2 and 256), which is why aeon's rewording moved no logic here.",
    "units_are_not_converted": "`units` says SCREEN LINES, 1:1 with the authored patchable(lo:, hi:), and 'Do not convert', the engine's single -1 is already applied in Raster_BuildSchedule. Nothing on Aurora's path adds or removes a 1; the module asserts that sentence is still in the file at load.",
    "game_scoped": "`game` is `sonic4`. The document declares bands for THAT game's raster module. As of this pin ALL FOUR channels are declared (RASTER_MAX_PATCH is 4), so the `no-band` arm, and with it `anchorSweepNoBandAdvisory`, is UNREACHABLE for sonic4 through the vendored map. The arm is kept because it is a property of the DATA and not of the code: another game, or aeon retiring a patchable(), brings it straight back. See pin_history_current_last."
  },
  "re_vendor": "git -C <AEON_DIR> fetch origin && git -C <AEON_DIR> show origin/master:games/sonic4/data/generated/effects_channel_bands.json > src/core/formats/effects/aeon-effects-channel-bands.json, then update `aeon.revision`, `aeon.revision_subject`, `aeon.blob`, `vendored.bytes`, `vendored.git_blob` and `pin_history_current_last` here, and re-run the suite: channel-bands.ts's module-load guards will tell you if a load-bearing sentence moved, rather than quietly warning on a rule the contract no longer holds.",
  "pin_history_current_last": [
    "a00e3bb0, 776a3ea1, landing (EW-TIMELINE-CLOCK, 2026-09-04). The file's first appearance in Aurora. Blob byte-identical at aeon origin/master 52be83a9 at the moment of vendoring.",
    "d6f8bf63, 99981671, re-vendored 2026-09-04. Line-number drift only from aeon's reels step 4b; no band value changed. Re-vendored rather than pinned back because the currency row's job is to track aeon's tip, and a stale pin held for cosmetic drift teaches the next reader to ignore the row.",
    "e3a22ece, 8343c8b, re-vendored 2026-09-04. A PARSED LEAF DIFF against the previous pin, run here rather than taken on a peer's word, found EXACTLY TWO changed leaves, both `source`: ojz_effects.emp:1563 -> :1739 and :1565 -> :1741. Nothing structural moved, no key added or removed, no type changed, every `lo`/`hi`/`lines` value identical, so the ASYMMETRIC edge behaviour this file is authoritative for is untouched. The line numbers moved because ojz_effects.emp grew above those declarations (EFFECTS-W1 F2/T3), not because a band did. ⚠ Re-vendored alongside two OTHER reds from the SAME aeon push that are deliberately NOT re-vendored: F2/T3 changed `base_swap` from an object to a LIST of per-plane bands carrying new `plane` and `restore_line` keys, which this repo's CLOSED preset schema refuses. That is a migration awaiting an amended schema from aeon, not a pin bump, and bumping its pin would have made a test load a document Aurora's own codec rejects.",
    "a762d207, ddd8881a, re-vendored 2026-09-05 (parcel/sec7-scene). A PARSED LEAF DIFF against the previous pin, run here rather than taken on the dispatcher's word, found EXACTLY 10 changed leaves of 27: `channels.0.source` 1739->1809, `channels.1.source` 1741->1811, and the 8 leaves of the two ADDED channels, 2 = {lo 3, hi 160, lines 158, ojz_effects.emp:1995} and 3 = {lo 162, hi 223, lines 62, ojz_effects.emp:1998}. Every one of `how_to_use`, `units`, `schema`, `game`, `_generated_by` and all six `edges` leaves compares BYTE-IDENTICAL, so no derived-sentence logic in channel-bands.ts or effects-preset.ts needed to move, and it did not. ⚠ TWO CONSEQUENCES WORTH KNOWING. (a) The refusal is now REACHABLE ON TWO CHANNELS, not one: channel 3 is 62 lines, so amp_shift 2 (128 px) and amp_shift 3 (64 px) are CERTAIN refusals there; channel 2 is 158 lines and channel 0 is 218, both wider than the ladder's widest 128 px rung, so neither can ever refuse. (b) `anchorSweepNoBandAdvisory` is now DEAD FOR sonic4, with 0..3 all declared and EFFECTS_PRESET_MAX_PATCH == 4 no channel can return `no-band`. Its tests construct the state directly and stay meaningful; the helper is deliberately NOT deleted.",
    "170556fc, b8913cda, re-vendored 2026-09-05 (parcel/chband-step3). STEP 3 OF 3 OF `CHBAND-PROSE-REPIN`, an expand-then-contract across two repos. A PARSED LEAF DIFF against the previous pin found EXACTLY 1 changed leaf of 27: `how_to_use`, and nothing else, every band value, every `source` line number, all six `edges` leaves, `units`, `schema`, `game` and `_generated_by` are BYTE-IDENTICAL. aeon restated the fit test in the REFUSAL direction only (`is <= channels[c].lines` -> `EXCEEDS channels[c].lines`) because the old opening sentence was clearance-shaped and contradicted its own later `never a clearance` clause. ⚠ THE POINT OF THE THREE STEPS: every miss on the travel-formula parser in channel-bands.ts is `fail()`, which THROWS AT MODULE LOAD, so a parser knowing only one phrasing does not degrade a warning, it stops the editor from starting the instant the other document is vendored. So step 1 (Aurora bbdf1890) widened BOTH parsers to accept either tail, step 2 (aeon b8913cda) landed the new text, and this step vendors it and DELETES the transitional `is <=` arm at both sites, the app module and the independent copy in scratchpad/anchor-authoring-harness.mjs. The old phrasing is now REFUSED rather than merely unused, and test/formats/effects-channel-bands-prose-repin.test.ts asserts that refusal, which is what proves the arm is gone rather than dormant. The three interlocks channel-bands.ts guards VERBATIM (`travel > lines is a CERTAIN refusal`, `travel <= lines is CANNOT TELL, never a clearance`, and `lines` is an INCLUSIVE COUNT) all survive aeon's rewording unchanged, and were re-checked against the extracted bytes before anything was deleted."
  ],
  "what_changed_at_this_pin": {
    "semantic": "NOTHING, for the THIRD time today. 27 leaves before and after, 0 added, 0 removed, 4 changed in place, all four a channel source LINE NUMBER. Every lo, hi and lines value identical; how_to_use, units, edges, schema and game byte identical.",
    "why": "aeon is moving code inside ojz_effects.emp, so the generated source references shift. The currency row has now gone red three times today on line numbers alone and zero times on a value.",
    "proposal_raised": "Aurora proposed to aeon and the hub that the source line be excluded from the currency comparison, or dropped from the emitted document. It is provenance an author never sees, it is the only field that has ever moved, and a gate that fires only on noise trains its reader to re vendor without looking, which is exactly how a real change gets waved through."
  }
}
