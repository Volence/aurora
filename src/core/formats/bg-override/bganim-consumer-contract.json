{
  "$comment": "VENDORED, MACHINE-READABLE EXTRACT OF THE AEON CONSUMER CONTRACT — DO NOT HAND-EDIT. Every value here was read out of the aeon source cited beside it at aeon commit 1ee8f8e68d826b18023639ab32a8f7c82f238e62 (origin/master, 2026-08-22), not copied from prose. This is a DERIVATION, not a byte copy of any aeon blob: it is re-read and re-authored per amendment, and `amendments` below records each later aeon revision a value was re-derived at. src/core/formats/bg-override/bg-override.ts reads its constants FROM this file so the codec has no second copy of any number, and test/formats/bg-override-contract-drift.test.ts holds this file to a content hash. That hash proves only that OUR copy is the one we pinned. ⚠ RE-RECONCILING BY HAND WAS THE PLAN AND IT DID NOT HAPPEN: `BG_TILE_CAPACITY` stayed at 448 here after aeon took the BG arena to 400, so Aurora accepted backgrounds of 401..448 tiles that aeon's own build refused, and the stale entry's citation named a line number that had moved too. Since 2026-09-06 test/formats/bg-override-contract-currency.test.ts reads aeon's authorities at a COMMITTED revision and fails on a divergence, skipping LOUDLY when aeon is absent — the VALUES are instrumented; the line numbers in each `authorities` entry are still checked by nothing but a reader.",
  "source": {
    "repo": "aeon",
    "commit": "1ee8f8e68d826b18023639ab32a8f7c82f238e62",
    "read": "2026-08-22",
    "documents": [
      "tools/EFFECTS_CONSUMER_CONTRACT.md §1.1/§1.2 (blob 45961f8f9b96de7a17164a535387e1a4d3a79b98)",
      "tools/inject_editor_bg.py — THE CONSUMER; where it and the contract doc disagree, it wins (blob 5cef80aaac156a7627ab119c06d6d846f450ca40)",
      "tools/bg_override_io.py — the shared read/refuse/write and the inseparability argument (blob 76b97b408c79f085eadce6d3422ce214eb1e4d88)",
      "tools/test_bg_emit.py — TestBgAnimBandCeiling + TestBgAnimBandCoherence (blob dd46fdf1893798d406f253a502550aadc3969d4f)",
      "engine/level/bg_anim.emp — the runtime (blob 3954cea3d7f18b0856ea78da5965e5804d7321ed)",
      "engine/system/constants.emp — the ceiling's sizing authority (blob 41c6b577454581cb149d8ea090fa2231fe177f2b)",
      "tools/vram_map.py — generated from games/sonic4/vram.toml (blob afd8a47ed0fc904268f67b09e4b8d419d3312a62)",
      "games/sonic4/vram.toml — THE VRAM PLACEMENT AUTHORITY, added to this list by the 2026-09-06 capacity re-vendor. tools/vram_map.py, engine/system/constants.emp's GENERATED block and docs/generated/vram-map-sonic4.md are all generated from it; reading vram_map.py alone is reading a copy, and the copy is what carried a stale line number here for a fortnight",
      "tools/regenerate-level.sh — the caller, which is what makes OUT_DIR project-root-relative (blob e80c9b00023d8c750ff85b1f6828ab84567fdfc7)"
    ],
    "empyrean": "docs/AURORA_EFFECTS_SCHEMA.md §5 / §5.1 / §5.2 at empyrean 8f3fbf1 — the writer-normative half and the sole-writer ruling"
  },
  "path": "games/sonic4/data/editor_bg_override.json",
  "outputDir": {
    "value": "games/sonic4/data/generated/ojz/act1",
    "read": "2026-08-26 — added at the SAME pinned commit 1ee8f8e6… and the SAME blob 5cef80aa… the rest of this file was read from, verified byte-for-byte with `git -C ../aeon show <commit>:tools/inject_editor_bg.py`. Not a re-vendoring: no other value in this file moved.",
    "why": "THE ACT BINDING, and the one fact that says WHICH act this per-GAME document governs. The consumer reads one input file and writes zone_bg.bin / bg_tiles.bin / bg_anim.emp / bg_anim_banks.bin into ONE hardcoded directory, so the document describes exactly the act whose generated data lives there — not every act in the project. Nothing inside the document names an act; this literal is the whole of the binding, which is why it is vendored here rather than inferred. Aurora matches it against project.json's per-act `stripPath` (the same project-root-relative directory) and treats a project whose acts point elsewhere as having NO overridden act — the safe direction, since the document then governs nothing Aurora is showing. SEAM, reported to aeon: the pairing is a coincidence of two hardcodes, one per repo. Closing it means aeon deriving OUT_DIR from project.json (or naming the act in the document) rather than both sides spelling the same path.",
    "authorities": [
      "aeon tools/inject_editor_bg.py — `OUT_DIR = os.path.join(os.path.dirname(__file__), '..', 'games', 'sonic4', 'data', 'generated', 'ojz', 'act1')`, and every write in `main()` is `os.path.join(OUT_DIR, ...)`: zone_bg.bin, bg_tiles.bin, bg_anim.emp, bg_anim_banks.bin, ojz_palette.bin",
      "aeon tools/regenerate-level.sh — `if [[ -f games/sonic4/data/editor_bg_override.json ]]; then python3 \"${TOOLS}/inject_editor_bg.py\"; fi`, run with the repo root as cwd, which is what makes OUT_DIR project-root-relative"
    ],
    "note": "DRIFT DISCLOSED, NOT RE-VENDORED (aurora, 2026-09-03, reading aeon 3a4712faa920100653669c1ec3fc26c2da71ef68). `OUT_DIR` and the input path are no longer hardcoded literals: aeon's `BgActNames.out_dir()` derives games/sonic4/data/generated/<zone_id>/<act_id> and `BgActNames.override_path()` derives the input file, both from project.json's zone/act INDICES (defaults zone 0 / act 0). BOTH LITERALS BELOW STILL HOLD FOR THE DEFAULT ACT — `LEGACY_OVERRIDE_ACT = ('ojz', 'act1')` keeps that one act on the un-suffixed editor_bg_override.json, and its out_dir is exactly this string — so nothing in this codec is wrong today. What changed is the KIND of fact: the pairing is no longer two hardcodes but one hardcode against one derivation, and a project whose first act is not ojz/act1 would break the match. Re-vendoring that half is its own parcel; it is recorded here rather than silently corrected because a stale citation is worse than a stale value. RE-VERIFIED 2026-09-06 at aeon f14b21a89fd755b011419a2a30822f06032a8dbe (the capacity re-vendor below): `LEGACY_OVERRIDE_ACT = ('ojz', 'act1')` at inject_editor_bg.py:473 is unchanged, `BgActNames.out_dir()` still joins `games/sonic4/data/generated/<zone_id>/<act_id>`, and aeon's root project.json still names `ojz` / `act1` as its first zone's first act — so the derived default is still exactly this literal. The SHAPE hazard this note records is unchanged and still not re-vendored."
  },

  "constants": {
    "BGANIM_MAX_BANDS": {
      "value": 4,
      "why": "The band ceiling. NOT pinned from prose: read out of all THREE deliberate authorities, which agree at 4. Raising it is a three-file engine change and never a writer decision.",
      "authorities": [
        "aeon engine/system/constants.emp:312 — `pub const BGANIM_MAX_BANDS = 4`; sizes engine/ram.emp:697 `BgAnim_LastStep: [u16; BGANIM_MAX_BANDS]`",
        "aeon engine/level/bg_anim.emp:93 — `const BGANIM_MAX_BANDS = 4`, a DELIBERATE module-local mirror (the file is lowered standalone by bg_anim_port against an empty symbol table, so it may not import engine.constants); bounds the DEBUG-only runtime assert",
        "aeon tools/inject_editor_bg.py — `BGANIM_MAX_BANDS = 4`, the emitter cap and the RELEASE defense (`assert len(anims) <= BGANIM_MAX_BANDS`)"
      ],
      "aeonGate": "aeon tools/test_bg_emit.py::TestBgAnimBandCeiling::test_all_three_authorities_agree — reads all three and fails on disagreement"
    },
    "BGANIM_PHASE_BANKS": {
      "value": 8,
      "why": "`phases` carries EXACTLY this many banks — not at most, not at least. Pre-shifted art 1px apart; the fine phase is `step & 7`.",
      "authorities": [
        "aeon engine/level/bg_anim.emp:95 — `const BGANIM_BANKS = 8` and `banks: [*u8; BGANIM_BANKS]` (line 113) in `struct bganim_band`",
        "aeon tools/inject_editor_bg.py:896 — `assert len(b['bank_offsets']) == 8, 'bganim_band.banks is [*u8; 8]: each band needs exactly 8 phases'`, and `bank_offsets` is built `for ph in range(len(a['phases']))`, so 8 phases in is the only input that satisfies it"
      ]
    },
    "BG_TILE_CAPACITY": {
      "value": 400,
      "why": "`len(tiles) <= BG_TILE_CAPACITY`. TWO NUMBERS, AND THEY ARE NOT THE SAME NUMBER. The PHYSICAL run under the sprite attribute table is $8000..$B7FF = 448 slots; the capacity is what aeon's VRAM map DECLARES the BG arena owns inside that run, and since EFFECTS-W1 item 9d that is 400 — the top 48 slots ($B200..$B7FF, 1424..1471) are the `waterline_strips` region. Conflating the two is exactly the defect this entry was re-vendored to fix: Aurora accepted 401..448 tiles and aeon's build refused them. A CAPACITY IS AN ALLOCATION, NOT A HARDWARE FACT, so it can be reassigned again; nothing here should be restated as permanent. Read the value, never the reason.",
      "authorities": [
        "aeon games/sonic4/vram.toml — THE AUTHORITY: `[[region]] name = \"bg_region\"`, `base = 1024`, `tiles = 400` (was 448), `band_reserve = 80` (was 128). tools/gen_vram_map.py generates tools/vram_map.py, engine/system/constants.emp's GENERATED block and docs/generated/vram-map-sonic4.md from it, and tools/test_gen_vram_map.py::test_generated_artifacts_are_in_sync fails the build on an un-regenerated edit",
        "aeon tools/vram_map.py:34 — `BG_TILE_CAPACITY = 400`, GENERATED from that file (the four-copies-of-448 incident is why inject_editor_bg.py imports it rather than restating it); the same block carries `BG_BAND_RESERVE = 80` and `BG_STATIC_TILE_BUDGET = 320`",
        "aeon engine/system/constants.emp:607 — `pub const BG_TILE_CAPACITY = 400`, the engine-side declaration, with `VRAM_WATERLINE_STRIPS = BG_TILE_BASE_VRAM + BG_TILE_CAPACITY * 32` immediately below it: the strips start exactly where the arena ends, which is what makes 400 the number a too-large blob sprays past",
        "aeon tools/inject_editor_bg.py:1023 — `assert len(tiles) <= BG_TILE_CAPACITY`, importing it from vram_map at line 36"
      ]
    },
    "BG_TILE_BASE_SLOT": {
      "value": 1024,
      "why": "Not enforced by this codec — recorded because it is what makes the PREFIX identity an identity: a band's DMA destination is BG_TILE_BASE_VRAM + slot_base*32 with BG_TILE_BASE_VRAM = $8000, and 1024 * 32 = $8000 IS the tile blob's own base. So slot 0 is literally tiles[0]; bands DMA OVER the static blob's leading slots, never beside them.",
      "authorities": [
        "aeon tools/vram_map.py:33 — `BG_TILE_BASE_SLOT = 1024`, GENERATED from games/sonic4/vram.toml's `bg_region` `base = 1024`; aeon engine/system/constants.emp:606 declares it as `BG_TILE_BASE_VRAM / 32`",
        "aeon tools/inject_editor_bg.py — `BG_TILE_BASE_VRAM = 0x8000`; `vram_dest = BG_TILE_BASE_VRAM + b['slot_base'] * 32`; and the layout rebase `(idx + BG_TILE_BASE_SLOT) & 0x7FF`"
      ]
    },
    "TILE_BYTES": {
      "value": 32,
      "why": "One 8x8 4bpp tile. The band's ROTATION UNIT — `rows * TILE_BYTES` on a horizontal band, `cols * TILE_BYTES` on a vertical one — is the quantity required to be a power of two.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:622 — `unit_bytes = unit_tiles * 32` inside `band_axis_geometry` (was the axis-blind `col_bytes = rows * 32` before the 2026-09-02 axis amendment; the FACTOR did not move, the key it multiplies did)",
        "aeon engine/level/bg_anim.emp:124 — `const TILE_BYTES_SHIFT = 5 // log2(32 bytes per 8x8 tile)`"
      ]
    },
    "TILE_PIXELS": {
      "value": 64,
      "why": "Each tile in `tiles` and in every `phases` bank is a flat array of exactly this many pixel values, row-major 8x8.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1046 — the pack loop `for row in range(8): for col in range(4)` indexes `t[row*8 + col*2]` and `t[row*8 + col*2 + 1]`, whose highest index is 63"
      ]
    },
    "TILE_PIXEL_MAX": {
      "value": 15,
      "why": "4bpp. The consumer MASKS rather than rejects (`hi = t[...] & 0xF`), so a 16 bakes silently as a 0 — which is why the writer refuses out-of-range instead of relying on the mask.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1048 — `hi = t[row*8 + col*2] & 0xF`",
        "empyrean docs/AURORA_EFFECTS_SCHEMA.md §5 — writer-normative: `each tile = cols*rows tiles, each tile = 64 pixel values 0..15`"
      ]
    },
    "TILE_WIDTH_PX": {
      "value": 8,
      "why": "`pattern_px` must equal `period_tiles * TILE_WIDTH_PX` — the pattern's period in pixels ALONG THE DECLARED AXIS (`cols` horizontal, `rows` vertical), from which the consumer derives `step_mask = pattern_px - 1`.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:633 — `band_axis_geometry` returns `period_tiles * 8`, asserted at line 837 as `pattern_px == period_px` (was `assert pattern_px == cols * 8` before the 2026-09-02 axis amendment; 8 did not move, the key did)"
      ]
    },
    "BG_LAYOUT_WORDS": {
      "value": 4096,
      "why": "64x64 nametable words, the post-pad length the consumer asserts.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1022 — `assert len(layout) == 4096, f'layout must be 64x32 or 64x64 words, got {len(layout)}'`; `COLS, ROWS = 64, 64`"
      ]
    },
    "BG_LAYOUT_WORDS_LEGACY": {
      "value": 2048,
      "why": "64x32. The consumer ZERO-PADS it to 4096 rather than refusing, so a legacy file is legal input. This codec accepts it and does NOT rewrite it: silently re-lengthening someone's layout is an unrequested edit, and the pad is the consumer's business.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1020 — `if len(layout) == 2048: layout = layout + [0]*2048  # legacy 32-row layout: pad to 64 rows`"
      ]
    },
    "LAYOUT_WORD_MAX": {
      "value": 65535,
      "why": "Each layout entry is packed big-endian unsigned 16-bit; anything outside 0..65535 raises inside the consumer rather than baking.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1039 — `struct.pack_into('>H', nt, (col * ROWS + row) * 2, word)`"
      ]
    },
    "LAYOUT_TILE_INDEX_MASK": {
      "value": 2047,
      "why": "The low bits of a layout word hold the BLOB-LOCAL tile index; every bit above them is the Genesis nametable's own attribute field (priority / palette line / flips), which the consumer PRESERVES verbatim. A writer that renumbers `tiles` must therefore rewrite `word & LAYOUT_TILE_INDEX_MASK` and leave `word & ~LAYOUT_TILE_INDEX_MASK` alone. THE ESCAPE MATTERS AS MUCH AS THE MASK: the consumer rebases only `if word != 0`, so a layout word of EXACTLY zero is passed through unrebased and renders VRAM tile 0 (blank) — it does NOT mean tiles[0]. A renumbering that turns a nonzero word into zero, or a zero word into nonzero, silently changes what that cell renders while every other assert in the consumer still passes.",
      "authorities": [
        "aeon tools/inject_editor_bg.py:1036 — the nametable loop: `if word != 0: idx = word & 0x7FF; word = (word & ~0x7FF) | ((idx + BG_TILE_BASE_SLOT) & 0x7FF)`"
      ]
    }
  },

  "drivers": {
    "$comment": "aeon tools/inject_editor_bg.py — `DRIVERS = {'camera_x': 0, 'camera_y': 1, 'timer': 2}`, mirrored by engine/level/bg_anim.emp's `driver` field doc (0 = Camera_X, 1 = Camera_Y, 2 = Logic_Tick low word). A driver names the SCALAR SOURCE only and NEVER an axis: `camera_y` does NOT mean vertical motion, and it is the name that reads like a vertical instruction and is not one. Which way a band moves is the `axis` key (default \"horizontal\"), added 2026-09-02 at aeon 3a4712faa920100653669c1ec3fc26c2da71ef68; before that every band moved horizontally, and this comment said so — the sentence is retired, not softened.",
    "camera_x": 0,
    "camera_y": 1,
    "timer": 2
  },

  "topLevelKeys": {
    "$comment": "Declaration order IS the canonical write order (aeon EFFECTS_CONSUMER_CONTRACT.md §1.1 table order). `ownership` is Aurora's, per the 2026-08-22 sole-writer ruling (aurora docs/reviews/2026-08-22-bg-override-ownership-ruling.md §5; empyrean AURORA_EFFECTS_SCHEMA.md §5.2).",
    "layout": {
      "required": true,
      "ownership": "own",
      "cite": "inject_editor_bg.py — `layout, tiles = data['layout'], data['tiles']`, then the row-major -> column-major transpose"
    },
    "tiles": {
      "required": true,
      "ownership": "own",
      "cite": "inject_editor_bg.py — `data['tiles']`, capacity-asserted and packed into bg_tiles.bin"
    },
    "anims": {
      "required": false,
      "ownership": "own",
      "cite": "inject_editor_bg.py — `anims = data.get('anims')`; absent or falsy -> the disabled stub (band_count = 0)",
      "emptyIsUnauthored": "aeon tools/test_bg_emit.py::TestBgAnimBandCoherence::test_live_override_file_is_coherent_if_it_has_bands asserts `assertNotIn('anims', live)` when there are no bands, with the reason 'an empty `anims` key is neither absent nor authored'. That binds the WRITER: Aurora never emits an empty array. It does NOT bind the reader — inject_editor_bg.py bakes such a document as the disabled stub without complaint, so refusing to open it would make Aurora unable to load a file its own consumer accepts. The reader therefore DROPS the key and reports it in notices; the refusal lives on the write path, which is the boundary that can enforce it. The single exception is an empty `anims` beside a legacy `anim`: the consumer's fallback is keyed on `anims is None`, so an empty array suppresses it and the band is silently dropped — there the emptiness is load-bearing and normalizing would be a guess, so the reader refuses."
    },
    "anim": {
      "required": false,
      "ownership": "legacy-read-only",
      "cite": "inject_editor_bg.py — `if anims is None and data.get('anim'): anims = [data['anim']]`",
      "writerRule": "aeon EFFECTS_CONSUMER_CONTRACT.md §1.1: 'LEGACY single-band form … Writers must not emit it (read-side compatibility only)'. This codec therefore UPGRADES it on read (anim -> anims[0]) and refuses to serialize a document still carrying it — the only behaviour that neither emits the legacy key nor destroys the band it holds."
    },
    "palette": {
      "required": false,
      "ownership": "round-trip",
      "cite": "inject_editor_bg.py — `if 'palette' in data:` -> `assert len(words) == 16` -> stamped into ojz_palette.bin",
      "notValidatedHere": "Aurora does not own this key, so it does not judge it. Refusing a file over a key whose constraint belongs to someone else (and which the drift rule lets them change) would block an author from opening a file its owner considers fine."
    },
    "palette_line": {
      "required": false,
      "ownership": "round-trip",
      "cite": "inject_editor_bg.py — `cram_line = int(data.get('palette_line', 2)) & 3`; `file_line = cram_line - 1` must be >= 0",
      "notValidatedHere": "same reason as `palette`"
    }
  },

  "bandKeys": {
    "$comment": "Each element of `anims`. aeon EFFECTS_CONSUMER_CONTRACT.md §1.2; declaration order is that table's order and is the canonical write order for a band.",
    "cols": {
      "required": true,
      "kind": "positiveInt",
      "cite": "inject_editor_bg.py — `cols, rows = a['cols'], a['rows']`; on a HORIZONTAL band `cols` is `_AXIS_PERIOD_TILES` and `pattern_px == cols*8`, while on a VERTICAL band `cols` is `_AXIS_UNIT_TILES` and `band_axis_geometry` asserts `(1 << unit_shift) == cols*32`"
    },
    "rows": {
      "required": true,
      "kind": "positiveInt",
      "cite": "inject_editor_bg.py — `cols, rows = a['cols'], a['rows']`; on a HORIZONTAL band `rows` is `_AXIS_UNIT_TILES` and `band_axis_geometry` asserts `(1 << unit_shift) == rows*32`, while on a VERTICAL band `rows` is `_AXIS_PERIOD_TILES` and `pattern_px == rows*8`"
    },
    "axis": {
      "required": false,
      "kind": "bandAxis",
      "default": "horizontal",
      "values": [
        "horizontal",
        "vertical"
      ],
      "unitKey": {
        "horizontal": "rows",
        "vertical": "cols"
      },
      "periodKey": {
        "horizontal": "cols",
        "vertical": "rows"
      },
      "cite": "inject_editor_bg.py — `axis = a.get('axis', 'horizontal')` in `band_axis_geometry`, refused by name against `BAND_AXES = ('horizontal', 'vertical')`; `unitKey`/`periodKey` are its `_AXIS_UNIT_TILES` / `_AXIS_PERIOD_TILES` tables verbatim",
      "why": "Which way the band's pattern TRANSLATES. NOT the same thing as `driver`, which names the SCALAR the step is read from and never an axis. The engine is axis-agnostic and always was — `col_shift` is log2 of the rotation UNIT in bytes and `step_mask` is the period in px minus 1 — so the vertical arm reuses the same whole-unit DMA rotate with no engine byte changed. Direction is fixed and is NOT a key: an increasing driver scrolls a horizontal band LEFT and a vertical band UP.",
      "aeonGate": "aeon tools/test_bg_emit.py::TestBgAnimMotionAxis"
    },
    "pattern_px": {
      "required": true,
      "kind": "positiveInt",
      "cite": "inject_editor_bg.py — `assert pattern_px == period_px` where `band_axis_geometry` returns `period_tiles * 8`: cols*8 on a horizontal band, rows*8 on a vertical one"
    },
    "driver": {
      "required": false,
      "kind": "driverName",
      "default": "camera_x",
      "cite": "inject_editor_bg.py — `DRIVERS[a.get('driver', 'camera_x')]`; an unlisted name is a KeyError at bake"
    },
    "rate_shift": {
      "required": false,
      "kind": "nonNegativeInt",
      "default": 2,
      "cite": "inject_editor_bg.py — `a.get('rate_shift', 2)`; engine: `step = driver_value >> rate_shift`"
    },
    "slot_base": {
      "required": false,
      "kind": "nonNegativeInt",
      "cite": "inject_editor_bg.py — `slot_base = a.get('slot_base', slot_cursor)`; `assert slot_base == slot_cursor, 'bands must pack contiguously from slot 0'`"
    },
    "phases": {
      "required": true,
      "kind": "phaseBanks",
      "cite": "inject_editor_bg.py — `for bank in a['phases']: assert len(bank) == n`, and validate_band_coherence's `phases[0] == tiles[base:base+n]`"
    }
  },

  "invariants": {
    "bandCeiling": "len(anims) <= BGANIM_MAX_BANDS",
    "rotationUnitPowerOfTwo": "The band's ROTATION UNIT in bytes must be a power of two — rows * TILE_BYTES on a horizontal band, cols * TILE_BYTES on a vertical one (aeon `band_axis_geometry`: `unit_bytes = a[_AXIS_UNIT_TILES[axis]] * 32`, `unit_shift = unit_bytes.bit_length() - 1`, `assert (1 << unit_shift) == unit_bytes`). The rule keeps its SHAPE across the axes — the runtime rotates a whole unit by SHIFTING — and changes only which band key it is spelled on. Was `columnBytesPowerOfTwo`, which stated the horizontal reading as if it were the only one.",
    "patternPeriod": "pattern_px == the period ALONG THE AXIS: cols * TILE_WIDTH_PX on a horizontal band, rows * TILE_WIDTH_PX on a vertical one (aeon `band_axis_geometry` returns `period_tiles * 8`; `main()` asserts `pattern_px == period_px`). Was `patternWidth`, which is the horizontal name for it.",
    "contiguousPacking": "bands pack contiguously from slot 0 in list order; slot_base, when present, must equal the running cursor",
    "insideTheBlob": "slot_base + cols*rows <= len(tiles) for every band",
    "prefixIdentity": "phases[0] == tiles[slot_base : slot_base + cols*rows] — the band's rest state IS the static tiles it covers. This is the invariant that makes anims/tiles/layout ONE unit rather than three separable keys: a violation bakes CLEANLY and ships silently corrupt art, and every other assert in the consumer still passes (aeon docs/BUGS.md TOOL-01; tools/bg_override_io.py's docstring; aeon tools/test_bg_emit.py::TestBgAnimBandCoherence).",
    "capacity": "len(tiles) <= BG_TILE_CAPACITY. Animated slots are a PREFIX of tiles, NOT an addition — they cost no capacity beyond the array that already holds them (empyrean §5.1, which corrects an earlier `tiles + animated slots <= BG_TILE_CAPACITY` rule that would have told Aurora the flagship act cannot carry any band at all). THE CEILING IS A DECLARED ALLOCATION AND IT HAS ALREADY MOVED ONCE (448 -> 400 at aeon EFFECTS-W1 item 9d, when 48 slots were reassigned to `waterline_strips`). Never restate the number in prose or a test: read `constants.BG_TILE_CAPACITY.value`, which is what every consumer in this repo does.",
    "layoutTileIndex": "layout[row*64 + col] is a nametable word whose low LAYOUT_TILE_INDEX_MASK bits are a blob-local index into `tiles` and whose remaining bits are attributes the consumer preserves — EXCEPT that a word of exactly 0 is passed through unrebased and renders VRAM tile 0 (blank) rather than tiles[0]. Adding or removing a band renumbers `tiles`, so every nonzero word must be rewritten INDEX-ONLY, and a rewrite that would produce the value 0 is unrepresentable rather than merely different.",
    "slotOrder": "WRITER OBLIGATION 1, and the consumer CANNOT CHECK IT. Band cell (c, r) is slot `slot_base + c*rows + r` on a HORIZONTAL band (column-major) and `slot_base + r*cols + c` on a VERTICAL one (ROW-major). It lives in `layout` — which cell references which slot — and the same set of slots reads as a scroll under one order and as a shimmer under the other. aeon: 'a band's slots are deduped against the static blob and appear at many cells — so nothing checks this'. Any Aurora assertion over the SET, the COUNT or a CHECKSUM of a band's slots is vacuous here by construction; only the ORDER discriminates.",
    "phaseAxis": "WRITER OBLIGATION 2. The eight phase banks must be translations ALONG THE DECLARED AXIS. aeon refuses exactly one case and admits everything else: a vertical band whose phases are exact HORIZONTAL translations of phase 0 and are not also vertical ones (`validate_band_phase_axis`) — the reachable accident of a horizontal-only shift-fill run over a band declared vertical. Composites are deliberately admitted on both axes, because the shipped horizontal bands are composites rather than pure rolls.",
    "axisRoundTrip": "WRITER OBLIGATION 3. `axis` must survive a load / edit-something-else / save. Dropping it silently reverts the band to horizontal, and obligation 2's guard cannot see a band that no longer claims to be vertical. Absent means \"horizontal\", and Aurora never writes the default in — a save of an untouched document is not a diff."
  },

  "amendments": [
    {
      "id": "axis",
      "repo": "aeon",
      "commit": "3a4712faa920100653669c1ec3fc26c2da71ef68",
      "read": "2026-09-03",
      "what": "THE MOTION AXIS (aeon EFFECTS-W1 DoD item 8, merged as chain 210). Adds `bandKeys.axis`; RENAMES invariant `columnBytesPowerOfTwo` -> `rotationUnitPowerOfTwo` and `patternWidth` -> `patternPeriod`, because both rules now read off a DIFFERENT band key per axis; and adds the three writer obligations the consumer explicitly cannot check (`slotOrder`, `phaseAxis`, `axisRoundTrip`). No engine byte moved: the vertical arm reuses the same whole-unit DMA rotate, so the power-of-two rule keeps its SHAPE and changes only which key it is spelled on.",
      "documents": [
        "tools/inject_editor_bg.py — THE CONSUMER: `BAND_AXES = ('horizontal', 'vertical')`, `_AXIS_UNIT_TILES = {'horizontal': 'rows', 'vertical': 'cols'}`, `_AXIS_PERIOD_TILES = {'horizontal': 'cols', 'vertical': 'rows'}`, `band_axis_geometry()` (the single derivation of col_shift and pattern period) and `validate_band_phase_axis()` (the one refusal)",
        "tools/EFFECTS_CONSUMER_CONTRACT.md §1.2 — the `axis` row and the block '`axis` — three writer obligations the consumer CANNOT check (added 2026-09-02)'",
        "tools/test_bg_emit.py::TestBgAnimMotionAxis — the aeon-side gate on all of it"
      ],
      "notALL": "SCOPE: this amendment re-derived the AXIS half only. Two values in this file were read at the 2026-08-22 commit and were NOT re-vendored here, and one of them has MOVED IN SHAPE at this revision — see `outputDir.note`. SUPERSEDED BY THE 2026-09-06 amendment below, which re-read EVERY constant."
    },
    {
      "id": "bg-capacity",
      "repo": "aeon",
      "commit": "f14b21a89fd755b011419a2a30822f06032a8dbe",
      "read": "2026-09-06",
      "what": "THE BG TILE CEILING SHRANK, 448 -> 400 (aeon EFFECTS-W1 item 9d, the Hydrocity waterline's ART half). `bg_region` in games/sonic4/vram.toml went `tiles = 448` -> `400` and `band_reserve = 128` -> `80` in the same edit, so the STATIC importer budget (tiles - reserve) is unchanged at 320 and the shipped background blob did not move; the 48 slots became the `waterline_strips` region at base 1424. Aurora's validator refuses only `tiles.length > BG_TILE_CAPACITY`, so for as long as this file said 448 Aurora ACCEPTED documents of 401..448 tiles that aeon's `assert len(tiles) <= BG_TILE_CAPACITY` then refused: the author was told it fits and the build said no.",
      "documents": [
        "games/sonic4/vram.toml — the `bg_region` and `waterline_strips` region blocks, and the comment in each recording where the 48 came from",
        "tools/vram_map.py — the GENERATED block: `BG_TILE_CAPACITY = 400`, `BG_BAND_RESERVE = 80`, `BG_STATIC_TILE_BUDGET = 320`, `REGIONS['bg_region'] = {'base': 1024, 'tiles': 400}`",
        "engine/system/constants.emp — `pub const BG_TILE_CAPACITY = 400` and the `VRAM_WATERLINE_STRIPS` derivation + `ensure()` directly below it",
        "tools/inject_editor_bg.py — THE CONSUMER, unchanged in shape: it still imports the constant from vram_map and asserts `len(tiles) <= BG_TILE_CAPACITY`"
      ],
      "scope": "FULL SWEEP, unlike the `axis` amendment above. Every constant, every driver, every top-level and band key and every invariant in this file was re-read at this revision, not only the one that moved. ONE VALUE MOVED (BG_TILE_CAPACITY). Ten citations were repaired without their values moving — six stale line numbers (vram_map.py:26 -> :34 and :25 -> :33, constants.emp:212 -> :312, ram.emp:526 -> :697) and two quoted source lines the 2026-09-02 axis amendment had rewritten in aeon but not here (`col_bytes = rows * 32` -> `unit_bytes = unit_tiles * 32`; `assert pattern_px == cols * 8` -> `assert pattern_px == period_px`). `outputDir` was re-verified and still holds.",
      "lesson": "THE STALE CITATION AND THE STALE VALUE ARRIVED TOGETHER AND ONLY THE VALUE WAS EVER GOING TO BE NOTICED. `tools/vram_map.py:26 — BG_TILE_CAPACITY = 448` was wrong in the number AND in the coordinate, and nothing in this repo could see either: the drift gate hashes OUR copy, which equals itself by construction. That gap is now closed on the value side by test/formats/bg-override-contract-currency.test.ts, which reads aeon's authorities at origin/master and skips LOUDLY when aeon is absent. It is NOT closed on the coordinate side — a line number is still only as good as the last person who looked."
    }
  ]
}
