{
  "$what": "Provenance of ojz_act1.rows.json — the AEON HALF OF THE SHARED GOLDEN, rows side. The exact Region rows ojz_act1.regions.json beside it must flatten to, in the ENGINE'S INCLUSIVE form (x0/x1/y0/y1), plus the act's own size. test/formats/regions-seam.test.ts asserts Aurora's TypeScript flattening reproduces them row for row on every field after its own exclusive-to-inclusive conversion; aeon's tools/test_regions_doc.py asserts its Python load_act_regions + flatten reproduces the same rows. Spec §5.4 (empyrean docs/superpowers/specs/2026-09-14-aurora-regions-editor-design.md).",
  "$why_this_file_is_not_circular": "Its own `_provenance.typed_by_hand_from` says it: the ten rows were TRANSCRIBED BY HAND out of games/sonic4/data/levels/ojz/act1/act_descriptor.emp's OJZ_ACT1_REGION_ROWS call sites and the constants they name (OJZ_NIGHT_X0 = 3400, OJZ_NIGHT_X1 = 4799, OJZ_SEC2_X1 = 6143 in release), NOT produced by the flattener they are used to test. That is the entire value of the file. Generating it from tools/region_flatten.py would make every assertion against it circular, and generating it from Aurora's flatten.ts would make it circular on this side too — so a future re-vendor must keep the hand transcription and must NOT be repaired into agreement with either flattener. A disagreement is a FINDING about which side is wrong, and that outranks a green.",
  "$why_vendored_and_not_read_at_a_revision": "Same as the document beside it: a golden read out of a sibling checkout at test time either vanishes on a lone clone or silently measures a peer's uncommitted edits. Extracted through git OBJECTS at a named revision and re-hashed here; the currency row in test/formats/aeon-fixture-currency.test.ts pays for the staleness.",
  "$the_eleventh_row_is_not_ours_to_have": "`_provenance.release_shape_only` records a DEBUG shape carrying an ELEVENTH row (OJZ_E2_SNAP_ROWS, a look fixture at x 5600..6143 y 0..2047 binding OJZ_Preset_NightSnap) which also shortens `sec2` to x1 = 5599 to make room. A regions document HAS NO WAY TO SAY `DEBUG` — the contract schema is closed and carries no shape key, and the hub ruled on 2026-09-16 (empyrean 39b8405) that it stays closed — so these ten rows are the RELEASE table and the DEBUG delta is aeon's open item, booked in its docs/DEFERRED_WORK.md under REGIONS-GOLDEN-GAP. A disagreement between this table and a DEBUG ROM is therefore NOT a flattener defect on either side, and nothing in Aurora should be changed to chase one.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "path": "tools/fixtures/regions/ojz_act1.rows.json",
    "branch_that_answers_currency": "origin/master",
    "revision": "a977fa64656cd0e1c3f2624a3cff26f37eda58cd",
    "revision_subject": "id(REGIONS-EMIT phase 1): row 9 becomes `ojz_preset_night`, and the count of four is confirmed; aeon committed 2026-09-16T05:56:20-04:00",
    "revision_is": "THE PIN OF RECORD IS THE LAST COMMIT THAT TOUCHED THE PATH. `git -C <aeon> log --oneline origin/master -- tools/fixtures/regions/ojz_act1.rows.json` now names THREE commits — a977fa64 (newest), 5713201 and 9772326 — and a977fa64 is the same commit the document beside it is pinned at, deliberately, because the two ARE the golden and a pair stitched from two revisions certifies nothing. Re-derived per path with `log -- <path>` at 2026-09-16T10:5xZ after a fetch, never taken from the dispatch: the brief's cross-check named the same SHA and agreeing with it is not the same as having measured it. a977fa64 has ONE parent (`git log -1 --format=%P`), so it is not a merge — the miss this file records twice below.",
    "blob": "f5f8c37fdbd935095ffbe88d4e20c1e6b2e40dcd",
    "resolved_by": "git -C <AEON_DIR> show a977fa64656cd0e1c3f2624a3cff26f37eda58cd:tools/fixtures/regions/ojz_act1.rows.json, extracted by `scripts/revendor.mjs` — never retyped, never reformatted — and RE-HASHED on this side before anything was believed about it: `git hash-object` over the vendored bytes returns f5f8c37fdbd935095ffbe88d4e20c1e6b2e40dcd, the SAME object id aeon stores, resolved independently with `git rev-parse <rev>:<path>`. AEON_DIR appears only in interactive shell commands and in no committed file. ⚠ THE REDIRECT IS THE DANGEROUS STEP UNDER zsh: an UNBRACED `$REV:tools/...` applies the `:c` parameter modifier and eats a character, so `git show` fails AFTER `>` has already truncated the target and the re-hash then answers the EMPTY BLOB e69de29bb2d1d6434b8b29ae775ad8c2e48c5391 for every file at once. The 2026-09-16 re-vendors went through `scripts/revendor.mjs`, which never builds that string in a shell at all and re-hashes the extracted bytes against `rev-parse <rev>:<path>` before touching the sidecar.",
    "revision_published": "a977fa64 is an ancestor of aeon origin/master (3794c99bfc8ce0e612df2285576ad274880630a6 after `git fetch origin` at 2026-09-16T10:5xZ), checked with `git merge-base --is-ancestor` (exit 0), and origin/master:tools/fixtures/regions/ojz_act1.rows.json resolves to the SAME blob f5f8c37f, so the pin is current at tip and not merely correctly cited.",
    "dispatch_named_a_different_revision": "TWICE NOW, AND THE SECOND TIME IS THE INSTRUCTIVE ONE. The first vendoring's brief cited aeon e3b21e7267fa0590fdf43fbede068a98fc681c16, which resolved this path to the blob of the day only because it did not touch the path. The 2026-09-16 bg re-vendor was handed 0c1b11cbd2d558b935410dd639e809fc2922fb87, and that one is worse than a bystander commit: it is the MERGE that brought 5713201 to origin/master, so `git diff 9332c1af 0c1b11cb -- tools/fixtures/regions/` (the diff against its FIRST PARENT) shows both files changed and it looks like the introducing commit. It is not. A merge that resolves a blob is the weaker citation on the same grounds a tip SHA is: it dates the repository's history, not the change. Re-derive the pin from `git log <path>` every time; do not take it from the message that told you the fixture moved."
  },
  "fixture": {
    "path": "test/fixtures/regions/ojz_act1.rows.json",
    "bytes": 7955,
    "sha256": "89f93bff6e8e2bd942f8650f8147aa5b6e24a3df23ad35e1fdffd957e5962a7e",
    "git_blob": "f5f8c37fdbd935095ffbe88d4e20c1e6b2e40dcd",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object aeon stores, which is what test/formats/aeon-fixture-currency.test.ts recomputes from these bytes on every run"
  },
  "what_this_pin_carries": "TEN ROWS, counted from the vendored bytes, each carrying exactly `index`, `id`, `x0`, `x1`, `y0`, `y1`, `preset`, `sceneRef`, `rasterRef`, `bg` — aeon's ROW_KEYS, the shared vocabulary, a ten-element tuple since 5713201. `name` is in the DOCUMENT and NOT in a row: the engine never reads an author's label, so Aurora's RegionRow does not carry one either and the seam test compares whole row objects rather than a projection, which is what makes an extra key on our side go red. Three rows carry a non-null `sceneRef` and two a non-null `rasterRef`; the remaining bindings are EXPLICIT nulls rather than absent keys, which is why flatten.ts normalises `undefined` to `null` and why the seam test asserts the null-valued keys are present. `act_w` and `act_h` are 6144 x 6144 and come from the act's .emp descriptor (ACT_W/ACT_H) — the document carries no act size at all, so Aurora's flatten takes them as an argument; the seam test derives 6144 independently from the document's own covered extent rather than copying the number out of this file.",
  "what_the_bg_re_vendor_added_and_what_it_cannot_witness": "ALL TEN ROWS GREW A `bg` OBJECT and every one of them is `{\"layoutRef\": null, \"span\": null}` — counted from the vendored bytes, not taken on the dispatch's word. What that pins is THE COLLAPSE, and only the collapse: the contract gives ONE engine fact (`Region.rg_bg_layout = 0`, `rg_bg_span = 0`) three legal document spellings, and the document beside this file now ships all three — `bg` absent on eight regions, `bg.layoutRef` explicitly null on `sec3`, and the sentinel `\"@act\"` on `sec0` — so a flattener that carried `\"@act\"` through verbatim, or that read an absent `bg` differently from an explicit null, goes red on this table. ⚠ WHAT IT CANNOT WITNESS, named rather than implied away: NO ROW HERE CARRIES A NON-DEFAULT BACKGROUND, so a flattener that ignored `bg` entirely and hard-coded `{null, null}` would ALSO be green against these ten rows. The discriminating evidence is not in this file at all — it is in the DOCUMENT beside it, whose `\"@act\"` and explicit-null spellings are the only thing separating the collapse from a hard-coded null, which is why the seam test drives the collapse off the document's own spellings and refuses to be vacuous if a re-vendor ever removes them. A NAMED layout is unwitnessable on either side today: aeon's tools/effects_gen.py `_check_region_bg` REFUSES every `bg.layoutRef` but `\"@act\"` and null (nothing lowers the document into a region table; the layout library carries no HEIGHT, so the derived-span rule has nothing to derive from; `rg_bg_span` has no engine reader until part 2 step 4), so `span` is null on every row and the derived-span half of the contract is UNTESTED ON BOTH SIDES. Booked in aeon's docs/DEFERRED_WORK.md under REGIONS-BG-GOLDEN-GAP, with a tripwire in aeon's tools/test_regions_doc.py that fails the day the refusal lifts without this file growing the non-default row it then owes.",
  "pin_history_current_last": [
    "b8ca9cd4, 9772326, the first vendoring (2026-09-16), landed with the document beside it at the same revision.",
    "c5d31823, 5713201, the `bg` re-vendor (2026-09-16), landed with the document beside it at the same revision. All ten rows gained `bg: {layoutRef: null, span: null}`; NOTHING ELSE MOVED — the ten rectangles, ids, presets, sceneRefs and rasterRefs are byte-identical in meaning to the previous pin, and the night straddle is intact. Aurora's src/core/formats/regions/flatten.ts grew the matching field in the same parcel; the two halves could not be separated, because the currency gate goes green on a re-vendor alone while the seam would go red.",
    "f5f8c37f, a977fa64, the key-less row's id (2026-09-16), landed with the document beside it at the same revision. ONE ROW MOVED AND ONE PROSE KEY ARRIVED: row 9's `id` went `night` -> `ojz_preset_night`, and `_provenance.row_9_id_is_RULED_not_transcribed` was added to say why. Diffed BY ID and not by index: the ten ids are `sec0`..`sec8` plus row 9's, rows 0-8 are unchanged in every field, and no rectangle, preset, sceneRef, rasterRef or `bg` moved anywhere in the file. The night straddle is intact (x0 3400, x1 4799, neither on a 2048 multiple) — re-checked by hand on the vendored bytes, since a grid-aligned re-vendor is green everywhere except the one row that asserts it. Aurora's flatten.ts needed NO change: the id is carried through, not computed."
  ],
  "re_vendor": "git -C <aeon> fetch origin && git -C <aeon> show <the commit that last touched the path>:tools/fixtures/regions/ojz_act1.rows.json > test/fixtures/regions/ojz_act1.rows.json, then update `aeon.revision`, `aeon.blob`, `fixture.bytes`, `fixture.sha256`, `fixture.git_blob` and `pin_history_current_last` here, re-vendor the DOCUMENT at the same revision, and re-run test/formats/. Diff the rows BY ID and never by index. If the new rows disagree with what Aurora's flatten produces, do NOT adjust flatten.ts toward the table: work out which side is wrong and report it.",
  "row_9_id_is_RULED_and_MINTED_BY_AURORA": "⚠ ROW 9's `id` IS `ojz_preset_night` BY RULING, AND THE RULE THAT PRODUCES IT LIVES ON THIS SIDE. Empyrean `docs/AURORA_REGIONS_SCHEMA.md` at `origin/main`, \"A KEY-LESS ROW'S ID IS ITS PRESET SYMBOL, LOWERCASED - AND AEON'S `night` MOVES\" (2026-09-16T09:39Z, OVERTURNABLE BY ONE WORD). Rows 0-8 are KEYED to sections and keep `sec0`..`sec8`; row 9 is KEY-LESS, and `night` was an English word the aeon lane typed into a fixture with nothing deriving it. The file's own `_provenance.row_9_id_is_RULED_not_transcribed` says the same in aeon's words and adds the half that matters here: AEON DOES NOT IMPLEMENT THE RULE. The minting is Aurora's migration's (`src/core/editing/migrate-sections.ts`, `idFromPreset`), written ONCE and never re-derived, and aeon's `tools/test_regions_doc.py` derives its expectation from this file's own `preset` field instead of implementing a second copy. So this row is the one place the two spellings are compared, and `test/formats/regions-migrate-act1.test.ts` is where the migration's own minting is checked against it."
}
