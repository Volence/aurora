{
  "$what": "Provenance of ojz_act1.regions.json — the AEON HALF OF THE SHARED GOLDEN, document side. It is one act's regions document in the contract shape aurora-regions.schema.json describes, published by aeon precisely so both repos can flatten it and compare. test/formats/regions-seam.test.ts parses these bytes with Aurora's own parseRegionsDocument and flattens them, and asserts the result reproduces the rows in ojz_act1.rows.json beside it.",
  "$why_vendored_and_not_read_at_a_revision": "The same answer regions-vectors.provenance.json gives: read through `git show` AT TEST TIME the golden would vanish on a machine with no aeon beside it, and a seam test that skips when it cannot find its fixture is a seam that certifies by being absent. Vendored bytes run from a lone clone; the currency row in test/formats/aeon-fixture-currency.test.ts is what pays for the staleness. The extraction was still through git OBJECTS at a named revision, never through the sibling working tree — aeon's session was mid-parcel on the day this was vendored, so its working tree held uncommitted edits.",
  "$why_this_document_is_the_one_worth_having": "It is not a shape sample. Its region `night` straddles the section line at x = 4096 with NEITHER edge on a multiple of the 2048 section size (x 3400..4799 exclusive-form x = 3400, w = 1400), and the two regions whose spans are its complement, `sec1` (w = 1352) and `sec2` (w = 1344), are off the grid because of it. A document whose regions all sat on the section grid could not tell a correct flattener from one that quietly snapped to sections. DO NOT TIDY IT: a re-vendor that lands a grid-aligned document keeps every row in the seam test green and silently stops exercising the thing the fixture exists for. The rows file's own `_provenance.night_is_the_point` says the same in aeon's words, and regions-seam.test.ts asserts the straddle as a property of the fixture so the loss would go red.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "path": "tools/fixtures/regions/ojz_act1.regions.json",
    "branch_that_answers_currency": "origin/master",
    "revision": "571320172be3f3e3ae3e85b8b89c052ec83eb832",
    "revision_subject": "golden: the bg half the seam CAN check, and the three reasons it cannot check the other one; aeon committed 2026-09-16T05:06:34-04:00",
    "revision_is": "THE PIN OF RECORD IS THE LAST COMMIT THAT TOUCHED THE PATH, not a tip and not a merge. `git -C <aeon> log --oneline origin/master -- tools/fixtures/regions/ojz_act1.regions.json` names exactly two commits, 5713201 (newest) and 9772326 (the first vendoring). A tip SHA would satisfy the blob check and is the weaker citation, because it dates the repository rather than the change — the warning aurora-regions.schema.provenance.json records, applied here at both vendorings.",
    "blob": "d45a5d46f55ca22a5f446739ee3e9a914764ef9e",
    "resolved_by": "git -C <AEON_DIR> show 571320172be3f3e3ae3e85b8b89c052ec83eb832:tools/fixtures/regions/ojz_act1.regions.json, REDIRECTED straight into test/fixtures/regions/ojz_act1.regions.json — never retyped, never reformatted, never hand edited — and RE-HASHED on this side before anything was believed about it: `git hash-object` over the vendored bytes returns d45a5d46f55ca22a5f446739ee3e9a914764ef9e, the SAME object id aeon stores, resolved independently with `git rev-parse <rev>:<path>`. AEON_DIR appears only in interactive shell commands and in no committed file. ⚠ BRACE THE REVISION VARIABLE: under zsh an unbraced `$REV:tools/...` applies the `:c` modifier, `git show` fails after `>` has truncated the target, and the re-hash answers the empty blob e69de29b for every file at once. The 2026-09-16 re-vendor extracted to a scratch file, hashed there, and only then copied into place.",
    "revision_published": "5713201 is an ancestor of aeon origin/master (0c1b11cbd2d558b935410dd639e809fc2922fb87 after `git fetch origin` at 2026-09-16T05:27:36-04:00), checked with `git merge-base --is-ancestor` (exit 0). origin/master:tools/fixtures/regions/ojz_act1.regions.json resolves to the SAME blob d45a5d46, so this pin is current at tip and not merely correctly cited; exactly one commit separates 5713201 from the tip and it is the merge that brought it.",
    "dispatch_named_a_different_revision": "TWICE NOW. The first vendoring's brief cited aeon e3b21e7267fa0590fdf43fbede068a98fc681c16, a bystander that resolved this path to the blob of the day only because it does not touch the path. The 2026-09-16 bg re-vendor was handed 0c1b11cbd2d558b935410dd639e809fc2922fb87, which is the MERGE COMMIT whose SECOND PARENT is 5713201 — and a merge is the harder miss, because its diff against its first parent (`git diff 9332c1af 0c1b11cb -- tools/fixtures/regions/`) shows both golden files changed, so it reads exactly like the introducing commit. It is not one. Re-derive the pin from `git log <path>` every time; a revision arriving in prose is a claim, not a measurement."
  },
  "fixture": {
    "path": "test/fixtures/regions/ojz_act1.regions.json",
    "bytes": 1938,
    "sha256": "cb8fb27ef7600f4bdc1a5ea80d2167db35147c461e8b347b590241e76c45250b",
    "git_blob": "d45a5d46f55ca22a5f446739ee3e9a914764ef9e",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object aeon stores, which is what test/formats/aeon-fixture-currency.test.ts recomputes from these bytes on every run"
  },
  "what_this_pin_carries": "THE FIRST VENDORING, so there is no diff to read; what is worth recording is what arrived and what our side made of it. TEN REGIONS, counted from the vendored bytes rather than taken on the dispatch's word, with keys `schema`, `act`, `regions` at the root and `id`, `name`, `preset`, `rect` plus optional `sceneRef`/`rasterRef` on a region. AURORA'S CLOSED SCHEMA ACCEPTED IT UNCHANGED: every key aeon's golden carries is a key aurora-regions.schema.json declares, so `unevaluatedProperties: false` refused nothing and no byte of the golden was massaged to make it parse. That is a FINDING in the parcel's own terms and it is recorded here because a silent pass reads the same as a check nobody ran. The document carries NO act size: `act_w`/`act_h` in the rows file come from the act's `.emp` descriptor (ACT_W/ACT_H), which is why flatten.ts takes bounds as an argument and never derives them from what it is checking.",
  "this_document_is_now_the_DISCRIMINATING_half_of_the_golden": "⚠ READ THIS BEFORE TIDYING ANY `bg` KEY OUT OF THIS FILE. At the 5713201 pin two of the ten regions grew a `bg` block — `sec0` carries `{\"layoutRef\": \"@act\"}` and `sec3` carries `{\"layoutRef\": null}` — while the other eight carry no `bg` at all. That 2-of-10 is not sample data: it is the ONLY evidence in the whole golden that separates a correct flattener from one that hard-codes nulls. The rows file beside this one is `{layoutRef: null, span: null}` on ALL TEN rows, because all three spellings ARE one engine fact (`Region.rg_bg_layout = 0`), so nothing in the rows file can tell a collapse from a constant. Delete the `\"@act\"` on `sec0` and the seam keeps comparing `bg` field-for-field, stays green, and stops proving the sentinel is collapsed; delete the explicit null on `sec3` and it stops proving an explicit null and an absent key agree. regions-seam.test.ts asserts BOTH spellings are present in these bytes and fails loudly if either goes, precisely so this cannot happen quietly. Aeon's tools/test_regions_doc.py carries the same anti-vacuity assertion on its side; the pair is deliberate.",
  "pin_history_current_last": [
    "f3e19125, 9772326, the first vendoring (2026-09-16), landed with the rows file beside it at the same revision.",
    "d45a5d46, 5713201, the `bg` re-vendor (2026-09-16), landed with the rows file beside it at the same revision. Two lines added and nothing else moved: `bg: {\"layoutRef\": \"@act\"}` on `sec0` and `bg: {\"layoutRef\": null}` on `sec3`. No rectangle, id, preset or ref changed, and the night straddle (x 3400, w 1400) is intact — re-checked by hand on the vendored bytes, since a grid-aligned re-vendor is green everywhere except the one row that asserts it. AURORA'S CLOSED SCHEMA ACCEPTED THE NEW KEY UNCHANGED: `bg` was already declared (and `bg.layoutRef`'s pattern widened at empyrean b1ba5204), so `unevaluatedProperties: false` refused nothing."
  ],
  "re_vendor": "git -C <aeon> fetch origin && git -C <aeon> show <the commit that last touched the path, from `git log origin/master -- tools/fixtures/regions/ojz_act1.regions.json`>:tools/fixtures/regions/ojz_act1.regions.json > test/fixtures/regions/ojz_act1.regions.json, then update `aeon.revision`, `aeon.blob`, `fixture.bytes`, `fixture.sha256`, `fixture.git_blob` and `pin_history_current_last` here, RE-VENDOR THE ROWS FILE AT THE SAME REVISION (the pair is the golden and a stitched pair certifies nothing), and re-run test/formats/. Diff the regions BY ID and never by index, and re-check the night straddle by hand: a grid-aligned re-vendor is green everywhere except the one row that asserts the straddle."
}
