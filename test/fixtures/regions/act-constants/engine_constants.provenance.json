{
  "$what": "Provenance of engine_constants.emp — aeon's engine/system/constants.emp, vendored VERBATIM at a named aeon revision. It is the OTHER HALF of rule 4's constant chain: the act descriptor derives REGION_MIN_SPAN and the four CENTRE_* bounds, and the leaves those derivations name (SCREEN_WIDTH, SCREEN_HEIGHT, CAM_SCREEN_HALF_W, CAM_SCREEN_HALF_H, CAM_MAX_Y_STEP) are declared HERE and nowhere in the act file.",
  "$why_the_file_is_renamed": "The vendored basename is engine_constants.emp, not constants.emp. `aeon.path` carries the real path and is what the currency sweep reads; the local name says which constants these are, in a fixtures directory that already holds an act descriptor. The bytes are untouched.",
  "$why_the_whole_86_kb": "Same reason as the descriptor beside it: a trimmed constants file is a hand-written one. The five leaves this reader needs sit among hundreds of other declarations, several of which are derived from each other (SCREEN_TILES_W = SCREEN_WIDTH >> 3) and many of which are written in aeon's `$XXXX` hex. A reader that only works on a 20-line excerpt has not been tested on the file it will actually be handed.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "path": "engine/system/constants.emp",
    "branch_that_answers_currency": "origin/master",
    "revision": "787a9980a474a344f25d96750b0b5dc5a0967c11",
    "revision_is": "THE PIN OF RECORD IS THE LAST COMMIT THAT TOUCHED THE PATH: `git log --oneline c838a30f -- engine/system/constants.emp` names 787a9980 as the newest. Measured 2026-09-25 with aeon origin/master = c838a30f5b4166af90f791da5b0512ce9494ecff (after `git fetch -q origin`), the one revision this re-vendor pinned for all four stale aeon fixtures. The blob is the same at both. act_descriptor.emp is unchanged at c838a30f (blob cbf001fb, its own pin 4802c664), so the pair still agrees.",
    "blob": "faa16c4e7a61e1748a91d1d3bee9459efc0916ca",
    "last_changed_by": "787a9980 perf(page-cache): bounded-direct patch regime for a streaming act's first block (adds PAGECACHE_DIRECT_RESIDENT = $FF and PAGECACHE_DIRECT_BOUNDED = $01 with their comment block; 11 lines added, nothing else changed)",
    "resolved_by": "scripts/revendor.mjs <fixture> <this sidecar> <AEON_DIR> <path> 787a9980 — bytes from `git show <rev>:<path>`, re-hashed on this side with `git hash-object` and compared to `git rev-parse <rev>:<path>` before the sidecar was written. Never the sibling working tree.",
    "revision_published": "787a9980 is an ancestor of aeon origin/master (c838a30f), checked with `git merge-base --is-ancestor` (exit 0); test/formats/aeon-fixture-currency.test.ts re-asserts reachability and byte equality at the tip on every run."
  },
  "fixture": {
    "path": "test/fixtures/regions/act-constants/engine_constants.emp",
    "bytes": 86999,
    "sha256": "d3997e138fa8d466f0944a10d38e514b8aab2aa4f21d277eed13659d3cd26ff9",
    "git_blob": "faa16c4e7a61e1748a91d1d3bee9459efc0916ca",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object aeon stores"
  },
  "paired_with": {
    "fixture": "test/fixtures/regions/act-constants/act_descriptor.emp",
    "why": "The descriptor holds the derivations, this file holds their leaves. Neither half resolves a rule-4 constant alone."
  },
  "module_line_is_load_bearing": "This file declares `module engine.constants`, and the descriptor imports its leaves with `use engine.constants.{…}`. Aurora's reader checks those two agree before it trusts a value read out of this file, so a day when aeon moves these constants to another module fails loudly instead of resolving a name out of whatever file Aurora happened to open.",
  "re_vendor": "node scripts/revendor.mjs test/fixtures/regions/act-constants/engine_constants.emp test/fixtures/regions/act-constants/engine_constants.provenance.json <AEON_DIR> engine/system/constants.emp <the commit that last touched the path, from `git log origin/master -- <path>`> — then update `last_changed_by` and `revision_is` here, re-vendor act_descriptor.emp at a revision that agrees, and re-run the full suite."
}
