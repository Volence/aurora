{
  "$what": "Provenance of act_descriptor.emp — aeon's OJZ act 1 level descriptor, vendored VERBATIM at a named aeon revision so that Aurora's rule-4 constant reader is exercised against aeon's real text and not against a hand-written sample. The five per-row `ensure` predicates inside `ojz_region(...)` are rule 4 of the regions contract, and the constants they name (REGION_MIN_SPAN, CENTRE_X_MIN/MAX, CENTRE_Y_MIN/MAX) are DERIVED here, in a chain whose leaves live in engine/system/constants.emp — vendored beside this file as engine_constants.emp at a revision that agrees.",
  "$why_the_whole_file_and_not_the_20_lines_that_matter": "A trimmed fixture is a hand-written one, and it would certify the reader against a text no build ever sees. The whole file carries what actually makes this parsing hard: `const` inside prose comments, `if DEBUG == 1 { … } else { … }` initializers, two different widths (ACT_W and OJZ_AUTHORED_ACT_W), and names declared far from where they are used. Reading it whole is the only way a green row means the reader survives aeon's bytes.",
  "$not_the_same_file_as": "test/fixtures/effects/raster-owners/section-mode/act_descriptor.emp, which is an older trimmed copy with no provenance sidecar, kept for the raster-owners rows. That one is NOT pinned and must not be used to answer a question about aeon's current text.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "path": "games/sonic4/data/levels/ojz/act1/act_descriptor.emp",
    "branch_that_answers_currency": "origin/master",
    "revision": "4802c664a479f0eb3589eb99eb83694119bff5f6",
    "revision_is": "THE PIN OF RECORD IS THE LAST COMMIT THAT TOUCHED THE PATH: `git log -1 origin/master -- games/sonic4/data/levels/ojz/act1/act_descriptor.emp` names 4802c664 (a single-parent commit, checked with `rev-list --parents`). Measured 2026-09-25 by the aurora overseer after a fetch, with aeon origin/master = 40c891f57128d18e977fe2131be7d813656acda8.",
    "blob": "cbf001fb57ccbf5c996fe64a31643e1a2beba888",
    "last_changed_by": "4802c664 S2 row 7: the clip act's bytes move to entity_data.emp; effects_gen's drift gate was right (the series from 58ee5dbf: a clip act owns its region table)",
    "resolved_by": "scripts/revendor.mjs <fixture> <this sidecar> ../aeon <path> 4802c664a479f0eb3589eb99eb83694119bff5f6 — bytes from `git show <rev>:<path>`, re-hashed on this side and compared to aeon's blob before the sidecar was written. Never the sibling working tree.",
    "revision_published": "4802c664 is reachable from aeon origin/master (40c891f57128d18e977fe2131be7d813656acda8); test/formats/aeon-fixture-currency.test.ts re-asserts reachability and byte equality at the tip on every run.",
    "what_changed_at_this_pin": "`act_regions` now binds through `ojz_clip_act_regions(hand: OJZ_Act1_Regions)` and `act_region_count` through a new `OJZ_ACT_REGION_COUNT` (an `if OJZ_CLIP_ACT == 1` initializer); a new `use games.sonic4.ojz_clip_act_act1.{...}` import and a clip-act table walk were appended. None of the five rule-4 constants (REGION_MIN_SPAN, CENTRE_X_MIN/MAX, CENTRE_Y_MIN/MAX) or their derivations changed in this diff. engine_constants.emp's pin 7d4d0ce6 is still the last commit touching engine/system/constants.emp, so the pair agrees."
  },
  "fixture": {
    "path": "test/fixtures/regions/act-constants/act_descriptor.emp",
    "bytes": 80546,
    "sha256": "d786cb2c6e9927212fb7091296745448714f7ea2c1dde6f0ac7b257fe3f3eb94",
    "git_blob": "cbf001fb57ccbf5c996fe64a31643e1a2beba888",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object aeon stores"
  },
  "paired_with": {
    "fixture": "test/fixtures/regions/act-constants/engine_constants.emp",
    "why": "The chain LEAVES this file. CENTRE_X_MAX is written here as `ACT_W - SCREEN_WIDTH + CAM_SCREEN_HALF_W`, and SCREEN_WIDTH and CAM_SCREEN_HALF_W are not declared here at all: line 96 of this file is `use engine.constants.{SCREEN_WIDTH, SCREEN_HEIGHT, CAM_SCREEN_HALF_W, CAM_SCREEN_HALF_H, CAM_MAX_Y_STEP}`. A test that vendored only this half could not resolve a single one of the five rule-4 constants, so the pair is vendored together and re-vendored together.",
    "what_a_mismatched_pair_would_certify": "Nothing. A derivation read from one aeon commit evaluated over leaves read from another is a number no build ever computed."
  },
  "re_vendor": "node scripts/revendor.mjs test/fixtures/regions/act-constants/act_descriptor.emp test/fixtures/regions/act-constants/act_descriptor.provenance.json <AEON_DIR> games/sonic4/data/levels/ojz/act1/act_descriptor.emp <the commit that last touched the path, from `git log origin/master -- <path>`> — then update `last_changed_by` and `revision_is` here, re-vendor engine_constants.emp at a revision that agrees, and re-run the full suite. ⚠ In zsh, brace the revision variable (`${REV}:<path>`) or the `:c` parameter modifier eats a character AND the `>` redirect has already truncated the fixture."
}
