{
  "$what": "Provenance of regions-vectors.json, empyrean's accept/reject DOCUMENT VECTORS for contract/schema/aurora-regions.schema.json, vendored beside the test that runs them. test/formats/regions-vectors.test.ts reads `empyrean.blob` from here as its pin of record and runs every case through parseRegionsDocument: each `expect: pass` document must parse and survive a serialize with nothing dropped, each `expect: fail` document must be refused BY THE SCHEMA and for the pointer the case's own reason names.",
  "$why_a_sidecar": "The same shape as src/core/formats/regions/aurora-regions.schema.provenance.json, for the same reason: a pin recorded as prose in a test header goes stale without anything going red, because nothing hashes a comment. The hash lives HERE, once, and the test reads it.",
  "$why_vendored_and_not_read_at_a_revision": "Read through `git show` AT TEST TIME the vectors would vanish on a machine with no empyrean beside it, and a conformance suite that skips when it cannot find its cases is a suite that certifies by being absent. Vendored bytes run from a lone clone; the currency block in the test is what pays for the staleness, the way effects-preset-vectors.test.ts pays for its own. The extraction was still through git objects at a named revision, never through the sibling working tree.",
  "$what_the_vectors_are_not": "A GOLDEN. Aeon has not built the generator half of regions part 2, so no generated artifact exists to compare against and nothing in this repo claims one does. These are the CONTRACT's own accept/reject cases for the document SHAPE, published by the hub with the schema. They are also not existence vectors: the vectors' own $comment lists what is deliberately absent, and it is the same list the schema's description gives. That `preset` names a real record, that `bg.layoutRef` names a real layout, that `bg.span` equals the referenced layout's height, that rectangles lie inside the act or tile it without gaps, and that the union of referenced layouts fits BG_TILE_CAPACITY are all outside a shape schema.",
  "empyrean": {
    "repo": "git@github.com:Volence/empyrean.git",
    "path": "contract/schema/tests/regions-vectors.json",
    "revision": "b1ba5204d6cbb4ceaffdd110ab8acb902f2962a4",
    "revision_subject": "a pattern over a foreign namespace legislated a shape none of its members ever had; the `bg.layoutRef` pattern widened from ^(@act|[a-z][a-z0-9_]{0,31})$ to ^(@act|[a-z][a-z0-9_-]{0,127})$, with 39 lines of reason added to empyrean docs/AURORA_REGIONS_SCHEMA.md and nothing else in the commit; empyrean committed 2026-09-16T05:11:46-04:00",
    "blob": "cf599c8acc5cababd57455d8da8480eb35054db4",
    "resolved_by": "git -C <EMPYREAN_DIR> show b1ba5204d6cbb4ceaffdd110ab8acb902f2962a4:contract/schema/tests/regions-vectors.json, read through git OBJECTS, never through the sibling working tree. Extracted by REDIRECTING `git show` straight into test/fixtures/regions/regions-vectors.json, never retyped and never hand edited, and RE-HASHED on this side before anything was believed about it: `git hash-object` on the vendored bytes returns cf599c8acc5cababd57455d8da8480eb35054db4, the same object id at this revision, at the previous pin c3f892f and at origin/main, each resolved independently with `git rev-parse <rev>:<path>`. THE BYTES DID NOT MOVE AT THIS RE-PIN — the extraction was re-run anyway rather than asserted, and `git diff` names only this sidecar and not the fixture. EMPYREAN_DIR came from test/support/sibling-root.mjs, RUN in this worktree; the absolute path appears only in interactive shell commands and in no committed file.",
    "revision_published": "b1ba5204 is an ancestor of empyrean origin/main (a5bfe45e42c2cc4c1f3af8954d4bec149373dfd6 after `git fetch origin`), checked with `git merge-base --is-ancestor` (exit 0), and origin/main:contract/schema/tests/regions-vectors.json resolves to the SAME blob cf599c8a, so this pin is current at tip and not merely correctly cited. b1ba5204 is NOT the tip (one commit back) AND IT DOES NOT TOUCH THIS PATH: `git log origin/main -- contract/schema/tests/regions-vectors.json` still names c3f892f as the only commit that has ever touched this file. Currency here is therefore the blob equality at origin/main plus that log, never a tip identity and never a claim that this commit published these bytes. ⚠ THE TIP SHA ABOVE IS AN OBSERVATION AT A MOMENT, NOT A CLAIM THAT SURVIVES: empyrean origin/main moved THREE TIMES during the hour this re-pin took (4aee3797 -> 19c89126 -> a5bfe45e, the middle one read off a red run's own failure message), so a reader who compares that SHA to today's will find it stale and learn nothing from the difference. The two claims that DO survive, and that a reader should RE-RUN rather than trust: b1ba5204 is an ancestor of whatever origin/main is now (`git merge-base --is-ancestor`, exit 0 at each of the three tips), and origin/main resolves the path to the pinned blob (checked at each of the three). `git log origin/main -- <path>` still names b1ba5204 as the newest commit to touch contract/schema/aurora-regions.schema.json, which is the pin and the thing a moving tip cannot change.",
    "branch_that_answers_currency": "origin/main"
  },
  "vendored": {
    "path": "test/fixtures/regions/regions-vectors.json",
    "bytes": 6232,
    "git_blob": "cf599c8acc5cababd57455d8da8480eb35054db4",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object empyrean stores, which is what test/formats/regions-vectors.test.ts recomputes from these bytes on every run"
  },
  "schema_these_vectors_belong_to": {
    "sidecar": "src/core/formats/regions/aurora-regions.schema.provenance.json",
    "same_revision": "Both sidecars cite ONE empyrean revision, and they are re-pinned together. A re-pin of one without the other is a mismatch that would surface as the schema refusing a vector the contract says it accepts, or accepting one it says to refuse, and it is asserted directly by a row in regions-vectors.test.ts rather than left to be noticed. ⚠ WHAT THE 2026-09-16 RE-PIN LEARNED ABOUT THAT RULE, recorded because it is a real tension and not a tidy one: b1ba5204 amends the SCHEMA and does not touch this path, so the two conventions in these sidecars disagree — \"pin the commit that last touched the path\" would keep this file at c3f892f, and \"both cite one revision\" moves it. THE SECOND WINS, for three reasons, and the pin here is therefore A REVISION AT WHICH THESE BYTES ARE THE PUBLISHED ONES rather than the revision that published them: (1) this field and the equality row are the SPECIFIC rule written for exactly this moment, while \"the last commit to touch the path\" is the generic extraction recipe; (2) the drift gate's own remediation text ends \"re-vendor the VECTORS at the same revision\", meaning the schema's new one; (3) the weaker citation is contained, because `empyrean.revision_published` here states in full that this commit does not touch this path and that `git log origin/main -- <path>` still names c3f892f as the only one that ever has. The load-bearing pin is the BLOB either way, and it did not move."
  },
  "what_this_pin_carries": "A RE-PIN THAT MOVED NO BYTES. The ten cases are the ones the previous entry counted and they are untouched: 3 EXPECTED-PASS and 7 EXPECTED-FAIL, blob cf599c8a at c3f892f, at b1ba5204 and at origin/main alike. The three accepts are the minimal region (identity by rectangle plus a preset, no background), the off-grid night region carrying name, rasterRef, sceneRef and a bg with a derived span, and the `@act` sentinel on layoutRef. The seven refusals are effectsRef on a region (ruling Q8 keeps the key reserved), a region with no preset, a per-section binding key, the anchor triple authored on the region as v_offset, a zero-width rectangle, an empty regions array, and an unknown key nested inside bg. THE LAST ONE IS THE ONE THAT EARNS THE SET: it is the only case that proves the document is closed at a level OTHER than the root, which is where a key nobody agreed to would actually reach a consumer. WHY THE SCHEMA AMENDMENT AT b1ba5204 FLIPS NO CASE HERE, checked case by case rather than inferred from the commit touching one path: only three cases mention layoutRef at all, and their values are `ojz_forest` and `@act`, both legal under the old pattern and the new. The widening ACCEPTS MORE, so it could only turn an expected-FAIL green, and no expected-FAIL case here is a layoutRef SPELLING case — the one bg refusal is an unknown KEY nested inside bg, which closure refuses and no pattern governs.",
  "pin_history_current_last": [
    "cf599c8a, c3f892f, the first vendoring (2026-09-16), landed with the schema at the same revision.",
    "cf599c8a, b1ba520, a re-pin that moved no bytes (2026-09-16). The blob is unchanged from the first vendoring; only `revision` moved, to stay equal to the schema sidecar's after empyrean widened `bg.layoutRef`'s pattern. The case set was diffed BY NAME and is identical, ten cases, none added and none deleted."
  ],
  "re_vendor": "git -C <empyrean> fetch origin && git -C <empyrean> show <the commit that last touched the path>:contract/schema/tests/regions-vectors.json > test/fixtures/regions/regions-vectors.json, then update `empyrean.revision`, `empyrean.blob`, `vendored.bytes`, `vendored.git_blob` and `pin_history_current_last` here, re-vendor the SCHEMA at the same revision, and re-run the full suite. Diff the case set BY NAME and never by index: the effects vectors' history has a landing that DELETED seven cases while the count grew, so a growing total is not evidence that the existing rows are untouched. ⚠ TRAP MET DOING EXACTLY THIS, 2026-09-16: in zsh, `$REV:contract/...` applies the `:c` PARAMETER MODIFIER and eats the \"c\", so `git show` fails with \"ambiguous argument\" — but the `>` redirect has ALREADY TRUNCATED the vendored file to zero bytes, and the re-hash then answers e69de29b (the empty blob) for every file at once. Brace the variable (`${REV}:<path>`), and re-hash before believing anything."
}
