{
  "$what": "Provenance of validate-json.cases.json: an OUTPUT of aeon's own tools/clip_manifest.py, captured from a real run, vendored for the donor page's row-213 readers (ROADMAP row 213, docs/reviews/2026-09-25-donor-clip-asks.md). Not a committed aeon file, so it is pinned by the revision and the command that produced it.",
  "$why": "the unit rows read aeon's answers, not numbers typed by hand: the per-clip rows, their field list (the file's own pool.per_clip_fields) and every refusal/crash shape are aeon's. The opt-in fidelity rig (test/live/donor-fidelity.test.ts) re-runs the same tools at aeon origin/master and holds the vendored per_clip_fields to the live bake's, so a change in aeon's contract reds there.",
  "aeon": {
    "repo": "git@github.com:Volence/aeon.git",
    "branch_that_answers_currency": "origin/master",
    "revision": "a0c63764eac113f5da35b01f40d0d6b5933a2cb1",
    "revision_is": "aeon origin/master tip, read 2026-09-25 after the fetch. tools/clip_act_bake.py and tools/clip_manifest.py were last changed at e6daa4dbcec19dc776523ac995cad861f33e500e (git log -1 a0c63764 -- <both>), inside the landing 1d9afb25; a0c63764 itself changes only docs.",
    "re_measure": "when tool_path's blob at aeon origin/master differs from tool_blob, materialise that revision as below, re-run fixture.command, re-vendor the fixture and update its sha256, tool_blob and revision here (and in the sibling markers of test/fixtures/clips/aeon-outputs/)",
    "materialised_by": "git -C <aeon> archive a0c63764 | tar -x into a scratch directory; EMPYREAN_SUITE_ROOT=<suite> python3 tools/s2_zone_convert.py convert --all-six there (exit 0, 6 zones, 0 differing)",
    "tool_path": "tools/clip_manifest.py",
    "tool_blob": "1ca2675af61e992bae4f00f24bb2eba39ce3dfcd"
  },
  "fixture": {
    "path": "test/fixtures/clips/aeon-outputs/validate-json.cases.json",
    "command": "EMPYREAN_SUITE_ROOT=<suite> python3 test/fixtures/clips/aeon-outputs/gen_validate_json.py <scratch>/vj2 <this file>, run from the materialised copy: nine real subprocesses of `python3 tools/clip_manifest.py validate <path> --donor-root <copy>/games/sonic4/data/donors --json`, each {exit, stdout, stderr} verbatim",
    "why_these": "an acceptance, an acceptance with a W3 naming two clips, R7 (one clip), R10 clip/clip and clip/corridor (pair rules), R12 after a W2 (a warning kept under a refusal), the untagged top-level refusal (rule null, no subjects), and the two CRASHES aeon documents (not JSON; a missing path): exit 1, empty stdout, a traceback",
    "sha256": "3fdd4f133472e6c5f0217286d30911f282656daa565d36c6b3786601d46d7291"
  },
  "generator": {
    "path": "test/fixtures/clips/aeon-outputs/gen_validate_json.py",
    "what": "the generator fixture.command runs; the mutations are aeon's own from tools/test_clip_manifest_json.py at 1d9afb25",
    "sha256": "879776f3f662b9b530e7683cb79b0a5272455d4749866a1fe7920b622753cc5b"
  }
}
