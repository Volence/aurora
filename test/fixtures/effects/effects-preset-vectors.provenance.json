{
  "$what": "Provenance of effects-preset-vectors.json — empyrean's accept/reject DOCUMENT VECTORS for contract/schema/aurora-effects-preset.schema.json, vendored beside the tests that run them. test/formats/effects-preset-vectors.test.ts reads `empyrean.blob` from here as its pin of record and runs every case through parseEffectsPreset: each `expect: pass` document must parse and survive a save with nothing dropped, each `expect: fail` document must be refused by the SCHEMA (not by the filename/id rule, which the vectors do not exercise).",
  "$why_a_sidecar": "The same shape as src/core/formats/effects/aurora-effects-preset.schema.provenance.json, for the same reason: a pin recorded as prose in a test header goes stale without anything going red, because nothing hashes a comment. The hash lives HERE, once, and the test reads it.",
  "$what_the_vectors_are_not": "Value vectors. The schema is normative for SHAPE only (empyrean AURORA_EFFECTS_SCHEMA.md section 7.1), so a legal-shaped document carrying an out-of-range period or a shift of 9 PASSES here and is refused by the engine's own ensure with the measurement behind it. Two generator refusals the vectors' own $comment names as inexpressible here: cycles: [] and a variants array longer than PAL_MAX_VARIANTS.",
  "empyrean": {
    "repo": "git@github.com:Volence/empyrean.git",
    "path": "contract/schema/tests/effects-preset-vectors.json",
    "revision": "d36d70417b580e5b5134f772bc85a6fc3474d295",
    "revision_subject": "effects schema: the preset document gains patch_world_ys and patch_motion (DoD item 4 authoring key, per aeon key-shape artifact 81b2a719; AURORA_EFFECTS_SCHEMA.md 7.3)",
    "blob": "cc3f0f96cf572b1731e0a21ad3e9188b2bfd8958",
    "resolved_by": "git -C ../empyrean show d36d704:contract/schema/tests/effects-preset-vectors.json — read through git OBJECTS, never through the sibling working tree. THE BYTES DID NOT MOVE: this file is blob cc3f0f96 at 12aecd5 and at d36d704 alike, so nothing was re-extracted on 2026-09-03 and the vendored copy is untouched. Only the revision this sidecar CITES advanced, so that it and the schema's sidecar name one revision.",
    "revision_published": "d36d704 is an ancestor of empyrean origin/main (4dcd520e1d3989e19bfdd3c3f3a38181e6e6910b at re-pinning, after `git fetch origin`), checked with `git merge-base --is-ancestor`, and origin/main:contract/schema/tests/effects-preset-vectors.json resolves to the SAME blob cc3f0f96 — current at tip, not merely correctly cited.",
    "branch_that_answers_currency": "origin/main"
  },
  "vendored": {
    "path": "test/fixtures/effects/effects-preset-vectors.json",
    "bytes": 6035,
    "git_blob": "cc3f0f96cf572b1731e0a21ad3e9188b2bfd8958",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object empyrean stores, which is what effects-preset-vectors.test.ts recomputes from these bytes on every run"
  },
  "schema_these_vectors_belong_to": {
    "sidecar": "src/core/formats/effects/aurora-effects-preset.schema.provenance.json",
    "same_revision": "Both sidecars cite ONE empyrean revision, and they are re-pinned together. 12aecd5 (EW-REVENDOR-PRESET) vendored both files; d36d704 (EW-CHANNELS-WRITER) moved the SCHEMA's bytes and left these alone, so this sidecar's revision advanced with the schema's while its blob stayed cc3f0f96. A re-pin of one without the other is a mismatch the vectors test would report as the schema refusing a vector the contract says it accepts, or vice versa.",
    "no_published_vectors_for_the_2026_09_03_keys": "d36d704 published no vector rows for patch_world_ys / patch_motion — empyrean's §7.3 records a sixteen-vector probe it ran in its OWN repo but did not commit into contract/schema/tests/. So the accept/reject rows for the two new keys in Aurora are AURORA'S OWN, written from the schema and §7.3, and live in test/formats/effects-preset.test.ts rather than here. If empyrean later publishes them, they arrive through this file's re_vendor command and supersede nothing: Aurora's rows and the contract's would then both run."
  },
  "pin_history_current_last": [
    "cc3f0f96 — 12aecd5, landing (the vectors' first revision; byte-identical at origin/main 63c85aeb)",
    "cc3f0f96 — d36d704, revision advanced 2026-09-03 with the schema's re-vendor; the BLOB DID NOT CHANGE (no vectors were published for patch_world_ys / patch_motion), byte-identical at origin/main 4dcd520e"
  ],
  "re_vendor": "git -C <empyrean> fetch origin && git -C <empyrean> show origin/main:contract/schema/tests/effects-preset-vectors.json > test/fixtures/effects/effects-preset-vectors.json — then update `empyrean.revision`, `empyrean.blob`, `vendored.bytes`, `vendored.git_blob` and `pin_history_current_last` here, re-vendor the SCHEMA at the same revision, and re-run the full suite."
}
