{
  "$what": "Provenance of effects-preset-vectors.json \u2014 empyrean's accept/reject DOCUMENT VECTORS for contract/schema/aurora-effects-preset.schema.json, vendored beside the tests that run them. test/formats/effects-preset-vectors.test.ts reads `empyrean.blob` from here as its pin of record and runs every case through parseEffectsPreset: each `expect: pass` document must parse and survive a save with nothing dropped, each `expect: fail` document must be refused by the SCHEMA (not by the filename/id rule, which the vectors do not exercise).",
  "$why_a_sidecar": "The same shape as src/core/formats/effects/aurora-effects-preset.schema.provenance.json, for the same reason: a pin recorded as prose in a test header goes stale without anything going red, because nothing hashes a comment. The hash lives HERE, once, and the test reads it.",
  "$what_the_vectors_are_not": "Value vectors. The schema is normative for SHAPE only (empyrean AURORA_EFFECTS_SCHEMA.md section 7.1), so a legal-shaped document carrying an out-of-range period or a shift of 9 PASSES here and is refused by the engine's own ensure with the measurement behind it. Two generator refusals the vectors' own $comment names as inexpressible here: cycles: [] and a variants array longer than PAL_MAX_VARIANTS.",
  "empyrean": {
    "repo": "git@github.com:Volence/empyrean.git",
    "path": "contract/schema/tests/effects-preset-vectors.json",
    "revision": "d299279bf9fc7f7d50fc7974035a5ad38fdd8261",
    "revision_subject": "effects preset schema: the order-rule parenthetical said sort; document order, no sort (aurora's finding); bank 2026-09-04T23:28:10Z",
    "blob": "87af55c98dfb8f873ca631d431e192c24b02aa56",
    "resolved_by": "git -C <EMPYREAN_DIR> show c4a1da23fd17f2e343b152e275283193ef0d46ab:contract/schema/tests/effects-preset-vectors.json \u2014 read through git OBJECTS, never through the sibling working tree. \u26a0 THIS TIME THERE WAS SOMETHING TO EXTRACT, and that is the break from the four landings below: the blob really moved, af5b5cee -> 6dff7676. Extracted by redirecting `git show` straight into test/fixtures/effects/effects-preset-vectors.json, never retyped, and RE-HASHED with `git hash-object` on the vendored bytes before anything believed it (6dff76769e39186f0b4a332d990b297bd7a83794, the same id at the pinned revision and at origin/main, each resolved independently with `git rev-parse <rev>:<path>`). `git log --follow` over the path now names c4a1da2 as its LAST touching commit with 5bd76ba the one before \u2014 which is the reading that matters, because it proves the four description-only amendments in between really did leave the file alone rather than moving it and moving it back. EMPYREAN_DIR: `test/support/sibling-root.mjs` requireSiblingPath('empyrean') was RUN in this worktree and its answer used; the absolute path appears only in interactive shell commands. No absolute sibling path entered a committed executable file.",
    "revision_published": "c4a1da2 is an ancestor of empyrean origin/main (a8a115bcec85a98dbbdf356b6b51f0f65aa6e92a at re-vendoring, after `git fetch origin`), checked with `git merge-base --is-ancestor` (exit 0), and origin/main:contract/schema/tests/effects-preset-vectors.json resolves to the SAME blob 6dff7676 \u2014 current at tip, not merely correctly cited.",
    "branch_that_answers_currency": "origin/main"
  },
  "vendored": {
    "path": "test/fixtures/effects/effects-preset-vectors.json",
    "bytes": 31351,
    "git_blob": "87af55c98dfb8f873ca631d431e192c24b02aa56",
    "identical_to_source": "byte-for-byte; the git blob id above is the SAME object empyrean stores, which is what effects-preset-vectors.test.ts recomputes from these bytes on every run"
  },
  "schema_these_vectors_belong_to": {
    "sidecar": "src/core/formats/effects/aurora-effects-preset.schema.provenance.json",
    "same_revision": "Both sidecars cite ONE empyrean revision, and they are re-pinned together. 12aecd5 (EW-REVENDOR-PRESET) vendored both files; d36d704 (EW-CHANNELS-WRITER) moved the SCHEMA's bytes and left these alone, so this sidecar's revision advanced with the schema's while its blob stayed cc3f0f96. A re-pin of one without the other is a mismatch the vectors test would report as the schema refusing a vector the contract says it accepts, or vice versa.",
    "no_published_vectors_for_the_2026_09_03_keys": "TRUE OF d36d704, NOT OF 9233883 OR 5bd76ba, and the difference is the point. d36d704 published no vector rows for patch_world_ys / patch_motion, so Aurora wrote its own in test/formats/effects-preset.test.ts. 9233883 published ELEVEN for `ramp` (6 cases -> 17) and 5bd76ba published SEVEN for `base_swap` (17 -> 24). THE SET IS NOW 24 CASES, 5 EXPECTED-PASS AND 19 EXPECTED-FAIL, counted here from the vendored bytes and agreeing with both the hub's description and the overseer's independent count. The one new PASS is THE DOCUMENT AEON ACTUALLY SHIPS: {schema 1, id ojz_sec6_baseswap, base_swap {line 160, target 57344}}, bound by section_6.meta.json's rasterRef \u2014 so the row that proves an author can open section 6 is the contract's own, not Aurora's. The six new refusals are base_swap+ramp, target 57345 (off the $2000 granule), target 65536, line 2, a missing target, and a `plane` key. They run here through parseEffectsPreset like every other vector.",
    "why_a_description_only_cr_moves_no_vector": "e9409dc changed TWO description strings and nothing else (verified in Aurora by a parsed leaf diff and by a descriptions-stripped comparison; see the schema sidecar's pin history). NO VECTOR CAN NOTICE THAT: a vector asserts that a DOCUMENT is accepted or refused, and prose refuses nothing. So the blob correctly did not move, the case count stays at 24, and this sidecar's revision advances alone \u2014 exactly the d36d704 shape, for a different reason. \u26a0 AND 'no vector moved' IS NOT 'no code moved': the amendment's two sentences are PARSED by preset.ts into constants that reach a painted readout, and one of them changed value from 1 to 2. The vectors were the right thing to leave alone and the wrong place to look for the impact. See docs/reviews/2026-09-03-ew-revendor-boundary.md."
  },
  "pin_history_current_last": [
    "cc3f0f96 \u2014 12aecd5, landing (the vectors' first revision; byte-identical at origin/main 63c85aeb)",
    "cc3f0f96 \u2014 d36d704, revision advanced 2026-09-03 with the schema's re-vendor; the BLOB DID NOT CHANGE (no vectors were published for patch_world_ys / patch_motion), byte-identical at origin/main 4dcd520e",
    "d8a5f358 \u2014 9233883, re-vendored 2026-09-03; THE BLOB CHANGED (11 rows added for `ramp`, 6 cases -> 17). A parsed diff found 142 added leaves, 0 removed and 0 changed in place, so no pre-existing vector moved. Byte-identical at origin/main f9fdfbe1.",
    "af5b5cee \u2014 5bd76ba, re-vendored 2026-09-03; THE BLOB CHANGED AGAIN (7 rows added for `base_swap`, 17 cases -> 24). A parsed leaf diff found 62 added leaves, 0 removed and 0 CHANGED IN PLACE, and the additions are all at cases/17..23 \u2014 so the existing 17 rows are UNTOUCHED and this is an append, which is the fact that makes it a re-vendor rather than a migration. Verified here, not taken on the hub's word. Byte-identical at origin/main 6da51293.",
    "af5b5cee \u2014 e9409dc, revision advanced 2026-09-03 with the schema's re-vendor; THE BLOB DID NOT CHANGE. Hashed both ways before believing it: the blob at e9409dc and at origin/main is af5b5cee, and `git hash-object` on the bytes already vendored here returns the same id, so the file was never rewritten. The amendment is DESCRIPTION-ONLY (two strings) and no vector asserts prose, so 24 cases, 5 pass / 19 fail, unchanged.",
    "af5b5cee \u2014 bfc000e, revision advanced 2026-09-04 with the schema's re-vendor; THE BLOB DID NOT CHANGE, AGAIN. e9409dc's finding was NOT reused: this was re-measured, and with one method the previous landing did not use \u2014 `git log --follow` over the path, which names 5bd76ba as its last touching commit and lists neither of the two amendments since. Same DESCRIPTION-ONLY shape (the SAME TWO description strings moved, at the same two JSON pointers), so 24 cases, 5 pass / 19 fail, unchanged. \u26a0 SKIPPED REVISION, RECORDED SO NOBODY LOOKS FOR IT: d5e0e7a lies between e9409dc and bfc000e and Aurora never pinned it on either sidecar \u2014 bfc000e supersedes it. The vectors' blob is af5b5cee there too, so nothing was missed on this side either."
,
    "af5b5cee \u2014 dfd11bb, revision advanced 2026-09-04 with the schema's re-vendor (EW-9-ROWREMAP-CONTROL step 1a); THE BLOB DID NOT CHANGE, A THIRD TIME. Re-measured rather than inherited, by the same two methods: `git log --follow` names 5bd76ba as the path's last touching commit and lists none of the three amendments since, and the blob is af5b5cee at origin/main and on disk. Same DESCRIPTION-ONLY shape but a DIFFERENT pointer from the two landings above \u2014 this amendment moved exactly ONE description, /properties/patch_motion, where e9409dc and bfc000e both moved /properties/ramp and /$defs/ramp/properties/top. 24 cases, 5 pass / 19 fail, unchanged, because no vector asserts prose.",
"6dff7676 — c4a1da2, re-vendored 2026-09-04 (EW-BOUNDARY-CODEC); THE BLOB CHANGED, breaking a run of four landings that did not. 12 rows added for `boundary`, 24 cases -> 36, and the split goes 5 pass / 19 fail -> 7 EXPECTED-PASS and 29 EXPECTED-FAIL, counted here from the vendored bytes rather than taken on the dispatch's word. A PARSED LEAF DIFF found 193 added, 0 removed and 2 CHANGED IN PLACE — /cases/17/name and /cases/17/why. ⚠ THAT IN-PLACE PAIR IS THE ONE THING TO READ TWICE: it is a RE-PIN OF PROSE, not of a document. Case 17's `doc` is byte-identical (no leaf under /cases/17/doc moved); what changed is the sentence explaining WHERE the shipped section-6 preset's fire line comes from, because aeon moved that binding from 160 (850d4c60) to 3 (8bf6df74). Every one of the 193 additions is at cases/24..35, so the existing 24 rows are untouched and this is an append. The two new PASS rows are the shipped moving water alone (case 24) and the same boundary with patch_world_ys[0] and patch_motion[0] beside it (case 25) — the document that says 'this line follows the world AND sweeps'. The ten refusals: boundary+bands, a null boundary, channel 4, lo 2, hi 224, line 2, pal_region carrying addr, an `on` cram arm, a missing channel, and a `vsplit` key.",
    "87af55c9 — 8f56c2c, re-vendored 2026-09-04 (the base_swap LIST migration); THE BLOB CHANGED, and this is the FIRST re-vendor in this file's history that REMOVED cases rather than appending. 36 cases -> 41, and the split goes 7 pass / 29 fail -> 8 EXPECTED-PASS and 33 EXPECTED-FAIL, counted here from the vendored bytes rather than taken on the hub's word. SEVEN ROWS WERE DELETED and TWELVE ADDED: every one of the seven was a base_swap case built on the single-object {line, target} shape, INCLUDING the old case 17 pass row ('the section-6 preset as bound at aeon 850d4c60, line 160, target $E000'), which the amendment turns into a REFUSAL. — AND THAT IS THE DIFFERENCE FROM EVERY LANDING ABOVE: a case can now be DELETED, so 'the existing rows are untouched' is no longer a safe reading of a growing count, and a future sidecar must diff BY NAME rather than by index. Measured here: the 29 cases the two sets share are byte-identical, with no in-place change at all — unlike c4a1da2, which re-pinned case 17's prose. The two new PASS rows are the exact two-band document aeon binds at 89ba7c5f, and a one-band list with NO restore_line — the shipped single-edge shape, which is what the old object form expressed. The ten new refusals: the old single-object form, an empty list, a missing plane, plane SpriteTable, an off-granule target, a target of 65536, a line of 2, a string restore_line, an undeclared key inside a band, and base_swap beside another raster key.",
    "87af55c9 — d299279, revision pin ONLY, 2026-09-04. THE VECTORS DID NOT MOVE: byte-identical at 8f56c2c and d299279 (blob 87af55c9 at both, compared by git rev-parse, not assumed). d299279 is a description-only fix to $defs/base_swap — the order-derivation parenthetical that had told consumers to SORT the flattened fire sequence. Re-pinned here because the drift gate requires the vectors and the schema at the SAME empyrean revision, and it CAUGHT this: re-vendoring the schema alone took the suite to 1 failed / 7221 passed, naming exactly this row. That gate is the reason a schema can never drift away from the cases that prove it."
  ],
  "re_vendor": "git -C <empyrean> fetch origin && git -C <empyrean> show origin/main:contract/schema/tests/effects-preset-vectors.json > test/fixtures/effects/effects-preset-vectors.json \u2014 then update `empyrean.revision`, `empyrean.blob`, `vendored.bytes`, `vendored.git_blob` and `pin_history_current_last` here, re-vendor the SCHEMA at the same revision, and re-run the full suite."
}
